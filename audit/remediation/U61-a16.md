# Remediation U61 - audit agent-16.md

Unit owns three whole test files. Three audit findings, all FIXED (none deleted).

## Per-finding results

| file:line - test | severity | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- | --- |
| tests/test_core/test_realcov_07a_disassembler.py:196-200 - test_auto_detect_raw_bytes_raises_unsupported | Low | FIXED | Drives the real `detect_format_and_arch` chain on `bytes(range(64))` (no mock). Now asserts the exact exception type `UnsupportedArchitectureError`, the exact descriptive message `"unsupported architecture: 'unknown'"`, the offending `error.arch == "unknown"`, and that it remains a `ValueError` subclass. Added two error-path edge cases: empty buffer `b""` and a truncated `b"MZ"` PE magic - both must raise `UnsupportedArchitectureError(arch="unknown")` rather than silently fall back to x86-64. |
| tests/test_providers/test_local_transformers_provider.py:116-199 - XPU and memory estimation tests | Medium | FIXED | Replaced "reasonable range" checks with exact-byte oracles derived from independently-known facts (model-card param counts: Mistral-7B = 7e9, Phi-3-mini = 3.8e9; IEEE/quant storage: fp16=2, int8=1, int4=0.5, fp32=4 bytes/param; documented 1.3x activation overhead). New tests assert exact byte totals (`9_880_000_000` Phi-3 fp16; `18_200_000_000` Mistral fp16; `14_000_000_000` no-activation; `9_100_000_000` int8; `4_550_000_000` int4; `36_400_000_000` fp32) and exact theoretical ratios (`int8*2 == fp16`, `int4*4 == fp16`, `fp32 == fp16*2`). `select_dtype_for_memory` now asserts the chosen dtype is exactly `int4` for a 3 GiB budget plus the exact estimate. XPU detection tests rewritten from bare `isinstance` into logical-invariant oracles: `is_xpu_available() == (get_xpu_device_count() > 0)` and `is_arc_b580() -> is_xpu_available()`. |
| tests/test_ui/test_realcov_13b_hex_pattern_code_editor.py:128-138 - test_type_names_are_deduplicated_and_sorted | Low | FIXED | Split into two real gates. `test_type_names_are_sorted_alphabetically` now asserts ORDERING ON THE MODEL DIRECTLY: it feeds shuffled `["uz","ua","um","ub","uq"]` (insertion order differs from sorted) and asserts (a) the backing `QStringListModel`'s full string list read row-by-row via the new `model_rows()` accessor equals `["ua","ub","um","uq","uz"]`, and (b) a SINGLE `u` prefix returns `completions_for("u") == ["ua","ub","um","uq","uz"]` WITHOUT the test sorting the result. This removes the prior aggregation-by-alphabetical-loop that laundered the model's order through the test's iteration order. Empirically falsifiable: replacing production `sorted(set(names))` with insertion-order dedup (`list(dict.fromkeys(names))`) turns this test RED (`'uz' != 'ua'` at index 0). `test_type_names_are_deduplicated` feeds five tokens with two distinct values and asserts exactly `["u16","u32"]` with `len == 2`. `test_offers_real_unsigned_types` asserts the fixed known unsigned-width family literal `["u128","u16","u32","u64","u8"]`. All driven through the real `PatternCodeEditor`/`QCompleter` model (no mock). |

## Verification (project env, only touched files)

Files:
- tests/test_core/test_realcov_07a_disassembler.py
- tests/test_providers/test_local_transformers_provider.py
- tests/test_ui/test_realcov_13b_hex_pattern_code_editor.py

| command | result |
| --- | --- |
| `pixi run ruff check <files>` | All checks passed! |
| `pixi run ruff format <files>` then `pixi run ruff format --check <files>` | 3 files already formatted (no changes) |
| `pixi run basedpyright <files>` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint <files>` | No violations |
| `pixi run pydocstyle <files>` | clean (no output) |
| `pixi run pytest <files> -p no:timeout` | 69 passed, 2 warnings in 22.26s |

All six gates genuinely green; no test skipped or xfailed to hide breakage.
