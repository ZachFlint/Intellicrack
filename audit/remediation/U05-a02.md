# Remediation U05-a02 - tests/test_hexcore_e2e/test_hexpat_evaluator.py

Source audit: `audit/agent-02.md` (finding "All 33 test functions", Critical).

## Finding

| file:line | test | status | real input / oracle / exact assertion now backing it |
|---|---|---|---|
| tests/test_hexcore_e2e/test_hexpat_evaluator.py:file-level | All 33 test functions (weak-assertion-on-rich-output: arithmetic/bitwise/control-flow tests asserted only the resolved `@` offset or list membership, never the computed value, so a broken operator went undetected) | FIXED | Whole file rewritten to 43 value-validating gates. Two independent oracles drive every test: (1) a **value-oracle guard** `u8 v = <expr>; if (v == <hand-computed constant>) { u8 correct @ A; } else/if-bug { u8 wrong @ B; }` asserting `_names(results) == ["correct"]` so a wrong computed value takes the bug branch and fails (e.g. `0xF0 \| 0x0F == 0xFF`, `17 % 5 == 2` vs quotient `3`, `3 - 10 == -7` vs `7`/`249`, `2 + 3*4 == 14` vs `20`, `(u8)(0x1FF) == 0xFF` vs `0x1FF`); (2) a **placement oracle** over `bytes(range(256))` (byte[N]==N) asserting both `offset` and the read-back `raw_bytes`/`display_value` (e.g. `u8 @ (1<<3)` -> offset 8, `raw_bytes==[8]`, `display_value=="0x8"`). Error paths assert `HexPatRuntimeError` with the exact message substring for `/0` ("division by zero") and `%0` ("modulo by zero"). Edge/boundary cases added: signed-negative subtraction, no-u8-wrap on add, operator precedence, comparison/logical full truth tables incl. boundary equality, u32 full-width read with exact little-endian bytes `[0x2C,0x01,0x00,0x00]`, u8->u32 promotion, float<->int cast truncation, loop-variable non-leak. |

The audit said 33 functions; the rewrite consolidates and expands them into 43 stronger functions across nine real-gate classes. No test was deleted - every behavior was expressible as a real value gate.

## Falsifiability check

Mutated production `evaluator.py` binary-op handler `op == "-"` from `lv - rv` to `lv + rv`; `test_subtraction_value_is_difference_not_sum` and `test_subtraction_negative_result_is_signed` both went RED (`2 failed`). Reverted the mutation; full file green again and `git status` shows no production diff.

## Verification (all green, only the touched file)

| command | result |
|---|---|
| `pixi run ruff check tests/test_hexcore_e2e/test_hexpat_evaluator.py` | All checks passed! |
| `pixi run ruff format tests/test_hexcore_e2e/test_hexpat_evaluator.py` then `--check` | 1 file already formatted (no changes) |
| `pixi run basedpyright tests/test_hexcore_e2e/test_hexpat_evaluator.py` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint tests/test_hexcore_e2e/test_hexpat_evaluator.py` | No violations |
| `pixi run pydocstyle tests/test_hexcore_e2e/test_hexpat_evaluator.py` | clean (no output) |
| `pixi run pytest tests/test_hexcore_e2e/test_hexpat_evaluator.py -p no:timeout` | 43 passed |
