# U70-a12 Remediation Report

## Findings and Fixes

| # | File:Line - Test | Severity | Status | Fix Summary |
|---|-----------------|----------|--------|-------------|
| 1 | `tests/test_bridges/test_win32_types.py:200-211` - `test_value_matches_expected_bit_pattern` | Low | FIXED | Removed tautological `wintypes.HANDLE(-1).value` re-derivation. Now asserts `INVALID_HANDLE_VALUE == 0xFFFFFFFFFFFFFFFF` (64-bit) or `0xFFFFFFFF` (32-bit) using `ctypes.sizeof(ctypes.c_void_p)` as the independent oracle, which is a different mechanism than the production code uses. |
| 2 | `tests/test_hexcore_e2e/test_bridge_yara_deep.py:303-312` - `test_nonexistent_yar_file_raises_or_returns_error` | Low | FIXED | Narrowed catch-all `Exception` to `_YARA_ERROR_CLASS` (`yara.Error`), the specific exception yara-python raises for missing files (errno 2 / ENOENT). Also asserts the error message encodes "2" or "No such file" to confirm the ENOENT signal propagates faithfully. |
| 3 | `tests/test_providers/test_http_status_helper.py:62-71` - `test_returns_none_for_unmatched_status` | Low | FIXED | Expanded single-code test to `@pytest.mark.parametrize` over 9 unmatched status codes (0, 200, 301, 302, 400, 404, 500, 502, 504). Each parametrized case asserts the exact `None` return value with a descriptive failure message, catching any future change that raises for unrecognised codes. |
| 4 | `tests/test_ui/test_realcov_15_resource_url_dispatch.py:119-143` - `test_resource_button_click_routes_real_url_through_qt` | Low | FIXED | Added pre-click assertions: `btn.isEnabled()`, `btn.text() == label`, and `len(buttons) == len(expected)`. Any button removal, label change, or disabling now fails the gate before clicking. Added widget `show()`/`hide()`/`deleteLater()` for deterministic Qt state. |

## Verification Results

| Check | Result |
|-------|--------|
| `ruff check` | PASS (0 findings) |
| `ruff format --check` | PASS (4 files already formatted) |
| `basedpyright` | PASS (0 errors, 0 warnings) |
| `pydoclint` | PASS (0 violations) |
| `pydocstyle` | PASS (0 findings) |
| `pytest` | PASS (117 passed in 8.73s) |
