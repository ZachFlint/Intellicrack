# U54-a18 Remediation Report

## Findings and Resolutions

| ref | severity | status | oracle |
|-----|----------|--------|--------|
| `test_hex_editor_pe_methods.py:182-195` `test_no_document_raises_runtime_error` | Medium | FIXED | Bridge with `document=None`; `pytest.raises(RuntimeError, match=r'no document')` asserts the exact guard message `"no document open"` from the `_ERR_NO_DOCUMENT` constant, not just the exception type |
| `test_hex_editor_pe_methods.py:92-100` `test_tool_function_exposed` | Low | FIXED | `ToolRegistry.register_bridge(HEX_EDITOR, bridge)` then `execute_tool_call('hex_editor', tool_name, {})` must raise `ToolError` (no document), proving method is present, callable, and reachable via production dispatch path |
| `test_hex_editor_pe_methods.py:102-108` `test_tool_owner_is_hex_editor` | Low | FIXED | `register_bridge` then `get_hex_editor_bridge()` returns the exact registered instance (identity check); `execute_tool_call('hex_editor', 'hex_editor.get_pe_sections', {})` raises `ToolError` proving the dispatch path is live |
| `test_realcov_02b_named_pipe_real.py:243-267` `test_real_send_command_round_trip` | Medium | FIXED | `@pytest.mark.timeout(30)` added as outer determinism guard; inner `asyncio.wait_for(timeout=8.0)` still asserts `response['echo_command']=='inspect'` and `echo_params` match exact sent dict |
| `test_realcov_02b_named_pipe_real.py:357-373` `test_real_connect_missing_pipe_raises_with_error_code` | Low | FIXED | `connect_timeout` elevated to `5.0s` and `@pytest.mark.timeout(15)` added; asserts exact string `"error 2"` in message (`ERROR_FILE_NOT_FOUND` integer code) and `format_error_hint(2)` non-None hint text both present in `ToolError` |
| `test_openai_format_helpers.py` (file-level: lacks real provider tests) | Medium | FIXED | 5 new tests using `ChatCompletion.model_validate()` and `ChatCompletionChunk.model_validate()` (real SDK Pydantic v2 objects); asserts exact prompt/completion/total token values round-trip through `CompletionUsage` into `UsageInfo`, and zero-total fallback to sum |

## Six-Command Results

- **ruff check**: PASS - 0 findings
- **ruff format**: PASS - 3 files already formatted, no changes
- **basedpyright**: PASS - 0 errors, 0 warnings, 0 notes (venvPath warning is harmless noise per instructions)
- **pydoclint**: PASS - No violations
- **pydocstyle**: PASS - No findings
- **pytest**: PASS - 50 passed, 7 skipped in 6.19s (skips are `spawns_process` marker; require Docker sandbox / `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1`; confirmed passing with env var set)
