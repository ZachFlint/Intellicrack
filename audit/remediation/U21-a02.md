# Remediation U21-a02

Unit owner of:
- `tests/test_ui/log_viewer/test_realcov_15_window_real_logs.py`
- `tests/test_ui/test_hxd_panel.py`

Source report: `audit/agent-02.md`. 5 findings, all accounted for.

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `test_realcov_15_window_real_logs.py:118` - `test_window_level_filter_over_real_records` | FIXED | Renamed to `test_window_level_filter_excludes_lower_levels`. Drives 4 real structlog events (INFO/WARNING/ERROR/CRITICAL) through `setup_logging` to disk, backfills via the real `LogViewerWindow`/tail reader, applies `set_min_level(logging.ERROR)`, then maps every proxy row back to its source record. Replaces the subset check with exact equality: `visible_events == {"real_error_only","real_critical_only"}`, `visible_levels == {"ERROR","CRITICAL"}`, explicit `not in` for the dropped INFO/WARNING events, and `proxy.rowCount() == 2`. Filter application is synchronous (`invalidateFilter`), so the assertion is deterministic after the `waitUntil` backfill gate. Added `test_window_level_filter_relaxation_restores_records` (narrow to ERROR then widen to INFO re-admits the hidden INFO event). |
| `test_hxd_panel.py:23-251` - `TestFindHxdExecutable` / `TestHxDPanelConstruction` (High) | FIXED | Detection now planted against a real `HxD.exe` (a real copy of the interpreter binary) on a controlled `PATH`: asserts `result == planted` and byte-equality. Added negative gates: empty PATH -> `None`, directory named `HxD.exe` rejected (`is_file` branch), non-Windows short-circuit -> `None`. Construction tests assert exact label strings (`"HxD: not found"` / `f"HxD: {path}"`) by driving the real `_update_status_label`, and exact initial idle state. The tautological `test_hxd_exe_matches_finder` was removed in favor of a status-label gate whose oracle is an independent `find_hxd_executable()` call. |
| `test_hxd_panel.py:119-154` - `TestHxDPanelFileLoadingPreconditions` (Medium) | FIXED | Replaced no-op conditional/library tests with real-process gates. `load_file` is driven against a real spawned `QProcess` (interpreter opening a real blocking `.py` target): success returns `True`, `process.state() == Running`, `processId() > 0`, `current_file == target`, label updated. Missing-editor and non-existent-target paths assert exact `False` returns with `process is None`. String-path coercion verified by `isinstance(current_file, Path)` plus a real launch. Pure-library `test_path_conversion` and setup-only `test_hxd_none_blocks_launch` deleted. |
| `test_hxd_panel.py:157-213` - `TestHxDPanelLifecycle` (Medium) | FIXED | Lifecycle tests now start a real child process first, then assert real termination: after `terminate_existing` / `stop_tool` / `cleanup`, the captured `QProcess.state() == NotRunning` and `panel.process is None`. `stop_tool` gate also asserts `tool_closed` emitted exactly once and the label reset. Double-terminate after a real load asserts idempotent teardown of the same captured process. |
| `test_hxd_panel.py:216-251` - `TestHxDPanelToolbar` (Low) | FIXED | Smoke checks (`test_status_label_exists`, `test_hxd_exe_attribute_type`) removed. Label tests assert exact transitions: `"HxD not launched"` -> `f"HxD: {target.name}"` after a real load, and back to `"HxD not launched"` after `stop_tool`. Availability rendering asserted exactly via `_update_status_label` with planted/None exe. |

No production source was modified. The window backfill test surfaced the genuine on-disk logger name (`intellicrack.intellicrack.tests.window_real`, structlog prepends `intellicrack.`); the exact-equality assertion was set to that independently-verified value rather than the masking substring the original used.

## Verification (pixi env, touched files only)

```
pixi run ruff check    tests/test_ui/log_viewer/test_realcov_15_window_real_logs.py tests/test_ui/test_hxd_panel.py   -> All checks passed!
pixi run ruff format --check  <same two files>                                                                       -> 2 files already formatted
pixi run basedpyright  <same two files>                                                                              -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint     <same two files>                                                                              -> No violations
pixi run pydocstyle    <same two files>                                                                              -> (no output; pass)
pixi run pytest        <same two files> -p no:timeout                                                                -> 25 passed, 2 warnings
```

All six commands genuinely green; every test PASSES (none skipped/xfailed to hide breakage on this host).
