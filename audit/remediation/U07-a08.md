# Remediation U07-a08

Unit file owned: `tests/test_core/test_realcov_07b_xml_gen.py`
Source audit: `audit/agent-08.md` (1 finding).

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_core/test_realcov_07b_xml_gen.py:116-120` - `test_special_characters_in_text_are_escaped` (incomplete, no assertion) | FIXED | The whole file was rewritten to drive the real production consumer `WindowsSandbox._generate_wsb_config` (which imports `Element`/`SubElement`/`indent`/`ElementTree` directly from `intellicrack.core.xml_gen`) and write a real `.wsb` file to disk, then re-parse it with hardened `defusedxml`. The escaping concern is covered by `test_special_characters_in_paths_are_escaped_and_recovered`: a `SandboxConfig` shared-folder guest path `C:\Sand "<box>" & Co` is run through the real generator; it asserts the serialized bytes contain `&lt;box&gt;` and `&amp; Co`, contain no raw `<box>`, and that `defusedxml` recovers the exact original unescaped string. Oracle is the independently-known input string and the XML 1.0 escaping spec, not the implementation's own output. |

## What changed and why it is now a real gate

The original file re-built the WSB tree by hand inside the test (an answer-shaped
re-implementation) and the flagged test ended mid-setup with no assertion. The rewrite:

- Drives the actual production code path (`WindowsSandbox._generate_wsb_config`), the only
  first-party consumer of the re-exported `xml_gen` primitives, writing a real `.wsb` file.
- Uses the `SandboxConfig` dataclass inputs and documented Windows Sandbox schema constants
  (`WindowsSandbox.SANDBOX_SHARED_PATH`) as the independent oracle. No frozen implementation output.
- Asserts exact node values and full structure (every scalar setting, both mapped-folder records
  field-by-field, the logon command string), not existence.
- Adds the missing coverage dimensions: boundary input (`memory_limit_mb=0` omits `MemoryInMB`),
  toggle input (all peripherals enabled flip to `Enable`), adversarial input (reserved XML chars in
  paths), and the error path (uninitialized paths raise `SandboxError` with the surfaced message).
- Falsifiability verified: mutating the production `Networking` node text to a constant turns 3 tests
  red; the source was restored byte-identical afterward (no `git diff`).

No mocks, no patches, no skips/xfails. Tests are deterministic (each builds a fresh sandbox over a
pytest `tmp_path`, no shared state, no sleeps).

## Verification (all green)

| Command | Result |
| --- | --- |
| `pixi run ruff check tests/test_core/test_realcov_07b_xml_gen.py` | All checks passed |
| `pixi run ruff format tests/test_core/test_realcov_07b_xml_gen.py` then `--check` | 1 file already formatted (no changes) |
| `pixi run basedpyright tests/test_core/test_realcov_07b_xml_gen.py` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint tests/test_core/test_realcov_07b_xml_gen.py` | No violations |
| `pixi run pydocstyle tests/test_core/test_realcov_07b_xml_gen.py` | No findings |
| `pixi run pytest tests/test_core/test_realcov_07b_xml_gen.py -p no:timeout` | 7 passed in 1.76s |
