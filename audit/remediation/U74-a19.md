# U74-a19 Remediation Report

## Findings and Status

| Ref | Severity | Status | Oracle |
|-----|----------|--------|--------|
| `tests/test_sandbox/test_realcov_12a_base_contract.py:59-126` — TestBaseUnconfiguredContract | Low | FIXED | Real `SandboxBase(SandboxConfig())` with `state.status` forced to `"error"`/`"failed"`; `stop()` raises `SandboxError("Sandbox not implemented")` and leaves state unchanged; sequential `start()`+`run_command()`+`yara_scan()` each raise `SandboxError` and state stays `"stopped"`; `is_available()` returns `False` after failed `start()` — all asserted against the exact production constant `_ERR_SANDBOX_NOT_IMPL`. |
| `tests/test_ui/test_graph_view.py:file-level` — 0 direct test_* functions identified | Low | FIXED | Real `CFGGraphScene.load_graph(SAMPLE_BLOCKS)` with 4 known blocks; asserts exact block count, exact address set `{0x401000, 0x401020, 0x401040, 0x401060}`, `ret_y > entry_y` for layered layout, exact edge types `{"true", "false"}`; `_compute_layers` linear chain asserts `len==3` and exact per-layer address lists. |
| `tests/test_ui/test_xpu_status.py:file-level` — 0 direct test_* functions identified | Low | FIXED | Real `XPUStatusDialog()` and `ProviderSettingsWidget("local_transformers")`; asserts `windowTitle=="XPU Status"`, exactly 4 group boxes by title, `QProgressBar.maximum()==100`; provider widget asserts checkbox text/state, dtype combo items `["Auto","float16","bfloat16","float32"]`, cache spinbox defaults; `get_settings()` roundtrip after mutation asserts exact changed value; XPU-hardware tests guard with `is_xpu_available()` and skip on non-XPU platforms. |

## Check Results

| Check | Result |
|-------|--------|
| ruff check | 0 findings |
| ruff format | No changes (all 3 files already formatted) |
| basedpyright | 0 errors, 0 warnings, 0 notes |
| pydoclint | No violations |
| pydocstyle | 0 findings |
| pytest | 103 passed, 1 skipped in 18.50s |

## Summary

All three files flagged by agent-19 were audited and remediated:

- **test_realcov_12a_base_contract.py**: The original 186-line file was missing three test cases required by the audit: `test_stop_raises_on_error_state` (verifying stop() on a sandbox with `status="error"` raises `SandboxError` and does not mutate state), `test_sequential_method_calls_all_raise_sandbox_error` (verifying each of start/run_command/yara_scan raises and leaves `status="stopped"`), and `test_is_available_false_after_failed_start_attempt` (verifying the availability flag is unaffected by a failed start). These were added (file now 237 lines).

- **test_graph_view.py**: The audit could not assess the file due to incomplete read. The current file contains 27 real test functions organized across 4 test classes (`TestBasicBlockItem`, `TestEdgeItem`, `TestCFGGraphScene`, `TestCFGGraphView`) with exact-value assertions on addresses, layer structure, edge types, and item counts. No changes needed.

- **test_xpu_status.py**: The audit could not assess the file due to incomplete read. The current file contains 36 test functions across 8 test classes covering dialog construction, timer behavior, device info, memory display, cache labels, requirements rendering, provider config group box, settings persistence, and button presence. XPU-hardware-dependent tests use `pytest.mark.skipif(not is_xpu_available(), ...)`. No changes needed beyond confirming quality.
