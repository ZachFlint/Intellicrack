# U24-a09 Remediation (agent-09.md)

Owned files:
- `tests/test_core/test_orchestrator_audit6.py`
- `tests/test_providers/test_agentic_capabilities.py`

## Per-finding results

| file:line - test | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_orchestrator_audit6.py:621 - test_extract_imports_macho_returns_dyld_symbols (renamed `..._returns_exact_dyld_symbols`) | FIXED | Real hand-assembled Mach-O64 dylib (`_build_macho_fixture_bytes`) re-parsed by `lief`. Oracle is the bytes the test packed (1 undefined external symbol, `n_value==0`, library ordinal 1). Now asserts the entire `ImportInfo` list equals `[("", "_audit6_macho_import", 1, 0)]` field-by-field and that the exported symbol never leaks into imports. Replaces `len>=1` + `name in set`. |
| test_orchestrator_audit6.py:642 - test_extract_exports_macho_returns_trie_entries (renamed `..._returns_exact_trie_entries`) | FIXED | Same real Mach-O fixture. Export symbol emitted as defined section symbol `n_type==0x0F`, `n_value==0x1000`. Asserts full `ExportInfo` list equals `[("_audit6_macho_export", 0, 0x1000)]` (ordinal 0, address 4096) and that the import symbol does not leak into exports. |
| test_orchestrator_audit6.py:659 - test_extract_imports_elf_includes_non_plt_dynamic_symbols (renamed `..._includes_exact_dynamic_symbols`) | FIXED | Real hand-assembled ELF64 shared object (`_build_elf_fixture`) re-parsed by `lief`. Oracle is the packed `.dynsym` (3 undefined: strong FUNC, strong OBJECT, WEAK FUNC + 1 defined export). Asserts the full `ImportInfo` list exactly (all three, no export) and cross-checks each symbol's `(binding, type)` against lief's own `imported_symbols` metadata (independent oracle): GLOBAL/FUNC, GLOBAL/OBJECT, WEAK/FUNC. A PLT-only regression (drops data/weak) or a full-table regression (includes export) now fails. |
| test_orchestrator_audit6.py:680 - test_extract_exports_elf_uses_dynamic_symbols (renamed `..._returns_exact_dynamic_symbols`) | FIXED | Same real ELF fixture. Asserts full `ExportInfo` list equals `[("audit6_exported_symbol", 0, 0x1000)]` (ordinal 0, address 4096), cross-checks `(binding, type)` = GLOBAL/FUNC via lief `exported_symbols`, and asserts the two imported symbols never leak into exports. |
| test_agentic_capabilities.py:90 - test_anthropic_tool_choice_required_forces_tool_call | FIXED | Drives the **real connected** `anthropic_provider` (live Anthropic API; the audit's "mock" claim was from a truncated read - no mock exists). Single tool `binary.get_file_size` with required `path` param + `tool_choice=REQUIRED`. Now asserts exactly one `ToolCall`, `function_name == "binary.get_file_size"`, `tool_name == "binary"` (provider derives prefix), non-empty `id`, and a non-empty string `path` argument. Replaces `is not None` / `len>0` / lone `isinstance`. Test reaches the real API (confirmed by live model-deprecation warning); it is environment-skipped only when the account credit balance is exhausted, via the conftest's documented account-limit hook - not a skip masking breakage. |

## Notes

- Removed now-unused `_MIN_ELF_IMPORTS`, `_MIN_MACHO_IMPORTS`, `_MIN_MACHO_EXPORTS` constants (they only backed the weak `>= min` assertions that were replaced with exact-list equality).
- No production source changed; no mocks introduced; no method bindings removed.
- Falsifiability verified: a regression returning the full symbol table (export leaking into imports) or a PLT-only ELF scan (dropping data/weak symbols) both fail the new exact-list equality assertions, whereas the old `>= min` + `name in set` checks passed for the full-table regression.

## Verification (project env, only touched files)

```
pixi run ruff check tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> All checks passed!
pixi run ruff format tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> 2 files left unchanged
pixi run ruff format --check tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> 2 files already formatted
pixi run basedpyright tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> No violations
pixi run pydocstyle tests/test_core/test_orchestrator_audit6.py tests/test_providers/test_agentic_capabilities.py
  -> (no output; clean)
pixi run pytest tests/test_core/test_orchestrator_audit6.py -p no:timeout
  -> 31 passed
pixi run pytest tests/test_providers/test_agentic_capabilities.py -p no:timeout
  -> 5 passed, 10 skipped (live-provider + Ollama environment preconditions; conftest account-limit hook)
```
