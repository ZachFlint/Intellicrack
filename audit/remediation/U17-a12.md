# U17 (agent-12.md) remediation - hex panel selection/dispatch, hexcore bridge, process panel

Unit: U17-a12. All 5 audit findings on the owned files are remediated to real gates.

## Per-finding results

| File:line - test | Status | Real input / independent oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_audit4/c16_hex_panel_selection_dispatch/test_selection_dispatch.py:543-589` - `TestCopyAsClipboardError` | FIXED | Removed the `patch("...panel.QApplication")` / `patch("...show_warning")` doubles. Success path now drives a real `HexEditorWidget` backed by a real `intellicrack_hexcore` document with a real selection (`DE AD BE EF 00 11`, range 0..3) through the production `_do_copy_as`, and asserts the exact string dispatched to the real `QClipboard.setText` equals the documented format (`"DE AD BE EF"` for hex, `"3q2+7w=="` for base64 - oracle = the documented `copy_as_format` spec, computed independently). The two failure paths isolate ONLY the irreproducible OS boundaries (a clipboard the platform cannot provide -> `None`; a `setText` the platform makes raise `RuntimeError`; the OS-modal `QMessageBox.warning`) and assert the real, user-visible side effects: the real structlog `copy_as_no_clipboard` / `copy_as_clipboard_write_failed` JSON-Lines records (with `fmt="hex"`) emitted through the real `setup_logging` pipeline, plus the exact warning title/message ("Clipboard Unavailable" + documented body; "Clipboard Write Failed" + the surfaced exception text). The deleted source-grep smoke test (`test_panel_module_references_show_warning_in_copy_as`) was removed as coverage theater, fully superseded by the behavioural gates above. |
| `tests/test_hexcore_e2e/test_bridge_alignment_color.py:50-66` - `test_snap_to_alignment_512` | FIXED | Added `test_snap_to_nearest_boundary_with_state_oracle`, an 11-case parametrization driving a real `HexEditorBridge` over a real 16 KiB file. Each expected value is a hand-computed nearest boundary (ties resolved upward) per the documented contract - never the implementation output - covering floor, ceil, exact-midpoint ties (256->512, 768->1024, 1536->2048, 4096->8192), alignment of 1, and large alignments. Every case cross-checks against a SECOND independent oracle: the value the bridge persists into a separate `HexDocumentState` (`state.cursor_offset`) plus `get_cursor_position()`. Added `test_snap_rejects_non_positive_alignment` asserting `ValueError` for `0` and `-512` and that the cursor is preserved. Falsifiability verified: a floor-only mutation of the production snap turned 8 of these red. |
| `tests/test_hexcore_e2e/test_bridge_display_modes_complete.py:46-53` - `test_set_hex16_be_returns_true` | FIXED | Rewritten to `test_set_hex16_be_changes_mode_and_propagates_to_state`: attaches an independent `HexDocumentState`, asserts the documented default (`hex8`) and a not-already-`hex16_be` precondition, then after the set asserts `result is True`, `get_display_mode() == "hex16_be"`, AND the independent state-holder oracle `state.get_display_mode() == "hex16_be"`. Added `test_set_invalid_mode_raises_value_error_and_preserves_mode` asserting `ValueError` on an unknown mode and that the previously set mode survives. |
| `tests/test_ui/test_process_panel.py:352-355` - `test_tool_definition_count` | FIXED | Replaced the bare `== 54` count with `test_every_function_is_fully_formed_and_unique` (unique `process.`-scoped names, non-empty description + return docs - a padding stub fails), `test_dispatch_suffix_resolves_to_callable_signature_compatible_coroutine` (each function's production dispatch suffix resolves via the real `getattr` rule to a callable async bound method whose signature accepts every required tool parameter), and `test_list_function_executes_end_to_end_through_real_registry` (registers a live `ProcessBridge` in a real `ToolRegistry` and invokes `process.list`, asserting the live OS oracle: the current `os.getpid()` python image appears exactly once as a `ProcessInfo`). |
| `tests/test_ui/test_process_panel.py:363-373` - `test_function_names_map_to_methods` | FIXED | Superseded by the suffix-resolution + end-to-end tests above, which assert `callable`, `asyncio.iscoroutinefunction`, and signature compatibility (not bare `hasattr`), plus `test_list_detailed_filter_argument_flows_through_registry` (filter argument forwarded through the registry, every returned record matches `python`, current pid present) and `test_unknown_function_raises_tool_error_through_registry` (undeclared name raises `ToolError`, not a silent no-op). |

## Verification (all six commands, owned files only, all green)

Files:
`tests/test_audit4/c16_hex_panel_selection_dispatch/test_selection_dispatch.py`
`tests/test_hexcore_e2e/test_bridge_alignment_color.py`
`tests/test_hexcore_e2e/test_bridge_display_modes_complete.py`
`tests/test_ui/test_process_panel.py`

- `pixi run ruff check <files>` -> All checks passed!
- `pixi run ruff format <files>` -> 4 files left unchanged; `pixi run ruff format --check <files>` -> 4 files already formatted
- `pixi run basedpyright <files>` -> 0 errors, 0 warnings, 0 notes
- `pixi run pydoclint <files>` -> No violations
- `pixi run pydocstyle <files>` -> (no output; clean)
- `pixi run pytest <files> -p no:timeout` -> 92 passed (selection_dispatch 15, alignment_color 21, display_modes_complete 22, process_panel 34); selection_dispatch re-run 3x green for determinism

No production source was modified (a temporary falsifiability mutation in `hex_editor.py` was reverted; `git diff` shows no change).
