# U17-a12 Remediation

Worktree: `D:/ic-wt2/U17-a12` (branch `wf2/U17-a12`, left intact for review/merge).

Four files were already hardened on HEAD (commit f06ab769 "harden test suite"); the
audit findings reference the pre-rewrite line numbers and test names. Each rewritten
test was re-verified against the live production code (`HexEditorPanel._do_copy_as`,
`HexEditorBridge.snap_to_alignment` / `set_display_mode`, `ToolRegistry.execute_tool_call`)
to confirm it is a genuine falsifiable gate. The one finding whose weak named test still
existed literally (`test_snap_to_alignment_512`) was rewritten in this worktree to add a
hand-computed nearest-boundary oracle and an independent `HexDocumentState` cross-check.

## Per-finding results

| # | Ref (file:line - test) | Severity | Status | Real input + independent oracle + exact assertion |
|---|------------------------|----------|--------|---------------------------------------------------|
| 1 | test_selection_dispatch.py:543-589 - TestCopyAsClipboardError | High | FIXED | Real 6-byte hexcore doc `DE AD BE EF 00 11` selected 0..3 driven through production `_do_copy_as` against the real `QClipboard`. Success: dispatched clipboard text == documented `DE AD BE EF` / base64 `3q2+7w==` (independent encoding oracle). Failure: only OS boundaries isolated (clipboard `None` / `setText` `RuntimeError` + OS-modal `QMessageBox.warning`); asserts real structlog `copy_as_no_clipboard` / `copy_as_clipboard_write_failed` records and the exact production warning title+message. Dropping the production `show_warning` calls makes the recorded warnings empty -> RED. |
| 2 | test_bridge_alignment_color.py:50-66 - test_snap_to_alignment_512 | Medium | FIXED | Real `HexEditorBridge` over a 4096-byte file, cursor 1000, `snap_to_alignment(512)`. Nearest boundary hand-computed in-test (488 vs 24 -> 1024, never copied from impl); `result`, `get_cursor_position()`, and independent `HexDocumentState.cursor_offset` all asserted == 1024. Floor-only / alignment-ignoring break yields 512 -> RED. Companion parametrized test covers floor/ceil/tie-up; `test_snap_rejects_non_positive_alignment` asserts `ValueError("alignment_size must be positive")` with cursor unchanged. |
| 3 | test_bridge_display_modes_complete.py:46-53 - test_set_hex16_be_returns_true | Medium | FIXED | Rewritten as `test_set_hex16_be_changes_mode_and_propagates_to_state`: asserts documented `hex8` default precondition on the independent `HexDocumentState` oracle, `set_display_mode("hex16_be")` returns True, fresh `get_display_mode()` read-back == `hex16_be`, and `state.get_display_mode()` == `hex16_be`. `test_set_invalid_mode_...` asserts `ValueError("unknown display mode")` leaves the prior mode intact. A True-returning no-op set -> RED. |
| 4 | test_process_panel.py:352-355 - test_tool_definition_count | Low | FIXED | Bare count replaced by uniqueness + non-empty name/description/returns gate, the production-suffix coroutine/signature resolution gate, and an end-to-end `process.list` through a real `ToolRegistry` returning `ProcessInfo` records that include the live `os.getpid()` python test process exactly once. A dummy stub fails the empty-doc/coroutine/signature gates. |
| 5 | test_process_panel.py:363-373 - test_function_names_map_to_methods | Low | FIXED | `hasattr`-only loop replaced by callable + `asyncio.iscoroutinefunction` + required-parameter signature compatibility for every advertised function, a filter-argument flow test (`filter_name='python'` forwarded through the real registry), and `test_unknown_function_raises_tool_error_through_registry` asserting `ToolError` via the real dispatcher. Non-callable/raising-property/signature-mismatch -> RED. |

## Six-command verification (cwd `D:/ic-wt2/U17-a12`, main pixi env)

| Check | Result |
|-------|--------|
| ruff check | All checks passed! |
| ruff format / --check | 4 files left unchanged / 4 files already formatted |
| basedpyright (`--venvpath D:/Intellicrack/.pixi/envs`) | 0 errors, 0 warnings, 0 notes (exit 0) |
| pydoclint | No violations |
| pydocstyle | Clean (no findings) |
| pytest (`-p no:timeout -p no:cacheprovider`) | 92 passed in 4.28s |

Note: basedpyright requires `--venvpath D:/Intellicrack/.pixi/envs` because the worktree
has no local `.pixi/envs`; without it basedpyright cannot resolve the project venv and
exits 3 with a venvPath warning. With the explicit main-env venvpath it is clean (exit 0).
The locked `[tool.basedpyright]` config was not modified.

## Files modified

- `tests/test_hexcore_e2e/test_bridge_alignment_color.py` (strengthened `test_snap_to_alignment_512`).
