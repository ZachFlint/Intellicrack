# U38 remediation (agent-01.md findings)

Unit owns four whole test files. All 5 findings addressed; every flagged test
rewritten into a real gate (none deleted).

## Per-finding results

| file:line - test | status | real input / independent oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_audit4/b2_process_tab/test_process_tab.py:207 - test_inject_warns_when_no_process_attached | FIXED | Real `ProcessTab._on_inject_dll` driven with `_attached_pid is None`. Oracle = exact dialog strings from production source: asserts exactly one warning, title == "Not Attached", message == "No process is currently attached. Attach to a process before injecting a DLL.", AND spies on `run_bridge_coroutine_logged` + `QFileDialog.getOpenFileName` to prove neither is reached (guard short-circuits). Renamed to `test_inject_warns_with_exact_message_and_skips_bridge_when_unattached`. |
| tests/test_audit4/b2_process_tab/test_process_tab.py:228 - test_inject_does_not_warn_when_attached | FIXED | Real handler with `_attached_pid=1234`. Asserts `getOpenFileName` actually invoked (proves the inject path advanced past the guard), no warning titled "Not Attached" raised, and empty selection aborts before any bridge dispatch (`run_bridge_coroutine_logged` spy stays empty). Renamed to `test_inject_when_attached_opens_file_dialog_and_skips_not_attached_warning`. |
| tests/test_audit5/u7_ui_providerconfig/test_provider_specific_ui.py:159 - test_resource_button_invokes_qdesktopservices | FIXED | Real `ProviderSettingsWidget` per provider; clicks the genuine `QPushButton` (real `clicked` signal/slot). Oracle = the `_PROVIDER_RESOURCE_LINKS` table (independent source of truth): asserts button labels == table labels, each click yields `openUrl.call_count == 1` (catches missing/double wiring), and the captured `QUrl.toString()` equals the table URL for that exact label. The companion failure test (`test_open_resource_url_surfaces_exact_failure_dialog`) now asserts `show_warning` fires exactly once with parent == widget, title == "Open Link Failed", and body == `f"Could not open {label} ({url})."` built from the table. Renamed to `test_resource_button_click_routes_exact_url_once_to_browser`. |
| tests/test_audit7/ui_panels_process/test_threads_auto_refresh.py:217 - test_toggling_on_starts_timer_and_increments_call_count | FIXED | Real `ThreadsTab` QTimer drives the dispatcher; counter records `time.perf_counter()` per call over a ~9.8 s window. Oracle = the documented 3000 ms cadence: asserts >=3 dispatches, >=2 inter-call gaps, and median gap in [2400, 3700] ms. A 1 ms regression (median ~0 ms) or stopped timer (no gaps) now fails, whereas the old `calls > 1` passed regardless of interval. Renamed to `test_toggling_on_drives_timer_at_three_second_interval`. |
| tests/test_ui/log_viewer/test_realcov_15_tail_reader_real_logs.py:52 - test_reader_parses_real_structlog_records | FIXED | Real events emitted through production `setup_logging` -> on-disk JSON-Lines -> `LogFileTailReader`. Oracle = independently-known constants (not the parser's own output): event == "real_alpha_event", level == "INFO" (upper-cased from on-disk "info"), logger == "intellicrack.intellicrack.tests.tail_real", module == this test filename, function == this test function name, timestamp validated against the documented `%Y-%m-%d %H:%M:%S` format via `time.strptime` with sane component bounds, line_number a positive int, extras == exactly `{"widget": "hex", "count": 7}`; the WARNING record cross-checked (level == "WARNING", extras == `{"origin": "worker"}`). |

## Notes on approach

- No production source modified; only the four owned test files were touched.
- `QDesktopServices.openUrl` and `QFileDialog.getOpenFileName` remain patched
  because they are OS facilities (browser launch / native file chooser), not the
  unit under test; the real button-click -> handler path is exercised end to end.
- Timestamp validation uses `time.strptime` (a real stdlib parser) rather than
  `datetime.strptime` to keep the genuine format-validation oracle without a
  ruff DTZ007 suppression. No `noqa` / `type: ignore` / `pyright: ignore` added.

## Verification (all six green, run on only the four touched files)

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format <files>           -> 4 files already formatted
pixi run ruff format --check <files>   -> 4 files already formatted
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> (no output; clean)
pixi run pytest <files> -p no:timeout  -> 37 passed in 33.22s
```

Files (space-separated) used for every command:
tests/test_audit4/b2_process_tab/test_process_tab.py tests/test_audit5/u7_ui_providerconfig/test_provider_specific_ui.py tests/test_audit7/ui_panels_process/test_threads_auto_refresh.py tests/test_ui/log_viewer/test_realcov_15_tail_reader_real_logs.py
