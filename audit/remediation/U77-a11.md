# Remediation U77 (agent-11.md)

Owned file: `tests/test_ui/test_realcov_15_preferences_dialog.py`

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_ui/test_realcov_15_preferences_dialog.py:57-83 - test_accept_emits_settings_changed_with_edited_config | FIXED | Drives a fully non-default real `Config` (every scalar distinct from defaults; seeded non-default `providers`/`tools` sub-configs) through the live `PreferencesDialog`, edits only the tools-directory `QLineEdit` in the real widget tree, then accepts. Asserts the entire emitted `Config` field-by-field against an independently-derived oracle (the dialog contract: `dataclasses.replace` preserves untouched top-level fields `data_directory`/`providers`/`tools`/`sandbox`; appearance/session widgets round-trip loaded values; the logging widget rebuilds `LogConfig` dropping `retention_days`/`json_file`, so those reset to defaults 14/True). New asserts: `data_directory == orig_data`, `default_provider is OPENAI`, `confirmation_level is ALL`, `providers == original`, `tools == original`, `sandbox == original`, `logs_directory == orig_logs`, full `ui`/`session` equality, full `log` equality with `retention_days=14`/`json_file=True`, and `get_config() == emitted`. Removes the prior weak single-field `isinstance` + edited-field-only check. |

Falsifiability: corrupting `_build_config` so any untouched field resets to its
default (e.g. dropping `data_directory` from the `replace` call, or wrongly
carrying the original `log.retention_days`) makes the audit asserts go red.

## Verification (all green)

```
pixi run ruff check tests/test_ui/test_realcov_15_preferences_dialog.py        -> All checks passed!
pixi run ruff format tests/test_ui/test_realcov_15_preferences_dialog.py       -> formatted
pixi run ruff format --check tests/test_ui/test_realcov_15_preferences_dialog.py -> already formatted (no changes)
pixi run basedpyright tests/test_ui/test_realcov_15_preferences_dialog.py      -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_ui/test_realcov_15_preferences_dialog.py         -> No violations
pixi run pydocstyle tests/test_ui/test_realcov_15_preferences_dialog.py        -> (no output, clean)
pixi run pytest tests/test_ui/test_realcov_15_preferences_dialog.py -p no:timeout -> 3 passed
```
