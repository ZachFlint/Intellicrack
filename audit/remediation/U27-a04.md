# U27-a04 Remediation (agent-04.md)

Worktree: `D:/ic-wt2/U27-a04` (branch `wf2/U27-a04`, left intact).

Unit U27 owns two whole test files. All 4 audit findings are accounted for
below. Each flagged test is a real, falsifiable gate driving real collaborators
(the real `intellicrack_hexcore.HexDocument`, the real `QMessageBox`
confirmation dialog, real `powershell.exe` producing real minidumps) against
independent oracles. No test was deleted.

The agent-04 findings were recorded against the pre-fix versions (old line
numbers 225/65/324/52); the production fixes and test rewrites already landed at
HEAD (commits `86511851` audit4 C6 notify + offload/stream and `3267dc2a`
F-0021 minidump target PID, including the `windows.py` here-string terminator
fix). This unit re-audited each owned test against its finding and against the
test-writer quality bar, confirmed every oracle is backed by the installed
production source (runtime-inspected `HashingMixin._on_repair_pe_checksum`, the
`_PE_CHECKSUM_OFFSET=0x58 / LEN=4` constants, the real `HexDocument` API, and
`WindowsSandbox.dump_memory`'s `Create('...')`/`$targetPid`/`OpenProcess` script),
and re-ran the full six-command gate green. No further test edits were needed.

## Per-finding outcomes

| File:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_audit4/c6_hex_hashing/test_hashing.py:225 - `message_box_yes` | FIXED | No `QMessageBox.question` monkeypatch. The repair flow drives the **real** `QMessageBox` via a `QTimer.singleShot` that locates the modal dialog and clicks its real `Yes`/`No` button. `test_confirmed_repair_writes_real_checksum_and_fires_notify` clicks `Yes` and asserts the real hexcore `CheckSum` becomes valid plus exactly one `DATA_MODIFIED` event with offset `0x58`, length `4`, source `hex-editor.hashing.repair_pe_checksum`; `test_declined_dialog_leaves_checksum_unwritten_and_silent` clicks `No` and asserts the stored checksum stays at its original invalid value and **no** `DATA_MODIFIED` event fires - the decline path a forced-Yes stub could never gate. |
| tests/test_audit4/c6_hex_hashing/test_hashing.py:65 - `StubPeDocument` | FIXED | No hand-built stub. Tests open a real, structurally valid minimal 64-bit PE image with `intellicrack_hexcore.HexDocument.open_bytes` / `.open`, so the mixin exercises the real `repair_pe_checksum` / `verify_pe_checksum` / `read` / `length` / `file_path` surface. Oracle: `verify_pe_checksum()['calculated']` (the hexcore PE checksum algorithm on a known image) - the test asserts post-repair `valid=True` with `stored == calculated` and that `read(offset, 4)` returns exactly those 4 little-endian checksum bytes. |
| tests/test_audit4/c6_hex_hashing/test_hashing.py:324 - `_build_synthetic_payload` | FIXED | The 50 MiB offload test asserts **both** the `tracemalloc` UI-thread budget (< 10 MiB) **and** CRC correctness against the independent `zlib.crc32` oracle. A boundary test (empty file -> `zlib.crc32(b"")`) and a correctness test cross-check the emitted value against `zlib.crc32`, the mmap streaming path, and the document-chunk streaming path. A streaming-but-wrong CRC fails on value; a correct-but-slurping impl fails on memory. |
| tests/test_audit7/sandbox_windows/test_memory_dump_target_pid.py:52 - `_RecordingSandbox` | FIXED | No regex-matching spy. `_RealPowerShellSandbox.run_command` runs the **exact** PowerShell script production emits through real `powershell.exe` against a real owned child process (only the in-guest `Create('...')` destination is remapped to the host shared folder). Oracle: the produced minidump's `MINIDUMP_MISC_INFO.ProcessId` (written by the Windows `MiniDumpWriteDump` API, decoded by the test's stream-directory parser) must equal the requested `target_pid` and differ from `os.getpid()`. Also asserts `GetCurrentProcess()` is absent (the F-0021 bug pattern) and `$targetPid`/`OpenProcess`/`CloseHandle`/`} finally {` are present; the `OpenProcess` failure path (System pid 4) asserts a `SandboxError` and that no dump file remains. Bridge tests register a real sandbox via the real `SandboxBridge.register_existing_sandbox` and drive the real `memory_dump`. |

## Note on the related production fix (landed at HEAD)

`src/intellicrack/sandbox/windows.py` `dump_memory` previously emitted the
`Add-Type` here-string terminator `"@` on the same line as `}`, which real
PowerShell rejects (the terminator must begin a line). That fix already landed
in commit `3267dc2a`; the current real-PowerShell gate confirms the dispatched
script parses, `MiniDumpWriteDump` succeeds, and the dump's embedded
`ProcessId` equals the target. No `src` edit was made from this worktree
(pytest runs against the installed main `src`).

## Verification (all six green on the owned files, run from `D:/ic-wt2/U27-a04`)

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format --check <files>   -> 2 files already formatted (only the harmless COM812 formatter-conflict warning)
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> (no output) exit 0, no violations
pixi run pytest <files> -p no:timeout -p no:cacheprovider -> 15 passed in 9.44s (6 hashing + 9 sandbox)
```

Files:
`tests/test_audit4/c6_hex_hashing/test_hashing.py`,
`tests/test_audit7/sandbox_windows/test_memory_dump_target_pid.py`.
