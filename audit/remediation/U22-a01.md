# Remediation U22-a01

Unit U22 owns two whole test files. All three audit findings (agent-01.md) are
addressed. No production source was changed; only test files were rewritten.

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_audit4/b4_memory_tab/test_realcov_14a_memory_tab.py:195` - `test_region_map_populated_from_real_memory_map` | FIXED (rewritten as `test_region_map_rows_round_trip_real_image_regions`) | Drives the real `MemoryTab._refresh_regions` against a real `ProcessBridge` attached to this process. Independent oracle: a separate `ProcessBridge.get_memory_map(resolve_names=True)` enumeration, scoped to stable `MEM_IMAGE` regions (>100 present). For every rendered image row whose base matches the oracle, asserts the rendered size/protection/state/type/module cells equal the real `MemoryRegion` fields exactly (`0x{size:X}`, `protection`, `state`, `type`, `module_name`), and that nearly all real image regions (`>= count-8`) round-trip. Falsifiability confirmed: halving the rendered size in `_refresh_regions` turns this red. |
| `tests/test_audit4/b4_memory_tab/test_realcov_14a_memory_tab.py:222` - `test_region_map_contains_real_module_region` | FIXED (rewritten as `test_region_map_pins_real_module_header_region`) | For both `ntdll.dll` and `kernel32.dll`, resolves the real image base + size via the independent `get_modules` oracle. Asserts the module base appears verbatim as a rendered region base (exact match, not containment), that region is typed `image`, is readable, names the real module, the header region size is within the image span, and the module's full `[base, base+size)` image span is covered by contiguous rendered image regions. Falsifiability: halved render size / off-by-page base turns it red. |
| `tests/test_audit7/sandbox_manager/test_realcov_12a_manager_real.py:131` - `test_available_types_are_a_subset_of_real_probes` | FIXED (replaced by two gates) | Replaced the boolean-equivalence check with: (1) `test_absent_real_qemu_binary_excludes_qemu_from_reported_types` - a real `QEMUSandbox` subclass on a no-QEMU host (real, unmodified `is_available` runs its genuine `qemu_path is None` branch), routed through the real manager `_probe_type`/cache/`get_available_types`; asserts the independent direct probe is `False`, `qemu` is absent from reported types, and the False result is cached verbatim. (2) `test_reported_qemu_availability_matches_real_independent_probe` - real unmodified `QEMUSandbox` probed through the manager; asserts manager `qemu` membership and cached value equal an independent direct `QEMUSandbox.is_available()` call exactly. Falsifiability: fabricating `get_available_types` to return all types turns both red. |

No tests were deleted. No mocks/patches of the operation under test are used:
the `_ProbeRoutingManager` injects only which real sandbox object the real
`_probe_type` constructs, and `_AbsentBinaryQEMUSandbox` overrides only the
executable-discovery helper so the real availability decision executes its
genuine no-binary branch.

## Verification (all green)

Commands run in the pixi env on only the two touched files
(`TESTS = tests/test_audit4/b4_memory_tab/test_realcov_14a_memory_tab.py tests/test_audit7/sandbox_manager/test_realcov_12a_manager_real.py`):

```
pixi run ruff check $TESTS            -> All checks passed!
pixi run ruff format $TESTS           -> formatted
pixi run ruff format --check $TESTS   -> 2 files already formatted (no changes)
pixi run basedpyright $TESTS          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint $TESTS             -> No violations
pixi run pydocstyle $TESTS            -> (exit 0, no output)
INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1 pixi run pytest $TESTS -p no:timeout
                                      -> 13 passed, 2 warnings
```

The `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` override is required only so the
`@pytest.mark.spawns_process` tests (real QEMU accelerator probe) execute on the
host instead of being environment-skipped; with it set, all 13 tests run and
pass (none skipped).
