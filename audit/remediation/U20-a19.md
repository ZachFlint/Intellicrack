# Remediation U20-a19

Worktree: `D:/ic-wt2/U20-a19` (branch `wf2/U20-a19`, left intact for review/merge).

Source report: `audit/agent-19.md`. Four findings on three files, all rewritten
into real falsifiable gates. No production source edited.

## Per-finding outcomes

| Ref | Severity | Status | Real input + independent oracle + exact assertion |
|---|---|---|---|
| `test_realcov_14a_process_tab.py:152-169` `test_process_table_populated_from_real_enumeration` (renamed `test_process_table_renders_real_fields_for_running_interpreter`) | Medium | FIXED | Real `ProcessBridge` enumeration drives the tab. The row for `os.getpid()` must render the genuine image name (`== Path(sys.executable).name`, contains `python`) and a thread count cross-checked against the independent `psutil.Process(os.getpid()).num_threads()` oracle. Replaces the `len(pids) >= 2` / `all(pid >= 0)` weak checks; a hardcoded or stale snapshot diverges and fails. |
| `test_realcov_14a_process_tab.py:172-183` `test_process_count_label_matches_real_rows` | Medium | FIXED | Splits the previously tautological label check into two independent gates: (1) rendered `row_count() > 10` and `>= len(psutil.pids()) // 4` (real-system scale, not a single injected row) with `os.getpid()` present; (2) the count-label text equals `f"{rows} processes"` derived from that real count. |
| `test_plugin_deploy.py` file-level (fake `DUMMY_PE = b"MZ" + nulls`, weak deploy assertion) | High | FIXED | Replaces stub MZ prefixes with programmatically built, spec-valid PE images carrying the correct `IMAGE_FILE_MACHINE` (AMD64 for `.dp64`, I386 for `.dp32`). After deployment, `_assert_deployed_pe` checks SHA-256 byte-identity source-vs-target **and** parses the deployed file with `pefile`, asserting the machine type matches the target arch plus the `MZ` / `PE\0\0` signatures. Falsifiability confirmed: arch-cross and byte-corruption both raise. |
| `test_x64dbg_api_coverage.py:45-70` `test_debugger_control_methods_exist` (rewritten as parametrized `test_control_method_classifies_unavailable_plugin`) | High | FIXED | Parametrized over `step_into`/`step_over`/`step_out`/`run`/`pause`/`stop`. Asserts the bridge's real error-classification contract on the unavailable-plugin path: `ToolError.tool_name == "x64dbg"`, `details["x64dbg_error_code"] == "plugin_unavailable"`, and `details["command"] == <originating method>`. A swallowed fault, returned value, wrong code, or mislabelled command fails the gate. |

## Six-command verification (run from `D:/ic-wt2/U20-a19`, main pixi env)

| Command | Result |
|---|---|
| `ruff check` | All checks passed! |
| `ruff format` (+ `--check`) | 3 files left unchanged |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | No findings |
| `pytest -p no:timeout -p no:cacheprovider` | 31 passed in 9.72s |

All gates are genuinely green: zero lint/type/doc findings and every test passes
on the live Windows host (no skips/xfail masking breakage). The process-tab tests
run real Win32 enumeration; the plugin-deploy tests build real PEs and validate
them with `pefile`; the x64dbg control tests validate the bridge's structured
error contract.
