# Unit U25-a10 Remediation

Worktree: `D:/ic-wt2/U25-a10` (branch `wf2/U25-a10`, left intact for review/merge)

## Summary

The audit (agent-10.md) flagged three High findings across two files. On
inspection of current `HEAD` (commit `f06ab769` "refactor: harden test suite
and resolve core integration bugs"), **all three findings were already
remediated** in the committed test files. The audit line references
(427-505, 738+) point at pre-remediation content that no longer exists:

- `test_templates_pattern.py` no longer monkeypatches `QMessageBox`,
  `QFileDialog`, `hexpat_interpreter_available`, or `HexPatInterpreter_cls`.
  It drives the real `intellicrack_hexcore.HexDocument`, the real
  `HexPatCompiler`, and the real `HexPatInterpreter` against real PE/ELF/header
  buffers, replacing the modal dialogs with a dependency-injected
  `UserNotificationRecorder` (the production code exposes a `_user_notifier`
  slot and non-interactive `_import_template_from_path` /
  `_remove_template_named` entry points for exactly this).
- `test_anthropic_provider.py` asserts the real provider error contract on the
  unconditional credential-rejection / not-connected paths and drives the live
  `/v1/models` endpoint with strong assertions on independently-known
  catalogue facts (`claude-*` ids, opus/sonnet/haiku families, API display
  names) plus the full `ModelInfo` mapping contract (200k window, tool/vision/
  streaming flags). `test_invalid_key_...` drives the real live probe inside
  `connect()` with a real-but-invalid `sk-ant-` key and asserts the normalized
  `AuthenticationError("Invalid API key")`.

No test-file edits were therefore required; the work was verification of
falsifiability plus provisioning the real `ANTHROPIC_API_KEY` (copied the
gitignored `.env` into the worktree) so the four live Anthropic gates actually
run rather than skip. Each finding was confirmed to go RED if the covered
production code were corrupted (notification removal, dropped capability flag,
removed live probe, wrong decoded field count).

## Per-finding results

| Ref | Severity | Status | Real input + independent oracle + exact assertion |
| --- | --- | --- | --- |
| `test_anthropic_provider.py:44-240` all `@pytest.mark.integration` | High | FIXED | Live `/v1/models` + real-but-invalid key. Oracle: Anthropic catalogue scheme (every id `claude-*`, opus/sonnet/haiku present, display names `Claude*`) and provider `_MSG_*` / `_build_model_info` contract. Asserts `claude-*` prefix per id, all three families, full `ModelInfo` (provider==ANTHROPIC, `context_window==200000`, tools/vision/streaming True), stable paginated id set, and exact `AuthenticationError`/`ProviderError` strings on the unconditional credential/not-connected paths. |
| `test_templates_pattern.py:427-505` dialog fixtures | High | FIXED | Real compiled JSON staged on disk + malformed JSON; remove by name. Oracle: live `HexDocument` registry (`list_templates` before/after) + notification contract. No `QFileDialog`/`QMessageBox` patch (DI `UserNotificationRecorder`). Asserts imported template present with `TEMPLATE_REGISTERED {'template_name':'IMPORTED_HDR'}`, malformed import unchanged registry + exactly one `('Import Template','warning')` notice starting `Import failed:`, remove drops name + `TEMPLATE_REMOVED {'template_name':'DROPME'}`. |
| `test_templates_pattern.py:738` interpreter monkeypatch group | High | FIXED | Real `.hexpat` DSL + real `HexPatCompiler` over a live `HexDocument` (bytes `0x5A4D`/`0xDEADBEEF`). Oracle: real interpreter/engine decoding exactly two fields. No `hexpat_interpreter_available`/`HexPatInterpreter_cls` patch. Asserts compile branch registers `COMPILEDHDR` + emits `TEMPLATE_REGISTERED` and `PATTERN_EXECUTED {'pattern_name':'COMPILEDHDR','field_count':2}`; interpreter branch leaves registry untouched yet emits `<inline>` register+execute with `field_count==2`; distinct audit-source loop guards verified by self-suppressing source-bound recorders. |

## Six-command verification (from `D:/ic-wt2/U25-a10`, main pixi env)

| Command | Result |
| --- | --- |
| `ruff check` | All checks passed! |
| `ruff format` then `ruff format --check` | 2 files already formatted (no changes) |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | exit 0, no findings |
| `pytest ... -p no:timeout -p no:cacheprovider` | 21 passed in 4.37s (8 anthropic incl. 4 live-API gates, 13 templates/pattern) |

All six checks are genuinely green: zero ruff/type/doc findings and 21 tests
PASS (no skips/xfails masking breakage; the live Anthropic gates run against
the real API with the provisioned key).
