# Remediation U58-a03

File owned: `tests/test_hexcore_e2e/test_hexpat_control_flow.py`
Source audit: `audit/agent-03.md`

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `test_hexpat_control_flow.py:26 - test_while_counter_loop_produces_fields` | FIXED | Real input `bytes(range(16))` driven through `execute_bytes`; oracle is the identity mapping offset->value of `bytes(range(16))`. Now asserts exact `name`, `offset` (`[0,1,2,3]`), `size`, `raw_bytes` (`[[0],[1],[2],[3]]`) and `display_value` (`["0x0","0x1","0x2","0x3"]`) field-by-field, not just `len == 4`. |
| `test_hexpat_control_flow.py:37 - test_while_sentinel_stops_at_zero` | FIXED | Real input `b"\x01\x02\x03\x00\x05\x06"`; oracle is the buffer layout (three non-zero leading bytes then a 0x00 sentinel). Asserts exactly 3 fields at offsets `[0,1,2]` with `raw_bytes` `[[1],[2],[3]]` / `display_value` `["0x1","0x2","0x3"]`, and explicitly asserts offsets 3, 4, 5 were never parsed (sentinel and trailing bytes excluded). |
| `test_hexpat_control_flow.py:48 - test_while_empty_body_terminates_immediately` | FIXED | Renamed to `test_while_false_condition_executes_zero_iterations` (asserts `results == []`) and added the positive counterpart `test_while_true_condition_executes_body`: real input `b"\xab\xcd\x00\x00"`, loop enters with `run == 1`, places `u8 x @ 0`, clears `run`. Asserts exactly one `x` field at offset 0 with `raw_bytes == [0xAB]` and `display_value == "0xAB"`, proving the guard is evaluated rather than always skipped. |
| `test_hexpat_control_flow.py:63 - test_for_loop_fixed_count` | FIXED | Real input `bytes(range(8))`, loop `i < 5`; oracle is the identity mapping of `bytes(range(8))`. Asserts 5 fields named `elem`, offsets `[0,1,2,3,4]`, `raw_bytes` `[[0]..[4]]`, `display_value` `["0x0".."0x4"]`, and that offsets 5..7 are not read. |
| `test_hexpat_control_flow.py:264 - test_while_loop_accumulates_variable` | FIXED | Real 16-byte input `bytes([1,2,3,4,5, 0*10, 0x7E])`; independent oracle is the hand-computed sum `1+2+3+4+5 == 15` and a distinctive sentinel byte `0x7E` planted only at offset 15. The accumulated total drives `u8 marker @ total`, so the test asserts the marker lands at `offset == 15` and reads `raw_bytes == [0x7E]` / `display_value == "0x7E"`. A miscalculated sum would place the marker on a 0x00 byte. Replaces the no-assertion (execution-only) test. |
| `test_hexpat_control_flow.py:274 - test_try_inside_for_loop_recovers_per_iteration` | FIXED | Real 4-byte input `b"\xaa\xbb\xcc\xdd"`; oracle is the buffer geometry: only i==0 has 4 bytes for `u32 big`, so it succeeds (`size 4`, `display_value "0xDDCCBBAA"`), while i==1,2,3 overflow and recover via the catch's `u8 small`. Asserts exactly 4 fields with names `["big","small","small","small"]`, sizes `[4,1,1,1]`, offsets `[0,1,2,3]`, and exact `raw_bytes`/`display_value` for each. Replaces `len(results) >= 1`. |

All findings FIXED (none deleted). One additional positive-path test was added to make the false/true while-guard pair a genuine gate.

## Verification block

Commands run in the pixi env against only the touched file:

```
pixi run ruff check tests/test_hexcore_e2e/test_hexpat_control_flow.py        -> All checks passed!
pixi run ruff format tests/test_hexcore_e2e/test_hexpat_control_flow.py       -> reformatted; format --check reports no changes
pixi run basedpyright tests/test_hexcore_e2e/test_hexpat_control_flow.py      -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_hexcore_e2e/test_hexpat_control_flow.py         -> No violations
pixi run pydocstyle tests/test_hexcore_e2e/test_hexpat_control_flow.py        -> (no output; clean)
pixi run pytest tests/test_hexcore_e2e/test_hexpat_control_flow.py -p no:timeout -> 20 passed
```

All six checks are genuinely green; all tests PASS (none skipped/xfailed).
