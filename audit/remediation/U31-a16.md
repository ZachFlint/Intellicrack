# U31-a16 Remediation

Worktree: `D:/ic-wt2/U31-a16` (branch `wf2/U31-a16`, left intact)

Files in unit:
- `tests/test_audit3/core/test_script_gen.py`
- `tests/test_audit7/sandbox_windows/test_realcov_12b_inline_monitors.py`

## Status note

The audit cited line ranges (`364-388`, `299-346`) and test names
(`test_validate_javascript_temp_logs_unlink_then_cleaned_only_on_success`,
`test_validate_javascript_unlink_failure_skips_cleaned_log`,
`test_network_monitor_source_captures_live_endpoints`) that no longer match the
current file contents. A prior commit (`f06ab769 "harden test suite and resolve
core integration bugs"`) already rewrote these flagged tests into genuine gates.
`git diff --stat HEAD -- tests/` shows zero changes in this worktree, so the
remediation lives in HEAD, not in new edits from this unit. This unit's work was
to **verify** that each flagged finding is genuinely remediated (falsifiable, real
inputs, independent oracles, no masked skips, no mock of the operation under test)
and that all six checks pass with the tests actually executing rather than being
skipped. Each finding was verified FIXED.

## Per-finding results

| Finding (ref) | Severity | Status | Oracle / why it is now a real gate |
| --- | --- | --- | --- |
| `test_script_gen.py:364-376 - test_validate_javascript_temp_logs_unlink_then_cleaned_only_on_success` | High | FIXED | Now `test_validate_javascript_emits_cleanup_after_unlink_attempt_on_real_run` (+ `..._reports_node_syntax_error_and_still_cleans_up`). Drives the real `node --check` path; the verdict oracle is node's own parser (`(True, None)` for valid JS; `SyntaxError` substring for invalid). Asserts **unconditionally** that `temp_file_cleaned` is in the event list, `temp_file_unlink_failed` is not, and `index(temp_file_cleaned) > index(temp_file_unlink_attempt)`. No `if cleaned in events` conditional and no post-hoc skip remain, so a silent cleanup regression fails the test. |
| `test_script_gen.py:378-388 - test_validate_javascript_unlink_failure_skips_cleaned_log` | Medium | FIXED | Now `test_validate_javascript_unlink_failure_suppresses_cleaned_log`. No `mock.patch.object(Path, "unlink")`. A real `structlog` processor observes production's genuine `temp_file_created` event, pulls the live temp path production chose, and `chmod`s that real file read-only via `pathlib.Path.chmod` so production's real `except OSError` unlink branch executes on Windows (`PermissionError`). Asserts `temp_file_unlink_attempt` and `temp_file_unlink_failed` ARE in events and `temp_file_cleaned` is NOT — the failure branch is proven to run, not vacuously absent. |
| `test_realcov_12b_inline_monitors.py:299-346 - test_network_monitor_source_captures_live_endpoints` | High | FIXED | Skips **only up front** via `_require_tcp_capability()` (probes `Get-NetTCPConnection` before any capture or socket setup). Then holds a real `ESTABLISHED` loopback pair on a known port across the capture window, runs the real `_network_monitor_source` under `pwsh`, parses with the real `parse_network_log`, and **hard-asserts** `tcp_records` is non-empty and that the held-port endpoint (independent oracle = the port the test itself opened) surfaces as an outbound established record plus a listener. Missing TCP records now FAIL; the masked post-hoc skip is gone. |

## Falsifiability spot-checks

- Unlink-failure gate fires production's `except OSError` branch (asserts
  `temp_file_unlink_failed` present) — proven non-vacuous on this Windows host.
- Network gate: if the monitor surfaced no/other-port TCP lines, the
  `until`-predicate loop would lapse the 45s deadline and `assert tcp_records` /
  `assert held_endpoint` would go RED.

## Six-command verification (run from `D:/ic-wt2/U31-a16`, main pixi env)

| Command | Result |
| --- | --- |
| `ruff check` | All checks passed! |
| `ruff format --check` | 2 files already formatted (no changes) |
| `basedpyright` | 0 errors, 0 warnings, 0 notes (worktree `.pixi/envs` junctioned to the main env so the locked config's relative `venvPath` resolves; config unmodified) |
| `pydoclint` | No violations |
| `pydocstyle` | exit 0 (no findings) |
| `pytest` (both files) | 40 passed in 28.25s (`INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` so the live-kernel monitor tests execute against the real OS instead of the host-process skip; 36 script_gen + 4 inline_monitors, zero skips) |
