# U31 - agent-16 remediation

Unit owns (whole files):
- `tests/test_audit3/core/test_script_gen.py`
- `tests/test_audit7/sandbox_windows/test_realcov_12b_inline_monitors.py`

## Per-finding outcomes

| file:line - test | severity | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- | --- |
| `tests/test_audit3/core/test_script_gen.py:364` - `test_validate_javascript_temp_logs_unlink_then_cleaned_only_on_success` | High | FIXED | Replaced the conditional `if "temp_file_cleaned" in events` (which no-op'd the check) and the post-assertion `pytest.skip("node not installed")`. Split into two real gates that require the real `node` runtime up front (`_require_node`). `test_validate_javascript_emits_cleanup_after_unlink_attempt_on_real_run` drives real `node --check` on valid JS, asserts the exact verdict `(is_valid, error) == (True, None)` (oracle = node's own parser), then asserts **unconditionally** that `temp_file_unlink_attempt` and `temp_file_cleaned` are both present and that `index(temp_file_cleaned) > index(temp_file_unlink_attempt)`. `test_validate_javascript_reports_node_syntax_error_and_still_cleans_up` feeds malformed JS `"function (){"`, asserts node's real `SyntaxError` text (`error != "node not installed"`, `"SyntaxError" in error`) and that cleanup still fires on the failure path. |
| `tests/test_audit3/core/test_script_gen.py:378` - `test_validate_javascript_unlink_failure_skips_cleaned_log` | Medium | FIXED | Removed `mock.patch.object(Path, "unlink", side_effect=OSError)`. New `test_validate_javascript_unlink_failure_suppresses_cleaned_log` drives the **real** production cleanup against a **real read-only file**: a leading structlog processor observes the genuine `temp_file_created` event, extracts the live temp path production chose, and marks that real file read-only via `Path.chmod(stat.S_IREAD)`. On Windows `Path.unlink` of a read-only file raises a real `PermissionError`, so production's real `except OSError` branch runs. Asserts `temp_file_unlink_attempt` and `temp_file_unlink_failed` are present and `temp_file_cleaned` is **absent**; also asserts a temp file was actually created. Side effect runs synchronously inside the `temp_file_created` log call (deterministic, no sleeps); the exact path is restored writable and unlinked afterwards so nothing leaks. |
| `tests/test_audit7/sandbox_windows/test_realcov_12b_inline_monitors.py:299` - `test_network_monitor_source_captures_live_endpoints` | High | FIXED | Removed the post-hoc `pytest.skip` that masked "no TCP records captured". Capability is now gated **before** any capture via `_require_tcp_capability` (skips only if `Get-NetTCPConnection` is entirely unavailable). The test establishes a real `ESTABLISHED` loopback pair on a known `held_port` and runs the real `_network_monitor_source` under `pwsh`, deterministically waiting (via `_capture_monitor_until`, polling the real log artifact up to 45s; no bare sleep) until the held endpoint `127.0.0.1:{held_port}|` appears, then terminates. Asserts (hard, no skip) that TCP records exist, that the exact held loopback endpoint on `held_port` was captured (independent oracle = the port the test itself opened), that it surfaces as an established/outbound record, and that a listening/inbound endpoint also appears. Per-record protocol/direction/port normalisation is still asserted. |

## Notes

- `node` v26.2.0 and `pwsh` 7 are present in the environment, so the real external tools are exercised (no papered-over skips on a capable host).
- The sandbox spawn tests carry the project's `spawns_process` marker; on a host they require `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` (or run under the Docker sandbox). This is the project's standard host-protection gate, not a finding-masking skip. With the override the spawn tests PASS.
- Determinism: the prior network test was flaky under randomized full-session ordering (fixed-sleep capture race under host load). Replaced the fixed settle with `_capture_monitor_until`, which synchronizes on the produced log artifact. Verified 40/40 pass across randomized seeds 3, 7, 11, 13, 17 (seed 3 previously failed).

## Verification (run in the pixi env on only the touched files)

Files: `tests/test_audit3/core/test_script_gen.py tests/test_audit7/sandbox_windows/test_realcov_12b_inline_monitors.py`

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format <files>           -> 2 files (formatted); ruff format --check -> 2 files already formatted
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> clean (no output)
INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1 pixi run pytest <files> -p no:timeout
                                       -> 40 passed in ~33s
```

All six checks genuinely green; pytest PASSES (not skipped/xfailed) with the authorized host-process override.
