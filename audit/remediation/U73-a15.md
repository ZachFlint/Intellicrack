# Remediation U73-a15 (agent-15.md)

Unit owns three whole test files. All three audit findings accounted for.

## Per-finding outcomes

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_hexcore_e2e/test_bridge_arithmetic.py:47 - `_setup_and_apply` | FIXED | The flagged item is a helper, not a test. Hardened it to also return the bridge's metadata dict and to read the selection back through the real native `intellicrack_hexcore` transform backend (write -> select -> transform -> read round trip). Added `TestSetupHelperFidelity` that pins the helper: NOT of `00 00 00 00` -> `FF FF FF FF`, and an XOR transform proven to mutate the persisted document while leaving trailing bytes `0x99 0x99` untouched. Every caller now asserts exact bytes computed by an independent bit-level oracle (plain Python `^/&/|`, and `_rol8`/`_ror8` reference rotations), never the implementation's own output. Added missing coverage dimensions: multi-bit shifts (`shl` by 3, `shr` by 4), multi-bit rotates (`rol` by 3, `ror` by 5), XOR zero-key identity, single-byte selection boundary with exact metadata `{"offset":0,"length":1,"operation":"not"}`, and an error path asserting unknown operation `"mul"` raises `ToolError` (surfaced, not silently identity). |
| tests/test_ui/log_viewer/test_proxy.py:59 - `test_min_level_filter` | FIXED | Drives the real `LogFilterProxyModel` over a real `LogRecordTableModel` seeded with DEBUG/INFO/WARNING records. New `_visible_events` helper reads each surviving row back via `proxy.index(row,0).data(Qt.ItemDataRole.UserRole)` and asserts exactly `["warn_event"]` survives a WARNING minimum (so INFO and DEBUG are proven excluded by name, not by count). Added boundary coverage: walking DEBUG->INFO->WARNING->ERROR minimums asserting the exact surviving event-id set at each step, a CRITICAL minimum hiding all rows, and an ERROR record passing a WARNING threshold while an INFO record below it is dropped. Independent oracle: the `logging` numeric level ordering. |
| tests/test_ui/test_hex_format.py:42 - `test_single_byte_layout` | FIXED | Drives the real `format_hex_dump(b"A", 0)` and decomposes the line into logical columns instead of freezing the full string: address column `== "00000000"` and `int(.,16)==0`; hex column equals `f"{0x41:02X}".ljust(48)` with `.split()==["41"]`; ASCII column `== "A"` with `ord(.)==0x41`; the two-space gaps are asserted at the documented offsets. Independent oracle: the byte's own value `ord("A")==0x41` and the module's documented 48-char hex field geometry, not a hardcoded rendered string. |

## Verification (project env, only touched files)

Files: `tests/test_hexcore_e2e/test_bridge_arithmetic.py tests/test_ui/log_viewer/test_proxy.py tests/test_ui/test_hex_format.py`

| Command | Result |
| --- | --- |
| `pixi run ruff check <files>` | All checks passed! |
| `pixi run ruff format <files>` then `--check` | 3 files already formatted (no changes) |
| `pixi run basedpyright <files>` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint <files>` | No violations |
| `pixi run pydocstyle <files>` | exit 0 (no findings) |
| `pixi run pytest <files> -p no:timeout` | 43 passed |
