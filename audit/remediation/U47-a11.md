# U47-a11 Remediation Report (Re-fix Attempt 3)

## Summary

Re-fix Attempt 3 resolves the single defect flagged by the reviewer: line 356 of
`tests/test_bridges/test_realcov_04_base.py` contained `# type: ignore[abstract]`,
a type suppression comment prohibited by CLAUDE.md. The comment was removed so the
line reads `BinaryOperationsBridge()` without any suppression directive.
`pytest.raises(TypeError)` continues to catch the instantiation failure at runtime,
and `enableTypeIgnoreComments: false` in `pyrightconfig.json` already made the comment
inert for basedpyright, so all six tool checks remain green with 56 tests passing.

## Findings

| Ref | Severity | Status | Fix summary |
|-----|----------|--------|-------------|
| `tests/test_audit5/u5_ui_mainwindow/test_ui_mainwindow.py:228-231` `TestHxDButtonHandlerCleanedUp` | Low | FIXED | Constructs `_NoHxDHolder` (no `_hxd_panel`); calls `_open_hxd_impl` via `getattr`; asserts exactly one `QMessageBox.warning` with title/body containing `"HxD"` and `"not found"`. A re-introduced `add_hxd_tab` raises `AttributeError`, failing the test. |
| `tests/test_audit5/u5_ui_mainwindow/test_ui_mainwindow.py:303-307` `TestSandboxPanelLookupUsesPanels` | Low | FIXED | `_RecordingPanel` logs `get_panel` calls; asserts `"sandbox"` in call ledger; a broken impl calling `get_active_tool_widget` leaves the ledger empty and fails. |
| `tests/test_bridges/test_realcov_04_base.py:145-178` `TestDisassemblyLineFromRealBinary` | Medium | FIXED | Independent capstone decode of `kernel32.dll` .text bytes; asserts `mnemonic`, `raw_bytes`, `address` match field-by-field. Re-fix Attempt 3: removed prohibited `# type: ignore[abstract]` from line 356. |
| `tests/test_core/test_realcov_05a_orchestration.py:145-150` orchestration integration | Medium | FIXED | `_assert_real_pe_tool_result` asserts `file_type=="pe"`, imports non-empty with `LoadLibrary`, sections with `".text"`, sha256 is 64-char hex. |
| `tests/test_hexcore_e2e/test_bridge_strings.py:50-73` `TestGetStrings` | Medium | FIXED | Exact oracle: ASCII offset=15, length=13, content=`"0Hello World!"`; UTF-16 offset=0, length=286, contains `"Test String"`. Wrong offset or content fails. |

## Verification Results

| Check | Result |
|-------|--------|
| `ruff check` | All checks passed |
| `ruff format --check` | 4 files already formatted |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | No violations |
| `pytest` | **56 passed in 3.61s** |

## Worktree

`D:/ic-wt2/U47-a11` (detached HEAD)
