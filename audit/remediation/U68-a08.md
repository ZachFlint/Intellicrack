# Remediation U68-a08 (agent-08.md)

Unit owns four whole test files. All four audit findings (Low) addressed by
rewriting the flagged tests into real, falsifiable gates. No deletions.

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_hexcore_e2e/test_bridge_pe_checksum.py:92 - test_no_document_raises | FIXED | Drives `verify_pe_checksum()` on a freshly constructed real `HexEditorBridge` (no document). Now asserts bridge state explicitly before AND after the raise: `bridge.document is None`, `bridge.state.binary_loaded is False`, `bridge.state.target_path is None` (independent oracle = the documented no-document contract). Catches a bridge that auto-constructs a default document at init (RuntimeError would fire for an unrelated reason) and a failed verify that leaves a partially-opened document/stale path behind. |
| tests/test_hexcore_e2e/test_realcov_09b_stdlib_realbin.py:321 - test_parse_int_invalid_raises_runtime_error | FIXED | Real `BuiltinFunctions._string_parse_int("not-an-int", 10)`. Now `pytest.raises(HexPatRuntimeError, match=r"cannot parse 'not-an-int' as base-10 integer")` and asserts `exc_info.value.__cause__` is a `ValueError` (the chained root cause from `int()`). Oracle = exact production message naming input+base, independently spelled in the test. Added companion `test_parse_int_unsupported_base_raises_with_base_in_message` (base 99 -> `match="unsupported base 99"`, `__cause__ is None`) to distinguish the base-guard boundary from the malformed-input path. |
| tests/test_providers/test_openai_provider.py:352 - test_disconnect_clears_connection_state | FIXED | Real live connect (live `/models` round-trip) then `disconnect()`. Now asserts `provider.client is not None` after connect, then after disconnect: `is_connected is False`, `provider.client is None`, and a subsequent `await provider.list_models()` raises `ProviderError` whose message equals the literal `Not connected to OpenAI API`. Proves real client teardown (not a mere flag flip) via the public not-connected guard, with the exact-message oracle held independently in the test. |
| tests/test_providers/test_safe_parse_stream_json.py:145 - test_parse_non_object_values_return_none | FIXED | Real `LLMProviderBase._safe_parse_stream_json` against a real structlog JSON-capture logger. Each non-object payload paired with its independently-asserted `json.loads` decode value (`42`->int, `[1,2,3]`->list, `"hello"`->str, `null`->None, `true`/`false`->bool); each must return `None`. Load-bearing gate: `_read_events(stream) == []` proves the silent-skip contract (no warning on well-formed non-objects). Added an in-stream control (malformed `{not json` on the SAME logger MUST yield exactly one `stream_json_parse_skipped` warning) so the empty-events assertion is provably non-vacuous. |

## Verification (project env, only the four touched files)

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format <files>           -> 1 reformatted, 3 unchanged
pixi run ruff format --check <files>   -> 4 files already formatted (no changes)
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> (no output; clean)
pixi run pytest <files> -p no:timeout  -> 62 passed in 16.77s
```

Files (space-separated) for every command:
tests/test_hexcore_e2e/test_bridge_pe_checksum.py
tests/test_hexcore_e2e/test_realcov_09b_stdlib_realbin.py
tests/test_providers/test_openai_provider.py
tests/test_providers/test_safe_parse_stream_json.py

All six gates genuinely green; the five remediated tests run and PASS (the live
disconnect test exercised the real connect path, not the skip).
