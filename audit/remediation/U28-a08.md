# Remediation U28-a08 (agent-08.md)

Unit owns: `tests/test_scripts/test_commit_message.py`,
`tests/test_ui/test_realcov_13b_hex_sections.py`. 4 findings, all FIXED.

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_scripts/test_commit_message.py:173-184 - `TestEstimateTokens` (3 tests) | FIXED | Tautology (`len//3` mirror) removed. Now drives a realistic 10-file unified diff and uses **tiktoken `cl100k_base`** (a different, trusted tokenizer) as the independent oracle: asserts the estimate lands within `0.6x-1.8x` of the true token count (`reference > 1000`), asserts the empty-string boundary equals the oracle (both 0), and asserts strict monotonicity (longer real diff -> strictly larger estimate). A wrong char/token divisor in either direction breaks the bounded-factor gate. |
| test_scripts/test_commit_message.py:326-348 - `TestCountTokensFallback` (3 tests) | FIXED | `MagicMock` client removed. Now a concrete `_StubGeminiClient` implementing only the real `models.count_tokens(*, model, contents)` surface raises **genuine** `google.genai` `ServerError(503)`/`ClientError(400)`/`too-large ClientError` and stdlib `ConnectionError`, driving the real `_count_tokens` fallback dispatch end to end. Each asserts the result equals `_estimate_tokens(text)` for the exact same text. Added: success path asserts exact pass-through of the API total (4242, distinct from the estimate); `total_tokens=None` falls back; and an uncaught `KeyError` **propagates** (`pytest.raises`), proving the catch is specific, not blanket. |
| test_scripts/test_commit_message.py:350-370 - `test_throttle_prevents_rapid_calls` | FIXED | Wide wall-clock `±20%` tolerance replaced with a deterministic virtual clock (monkeypatching the OS `time.monotonic`/`time.sleep` primitives, not the function under test). With interval 0.5s and 0.1s elapsed, asserts the function sleeps **exactly 0.4s** (`abs(slept[0]-0.4) < 1e-9`) and advances `_last_count_time` to 1000.4. Added a second gate: when the interval has already elapsed, asserts **no** sleep occurs and the timestamp updates. A delay calc ignoring `elapsed` or halving it fails the exact check. |
| test_ui/test_realcov_13b_hex_sections.py:113-122 - `test_min_length_is_enforced` | FIXED | Weak `>= 1` replaced. Drives the real `execute_strings_extraction` over real `kernel32.dll` at two floors (6 and 12), using the hexcore backend's authoritative `length` (codepoint count) field as the independent oracle. Asserts `min(length) >= floor` at each floor, asserts a record sits exactly on the low floor (proves the floor is binding), asserts the low floor admits sub-12 matches, and asserts raising the floor strictly drops them. Breaking min-length enforcement makes short records appear and fails. |

## Verification (all six green, owned files only)

```
pixi run ruff check tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
  -> All checks passed!
pixi run ruff format tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
pixi run ruff format --check tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
  -> 2 files already formatted (no changes)
pixi run basedpyright tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
  -> No violations
pixi run pydocstyle tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py
  -> (no output; clean)
pixi run pytest tests/test_scripts/test_commit_message.py tests/test_ui/test_realcov_13b_hex_sections.py -p no:timeout
  -> 28 passed (22 commit-message + 6 hex-sections)
```
