# U13-a17 Remediation - tests/test_providers/test_local_xpu_e2e.py

## Summary

All six audit findings on this file were already remediated at the worktree
base commit (`a0393603` "refactor: resolve audit findings and add real-data
test coverage", carried into HEAD `f06ab769`). The audit's cited line numbers
(690, 710, 731, 756, 806, 952) reference the pre-remediation revision; the
corresponding tests by name now carry genuine, falsifiable gates. Each was
re-verified to pass on real Intel Arc B580 XPU hardware with the real
`TinyLlama/TinyLlama-1.1B-Chat-v1.0` model and confirmed falsifiable.

Hardware/model confirmed live in the pixi env:
`torch 2.10.0+xpu`, `is_xpu_available() == True`, `is_arc_b580() == True`
(device "Intel(R) Arc(TM) B580 Graphics").

Falsifiability of the central coherence oracle was demonstrated directly:
`_assert_coherent_english` REJECTS `"aaaa..."`, `"<html>..."`, and short noise,
while ACCEPTING genuine prose - so a chat path returning degenerate/garbled
output drives the gate RED.

No source edits required; no findings are NEEDS_SRC.

## Per-finding results

| Audit ref (orig line) | Test | Severity | Status | Real input + independent oracle + exact assertion |
|---|---|---|---|---|
| 690 | `test_simple_chat_returns_response` | High | FIXED | Greedy XPU chat on "What is 2 + 2?"; oracle 2+2=4; asserts `"4" in content`, `re.search(r"2\s*\+\s*2")`, and full coherence gate. |
| 710 | `test_response_is_coherent_text` | High | FIXED | XPU chat on "What is the sky?"; model-independent coherence oracle `_assert_coherent_english(min_words=6, min_dictionary_hits=3)` (distinct tokens, >=5 unique letters, repetition-ratio<0.5, dictionary overlap). |
| 731 | `test_domain_prompt` | High | FIXED | Greedy XPU chat on a PE-header prompt; asserts coherent English plus `>=3` PE-domain terms from a fixed vocabulary and `"header"` present. |
| 756 | `test_stream_yields_chunks` | Medium | FIXED | XPU `chat_stream` on "List three primary colors"; asserts each chunk `isinstance str` and non-empty, `>=4` letter chunks, `>=4` distinct reconstructed words, `>=2` dictionary hits. |
| 806 | `test_stream_and_nonstream_both_produce_valid_output` | Medium | FIXED | Same prompt/model, both paths at `temperature=0.0`; whitespace-collapsed stream output must equal whitespace-collapsed non-stream output byte-for-byte (functional-alignment oracle). |
| 952 | `test_temperature_positive_produces_variation` | Medium | FIXED | Per-call seeding of all torch RNGs over 6 seeds; sampling-contract oracle: `temperature=0.0` yields exactly 1 distinct output (seed-invariant), `temperature=1.0` yields `>= len(seeds)-1` distinct and differs from greedy. |

## Six-command verification (pixi default env, run from `D:/ic-wt2/U13-a17`)

| Command | Result |
|---|---|
| `ruff check` | All checks passed! (exit 0) |
| `ruff format --check` | 1 file already formatted (exit 0) |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | No violations (exit 0) |
| `pytest -p no:timeout -p no:cacheprovider` | 53 passed in 87.86s |
