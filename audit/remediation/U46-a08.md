# U46-a08 Remediation Report

Unit U46 owns three test files. All 5 cited findings are FIXED (rewritten into
real gates). No tests deleted.

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_audit3/bridges/test_realcov_04_installer.py:143-180 - test_missing_executable_reports_failure | FIXED | Renamed `test_missing_executable_reports_exact_failure`. Network boundary no longer stubbed: a real `ThreadingHTTPServer` on loopback serves a real Cutter-shaped ZIP; only the registry `download_url` value (data) is repointed. The full `install_tool` pipeline (`_get_latest_release_url` -> `_download_file` over real socket -> extraction -> exe search) runs end to end. Asserts `result.error == "no expected executable found after install: Cutter"` exactly, plus `result.version is None`. |
| tests/test_audit3/bridges/test_realcov_04_installer.py:182-230 - test_present_executable_passes_exe_search | FIXED | Renamed `test_present_executable_advances_to_exact_version_failure`. Same real-HTTP pipeline; ZIP contains `cutter.exe` matching the real Cutter release layout. Asserts `result.error == "post-install version verification failed: Cutter"` exactly and `result.path / "cutter.exe"` is a real file. Also adds `test_download_http_404_reports_download_failure` covering the real 404 error path: asserts `result.error == "Download failed for Cutter"`. |
| tests/test_audit5/u2_bridges_frida/test_frida_bridge_audit5.py:531-568 - test_f0014_message_waiter_does_not_capture_loop_at_construction | FIXED | Renamed `test_f0014_message_waiter_resolves_delivery_loop_not_construction_loop`. Waiter constructed on loop A (closed immediately after). A `threading.Event` barrier holds message delivery until `event._loop` is confirmed bound to loop B; `asyncio.wait_for(timeout=5.0)` raises `TimeoutError` if the construction loop A was captured. Asserts `released is True` and `event.is_set()`. No bare sleeps. |
| tests/test_providers/test_realcov_10_google_safety.py:76-81 - test_candidate_safety_finish_reason_raises | FIXED | Real `google.genai.types` SDK objects. Replaced permissive `match=` regex with exact equality: `str(exc_info.value) == "Response blocked by safety filters: SAFETY"`. Added discriminating negative assertion: SAFETY enum in `prompt_feedback` with STOP candidate must NOT raise, proving the classifier reads `candidates[].finish_reason` specifically. |
| tests/test_providers/test_realcov_10_google_safety.py:125-160 - test_cancel_during_stream_stops_without_error | FIXED | Kept original (real live stream, cancel on first chunk). Added two edge-case gates: `test_double_cancel_is_idempotent` (two back-to-back cancels + post-loop third cancel, all no-op, flag stays True); `test_cancel_after_full_exhaustion_does_not_raise` (full natural stream: `"ready"` in output, `usage.total_tokens > 0`, flag stays False; then cancel flips to True with no in-flight task). All live tests gated on real `GOOGLE_API_KEY`. |

## Verification block (all six green on the touched files)

Files checked:
- tests/test_audit3/bridges/test_realcov_04_installer.py
- tests/test_audit5/u2_bridges_frida/test_frida_bridge_audit5.py
- tests/test_providers/test_realcov_10_google_safety.py

```
ruff check          -> All checks passed!
ruff format --check -> 3 files already formatted
basedpyright        -> 0 errors, 0 warnings, 0 notes
pydoclint           -> No violations
pydocstyle          -> (no output; clean)
pytest -p no:timeout -p no:cacheprovider -> 39 passed, 4 skipped in 8.82s
```

Skips are legitimate environment-capability gates, not masked breakage:
- 4 installer skips: `@pytest.mark.spawns_process` tests (probe_python_package / find_tool_detailed)
  require `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` or the Docker sandbox.
- Google live integration tests ran and passed (API key available in this session).
