# U46-a08 remediation (agent-08.md)

Unit U46 owns three test files. All 5 cited findings are FIXED (rewritten into
real gates). No tests deleted.

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_audit3/bridges/test_realcov_04_installer.py:143-180 - test_missing_executable_reports_failure | FIXED | Renamed `test_missing_executable_reports_exact_failure`. Network boundary is no longer stubbed: a real `ThreadingHTTPServer` on loopback serves a real Cutter-shaped ZIP and the Cutter registry `download_url` (pure data) is repointed at it, so the entire `install_tool` pipeline (`_get_latest_release_url` -> `_download_file` streaming over a real socket -> `_extract_archive` -> post-install exe search) runs end to end. Oracle = production constant `_ERR_NO_EXE_AFTER_INSTALL`. Asserts `result.error == "no expected executable found after install: Cutter"` exactly, plus `result.version is None`. |
| tests/test_audit3/bridges/test_realcov_04_installer.py:182-230 - test_present_executable_passes_exe_search | FIXED | Renamed `test_present_executable_advances_to_exact_version_failure`. Same real-HTTP pipeline; ZIP includes `Cutter-v2.3.0-Windows-x86_64/cutter.exe`. Exe search must accept the real extracted single-subdir layout, then version verification fails (synthetic PE has no version resource). Oracle = production constant `_ERR_NO_VERSION_AFTER_INSTALL`. Asserts `result.error == "post-install version verification failed: Cutter"` exactly (would read the missing-exe message if exe-search regressed), and `result.path / "cutter.exe"` is a real file on disk. New `test_download_http_404_reports_download_failure` drives the real 404 network-error path and asserts `result.error == "Download failed for Cutter"` with `result.path is None`. |
| tests/test_audit5/u2_bridges_frida/test_frida_bridge_audit5.py:531-568 - test_f0014_message_waiter_does_not_capture_loop_at_construction | FIXED | Renamed `test_f0014_message_waiter_resolves_delivery_loop_not_construction_loop`. The waiter (and its `asyncio.Event`) is constructed while loop A is the running loop; loop A is then fully closed (`loop_a.is_closed()` asserted). A brand-new loop B awaits the event; a separate OS thread delivers a real `send` message only after the event has bound to loop B (explicit `threading.Event` barrier + binding poll on `event._loop`, no sleeps). If `_set_event_threadsafe` had captured the construction loop, awaiting on B raises `RuntimeError` (`_LoopBoundMixin` bound-to-different-loop) -> verified manually that this path goes red. Asserts the await released, `event.is_set()`, and that the real `on_message` ran end to end: `messages == [delivered_payload]` and `dispatched == [delivered_payload]`. |
| tests/test_providers/test_realcov_10_google_safety.py:76-81 - test_candidate_safety_finish_reason_raises | FIXED | Real `google.genai.types` SDK objects (real `FinishReason.SAFETY`). Replaced the permissive `match=` with an exact-string oracle: `str(exc) == "Response blocked by safety filters: SAFETY"` (oracle = production constants `_MSG_CONTENT_BLOCKED` + reason name). Added a discriminating second case: a response carrying an empty `prompt_feedback` plus a `STOP` candidate must NOT raise, proving the decision is keyed off `candidates[].finish_reason` rather than the bare presence of a SAFETY enum anywhere. |
| tests/test_providers/test_realcov_10_google_safety.py:125-160 - test_cancel_during_stream_stops_without_error | FIXED | Kept the original (real live stream + cancel-on-first-chunk). Added two edge-case live gates: `test_double_cancel_is_idempotent` (two back-to-back `cancel_request` mid-stream plus a third after the generator closes, asserts no raise, flag stays `True`); `test_cancel_after_full_exhaustion_does_not_raise` (consume the full stream to natural completion, assert cancel flag stayed `False` throughout, `"ready"` appears in output, `get_pending_usage().total_tokens > 0`, then cancel flips the flag to `True` with no in-flight task). All three live tests are gated on a real `GOOGLE_API_KEY` and skip only on the conftest's sanctioned billing/quota precondition. |

## Verification block (all six green on the touched files)

Files:
- tests/test_audit3/bridges/test_realcov_04_installer.py
- tests/test_audit5/u2_bridges_frida/test_frida_bridge_audit5.py
- tests/test_providers/test_realcov_10_google_safety.py

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format <files>           -> applied; ruff format --check <files> -> 3 files already formatted
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> (no output; clean)
pixi run pytest <files> -p no:timeout  -> 36 passed, 7 skipped, 2 warnings
```

Skips are legitimate environment-capability gates, not masked breakage:
- 4 installer skips: pre-existing `spawns_process` tests (not in this unit's findings),
  host-process guard; run under the Docker sandbox.
- 3 Google live skips: the cancellation tests issued real Gemini API calls and hit the
  configured account's quota/spend cap; the conftest billing-skip path fires (not a defect).
  They execute fully when the live account has billing headroom.
