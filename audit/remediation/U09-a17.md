# Remediation U09 - agent-17.md

File owned: `tests/test_core/test_realcov_06_elevation_windows.py`

## Per-finding table

| file:line - test | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_core/test_realcov_06_elevation_windows.py:149` - `test_build_relaunch_command_targets_real_executable` | FIXED | Weak `Path(executable).is_file()`-only test replaced by 5 real gates driving the genuine `_build_relaunch_command` through all three production branches with real state (real `sys.frozen`/`sys.executable`, real `PIXI_*` env vars, real temp executables). Oracle is `subprocess.list2cmdline` (independent stdlib Win32-quoting algorithm) plus known-constant expected strings. Frozen branch asserts `executable == str(binary)` and `params == 'target.exe --analyze "path with space" --elevated'`; pixi branch asserts `executable == str(pixi_exe)` and the exact `run --manifest-path ... --environment ... python -m intellicrack ... --elevated` command with every original arg preserved in order; stale-`PIXI_EXE` boundary asserts fallback to `sys.executable` + `-m intellicrack --headless --elevated`; plain branch asserts `executable == sys.executable` + `-m intellicrack --no-gui sample.bin --elevated`; empty-args boundary asserts `-m intellicrack --elevated`. Each asserts the exact `ELEVATED_FLAG` placement, so swapping/dropping the guard flag or any arg turns the test red. |

## Notes

- No production source modified. `_build_relaunch_command` is exercised through
  the module `__dict__` accessor (typed wrapper) to satisfy basedpyright
  `reportPrivateUsage` without any suppression directive.
- State-mutating context managers (`_frozen_executable`, `_environment`) fully
  save and restore `sys.frozen`, `sys.executable`, and `PIXI_*` env vars, so the
  suite is order-independent (verified under `pytest-randomly`).
- The single SKIP (`test_maybe_elevate_when_already_elevated_returns_false`) is a
  pre-existing legitimate environment-capability skip (non-elevated process would
  raise a real UAC prompt) and is outside this finding's scope.

## Verification block

All commands run in the pixi env against the single owned file:

```
pixi run ruff check tests/test_core/test_realcov_06_elevation_windows.py
  -> All checks passed!
pixi run ruff format tests/test_core/test_realcov_06_elevation_windows.py
  -> formatted; ruff format --check -> 1 file already formatted (no changes)
pixi run basedpyright tests/test_core/test_realcov_06_elevation_windows.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_core/test_realcov_06_elevation_windows.py
  -> No violations
pixi run pydocstyle tests/test_core/test_realcov_06_elevation_windows.py
  -> No violations
pixi run pytest tests/test_core/test_realcov_06_elevation_windows.py -p no:timeout
  -> 9 passed, 1 skipped (legitimate UAC-prompt environment skip)
```
