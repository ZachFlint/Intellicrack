# Remediation U09 - agent-17.md

File owned: `tests/test_core/test_realcov_06_elevation_windows.py`

## Per-finding table

| file:line - test | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_core/test_realcov_06_elevation_windows.py:149` - `test_build_relaunch_command_targets_real_executable` | FIXED | Weak `Path(executable).is_file()`-only test replaced by 5 real gates driving the genuine `_build_relaunch_command` through all three production branches with real state (real `sys.frozen`/`sys.executable`, real `PIXI_*` env vars, real temp executables). Oracle is a hand-verified literal command string (cross-checked against `subprocess.list2cmdline`, the independent stdlib Win32-quoting algorithm) plus known-constant expected strings. Frozen branch asserts `executable == str(binary)` and `params == 'target.exe --analyze "path with space" --elevated'`; pixi branch asserts `executable == str(pixi_exe)` and the full literal `run --manifest-path <m> --environment default python -m intellicrack --gui --project demo --elevated` with every original arg preserved in order; stale-`PIXI_EXE` boundary asserts fallback to `sys.executable` + `-m intellicrack --headless --elevated`; plain branch asserts `executable == sys.executable` + `-m intellicrack --no-gui sample.bin --elevated`; empty-args boundary asserts `-m intellicrack --elevated`. Each asserts the exact `ELEVATED_FLAG` placement, so swapping/dropping the guard flag or any arg turns the test red. |

## What was hardened in this pass

- The pixi test previously asserted `params == subprocess.list2cmdline(...)` plus a
  partial tail slice. It now asserts a fully hand-built literal command string
  (static prefix + static suffix as string literals, manifest path quoted exactly
  as Win32 requires) so the load-bearing oracle no longer routes through the same
  stdlib helper the production code uses; the literal is the gate.
- Falsifiability re-confirmed: a corrupted builder that drops the `--elevated`
  guard flag or substitutes the package/arg order produces a string that differs
  from the literal oracle, turning the assertion red.

## Notes

- No production source modified. `_build_relaunch_command` is exercised through
  the module `__dict__` accessor (typed wrapper) to satisfy basedpyright
  `reportPrivateUsage` without any suppression directive.
- State-mutating context managers (`_frozen_executable`, `_environment`) fully
  save and restore `sys.frozen`, `sys.executable`, and `PIXI_*` env vars, so the
  suite is order-independent.
- The single SKIP (`test_maybe_elevate_when_already_elevated_returns_false`) is a
  legitimate environment-capability skip (a non-elevated process would raise a
  real, non-answerable UAC prompt) and is outside this finding's scope.

## Verification block

All commands run from `D:/ic-wt2/U09-a17` in the main pixi env against the single owned file:

```
ruff check ................. All checks passed!
ruff format + --check ...... 1 file reformatted, then 1 file already formatted (no changes)
basedpyright ............... 0 errors, 0 warnings, 0 notes
pydoclint .................. No violations
pydocstyle ................. exit 0 (no output)
pytest -p no:timeout ....... 9 passed, 1 skipped in 1.28s
```

Note: `basedpyright` is run with `D:/Intellicrack` as cwd so the locked
`.pixi/envs` venv resolves; run from inside the worktree it emits a spurious
`venvPath` warning (exit 3) while still reporting 0 type findings.
