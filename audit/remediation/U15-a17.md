# U15 - agent-17.md remediation (tests/test_hexcore_e2e/test_binary_diff.py)

Unit owns `tests/test_hexcore_e2e/test_binary_diff.py`. All 7 findings addressed
by rewriting each weak/tautological test into a real gate that asserts the
exact diff-result structure (`files_identical`, `total_differences`, the full
`regions` edit-script with exact `offset_a`/`offset_b`/`length`/`diff_type`,
and the `similarity` ratio) against independently-derived oracles.

Authoritative runtime schema (confirmed against the installed
`intellicrack_hexcore` native module): the result dict has exactly four keys -
`files_identical` (bool), `total_differences` (int), `regions` (list of
`{offset_a, offset_b, length, diff_type}`), and `similarity` (float). The
audit's stale "fix" hints referencing `modifications` / `changed_bytes` keys do
not exist in the engine and were not used. `diff_type` tags are `match`,
`modified`, `inserted_a`, `inserted_b`.

Oracles used (independent of the engine):
- `difflib.SequenceMatcher(...).get_opcodes()` to locate the single `replace`
  span (start/length) for the disjoint and partial-difference cases - a
  separate Myers reference implementation, not the engine's own output.
- `_expected_similarity(match_bytes, len_a, len_b)` = `match_bytes / max(len_a, len_b)`
  (1.0 when both empty) - a hand-derivable formula, cross-checked to equal the
  engine's reported ratio (0.0 / 0.5 / 0.666666 / 0.984375 / 1.0) within 1e-5.

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_binary_diff.py:35 - test_identical_bytes_reports_identical | FIXED | `bytes(range(64))` vs itself. Asserts `files_identical is True`, `total_differences == 0`, `similarity == 1.0`, and the exact single-region list `[{0,0,64,"match"}]`. OR-tolerance removed. |
| test_binary_diff.py:48 - test_completely_different_bytes_shows_low_similarity (renamed test_completely_different_bytes_is_single_modified_region) | FIXED | `0x00*64` vs `0xff*64`. difflib oracle confirms one replace span `(0,64)`. Asserts `total_differences == 64`, `similarity == 0.0`, and the exact region list `[{0,0,64,"modified"}]` (full key+value structure validated). |
| test_binary_diff.py:70 - test_diff_bytes_result_is_dict | FIXED (rewritten into test_diff_bytes_match_then_modified_region_layout) | 16-byte identity prefix + `0x11*8` vs same prefix + `0x22*8`. difflib oracle gives replace span `(16,8)`. Asserts exact two-region script `[{0,0,16,"match"},{16,16,8,"modified"}]`, `total_differences == 8`, similarity 16/24. Smoke `isinstance(dict)` gate replaced. |
| test_binary_diff.py:79 - test_diff_bytes_partial_difference_has_modifications (renamed test_diff_bytes_partial_difference_reports_modified_tail) | FIXED | `0x00*50+0xff*50` vs `0x00*100`. difflib oracle gives replace span `(50,50)`. Asserts the single `modified` region equals `{50,50,50,"modified"}`, match prefix total == 50, `total_differences == 50`, `similarity == 0.5`. Tautological `not files_identical or modifications==0` removed. |
| test_binary_diff.py:109 - test_diff_identical_files_reports_identical | FIXED | Two identical 64-byte files on disk. Asserts `files_identical is True`, `total_differences == 0`, `similarity == 1.0`, exact region list `[{0,0,64,"match"}]`. OR-tolerance removed. |
| test_binary_diff.py:125 - test_diff_files_result_has_expected_keys (renamed test_diff_files_result_has_full_schema_with_valid_values) | FIXED | `0x00*100` vs `0x00*50+0xff*50` files. Asserts the key set is exactly the four documented keys, types are correct, diff-byte sum over modified/inserted regions == `total_differences` == 50, and `similarity == 0.5`. "at least one recognized key" replaced with full-schema gate. |
| test_binary_diff.py:150 - test_diff_files_detects_known_modification_region | FIXED | `0x00*100` vs `0x00*50+0xff*50` files. difflib oracle gives replace span `(50,50)`. Asserts `regions[0]` is the exact 50-byte match prefix and the covering modified region equals `{50,50,50,"modified"}` (covers offset 50..100 fully), `total_differences == 50`, `similarity == 0.5`. "any non-match at offset>=50" replaced. |

## Additional coverage added (edge / error paths)

- `test_diff_empty_vs_empty_is_identical` / `test_diff_empty_files`: empty
  inputs -> `files_identical True`, `total_differences 0`, `similarity 1.0`,
  empty regions list.
- `test_diff_bytes_one_empty_is_full_insertion`: `b"ABC"` vs `b""` -> exact
  `[{0,0,3,"inserted_a"}]`, `total_differences 3`, `similarity 0.0`.
- `test_diff_files_truncated_tail_is_inserted_a`: `0xaa*200` vs `0xaa*100` ->
  exact `[{0,0,100,"match"},{100,100,100,"inserted_a"}]`, similarity 0.5.
- `test_diff_files_single_byte_change`: single byte at offset 32 -> exact
  three-region script, `total_differences 1`, similarity 63/64.
- `test_diff_files_missing_path_raises_io_error`: missing input file ->
  `pytest.raises(OSError, match="Failed to read")`, asserting the failure is
  surfaced, not swallowed into a dict.

## Verification (all six green)

Commands run in the pixi env on the single touched file
`tests/test_hexcore_e2e/test_binary_diff.py`:

- `pixi run ruff check tests/test_hexcore_e2e/test_binary_diff.py` -> All checks passed!
- `pixi run ruff format tests/test_hexcore_e2e/test_binary_diff.py` then `--check` -> 1 file already formatted (no changes)
- `pixi run basedpyright tests/test_hexcore_e2e/test_binary_diff.py` -> 0 errors, 0 warnings, 0 notes
- `pixi run pydoclint tests/test_hexcore_e2e/test_binary_diff.py` -> No violations
- `pixi run pydocstyle tests/test_hexcore_e2e/test_binary_diff.py` -> (no output; clean)
- `pixi run pytest tests/test_hexcore_e2e/test_binary_diff.py -p no:timeout` -> 13 passed
