# Remediation U32 (agent-17.md)

Unit owns two test files (whole-file ownership):

- `tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py`
- `tests/test_ui/log_viewer/test_model.py`

## Per-finding outcomes

| File:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py:261` - `test_constructor_receives_callable_print_sink` | FIXED | Replaced the `callable(print_sink)`-only check (against an interpreter double) with `TestPrintSinkWiredToOutputWidgetEndToEnd`, which drives the **real** `HexPatInterpreter` (no monkeypatch/double) through the production `_apply_via_interpreter`. A real pattern `fn __ping() { builtin::std::io::print("hello-ui-print-sink"); return 0; }; u8 __mark @ __ping();` evaluates against a `HexDocument`-shaped stub; oracle is the literal printed line. Asserts `print_output_widget().toPlainText().splitlines() == ["hello-ui-print-sink"]` and `error_display_widget()` is empty. Added negative companion `test_pattern_without_print_leaves_output_widget_empty` (real `u8 silent @ 0x00;` -> widget stays empty), proving widget content originates from `std::print` and not from apply machinery. Mutation check: setting `print_sink=None` in production made the gate go red. |
| `tests/test_ui/log_viewer/test_model.py:79` - `test_column_data_for_display_role` | FIXED | Extras column now driven with mixed JSON types `widget="x", count=3, active=True`. Oracle is the stdlib `json` module (a different trusted serializer from production `extras_to_compact_json`). Asserts `json.loads(extras_text) == {"widget": "x", "count": 3, "active": True}`, that `count` round-trips as `int`, `active is True`, the text is single-line (no `\n`/`\t`), and the `UserRole` raw record equals the full expected `LogRecordDict`. Replaces the prior weak `'"widget"' in extras_text` substring check. |
| `tests/test_ui/log_viewer/test_model.py:154` - `test_background_role_tints_warn_error_critical_only` | FIXED | Replaced `isinstance(..., QColor)`-only checks with exact-value assertions per level using the published severity palette as the independent constant oracle: DEBUG/INFO -> `None`; WARNING -> `QColor(60, 48, 16)`; ERROR -> `QColor(70, 24, 24)`; CRITICAL -> `QColor(70, 16, 56)`. Also asserts the three tinted colors are mutually distinct, so a swapped/inverted mapping fails. Mutation check: swapping WARNING's background to ERROR's value made the gate go red. |

## Verification (project env, only the two touched files)

```
pixi run ruff check    tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py   -> All checks passed!
pixi run ruff format   tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py   -> 2 files unchanged
pixi run ruff format --check ...                                                                                              -> 2 files already formatted
pixi run basedpyright  tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py   -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint     tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py   -> No violations
pixi run pydocstyle    tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py   -> (no findings)
pixi run pytest        tests/test_audit7/u12_hexpat_print_sink/test_ui_print_sink.py tests/test_ui/log_viewer/test_model.py -p no:timeout   -> 19 passed
```

All six checks genuinely green. No production source changed (a temporary mutation was used only to confirm falsifiability and was reverted via `git checkout`).
