# Remediation U72-a06 (agent-06.md)

Unit owns two whole test files:

- `tests/test_core/test_logging_audit6.py`
- `tests/test_hexcore_e2e/test_bridge_base_convert.py`

All 5 audit findings on these files were **FIXED** (rewritten into real,
falsifiable gates). No tests deleted.

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_logging_audit6.py:60 - test_default_falls_back_to_cwd_when_no_config | FIXED | Renamed `test_no_config_writes_logs_under_cwd_logs`. Drives real `setup_logging(file_enabled=True, json_file=True)` with no `log_dir` under a `monkeypatch.chdir(tmp_path)` cwd and an absent config dir. Asserts a real `intellicrack.log` is created in `Path.cwd()/logs`, parses the JSON line, and asserts the emitted record's `event`, `probe is True`, and `level == "info"` (independently-known structlog fields, not the function's return value). |
| test_logging_audit6.py:81 - test_default_uses_configured_logs_directory | FIXED | Renamed `test_configured_logs_directory_receives_real_logs`. Builds a real `Config` saved via `Config.save` to a real `config.toml`, points production `get_config_dir` at it. Asserts logging actually writes `intellicrack.log` into the configured `logs_directory` AND that no file appears under cwd/logs, plus the parsed record's `logger == "intellicrack.audit6"`. |
| test_logging_audit6.py:115 - test_default_uses_state_after_setup_logging | FIXED | Renamed `test_state_after_setup_logging_drives_real_log_files`. First `setup_logging` records the state dir; second `setup_logging` with no `log_dir` resolves via state and writes a real log file there. Parses the JSON record and asserts `probe is True`, proving logging used the state-resolved directory, not just that state was set. |
| test_logging_audit6.py:139 - test_setup_logging_records_resolved_dir | FIXED | Renamed `test_explicit_log_dir_is_written_and_recorded`. Asserts end-to-end that the real `intellicrack.log` appears in the explicit target with the emitted JSON record (`event`/`probe`/`level`) AND that `_logger_state.configured_log_dir` equals the target. |
| test_bridge_base_convert.py:107 - test_result_has_base_keys | FIXED | Renamed `test_result_full_structure_for_42`. Replaces key-presence-only checks with a full-dict equality assertion for input `42` against independently-known constants (`0x2a`, `0o52`, `0b101010`, all int/uint widths, and IEEE-754 `float32_le == "5.885453550164232e-44"`, `float64_le == "2.08e-322"`), so any representation regression breaks the gate. |

Added coverage dimension beyond the findings: `test_file_logging_disabled_writes_no_log_file`
asserts that `file_enabled=False` produces no `intellicrack.log` (edge/error path on the
config-to-sink bridge). The hex file's pre-existing `_run` PEP 695 type-parameter error
was fixed (unified on `T`, removed the unused `_T` TypeVar) so basedpyright is clean.

## Falsifiability check

Temporarily corrupting `_default_log_dir` to always return `Path.cwd()/"logs"`
(ignoring state and config) made `test_configured_logs_directory_receives_real_logs`
and `test_state_after_setup_logging_drives_real_log_files` FAIL (2 failed), then the
source was reverted (git diff clean) and all tests pass again.

## Verification block

Commands run in the project env on only the touched files:

```
pixi run ruff check tests/test_core/test_logging_audit6.py tests/test_hexcore_e2e/test_bridge_base_convert.py
  -> All checks passed!
pixi run ruff format tests/test_core/test_logging_audit6.py tests/test_hexcore_e2e/test_bridge_base_convert.py
pixi run ruff format --check  (same files)
  -> 2 files already formatted (no changes)
pixi run basedpyright (same files)
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint (same files)
  -> No violations
pixi run pydocstyle (same files)
  -> (no output; clean)
pixi run pytest (same files) -p no:timeout -p no:cacheprovider
  -> 15 passed in 3.23s
```

All six checks genuinely green; tests PASS (none skipped/xfailed to hide breakage).
The hexcore-dependent file runs (native module is built) - not skipped.
