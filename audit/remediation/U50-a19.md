# U50-a19 Remediation

## Findings and Actions

| Ref | Severity | Status | Action |
|-----|----------|--------|--------|
| test_realcov_14a_x64dbg_panel.py - render-method tests using _X64DbgPanelProbe | Medium | FIXED | Added `test_apply_module_exports_renders_exact_cell_values`: scans detail table column 0 for LoadLibraryA row, then asserts column 1 (ordinal) is a non-empty integer string and column 2 (address) starts with `0x`. Added `detail_cell(row, col)` accessor to the probe class. Catches silent column population failures. |
| test_realcov_01_pe_format_real_binaries.py - pefile oracle circular comparison | Medium | FIXED | Added four independent-constant tests: `test_e_lfanew_kernel32_known_constant` (0x100, verified from raw `struct.unpack`), `test_coff_machine_is_amd64_known_constant` (0x8664 from PE/COFF spec), `test_optional_header_known_constants_kernel32` (ImageBase=0x180000000 from raw bytes; is_pe64=True from PE32+ spec constant), `test_section_table_known_names_kernel32` (.text/.rdata from MSVC ABI). These are not derived from pefile or test output - they would catch both pefile and helper returning the same wrong value. |
| test_hexpat_stdlib.py:32-68 - TestMemoryFunctions weak assertions | Medium | FIXED | Strengthened `test_read_unsigned_1_byte` to assert exact offset/size/raw_bytes/display_value. Added `_assert_full_field_structure` helper validating all mandatory dict keys with correct types. Added boundary tests: `test_mem_read_unsigned_beyond_end_raises` and `test_mem_read_unsigned_overflow_raises` verify `HexPatRuntimeError` on OOB access. Added `test_execute_bytes_zero_length_data_raises`. Added `test_execute_bytes_max_u8_full_structure` and `test_execute_bytes_u32_full_structure` asserting exact raw_bytes and display_value. |
| test_real_bridge_schemas.py:127-155 - circular schema oracle | Medium | FIXED | Added `test_non_array_parameters_carry_correct_type_in_schema`: verifies every non-array param across all 7 bridges and all 6 schema formats carries a valid primitive type string. Added `test_required_parameters_in_schema_match_tool_definition`: iterates all bridge functions with required params and verifies each appears in the schema's `required` array - handles all three envelope formats (Anthropic/Google top-level name vs OpenAI nested `function.name`). |

## Verification Results

- **ruff**: 0 findings
- **ruff format**: all 4 files already formatted
- **basedpyright**: 0 errors, 0 warnings (venvPath warning is harmless noise)
- **pydoclint**: no violations
- **pydocstyle**: no violations
- **pytest**: 90 passed in 9.91s
