# U03-a19 Remediation — tests/test_audit4/a3_qemu_sandbox/test_qemu_sandbox.py

Worktree: `D:/ic-wt2/U03-a19` (branch `wf2/U03-a19`, intact for merge).

The three audited tests are real falsifiable gates that drive the production
QEMU-sandbox helpers (`_ensure_agent_connected`, `_poll_for_result`,
`_generate_execution_script`) end to end with real sockets, real files, and real
subprocess execution. No mock stubs the operation under test in any of the three
findings. All production helpers exist in `src/intellicrack/sandbox/qemu.py` with
signatures matching the test harness, so the tests exercise installed-main source.

## Per-finding results

| Ref | Severity | Status | Real input + independent oracle + assertion |
| --- | --- | --- | --- |
| `:378-422` `TestF0002AgentConnectCalled.test_agent_connect_invoked_during_start` | Critical | FIXED | Real loopback `asyncio` server (ephemeral port) + real `GuestAgentClient` driven through production `_ensure_agent_connected` (no patches). Oracle: server's accepted-peer counter + `agent.is_connected`. Asserts `is_connected is True` and exactly one socket accepted; companion test reserves-then-frees a dead port and asserts `pytest.raises(SandboxError)` with `is_connected is False`. Drops the old approach that patched `is_available`/`_build_qemu_command`/`_connect_and_verify_qmp`/`_cleanup` and called `connect()` outside `start()`. |
| `:439-494` `TestF0003PollForResult.test_poll_reads_exit_code_from_result_file` | High | FIXED | Real `_poll_for_result` over real result files. Oracle: documented contract (stripped all-digit body = decimal exit code; else `-1`). Parametrized well-formed (`0,1,42,255,'007'->7,'  13  '->13`), parametrized malformed (`'','abc','0x42','-1','1.5','exit: 0','42 43'` -> `-1`), and `'garbage\n7'` -> `-1` pinning whole-file-strip (not last-line). Asserts the exact 3-tuple incl. empty stdout/stderr without sidecars. |
| `:591-630` Windows/Linux script-redirect tests | Medium | FIXED | Production-generated script rewritten onto a real host out-dir and executed by `cmd.exe` / `bash` with known tokens (`HELLO_OUT`/`HELLO_ERR` exit 3; `LINUX_OUT`/`LINUX_ERR` exit 5). Oracle: real OS streams. Asserts stdout token only in `.stdout`, stderr token only in `.stderr` (no crossover), and result file records the exact exit code. Validates real `1>`/`2>` + `echo %ERRORLEVEL%`/`$?` syntax, not substring presence. |

## Six-command verification (cwd `D:/ic-wt2/U03-a19`, main pixi env)

| Command | Result |
| --- | --- |
| `ruff check` | All checks passed |
| `ruff format --check` | 1 file already formatted (no changes) |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | Clean (no output) |
| `pytest -p no:timeout -p no:cacheprovider` | 52 passed in 29.39s (0 skipped/xfailed) |

All three findings are genuine gates: corrupting the covered production code
(stop awaiting `connect`, parse the result file's last line / as hex / default 0,
or break the `1>`/`2>` redirection) makes the respective test go red.
