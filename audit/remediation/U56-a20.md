# U56-a20 Remediation Report

## Findings

| Ref | Severity | Status | Oracle |
|-----|----------|--------|--------|
| `tests/test_audit7/sandbox_windows/test_launch_failure_detection.py:23 - _detect_returns_rpc` | Low | FIXED (no-op) | Helper function, not a test; consuming tests make real assertions on exception type and message content |
| `tests/test_bridges/test_hex_editor_top_audit1.py:192 - _PatchesOnlyDoc` | Low | FIXED (no-op) | Helper class, not a test class; consuming tests assert OverflowError with exact messages |
| `tests/test_ui/test_state_persistence.py:142 - test_restore_tab_state_tab_openers_keys` | Medium | FIXED | Passes all 9 registered keys in `tab_names`; asserts `find_tab_by_title(key) >= 0` and `tabText(idx) == key` for each; asserts `tab_widget.count() >= 9` |
| `tests/test_ui/test_theme_manager.py:193 - test_apply_invalid_theme_uses_default` | Medium | FIXED | Captures `theme_changed` signal; asserts `result is True`, `current_theme == DEFAULT_THEME`, signal emitted exactly once with `DEFAULT_THEME`, and `QApplication.styleSheet() == get_stylesheet(DEFAULT_THEME)` |

## Changes Made

- `tests/test_ui/test_state_persistence.py`: Rewrote `test_restore_tab_state_tab_openers_keys` to pass the actual 9 registered keys through `tab_names`, then assert each tab is open (`idx >= 0`) and `tabText(idx) == key`. Removed the non-existent "Binary" key and the vacuous `isinstance(idx, int)` assertion.

- `tests/test_ui/test_theme_manager.py`: Rewrote `test_apply_invalid_theme_uses_default` to verify the fallback applies the stylesheet to `QApplication` and emits `theme_changed` with `DEFAULT_THEME`. Added `QApplication` import at module level.

## Six-Command Results

- ruff check: PASS (0 findings)
- ruff format: PASS (no changes after format pass)
- basedpyright: PASS (0 errors, 0 warnings, 0 notes)
- pydoclint: PASS (0 violations)
- pydocstyle: PASS (0 violations)
- pytest: PASS (128 passed in 7.43s)
