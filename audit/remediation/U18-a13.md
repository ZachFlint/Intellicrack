# U18-a13 Remediation

Worktree: `D:/ic-wt2/U18-a13` (branch `wf2/U18-a13`, left intact).

All four audit findings on this unit were rewritten into real falsifiable
gates. No test was deleted; no production source under `D:/Intellicrack/src`
was modified. Real fixtures (built PE/ELF binaries, on-disk log files, the
native `intellicrack_hexcore` module, a failing `ghidra_bridge` RPC double)
exercise the real code paths.

## Per-finding results

| Finding (file:line - test) | Severity | Violation | Status | Real input + independent oracle + exact assertion |
| --- | --- | --- | --- | --- |
| `test_bridges/test_ghidra_f11_audit.py:64-99` - `test_f11_define_structure_logging` & `test_f11_create_function_logging` | High | Mock-the-thing-under-test (`patch` on `_logger`) | FIXED | Real `GhidraBridge` + failing RPC double drives the actual `_execute_remote` failure path. The `_logger` patch is removed; `structlog.testing.capture_logs` captures the genuine warning. Asserts exact `ToolError` text, the chained `__cause__` (`ToolError` -> `RuntimeError("simulated_remote_error")`), and the captured record (`event`, `log_level=="warning"`, `struct_name`/`address`, `error`). Adds not-connected paths asserting the `ghidra_not_connected` error record. |
| `test_core/test_script_gen.py:343` (file-level) | Medium | Weak-assertion-on-rich-output (enum value == constant only) | FIXED | New `test_script_manager_save_uses_language_extension_on_disk` drives the real `ScriptManager.save_script` persistence path and asserts the on-disk file's exact `name`/`suffix`/`parent`/content for every language (oracle = conventional per-tool extension, independent of the production map). New `test_script_context_to_prompt_full_structure_exact_layout` asserts the whole rendered prompt equals a hand-written expected string, pinning section order, headers, `0xDEADBEEF (3735928559)` formatting, strategy-description expansion, and the Frida API-reference footer. |
| `test_hexcore_e2e/test_bridge_bit_ops.py:53-93` - `test_get_bit_returns_correct_values` & similar | Medium | Weak-assertion-on-rich-output (no determinism / layout / boundary coverage) | FIXED | Real Rust-backed `HexDocument` via `HexEditorBridge` over an 8-byte adversarial pattern. Asserts every bit of every byte at every offset equals the independent LSB-0 oracle `(byte >> i) & 1` (catches off-by-one/endianness/offset bugs); `get_bit` determinism across 5 repeats; builds a full byte by setting bits 0..7 -> `0x01..0x80` with read-back; clear-only-that-bit and idempotent-set; double-toggle restores the byte and alternates the return; `bit_index` in `{8,9,-1,-8,16,64}` consistently raises `ValueError`; no-document raises `RuntimeError`. |
| `test_sandbox/test_log_parsers.py:127-177` - `parse_file_log` & `parse_registry_log` | Medium | Weak-assertion-on-rich-output (single simple lines only) | FIXED | New `TestParseFileLogEdgeCases` / `TestParseRegistryLogEdgeCases` write real malformed on-disk logs and assert the exact documented behaviour: unescaped pipe in path/key shifts the remainder into the optional fields; colon-bearing Windows paths preserved; non-numeric/negative size collapses to `None`; operation-alias normalization (`move`->`renamed`, `write`->`modified`, unknown->`modified`); non-ASCII UTF-8 key/value round-trip; extra trailing field ignored; empty middle field collapses to `None`; interleaved valid/malformed lines yield only well-formed records in original order. |

## Six-command verification

Run from `D:/ic-wt2/U18-a13` with the main pixi env
(`--manifest-path D:/Intellicrack/pyproject.toml`):

| Command | Result |
| --- | --- |
| `ruff check` (4 files) | All checks passed |
| `ruff format` + `ruff format --check` (4 files) | 4 files already formatted (no changes on re-check) |
| `basedpyright` (4 files) | 0 errors, 0 warnings, 0 notes |
| `pydoclint` (4 files) | No violations |
| `pydocstyle` (4 files) | No findings |
| `pytest -p no:timeout -p no:cacheprovider` (4 files) | 186 passed |

All six checks are genuinely green. No skips/xfails mask breakage; the
hexcore tests run against the built native module and the ghidra tests run
the real bridge error path.
