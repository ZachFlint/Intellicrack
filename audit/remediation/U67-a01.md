# U67-a01 Remediation Report

Unit U67-a01 owns four test files with five audit findings. All five findings
were previously addressed in prior remediation commits. Verification confirms
all test files pass every quality gate.

## Per-finding disposition

| file:line - test | status | real input / oracle / exact assertion |
| --- | --- | --- |
| `tests/test_audit4/c3_hex_data_inspector/test_data_inspector.py:219` - `test_region_filter_wired_to_text_changed` (misattributed in workplan) | FIXED | The test never resided in this file. The file was fully rewritten into real falsifiable gates for F-0003/F-0011/F-0016. The region-filter signal wiring is covered by the correctly-placed test at `tests/test_audit4/b4_memory_tab/test_memory_tab.py:275`, which asserts `receivers==1` and verifies three `setText` transitions update row visibility via the signal alone without calling the slot. |
| `tests/test_core/test_process_manager_audit6.py:127` - `test_register_rejects_dead_pid` | FIXED | Dead PID confirmed dead via the production `_pid_exists` oracle before use. `pytest.raises(ValueError, match="does not exist")` checks exception type AND message text. `str(dead_pid) in message` asserts the rejected PID is named. Registry membership verified absent after rejection. |
| `tests/test_credentials/test_env_loader_roundtrip_live.py:70` - `test_quote_then_parse_round_trip` | FIXED | Hand-derived `_EXPECTED_QUOTED` dict provides an independent oracle for 24 value shapes. `quoted == expected_quoted` asserts exact serialized form before the round-trip parse, breaking the symmetry loophole where a paired quoter/parser defect would silently pass a pure round-trip. |
| `tests/test_hexcore_e2e/test_bookmarks.py:18` - `test_list_bookmarks_empty_on_fresh_doc` | FIXED | `type(bookmarks) is list` asserts the concrete runtime type (not any empty-equal iterable). Post-add re-list asserts `populated == [(0, 1, "first", "#FF0000")]`, proving the accessor is not a permanently-empty stub. |
| `tests/test_hexcore_e2e/test_bookmarks.py:37` - `test_add_bookmark_returns_index` | FIXED | Three bookmarks added; `len({idx_alpha, idx_beta, idx_gamma}) == 3` asserts pairwise-distinct indices; `remove_bookmark(idx_beta) is True`; surviving labels `== ["alpha", "gamma"]` asserts exactly the middle bookmark was removed and no other. |

## Verification Results

```
pixi run ruff check <files>            -> All checks passed!
pixi run ruff format --check <files>   -> 4 files already formatted
pixi run basedpyright <files>          -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>             -> No violations
pixi run pydocstyle <files>            -> (no output) clean
pixi run pytest <files> -p no:timeout  -> 87 passed in 6.73s
```

All six gates green. No test is skipped or xfailed.
