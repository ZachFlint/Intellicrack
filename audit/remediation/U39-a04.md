# U39-a04 Remediation Report

## Summary

All 6 findings from agent-04.md resolved. 158 tests pass, 2 skip (Unix-only + missing Anthropic key).

## Findings

| # | File | Finding | Severity | Status | Resolution |
|---|------|---------|----------|--------|------------|
| 1 | test_follow_cursor_debounce.py | `TestProductionOnDisassembleReadsCorrectOffset` accesses `_on_disassemble`, `_disasm_last_dispatched_offset`, `_disasm_in_flight` directly from outside class hierarchy (`reportPrivateUsage` x8) | High | FIXED | Added `last_dispatched_offset_for_test()` and `disasm_in_flight_for_test()` public accessor methods to `_DebouncingHarness`; test class uses only public interface |
| 2 | test_process_manager.py | `cast(int, tp.process.pid)` is `reportUnnecessaryCast` since `.pid` is already `int` | Medium | FIXED | New test written without any `cast`; uses `tp.pid` directly after `None` guard |
| 3 | test_sandbox/test_analysis.py:734 | `DllLoadEvent` constructed with only 6 positional args; missing required `event_id` and `payload_schema` fields (`reportCallIssue`) | High | FIXED | Added `event_id=10, payload_schema=""` to all `DllLoadEvent` constructors in `test_dll_events` |
| 4 | test_process_manager.py | `test_run_tracked_registers_then_unregisters_process` -- observer thread races process completion, `observed_pids=[]` | Critical | FIXED | Background thread runs `run_tracked(sleep 2s)`; tight 10ms polling loop exits on first non-empty `get_all_tracked()` snapshot; `threading.Event` confirms observation before `join`; deterministic |
| 5+6 | test_sandbox/test_analysis.py | `TestRealisticTrafficC2Detection` -- `ts_offset(630)` produces invalid ISO timestamp `2026-03-15T10:00:630`; beaconing not detected | Critical | FIXED | `TestRealisticTrafficC2Detection` added using `_iso_ts(datetime, float)` helper (real `datetime` arithmetic); all timestamps valid ISO-8601; 20 connections at 30s intervals (CV≈0.05) detected; irregular normal traffic (CV>>0.3) not flagged |
| 7 | test_providers/test_credential_loading.py | `test_loader_finds_env_file` -- `env_file_path` fixture resolves to worktree `.env` which does not exist | High | FIXED | Test rewritten to use `tmp_path` fixture; creates minimal `.env` file; asserts `loader.env_path == env_file` and `loader.env_path.exists()` |

## Check Results

| Check | Result |
|-------|--------|
| ruff check | PASS (0 findings) |
| ruff format --check | PASS (4 files already formatted) |
| basedpyright | PASS (0 errors, 0 warnings, 0 notes) |
| pydoclint | PASS (no violations) |
| pydocstyle | PASS (no findings) |
| pytest | PASS (158 passed, 2 skipped in 11.44s) |
