# U04-a11 Remediation

Worktree: `D:/ic-wt2/U04-a11` (branch `wf2/U04-a11`)

Files owned and modified:

- `tests/test_audit4/c8_hex_signatures_offload/test_signatures_offload.py`
- `tests/test_sandbox/test_sandbox_bridge.py`

## Per-finding results

| # | Ref | Severity | Status | Real input + independent oracle + exact assertion |
|---|-----|----------|--------|----------------------------------------------------|
| 1 | `test_signatures_offload.py:173` `TestReadDocumentForScan.test_bytes_passthrough` | Low | FIXED | The vacuous identity test (stub built with the exact answer bytes) is replaced by `test_reads_full_document_with_correct_offset_and_length` and `test_partial_read_would_not_satisfy_full_content`. Input is a non-trivial `bytes(range(256)) * 2` document plus a length-capping document; the oracle is the test-chosen content and the known full length (512). Asserts `result == content`, `read_call_count == 1`, `read_args == [(0, 512)]`, and (capped doc) `requested_lengths == [len(content)]` with `result == content[:4]`. A no-op or wrong offset/length implementation now fails (confirmed RED by patching the expected `read_args`). |
| 2 | `test_signatures_offload.py:356` `TestUIThreadAllocationBudget.test_ui_thread_does_not_materialise_large_document` | Medium | FIXED | Removed the `if harness.worker() is not None` guard that let the whole gate no-op. Now unconditionally asserts a worker is spawned, `read_call_count == 0` right after `trigger_scan()` (no UI-thread read), the worker finishes, `read_call_count >= 1`, `read_thread_ids` is non-empty, every read thread id differs from the main thread id, and `tracemalloc` peak UI-thread allocation for a real 50 MiB document stays under 1 MiB. A failure to spawn the worker or a UI-thread read now fails the test. |
| 3 | `test_sandbox_bridge.py:50` fixture-heavy tests (InMemorySandbox) | Critical | FIXED | Added `TestBridgeRealSandboxLifecycle` (8 tests, `@pytest.mark.integration`/`@pytest.mark.spawns_process`). A real `SandboxBridge` is attached to `_RealLocalManager`, which wraps the conftest `LocalProcessSandbox` that genuinely executes `sys.executable` as an OS subprocess and reports file changes by diffing its work directory. The oracle is a deterministic driver script whose payload bytes, stdout text, exit code, and dropped-file name are test-chosen. Asserts the bridge surfaces exact stdout, exit code (0 and 9), `success`/`error` result, the genuinely-created artefact (`created` op, exact path, `size == len(payload)`, on-disk SHA-256 match), `bridge.state` flags; a real `copy_to` -> `copy_from` byte round-trip through the workdir; `extract_iocs` returning IPv4 `203.0.113.50` parsed from a genuinely-observed dropped-file path (confirmed RED by patching the expected IP); and `ToolError` on missing binary, missing sandbox file, and no-report-before-run. The fast in-memory fixture tests are retained as unit coverage. |

## Six-command verification (from `D:/ic-wt2/U04-a11`, main pixi env)

| Command | Result |
|---------|--------|
| `ruff check` | All checks passed! |
| `ruff format --check` | 2 files already formatted |
| `basedpyright` | 0 errors, 0 warnings, 0 notes |
| `pydoclint` | No violations |
| `pydocstyle` | clean (no output) |
| `pytest -p no:timeout -p no:cacheprovider` | 102 passed in 7.96s |

Notes:

- The new finding-3 tests genuinely spawn a short-lived `python` subprocess and
  are marked `spawns_process`. The repo conftest auto-skips such tests on the
  host unless `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` is set (the helper's
  documented host-debug escape hatch) or the run is inside the Docker sandbox.
  The pytest verification above was run with that env var set so the tests
  actually execute and PASS (not skip); in the standard in-container run they
  pass without the env var.
- No production source under `D:/Intellicrack/src` was modified.
