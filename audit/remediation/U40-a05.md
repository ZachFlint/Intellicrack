# U40-a05 Remediation Report

## Findings and Resolutions

| # | File:Line - Test | Severity | Status | Oracle / Fix |
|---|-----------------|----------|--------|-------------|
| 1 | `tests/test_bridges/test_x64dbg_events.py:104` - `test_unregister_nonexistent_does_not_raise` | Low | FIXED | Added precondition `assert len(bridge.event_callbacks)==0` and postcondition `assert len(bridge.event_callbacks)==0`; test now fails if `unregister_event_callback` corrupts the empty list |
| 2 | `tests/test_hexpat/test_lexer.py:144` - `test_unterminated_string_raises` | Low | FIXED | Added `getattr(lexer, '_pos')==0` and `getattr(lexer, '_line')==1` precondition checks before `tokenize()`; confirms error occurs during tokenization at the expected initial state |
| 3 | `tests/test_providers/test_realcov_11_model_loader.py:130` - `test_fp16_matches_two_bytes_per_param_with_overhead` | Medium | FIXED | Calls `_estimate_parameter_count('meta-llama/Llama-3.2-1B-Instruct')` and asserts it equals `1_000_000_000`; uses that real value to compute expected memory, verifying both the model-id mapping and the arithmetic formula |
| 4 | `tests/test_ui/log_viewer/test_app_integration.py:46` - `test_main_window_installs_qt_log_handler` | Low | FIXED | Added `_assert_handler_wired()`: asserts handler is in `root.handlers`; connects a slot and emits a probe log record; asserts the probe message appears in `received`, proving the handler is live |
| 5 | `tests/test_ui/log_viewer/test_app_integration.py:65` - `test_log_viewer_lazy_construction` | Low | FIXED | Added `first.isVisible()` assertion; second `open_log_viewer()` call identity check proves caching; also patched `_restore_window_state` to fix pre-existing `ToolError` that was failing all tests in this file |
| 6 | `tests/test_ui/test_realcov_14b_script_manager.py:77` - `test_save_persists_real_content` | Medium | FIXED | Added three error-path tests to `TestScriptManagerRealPersistence`: disk persistence verified by reading actual file after cache clear; empty-name blocked-save asserts warning dialog and empty backend; read-only directory confirms in-memory entry survives despite disk write failure |

## Verification Results

```
ruff check:     PASS (0 findings)
ruff format:    PASS (no changes needed)
basedpyright:   PASS (0 errors, 0 warnings - venvPath warning is harmless noise)
pydoclint:      PASS (0 violations)
pydocstyle:     PASS (0 violations)
pytest:         PASS (69 passed, 1 skipped in 12.72s)
```

The 1 skip is `TestLoadModelForXpuErrorPath::test_raises_runtime_error_when_xpu_unavailable` which correctly skips when XPU hardware is present (the test itself has a guard for this legitimate skip).
