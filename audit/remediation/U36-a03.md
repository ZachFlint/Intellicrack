# Remediation U36-a03 — tests/test_core/test_config.py

Source audit: `audit/agent-03.md` (28 findings). All 28 accounted for. Default
counts/values used as oracles are derived from the documented dataclass
specifications and the `_default_providers()` / `_default_tools()` factories in
`src/intellicrack/core/config.py` (and the `ProviderName`/`ToolName`/
`ConfirmationLevel` enum definitions in `src/intellicrack/core/types.py`), not
captured from a live run.

## Per-finding table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
|---|---|---|
| test_config.py:81 - test_provider_config_defaults | FIXED | `ProviderConfig()` asserted field-for-field: enabled True, api_base None, default_model None, timeout 120, max_retries 3. |
| test_config.py:91 - test_tool_config_defaults | FIXED | `ToolConfig()` asserts all five fields incl. previously-missing `path is None` and `port is None`. |
| test_config.py:100 - test_sandbox_config_defaults | FIXED | `SandboxConfig()` asserts all four documented defaults (enabled, timeout 300, mem 2048, network False). |
| test_config.py:109 - test_ui_config_defaults | FIXED | `UIConfig()` asserts every field (theme system, font JetBrains Mono, size 11, show_tool_calls True); confirmed dataclass has no other fields. |
| test_config.py:118 - test_session_config_defaults | FIXED | `SessionConfig()` asserts all three documented fields; confirmed no additional fields exist. |
| test_config.py:126 - test_log_config_defaults | FIXED | `LogConfig()` asserts all seven documented fields incl. json_file True. |
| test_config.py:138 - test_config_default | FIXED | Exact provider keyset+count (8), tool keyset+count (5), OLLAMA api_base/timeout, LOCAL_TRANSFORMERS model/retries, GHIDRA port/timeout, PROCESS auto_install False, and exact top-level section set. |
| test_config.py:149 - test_config_ensure_directories | FIXED + edges | Happy path retained; added nested-parents creation, idempotent run preserving a sentinel file, and file-collision raising `FileExistsError`/`NotADirectoryError`. |
| test_config.py:166 - test_config_get_provider_config | FIXED | Asserts returned object IS the stored ANTHROPIC instance with exact field values, and is distinct from OLLAMA's config (different api_base). |
| test_config.py:173 - test_config_get_provider_config_unknown | FIXED | Two sequential lookups of a missing provider return independent `ProviderConfig()` instances; mutating one does not affect the next (no shared cache). |
| test_config.py:180 - test_config_get_tool_config | FIXED | Asserts returned object IS stored GHIDRA config (port 4768, timeout 120) and differs from X64DBG (port None, timeout 30). |
| test_config.py:187 - test_config_is_provider_enabled | FIXED | Config with ANTHROPIC disabled + OPENAI enabled: asserts False/True distinction plus default-config True. |
| test_config.py:193 - test_config_is_tool_enabled | FIXED | Config with GHIDRA disabled + X64DBG enabled: asserts False/True distinction plus default-config True. |
| test_config.py:199 - test_config_to_dict_round_trip | FIXED | Builds Config with non-default values across every section, serialises via `_to_dict`, deserialises via `from_dict`, asserts each section equals the original dataclass. |
| test_config.py:212 - test_config_from_dict_empty | FIXED | `from_dict({})` compared section-by-section against `Config.default()` (providers, tools, sandbox, ui, session, log, provider/confirmation). |
| test_config.py:220 - test_config_from_dict_custom_general | FIXED | Custom general parsed AND all other sections asserted equal to `Config.default()`'s. |
| test_config.py:233 - test_config_from_dict_invalid_provider_fallback | FIXED | `structlog.testing.capture_logs` oracle: exactly one `config_invalid_provider_name` warning with value="nonexistent", fallback="anthropic"; result falls back to ANTHROPIC. |
| test_config.py:242 - test_config_from_dict_invalid_confirmation_fallback | FIXED | capture_logs: one `config_invalid_confirmation_level` warning value="badvalue" fallback="destructive"; result falls back to DESTRUCTIVE. |
| test_config.py:251 - test_config_parse_providers_unknown_skipped | FIXED | Exact result keyset==8 known providers; ANTHROPIC override applied (enabled False, timeout 45); OPENAI untouched default; captured `config_unknown_provider_skipped` warning value="unknown_provider". |
| test_config.py:262 - test_config_parse_tools_unknown_skipped | FIXED | Exact keyset==5 known tools; GHIDRA disabled but port/timeout defaults preserved; FRIDA default; captured `config_unknown_tool_skipped` value="unknown_tool". |
| test_config.py:272 - test_config_parse_tools_with_path | FIXED | path "/opt/ghidra" -> `Path("/opt/ghidra")` and all other GHIDRA fields stay default; added invalid-path-type test asserting fallback to None + `config_invalid_tool_path` warning. |
| test_config.py:281 - test_config_parse_sub_configs_defaults | FIXED | Each returned sub-config asserted equal to its fresh dataclass default. |
| test_config.py:290 - test_config_parse_sub_configs_custom | FIXED | Custom overrides asserted AND every unspecified field asserted at its documented default across all four sub-configs. |
| test_config.py:306 - test_config_load_from_toml | FIXED + edges | Real TOML file load asserts overrides + unspecified-default sections; added missing-file -> FileNotFoundError and malformed-TOML -> tomllib.TOMLDecodeError. |
| test_config.py:331 - test_config_save_and_reload | FIXED | Config with non-default values across all sections written to TOML and reloaded; every section asserted equal field-for-field; added save-without-tomli_w -> ImportError error path. |
| test_config.py:355 - test_get_project_root_returns_repo_root | FIXED | Asserts root/src dir, pyproject.toml, src/intellicrack/core/config.py marker files, and that the imported config module file is relative_to the root. |
| test_config.py:362 - test_get_config_dir_is_under_project_root | FIXED | Asserts name/parent/is_absolute; added writability test that relocates root to tmp and writes/reads a real file under the config dir. |
| test_config.py:369 - test_get_config_file_joins_filename | FIXED | Asserts name, parent, is_absolute, and that the OS-native separator appears in the rendered path. |

No tests were deleted; every finding was rewritten into a real gate. No
production source was modified.

## Verification (project env, this file only)

| Command | Result |
|---|---|
| `pixi run ruff check tests/test_core/test_config.py` | All checks passed! |
| `pixi run ruff format tests/test_core/test_config.py` | 1 file left unchanged |
| `pixi run ruff format --check tests/test_core/test_config.py` | 1 file already formatted (no changes) |
| `pixi run basedpyright tests/test_core/test_config.py` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint tests/test_core/test_config.py` | No violations |
| `pixi run pydocstyle tests/test_core/test_config.py` | exit 0, no output |
| `pixi run pytest tests/test_core/test_config.py -p no:timeout` | 36 passed |
