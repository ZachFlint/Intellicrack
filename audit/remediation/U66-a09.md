# Remediation U66-a09 (agent-09.md)

Unit owns:
- `tests/test_hexcore_e2e/test_bridge_document_info.py`
- `tests/test_hexcore_e2e/test_encodings.py`

All 10 findings are Low-severity Weak-assertion-on-rich-output / Smoke-test-as-gate.
Every flagged single-field / type-only assertion was rewritten into a real gate
driving a real `HexEditorBridge` / native `HexDocument` against a real PE binary
or real byte buffer, asserting the full structure / exact codepoints against an
independent oracle, and adding error-path and boundary coverage.

## Findings table

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_bridge_document_info.py:49 - test_no_document_file_path_is_none | FIXED | Real `HexEditorBridge` with no document; asserts the entire `get_document_info()` dict equals the exact empty-state contract `{file_path:None,size:0,modified:False,cursor:0,selection:None}` and that keys == the required-key set. (now `test_no_document_full_state_is_empty_and_consistent`) |
| test_bridge_document_info.py:58 - test_no_document_size_is_zero | FIXED | Same comprehensive empty-state dict equality gate; plus a determinism gate asserting two consecutive calls return byte-for-byte equal dicts. (folded into full-state + `test_no_document_state_stable_across_repeated_calls`) |
| test_bridge_document_info.py:67 - test_no_document_modified_is_false | FIXED | Covered by the full empty-state dict equality (modified is one pinned field of the whole-dict oracle), not a lone field check. |
| test_bridge_document_info.py:76 - test_no_document_cursor_is_zero | FIXED | Covered by the full empty-state dict equality (cursor pinned to 0 within the whole-dict oracle). |
| test_bridge_document_info.py:85 - test_no_document_selection_is_none | FIXED | Covered by the full empty-state dict equality (selection pinned to None within the whole-dict oracle). |
| test_encodings.py:27 - test_decode_utf8_hello_world | FIXED | Real `HexDocument.open_bytes` of UTF-8 `"café"` (bytes pinned to `63 61 66 C3 A9`); asserts UTF-8 decode == original AND the same bytes under `latin-1` decode to the per-byte oracle `"cafÃ©"` (proves encoding arg drives decoding). New sibling asserts invalid UTF-8 `80 FF 41` -> exactly `[U+FFFD,U+FFFD,'A']`. (now `test_decode_utf8_multibyte_and_distinguishes_from_latin1` + `test_decode_invalid_utf8_uses_replacement_characters`) |
| test_encodings.py:40 - test_decode_ascii_text | FIXED | Asserts ASCII decode masks the high bit: bytes `41 E9 FF 80 7A` -> `chr(b & 0x7F)` oracle == `"Ai\x7f\x00z"` (bit-arithmetic oracle, independent of the impl). Adds unknown-encoding `ValueError` gate. (now `test_decode_ascii_masks_high_bit` + `test_decode_unknown_encoding_raises_value_error`) |
| test_encodings.py:53 - test_decode_latin1_text | FIXED | Decodes full byte range `0x80-0xFF`; asserts C1 range against the public WHATWG windows-1252 index (Euro, trademark, OE, etc.) and asserts `0xA0-0xFF` is pure byte=codepoint identity. (now `test_decode_latin1_high_range_matches_whatwg_table`) |
| test_encodings.py:66 - test_decode_at_non_zero_offset | FIXED | Three distinct ASCII tokens at known offsets in one buffer; asserts the exact token at each offset including the final token ending at the last document byte, plus zero-length span and out-of-range offset both yield empty strings. (now `test_decode_at_multiple_offsets_and_boundaries`) |
| test_encodings.py:80 - test_decode_returns_string | DELETED-AND-MERGED (FIXED) | Type-only smoke check removed; content correctness is now gated by the strengthened decode tests above (exact-string equality across UTF-8/ASCII/Latin-1/UTF-16 with independent oracles), per the audit's "merge with content verification tests" recommendation. |

All five document_info no-document findings collapse into two strong gates
(`test_no_document_full_state_is_empty_and_consistent`,
`test_no_document_state_stable_across_repeated_calls`) plus an error-path gate
(`test_open_missing_file_raises_oserror_and_state_stays_empty`). The with-file
tests were also upgraded to whole-dict equality and byte-exact write/undo gates.

## Verification (all green)

```
pixi run ruff check tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
  -> All checks passed!
pixi run ruff format tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
pixi run ruff format --check tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
  -> 2 files already formatted (no changes)
pixi run basedpyright tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
  -> No violations
pixi run pydocstyle tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py
  -> (no output; clean)
pixi run pytest tests/test_hexcore_e2e/test_bridge_document_info.py tests/test_hexcore_e2e/test_encodings.py -p no:timeout
  -> 35 passed (deterministic across 3 consecutive runs)
```
