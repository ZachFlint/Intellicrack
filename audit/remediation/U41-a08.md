# U41-a08 Remediation Status

## Unit: U41-a08

| # | File:Line — Test | Severity | Status | Fix Summary |
|---|---|---|---|---|
| 1 | `test_tool_registry_session.py:170-174` — `test_set_session_none_detaches_all_bridges` | Medium | FIXED | Added `set_last_error`/`publish_state` public helpers to `_CountingBridge`; test now publishes a sentinel while wired, severs via `set_session(None)`, publishes a different value, and asserts the session copy still holds the pre-detach sentinel. Would fail if `set_session(None)` were deleted. |
| 2 | `test_ghidra_audit6.py:143-149` — `test_create_bridge_script_oserror_raises_toolerror` | Low | FIXED | Replaced global `patch.object(Path, "write_text", ...)` with a pre-created read-only `start_bridge.py` file; only `tempfile.mkdtemp` is patched to return the controlled directory so the real OS `PermissionError` fires naturally. |
| 3 | `test_realcov_03c_cutter.py:file-level` — `TestRealPatching` skip meta-issue | Low | FIXED | Added `test_rizin_backend_availability_gate`: calls `pytest.fail` with a diagnostic message when `INTELLICRACK_REQUIRE_RIZIN=1` but the backend is absent; distinguishes CI infrastructure failures from expected developer-workstation skips. |
| 4 | `test_credential_store_live.py:188-219` — `test_singleton_thread_safe` | Medium | FIXED | Added `type(instance) is CredentialStore` assertion alongside the existing `id()` equality check; proves the singleton is the correct concrete type rather than an accidental address collision of short-lived objects. |
| 5 | `test_bridge_bps_ups.py:88-95` — `test_no_document_raises` | Low | FIXED | Added `assert bridge.document is None` precondition before calling `export_patches_bps`; proves the `RuntimeError` fires from the no-document guard specifically, not from any subsequent unrelated code path. |
| 6 | `test_bridge_bps_ups.py:72-86` — `test_bps_import_invalid_patch_raises` | Low | FIXED | Added second test payload: `b"BPS1" + 8 corrupt bytes` which has the correct magic but corrupt body; asserts `ValueError|RuntimeError` is raised, probing the body validation path that runs after the magic-byte check. |

## Check Results

| Check | Result |
|---|---|
| ruff check | PASS (0 findings) |
| ruff format --check | PASS (5 files already formatted) |
| basedpyright | PASS (0 errors; venvPath warning is harmless infrastructure noise) |
| pydoclint | PASS (No violations) |
| pydocstyle | PASS (0 findings) |
| pytest | PASS (97 passed, 31 skipped) |

## pytest Summary

97 passed, 31 skipped in 17.63s

The 31 skips are all from `tests/test_bridges/test_realcov_03c_cutter.py` — requires external rizin/radare2 process and refuses to run on host without `INTELLICRACK_ALLOW_HOST_PROCESS_TESTS=1` or a Docker sandbox. This is pre-existing infrastructure behaviour, not a test failure. The new `test_rizin_backend_availability_gate` test also skips on this host (backend absent) but would fail hard in CI when `INTELLICRACK_REQUIRE_RIZIN=1`.
