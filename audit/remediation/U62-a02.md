# Remediation U62-a02 (agent-02.md)

Unit owns two whole test files. Both findings accounted for.

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_audit3/sandbox/test_clipboard_monitor.py:385 - test_smoke_script_logs_clipboard_change | FIXED (renamed `test_script_logs_exact_structure_of_real_clipboard_write`) | Real input: the live `clipboard_monitor.ps1` run under `pwsh` against the real Windows clipboard, fed a known sentinel `"audit3-smoke-7f3a2b9c"` via `Set-Clipboard`. New `_parse_changed_records` helper parses the pipe-delimited `ts\|changed\|format\|preview\|size\|owner_pid\|proc` record. Independent oracle: UTF-8 byte length computed in-test (`len(sentinel.encode("utf-8"))`) and `datetime.fromisoformat`. Exact assertions: a `changed` record whose `preview` == the exact sentinel, `format` == `"Text"`, `size_bytes` == the independently computed byte count, and `timestamp` parses to a tz-aware ISO-8601 value. Replaces the prior existence-only `log exists` / `contents non-empty` checks. |
| tests/test_audit4/b7_process_panel_workers/test_tracked_refresh_worker.py:180 - test_refresh_error_signal_exists | FIXED (replaced by two behavior gates) | Removed the bare `hasattr` smoke check. New `test_success_emits_finished_with_real_snapshot` drives `TrackedRefreshWorker` against the **real** `ProcessManager` singleton holding a real spawned child process (`subprocess.Popen([sys.executable, "-c", "import sys; sys.stdin.read()"])`) registered via the real `manager.register(...)`; pumps the Qt loop and asserts the `refresh_finished` payload contains exactly that pid with `name` == registered name, `process_type` == `ProcessType.EXTERNAL_TOOL.value`, `status` == `"Running"`, and a serialized `registered_at` of the expected width, while `refresh_error` stays silent. New `test_refresh_error_signal_delivers_payload_through_connected_slot` connects a real slot, emits a payload across `pyqtSignal(str)`, pumps the loop, and asserts the slot received exactly that string, plus asserts emitting `object()` raises `TypeError` (proving the str arity/typing). No mock of the operation under test. |

Both rewrites are falsifiable: corrupting `_collect_tracked_snapshot`'s serialization (wrong field, wrong running classification) reddens the snapshot test; dropping/renaming the `refresh_error` signal reddens the delivery test on connect; logging the wrong clipboard content/size/timestamp reddens the structure test.

Notes:
- The pre-existing error-path tests (`test_error_emits_refresh_error_signal`, `test_error_does_not_emit_refresh_finished`, `test_error_message_contains_prefix`) were not flagged and were left intact; they exercise the real `run()` body. The new fixture resets the `ProcessManager` singleton before/after to keep tests order-independent.
- `subprocess.Popen` with `sys.executable` is covered by the locked `tests/**` ruff per-file-ignore for `S603`/`S404`; no inline suppression used.

## Verification (all green, only touched files)

Files:
- tests/test_audit3/sandbox/test_clipboard_monitor.py
- tests/test_audit4/b7_process_panel_workers/test_tracked_refresh_worker.py

| Command | Result |
| --- | --- |
| `pixi run ruff check <files>` | All checks passed! |
| `pixi run ruff format <files>` then `--check` | 2 files already formatted (no changes) |
| `pixi run basedpyright <files>` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint <files>` | No violations |
| `pixi run pydocstyle <files>` | No findings |
| `pixi run pytest <files> -p no:timeout` | 15 passed in 38.96s, 2 warnings |
