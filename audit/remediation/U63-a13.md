# Remediation U63-a13 (agent-13.md)

Owned file: `tests/test_hexcore_e2e/test_hashing.py`

## Finding

| file:line - test | Severity | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- | --- |
| `tests/test_hexcore_e2e/test_hashing.py:101-127` - `test_sha3_256_matches_hashlib_if_supported` & `test_sha3_512_matches_hashlib_if_supported` | Medium | FIXED | The cannot-fail `try/except (RuntimeError, ValueError): pytest.skip(...)` skip-mask was removed entirely; the `_if_supported` variants no longer exist. SHA3-256/512 and BLAKE2b/s are unconditional match arms in `src/intellicrack-hexcore/src/hash.rs` (no feature gate), so they are always available and must never skip. `test_sha3_256_matches_hashlib` / `test_sha3_512_matches_hashlib` drive the real 256-byte payload (0x00-0xFF) through the real `HexDocument.compute_hash` bridge and assert equality with the independent CPython `hashlib.sha3_256`/`hashlib.sha3_512` oracle, unconditionally. Added independent published known-answer gates (a second oracle distinct from hashlib): `test_sha3_256_empty_matches_nist_known_answer` pins SHA3-256("")==`a7ffc6f8...80f8434a` and new `test_sha3_512_empty_matches_nist_known_answer` pins SHA3-512("")==`a69f73cc...281dcd26` (both FIPS-202 NIST KATs). New `test_guaranteed_algorithms_run_unconditionally` drives the payload through md5/sha1/sha256/sha512 in one pass and asserts the full result dict equals the hashlib reference dict, so a present-but-broken core arm fails (never skips). New `test_blake2b_abc_matches_rfc7693_known_answer` and `test_blake2s_abc_matches_rfc7693_known_answer` pin BLAKE2b-256("abc")==`bddd813c...d52319` and BLAKE2s-256("abc")==`508c5e8c...675982` (RFC 7693 official vectors) so a blake arm mis-wired to a different primitive but still emitting a plausible 64-hex digest is caught. |

## Falsifiability

Corrupting any SHA3 / BLAKE2 / core-hash match arm in the Rust hash module (wrong
primitive, padding, rate, output truncation, or removing an always-available arm)
turns the corresponding exact-digest assertion red; nothing skips. The NIST FIPS-202
and RFC 7693 constants are independent of both the implementation and of hashlib.

## Notes

- No mocks/patches/monkeypatch; every test exercises the real `intellicrack_hexcore`
  bridge end to end via `compute_hash` on real `HexDocument` objects.
- Independent oracles: Python `hashlib` (a different implementation from the Rust
  `sha3`/`blake2`/`md-5`/`sha1`/`sha2` crates) plus published NIST FIPS-202 SHA3 and
  RFC 7693 BLAKE2 known-answer constants.
- All tests run with zero skips and zero xfails; deterministic across repeated runs.

## Verification (all six green)

| Command | Result |
| --- | --- |
| `pixi run ruff check tests/test_hexcore_e2e/test_hashing.py` | All checks passed |
| `pixi run ruff format tests/test_hexcore_e2e/test_hashing.py` (+ `--check`) | 1 file already formatted (no changes) |
| `pixi run basedpyright tests/test_hexcore_e2e/test_hashing.py` | 0 errors, 0 warnings, 0 notes |
| `pixi run pydoclint tests/test_hexcore_e2e/test_hashing.py` | No violations |
| `pixi run pydocstyle tests/test_hexcore_e2e/test_hashing.py` | No findings |
| `pixi run pytest tests/test_hexcore_e2e/test_hashing.py -p no:timeout -p no:cacheprovider` | 33 passed in 1.27s (0 skipped, deterministic) |
