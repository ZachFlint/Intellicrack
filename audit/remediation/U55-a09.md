# U55-a09 Remediation (agent-09.md)

Unit owns two whole test files:
- `tests/test_hexcore_e2e/test_bridge_compare_files.py`
- `tests/test_hexcore_e2e/test_bridge_patches.py`

All 5 findings are FIXED (rewritten into real gates). No deletions.

## Per-finding table

| file:line - test | Status | Real input / independent oracle / exact assertion now backing it |
|---|---|---|
| test_bridge_compare_files.py:63 - test_identical_files_reports_identical | FIXED | Two identical 128-byte files driven through the real `compare_files` bridge -> native `diff_files`. Asserts exact schema `{files_identical,total_differences,regions,similarity}`, `files_identical is True`, `total_differences == 0`, similarity within 1e-5 of 1.0, and the exact single region `{offset_a:0,offset_b:0,length:128,diff_type:"match"}`. Oracle: documented engine contract + difflib-derived match count used in sibling tests. |
| test_bridge_compare_files.py:79 - test_identical_files_have_zero_differences | FIXED | Identical 64-byte files. Asserts the canonical field `total_differences == 0` (no loose OR across alternate field names), `files_identical is True`, and that summed non-match region lengths == 0. Schema validated exactly. |
| test_bridge_patches.py:43 - test_export_patches_returns_string (now test_export_patches_ips_decodes_to_exact_written_bytes) | FIXED | Real PE-sized buffer, two non-contiguous `write_bytes` then `export_patches("ips")`. Asserts the base64 round-trips under `validate=True`, magic `PATCH`, footer `EOF`, and an independent IPS-spec decoder yields exactly `{0x10:b"\xaa\xbb\xcc\xdd", 0x80:b"\x11\x22"}`. Oracle: published IPS format parser (distinct from the native Rust exporter). |
| test_bridge_patches.py:59 - test_export_patches_ips_decodes_to_bytes_starting_with_patch (now test_export_patches_ips_record_byte_layout_matches_spec) | FIXED | 4-byte write at offset 4. Asserts the entire blob byte-for-byte: `PATCH` + `00 00 04` (3-byte BE offset) + `00 04` (2-byte BE size) + data + `EOF`, plus decoder map `{4:b"\xde\xad\xbe\xef"}`. Hand-computed spec constant as oracle. |
| test_bridge_patches.py:75 - test_export_patches_ips32_returns_valid_base64 (now test_export_patches_ips32_byte_layout_and_decode) | FIXED | 1-byte write at offset 0, `export_patches("ips32")`. Asserts IPS32 magic, 4-byte BE offset `00 00 00 00`, size `00 01`, data, and `EEOF` footer byte-for-byte, plus decoder map `{0:b"\xff"}`. |

## Added coverage dimensions (beyond the 5 findings)

- compare_files: exact single-byte modified region (offset 32), modified-tail region, truncation -> `inserted_a`, empty-vs-empty, self-compare, fully-disjoint single `modified` region, real-PE single-byte patch pinpointed at 0x200, PE-vs-ELF real-binary difference, missing-file -> `OSError`, unavailable-hexcore -> `RuntimeError`. difflib `SequenceMatcher` is the independent match-byte oracle; similarity cross-checked by an independent ratio formula.
- patches: IPS32 high-offset (>24-bit) field width, unknown-format -> `ToolError`, no-document -> `RuntimeError`, exact import record count + post-import byte verification, unknown-magic import -> `ToolError`, IPS and IPS32 full roundtrips with exact `read_bytes` verification.

## Verification block (all green)

Commands run in project env on only the two touched files:

1. `pixi run ruff check tests/test_hexcore_e2e/test_bridge_compare_files.py tests/test_hexcore_e2e/test_bridge_patches.py` -> All checks passed!
2. `pixi run ruff format ...` then `pixi run ruff format --check ...` -> 2 files already formatted (no changes).
3. `pixi run basedpyright ...` -> 0 errors, 0 warnings, 0 notes.
4. `pixi run pydoclint ...` -> No violations.
5. `pixi run pydocstyle ...` -> clean (no output).
6. `pixi run pytest ... -p no:timeout` -> 22 passed.
