# Remediation U06-a03

Unit: tests/test_providers/test_providers_local_audit1.py
Source audit: agent-03.md
Worktree: D:/ic-wt2/U06-a03

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_providers/test_providers_local_audit1.py:218 - test_f0001_b580_device_ids_constant_drives_detection | FIXED | The tautological all-positive loop is now a falsifiable gate over the real production `_is_b580_device(device_name, device_id)` from `intellicrack.providers.xpu_utils`. Positive oracle: the independently-documented four Intel Arc B580 device-ID spellings of PCI ID `0xE20B` (`{"0xe20b","e20b","E20B","0xE20B"}`), asserted as the exact frozenset and each matched against a deliberately non-B580 name (`"Generic GPU"`, no `b580` substring) so only the device-ID path can drive the verdict. Negative oracle: real non-B580 PCI device IDs from a different source - Intel Battlemage `0xE20C`/`e20c`, NVIDIA RTX 4090 `0x2684`, AMD RX 6700 XT `73DF`/`73df`, plus empty/garbage/truncated (`"", "not-an-id", "e20"`) - each asserted to NOT classify as B580. A companion test `test_f0001_b580_device_name_path_is_independent_of_device_id` pins the independent name-based fallback: a non-B580 ID (`0x2684`) with a B580 product name matches, the same ID with a non-B580 name does not. |

Falsifiability: if `_is_b580_device` regresses to ignore `_B580_DEVICE_IDS`, match any ID, or substring-match, the negative assertions go red; if it drops the device-name fallback, the name-path test goes red. Verified against the production logic `normalized_device_id in {lower(_B580_DEVICE_IDS)} or "b580" in device_name.lower()`.

Note: the audit ran against a pre-`f06ab769` revision; commit `f06ab769`
already rewrote this test into the real gate described above. This unit
re-verified the gate stands from a HEAD-forked worktree.

## Verification (run from D:/ic-wt2/U06-a03, main pixi env via --manifest-path)

| Command | Result |
| --- | --- |
| `ruff check ...test_providers_local_audit1.py` | All checks passed (0 findings) |
| `ruff format --check ...` | 1 file already formatted (no changes) |
| `basedpyright ...` | 0 errors, 0 warnings, 0 notes |
| `pydoclint ...` | No violations |
| `pydocstyle ...` | (clean, no output) |
| `pytest ... -p no:timeout -p no:cacheprovider` | 16 passed in 1.54s |

All six gates genuinely green.
