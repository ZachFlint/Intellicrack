# U45-a08 Remediation

Unit `U45-a08` (report `agent-08.md`), 8 findings across two owned files:

- `tests/test_core/test_session_audit6.py`
- `tests/test_openai_provider.py` (physical path `tests/test_providers/test_openai_provider.py`)

The `tests/test_providers/test_openai_provider.py:250-272 - test_disconnect_clears_connection_state`
finding belongs to unit `U68-a08`, not this unit, and was left untouched.

## Per-finding table

| File:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_providers/test_openai_provider.py:221-227 - test_connection_with_invalid_key_raises_error | FIXED | Renamed to `test_connection_with_invalid_key_maps_live_401_to_authentication_error`. Drives the real `connect` path against the live `api.openai.com/models` endpoint with a bogus `sk-...` key (skips only if the host is genuinely unreachable). Asserts message starts with `Invalid OpenAI API key:`, the chained `__cause__` is a real `openai.AuthenticationError` with `status_code == 401`, and `is_connected is False` / `client is None` after the failure. |
| test_providers/test_openai_provider.py:231-237 - test_connection_with_empty_key_raises_error | FIXED | Split into `test_connection_with_empty_key_rejected_before_any_network_call` plus boundary companion `test_connection_with_none_key_rejected_before_any_network_call`. Real provider, real empty/`None` credentials. Asserts the exact pre-connection guard message `"OpenAI API key is required"` and that no client was constructed (`client is None`, `is_connected is False`), proving validation runs before any network round-trip. |
| test_providers/test_openai_provider.py:241-246 - test_list_models_without_connection_raises_error | FIXED | Renamed to `test_list_models_without_connection_raises_specific_error`. Asserts the fresh provider is `is_connected is False` and `client is None` first, then that `list_models` raises `ProviderError` with the exact message `"Not connected to OpenAI API"`, and that the flag is not flipped by the failure. |
| test_core/test_session_audit6.py:89-130 - test_auto_save_loop_survives_exception_and_resumes | FIXED | Renamed to `test_auto_save_loop_survives_real_locked_db_and_resumes`. Removed the `SessionStore.save` monkeypatch entirely. A second SQLite connection holds `BEGIN EXCLUSIVE`, so real `store.save` calls fail with `OperationalError: database is locked` (asserted directly). The loop survives the locked window (`manager.is_auto_saving is True`); after releasing the lock the sentinel tag `recovered-after-lock` is persisted and reloaded from real SQLite. Removing the loop's broad failure guard kills the task on the first lock error -> sentinel never persists. |
| test_core/test_session_audit6.py:383-425 - test_concurrent_updates_serialise_and_complete | FIXED | Renamed to `test_concurrent_updates_serialise_and_persist_each_session`. Replaced the `SessionStore.save` monkeypatch with a real `SessionStore` subclass (`_InstrumentedSessionStore`) that records save threads/overlap while persisting real data. Eight sessions get distinct tags + `ToolState`; after concurrent `manager.update`, asserts exactly 8 update-saves, `max_concurrent_saves == 1`, and field-by-field integrity (id, name, tags, tool_state connected/process_attached/target_path) reloaded from SQLite. |
| test_core/test_session_audit6.py:142-145 - test_session_has_set_tool_state | DELETED | Smoke-test-as-gate (`hasattr` only). Removed per audit recommendation; the real gate `test_set_tool_state_round_trips_through_store` (calls the writer and round-trips through SQLite) already covers the behaviour. |
| test_core/test_session_audit6.py:205-209 - test_session_has_add_tag | DELETED | Smoke-test-as-gate (`hasattr` only). Removed per audit recommendation; `test_add_tag_round_trip_through_store` and `test_remove_tag_persists` already exercise the writers and assert persisted state. |
| test_core/test_session_audit6.py:328-334 - test_hex_document_full_protocol_body_is_declarative | FIXED | Kept the structural declarative-body check and paired it with a real behavioural contract class. New `_RealHexDocumentAdapter` wraps a live `intellicrack_hexcore.HexDocument` and fully conforms to `HexDocumentFull`; tests assert `isinstance(adapter, HexDocumentFull)` is True (and an incomplete read-only doc is rejected), and drive the real `TemplateManager.bootstrap_builtins` consumer to export all real built-in templates, asserting written JSON `name` and full content equal the document's own `export_template_json`. |

## Verification (all green)

Commands run in the project env (`pixi run ...`) on only the touched files:

```
pixi run ruff check tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> All checks passed!
pixi run ruff format tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> 2 files left unchanged
pixi run ruff format --check tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> 2 files already formatted
pixi run basedpyright tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> No violations
pixi run pydocstyle tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py
  -> No violations
pixi run pytest tests/test_core/test_session_audit6.py tests/test_providers/test_openai_provider.py -p no:timeout
  -> 30 passed, 2 warnings
```
