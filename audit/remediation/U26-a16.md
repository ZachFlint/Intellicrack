# Remediation U26-a16 (agent-16.md)

Unit owns three whole test files. All 4 findings accounted for.

## Per-finding results

| file:line - test | status | real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_hexcore_e2e/test_bridge_display.py:42-69 - TestBridgeDisplayMode | FIXED | Drives the real `HexEditorBridge` with a real `HexDocumentState` (a separate orchestration component) attached as an independent oracle plus a real observer callback registered on the state holder. Asserts: default is exactly `hex8` and state holder agrees; every set mode round-trips and lands verbatim in both `state.get_display_mode()` and the observer's captured `DISPLAY_MODE_CHANGED` payload; a 6-mode sequence persists each value until the next change (`recorder.display_modes == sequence`); an invalid mode and the empty string both raise `ValueError(match="unknown display mode")` and leave the prior mode unmutated. |
| tests/test_hexcore_e2e/test_bridge_display.py:94-177 - TestBridgeHighlights | FIXED | Real bridge + real state holder + observer. After `add_highlight_rule`, asserts the full retrieved rule dict equals the exact expected `{id, condition_type, condition_params, color}` (e.g. `byte_value`/`{"value":255}`/`#00FF00`), that the same full object is propagated into `state.get_highlight_rules()` and the `HIGHLIGHT_RULE_ADDED` observer payload, that two rules get distinct 36-char UUID ids (4 hyphens) and coexist with their own params/colors, that removal returns `True`, purges the rule from list and state, emits a `HIGHLIGHT_RULE_REMOVED` event, leaves other rules intact, and that removing an unknown id returns `False` and emits no event. |
| tests/test_hexcore_e2e/test_bridge_transforms_deep.py:86-141 - TestApplyPipelineSingleStep (+ whole file) | FIXED | Real bridge over the real Rust `intellicrack_hexcore` backend against real on-disk binaries. Every transform/pipeline test asserts the exact transformed bytes against an independent oracle: closed-form XOR (`_xor` helper + hand-checked literals e.g. `10 20 30 40` XOR `AA` == `ba8a9aea`), bit-complement (`bit_invert` == `~b & 0xFF` == `f00fff005a`), `byte_reverse` == `payload[::-1]`, `rot_n` shift 13 `HELLO`->`URYYB`, `base64_encode` == stdlib `base64.b64encode`. Non-commutative ordering proven with `xor_repeating`+`byte_reverse` (`fb9a9bba` vs `ea8b8aab`, both computed by hand). Added edge/error coverage: empty pipeline returns original, zero-length range returns `""`, in-place subrange leaves neighbors untouched (verified via `read_bytes`), unknown step is skipped, a real step after an unknown step still applies, malformed JSON raises `json.JSONDecodeError`, in-place `base64_encode` raises `ValueError`, and transform with no document open raises `RuntimeError`. Removed all capability `pytest.skip`s and the exception-swallowing invalid-step test; replaced the non-existent `bitwise_not`/`rot13` names with the backend's real `bit_invert`/`rot_n`; a module-level capability assertion fails loudly if the backend regresses. |
| tests/test_providers/test_google_chat_live.py:52-179 - Live integration tests | FIXED | Live `GoogleProvider` against `gemini-2.0-flash` (the prior `gemini-1.5-flash` is retired and 404s). Removed the in-test `except AuthenticationError/ProviderError: skip` masking; only the transient `RateLimitError` is skipped in-test. Deterministic temperature-0 prompt asserts `"ready" in content.lower()` (semantic correctness, case-insensitive) for both `chat()` and `chat_stream()`, `message.role == "assistant"`, `tool_calls is None`, at least one stream chunk, and usage metadata is positive, internally consistent (`total == prompt + completion`), and cleared after a single `get_pending_usage()`. Genuine breakage (retired model / rejected key) now propagates as `ProviderError`/`AuthenticationError` and FAILS; only the suite-level `pytest_runtest_call` hook skips on recognized account billing/quota-cap signals (an environment precondition). Falsifiability proven: pointing the test at the retired `gemini-1.5-flash` produced `1 failed` (not skipped). |

## Verification (all green)

Commands run in the pixi env on only the three touched files:

- `pixi run ruff check ...` -> All checks passed!
- `pixi run ruff format ...` then `--check` -> 3 files already formatted (no changes)
- `pixi run basedpyright ...` -> 0 errors, 0 warnings, 0 notes
- `pixi run pydoclint ...` -> No violations
- `pixi run pydocstyle ...` -> no findings
- `pixi run pytest ... -p no:timeout` -> 34 passed, 2 skipped

The 2 skips are the live Google tests, skipped solely by the existing suite-level
`tests/test_providers/conftest.py` hook for the recognized "quota or spending cap
exhausted" account-limit signal (an unmet billing precondition, not in-test masking
of breakage). The retired-model falsifiability probe confirmed real breakage fails.
