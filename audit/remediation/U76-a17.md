# Remediation U76 - agent-17.md

Unit: `tests/test_hexcore_e2e/test_read_write_ops.py` (owned in full)

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_hexcore_e2e/test_read_write_ops.py:59 - test_read_across_byte_boundaries | FIXED | Real `HexDocument` loaded from a disk file holding the 256-byte identity sequence. Independent oracle is the identity definition `value == offset`: boundary reads are asserted equal to `bytes(range(126,130))` and `bytes(range(253,256))` (derived from the math, not from `sample_bytes`), retaining the explicit `0x7E..0x81` / `0xFD..0xFF` constants as a second check. New `TestFixtureIntegrity` independently proves the fixture is the identity sequence (`read_byte(i) == i` for all 256 offsets, plus a full-buffer compare to `bytes(range(256))`). Added error/edge gates: past-end read clamps to the in-bounds suffix; `read_byte(256)` raises `IndexError`; out-of-range `read` raises `ValueError` matching `beyond document size`. |
| tests/test_hexcore_e2e/test_read_write_ops.py:107 - test_write_does_not_change_surrounding_bytes | FIXED | Real in-memory `HexDocument` from `open_bytes` on the identity sequence. Dropped the `sample_bytes` dependency entirely; surrounding bytes are asserted against the identity oracle: after `write_bytes(50, b"\xaa\xbb")`, offsets 48-49 must equal `bytes([48,49])` and 52-53 `bytes([52,53])` (plus `read_byte(49)==49`, `read_byte(52)==52`), while offsets 50-51 must equal the new payload `b"\xaa\xbb"`. Fixture identity is independently verified by `TestFixtureIntegrity::test_in_memory_doc_is_identity_sequence`. |

Falsifiability: corrupting the read offset/length math, the write boundary, or
loading wrong bytes makes the identity-derived assertions go red; a corrupted
fixture is now caught by `TestFixtureIntegrity` instead of silently passing.

Environment note: the installed `intellicrack_hexcore` wheel was stale (the
native `.pyd` was nested without a top-level re-export, so `hexcore.HexDocument`
did not resolve and every test in the file errored at setup). Rebuilt the wheel
with `maturin build --release` and reinstalled so the real bridge runs end to
end. No production source was modified.

## Verification (all green on the touched file)

```
pixi run ruff check tests/test_hexcore_e2e/test_read_write_ops.py
  -> All checks passed!
pixi run ruff format tests/test_hexcore_e2e/test_read_write_ops.py
  -> 1 file left unchanged
pixi run ruff format --check tests/test_hexcore_e2e/test_read_write_ops.py
  -> 1 file already formatted
pixi run basedpyright tests/test_hexcore_e2e/test_read_write_ops.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_hexcore_e2e/test_read_write_ops.py
  -> No violations
pixi run pydocstyle tests/test_hexcore_e2e/test_read_write_ops.py
  -> (no output; clean)
pixi run pytest tests/test_hexcore_e2e/test_read_write_ops.py -p no:timeout
  -> 23 passed in 1.58s
```
