# Remediation U76 - agent-17.md

Unit: `tests/test_hexcore_e2e/test_read_write_ops.py` (owned in full)

## Findings

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_hexcore_e2e/test_read_write_ops.py:59 - test_read_across_byte_boundaries | FIXED | Real `HexDocument` loaded through the real `intellicrack_hexcore` native bridge from a disk file holding the 256-byte identity sequence. Primary oracle is a hand-enumerated, human-verifiable constant: boundary reads at offsets 126..129 and 253..255 are asserted equal to `bytes([0x7E,0x7F,0x80,0x81])` and `bytes([0xFD,0xFE,0xFF])`, cross-checked against the identity definition `bytes(range(126,130))` / `bytes(range(253,256))` (independent of `sample_bytes`, the load source). Added a single-byte read at the 0x80 sign boundary asserting `bytes([0x80])`. `TestFixtureIntegrity` independently proves the fixture is the identity sequence (`read_byte(i) == i` for all 256 offsets plus a full-buffer compare to `bytes(range(256))`). Falsifies on any off-by-one/wrong-offset read or a fixture that lost its high half. |
| tests/test_hexcore_e2e/test_read_write_ops.py:107 - test_write_does_not_change_surrounding_bytes | FIXED | Real in-memory `HexDocument` from `open_bytes` on the identity sequence. No dependency on `sample_bytes`. Pre-write assertions pin offsets 48..53 at the identity values `bytes([48,49,50,51,52,53])` and the target at `bytes([50,51])`. After `write_bytes(50, b"\xaa\xbb")` the target reads exactly `b"\xaa\xbb"` (proving a real mutation, since it previously held 0x32/0x33), while neighbors 48-49 and 52-53 still read `bytes([48,49])` / `bytes([52,53])` (plus `read_byte(49)==49`, `read_byte(52)==52`). Fixture identity is independently verified by `TestFixtureIntegrity::test_in_memory_doc_is_identity_sequence`. Falsifies on a no-op write, an over-write that bleeds into a neighbor, or a corrupted fixture. |

Falsifiability: corrupting the read offset/length math, the write boundary, or
loading wrong bytes makes both the hand-verified-constant and identity-derived
assertions go red; a corrupted fixture is caught by `TestFixtureIntegrity`
instead of silently passing. Oracle independence was confirmed empirically: the
real native module returns `read(126,4) == 7e7f8081` and `read_byte(255) == 255`,
agreeing with the hand-verified constants.

Environment note: the installed `intellicrack_hexcore` wheel had been rebuilt in a
prior pass (`maturin build --release`) so the real bridge resolves and runs end to
end. No production source was modified.

## Verification (all green on the touched file)

```
pixi run ruff check tests/test_hexcore_e2e/test_read_write_ops.py
  -> All checks passed!
pixi run ruff format tests/test_hexcore_e2e/test_read_write_ops.py
  -> 1 file left unchanged
pixi run ruff format --check tests/test_hexcore_e2e/test_read_write_ops.py
  -> 1 file already formatted
pixi run basedpyright tests/test_hexcore_e2e/test_read_write_ops.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_hexcore_e2e/test_read_write_ops.py
  -> No violations
pixi run pydocstyle tests/test_hexcore_e2e/test_read_write_ops.py
  -> (no output; clean)
pixi run pytest tests/test_hexcore_e2e/test_read_write_ops.py -p no:timeout -p no:cacheprovider
  -> 23 passed in 1.16s
```
