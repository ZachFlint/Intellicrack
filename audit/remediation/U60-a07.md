# U60 - agent-07 remediation (unit U60-a07)

Owned files:
- `tests/test_providers/test_huggingface_provider.py`
- `tests/test_ui/test_splash_screen.py`

Production change (genuinely-missing real functionality required by a gate):
- `src/intellicrack/providers/huggingface.py`: added a public `build_model_info_list`
  static method that wraps the existing `_build_model_info_list` normaliser and is now
  the path `list_models` calls. This exposes the bridge's Hub-record -> `ModelInfo`
  mapping as a first-class, testable surface (mirroring the existing public
  `load_splash_pixmap` / `create_fallback_pixmap` pattern), so the parser can be gated
  without a live API call and without accessing a protected member from tests.

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_huggingface_provider.py:48-63 - TestHuggingFaceModelListing.test_list_models_returns_non_empty_list | FIXED | Renamed to `test_list_models_returns_fully_valid_model_records`. Drives the real HuggingFace Hub bridge; asserts EVERY returned record (not a prefix) is a `ModelInfo` with non-whitespace `id`, non-whitespace `name`, `provider == HUGGINGFACE`, `context_window > 0`, boolean `supports_tools`/`supports_vision`, `supports_streaming is True`, and that all ids are unique (de-dup contract). A parser returning `[None,...]`/duplicates fails. |
| test_huggingface_provider.py:106-107 - TestHuggingFaceModelListing.test_model_info_has_valid_id | FIXED | Renamed to `test_all_model_ids_match_hub_naming_and_derive_name`. Iterates the ENTIRE returned list; asserts each id is a non-whitespace string with no spaces and <=1 `/`, and independently re-derives the expected short name (tail after final `/`) and asserts `ModelInfo.name` equals it for every record. Plus new deterministic class `TestModelInfoNormalization` exercises the real production `build_model_info_list` against genuine `huggingface_hub.ModelInfo` records with independently-known oracles: exact field mapping, function-calling -> `supports_tools`, image-text-to-text -> `supports_vision`, bare-id naming, order-preserving de-duplication, empty-id drop, empty-input -> empty list. |
| test_splash_screen.py:313-319 - TestProgressSignal.test_progress_signal_emits | FIXED | Replaced with `test_progress_signal_delivers_args_to_connected_slot` (connects a real recorder slot, asserts it received exactly `(50, "Loading tools...")`, and asserts the production `_on_progress_updated` slot mutated `progress`/`status`) and `test_progress_signal_preserves_every_rapid_emission` (10 rapid emissions delivered in order with no loss, final state asserted). |
| test_splash_screen.py:378-388 - TestSplashPixmapLoading.test_load_splash_pixmap_returns_qpixmap | FIXED | Renamed to `test_load_splash_pixmap_loads_real_asset_at_expected_scaled_size`. Independently loads the real on-disk `splash.png` and applies the same KeepAspectRatio scale as an oracle; asserts the bridge output is non-null, positive-size, within the target box, and matches the independently-scaled width/height exactly. A fallback or mis-scale regression fails. |
| test_splash_screen.py:506-516 - TestSplashScreenIntegration.test_splash_screen_no_exceptions_on_operations | FIXED | Replaced with `test_splash_operations_drive_correct_state_transitions`. Removed the swallow-everything try/except; drives the real lifecycle and asserts exact observable state after each step: `isVisible() is True` after `show()`; exact `progress`/`status`/animation `endValue()` after each `set_progress`; `isVisible() is False` after `hide()`. Cleanup via fixture teardown. |

## Verification (all six green; pytest PASS, zero skips)

Commands run in the project pixi env on the touched files:

```
pixi run ruff check tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py
  -> All checks passed!
pixi run ruff format tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py
pixi run ruff format --check tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py
  -> 2 files already formatted (no changes)
pixi run basedpyright tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py src/intellicrack/providers/huggingface.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py
  -> No violations
pixi run pydocstyle tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py
  -> (no output / clean)
pixi run pytest tests/test_providers/test_huggingface_provider.py tests/test_ui/test_splash_screen.py -p no:timeout
  -> test_huggingface_provider.py: 22 passed, 0 skipped (live API token configured; live bridge tests ran for real)
  -> test_splash_screen.py: 87 passed
```

Source linting also confirmed green: `ruff check`, `pydoclint`, `pydocstyle` on
`src/intellicrack/providers/huggingface.py` all clean.
