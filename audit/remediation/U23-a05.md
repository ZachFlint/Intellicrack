# U23-a05 Remediation

Worktree: `D:/ic-wt2/U23-a05` (branch `wf2/U23-a05`, left intact for review/merge).

All test edits live in the worktree; production `src/` was not modified. Tests
run against the installed main `src`.

## Findings

| # | Ref | Severity | Status | Real input + independent oracle + exact assertion |
|---|-----|----------|--------|----------------------------------------------------|
| 1 | `tests/test_bridges/test_ghidra.py:46-50` — `test_bridge_instantiation` | Medium | FIXED | The original test only asserted `b is not None` (smoke gate). Rewritten as `test_bridge_instantiation_initializes_real_state`: constructs a real `GhidraBridge()` and asserts the full documented init contract — `name is ToolName.GHIDRA`; `ghidra_path`/`project_path` are `None` (connection-dependent state unset); public `DEFAULT_PORT == 4768`; `capabilities` exact flags (`supports_static_analysis`/`supports_decompilation`/`supports_scripting` True, `supports_patching` False); exact `supported_formats == ["pe","elf","macho","raw","coff"]`; exact 11-entry `supported_architectures`; `tool_definition.tool_name is ToolName.GHIDRA` with exactly 81 functions. Expected values come from the Ghidra integration contract, not the constructor's own output. Verified RED by mutating the expected port to 9999. |
| 2 | `tests/test_providers/test_discovery_unit.py:40-200` — `_DiscoveryProvider` class and related | High | FIXED | The `_DiscoveryProvider` stub is a legitimate *input* feeding the real `ModelDiscovery`/`DiscoveryCache`/`DiscoveryFilter` logic (the thing under test), so the existing stub-driven tests were kept. The audit gap — no exercise of a real provider's connection-state contract — is closed by the new `TestRealProviderConnectionContract`, which drives the genuine production `OpenAIProvider` (offline, unconnected by base-class contract, no credentials/sockets) through `ModelDiscovery.discover_provider`. Asserts: real `is_connected` is `False` and gates discovery → returns `[]` and invalidates a stale cache entry; real `OpenAIProvider.list_models()` raises `ProviderError("Not connected to OpenAI API")`; `connect`→`disconnect` round-trips the real `connected`/`is_connected` flag. A regression that pins `is_connected` true or drops the guard now fails. |
| 3 | `tests/ui/test_system_tab_warnings.py:185-205` — `test_refresh_mitigations_unattached_shows_warning` | High | FIXED | Rewritten to use the **real production `ProcessBridge`** (not `_StubBridge`) via a new `_make_real_bridge_tab` helper, so the unattached guard runs against a real bridge object and real `set_bridge`/`set_attached_pid` wiring. Drives `_refresh_mitigations` with `pid=None` and asserts: exact warning title `== "Query Mitigations"`; exact message `== "Not attached to any process"` (cross-checked against production `_NOT_ATTACHED_MSG` to catch contract drift); the `_raw_output` widget mirrors the not-attached message; exactly one warning shown; and the `run_bridge_coroutine_logged` dispatch boundary is never reached (tripwire recorder stays empty — boundary assertion, not a fake double of the guard). Verified RED by mutating the asserted title. |

## Six-command verification (cwd `D:/ic-wt2/U23-a05`, main pixi env)

| Command | Result |
|---------|--------|
| `ruff check` (3 files) | All checks passed! |
| `ruff format --check` (3 files) | 3 files already formatted |
| `basedpyright` (3 files) | 0 errors, 0 warnings, 0 notes |
| `pydoclint` (3 files) | No violations |
| `pydocstyle` (3 files) | clean (exit 0) |
| `pytest -p no:timeout -p no:cacheprovider` (3 files) | 121 passed in 3.60s |

Notes:
- The `ruff format` warning about `COM812` and the basedpyright `venvPath`
  message are pre-existing environment notices, not findings on these files.
- A forbidden `# type: ignore[assignment]` on the pre-existing
  `sys.modules["ghidra_bridge"] = None` poison-import line (needed to keep the
  whole file basedpyright-green) was replaced with a type-correct
  `cast("ModuleType", None)`; no behaviour change.
