# Remediation U59-a06

Unit: U59 (agent-06.md). Owned files:
- tests/test_hexcore_e2e/test_bridge_ai_context.py
- tests/test_providers/test_registry_thread_safety_live.py

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| test_bridge_ai_context.py:51 - test_get_context_for_ai_contains_expected_top_level_keys | FIXED | Renamed to `test_get_context_for_ai_top_level_values_match_freshly_opened_pe`. Drives a real 1024-byte PE (conftest builder) through `open_file` then `get_context_for_ai`. Independent oracle = filesystem `Path.stat().st_size` (== 1024) plus freshly-opened invariants: asserts `file_path == str(pe_binary)`, `size == stat().st_size == 1024`, `modified is False`, `cursor == 0`, `selection is None` - exact values, not key presence. |
| test_bridge_ai_context.py:77 - test_get_context_for_ai_bookmarks_is_list | FIXED | Replaced by three gates. `..._bookmarks_match_added_bookmark_exactly` adds (0, 2, "MZ_magic") and asserts the list is exactly `[{"offset":0,"length":2,"label":"MZ_magic"}]` with `bookmark_count_total == 1` and `bookmark_truncated is False`. `..._bookmarks_empty_when_none_added` asserts `[]`, count 0, untruncated. `..._bookmarks_sorted_and_truncated_at_limit` adds 3 out-of-order bookmarks, requests `bookmark_limit=2`, asserts offsets `[0,16]`, labels `["first","second"]`, total 3, truncated True. Oracle = the literals passed to `add_bookmark` plus the requested cap. |
| test_bridge_ai_context.py:102 - test_get_context_for_ai_size_is_positive | FIXED | Renamed to `test_get_context_for_ai_size_equals_document_length`. Independent oracle = `pe_binary.stat().st_size`; asserts `ctx["size"] == pe_binary.stat().st_size == 1024`. Added `..._size_with_no_document_is_zero` covering the no-document branch (size 0, file_path None, no `bytes_at_cursor`). |
| test_registry_thread_safety_live.py:22 - test_get_provider_registry_thread_safe_singleton | FIXED | Replaced `importlib.reload` (module-wide side effects, order risk) with the production `reset_provider_registry` via a before/after fixture. Removed the `INTELLICRACK_LOCAL_TESTS` skip guard so the in-process gate genuinely runs. `..._concurrent_first_access_returns_single_shared_instance`: 32 barrier-synced threads, asserts the set of returned ids collapses to one, instance is a real `ProviderRegistry`, all `is` identical, and a later plain call returns the same object. `..._honors_loader_and_ignores_later_loaders`: real `_RecordingCredentialLoader` + real `OllamaProvider` class; `connect_provider` drives the credential-loader fallback, asserting first loader recorded `[OLLAMA]`, second recorded `[]`, and `ProviderError("No credentials")` surfaced. `..._reset_provider_registry_rebuilds_distinct_instance`: asserts pre/post-reset instances are distinct real registries. |

## Coverage dimensions added
- Edge/boundary: empty bookmark list, no-document branch, bookmark limit truncation and offset sorting, full 256-byte cursor window cross-checked byte-for-byte against on-disk bytes (DOS header `4D 5A`).
- Error paths: negative `bookmark_limit` raises `ValueError` (asserted, not swallowed); credential-loader fallback raises `ProviderError` surfaced not swallowed.
- Determinism: registry concurrency gated by `threading.Barrier`; isolation via `reset_provider_registry` fixture; verified order-independent under pytest-randomly across 3 seeds.

## Verification (all green)

```
pixi run ruff check <files>              -> All checks passed!
pixi run ruff format <files>             -> reformatted; ruff format --check -> already formatted (no changes)
pixi run basedpyright <files>            -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint <files>               -> No violations
pixi run pydocstyle <files>              -> (no output; clean)
pixi run pytest <files> -p no:timeout    -> 14 passed, 2 warnings
```

Files: tests/test_hexcore_e2e/test_bridge_ai_context.py tests/test_providers/test_registry_thread_safety_live.py
Registry gate re-run under 3 random seeds: 3 passed each.
