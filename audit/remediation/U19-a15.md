# U19-a15 Remediation

Worktree: `D:/ic-wt2/U19-a15` (branch `wf2/U19-a15`, left intact)

All four audit findings on this unit's files were already remediated in HEAD
commit `f06ab769` ("harden test suite and resolve core integration bugs"). Each
flagged test was re-verified against the test-writer quality bar: real input,
independent oracle, exact-value assertions, runs (not skipped), and falsifiable.
No further test edits were required; the rewrites were confirmed to be genuine
gates and all checks are green. `filesModified` is therefore empty.

## Per-finding results

| File:Line | Test | Severity | Status | Real input + independent oracle + exact assertion |
|---|---|---|---|---|
| test_audit3/core/test_disassembler.py:86 | test_f0002_auto_detect_arch_known_returns_capstone_pair (now `..._known_resolves_to_real_x86_64_decoding`) | Medium | FIXED | Bytes `48 31 c0 48 89 e5 c3` decoded via live capstone using the `(arch,mode)` `auto_detect_arch` returns for `x86_64`. Oracle is the x86-64 ISA, not Intellicrack's map. Verified mode-sensitive: 64-bit = `xor rax,rax / mov rbp,rsp / ret`; 32-bit = `dec eax / xor eax,eax / ...`. Asserts exact list `[(0x1000,'xor','rax, rax'),(0x1003,'mov','rbp, rsp'),(0x1006,'ret','')]`. A corrupted `_CAPSTONE_ARCH_MODE_MAP['x86_64']` (e.g. -> `('x86','32')`) changes the mnemonics and fails. |
| test_audit5/u1_bridges_cutter/test_cutter_bridge.py:94 | testvalidate_r2_argument_rejects_control_chars (now `test_validate_...`) | High | FIXED | Function renamed so pytest discovers it. Drives 11 rizin metacharacters (`; \n \r @ \| ~ \` > < $ #`) each embedded in a safe identifier through the real `validate_r2_argument`; asserts `ToolError` raised and field name `symbol` in the message, plus leading-`!` shell-escape rejection. |
| test_audit5/u1_bridges_cutter/test_cutter_bridge.py:430 | testvalidate_r2_argument_accepts_safe_strings (now `test_validate_...`) | High | FIXED | Function renamed so pytest discovers it. Drives real safe identifiers through `validate_r2_argument` and asserts byte-for-byte identity (`return == input`), guarding the accept path against silent stripping/rewriting. |
| test_hexcore_e2e/test_bridge_new_capabilities.py:350-399 | test_search_text_encoded_available_on_document + _build_numeric_format tests | Medium | FIXED | `hasattr` smoke test replaced by two real gates calling `doc.search_text_encoded` on a real UTF-16LE buffer `Hello World Hello`: exact offsets `[(16,10),(40,10)]` and case-sensitivity (`[]` sensitive vs `[(16,10),(40,10)]` insensitive). `TestBuildNumericFormat` now feeds each format string into `struct.pack`/`unpack`/`calcsize` and asserts canonical bytes (e.g. `0x12345678` LE -> `78 56 34 12`, INT64_MIN, two's-complement `-2` BE -> `ff fe`, `struct.error` on overflow/negative, `ValueError` on size 3). |

## Six-command verification (from `D:/ic-wt2/U19-a15`, main pixi env)

| Check | Result |
|---|---|
| ruff check | All checks passed! |
| ruff format --check | 3 files already formatted |
| basedpyright | 0 errors, 0 warnings, 0 notes |
| pydoclint | No violations |
| pydocstyle | clean (exit 0) |
| pytest | 76 passed in 2.30s (0 skipped) |

The four finding-specific tests were also run individually with `-v` and all
PASSED (12 passed), confirming none are skipped or xfailed.
