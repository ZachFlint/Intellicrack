# U19 - agent-15.md remediation

Unit owner files:

- tests/test_audit3/core/test_disassembler.py
- tests/test_audit5/u1_bridges_cutter/test_cutter_bridge.py
- tests/test_hexcore_e2e/test_bridge_new_capabilities.py

## Per-finding results

| File:line - test | Severity | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- | --- |
| test_disassembler.py:86 - test_f0002_auto_detect_arch_known_returns_capstone_pair | Medium | FIXED | Renamed to `test_f0002_auto_detect_arch_known_resolves_to_real_x86_64_decoding`. Instead of re-asserting the literal `("x86","64")` map entry (tautological), the returned `(arch, mode)` pair is driven through a live capstone disassembly of the mode-sensitive byte sequence `48 31 c0 48 89 e5 c3`. Independent oracle is the x86-64 ISA encoding: asserts exact `[(0x1000,"xor","rax, rax"),(0x1003,"mov","rbp, rsp"),(0x1006,"ret","")]`. Verified red under a corrupted map entry (`("x86","32")` yields `dec eax / xor eax, eax / ...`). |
| test_cutter_bridge.py:412 - testvalidate_r2_argument_rejects_control_chars | High | FIXED | Renamed to `test_validate_r2_argument_rejects_control_chars` (now actually discovered by pytest). Oracle is the rizin command grammar, enumerated independently: each of `; \n \r @ | ~ \` > < $ #` embedded once inside a safe identifier must raise `ToolError`; asserts the field name `symbol` is surfaced in the message, and that a leading `!` raises with the documented "must not start with '!'" text. Adds the previously-missing newline (`\n`/`\r`) injection vectors. |
| test_cutter_bridge.py:430 - testvalidate_r2_argument_accepts_safe_strings | High | FIXED | Renamed to `test_validate_r2_argument_accepts_safe_strings` (now discovered). Asserts six safe inputs (symbol, `main`, `0x401000`, `sub.text-1`, mangled name, empty string) return byte-for-byte unchanged, plus a boundary case proving `!` is only rejected at position 0 (`name!bang` passes through verbatim). |
| test_bridge_new_capabilities.py:355 - test_search_text_encoded_available_on_document + TestBuildNumericFormat | Medium | FIXED | Replaced the `hasattr` smoke test with two real gates calling the native `search_text_encoded` on a real UTF-16LE document of `Hello World Hello`: asserts exact match offsets `[(16,10),(40,10)]` (offsets/lengths fixed by the layout, mirroring the Rust `test_search_text_utf16le` oracle of 2 hits) and that the `case_sensitive` flag is honoured (`hello` cs=`[]`, ci=`[(16,10),(40,10)]`). Rewrote `TestBuildNumericFormat` to feed each produced struct format into `struct.pack`/`unpack` and assert independently-known binary layouts (`0x12345678`->`78 56 34 12`, int16 BE `-2`->`ff fe`, uint8 `255`->`ff` with overflow `struct.error`, INT64_MIN->`00..80`, endianness flip), not the literal format string. |

## Production fix (legitimately required by a clean gate)

While verifying the touched test file `test_disassembler.py`, the clean gate
`test_f0009_disassemble_to_lines_buffer_omits_binary_path` (audit "Clean tests",
line 144) was failing because `HexDisassembler.disassemble_to_lines` regressed to
logging `binary_path=None` instead of omitting the field for buffer input. The
docstring documents the correct behavior ("the field is omitted from the log call
entirely"). Fixed the implementation in
`src/intellicrack/core/disassembler.py` to build the structured-log payload
conditionally and only include `binary_path` when a path is supplied. No test was
forced green; the production behavior now matches its documented contract.

## Verification (all green)

Commands run in the pixi env on every touched file, including the modified
production source `src/intellicrack/core/disassembler.py` (`FILES` =
`src/intellicrack/core/disassembler.py tests/test_audit3/core/test_disassembler.py
tests/test_audit5/u1_bridges_cutter/test_cutter_bridge.py
tests/test_hexcore_e2e/test_bridge_new_capabilities.py`):

```
pixi run ruff check $FILES
  -> All checks passed!
pixi run ruff format --check $FILES
  -> 4 files already formatted (no changes)
pixi run basedpyright $FILES
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint $FILES
  -> No violations
pixi run pydocstyle $FILES
  -> (no output; clean)
pixi run pytest tests/test_audit3/core/test_disassembler.py tests/test_audit5/u1_bridges_cutter/test_cutter_bridge.py tests/test_hexcore_e2e/test_bridge_new_capabilities.py -p no:timeout
  -> 76 passed, 3 warnings
```

Attempt-2 fix: the previously pre-existing ruff E302 at `disassembler.py:59`
(`expected 2 blank lines before class UnsupportedArchitectureError, found 1`) is
now resolved by adding one blank line so two blank lines separate the closing
module-docstring `"""` (line 57) from `class UnsupportedArchitectureError`
(now line 60). `ruff check` and `ruff format --check` are both clean on
`disassembler.py`; all six checks are green on all four reported-modified files.
