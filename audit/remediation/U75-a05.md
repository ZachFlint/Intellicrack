# Remediation U75-a05 (agent-05.md)

Unit owns:
- tests/test_ui/test_realcov_15_chat_panel.py
- tests/test_ui/test_tool_status_dialog_prefetch.py

## Per-finding results

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| tests/test_ui/test_realcov_15_chat_panel.py:64-78 - test_send_button_emits_typed_text | FIXED | Drives the real `ChatPanel` Send button via `qtbot.mouseClick` with real typed text. Connects a capture list to `message_submitted` to assert the FULL contract independently of the implementation: exactly one emission (`emitted == [("disassemble the entry point",)]`), single `str` payload (`len(blocker.args) == 1`, `isinstance(..., str)`), input cleared, and the falsifiable side-effect contract that the submit path is re-emission only (`findChildren(MessageBubble) == []`, `get_messages() == []`) plus Send button left enabled. Added boundary test `test_send_button_whitespace_only_does_not_emit`: whitespace-only input must emit nothing and preserve the text (covers the `.strip()` guard in `ChatInput._on_send`). |
| tests/test_ui/test_tool_status_dialog_prefetch.py:133-157 - test_prefetched_data_skips_initial_worker_spawn | FIXED | Drives the real `ToolStatusDialog(tool_statuses=...)` prefetch render with a complete `ToolStatusEntry` payload. Replaces the two-row spot-check with a full-list oracle: all six rendered `QListWidgetItem` texts are compared exactly, in canonical tool order, against `_EXPECTED_ROWS` (independently constructed `"<icon>  <display_name> - <message>"` strings covering every tool's status glyph, name, and message), plus `currentRow() == 0`. A scrambled order, blanked row, wrong availability glyph, or dropped tool now fails. The partial-payload helper was likewise upgraded to compare all six rows exactly (`✓  Ghidra ...` followed by five `... <tool> - Status unknown` placeholders). |

## Notes on monkeypatch

The `ToolStatusCheckWorker.start` counting stub remains; it is not the operation under
test for these findings. The flagged prefetched-render path spawns zero workers, so the
stub only verifies that contract (`recorder.calls == 0`). The audit's sole violation for
this unit was the weak rich-output assertion, which is now a full exact-structure oracle.

## Verification (all six green)

```
pixi run ruff check tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> All checks passed!
pixi run ruff format tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
pixi run ruff format --check tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> 2 files already formatted (no changes)
pixi run basedpyright tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> No violations
pixi run pydocstyle tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> (no findings)
pixi run pytest tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py -p no:timeout
  -> 13 passed
```
