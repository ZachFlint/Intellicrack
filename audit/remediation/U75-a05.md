# Remediation U75-a05 (agent-05.md)

Unit owns these test files (whole files, never split):

- `tests/test_ui/test_realcov_15_chat_panel.py`
- `tests/test_ui/test_tool_status_dialog_prefetch.py`

## Per-finding outcomes

| file:line - test | Status | Real input / oracle / exact assertion now backing it |
| --- | --- | --- |
| `tests/test_ui/test_realcov_15_chat_panel.py:64 - test_send_button_emits_typed_text` | FIXED | Drives the real `ChatPanel` Send button via `qtbot`. Independently records both the inner `ChatInput.message_submitted` and the outer `ChatPanel.message_submitted` signals. Oracle is the user-supplied text `"disassemble the entry point"` (typed with surrounding whitespace to also gate `_on_send`'s `.strip()`). Asserts the inner signal fired exactly once with the stripped text, the panel relayed that single emission verbatim (gates the `_input.message_submitted.connect(self.message_submitted.emit)` wiring), the payload is exactly one `str`, the input cleared, and no bubble/stored message was created. Verified falsifiable: replacing the relay `connect` with `pass` makes the test fail. |
| `tests/test_ui/test_tool_status_dialog_prefetch.py:133 - test_prefetched_data_skips_initial_worker_spawn` | FIXED | Drives the real `ToolStatusDialog(tool_statuses=...)` end to end with **no** fake `QThread.start` (removed the monkeypatch/recorder for this path). Oracle is an independently built six-tool payload; asserts the real internal `dialog._status_workers` list is empty (no worker ever constructed/started), all six rows render exactly against `_EXPECTED_ROWS` in canonical `_tool_rows()` order, and each row's leading glyph is cross-checked against the supplied `available` flag (`✓`/`✗`), display name, and message. Verified falsifiable: swapping the production glyph ternary makes the test fail. |

## Collateral hardening (non-flagged tests in same file, fake-double removed / strengthened)

- `test_partial_prefetched_payload_renders_unknown_for_missing`: monkeypatch removed; now asserts on the real empty `_status_workers` list plus full six-row render with the `... <name> - Status unknown` placeholder for every missing tool in canonical order.
- `test_no_prefetched_data_spawns_workers` and `test_refresh_button_spawns_workers_even_after_prefetch`: now also assert the real `dialog._status_workers` list holds exactly one real `ToolStatusCheckWorker` per tool in canonical `_tool_rows()` order (only `QThread.start` framework plumbing is stubbed so no OS threads launch and the list stays deterministic).

## Verification block (pixi env, only touched files)

```
pixi run ruff check tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> All checks passed!
pixi run ruff format tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> 2 files left unchanged
pixi run ruff format --check tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> 2 files already formatted (no changes)
pixi run basedpyright tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> 0 errors, 0 warnings, 0 notes
pixi run pydoclint tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> No violations
pixi run pydocstyle tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py
  -> (no output / clean)
pixi run pytest tests/test_ui/test_realcov_15_chat_panel.py tests/test_ui/test_tool_status_dialog_prefetch.py -p no:timeout -p no:cacheprovider
  -> 13 passed, 2 warnings in 2.92s (random-order plugin active: order-independent)
```

No production source modified (mutation tests reverted; `git status` clean for `src/`).
