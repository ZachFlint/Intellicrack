# U64-a11 Remediation Report

## Findings and Resolutions

| Ref | Severity | Status | Resolution |
|-----|----------|--------|------------|
| `tests/test_audit4/d1_pyproject/test_runtime_extras_separation.py:144-151` — `test_dev_tools_absent_from_runtime_deps` | Low | FIXED | Fixed production defect F-0001: `pyproject.toml` `[project].dependencies` restructured from 113 packages to 15 genuine runtime imports. All 22+ dev-only packages (bandit, basedpyright, black, bumpversion, commitizen, flake8, isort, mkdocs-material, monkeytype, mypy, nox, pre-commit, pyannotate, pydoclint, pydocstyle, pylint, pytest, ruff, safety, sphinx, tox, twine) removed from runtime deps; they remain only in `[project.optional-dependencies]` extras. Runtime deps are now: anthropic, capstone, cryptography, frida, google-genai, httpx, huggingface-hub, keyring, lief, openai, psutil, pyyaml, r2pipe, structlog, yara-python (15 packages, well within the <= 25 budget). Both `test_dev_tools_absent_from_runtime_deps` and `test_runtime_deps_are_modest_in_size` now pass. |
| `tests/test_bridges/test_x64dbg.py:72-95` — `test_bridge_initial_state` / `test_bridge_has_capabilities` | Low | FIXED | `test_bridge_initial_state` now verifies state mutations (attached_pid, binary_path, is_64bit accept and retain mutations). `test_bridge_has_capabilities` now asserts supported_architectures is a non-empty list with at least 2 entries, supported_formats is a non-empty list, and supports_static_analysis is False. |
| `tests/test_credentials/test_oauth_manager_live.py:149-167` — `test_singleton_thread_safety` | Low | FIXED | After singleton identity check, invokes `generate_pkce_pair()` on the shared instance and asserts: code_verifier len > 40, code_challenge len > 30, verifier != challenge, `verify_pkce_pair()` returns True for fresh pair, returns False for mutated verifier. |
| `tests/test_hexcore_e2e/test_bridge_copy_as.py:61-83` — TestBridgeCopyAs format tests | Low | FIXED | All shape-only checks replaced with exact value assertions: c_array, rust_array, go_slice now parse hex tokens and decode via `int(t, 16)` to verify `b'\xde\xad\xbe\xef'`. python format verifies all 4 escape sequences present in inner string. markdown_table verifies all 4 hex values (DE/AD/BE/EF) present across data rows. |
| `tests/test_providers/test_google_provider.py:44-59` — `test_list_models_returns_non_empty_list` | Low | FIXED | After `len > 0`, filters models for 'gemini' in id.lower(), asserts at least one Gemini model exists, and checks first_gemini.id is non-empty str, provider == GOOGLE, context_window > 0. |
| `tests/test_providers/test_provider_bugfixes.py:45-49` — `test_init_model_discovery_is_coroutine` | Low | FIXED | Added `test_init_model_discovery_returns_discovery_and_cache_path` which actually awaits `init_model_discovery` with real `ProviderRegistry` + real `Config(data_directory=tmp_path)` and asserts the return is a `(ModelDiscovery, Path)` tuple with the correct cache filename. |

## Check Results

| Check | Result |
|-------|--------|
| ruff check | PASS — 0 findings |
| ruff format | PASS — all files formatted, no changes |
| basedpyright | PASS — 0 errors, 0 warnings, 0 notes |
| pydoclint | PASS — no violations |
| pydocstyle | PASS — no violations |
| pytest | PASS — 80 passed in 13.98s |
