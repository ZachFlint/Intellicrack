# Test-Gate Audit - `tests/test_sandbox`

**22 confirmed non-gate defects** (high: 5, medium: 7, low: 10) | 3 flags refuted (genuine gates).

## Confirmed non-gate tests

### `tests/test_sandbox/test_base_types.py`

- **TestTypedDictConstruction::test_file_change** (line 74) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: The test constructs a FileChange TypedDict and immediately asserts the values it just provided. It does NOT exercise any production code that reads these fields. TypedDicts have no runtime validation; renaming 'path' to 'file_path' in the type definition would not break this test because the keyword argument would still populate fc['path']. The actual gates (that FileChange fields are READ correctly by generate_timeline, etc.) are tested elsewhere. This test only verifies dict construction syntax, not production functionality.
  - Fix: Delete this test. The real gates are in test_analysis.py (test_file_events, test_full_sample_report) which actually call generate_timeline and assert on its output. Those tests verify that production code correctly reads the 'path' field when generating events.
  - Independent check: test_file_change (line 74) constructs FileChange(path='C:\\test.txt', ...) and then asserts fc['path'] == 'C:\\test.txt'. Because TypedDicts have no runtime key validation — they are plain dicts at runtime — renaming the 'path' field to anything else in the FileChange TypedDict definition (base.py line 115) would not break this test at all; the keyword argument still populates a dict key 'path' and the assert reads …

- **TestTypedDictConstruction::test_registry_change** (line 89) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: Identical to test_file_change. Constructs RegistryChange and asserts on values just provided. Does not exercise production code (match_behaviors, generate_timeline) that reads 'key' and 'operation' fields. Renaming those fields in the TypedDict definition would not break this test.
  - Fix: Delete this test. Real gates are in test_analysis.py (test_registry_events, test_full_sample_report) which call generate_timeline/match_behaviors and assert on actual field reading.
  - Independent check: test_registry_change (line 89) constructs RegistryChange(key='HKLM\\SOFTWARE\\Test', operation='modified', ...) and asserts rc['key'] and rc['operation']. Identical pattern: TypedDict is a plain dict at runtime, so renaming 'key' or 'operation' in the production RegistryChange definition (base.py lines 125-130) would not break this test. Production code in analysis.py that reads rc['key'] to match behaviors is never…

- **TestTypedDictConstruction::test_network_activity** (line 105) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: Same pattern: constructs NetworkActivity, asserts test-provided values. Does not exercise detect_c2_patterns or generate_timeline, which read 'bytes_sent', 'bytes_received', 'protocol', etc. Real production reads of these fields are not tested here.
  - Fix: Delete. Real gates in test_analysis.py: test_multiple_patterns_simultaneously, test_full_sample_report, and tests like test_balanced_traffic_not_exfil verify that production code correctly reads and interprets these fields.
  - Independent check: test_network_activity (line 105) constructs NetworkActivity(..., bytes_sent=100, bytes_received=200) and asserts na['protocol'] == 'tcp' and na['bytes_sent'] == 100. No production consumer of bytes_sent/bytes_received/protocol is invoked; analysis.py's detect_c2_patterns and generate_timeline are not called. If 'bytes_sent' were renamed in the TypedDict definition (base.py line 142), this test would still pass becau…

- **TestTypedDictConstruction::test_api_call** (line 134) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: Constructs ApiCall, asserts test-provided 'api_name' and 'arguments'. Does not call match_behaviors (which reads 'api_name' to detect anti-debug patterns via _ANTI_DEBUG_APIS) or _timeline_add_api_events. No production read path exercised.
  - Fix: Delete. Real gate: add a test that calls match_behaviors with an ApiCall containing 'IsDebuggerPresent' and assert the T1497 match is returned. That would fail if api_name reading breaks.
  - Independent check: test_api_call (line 134) constructs ApiCall(api_name='CreateFileW', arguments=['arg1','arg2'], ...) and asserts ac['api_name'] == 'CreateFileW' and len(ac['arguments']) == 2. The production function match_behaviors in analysis.py reads api_name to detect anti-debug patterns via _ANTI_DEBUG_APIS, but that function is never called here. Renaming 'api_name' in the TypedDict definition (base.py line 166) would not break…

- **TestTypedDictConstruction::test_ioc_entry** (line 226) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: Constructs IOCEntry, asserts 'ioc_type' and 'value' fields. No production code exercises these fields (IOCEntry is produced by extract_iocs, not consumed by tests here). This test only validates dict construction, not that any code reads IOCEntry fields correctly.
  - Fix: Delete. IOCEntry objects are created by extract_iocs; the real gate is the test_full_sample_report test which asserts extract_iocs produces iocs and checks their types. That indirectly verifies ioc_type field is set.
  - Independent check: test_ioc_entry (line 226) constructs IOCEntry(ioc_type='ipv4', value='203.0.113.1', ...) and asserts ioc['ioc_type'] == 'ipv4' and ioc['value'] == '203.0.113.1'. IOCEntry is produced by extract_iocs in analysis.py; no production code that reads ioc['ioc_type'] or ioc['value'] is called in this test. TypedDicts have no runtime field validation, so renaming these fields in the production definition (base.py lines 251-…

### `tests/test_sandbox/test_analysis.py`

- **TestDetectC2Patterns::test_multiple_patterns_simultaneously** (line 635) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Fixture sample_network_activity is designed to trigger beaconing (5x 185.220.101.45 connections), DGA (xkqwzjrtmnpv.evil.com), C2 port (62.102.148.69:4444), and exfiltration (5MB to 51.15.192.49). That's 4+ pattern types. Test only checks len(pattern_types) >= 2. If production removes _detect_beaconing() call entirely from detect_c2_patterns, test would still pass because dga_domain, known_c2_port, and data_exfiltration would still be detected (>= 2). This gate misses the realistic breakage of disabling one detector.
  - Fix: Add explicit assertions: assert 'beaconing' in pattern_types, assert 'dga_domain' in pattern_types, assert 'data_exfiltration' in pattern_types, assert 'known_c2_port' in pattern_types. This makes each detector a hard gate.
  - Independent check: The test at line 635 asserts only `len(pattern_types) >= 2`. The fixture `sample_network_activity` produces 4 pattern types: beaconing (5 uniform-interval connections), dga_domain (xkqwzjrtmnpv.evil.com, entropy well above 3.5), known_c2_port (port 4444), and data_exfiltration (5MB sent / 100 received, ratio > 10). Removing `_detect_beaconing()` from `detect_c2_patterns` entirely still leaves dga_domain, known_c2_po…

### `tests/test_sandbox/test_realcov_04_sandbox_bridge.py`

- **TestToolDefinitionDispatchEndToEnd::test_status_dispatch** (line 58) - `mock-shadows-target` / **medium** - _[verified accurate]_
  - Why it is not a gate: The `sandbox_bridge` fixture is backed by `StubManager` (from conftest.py), not the real `SandboxManager`. The fixture's `get_status()` returns hardcoded data with `total_count: len(self._instances)` = 2 because two stub instances are pre-populated. The real `SandboxManager.get_status()` is never called. If the real manager's method signature changed, returned a different structure, or was removed entirely, this test would still pass because it exercises only the stub.
  - Fix: Instantiate the real `SandboxManager` instead of `StubManager`, or add a separate test that calls through to the real manager. Alternatively, verify that the returned data matches the documented API contract by asserting specific fields beyond just presence (e.g., `status['available_types']` is a list of strings).
  - Independent check: The sandbox_bridge fixture (conftest.py line 1473) attaches a StubManager whose get_status() (conftest.py lines 915-937) returns hardcoded data including `total_count: len(self._instances)` = 2. SandboxBridge.status() (sandbox_bridge.py line 1376-1384) simply calls `manager.get_status()` and the test asserts `result['total_count'] == 2`. If the real SandboxManager.get_status() changed its return schema or was remove…

- **TestToolDefinitionDispatchEndToEnd::test_list_dispatch** (line 65) - `mock-shadows-target` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: The `sandbox_bridge` fixture uses `StubManager` whose `get_status()` builds the instance list from a hardcoded `_instances` dict. The real `SandboxBridge.list()` delegates to `manager.get_status()['instances']`; since `StubManager` provides the data, the assertion that returned ids match `{'win-test-001', 'qemu-test-001'}` validates only the stub's fixed data, not the real manager's list serialization path. A regression in the real manager would not be caught.
  - Fix: Use the real `SandboxManager` with real instances, or at minimum assert that returned entries include a field (like `created_at` as an ISO8601 string) that requires the real bridge serialization logic to run.
  - Independent check: The defect is real (mock-shadows-target) but the claim's mechanism is wrong. The claim states 'SandboxBridge.list() delegates to manager.get_status()["instances"]' but the actual production code at sandbox_bridge.py lines 1386-1405 iterates `manager.instances` directly (a property), NOT get_status(). StubManager.instances (conftest.py line 792) returns hardcoded stub instances from `_instances` dict. The assertions …

- **TestStubManagerFullCallChain::test_create_updates_state_with_real_fields** (line 129) - `mock-shadows-target` / **medium** - _[verified accurate]_
  - Why it is not a gate: Despite its docstring claiming 'non-mock call chain', the `sandbox_bridge` fixture is backed by `StubManager`, not the real `SandboxManager`. When `bridge.create(sandbox_type='qemu')` is called, it creates an `InMemoryQEMUSandbox` via the stub, not a real QEMU sandbox. The assertion `result['type'] == 'qemu'` checks what the stub returned; the real `SandboxManager.create()` path (which calls `QEMUSandbox.__init__`, checks availability, and performs actual setup) is never exercised. A regression in the real `create()` implementation would not be detected.
  - Fix: Clarify the test's intent: either rename it to explicitly document that it tests stub-backed bridge wiring (not the full production path), or use the real `SandboxManager` for genuine integration testing (accepting that it may require environment capabilities and skip cleanly when absent).
  - Independent check: The sandbox_bridge fixture is backed by StubManager. When bridge.create(sandbox_type='qemu') is called, it invokes StubManager.create() (conftest.py lines 809-841) which creates an InMemoryQEMUSandbox and immediately starts it. StubInstance.id is a UUID and stub.state.status is 'running'. The assertions `result['type'] == 'qemu'` and `result['status'] == 'running'` check what StubManager returned. The real SandboxMa…

### `tests/test_sandbox/test_realcov_12b_analysis_real.py`

- **test_generate_timeline_orders_real_events** (line 414) - `conditional-never-runs` / **medium** - _[verified accurate]_
  - Why it is not a gate: At line 414, the guard `if report.process_activity:` protects the strongest assertion `assert process_only`. On a system where the real PowerShell process monitor runs but parsing yields an empty list (e.g., due to a parser schema mismatch), `report.process_activity` would be `[]` (falsy), the if body never executes, and the test passes even though the end-to-end capture-parse-filter pipeline is broken. The earlier assertion at line 410 (`assert categories & {'process', 'network'}`) provides only partial coverage, checking only that the combined timeline from all sources includes process/network categories, not that process_activity itself is populated.
  - Fix: Remove the conditional guard and assert unconditionally that `process_only` is non-empty after filtering: `assert process_only, 'process_activity must be captured and generate timeline events'`. A real Windows kernel monitor capture always observes at least one process (kernel, services, etc.), so empty process_activity indicates a genuine parsing or capture failure that the test must catch.
  - Independent check: At test line 414, `if report.process_activity:` guards the assertion `assert process_only, 'process category timeline must be non-empty when real process activity exists'`. This is the strongest assertion - that the process-category filter correctly returns events when process data exists. If the real PowerShell process monitor runs but the parser yields an empty list (e.g., due to a schema mismatch in parse_process…

### `tests/test_sandbox/test_sandbox_bridge.py`

- **TestInitializeShutdown::test_ensure_manager_creates** (line 308) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only checks `assert mgr is not None`. While it WOULD fail if ensure_manager() returns None (a logic error), it would NOT fail if the method returns an incorrectly initialized SandboxManager or a wrong object type. For example, if the line `self._manager = SandboxManager()` were changed to `self._manager = object()`, the test would pass but subsequent operations using the manager (like `create()` or `get_available_types()`) would fail. The test verifies existence but not correctness.
  - Fix: Assert that the returned value is an instance of SandboxManager and that it has callable methods expected by the bridge, e.g., `assert isinstance(mgr, SandboxManager)` and `assert hasattr(mgr, 'get_available_types')`. Alternatively, call a method on the manager within this test to verify it is functional.
  - Independent check: The test at line 308 only asserts `mgr is not None`. `ensure_manager()` creates a `SandboxManager()` instance and returns it. If the production code were changed to return an `object()` instance or a differently-typed wrong object, the assertion still passes since any object is not None. The test does not verify the returned object is actually a `SandboxManager` with functioning methods. The weak-existence classific…

- **TestAnalysisWrappers::test_diff_success** (line 1049) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The diff structure is checked only with `assert len(diff_section) > 0`. The `diff_reports` function always returns at minimum `{"scalars": {...}}` plus 11 field comparison dicts (file_changes, registry_changes, network_activity, etc.), so the length is always >= 12. However, if the entire field_key_map loop (lines 1319-1344) were accidentally deleted, diff_reports would still return `{"scalars": {...}}` with len=1, and the test would pass despite losing all field-level comparison logic. A regression that removes per-field diffs would not be caught.
  - Fix: Assert the presence of specific per-field diff keys known to differ between the win and qemu fixture instances. For example, `assert "file_changes" in diff_section` and `assert diff_section["file_changes"]["unique_to_a"]` (win has a file change that qemu does not). Also verify `assert diff_section["network_activity"]["unique_to_a"]` to ensure network activity comparison is included.
  - Independent check: The test at line 1049 only asserts `len(diff_section) > 0`. The production `diff_reports()` at analysis.py:1294 always initializes `result` with at least the `'scalars'` key before any loop iteration, so `len >= 1` is always true. If the entire `field_key_map` loop (lines 1319-1344) were deleted, `diff_reports` would still return `{'scalars': {...}}` with len=1, passing the test despite losing all 11 per-field compa…

### `tests/test_sandbox/test_analysis.py`

- **TestExtractIOCs::test_full_sample_report** (line 848) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: sample_report fixture contains network activity (ipv4), file paths (sha256), registry values, and process command lines (urls, emails). Should produce 5+ IOC types (ipv4, domain, url, sha256, email). Test only checks len(ioc_types) >= 2. Removing 3 IOC extractors from extract_iocs would still leave 2+ types and test passes. Does not gate each individual extractor.
  - Fix: Assert each expected IOC type: assert 'ipv4' in ioc_types, assert 'url' in ioc_types, assert 'sha256' in ioc_types, etc. Matching the sample_report fixture content.
  - Independent check: The test at line 848 asserts only `len(ioc_types) >= 2`. The full `sample_report` fixture populates: network_activity (public IPs → ipv4, DGA domain → domain), registry_changes (admin@evil.com → email, C:\Temp\payload.exe value_data), process_activity (https://evil.com/dl.exe command_line → url, domain). Production normally yields ipv4, domain, url, and email (4+ types). Removing three IOC extractors (e.g., _scan_te…

- **TestGenerateTimeline::test_full_sample_report** (line 1114) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: sample_report fixture (see conftest starting ~line 1416) is populated with file_changes, registry_changes, network_activity, process_activity, api_calls, service_changes, kernel_objects, dll_loads, injection_events, clipboard_events. That's 10 categories total. Test checks len(categories) >= 5. Production could disable 5 category generators (e.g., remove _timeline_add_dll_events, _timeline_add_injection_events, _timeline_add_service_events, _timeline_add_kernel_events, _timeline_add_clipboard_events) and test would still pass with file, registry, network, process, api categories remaining (5 categories). Does not individually gate each category generator.
  - Fix: Assert all 10 categories or at least a representative set: assert 'file' in categories, assert 'network' in categories, assert 'api' in categories, etc. Make each timeline generator a hard gate.
  - Independent check: The test at line 1114 asserts `len(categories) >= 5`. The `sample_report` fixture provides data for all 10 category generators in `generate_timeline`: file, registry, network, process, api, service, kernel, dll, injection, clipboard. Removing 5 of the 10 generator calls (_timeline_add_dll_events, _timeline_add_injection_events, _timeline_add_service_events, _timeline_add_kernel_events, _timeline_add_clipboard_events…

- **TestMatchBehaviors::test_full_sample_report** (line 1467) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: sample_report contains service creation, registry persistence, injection events, and api calls. Should trigger Persistence (service + registry), Evasion (injection), and potentially Discovery. Test checks len(categories) >= 3. Production could break/disable 2 matching categories and test still passes with 3+ remaining. Does not individually gate each behavior category.
  - Fix: Assert specific MITRE categories expected from sample_report: assert 'Persistence' in categories, assert 'Defense Evasion' in categories, etc. This makes each matching category a hard gate.
  - Independent check: The test at line 1467 asserts `len(categories) >= 3`. The `sample_report` fixture triggers all five MITRE category matchers: Persistence (service creation via MalSvc, Run key via HKLM Run, schtasks.exe), Defense Evasion (injection event, IsDebuggerPresent API, Sleep(120000ms)), Command and Control (beaconing from network, DoH to 1.1.1.1:443), Exfiltration (5MB outbound, clipboard read), Discovery (whoami.exe). That'…

### `tests/test_sandbox/test_manager.py`

- **TestSandboxInstance::test_touch_updates_last_used** (line 294) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The assertion `inst.last_used >= before` where `before = inst.last_used` is reflexive: if `touch()` becomes a no-op (e.g., a body removed but method still exists), the test passes because `last_used` unchanged means `last_used >= last_used` is true. A production regression where `touch()` deletes the entire method would raise AttributeError and fail the test, but a subtle mutation to `pass` would not be caught. The `>=` operator should be replaced with `>` combined with a small sleep to enforce that the timestamp genuinely advances.
  - Fix: Use strict `>` comparison and add `await asyncio.sleep(0.01)` before calling `touch()` to ensure the timestamp advances beyond the initial value, catching no-op implementations.
  - Independent check: The test captures `before = inst.last_used` (an immutable datetime), calls `inst.touch()`, then asserts `inst.last_used >= before`. If `touch()` is mutated to a no-op (`pass`), `inst.last_used` remains the same datetime object as `before`, making `before >= before` reflexively true. The `>=` operator cannot detect this failure mode. The test only uses `_TestInstance` (not the real `SandboxInstance`), and the real `S…

- **TestSandboxInstance::test_last_report_settable** (line 309) - `other` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The test instantiates `_TestInstance` (a test-local helper class) instead of the real `SandboxInstance` from `intellicrack.sandbox.manager`. The assertion `inst.last_report is report` only validates that the test helper's `__init__` stores the constructor argument. If the real `SandboxInstance.last_report` field were removed, renamed, or made read-only, this test would never detect it because it exercises only the stub.
  - Fix: Instantiate the real `SandboxInstance` (as done in `test_timestamps_are_set` at line 272) and test its `last_report` property directly.
  - Independent check: The finding is correct that the test exercises `_TestInstance` (the test-local stub class) rather than the real `SandboxInstance` imported from `intellicrack.sandbox.manager`. However, the category 'mock-shadows-target' is wrong: no `mock.patch` or `unittest.mock` is involved. The test uses a hand-rolled substitute class defined in the same test file — this is a fixture-substitution problem, not mock shadowing. The …

- **TestSandboxInstance::test_binary_path_settable** (line 329) - `other` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The test uses `_TestInstance(sb, binary_path=Path('test.exe'))` then asserts the constructor argument was stored. This validates only that the test helper's `__init__` stores parameters; the real `SandboxInstance.binary_path` is never exercised. A regression in the real class (e.g., removal of the `binary_path` field) would not be caught.
  - Fix: Use the real `SandboxInstance` class (from `intellicrack.sandbox.manager`) and test that the property can be read and written after instantiation.
  - Independent check: Same structural problem as G0305: the test exercises `_TestInstance(sb, binary_path=Path('test.exe'))` and asserts `inst.binary_path == Path('test.exe')`. This validates `_TestInstance.__init__`'s parameter storage (line 231 of the test file), not the real `SandboxInstance.binary_path` assignment (manager.py line 87). No `mock.patch` or `unittest.mock` is involved, so 'mock-shadows-target' is the wrong category. 'ot…

- **TestManagerProperties::test_instances_returns_copy** (line 349) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: The assertion `assert copy is not getattr(mgr, '_instances')` compares object identity between the returned list and the internal dict. Since `mgr.instances` returns `list(self._instances.values())`, a new list object is always created, making this assertion tautologically true regardless of any production change. The assertion does not check that mutations to the returned list do not affect the manager's internal state (the real contract being protected).
  - Fix: After calling `mgr.instances`, append a sentinel value to the returned list and assert that `len(mgr.instances)` is unchanged, proving the list is genuinely independent from the manager's internal dictionary.
  - Independent check: The assertion `copy is not getattr(mgr, '_instances')` is tautologically true: `mgr.instances` returns `list(self._instances.values())` (a new list object), while `mgr._instances` is a dict. A list can never be `is` the same object as a dict, so this identity check always passes regardless of the implementation. The real contract being protected (mutations to the returned list should not affect internal state) is ne…

- **TestManagerCreate::test_create_returns_instance** (line 361) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The assertion `assert inst.id is not None` passes for any non-None value. The test uses `_TestInstance` which generates ids via `f'test-{_TestInstance._counter:04d}'`, always a non-empty string. This assertion does not validate that the id is truly unique, non-empty, or matches the id stored in the manager. A real `SandboxManager.create()` using UUID-based ids would return a different id format, but the test would still pass.
  - Fix: Assert that the returned id is a non-empty string (e.g., `assert isinstance(inst.id, str) and len(inst.id) > 0`), and verify the id appears in the manager's instance list via `assert inst.id in {i.id for i in await mgr.list()}`.
  - Independent check: The test calls `await mgr.create()` and asserts `inst.id is not None`. `_TestableManager.create()` instantiates `_TestInstance`, whose `__init__` sets `self.id = f'test-{_TestInstance._counter:04d}'` — always a non-empty string, never None. The assertion cannot fail under any realistic breakage because the id is generated deterministically from a counter in the test fixture, not from the real `SandboxInstance` which…

- **TestManagerStatus::test_status_has_expected_keys** (line 476) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Assertions only check that four keys exist in the dict (`'max_instances' in status`, etc.) without asserting their values. A broken `get_status()` returning `{'max_instances': None, 'active_count': -999, 'total_count': 'wrong', 'instances': None}` would pass all assertions. The test does not validate the actual data types or bounds of the values returned.
  - Fix: Assert value correctness for a fresh manager: `assert status['max_instances'] == _MAX_INSTANCES`, `assert status['active_count'] == 0`, `assert status['total_count'] == 0`, and `assert status['instances'] == []`. If the manager has active instances, validate their structure and field types.
  - Independent check: The test only checks `'max_instances' in status`, `'active_count' in status`, `'total_count' in status`, and `'instances' in status` — pure key-existence checks with no value validation. A broken `_TestableManager.get_status()` returning `{'max_instances': None, 'active_count': -999, 'total_count': 'wrong', 'instances': None}` would pass all four assertions. The finding, category (weak-existence), and severity (low)…

### `tests/test_sandbox/test_sandbox_bridge.py`

- **TestBridgeInstantiation::test_not_none** (line 79) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: A successful Python constructor call cannot return None by definition. The assertion `assert bridge is not None` is logically always true. The only way it could fail is if an exception is raised during __init__, but then the test terminates before reaching the assertion. No realistic production regression in SandboxBridge.__init__ logic would cause this test to fail; any breakage either raises an exception (failing earlier) or succeeds in creating the object (passing the assertion).
  - Fix: Remove this test or replace it with assertions that verify critical bridge state is initialized correctly, such as `assert isinstance(bridge.name, ToolName)`, `assert isinstance(bridge._capabilities, BridgeCapabilities)`, and `assert hasattr(bridge, '_manager')`.
  - Independent check: The test at line 79 asserts `bridge is not None` immediately after `bridge = SandboxBridge()`. In Python, a constructor call always returns an instance of the class (or raises an exception). It is impossible for a constructor to return `None`. Therefore, this assertion is logically tautological — it can never be False unless an exception is raised first, and if an exception is raised the test fails before reaching t…

## Refuted flags (verified to be real gates - no action needed)

- `tests/test_sandbox/test_analysis.py` :: **TestRealWorldSandboxReport::test_emotet_multiple_pattern_types_detected** (line 1815) - The three specific assertions at lines 1819-1821 (assert 'beaconing' in pattern_types, assert 'data_exfiltration' in pattern_types, assert 'known_c2_port' in pattern_types) ARE real gates. These will fail if any of those three detectors break. The >= 3 check at line 1815 is indeed redundant given the explicit checks that follow, but the test overall is NOT a non-gate because the three 'in' assertions provide hard coverage of the critical patterns. If _detect_beaconing is broken, the test fails at line 1819. The redundancy is minor. _[Independent check: The finding was already marked confirmed=false by the original auditor. The test contains hard membership assertions at lines 1819-1821: `assert 'beaconing' in pattern_types`, `assert 'data_exfiltration' in pattern_types`, `assert 'known_c2_port' in pattern_types`. These are specific gates on three distinct detectors. If `_detect_beaconing` is removed or broken, the test fails at line 1819. If `_detect_exfiltration_…]_
- `tests/test_sandbox/test_realcov_04_sandbox_bridge.py` :: **TestStubManagerFullCallChain::test_destroy_then_list_reflects_removal** (line 157) - Although `sandbox_bridge` is backed by `StubManager`, the test validates a genuine contract: that `destroy()` removes an instance from the manager's view, as reflected by `list()`. The implementation of the destroy() and list() contract is correctly tested here - the stub-backed test confirms the bridge wiring is correct. While the real sandbox stop/teardown is not exercised, the manager's state-mutation contract (remove from dict, reflect in list) is a valid gate that would catch if destroy() or list() had a bug in their integration. _[Independent check: This test exercises real bridge logic, not just a stub. SandboxBridge.destroy() (sandbox_bridge.py lines 1096-1129) calls manager.destroy(instance_id) and on success returns {"success": True}. SandboxBridge.list() (lines 1386-1405) iterates manager.instances. Both the StubManager.destroy() (conftest.py lines 854-867) and StubManager.instances (line 792) track the same _instances dict with real state mutations - dest…]_
- `tests/test_sandbox/test_realcov_12b_analysis_real.py` :: **test_diff_reports_on_two_real_captures** (line 554) - Although the auditor recommends a partition invariant assertion, the test does validate a meaningful contract: line 580 (`assert proc_diff['common']`) ensures that `common` is non-empty after diffing two real captures of the same host. This assertion would fail if `diff_reports()` broke the partitioning and placed all processes into `unique_to_a` instead of `common`. The test also validates that process_activity from both reports is represented in the diff. The recommended partition invariant would strengthen the test, but the current assertions do catch real diff_reports() regressions. _[Independent check: The test runs two real captures (lines 566-567: _capture_real_report for both tmp_path/a and tmp_path/b), calls the real diff_reports() function (line 569), and at line 579 asserts `assert proc_diff['common'], 'two captures of the same host must share long-lived processes in common'`. This assertion will fail if diff_reports() breaks partitioning and places all processes into unique_to_a or unique_to_b instead of co…]_
