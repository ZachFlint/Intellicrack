{
  "file": "tests/test_hexcore_e2e/test_bridge_hash_advanced.py",
  "findings_addressed": [
    {
      "id": "G0151",
      "test": "TestCalculateHashRange::test_sha256_on_first_16_bytes_of_pe_is_nonempty_hex",
      "action": "hardened",
      "oracle": "hashlib.sha256(pe_bytes[:16]).hexdigest() — independent stdlib SHA-256 computation over the same first 16 bytes of the PE fixture",
      "documented_mutation": "In the production calculate_hash_range implementation, change 'sha256' digest algorithm to 'md5' (or return a wrong slice) — the assertion `result.lower() == expected.lower()` would fail because the MD5 digest differs from the SHA-256 digest."
    },
    {
      "id": "G0152",
      "test": "TestCalculateHashRange::test_sha1_range_is_valid_hex_digest",
      "action": "hardened",
      "oracle": "hashlib.new('sha1', _KNOWN_PAYLOAD[:64], usedforsecurity=False).hexdigest() — independent stdlib SHA-1 computation over bytes 0..64 of _KNOWN_PAYLOAD",
      "documented_mutation": "In the production calculate_hash_range implementation, change the range end from the supplied 'end' parameter to 'end - 1' — the assertion `result.lower() == expected.lower()` would fail because the digest of bytes 0..63 differs from bytes 0..64."
    },
    {
      "id": "G0153",
      "test": "TestCalculateHashCustomCrc::test_crc16_on_pe_bytes_returns_hex_string",
      "action": "hardened",
      "oracle": "_crc16_ccitt(pe_bytes[:16]) formatted as 04x — independent Python CRC-16/CCITT-FALSE reference computation over the first 16 bytes of the PE fixture",
      "documented_mutation": "In the production calculate_hash_custom_crc implementation, initialise the CRC accumulator with 0x0000 instead of the supplied init=0xFFFF — the resulting CRC-16 value would differ and the assertion would fail."
    },
    {
      "id": "G0154",
      "test": "TestCalculateHashCustomCrc::test_crc8_returns_valid_hex_string",
      "action": "hardened",
      "oracle": "_crc8_smbus(bytes(range(16))) formatted as 02x — independent Python CRC-8/SMBUS reference computation over the 16-byte payload",
      "documented_mutation": "In the production calculate_hash_custom_crc implementation, change the polynomial from the supplied poly=0x07 to 0x1D — the resulting CRC-8 value would differ and the assertion would fail."
    },
    {
      "id": "G0155",
      "test": "TestCalculateHashRange::test_md5_range_differs_from_sha256_range",
      "action": "deleted",
      "oracle": "test_sha256_result_matches_hashlib_on_same_slice and test_md5_range_matches_hashlib each independently verify correctness with a hashlib oracle on the same range; the mutual-inequality test adds no additional gate",
      "documented_mutation": "N/A — deleted as a redundant duplicate; the covering sibling tests already falsify any bug in the hash computation."
    },
    {
      "id": "G0156",
      "test": "TestCalculateHashCustomCrc::test_crc32_result_is_8_hex_chars",
      "action": "hardened",
      "oracle": "binascii.crc32(bytes(range(64))) & 0xFFFFFFFF formatted as 08x — stdlib CRC-32/ISO-HDLC computation (refin=True, refout=True, xorout=0xFFFFFFFF) over the same 64-byte payload",
      "documented_mutation": "In the production calculate_hash_custom_crc implementation, omit the xorout step (skip XOR with 0xFFFFFFFF) — the resulting CRC-32 value would differ from the binascii.crc32 oracle and the assertion would fail."
    }
  ],
  "prod_defects": [],
  "deletions": [
    {
      "test": "TestCalculateHashRange::test_md5_range_differs_from_sha256_range",
      "covered_by": "TestCalculateHashRange::test_sha256_result_matches_hashlib_on_same_slice and TestCalculateHashRange::test_md5_range_matches_hashlib"
    }
  ],
  "self_quality": {
    "ruff": "pass",
    "basedpyright": "pass",
    "pydoclint": "pass",
    "pydocstyle": "pass"
  },
  "notes": "All six findings resolved: five tests hardened with independent oracle comparisons (hashlib/binascii/_crc16_ccitt/_crc8_smbus), one weak mutual-inequality test deleted as a redundant duplicate covered by two stronger hashlib-oracle siblings. No production defects discovered. The pe_bytes fixture was added as a parameter to G0151 and G0153 tests to provide the byte content for oracle computation without re-reading the file."
}
