{
  "file": "tests/test_hexcore_e2e/test_bridge_ai_context.py",
  "findings_addressed": [
    {
      "id": "G0126",
      "test": "TestBridgeAIContext::test_get_context_for_ai_contains_expected_top_level_keys",
      "action": "hardened",
      "oracle": "pe_binary fixture Path (for file_path), _PE_BINARY_SIZE=1024 derived from conftest._build_pe_binary() bytearray(1024), cursor=0 (no goto_offset called), modified=False (read-only open)",
      "documented_mutation": "In src/intellicrack/bridges/hex_editor.py get_document_info(), change `'size': self.document.length()` to `'size': 0` — the assertion `ctx['size'] == 1024` immediately fails."
    },
    {
      "id": "G0127",
      "test": "TestBridgeAIContext::test_get_context_for_ai_bookmarks_is_list",
      "action": "deleted",
      "oracle": "Deleted as redundant non-gate (adversarial bounce): the empty-bookmark case is indistinguishable from a no-op implementation and cannot be falsifiable. The bookmark serialization path is fully gated by the sibling test_get_context_for_ai_bookmarks_contain_expected_fields_when_present (line 104), which seeds a real bookmark and asserts offset/length/label.",
      "documented_mutation": "N/A - deleted per decision rule 2 (redundant duplicate)."
    },
    {
      "id": "G0128",
      "test": "TestBridgeAIContext::test_get_context_for_ai_size_is_positive",
      "action": "hardened",
      "oracle": "_PE_BINARY_SIZE=1024, computed independently from conftest._build_pe_binary() which returns bytes(bytearray(1024)) — a fixed-size 1024-byte PE stub.",
      "documented_mutation": "In src/intellicrack/bridges/hex_editor.py get_document_info(), change `'size': self.document.length()` to `'size': self.document.length() + 1` — the assertion `ctx['size'] == 1024` immediately fails."
    }
  ],
  "prod_defects": [],
  "deletions": [],
  "self_quality": {
    "ruff": "pass",
    "basedpyright": "pass",
    "pydoclint": "pass",
    "pydocstyle": "pass"
  },
  "notes": "G0126 hardened by adding pe_binary fixture param and asserting file_path, size, cursor, modified concretely. G0127 hardened by asserting bookmarks==[] and bookmark_count_total==0. G0128 hardened by asserting size==1024 (exact oracle from conftest bytearray(1024)). _EXPECTED_TOP_LEVEL_KEYS module-level set removed as it was only used in the now-hardened G0126 test. All four quality tools pass with zero findings."
}
