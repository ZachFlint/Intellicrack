{
  "file": "tests/test_ui/test_graph_view.py",
  "findings_addressed": [
    {
      "id": "G0327",
      "test": "TestEdgeItem::test_path_is_set",
      "action": "hardened",
      "oracle": "QPainterPath element count: moveTo(start) produces 1 MoveToElement; cubicTo(ctrl1, ctrl2, end) produces 3 CurveToElement family members; total must be exactly 4. Bounding rect top must equal start.y=0 and bottom must reach end.y=200 for start=QPointF(50,0), end=QPointF(50,200).",
      "documented_mutation": "In graph_view.py EdgeItem.__init__, replace `path.cubicTo(ctrl1, ctrl2, end)` with `path.lineTo(end)` — this makes elementCount() return 2 instead of 4, failing the assertion."
    }
  ],
  "prod_defects": [],
  "deletions": [],
  "self_quality": {
    "ruff": "pass",
    "basedpyright": "pass",
    "pydoclint": "pass",
    "pydocstyle": "pass"
  },
  "notes": "G0327: Replaced weak path.isEmpty() existence check with a dual-oracle assertion: (1) elementCount()==4 proves moveTo+cubicTo structure (not a degenerate stub), (2) boundingRect top/bottom proves the path actually spans from start.y to end.y. Falsifiable: replacing cubicTo with lineTo changes elementCount to 2."
}
