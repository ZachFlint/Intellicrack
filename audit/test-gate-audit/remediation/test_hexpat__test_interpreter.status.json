{
  "file": "tests/test_hexpat/test_interpreter.py",
  "findings_addressed": [
    {
      "id": "G0246",
      "test": "TestBitfields::test_bitfield_extraction",
      "action": "hardened",
      "oracle": "Independent bit-masking/shifting: for raw_byte=0xB5 (0b10110101) with right_to_left order, expected_a=(0xB5>>0)&0x1=1, expected_b=(0xB5>>1)&0x1=0, expected_c=(0xB5>>2)&0x3=1, expected_d=(0xB5>>4)&0xF=11=0xB; display strings formatted as f'0x{val:X} ({width} bits)' matching evaluator line 1595",
      "documented_mutation": "In evaluator.py line 1586, change 'shift = ... bit_pos' to 'shift = 0' (always shift by zero); this makes all four fields decode as (0xB5 >> 0) & mask, so a=1, b=1, c=1, d=0xB5&0xF=5 -- b and c would have wrong display_values and the assertions fail"
    },
    {
      "id": "G0247",
      "test": "TestArrays::test_u8_array",
      "action": "hardened",
      "oracle": "Independent hex formatting: for raw_values=[10,20,30,40], expected displays are ['0xA','0x14','0x1E','0x28'] computed via f'0x{v:X}' matching evaluator _format_value line 711",
      "documented_mutation": "In evaluator.py _eval_array_type, change 'current_offset += int(elem[\"size\"])' to 'pass' (remove offset advancement); all four elements would be read from offset 0, making all display_values '0xA' instead of distinct values -- kids[1..3] display_value assertions fail"
    },
    {
      "id": "G0248",
      "test": "TestOutputFormat::test_parsed_field_keys",
      "action": "hardened",
      "oracle": "Independent computation: raw_byte=42, 42=0x2A so display_value='0x2A' via format(42,'X'); offset=0 (@ 0), size=1 (u8), raw_bytes=[42] (list(bytes([42]))), children=[] (primitive), description='' (no annotation)",
      "documented_mutation": "In evaluator.py _make_parsed_field line 295, change '\"offset\": offset' to '\"offset\": offset + 1'; field['offset'] would be 1 instead of 0, failing the assertion assert field['offset'] == 0"
    }
  ],
  "prod_defects": [],
  "deletions": [],
  "self_quality": {
    "ruff": "pass",
    "basedpyright": "pass",
    "pydoclint": "pass",
    "pydocstyle": "pass"
  },
  "notes": "All three findings were N8 (existence/type-only) weak assertions. G0246 now asserts exact bit-extracted display values for all four bitfield members using hand-computed expected values from the same bit-masking math the evaluator uses (right_to_left order, default). G0247 now asserts all four element display_values and sizes, gating the offset-advancement logic in _eval_array_type. G0248 now asserts exact values for every required key (name, offset, size, raw_bytes, display_value, children, description) rather than just key presence; validation_passed is not asserted because it is always None from _make_parsed_field (not a defect, just uninitialised metadata)."
}
