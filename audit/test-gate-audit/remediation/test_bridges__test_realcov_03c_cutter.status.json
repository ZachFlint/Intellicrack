{
  "file": "tests/test_bridges/test_realcov_03c_cutter.py",
  "findings_addressed": [
    {
      "id": "G0090",
      "test": "TestRealDisassembly::test_disassemble_function_text",
      "action": "hardened",
      "oracle": "structured disassemble() output for the same address: oracle_mnemonic = structured[0].mnemonic.lower().split()[0] must appear in at least one line of the disassemble_function() text, plus a frozenset of 20 known x86 mnemonics must match at least one line",
      "documented_mutation": "In src/intellicrack/bridges/cutter.py, change `result = await self._r2_cmd(f'pdf @ {address}')` to `result = 'placeholder'` — the oracle_mnemonic substring check and the x86 mnemonic membership check both fail immediately"
    },
    {
      "id": "G0091",
      "test": "TestRealByteIo::test_hexdump_real",
      "action": "hardened",
      "oracle": "read_bytes() at the same .text section VA: first_byte_hex = f'{raw[0]:02x}' must appear in the hexdump() and hexdump_words() output strings",
      "documented_mutation": "In src/intellicrack/bridges/cutter.py, change `result = await self._r2_cmd(f'px {length} @ {address}')` to `result = 'test output'` — first_byte_hex will not appear in 'test output' and the assertion fails"
    },
    {
      "id": "G0092",
      "test": "TestRealMetadata::test_headers_real",
      "action": "hardened",
      "oracle": "frozenset of 11 known PE header field names (machine, numberofsections, magic, imagebase, subsystem, entrypoint, sizeofimage, sizeofheaders, checksum, characteristics, timedatestamp): the set intersection of returned header names with this oracle set must be non-empty",
      "documented_mutation": "In src/intellicrack/bridges/cutter.py, change `_get_str(h, 'name')` in the get_headers list comprehension to `'unknown_field'` — names_lower becomes {'unknown_field'}, the intersection with known_pe_fields is empty, and the assertion fails"
    },
    {
      "id": "G0093",
      "test": "TestRealCrossFormatElf::test_elf_symbols_real",
      "action": "hardened",
      "oracle": "raw .dynsym section of the committed ELF fixture (true_x86_64) parsed independently with struct: fixture_names = _parse_elf64_dynsym_names(raw_bytes) extracts all 45 named .dynsym entries; oracle_names = frozenset({'__libc_start_main', 'abort', 'exit', 'strlen', 'strcmp', 'fclose', 'fflush', 'setlocale'}) is known to be a subset of fixture_names; returned_names & oracle_names must be non-empty",
      "documented_mutation": "In src/intellicrack/bridges/cutter.py, in get_symbols(), change `name=_get_str(s, 'name')` to `name=_get_str(s, 'type')` — every SymbolInfo.name becomes a rizin type string like 'FUNC' or 'OBJ'; none of those appear in oracle_names, so the intersection check fails immediately"
    }
  ],
  "prod_defects": [],
  "deletions": [],
  "self_quality": {
    "ruff": "pass",
    "basedpyright": "pass",
    "pydoclint": "pass",
    "pydocstyle": "pass"
  },
  "notes": "G0093 re-hardened (adversarial review round 2): replaced three purely structural checks (count>=5, non-empty name, address>0) with a cross-referenced oracle derived by parsing the actual .dynsym of the committed ELF fixture using struct directly, fully independent of CutterBridge. Module-level helpers _cstr64, _elf64_section_headers, _elf64_strtab_symbols, _parse_elf64_dynsym_names provide the oracle. The test asserts that returned_names & oracle_names is non-empty, anchoring the name-mapping to specific known symbol names from the fixture. Fixture has no .symtab; all 45 .dynsym entries have vaddr=0 (undefined external refs), so the old address>0 check was vacuously always-fail — removed in favor of the name oracle. All four tools pass."
}
