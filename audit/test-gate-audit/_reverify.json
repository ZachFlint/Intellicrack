{
  "G0021": {
    "id": "G0021",
    "verdict": "accurate",
    "note": "The test at line 318 installs a QSignalSpy and calls setText('ntdll'), then asserts len(spy)==1 and list(spy[0])==['ntdll']. Both assertions are guaranteed by Qt's QLineEdit unconditionally whenever setText is called with a different value. The sister test test_region_filter_wired_to_text_changed (line 276) already verifies receivers()==1 and drives row-visibility through setText end-to-end. G0021's test cannot detect whether _on_region_filter_changed is connected at all — if the connect() call were removed, spy emissions would still fire with the correct payload and both asserts would still pass. The test adds no independent gate beyond what Qt's signal machinery guarantees."
  },
  "G0022": {
    "id": "G0022",
    "verdict": "accurate",
    "note": "The test at line 462 patches run_bridge_coroutine_logged with _capture_dispatch, sets _attached_pid=1234, and asserts only that dispatch_calls is truthy. Production _on_read at line 458 calls run_bridge_coroutine_logged(self._bridge.read_memory(addr, size), ...). If production were changed to call self._bridge.write_memory instead, or pass a hard-coded address, dispatch_calls would still be non-empty. The test only gates that dispatch is called at all — it does not verify the coroutine identity (read_memory vs write_memory), the address argument, or the size argument. The finding is correctly categorized as mock-call-only."
  },
  "G0023": {
    "id": "G0023",
    "verdict": "accurate",
    "note": "The test at line 530 patches run_bridge_coroutine_logged and asserts only that dispatch_calls is truthy. Production _on_write at line 524 calls run_bridge_coroutine_logged(self._bridge.write_memory(addr, data), ...). If production were changed to dispatch bridge.read_memory instead, or to pass the wrong address or byte payload, dispatch_calls would still be non-empty and the assertion would pass. The identity of the dispatched coroutine and the arguments (address, data bytes) are never inspected. The finding is correctly categorized as mock-call-only."
  },
  "G0024": {
    "id": "G0024",
    "verdict": "accurate",
    "note": "The test at line 597 patches run_bridge_coroutine_logged and asserts only that dispatch_calls is truthy. Production _on_search at line 740 calls run_bridge_coroutine_logged(self._bridge.search_pattern(pattern), ...). If production were changed to dispatch bridge.read_memory instead of bridge.search_pattern, or to strip the pattern differently, dispatch_calls would still be non-empty and the truthy assertion would pass. The dispatched coroutine identity and the pattern argument are never checked. The finding is correctly categorized as mock-call-only."
  },
  "G0025": {
    "id": "G0025",
    "verdict": "accurate",
    "note": "The test at line 850 asserts: assert placeholder and ('0x' in placeholder.lower() or '7FF' in placeholder). Production sets _prot_addr.setPlaceholderText('0x7FF600000000') at line 322. Any non-empty string containing '0x' (including '0x' alone, 2 chars) satisfies the first disjunct. If production regressed to a generic '0x...' placeholder (4 chars), the test would still pass while the specific 64-bit format hint would be lost. The assertion is too permissive to gate the specific required placeholder. The finding is correctly categorized as weak-existence."
  },
  "G0026": {
    "id": "G0026",
    "verdict": "accurate",
    "note": "The test at line 862 asserts: '0x7FF600000000' in placeholder or ('0x' in placeholder and len(placeholder) > 4). The second disjunct completely undermines the first — any placeholder like '0x12345' (len=7 > 4, contains '0x') satisfies it. If production regressed from '0x7FF600000000' to a generic '0xABCDE' placeholder, the second disjunct would still pass while the specific 64-bit example address documented by F-0009 would be gone. The logical OR with a loose second condition defeats specificity. The finding is correctly categorized as weak-existence."
  },
  "G0027": {
    "id": "G0027",
    "verdict": "accurate",
    "note": "The parametrized test at line 245 asserts only result['output'].strip() is truthy for both scripts. For print('hello'), the sibling test_print_hello_appears_in_output (line 259) already checks 'hello' in result['output']. For 'x = 1\\nprint(x)', no test in the suite asserts that '1' appears in the output — the parametrized truthiness check would pass even if execute_script returned static non-empty output instead of capturing the actual print(x) result. The production execute_script at line 1065 does properly capture stdout via stdout_capture, but the test cannot detect a regression to static output for the second parametrized case. The finding is correctly categorized as weak-existence."
  },
  "G0101": {
    "id": "G0101",
    "verdict": "accurate",
    "note": "test_watchpoint_management at line 130 calls get_watchpoints() on a freshly-constructed bridge with empty _watchpoints and _pipe_client=None. The only assertion is isinstance(wps, list). Because no watchpoint is injected before the call (contrast with test_breakpoint_management which injects into bridge.breakpoints at line 104), get_watchpoints() returns [] every time. Any broken merge-and-return logic that still returns some list—wrong addresses, duplicated entries, wrong sizes, or entirely missing entries—would satisfy isinstance(wps, list) and leave the gate untripped. The breakpoint test at line 116-117 shows the correct pattern (len check + address check); this watchpoint test lacks both."
  },
  "G0102": {
    "id": "G0102",
    "verdict": "accurate",
    "note": "test_scan_with_hex_string at line 310-322 allocates a 16-byte marker buffer, computes ctypes.addressof into _buf_addr (underscore prefix, explicitly unused), then calls scan_memory('CAFEBABEDEADBEEFFEEDFACE12345678'). The only assertions are isinstance(results, list) and len(results) > 0. No assertion anchors any result back to _buf_addr. By contrast, test_scan_with_bytes (line 275-307) checks exact_matches[0].address == buf_addr and exact_matches[0].matched_bytes == expected_hex. A hex parser that strips every other byte pair, interprets the string little-endian, or returns spurious matches from unrelated memory regions can still satisfy len > 0. The hex-string parsing path is uncorrelated to the known buffer address."
  },
  "G0103": {
    "id": "G0103",
    "verdict": "accurate",
    "note": "test_scan_with_spaced_hex_string at line 325-339 is structurally identical to G0102. The 16-byte spaced-hex marker is written to buf, _buf_addr is computed but never appears in any assertion. The test only checks isinstance(results, list) and len(results) > 0. If the space-stripping via pattern.replace(' ', '') were broken—producing wrong bytes, off-by-one, or partial strips—scan_memory could still find a spurious match elsewhere in process memory and pass both checks. The space-delimiter parsing path is not gated to the known buffer address."
  },
  "G0104": {
    "id": "G0104",
    "verdict": "accurate",
    "note": "test_disabled_skips_elevation (line 187-197) patches is_windows→True and _relaunch_elevated→_fail_relaunch, but does NOT patch is_elevated. Without the 'if disabled: return False' guard, execution falls through to 'if already_attempted' (False, skip), then 'if is_elevated()' (real call: returns True on admin machine → returns False, or returns False on non-admin → reaches patched _relaunch_elevated which returns False, so maybe_elevate returns False). Either way the assertion 'is False' passes. No call-count check on _relaunch_elevated exists to detect whether the disabled path was correctly short-circuited."
  },
  "G0105": {
    "id": "G0105",
    "verdict": "accurate",
    "note": "test_already_attempted_does_not_loop (line 199-211) patches is_windows→True, is_elevated→False, and _relaunch_elevated→_fail_relaunch. Without the 'if already_attempted: return False' guard, execution reaches _relaunch_elevated (patched to return False), so maybe_elevate returns False. The assertion 'is False' still passes. The anti-loop semantic (the already-attempted child must never call _relaunch_elevated again) goes undetected because there is no assert_not_called or call-count check on _relaunch_elevated."
  },
  "G0106": {
    "id": "G0106",
    "verdict": "accurate",
    "note": "test_already_elevated_needs_no_relaunch (line 213-225) patches is_windows→True, is_elevated→True, and _relaunch_elevated→_fail_relaunch. Without the 'if is_elevated(): return False' guard, the code skips past that check and calls _relaunch_elevated, which is patched to return False, so maybe_elevate returns False. The assertion 'is False' passes whether the guard fires or not. No assert_not_called check on _relaunch_elevated exists to distinguish the short-circuit from the patched-decline path."
  },
  "G0107": {
    "id": "G0107",
    "verdict": "accurate",
    "note": "test_session_manager_requires_session_store_type (line 129-142) instantiates a real SessionStore and SessionManager, then checks hasattr(manager.store, 'save'), hasattr(manager.store, 'load'), and hasattr(manager.store, 'list_all'). These are method-name existence checks only; replacing any of the three methods with 'def save(self, *a): pass' (returning None) leaves all three hasattr checks passing. No behavioral assertion (round-trip, return value, DB write) is present in this test. Severity low is correct because sibling tests (test_save_and_load_session, test_session_roundtrip) exercise the actual store behavior."
  },
  "G0087": {
    "id": "G0087",
    "verdict": "accurate",
    "note": "The test at line 360-367 only asserts `isinstance(services, list)`. The production `enumerate_services` in process.py calls OpenSCManagerW/EnumServicesStatusExW via advapi32, but a stub that returns `[]` — which is a valid `list` — would satisfy the sole assertion. The test provides no oracle that real SCM data was fetched (e.g. non-empty result, presence of well-known services like 'eventlog'). Weak-existence, medium severity is correct."
  },
  "G0088": {
    "id": "G0088",
    "verdict": "accurate",
    "note": "The test at line 370-377 asserts `isinstance(active_services, list)` with the docstring explicitly acknowledging results can be empty. The `active=True` filter path in production sets `state_filter = SERVICE_ACTIVE`, but the test cannot distinguish this from `state_filter = SERVICE_STATE_ALL` or from returning `[]`. No cross-check validates that the filter was applied. Weak-existence, medium severity is correct."
  },
  "G0089": {
    "id": "G0089",
    "verdict": "refute",
    "note": "The batch JSON itself marks this `confirmed: false` and provides the refutation: the test iterates `_headless_env_blocklist` (which includes `PYTHONPATH`) and asserts each key is not in the returned environment. During pytest execution, pytest sets `PYTHONPATH` in `os.environ`. The production `_scrubbed_environment()` (ghidra.py:1569-1583) pops all blocklist keys. If the implementation were changed to `return dict(os.environ)` unchanged, `assert key not in env` would fail for `PYTHONPATH` (and likely others). The test is a real gate against a no-op scrubber."
  },
  "G0090": {
    "id": "G0090",
    "verdict": "accurate",
    "note": "The test at line 255-264 asserts `text.strip()` (truthy) and `len(text.splitlines()) >= 1`. The production method issues `pdf @ {address}` to radare2 on a real function address. However, a stub returning any non-empty string like `'x'` or an error message satisfies both assertions. No x86 mnemonic, address label, or disassembly structure is verified. Weak-existence, medium severity is correct."
  },
  "G0091": {
    "id": "G0091",
    "verdict": "accurate",
    "note": "The test at line 295-306 asserts only `dump.strip()` and `words.strip()` — any non-empty string from `hexdump` (using `px`) or `hexdump_words` (using `pxw`) passes. No hex-byte columns, address offset labels, or width structure is verified. A stub returning `'test'` would satisfy both assertions. Weak-existence, low severity is correct."
  },
  "G0092": {
    "id": "G0092",
    "verdict": "accurate",
    "note": "The test at line 481-489 asserts `headers` is non-empty and `any(header.name for header in headers)` — i.e. at least one HeaderInfo with any truthy name. A stub returning `[HeaderInfo(name='x', value='y', address=0)]` would pass. No known PE header field name (Machine, NumberOfSections, ImageBase, etc.) is verified. Weak-existence, low severity is correct."
  },
  "G0093": {
    "id": "G0093",
    "verdict": "accurate",
    "note": "The test at line 585-593 asserts `symbols` is non-empty and `any(symbol.name for symbol in symbols)` — at least one SymbolInfo with any truthy name. A stub returning `[SymbolInfo(name='_start', address=0, ...)]` would pass. No ELF-standard symbol names, address range validation, or real symbol-table structure is verified. Weak-existence, low severity is correct."
  },
  "G0069": {
    "id": "G0069",
    "verdict": "accurate",
    "note": "The test at line 125-139 asserts only end-state: `bridge.document is not None` before and `bridge.document is None` after `close_file()`. As verified in hex_editor.py lines 4848-4855, the `_state_lock` wraps `self.document = None` plus cursor/selection/path clearing. If the lock were removed entirely but the `self.document = None` assignment remained, both assertions still pass because the test never attempts concurrent access and only observes the sequential end state. The serialization contract the lock provides (preventing torn reads by concurrent threads) is untestable from a single-threaded sequential observer that only checks the completed mutation."
  },
  "G0070": {
    "id": "G0070",
    "verdict": "accurate",
    "note": "The conftest `_build_pe_binary()` builds a minimal PE with only a `.text` section and no import directory entries (PE_NUM_SECTIONS=1, no import table set up, optional header size=240 but all zeros). So `get_pe_imports()` returns `[]` regardless of whether `_document_disk_path_if_unmodified()` returns the disk path or None. Both `_open_pe_for_inspection(disk_path)` and `_open_pe_for_inspection(None)` parse the same bytes from the same minimal PE with no imports. `isinstance([], list)` is True. If the disk-path code path were broken or swapped for memory fallback, the result is still `[]` and the assertion still passes."
  },
  "G0071": {
    "id": "G0071",
    "verdict": "accurate",
    "note": "After `bridge.write_bytes(0, 'EB')` the MZ magic at byte 0 becomes 0xEB. In `get_pe_imports()` (hex_editor.py line 8701-8703), the bridge reads the first `_MIN_HEADER_SIZE` bytes and checks `head[:2] != b'MZ'`; since byte 0 is now 0xEB, this check fires and the method returns `[]` before even reaching `_open_pe_for_inspection`. `isinstance([], list)` is True. Whether the memory-fallback path is broken or silently swallowed is irrelevant because the MZ check short-circuits first. The test cannot detect whether any fallback path executed correctly."
  },
  "G0072": {
    "id": "G0072",
    "verdict": "accurate",
    "note": "The test at line 509-526 asserts `any(m['rule'] == 'mz_marker' for m in matches)`. In hex_editor.py lines 8896-8909, the bridge uses `disk_path` from `_document_disk_path_if_unmodified()` to call `scan_file_async` or falls back to `scan_data_async` on the in-memory bytes. Both code paths scan the same underlying bytes (the minimal PE starting with `MZ`). If the disk-path optimization were removed and the production code always used `scan_data_async`, the YARA rule `strings: $a = 'MZ'` still matches the first two bytes of the in-memory document and the assertion still passes. The test does not distinguish which scan code path executed."
  },
  "G0073": {
    "id": "G0073",
    "verdict": "reclassify",
    "corrected_category": "weak-existence",
    "corrected_severity": "medium",
    "note": "The test at lines 485-498 does have an assertion (`assert isinstance(result, list)`) so the category 'no-assertion' is wrong. It should be 'weak-existence' because `isinstance(result, list)` passes for both `[]` and any populated list. The test is structurally identical to `test_get_pe_imports_succeeds_on_unmodified_pe` (line 446-463): same fixture, same `open_file` call, same `get_pe_imports()` call, same `isinstance(result, list)` assertion. The duplicate adds zero gate coverage. Severity medium is correct since siblings G0070/G0071 already cover the same surface."
  },
  "G0074": {
    "id": "G0074",
    "verdict": "accurate",
    "note": "The test at lines 473-511 runs writer threads cycling `_display_mode` through `{'hex8', 'hex16_le', 'hex32_le'}` and reader threads sampling `get_display_mode()`, then asserts `all(value in allowed for value in samples)`. As confirmed in hex_state.py lines 388-411, both getter and setter take `self._lock`. The claim is correct that on CPython, the GIL ensures a single object-reference store/load (`self._display_mode = mode` / `return self._display_mode`) is effectively atomic at the bytecode level, so a reader can never observe a torn intermediate that is NOT one of the three published strings. No 4th string value can appear between the three valid ones. The assertion `all(value in allowed)` can never fail regardless of whether the lock exists, because only the three published string objects are ever assigned."
  },
  "G0001": {
    "id": "G0001",
    "verdict": "accurate",
    "note": "The test constructs InstallResult(success=True, kind='builtin') directly and asserts r.kind == 'builtin'. It never calls install_tool(). A regression where install_tool(PROCESS) returns kind='filesystem' instead of 'builtin' leaves this test green. The behavioral gate is test_install_tool_process_returns_builtin_kind at line 358 which calls the real ToolInstaller. The tautological/low classification is correct."
  },
  "G0002": {
    "id": "G0002",
    "verdict": "accurate",
    "note": "The test constructs FoundTool(kind='python_package') and asserts ft.kind == 'python_package'. FoundTool.kind is a required positional field with no default (line 231 of installer.py), so the test only exercises that the dataclass stores and returns what it was given. A production regression where find_tool_detailed returns kind='filesystem' instead of 'python_package' leaves this test green. The behavioral gate is test_find_tool_detailed_process_reports_builtin at line 384. Tautological/low is correct."
  },
  "G0003": {
    "id": "G0003",
    "verdict": "accurate",
    "note": "The test builds ArchDeployResult(arch='x64', filename='...', status='failed', error='permission denied') and asserts the same status and error back. This is pure dataclass round-trip. deploy_x64dbg_plugin_detailed is never called, so removing the error-population logic from that function would not make this test fail. Tautological/low is correct."
  },
  "G0004": {
    "id": "G0004",
    "verdict": "accurate",
    "note": "The test calls the real is_user_admin() (aliased to _is_user_admin in installer.py line 2382) and checks isinstance(result, bool). The production function always returns a bool: it returns True on non-Windows, bool(is_admin_fn()) on Windows success, or False on any error. isinstance(..., bool) therefore passes trivially. A stub hardcoded to False (breaking real admin detection) or True (granting spurious admin) both pass. The function's actual privilege detection is not gated by this test. Weak-existence/low is correct."
  },
  "G0005": {
    "id": "G0005",
    "verdict": "accurate",
    "note": "The test checks that exception names (ToolError, TimeoutError, asyncio.CancelledError, OSError, RuntimeError) appear in send_command.__doc__. The real exception-raising paths in the production implementation could all be removed and this test would still pass as long as the docstring text was preserved. The send_command behavioral exception paths are gated by separate tests. Docstring-content tests are a lint-level check, not a behavioral gate. The 'other'/medium classification is correct."
  },
  "G0006": {
    "id": "G0006",
    "verdict": "accurate",
    "note": "The test calls inspect.getsource(npc_module) and asserts the string 'os.name == \"nt\"' is absent. This enforces a coding convention (use sys.platform not os.name), not any behavioral property. If a developer introduced an incorrect platform check using a sys.platform variant, this test still passes. It is a lint rule expressed as a unit test and gates documentation/style, not production behavior. 'Other'/low is correct."
  },
  "G0007": {
    "id": "G0007",
    "verdict": "accurate",
    "note": "The test checks close().__doc__ for 'in-flight', 'thread', and 'asyncio.to_thread'/'thread pool'. The actual behavioral guarantee that close() waits for in-flight writes is gated by test_close_waits_for_inflight_write at line 784, which uses a slow_write patch and real async coordination. If close() regressed to return immediately before the write thread finished, the docstring test would remain green. This test gates documentation phrasing only. 'Other'/low is correct."
  },
  "G0075": {
    "id": "G0075",
    "verdict": "reclassify",
    "corrected_severity": "medium",
    "note": "The finding's core claim is correct: test_finds_binary_in_build_release (line 238) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` with no verification of the deployed artifact. A production bug that deployed `build/Release/intellicrack_bridge_x64.dp64` to the wrong arch directory (e.g., `release/x32/plugins/`) or corrupted bytes while still returning True would pass silently. However, the `is True` assertion IS a genuine gate for the narrower claim that `build/Release/` is in the search path — if `_find_plugin_source` removed that candidate, `found_count` would be 0 and `overall_success` would be False. The category `weak-existence` is correct. Severity `high` is inflated: deployment correctness (content + destination) is gated by sibling tests in `TestDeployX64dbgPlugin` (`test_deploys_dp64_binary` at line 321 and `test_deploys_both_architectures_to_matching_arch_trees` at line 353), both of which call `_assert_deployed_pe` for full byte-identical and arch verification. The unique uncovered gap is that the `build/Release/` discovery path is never exercised with a full deployment verification, making this a medium gap.",
    "corrected_category": "weak-existence",
    "corrected_line": 238
  },
  "G0076": {
    "id": "G0076",
    "verdict": "reclassify",
    "corrected_severity": "medium",
    "note": "The finding's core claim is correct: test_finds_binary_in_arch_specific_build (line 255) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` with no verification of the deployed artifact's location or content. A bug that deployed `build_x64/plugins/intellicrack_bridge_x64.dp64` to the wrong path while still returning True would be undetected. The `is True` assertion is a real gate for whether `build_x64/plugins/` appears in `_find_plugin_source`'s candidate list — removing it causes `found_count == 0` and returns False. The category `weak-existence` is correct. Severity `high` is inflated: deployment correctness (destination + content) for x64 binaries is fully gated by `test_deploys_dp64_binary` and `test_deploys_both_architectures_to_matching_arch_trees`, which call `_assert_deployed_pe`. The unverified gap (that a `build_x64/plugins/` source is correctly placed end-to-end) is medium severity.",
    "corrected_category": "weak-existence",
    "corrected_line": 255
  },
  "G0077": {
    "id": "G0077",
    "verdict": "reclassify",
    "corrected_severity": "medium",
    "note": "The finding's core claim is correct: test_finds_binary_in_arch_specific_release (line 272) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` for the `build_x32/Release/` discovery path, with no verification of the deployed artifact. A bug deploying to the wrong directory or corrupting content while returning True would pass undetected. The `is True` assertion is a real gate for `build_x32/Release/` being in the candidate search list. The category `weak-existence` is correct. Severity `high` is inflated: x32 deployment correctness (content + destination) is gated by `test_deploys_dp32_binary` (line 337) and `test_deploys_both_architectures_to_matching_arch_trees` (line 353), both of which call `_assert_deployed_pe`. The unverified gap is specifically that the `build_x32/Release/` discovery path is never paired with a full deployment artifact check, making this a medium severity gap.",
    "corrected_category": "weak-existence",
    "corrected_line": 272
  },
  "G0041": {
    "id": "G0041",
    "verdict": "refute",
    "note": "The test at line 70 asserts `info.eval_depth == DEFAULT_EVAL_DEPTH`, where `info = PragmaInfo()` (default-constructed). The two sides are independent at runtime: `info.eval_depth` is the dataclass field default from `PragmaInfo` (pragma.py line 56: `eval_depth: int = DEFAULT_EVAL_DEPTH`), and `DEFAULT_EVAL_DEPTH` is the module-level constant (pragma.py line 11). If a developer reverts the dataclass field to a hardcoded literal — e.g., `eval_depth: int = 32` — then `info.eval_depth == 32` while the imported `DEFAULT_EVAL_DEPTH == 512`, and the assertion fails. The 'tautological' label would only apply if both sides were derived from the same expression at assertion time; here they are not. The test is a genuine gate for the single-source-of-truth contract."
  },
  "G0042": {
    "id": "G0042",
    "verdict": "refute",
    "note": "The test at line 87 constructs a real `HexPatPreprocessor`, calls `pp.process('u32 x @ 0;')`, and checks `info.eval_depth == DEFAULT_EVAL_DEPTH`. In `preprocessor.py` line 122, the local variable `eval_depth` is initialized to `DEFAULT_EVAL_DEPTH`. If a developer reverts that line to `eval_depth: int = 32` (a plausible regression), then `info.eval_depth == 32` while `DEFAULT_EVAL_DEPTH == 512` at the assertion site, causing failure. The test exercises a real code path and the two assertion operands can diverge independently under a realistic regression. It is a genuine gate for the preprocessor's single-source-of-truth contract."
  },
  "G0043": {
    "id": "G0043",
    "verdict": "refute",
    "note": "The test at line 92 calls `extract_pragmas_fast('u32 x @ 0;')` and checks `info.eval_depth == DEFAULT_EVAL_DEPTH`. In `preprocessor.py` line 740, `extract_pragmas_fast` initializes `eval_depth: int = DEFAULT_EVAL_DEPTH`. If that line reverts to `eval_depth: int = 32`, then `info.eval_depth == 32` while `DEFAULT_EVAL_DEPTH == 512`, causing the assertion to fail. Same reasoning as G0042: the two operands are independently mutable and the test is a real gate for the shared-default contract in this second pipeline entry-point."
  },
  "G0044": {
    "id": "G0044",
    "verdict": "accurate",
    "note": "The test at line 36 only asserts `isinstance(DEFAULT_EVAL_DEPTH, int)`. Setting `DEFAULT_EVAL_DEPTH = 1` satisfies this assertion while breaking the meaningful constraint. Sibling tests `test_default_eval_depth_handles_tiff_pattern` (asserts `>= 100`) and `test_default_eval_depth_handles_common_parent_recursion` (asserts `>= 256`) provide the actual value-constraining gates. This existence check gates nothing that the sibling tests do not already gate and is fully subsumed."
  },
  "G0045": {
    "id": "G0045",
    "verdict": "refute",
    "note": "The test at line 79 asserts `info.array_limit == DEFAULT_ARRAY_LIMIT`, `info.pattern_limit == DEFAULT_PATTERN_LIMIT`, and `info.pointer_size == DEFAULT_POINTER_SIZE` on a default-constructed `PragmaInfo()`. Each dataclass field references its module constant (pragma.py lines 57-61). If any field reverts to a hardcoded literal — e.g., `pointer_size: int = 4` for a 32-bit target regression while `DEFAULT_POINTER_SIZE` remains `8` — the corresponding assertion fails. The two sides of each assertion are independently mutable and the test is a genuine gate for the single-source-of-truth contract across all three fields."
  },
  "G0046": {
    "id": "G0046",
    "verdict": "accurate",
    "note": "The test (line 469) invokes `_on_open_sandbox_panel`, then checks `resolved = window.tool_panel.get_panel('sandbox')`. If the result is `None`, it calls `pytest.skip(...)`. A broken `_on_open_sandbox_panel` that never registers the sandbox panel causes `get_panel('sandbox')` to return `None`, which is indistinguishable from the legitimate 'backend not installed' condition — the test skips rather than fails. The skip branch masks the failure in any CI environment without Windows Sandbox, making this a real skip-masks-failure defect."
  },
  "G0047": {
    "id": "G0047",
    "verdict": "refute",
    "note": "The test at line 484 calls `hasattr(doc, 'export_patches_bps_from_path')` directly on an `intellicrack_hexcore.HexDocument()` instance. The bridge in `hex_editor.py` lines 3955-3960 and 4012-4017 uses `getattr(self.document, 'export_patches_bps_from_path', None)` with a silent fallback to the mmap byte-slice path. If the Rust binding is removed from `lib.rs`, the roundtrip tests (`test_path_based_bps_export_returns_valid_patch`, `test_path_based_ups_export_returns_valid_patch`) silently route through the mmap fallback and remain green — they produce valid BPS/UPS patches regardless. Only `test_path_based_backend_binding_is_present` goes red because it queries the binding's presence directly on the native object. This test is the sole gate for 'binding removal masked by silent bridge fallback' — a non-redundant and real production gate. The 'weak-existence' characterization is wrong."
  },
  "G0048": {
    "id": "G0048",
    "verdict": "reclassify",
    "corrected_category": "asserts-on-stub-output",
    "corrected_severity": "medium",
    "corrected_line": 103,
    "note": "The defect stands but the category 'mock-shadows-target' is wrong: no mock or patch is used anywhere in this test. Instead, the test defines a local concrete class `_RealYaraRules` with its own `match` that returns `['match']`. The assertion `rules.match(data=b'abc') == ['match']` (line 105) exercises only this local stub class; no production `CompiledYaraRules.match` code is on the call stack. This maps to 'asserts-on-stub-output'. The `isinstance` check on line 104 does narrow-gate the `@runtime_checkable` decoration and the presence of the `match` attribute, but as the claim notes, those are already covered by `test_compiled_yara_rules_is_protocol` and `test_compiled_yara_match_body_is_ellipsis`. The unique value-assertion in this test (`== ['match']`) is entirely self-verifying against the test-local stub."
  },
  "G0049": {
    "id": "G0049",
    "verdict": "accurate",
    "note": "The test reads each PS1 file as text (line 297: `text = script_path.read_text(encoding='utf-8')`) and asserts the lifecycle helper function name, `-State 'started'`, and `-State 'stopped'` all appear in the source text. This is a genuine source-text scan. The claim correctly identifies the gap: a developer could enclose the `stopped` call in a dead-code branch (e.g. `if ($false) { ... }`) and the source-text scan would still pass while the runtime lifecycle record is never emitted. The category 'weak-existence' is accurate (the test gates outright deletion but not structural dead-code placement). The severity 'low' is appropriate given that the Windows-only integration test `test_kernel_object_monitor_finally_emits_stopped_record` closes the gap for `kernel_object_monitor.ps1`, and the three remaining scripts are only source-text-guarded."
  },
  "G0050": {
    "id": "G0050",
    "verdict": "reclassify",
    "corrected_category": "tautological",
    "corrected_severity": "medium",
    "corrected_line": 191,
    "note": "The defect stands but the category 'asserts-on-stub-output' is inaccurate: no stub is present. Both sides of the assertion call the same real production static method. `expected_smbios_types` (line 191) is built by calling `QEMUSandbox._anti_evasion_smbios_entries('default')` via the `smbios_entries_for_test` shim, and `reported_smbios_types` (lines 192-196) is derived from the techniques list that `apply_anti_evasion` builds on line 3521 of `qemu.py` by iterating `self._anti_evasion_smbios_entries(current_profile)` (confirmed in source). Both sides call the same production static method with the same argument. If `_anti_evasion_smbios_entries` is modified to drop an SMBIOS type, both `expected_smbios_types` and `reported_smbios_types` shrink identically and the assertion still passes. This is a tautological self-verification of the method against itself, not a test against an independent oracle."
  },
  "G0051": {
    "id": "G0051",
    "verdict": "accurate",
    "note": "The test asserts `elapsed >= write_duration` (1.0s) and `elapsed <= 2.5` (line 147-148). The claim is that a reverted fixed `sleep(2)` implementation would satisfy both bounds (2.0 is in [1.0, 2.5]) and the test would still pass. This is correct: the production `_wait_for_logs_stable` uses adaptive polling (polls `_MONITORING_LOG_NAMES` file sizes, waits for `stable_polls` consecutive unchanged counts), but a dumb `await asyncio.sleep(2.0)` would return in ~2.0s which lies squarely within [1.0, 2.5]. The test's upper bound of 2.5s is too loose to discriminate the prior fixed-sleep implementation from the new adaptive one. The companion test `test_returns_quickly_when_no_logs_exist` would catch a fixed sleep(2), but for this specific test the defect is real. Category 'other' and severity 'medium' are appropriate."
  },
  "G0052": {
    "id": "G0052",
    "verdict": "accurate",
    "note": "The test (line 541-545) constructs `_AttachTestSandbox` with a config and immediately asserts `sandbox.agent is None` — it never calls `_attach_qemu_agents` or any attach method. This is a pure init-invariant check. Any class that initialises `self._agent = None` (or uses a dataclass default) passes this test. The actual capability under test ('`sandbox.agent` is set after `_attach_qemu_agents` runs') is not exercised. A gutted `_attach_qemu_agents` that did nothing would not affect this test at all. Category 'tautological' and severity 'low' are both correct."
  },
  "G0053": {
    "id": "G0053",
    "verdict": "accurate",
    "note": "The test (lines 191-195) sets `probe.process = None` and calls `await probe.run_startup_health()` with no assertion on result or side-effects. The production code (windows.py line 637-638) returns immediately when `self.process is None`. There is no assertion to verify even that behaviour - a completely empty `_check_startup_health` body would also pass. No health-check logic is exercised. Category 'no-assertion' and severity 'low' are correct."
  },
  "G0054": {
    "id": "G0054",
    "verdict": "accurate",
    "note": "The test (lines 232-249) sets up a live process (`poll_result=None`) with the dialog detector monkeypatched to return None, then calls `await probe.run_startup_health()` and makes no assertion. The production code path when the process is alive and no dialog is detected falls through without raising, making the test a no-op gate: any implementation of `_check_startup_health` that does not raise on a live-and-healthy client passes. A completely missing `_detect_client_failure_dialog` call or a no-op body would also pass. Category 'no-assertion' and severity 'low' are correct."
  },
  "G0094": {
    "id": "G0094",
    "verdict": "accurate",
    "note": "The test patches `_get_analysis_module` at the module level and calls `bridge.extract_iocs` only once (line 2024), then asserts `call_count == 1`. A single call to any implementation — cached or not — always yields count == 1. The name claims 'multiple bridge calls' but none are made. The lru_cache behavior is not exercised because a second call never occurs. The test would pass with an uncached reimplementation that calls the underlying import once per invocation."
  },
  "G0095": {
    "id": "G0095",
    "verdict": "accurate",
    "note": "The test at line 2359 checks `any(kw in combined.lower() for kw in ['utc', 'iso', 'temp'])`. Inspecting sandbox_bridge.py lines 716, 736, and 780: all three tool-definition parameter descriptions for `sandbox.pcap_stop`, `sandbox.screenshot`, and `sandbox.memory_dump` contain the string 'auto-generated temp path'. The keyword 'temp' always matches regardless of whether UTC or ISO-8601 documentation is present or has been removed. Deleting all UTC/ISO references from those tool definitions would not change the test outcome because 'temp' is permanently in the path-description text."
  },
  "G0096": {
    "id": "G0096",
    "verdict": "accurate",
    "note": "The test at line 2036 only asserts `result is not None`. `_get_analysis_module()` is an lru_cache-decorated function that returns `types.ModuleType`. Any non-None return — including a MagicMock or a wrong module — satisfies the assertion. The sibling test `test_get_analysis_module_returns_same_object` (lines 2039-2047) actually gates the cache behavior by asserting `first is second`. The finding's severity 'low' is appropriate given the sibling test provides the actual gate."
  },
  "G0097": {
    "id": "G0097",
    "verdict": "refute",
    "note": "The test calls `get_dbghelp()` and then does `dbg.SymGetOptions.restype = ctypes.c_ulong; opts = dbg.SymGetOptions()`. With ctypes.WinDLL, accessing an attribute by function name invokes `GetProcAddress` on the DLL handle. If `get_dbghelp` returned a wrong DLL (e.g. kernel32, ntdll, user32), `GetProcAddress` for 'SymGetOptions' would return NULL and ctypes would raise `AttributeError`, causing the test to fail. `SymGetOptions` is a symbol unique to dbghelp.dll — no other standard Windows DLL exports it. Therefore the test genuinely gates that `get_dbghelp` returns the correct dbghelp.dll handle. A wrong DLL binding breaks the test. The finding's claim that 'any DWORD-returning export from any valid DLL would satisfy this assertion' is false because accessing an unexported symbol via ctypes raises AttributeError before the integer check is reached."
  },
  "G0098": {
    "id": "G0098",
    "verdict": "accurate",
    "note": "The test manually performs `x64dbg_bridge.next_bp_id += 1` and asserts the result is 2, exercising only the property getter/setter round-trip. Examining `set_breakpoint` in x64dbg.py (lines 3200-3208): it stores breakpoints keyed by address (`self._breakpoints[address] = BreakpointInfo(id=address, ...)`) and never reads or modifies `_next_bp_id`. The `_next_bp_id` counter is entirely unused by `set_breakpoint`. If `set_breakpoint` were broken to not allocate IDs properly, this test would still pass. The test verifies Python integer arithmetic via the property setter, not production breakpoint-allocation logic."
  },
  "G0099": {
    "id": "G0099",
    "verdict": "accurate",
    "note": "The test at line 202 manually increments `x64dbg_bridge.next_wp_id += 1` and asserts the result is 2. While `_next_wp_id` IS used in `set_watchpoint` at lines 3442-3443, the test never calls `set_watchpoint`. The test only exercises the property getter/setter. If `set_watchpoint` were broken to not increment the counter or to use a wrong counter, this test would still pass because it bypasses the production allocation path entirely. The sibling test `test_watchpoint_retrieved_via_get_watchpoints` (lines 213-235) provides the real gate for watchpoint storage and retrieval."
  },
  "G0100": {
    "id": "G0100",
    "verdict": "accurate",
    "note": "The test at line 425 only asserts `isinstance(memory_map, list)` and `len(memory_map) > 0`. While it does call the real `get_memory_regions()` path including `_walk_committed_memory_regions` with an actual VirtualQueryEx call on the current process, no region field content is verified (base_address, size, protection flags, state, type). The internal logic of `_append_committed_region` (protection mapping, state filtering) is completely untested. A stub returning `[MemoryRegion(...)]` with all fields zeroed would pass. The category 'weak-existence' and severity 'medium' are appropriate."
  },
  "G0028": {
    "id": "G0028",
    "verdict": "accurate",
    "note": "The assertion `assert mod is not None` on line 153 is genuinely tautological. `importlib.import_module` either raises `ImportError`/`ModuleNotFoundError` or returns a module object; it never returns None. The real gate - that the hex_editor package imports without error - is provided solely by the `import_module` call on line 152. If `__init__.py` were regressed to remove public API exports (e.g. HexEditorPanel) but still imported cleanly, line 153 would still pass green. The category 'tautological', severity 'low', and line 153 are all correct."
  },
  "G0029": {
    "id": "G0029",
    "verdict": "refute",
    "note": "The test on line 302 (`assert isinstance(result, bytes)`) IS a real gate for a distinct failure mode. In PyO3, `Vec<u8>` maps to Python `bytes` by default, but a Rust-side change to wrap in `PyByteArray` instead of `PyBytes` would cause this test to fail while the companion `test_export_patches_ips_emits_exact_spec_blob` (line 290) would remain green - because `bytes(bytearray_result) == expected_bytes` still holds (`bytes()` coerces bytearray). The production Rust binding at `src/intellicrack-hexcore/src/lib.rs` line 521 returns `PyResult<Vec<u8>>`; changing this to return a `PyByteArray`-annotated value is a realistic Rust refactor. `isinstance(result, bytes)` is `False` for `bytearray` since bytearray does not subclass bytes. This test gates the Python bytes-type contract independently of content correctness."
  },
  "G0030": {
    "id": "G0030",
    "verdict": "accurate",
    "note": "The assertion `len(events) >= 1` at line 251 only checks that at least one SELECTION_CHANGED event was fired; it does not check the payload. An implementation that fires SELECTION_CHANGED with an empty dict `{}` or wrong offsets would leave this assertion green. The companion test `test_selection_payload_matches_range` (line 254) calls `trigger_selection_changed` with identical arguments and asserts both `payload.get('start') == _SEL_START` and `payload.get('end') == _SEL_END` - a strict superset of G0030. No realistic production breakage exists that G0030 would catch while the companion remains green. The 'weak-existence' category, 'low' severity, and line 251 are correct."
  },
  "G0031": {
    "id": "G0031",
    "verdict": "accurate",
    "note": "The only assertion in `test_search_no_attribute_error_when_document_set` is `assert raised is None` at line 270. The post-call block at lines 272-275 is guarded by `if worker is not None:` and calls `worker.quit()`/`worker.wait()` only for cleanup - it never asserts the worker's existence. If `_on_search()` were changed to a silent no-op (e.g. returning early even when document is set), no AttributeError would be raised, `raised` stays None, the if-block is skipped, and the test passes green while no search worker was dispatched. The companion `test_search_dispatches_worker_with_correct_document` (line 217) does assert `worker is not None` and that the worker is bound to the correct document. The 'swallows-failure' category (the failure mode is swallowed), 'medium' severity, and line 245 are correct."
  },
  "G0032": {
    "id": "G0032",
    "verdict": "accurate",
    "note": "The assertion `assert length > 0` at line 239 is weak-existence: it passes for any positive integer (1, 100, 255, 256, etc.). The test uses a 256-byte document at cursor offset 0 with no selection/hex_widget, so the production code computes `write_len = min(len(result), 256) = 256` and emits 256. But `assert length > 0` would also pass if the production code emitted 1, or any other positive value, due to a regression in the min() computation or offset arithmetic. The sibling test `test_fires_with_selection_extent` (line 242) correctly asserts the exact value `length == 20`. The 'weak-existence' category, 'low' severity, and line 239 are correct."
  },
  "G0033": {
    "id": "G0033",
    "verdict": "accurate",
    "note": "The module-level `pytest.importorskip('intellicrack_hexcore')` at lines 36-39 unconditionally skips the entire test module when the Rust extension is absent. Only `test_streaming_file_path_matches_document_path` (line 243) actually calls `hexcore.HexDocument.open()`; all other tests - `TestStdlibHashesMatchReference` (8 parametrized + blake2), `TestChecksumsMatchReference` (5 methods), `TestFnvHashes` (4 variants), `test_streaming_file_path_matches_zlib`, and `TestFormatSize` - depend solely on pure-Python functions from `intellicrack.ui.panels.hex_editor.base` and Python stdlib (hashlib, zlib). When the hexcore build is broken, regressions in `compute_hash`, `compute_custom_crc`, FNV/Adler32, `compute_streaming_custom_crc` (file-path branch), and `format_size` are hidden behind a silent skip. The 'skip-masks-failure' category, 'medium' severity, and line 36 are correct."
  },
  "G0034": {
    "id": "G0034",
    "verdict": "accurate",
    "note": "The assertion `assert first == second` at line 173 is tautological: `compute_custom_crc` is a deterministic pure function, so calling it twice with identical arguments on identical data must always return equal values - any implementation (correct, broken, or returning a constant) satisfies `first == second`. A regression returning 0, len(data), or any stable wrong value would leave line 173 green. The real gate is line 174: `assert first != compute_custom_crc(data[:-1], ...)`, which verifies avalanche sensitivity to input length. The 'tautological' category, 'low' severity, and line 173 are correct."
  },
  "G0035": {
    "id": "G0035",
    "verdict": "refute",
    "note": "Line 263 (`assert real_names <= names`) is evaluated first and is the comprehensive guard: it asserts that every raw bridge export name (from `{str(entry['name']) for entry in exports}`) is a member of the rendered `names` set. If rendering used the wrong dict key, produced empty strings, or mangled names, `real_names` would NOT be a subset of `names`, and line 263 would raise AssertionError before line 264 is ever reached. The `or len(names) > 100` disjunct on line 264 is only reached when line 263 has already passed, meaning rendering was demonstrably correct for every actual export name. The auditor's scenario where mangled names slip through due to the `> 100` disjunct cannot occur without line 263 catching it first. The test IS a real gate for rendering correctness via line 263."
  },
  "G0036": {
    "id": "G0036",
    "verdict": "accurate",
    "note": "Production code at `widgets.py` line 735 unconditionally calls `self._prefetch_check.setChecked(True)` with no constructor parameter controlling that state. The constructor signature is `__init__(self, current_chunk_kb, current_budget_mb, current_usage_mb, parent)` — there is no `prefetch_on_scroll` parameter. The `prefetch_on_scroll` property at line 775 simply delegates to `self._prefetch_check.isChecked()`, which always returns `True` because line 735 hardcodes it. The test assertion `assert dialog.prefetch_on_scroll is True` on line 228 verifies a compile-time constant, not a round-tripped value. No realistic parameterized production breakage would turn it red. The `chunk_size_kb` and `memory_budget_mb` assertions on lines 226-227 are genuine round-trip gates, but line 228 is purely tautological."
  },
  "G0037": {
    "id": "G0037",
    "verdict": "accurate",
    "note": "Lines 102-103 (`assert widget.entropy_values() == result.entropy_values` and `assert widget.block_size() == ENTROPY_BLOCK_SIZE`) are genuine gates that verify `set_data` round-trips correctly. However, `_force_paint(widget, 400, 160)` on line 104 invokes `paintEvent` via `widget.render(pixmap)` with no subsequent assertion on the rendered content. The test's stated goal is 'paints_without_error' (smoke), but if `paintEvent` were replaced with a no-op or raised a caught exception internally, lines 102-103 would still pass and line 104 itself has no assertion on what was drawn. The finding accurately identifies the paint path as ungated at line 104; the no-assertion category and low severity are appropriate."
  },
  "G0038": {
    "id": "G0038",
    "verdict": "accurate",
    "note": "The test at line 857 checks `assert 'bytes object' in match.returns.lower()` against the static ToolDefinition returns string defined at `cutter.py` line 653: `'Assembled instruction as a Python bytes object (raw machine code).'`. This is entirely a documentation string check. The actual production implementation at line 2343 has signature `async def assemble_at(...) -> bytes` and returns `bytes.fromhex(...)` at line 2382. If an engineer changed the implementation to return `str` (hex string) instead of `bytes`, the docstring would remain unchanged and this test would still pass. The test would only fail if someone edits the documentation string, not if the implementation breaks. It gates documentation consistency, not runtime return type."
  },
  "G0039": {
    "id": "G0039",
    "verdict": "accurate",
    "note": "The test at line 824 asserts `bridge.capabilities.supports_dynamic_analysis is True`, which reads the `_capabilities` field set in the `CutterBridge` constructor at line 1121-1131. The `BridgeCapabilities` dataclass sets `supports_dynamic_analysis=True` as a hardcoded constructor argument (line 1123) with no conditional logic. If all five ESIL methods (`esil_eval`, `esil_step`, `esil_emulate_function`, `esil_init_memory`, `esil_set_pc`) were deleted from the bridge while the constructor flag remained `True`, this test would still pass. The `test_esil_methods_present` sibling test at line 832 already gates actual method presence. This test gates only a static boolean constant, not any real dynamic analysis capability."
  },
  "G0040": {
    "id": "G0040",
    "verdict": "refute",
    "note": "The test replaces `_execute_script_and_wait` with a mock returning controlled data (via `_patch_execute_script`), then exercises the REAL parsing logic in `get_memory_regions`. Production code at `frida_bridge.py` line 1881 sets `state='committed'` and line 1882 sets `type='image' if file_val is not None else 'private'`. The test asserts `r.state != 'MEM_COMMIT'` and `r.type != 'MEM_PRIVATE'`. If a developer changed line 1881 from `state='committed'` to `state='MEM_COMMIT'` (the original Win32 defect), the test would fail immediately on `assert r.state != 'MEM_COMMIT'`. This is a realistic production regression that the test catches. The mock replaces the Frida runtime transport but still exercises the actual Python parsing/construction logic where the defect lives. The test IS a real gate for the specific Win32 constant leak."
  },
  "G0008": {
    "id": "G0008",
    "verdict": "accurate",
    "note": "The probe script body at line 750 of the test unconditionally emits `Write-Output ('Module=ntoskrnl.exe')` as a string literal — it is not calling any production helper. `_slice_helpers_only` cuts the script at the first column-0 `try {`, so the ETW callback body at api_trace.ps1 line 283 (where `$module = 'ntoskrnl.exe'` is assigned) is excluded. No production module-resolution function exists in api_trace.ps1 (confirmed by grep). The assertion at line 783 (`assert \"Module=ntoskrnl.exe\" in out`) therefore always passes regardless of what the production script does with module names. Finding is correct."
  },
  "G0009": {
    "id": "G0009",
    "verdict": "accurate",
    "note": "The `produced_artefact` condition at line 364 is a three-way OR: `log_path.exists() or diag_path.exists() or bool((stderr or '').strip())`. dll_monitor.ps1 sets `$ErrorActionPreference = 'Stop'` at line 7, so any terminating error (broken ETW subscription, missing function, syntax error) will write a PowerShell exception traceback to stderr. That non-empty stderr satisfies the third branch, making `produced_artefact` True and the assertion pass — with zero monitoring output ever produced. The test cannot distinguish a healthy run from a crashed script."
  },
  "G0010": {
    "id": "G0010",
    "verdict": "accurate",
    "note": "The `produced_artefact` condition at line 455 of test_injection_monitor.py is the same three-way OR pattern: `(log_dir / _LOG_NAME).exists() or (log_dir / _DIAG_NAME).exists() or bool((stderr or '').strip())`. After F-0016, a non-admin run is expected to throw, producing a PowerShell exception traceback on stderr. Any regression that causes the script to crash earlier (before proper ETW setup) also emits stderr and satisfies the third branch. The test cannot distinguish a healthy early-throw from a completely dysfunctional script. Finding is correct."
  },
  "G0011": {
    "id": "G0011",
    "verdict": "accurate",
    "note": "The diagnostic content assertion at line 283 (`assert 'traceevent_dll_missing' in diag_text or 'TraceEvent' in diag_text`) is inside `if diag_path.exists():` at line 281. Only the exit-code check at line 276 (`assert completed.returncode != 0`) is unconditionally enforced. If the script throws before reaching the `Write-InjectionDiagnostic` call — or if that call's log-write path is removed — the diagnostic file is never created, the `if` branch is skipped silently, and the test passes on just the exit-code gate. The F-0016 contract requires both non-zero exit AND a structured diagnostic; this test only enforces the exit code. Finding is correct."
  },
  "G0012": {
    "id": "G0012",
    "verdict": "accurate",
    "note": "At line 440-448 of test_kernel_object_monitor.py, when `observed` is False on a non-admin run, the test calls `pytest.skip()` rather than `pytest.fail()`. If F-0021 regresses (poll cadence reverts to 3 s), the monitor would miss the 2000 ms transient mutex on non-admin CI: `observed` stays False, the non-admin branch fires, and CI records a skip — not a failure. The regression is completely masked on all typical non-admin CI pipelines. On admin runs `pytest.fail()` at line 449 is a real gate, but only admin runs actually enforce the polling cadence. Finding is correct."
  },
  "G0013": {
    "id": "G0013",
    "verdict": "accurate",
    "note": "The module-level `pytestmark` at lines 55-61 applies `pytest.mark.skipif(sys.platform != 'win32', ...)`, so the test only executes on Windows. On Windows, `os.name` is always `'nt'` by definition. The `assert os.name == \"nt\"` at line 507 therefore can never fail on any platform where this test runs — it is a tautological assertion. The test's real gate is `assert check.returncode == 0` at line 504 (the orphan process check). Category 'tautological', severity 'low', and line 507 are all correct."
  },
  "G0014": {
    "id": "G0014",
    "verdict": "accurate",
    "note": "`state_lines` is initialized as an empty dict at line 428. The population loop at line 429 only adds entries when lines match both `|state_changed|` and `|Spooler|`. If the script's log-write path is broken, the WMI subscription silently fails, or `-LogDir` is ignored, `log_path` may not exist or `pipe_contents` may be empty. In that case `state_lines` remains empty and the `for state, count in state_lines.items():` loop at line 439 never executes — the idempotency property is entirely unverified and the test passes vacuously. The test requires admin and the Spooler service (else it skips), so the vacuous-pass scenario only occurs under a genuine regression of the log-write path. Finding is correct."
  },
  "G0121": {
    "id": "G0121",
    "verdict": "accurate",
    "note": "test_shutdown_empty at line 201-207 calls `await registry.shutdown()` with zero assertions. The production `shutdown()` method (lines 264-283) clears `self._bridges` and resets `self._initialized = False`, but neither postcondition is checked by the test. If `shutdown()` were replaced with a no-op, the test would still pass as long as no exception is raised. The no-assertion categorization is correct and severity high is appropriate because the test name implies lifecycle correctness that is entirely unverified."
  },
  "G0122": {
    "id": "G0122",
    "verdict": "accurate",
    "note": "test_tools_directory at line 47-53 asserts only `registry.tools_directory.exists()`. The fixture creates `tools_dir = tmp_path / 'tools'` and that directory exists. If the production `tools_directory` property (line 128-134, returning `self._tools_dir`) were changed to return `self._tools_dir.parent` (i.e., `tmp_path`), the assertion still passes because `tmp_path` also exists. There is no identity check that the returned path equals the configured `tools_dir`. The weak-existence categorization is correct."
  },
  "G0123": {
    "id": "G0123",
    "verdict": "accurate",
    "note": "test_get_tool_definitions_after_init at line 249-261 asserts only `len(defs) > 0`. Production code (lines 528-532) iterates all bridges and silently swallows `AttributeError/RuntimeError/ToolError`, meaning if 6 of 7 bridges failed silently and only one returned a definition, `len(defs) > 0` would still pass. The test does not verify that all 7 bridges contributed definitions (unlike `test_initialize_creates_bridges` which checks all 7 names are registered, and `test_tool_definitions_have_functions` which checks per-definition function count). The weak-existence categorization is correct."
  },
  "G0126": {
    "id": "G0126",
    "verdict": "accurate",
    "note": "test_get_context_for_ai_contains_expected_top_level_keys at line 51-59 only checks that the five keys (file_path, size, modified, cursor, selection) are present in the returned dict. The `loaded_bridge` fixture opens a real PE binary (1024 bytes). Even if `get_document_info()` were replaced with a stub that always returned `{file_path: None, size: 0, modified: False, cursor: 0, selection: None}`, all five key-presence assertions would still pass. Value correctness is only tested by the sibling tests `test_get_context_for_ai_file_path_matches_opened_file` and `test_get_context_for_ai_cursor_reflects_goto_offset`. The weak-existence categorization and medium severity are correct."
  },
  "G0127": {
    "id": "G0127",
    "verdict": "accurate",
    "note": "test_get_context_for_ai_bookmarks_is_list at line 77-85 asserts only `isinstance(ctx['bookmarks'], list)`. An empty list or a list of wrong-typed items would satisfy this check. The bookmark content correctness (offset, length, label fields) is only covered by the companion test `test_get_context_for_ai_bookmarks_contain_expected_fields_when_present` (line 87). This test alone would pass even if `_populate_ai_bookmarks` stored an empty list or dropped all fields. The weak-existence categorization is correct, and low severity is appropriate given the companion test."
  },
  "G0128": {
    "id": "G0128",
    "verdict": "accurate",
    "note": "test_get_context_for_ai_size_is_positive at line 106-114 asserts `isinstance(ctx['size'], int) and ctx['size'] > 0`. The `loaded_bridge` fixture opens a 1024-byte PE binary (from `_build_pe_binary()` which creates `bytearray(1024)`). If `get_document_info()` (lines 4952-4975) were changed to return a hardcoded sentinel like `size: 1` instead of calling `self.document.length()`, the assertion `ctx['size'] > 0` would still pass. The actual file size (1024) is never checked against the returned value. The weak-existence categorization is correct and low severity is appropriate since value correctness is partially implied by the document being genuinely loaded."
  },
  "G0140": {
    "id": "G0140",
    "verdict": "accurate",
    "note": "The conftest.py at D:/Intellicrack/tests/test_hexcore_e2e/conftest.py line 36 has a module-level `pytest.importorskip('intellicrack_hexcore')` that causes pytest to skip the entire test directory when the Rust native extension is absent. The test at line 42-49 calls `bridge.get_display_mode()` which is implemented at hex_editor.py line 8505-8512 as pure Python: `return self._display_mode`. The `set_display_mode` method (line 8472-8503) only sets `self._display_mode = mode` (line 8498) with no dependency on the hexcore Rust module. If `intellicrack_hexcore` is absent in CI, all 11 display/highlight tests silently skip and any regression in the pure-Python state machine is undetected. The category `skip-masks-failure` and severity `high` are correct."
  },
  "G0141": {
    "id": "G0141",
    "verdict": "accurate",
    "note": "The file `test_bridge_display_modes_complete.py` has its own module-level `pytest.importorskip('intellicrack_hexcore')` at line 20, independent of the conftest. The test `test_roundtrip_hex32_le` at line 65-73 calls `bridge.set_display_mode('hex32_le')` then `bridge.get_display_mode()` - both are pure Python operations on `self._display_mode` (hex_editor.py lines 8498, 8512) with zero dependency on the Rust module. A bug that ignores the set argument (e.g., `self._display_mode = 'hex8'` always) would be completely undetected whenever hexcore is not built. The category `skip-masks-failure` and severity `high` are correct."
  },
  "G0142": {
    "id": "G0142",
    "verdict": "accurate",
    "note": "The test `test_no_document_all_required_keys_present` at line 94-101 only asserts `_REQUIRED_KEYS.issubset(info.keys())` - checking that keys {file_path, size, modified, cursor, selection} exist in the returned dict, without verifying any values. The production code at hex_editor.py lines 4959-4966 always returns a hard-coded dict with exactly those five keys, so this assertion can never fail under any realistic breakage that preserves the key names. The sibling tests (test_no_document_file_path_is_none, test_no_document_size_is_zero, etc.) already gate the actual values. The category `weak-existence` and severity `low` are correct."
  },
  "G0143": {
    "id": "G0143",
    "verdict": "accurate",
    "note": "The test `test_all_required_keys_present_with_file` at line 176-185 only asserts `_REQUIRED_KEYS.issubset(info.keys())` after opening a file. The production `get_document_info()` at hex_editor.py lines 4968-4974 always returns a dict with exactly those five keys, so this key-presence check cannot detect any value regression (e.g., returning None for file_path, 0 for size after a real file is opened, etc.). The sibling tests (test_open_pe_file_path_matches, test_open_pe_size_is_1024, etc.) already gate the actual values. The category `weak-existence` and severity `low` are correct."
  },
  "G0015": {
    "id": "G0015",
    "verdict": "accurate",
    "note": "Confirmed. In script_manager.py line 168, TYPES['x64dbg']['display'] is literally the string 'x64dbg', identical to the fallback `script_type` argument passed to get_display_name. At line 263-264, `info.get('display', script_type)` uses `script_type` as the default. Deleting or corrupting the 'display' key still returns 'x64dbg' via the fallback, so `assert ScriptTypeInfo.get_display_name('x64dbg') == 'x64dbg'` stays green regardless. The structural guard — that the 'display' key is actually present and sourced from TYPES — is absent."
  },
  "G0016": {
    "id": "G0016",
    "verdict": "accurate",
    "note": "Confirmed for the single targeted assertion at line 449. qemu.py line 939 unconditionally sets `self._qemu_config = qemu_config or QEMUConfig()`, and the property at line 961-967 simply returns `self._qemu_config`. No change to `is_available()` or `_detect_accelerator()` can cause `isinstance(sb.qemu_config, QEMUConfig)` to return False; this assertion is tautologically true. The finding correctly scopes its claim to only this one line; the broader test (lines 447-448 and 453-454) does gate real behavior."
  },
  "G0017": {
    "id": "G0017",
    "verdict": "accurate",
    "note": "Confirmed. Line 468 reads: `success = _process_events_until(qapp, lambda: panel._status_priv.text() != 'Privilege: Standard' or True)`. The `or True` makes the lambda always return True, so `_process_events_until` exits after the very first predicate check, never actually waiting for the async `_on_privs` callback to fire. The initial label (base.py line 204) is `'Privilege: Standard'`. When `SeChangeNotifyPrivilege` only is present, `_on_privs` sets the label to `'Privilege: Standard'` (same value, base.py line 298). The assertion `'Standard' in panel._status_priv.text()` therefore passes whether the async path ran or not, and would remain green if `_refresh_privilege_label` were deleted entirely."
  },
  "G0018": {
    "id": "G0018",
    "verdict": "refute",
    "note": "The finding (category: conditional-never-runs) is wrong. The for-loop at line 291-294 is not a logical defect; the safety property is enforced by the *emptiness* of `warning_calls`. The fixture sets `_attached_pid = 1234` and `_bridge` is non-None (set by the `tab` fixture via `t.set_bridge(bridge)`). Production code at process_tab.py line 603-612 checks `_bridge is None` then `_attached_pid is None`; with a bridge and pid set, neither guard fires. The file dialog is patched to return `('', '')` so execution returns before any `QMessageBox.warning` call. If the `_attached_pid is None` guard were removed or inverted, `QMessageBox.warning` would be called with title 'Not Attached'; `warning_calls` would be non-empty; the for-loop would execute; `'not attached' in title.lower()` would be True; the assertion would fail. The test correctly gates the guard's absence."
  },
  "G0019": {
    "id": "G0019",
    "verdict": "accurate",
    "note": "Confirmed. The test at lines 278-285 asserts only `os.getpid() in real_pids` (sanity check on the bridge snapshot) and `os.getpid() in tab.rendered_pids()`. No assertion connects `tab.rendered_pids()` to `real_pids` as a subset or superset. Production code that appended fabricated PIDs (e.g., `[0, -1, 999999]`) alongside real ones would pass as long as `os.getpid()` appeared in the rendered set. The docstring claims a subset gate but none is implemented."
  },
  "G0020": {
    "id": "G0020",
    "verdict": "accurate",
    "note": "Confirmed. The assertion `_get_tls_thread_combo(tab) is not _get_fiber_combo(tab)` only verifies that `tab._tls_thread_combo` and `tab._fiber_combo` are distinct Python objects (which they always are — threads_tab.py lines 318 and 341 assign separate `QComboBox()` instances). A developer could re-introduce F-0011 by changing `_on_tls` (threads_tab.py line 752) to read `self._fiber_combo.currentData()` instead of `self._tls_thread_combo.currentData()`, and this structural identity test would still pass. The actual behavioral gates are `test_tls_uses_its_own_selector` (TID 200 not TID 100) and `test_tls_thread_combo_independent_of_fiber_combo` (TID 42 not TID 999), both of which verify the dispatched value originates from the correct combo."
  },
  "G0108": {
    "id": "G0108",
    "verdict": "accurate",
    "note": "The test at line 225 asserts only `orch.state == 'idle'`. Production `__init__` sets `self._state: OrchestratorState = 'idle'` unconditionally at line 701, regardless of what config is passed. The `self._config = config or OrchestratorConfig()` wiring at line 698 stores the custom config, but no assertion verifies `orch._config.confirmation_level`, `orch._config.max_iterations`, or any other field from the supplied `OrchestratorConfig(confirmation_level=ConfirmationLevel.NONE, max_iterations=5)`. A regression that silently discarded the config argument (e.g. always using `OrchestratorConfig()`) would leave the sole assertion green."
  },
  "G0109": {
    "id": "G0109",
    "verdict": "accurate",
    "note": "The test at line 147 asserts only `isinstance(orch.provider_registry, ProviderRegistry)`. The production property at orchestrator.py line 762-768 returns `self._providers`. The `_make_orchestrator` helper injects a bare `ProviderRegistry()` with no pre-registered providers. If the property were changed to `return ProviderRegistry()` (a fresh empty instance ignoring `self._providers`), `isinstance(x, ProviderRegistry)` would still be True. No identity check or content check exists, so the storage and retrieval wiring is ungated."
  },
  "G0110": {
    "id": "G0110",
    "verdict": "accurate",
    "note": "The three assertions at lines 321-323 verify only `registry.get(ToolName.HEX_EDITOR) is bridge`, `registry.get_hex_editor_bridge() is bridge`, and `ToolName.HEX_EDITOR in registry.get_available_tools()`. These exercise only dictionary storage (tools.py `self._bridges[name] = bridge` in `register_bridge`) and dictionary retrieval (`self._bridges.get(name)` in `get()`). The entire `execute_tool_call` dispatch path (line 551+) — tool-name normalisation, bridge lookup, capability gating, attribute resolution — is never called. A regression that breaks dispatch while leaving storage intact (e.g. a capability gate that always rejects, or a renamed method) would leave all three assertions green."
  },
  "G0111": {
    "id": "G0111",
    "verdict": "refute",
    "note": "The finding is marked `confirmed: false` by the prior auditor, and the code confirms the test IS a real gate. Production tools.py line 576 does `tool_enum = ToolName(tool_name.lower())`. `ToolName` is a Python `enum.Enum` with values like `X64DBG = 'x64dbg'`. If `.lower()` is removed, calling `execute_tool_call('X64DBG', 'get_breakpoints', {})` would attempt `ToolName('X64DBG')`, which raises `ValueError` (since `'X64DBG'` != `'x64dbg'`), which is caught and re-raised as `ToolError`. The test does not catch `ToolError`, so both `result_upper` and `result_lower` calls would raise, causing the test to fail. This is a genuine gate for the normalisation logic."
  },
  "G0112": {
    "id": "G0112",
    "verdict": "accurate",
    "note": "Lines 131-132 assert `isinstance(available, list)` where `available = registry.get_available_tools()`. Production tools.py line 543 returns `list(self._bridges.keys())`. The registry is constructed with `ToolRegistry(reloaded.tools_directory)` but `initialize()` is never called, so `self._bridges` remains `{}` and the method always returns `[]`. `isinstance([], list)` is trivially True. Any regression to `get_available_tools` — returning `None`, deleting the method entirely, or populating the bridges incorrectly — would be caught only if it breaks the `list` type contract. The earlier assertions (lines 125-129) on `is_tool_enabled` and `tools_directory` are real gates, but the flagged line 131-132 asserts nothing meaningful about computed state."
  },
  "G0113": {
    "id": "G0113",
    "verdict": "accurate",
    "note": "The test body at line 156-173 guards the real assertion (`assert result is False` at line 173) behind `if not elevation.is_elevated(): pytest.skip(...)` at line 165. In any standard CI environment (Docker containers, unprivileged Windows agents, or any process without an elevated token), `is_elevated()` returns False and `pytest.skip()` is called, so the assertion at line 173 never executes. If `maybe_elevate()` were changed to return `True` when already elevated, the test would still show as a skip (green) in all normal CI runs. The test is only a gate when the test process itself holds an elevated token — an exceptional condition not controlled by the test."
  },
  "G0114": {
    "id": "G0114",
    "verdict": "accurate",
    "note": "The assertion at line 302 is `assert len(instructions) <= 5`. Production disassembler.py lines 260-272 iterate the capstone generator and break when `len(instructions) >= count`. A regression that causes the break to fire immediately (e.g. `>= 0` instead of `>= count`), or that causes `md.disasm` to return nothing, would produce an empty list. `assert 0 <= 5` is trivially True, so the test stays green while the count-capping feature is broken. No lower-bound assertion (e.g. `assert len(instructions) == 5` or `assert len(instructions) >= 1`) exists to catch this failure mode."
  },
  "G0129": {
    "id": "G0129",
    "verdict": "accurate",
    "note": "The test calls set_color_mode for all 5 modes and only asserts `result is True`. Production set_color_mode at line 8614 of hex_editor.py does `self._color_mode = mode` then returns True. If storage were silently broken (e.g., the assignment were a no-op or wrote a wrong value), the return value would still be True and the test would stay green. Only test_color_mode_roundtrip (one mode, 'byte_value') and test_set_color_mode_persists_and_notifies ('entropy') cover storage read-back. The remaining 3 modes ('none', 'template', 'content_type') are never read back. The weak-existence category and medium severity are correct."
  },
  "G0130": {
    "id": "G0130",
    "verdict": "accurate",
    "note": "Line 129 asserts `nearer == ceil_boundary` where `nearer` is assigned purely by the test's own arithmetic constant `ceil_boundary if (1000-512) > (1024-1000) else floor_boundary`. The condition 488 > 24 is always True regardless of what the bridge does, so this assertion can never fail due to a production breakage. The actual bridge gate is on lines 131-135 where `_run(bridge.snap_to_alignment(512))` is called and compared to `ceil_boundary`. Line 129 is a tautological oracle-sanity check embedded inside an otherwise-real gate test. The tautological category and low severity are correct for this specific line."
  },
  "G0131": {
    "id": "G0131",
    "verdict": "accurate",
    "note": "The test asserts only metadata shape and values (`result['operation'] == 'not'`, `result['length'] == 4`). Production apply_arithmetic_to_selection returns `{'offset': start, 'length': length, 'operation': operation}` (hex_editor.py lines 5759-5762) constructed directly from input arguments, not from transformed byte output. Breaking the NOT transform (bytes unchanged, identity no-op) leaves the metadata dict intact, so all assertions pass. The actual NOT-correctness is gated by test_not_selection (lines 136-144) which checks result bytes equal `bytes([0xFF, 0x00, 0x55, 0xAA])`. The no-assertion category and medium severity are correct."
  },
  "G0132": {
    "id": "G0132",
    "verdict": "refute",
    "note": "The JSON already self-refutes this finding (`confirmed: false`). Independently verified: _build_ups_patch (hex_editor.py lines 4508-4558) walks source and target XOR-byte by byte; when both files are identical, xor_val is always 0 and no hunk data is written to the patch. The output is magic (4 bytes) + encoded source_size (~2 bytes for 64) + encoded target_size (~2 bytes) + 3 CRC32s (12 bytes) ≈ 20 bytes, well under the 32-byte threshold. If the identical-file fast path were removed or the algorithm were broken to emit XOR hunks for identical bytes, the patch would grow far beyond 32 bytes and the assertion `len(decoded) < max_header_size` would fail. The test is a genuine production gate for the identical-file optimization."
  },
  "G0078": {
    "id": "G0078",
    "verdict": "accurate",
    "note": "The test at line 3553 performs a literal set-then-get: `mod.SizeOfStruct = ctypes.sizeof(IMAGEHLP_MODULE64)` then `assert mod.SizeOfStruct == ctypes.sizeof(IMAGEHLP_MODULE64)`. This is a tautology; ctypes field assignment and read-back of the same value is always equal. The second assertion `ctypes.sizeof(IMAGEHLP_MODULE64) > 584` is a compile-time struct-size check with no production behavior. Breaking the production `_resolve_module` method (e.g. omitting the SizeOfStruct assignment before calling SymGetModuleInfo64) would not affect either assertion."
  },
  "G0079": {
    "id": "G0079",
    "verdict": "accurate",
    "note": "The test at line 3949 creates `ticker_task`, awaits `get_handles()`, then awaits `ticker_task`. If `get_handles` blocks the event loop (no `asyncio.to_thread`), the ticker accumulates 0 ticks during step 2. But `await ticker_task` in step 3 then runs the ticker coroutine to completion (all 100 `asyncio.sleep(0)` iterations). The final assertion `ticks[0] == 100` holds in both the blocking and non-blocking cases, because `await ticker_task` guarantees all 100 ticks execute before the assertion is reached. A fully synchronous `get_handles` implementation would still pass this test."
  },
  "G0080": {
    "id": "G0080",
    "verdict": "accurate",
    "note": "Identical structural defect to G0079, at line 3976. The ticker task is awaited AFTER `enum_handles` completes. Even if `enum_handles` blocked the event loop (no `asyncio.to_thread`), `await ticker_task` in step 3 lets the ticker run all 50 `asyncio.sleep(0)` iterations to completion. The assertion `ticks[0] == 50` is satisfied regardless of whether the event loop was yielded during `enum_handles`. A purely synchronous `enum_handles` passes."
  },
  "G0081": {
    "id": "G0081",
    "verdict": "accurate",
    "note": "At line 486, the test asserts `isinstance(_get_debug_privilege_enabled(process_bridge), bool)`. The helper reads `bridge._debug_privilege_enabled`, which is declared as `bool = False` in `__init__` (line 1117 of process.py) and set to `True` only on successful elevation. It is always a `bool` regardless of whether elevation succeeded or failed. If `_elevate_debug_privilege` is removed or broken, the flag remains `False` (still a bool) and the test still passes. The test does not gate whether privilege elevation actually occurred."
  },
  "G0082": {
    "id": "G0082",
    "verdict": "accurate",
    "note": "At line 1013, `test_get_windows_no_crash` calls `attached_bridge.get_windows(os.getpid())` and asserts `isinstance(windows, list)`. While this would catch exceptions (e.g. if user32 became unavailable and ToolError was raised), it would not catch logic regressions where `get_windows` returns an empty list without actually calling `EnumWindows`. A stub `return []` implementation passes. The test class docstring itself says 'doesn't crash', confirming the intent matches the narrow coverage. Category `weak-existence` and severity `medium` are correct."
  },
  "G0083": {
    "id": "G0083",
    "verdict": "accurate",
    "note": "At line 1026, the test only asserts `isinstance(services, list)`. The production `list_services` calls `OpenSCManagerW` via advapi32. A stub returning `[]` would pass. The companion test `test_list_services_have_name_state` at line 1035 gates structure (name/state keys present) on real results, making this first test redundant as a gate. The finding correctly identifies it as `weak-existence`."
  },
  "G0084": {
    "id": "G0084",
    "verdict": "accurate",
    "note": "At line 3485, the test computes `expected_header = ctypes.sizeof(SYMBOL_INFO) - ctypes.sizeof(c_char*1024) + ctypes.sizeof(c_char)`. Since `SYMBOL_INFO.Name` is `c_char*1024`, this reduces to `sizeof(SYMBOL_INFO) - 1023`. Both assertions (`expected_header > 0` and `expected_header < ctypes.sizeof(SYMBOL_INFO)`) are algebraically guaranteed true for any positive-sized struct. No production code path is exercised; modifying `_resolve_symbol` to use a wrong header size would not change the outcome of these arithmetic assertions."
  },
  "G0085": {
    "id": "G0085",
    "verdict": "accurate",
    "note": "At line 3727, the test recomputes `sym_buf_size = sym_header_size + _MAX_SYM_NAME * ctypes.sizeof(c_char)` locally. The assertion `sym_buf_size >= sym_header_size + _MAX_SYM_NAME` is trivially true because `ctypes.sizeof(c_char) == 1`, so both sides are equal. The assertion `sym_buf_size > ctypes.sizeof(SYMBOL_INFO)` simplifies to `sym_header_size + 2000 > sizeof(SYMBOL_INFO)`, i.e. `(sizeof(SYMBOL_INFO) - 1023) + 2000 > sizeof(SYMBOL_INFO)`, i.e. `977 > 0` — always true. No production code is invoked; a broken `_resolve_symbol` allocating an insufficient buffer would not affect these arithmetic checks."
  },
  "G0086": {
    "id": "G0086",
    "verdict": "accurate",
    "note": "At line 1239, `test_enumerate_com_servers_returns_list` asserts `isinstance(result, list)`. The Python interpreter process has no COM servers registered in HKCR\\CLSID, so the expected real result is `[]`. A stub `return []` always passes. The test does not verify that the production code actually called `get_modules`, iterated DLL names, or queried the registry. Category `weak-existence` is correct; severity `low` is appropriate given an empty list is the realistic outcome in a test environment."
  },
  "G0133": {
    "id": "G0133",
    "verdict": "refute",
    "note": "The test at line 92 (assertion at line 101) calls the real copy_as('csharp_array') on a real document and asserts result.endswith('}'). The production code at line 5492 of hex_editor.py returns f\"new byte[] {{{inner}}}\". If that closing brace were removed (e.g., changed to f\"new byte[] {{{inner}}\"), the production string would no longer end with '}' and this test would fail. The assertion is narrow but catches a real structural regression in the csharp_array format. A sibling test (test_csharp_array_full_string_matches_payload, line 79) also gates the full string, but G0133 independently gates the closing-brace property. The finding's own claim field correctly states the test IS a real gate (confirmed: false meaning the defect was not confirmed), so the weak-existence categorization is wrong."
  },
  "G0134": {
    "id": "G0134",
    "verdict": "refute",
    "note": "The test at line 120 (assertion at line 129) calls the real copy_as('java_array') on a real document and asserts result.startswith('new byte[] {'). The production code at line 5500 of hex_editor.py returns \"new byte[] {\" + \", \".join(parts) + \"}\". If the prefix were changed (e.g., to \"byte[] {\" or \"ByteArray {\"), this test would fail. It is a narrow but genuine gate against prefix corruption in the java_array format. The finding's own claim field states the test IS a real gate; the weak-existence categorization is wrong."
  },
  "G0135": {
    "id": "G0135",
    "verdict": "refute",
    "note": "The test at line 189 (assertion at line 198) calls the real copy_as('javascript_array') and asserts result.endswith('])'). The production code at line 5503 returns f\"new Uint8Array([{inner}])\". Removing or changing the closing '])' suffix would break this assertion. This is a narrow but real structural gate. A sibling test (test_javascript_array_full_string_matches_payload, line 176) also does an exact equality check, but G0135 independently gates the closing bracket-paren sequence. The finding's own claim field states the test IS a real gate; the weak-existence categorization is wrong."
  },
  "G0136": {
    "id": "G0136",
    "verdict": "refute",
    "note": "The test at line 217 (assertion at line 226) calls the real copy_as('nasm_db') and asserts result.startswith('db '). The production code at line 5511 returns f\"db {inner}\". If the assembler directive prefix were changed (e.g., to 'nasm ' or 'DB '), this test would fail. It is a narrow but genuine gate against prefix corruption in nasm_db output. The finding's own claim field states the test IS a real gate; the weak-existence categorization is wrong."
  },
  "G0137": {
    "id": "G0137",
    "verdict": "accurate",
    "note": "The test at lines 105-119 uses a NOP-filled payload and asserts only that _EXPECTED_INSN_KEYS = {'address','bytes','mnemonic','operands','size'} is a subset of each instruction dict's keys. A stub returning dicts with all correct keys but empty/zero values (mnemonic='', address=0, bytes='', operands='', size=0) would satisfy this assertion. The test provides no oracle for field values. While sibling tests (test_disassemble_matches_independent_capstone_decode at line 55, test_disassemble_int3_mnemonic at line 90, test_disassemble_nop_has_size_one at line 135) verify actual values, this specific test is only a weak key-presence check."
  },
  "G0138": {
    "id": "G0138",
    "verdict": "accurate",
    "note": "The test at lines 74-81 asserts 1 <= len(results) <= 4 for disassembling 4 INT3 bytes (1 byte each) with count=4. The PE fixture places exactly 4 x 0xCC bytes at _PE_TEXT_OFFSET=0x200. A broken disassembler that decoded only 1 instruction (stopping after 1 byte) would return len=1, which satisfies 1 <= 1 <= 4 and the test would still pass. The range is too wide to gate the count-limiting behavior. No sibling test in this file asserts exactly 4 results for the PE text section case."
  },
  "G0139": {
    "id": "G0139",
    "verdict": "accurate",
    "note": "The test at lines 93-102 asserts _EXPECTED_INSN_KEYS.issubset(insn.keys()) for each instruction decoded from the PE .text section (4 INT3 bytes). This only checks that the five required keys are present in each dict; it does not check any field values. A stub returning dicts with all five keys mapping to empty/zero values (address=0, bytes='', mnemonic='', operands='', size=0) would pass. Sibling test test_disassemble_pe_text_int3_mnemonic (line 55) checks the mnemonic value but only partially gates field correctness. G0139 itself is a key-presence-only check and is a genuine weak-existence finding."
  },
  "G0161": {
    "id": "G0161",
    "verdict": "accurate",
    "note": "test_compile_simple_struct_returns_nonempty_string (line 67) calls real bridge.compile_pattern() and asserts only isinstance(result, str) and bool(result). The sibling test_compile_simple_struct_result_is_valid_json (line 78) does the json.loads() check. A broken compile_pattern returning any non-empty garbage string (e.g., 'E') would pass this test, so it adds no structural gate over the sibling. Category weak-existence, severity medium, and line 67 are all correct."
  },
  "G0162": {
    "id": "G0162",
    "verdict": "accurate",
    "note": "test_execute_u32_field_returns_field_list (line 135) calls execute_pattern and checks only isinstance(fields, list) and assert fields. The sibling tests test_execute_u32_field_has_correct_size (line 148) and test_execute_u32_field_has_correct_offset (line 161) pin name='value', size=4, and offset=0 via next(f for f in fields if f['name'] == 'value'). A stub returning [{'name': 'junk', 'offset': 999, 'size': 99}] passes the return-type test but would break the siblings. This test itself adds no gate. Category weak-existence, severity medium, line 135 are correct."
  },
  "G0163": {
    "id": "G0163",
    "verdict": "accurate",
    "note": "test_execute_struct_with_multiple_fields_returns_multiple_results (line 174) collects all_names from top-level name fields plus children, then asserts 'magic' in all_names or 'header' in all_names. Since the production interpreter wraps Header in a struct node named 'header', even a stub that returns [{'name': 'header', 'children': []}] (zero children, no magic field parsed) satisfies the OR branch 'header' in all_names. The multi-field parsing capability is not gated. Category weak-existence, severity medium, line 174 are correct."
  },
  "G0164": {
    "id": "G0164",
    "verdict": "accurate",
    "note": "test_auto_detect_with_pe_file_returns_list (line 345) uses loaded_bridge (PE binary opened in conftest) and asserts only isinstance(result, list). An empty list [] passes. The sibling test_auto_detect_with_no_document_raises_runtime_error (line 336) checks the no-document error path, but no sibling verifies that PE magic-byte auto-detection actually finds any patterns. A broken auto_detect_pattern returning [] would pass this test. Category weak-existence, severity medium, line 345 are correct."
  },
  "G0165": {
    "id": "G0165",
    "verdict": "accurate",
    "note": "test_compile_complex_struct_with_nested_types_is_valid_json (line 98) calls compile_pattern with nested Inner/Outer struct source and asserts isinstance(json.loads(result), dict). Any valid JSON object—including {} or {'ok': True}—passes. A stub compiler that returns '{}' regardless of input would pass this test. Neither the Inner/Outer struct names nor the field names (tag, val, hdr, sub) appear in any assertion. Category weak-existence, severity low, line 98 are correct."
  },
  "G0166": {
    "id": "G0166",
    "verdict": "accurate",
    "note": "test_compile_enum_produces_valid_json (line 109) compiles 'enum Color : u8 { Red = 0, Green = 1, Blue = 2 }; struct Wrapper { Color c; };' and asserts only isinstance(parsed, dict). Any JSON object passes. The enum name Color and members Red/Green/Blue are never asserted to appear in the output. A stub returning {} would pass. Category weak-existence, severity low, line 109 are correct."
  },
  "G0167": {
    "id": "G0167",
    "verdict": "accurate",
    "note": "test_compile_union_produces_valid_json (line 120) compiles 'union Word { u16 as_u16; u8 bytes[2]; }; struct Wrapper { Word w; };' and asserts only isinstance(parsed, dict). Any JSON object passes. Neither the union Word nor its members as_u16/bytes are asserted. A struct-like compiler would pass even with wrong union semantics. Category weak-existence, severity low, line 120 are correct."
  },
  "G0168": {
    "id": "G0168",
    "verdict": "accurate",
    "note": "test_list_hexpat_patterns_returns_list (line 308) calls list_hexpat_patterns() and asserts only isinstance(result, list). An empty list passes. The sibling test_list_hexpat_patterns_items_have_required_keys (line 317) is the real gate: it asserts len(result) > 0, that 'pe' is in pattern_names, and that each entry has name/description/category. A broken scanner returning [] would pass this test but fail the sibling. This test adds no gate on its own. Category weak-existence, severity low, line 308 are correct."
  },
  "G0144": {
    "id": "G0144",
    "verdict": "accurate",
    "note": "test_list_encodings_returns_nonempty_list (line 218) only asserts isinstance(result, list) and truthiness. The bridge fixture has no document open so list_encodings() hits the Python fallback (hex_editor.py:6832-6849) which always returns a hardcoded 14-entry list. A stub returning [{'name': '', 'label': ''}] or any non-empty list would pass. Content correctness is gated elsewhere by test_list_encodings_contains_utf8_entry (line 251) and test_list_encodings_contains_ascii_entry (line 262). Weak-existence/medium is an accurate characterization."
  },
  "G0145": {
    "id": "G0145",
    "verdict": "accurate",
    "note": "test_list_encodings_standalone_bridge (line 330) creates a fresh HexEditorBridge(), calls initialize(), then list_encodings() with no document open (self.document is None). This routes to the hardcoded Python fallback at hex_editor.py:6832-6849. The test asserts only isinstance(result, list) and result (truthiness). Any non-empty list passes. A stub returning [{'name': 'dummy', 'label': 'Dummy'}] would satisfy both assertions. No specific encoding names or labels are validated. Weak-existence/medium is accurate."
  },
  "G0146": {
    "id": "G0146",
    "verdict": "accurate",
    "note": "test_decode_text_returns_string_type (line 183) opens a real file and calls decode_text(0, 7, 'ascii'), then checks only isinstance(result, str). This is a narrow type-contract gate: it would catch a regression where decode_text returns bytes or None, but any str value (including 'WRONG') passes. The Python fallback at hex_editor.py:6789 calls data.decode() which always returns str, making the type invariant very stable. Sibling tests (test_decode_ascii_text_from_file at line 53, etc.) assert the actual content. Weak-existence/low accurately describes this test's narrow scope."
  },
  "G0147": {
    "id": "G0147",
    "verdict": "accurate",
    "note": "test_list_encodings_entries_have_name_key (line 229) iterates over list_encodings() results and asserts only 'name' in entry. A stub returning [{'name': '', 'label': ''}] passes because the key is present regardless of value. The companion test_list_encodings_names_are_nonempty_strings (line 273) validates non-emptiness, and test_list_encodings_contains_utf8_entry (line 251) validates real content. This test gates only schema shape (key presence), not correctness. Weak-existence/low is accurate."
  },
  "G0148": {
    "id": "G0148",
    "verdict": "accurate",
    "note": "test_list_encodings_entries_have_label_key (line 240) asserts only 'label' in entry for every result entry. A stub returning [{'name': 'utf-8', 'label': ''}] or even [{'name': 'x', 'label': None}] (if None were present) could pass the key-presence check. The companion test_list_encodings_labels_are_nonempty_strings (line 285) validates non-empty string values. This test covers only schema shape. Weak-existence/low is accurate."
  },
  "G0149": {
    "id": "G0149",
    "verdict": "accurate",
    "note": "test_list_encodings_names_are_nonempty_strings (line 273) checks isinstance(entry['name'], str) and len(entry['name']) > 0. A stub returning [{'name': 'fake-codec-x', 'label': 'Fake'}] passes because 'fake-codec-x' is a non-empty string, even though no such codec exists and decode_text would raise ValueError with it. Real codec name correctness is gated by test_list_encodings_contains_utf8_entry (line 251) and test_list_encodings_contains_ascii_entry (line 262). Weak-existence/low is accurate."
  },
  "G0150": {
    "id": "G0150",
    "verdict": "accurate",
    "note": "test_list_encodings_labels_are_nonempty_strings (line 285) checks isinstance(entry['label'], str) and len(entry['label']) > 0 for every entry. A stub returning [{'name': 'utf-8', 'label': 'x'}] passes because 'x' is a non-empty string. The companion test_list_encodings_with_open_document_is_content_independent (line 297-328) validates specific label values like 'UTF-8', 'EBCDIC Code Page 037', and 'Big5 (Chinese Traditional)'. This test gates only that labels are non-empty strings, not that they are meaningful. Weak-existence/low is accurate."
  },
  "G0115": {
    "id": "G0115",
    "verdict": "accurate",
    "note": "test_tree_structure_matches_consumer_schema never calls WindowsSandbox._generate_wsb_config (lines 1128-1185 of sandbox/windows.py). Instead it calls _build_wsb_config_tree(), a test-file helper that duplicates the production tree-building logic. The assertions (e.g. folder.findtext('ReadOnly') == 'false') verify the test's own construction, not the production method. If _generate_wsb_config changed to emit wrong tag names or values, this test would remain green. The tautological label is defensible: the test builds a tree with specific text values and then asserts those exact values back, which is a self-verifying loop on stdlib xml.etree. The gate is that xml_gen.py re-exports functional primitives, not that the production consumer uses them correctly."
  },
  "G0116": {
    "id": "G0116",
    "verdict": "reclassify",
    "corrected_category": "tautological",
    "corrected_severity": "medium",
    "corrected_line": 88,
    "note": "test_serialised_document_roundtrips_through_real_parser has the same defect as G0115: it builds its own tree via _build_wsb_config_tree() (a test-internal helper) without ever calling _generate_wsb_config. The finding is valid as a non-gate finding, but the category 'mock-shadows-target' is wrong — there are no mocks or patches anywhere in this test file. No unittest.mock, no @patch, no MagicMock. The correct category is 'tautological': the test builds a tree with known values (e.g. MemoryInMB='4096') then round-trips and asserts those same values come back, which is a self-verifying loop on Python's xml.etree + defusedxml. It never exercises _generate_wsb_config's actual tag-construction logic."
  },
  "G0117": {
    "id": "G0117",
    "verdict": "accurate",
    "note": "test_bypass_strategy_description_all_nonempty (line 117-120) iterates BypassStrategy members and asserts only that strategy.description is truthy. The production descriptions dict (script_gen.py lines 246-259) returns strings like 'Force function to return false (0)' — a placeholder such as ' ' (single space) or 'TODO' would still pass this assertion. The two preceding content-checking tests (test_bypass_strategy_description_return_true and test_bypass_strategy_description_nop) gate only RETURN_TRUE and NOP_FUNCTION; the 9 remaining strategies (RETURN_FALSE, RETURN_ZERO, RETURN_ONE, SKIP_CHECK, PATCH_JUMP, HOOK_REPLACE, MEMORY_PATCH, INLINE_PATCH, VIRTUALIZATION_DEFEAT) have only truthiness verified here. Weak-existence/low is the correct category and severity."
  },
  "G0118": {
    "id": "G0118",
    "verdict": "accurate",
    "note": "test_script_context_to_prompt_with_path (line 151) sets binary_path=Path('C:/test/app.exe') and asserts only 'Path:' in result. The production code (script_gen.py line 321) appends f'Path: {self.binary_path}'. If the code emitted 'Path: None' or 'Path: wrong_value', the key 'Path:' would still be in result and the test would pass. The actual path value 'C:/test/app.exe' (or 'C:\\test\\app.exe') is not checked here. While the comprehensive exact-layout test (line 475) does verify the correct path for a different binary_path value, the specific test under audit only asserts header presence. Weak-existence/low is correct."
  },
  "G0119": {
    "id": "G0119",
    "verdict": "accurate",
    "note": "test_script_context_to_prompt_with_additional_context (line 229) passes additional_context={'compiler': 'MSVC'} and asserts both 'Additional Analysis Context:' in result (line 236) and 'compiler' in result (line 237). The value 'MSVC' is never checked. The production code (script_gen.py lines 343-345) appends f'  - {k}: {v!r}' — if the value was dropped or wrong ('  - compiler:' with no MSVC), 'compiler' would still appear in the result and the test would pass. The comprehensive exact-layout test (line 475) asserts \"  - compiler: 'MSVC'\" but that test uses a different ScriptContext instance. The specific test under audit is a weak gate on key presence only. Weak-existence/low is correct."
  },
  "G0120": {
    "id": "G0120",
    "verdict": "accurate",
    "note": "test_script_context_to_prompt_with_language (line 240) passes ScriptLanguage.JAVASCRIPT and asserts only 'API Reference:' in result (line 244). The production code (script_gen.py line 398) appends f'\\n{language.value.upper()} API Reference:'. If the method appended only the header line but emitted no API content, or appended the wrong tool's API, the test would still pass. The content of the Frida API reference is gated by test_frida_api_reference_keys, and the exact prompt layout by test_script_context_to_prompt_full_structure_exact_layout — but this specific test only checks that some 'API Reference:' string appears. Weak-existence/low is correct."
  },
  "G0169": {
    "id": "G0169",
    "verdict": "accurate",
    "note": "The test at line 542-550 calls `getattr(bridge, fn, None)` for the three method names and then does `assert callable(attr)`. This only verifies the attribute exists on the bridge and is callable — it does not invoke the methods or check their return values. A stub that returns `[]` or always raises an unrelated exception would still be callable and would pass. The real behavioral gates for these methods are in sibling tests (e.g., lines 483-509) that call the methods with real PE files and verify actual output. The finding is accurate: this test gates only ToolRegistry getattr dispatch reachability, not correctness of the underlying PE introspection capability."
  },
  "G0170": {
    "id": "G0170",
    "verdict": "accurate",
    "note": "The test at line 43-50 calls `loaded_bridge.search_hex('4D 5A')` and only asserts `isinstance(results, list)`. An empty list `[]` satisfies this assertion. A broken implementation that returns an empty list or a list of garbage dicts would still pass. The real gates are in sibling tests: `test_search_hex_finds_mz_signature_at_offset_zero` (line 64) checks offset==0 is present and `test_search_hex_result_length_matches_pattern_byte_count` (line 74) verifies results[0]['length']==2. This test provides no gate against broken search functionality."
  },
  "G0171": {
    "id": "G0171",
    "verdict": "accurate",
    "note": "The test at line 52-62 calls `search_hex('4D 5A')`, asserts `results` is non-empty (line 59), and then checks only that 'offset' and 'length' keys exist in each item (lines 61-62). Values are not validated. A stub returning `[{'offset': -1, 'length': 999}]` would pass every assertion. The actual correctness of offsets and lengths is gated by sibling tests: `test_search_hex_finds_mz_signature_at_offset_zero` checks `0 in offsets` and `test_search_hex_result_length_matches_pattern_byte_count` checks `results[0]['length'] == 2`. The finding is accurate."
  },
  "G0172": {
    "id": "G0172",
    "verdict": "accurate",
    "note": "The test at line 193-205 writes `b'ABC123DEF456'`, opens the file, calls `search_regex(r'[A-Z]{3}')`, and only asserts `results` is truthy (non-empty list). It does not verify the count of matches, their offsets, or their lengths. An implementation returning `[{'offset': 0, 'length': 1}]` for any input with any matches would pass. The sibling test `test_search_regex_no_match_returns_empty_list` (line 207) only gates the empty-result case for a non-matching pattern; no test in the file verifies that the two correct matches (ABC at offset 0, DEF at offset 6) are both found with correct lengths. The finding is accurate."
  },
  "G0174": {
    "id": "G0174",
    "verdict": "refute",
    "note": "The original auditor marked `confirmed: false` and the claim itself concludes 'The test IS a real gate for execute_pattern functionality.' The test at lines 345-369 first skips if `_hexpat_available` is False, calls `loaded_bridge.execute_pattern('u8 sig_byte @ 0x00;')`, and asserts `any(e[0] == HexDocumentEvent.PATTERN_EXECUTED for e in events)`. If a realistic production breakage removed or disabled the `notify_pattern_executed` call in the bridge's `execute_pattern` implementation, the events list would remain empty and the assertion at line 369 would fail. The try/except only skips on `RuntimeError` containing 'not available' — it re-raises all other RuntimeErrors. The test genuinely gates that `execute_pattern` fires the PATTERN_EXECUTED event through the state holder callback mechanism."
  },
  "G0055": {
    "id": "G0055",
    "verdict": "accurate",
    "note": "The test creates two independent sandboxes: sb_fixed with _make_dump_handler(pid_to_embed=target_pid=3210) and sb_buggy with _make_dump_handler(pid_to_embed=powershell_host_pid=9999). The handler at lines 224-239 extracts the file path from the script via regex and writes _make_minidump_bytes(pid_to_embed) unconditionally — it never reads the target PID from the script content. All three assertions (fixed_pid==3210, buggy_pid==9999, fixed_pid!=buggy_pid) are resolved solely by the test-chosen constants, independent of whether the production dump_memory generated OpenProcess or GetCurrentProcess. A regression reverting to GetCurrentProcess() would leave the script text different but the handler would still write the test-supplied constant, so all assertions remain green. The test gates only that dump_memory completes without raising an exception, not that it uses the correct API call."
  },
  "G0056": {
    "id": "G0056",
    "verdict": "accurate",
    "note": "intellicrack.core.hexpat is a pure-Python package built into the repo under src/intellicrack/core/hexpat/. Its __init__.py (line 14) imports HexPatInterpreter from interpreter.py; a syntax error or removed class in interpreter.py causes the import of intellicrack.core.hexpat to raise ImportError. The module-level pytest.importorskip at line 43 catches that error and skips all 8 tests rather than allowing them to run and fail. The bridge's own _get_interpreter() at line 1748 would raise RuntimeError when hexpat is unavailable, which would cause the tests to fail red if they ran — but the importorskip prevents that. Since this package is not an optional external binary but a core repo module, the skip guard converts breakage into invisible skips rather than surfacing failures."
  },
  "G0057": {
    "id": "G0057",
    "verdict": "accurate",
    "note": "ThreadsTab.__init__ (lines 52-65) sets _auto_refresh_timer and calls _setup_ui() — neither _setup_ui nor any of the _build_* methods calls _refresh_threads or update_thread_list. _CountingThreadsTab.__init__ calls super().__init__() then sets self.refresh_call_count = 0 afterwards. The _CountingThreadsTab.refresh_call_count attribute is explicitly initialized to 0 after super().__init__ returns, and the override of _refresh_threads blocks any base-class invocation from incrementing it. The assertion 'counting_tab.refresh_call_count == 0' is guaranteed by construction and cannot fail under any production regression. Additionally, the failure message string at line 404 is missing the f prefix, so {counting_tab.refresh_call_count} is never interpolated into the diagnostic output."
  },
  "G0058": {
    "id": "G0058",
    "verdict": "accurate",
    "note": "The five combo boxes (_reg_combo at line 200, _stack_combo at line 244, _seh_combo, _fiber_combo, _tls_thread_combo) are constructed with bare QComboBox() calls inside the _build_* methods and no items are added at construction time. Qt guarantees QComboBox.count() == 0 on a freshly constructed, unfilled widget. The _CountingThreadsTab override of _refresh_threads prevents any call to update_thread_list from the timer or base class. Even if the production base class were modified to call update_thread_list from _setup_ui, the override would block it. All five assertions are guaranteed by Qt widget construction semantics and the subclass override, independent of any production code change."
  },
  "G0059": {
    "id": "G0059",
    "verdict": "accurate",
    "note": "The production read_bytes (lines 2896-2901) calls bytes.fromhex(result.strip()) and returns the result. The test at line 1482 only asserts isinstance(result, bytes). If read_bytes returned b'' (the fallback branch at line 2898-2899 when hex_str is empty), or b'\\x00\\x00\\x00', or any other bytes object due to a parsing regression, isinstance(result, bytes) would still pass. The actual byte content b'\\x48\\x8b\\x05' is never verified. A production defect that returned wrong or empty bytes would not be caught by this test."
  },
  "G0060": {
    "id": "G0060",
    "verdict": "accurate",
    "note": "The test at line 1563 asserts isinstance(result, str) for the return value of hexdump. The production hexdump (line 3898) returns result directly from _r2_cmd with no transformation. If hexdump returned '' (empty string) or any other str value, isinstance(result, str) would still pass. The test also verifies that a px command was dispatched with '128' in it (lines 1564-1566), which gates the command-dispatch path, but the return value content is never verified. Severity low is appropriate since the command-dispatch IS gated by the sibling assertions."
  },
  "G0061": {
    "id": "G0061",
    "verdict": "accurate",
    "note": "The test at line 1599 asserts isinstance(result, str) for the return value of esil_eval. The production esil_eval (line 3229) returns result directly from _r2_cmd without transformation. If esil_eval returned '' or any other string instead of '0x42', isinstance(result, str) would still pass. The test also verifies ae_cmds contains one 'ae ' command (lines 1600-1601), gating command dispatch, but the evaluation result content is never checked. A broken implementation that silently returned an empty or incorrect string would not be caught by this assertion."
  },
  "G0211": {
    "id": "G0211",
    "verdict": "accurate",
    "note": "test_sha256_output_is_64_hex_chars (def at line 196, assertions at lines 203-204) calls compute_hash('sha256') and checks only len==64 and all hex chars. test_sha256_matches_hashlib at line 67 asserts result.lower() == hashlib.sha256(sample_bytes).hexdigest().lower(), which is an exact-value equality check. Any implementation producing the correct 64-hex-char SHA-256 digest satisfies both tests. A stub returning 'a'*64 would pass the format test but fail the exact-value test, meaning this format test adds no detection beyond what the value-exact test already gates. Category weak-existence and severity low are correct."
  },
  "G0212": {
    "id": "G0212",
    "verdict": "accurate",
    "note": "test_md5_output_is_32_hex_chars (def at line 206, assertions at lines 213-214) checks only len==32 and all hex chars. test_md5_matches_hashlib at line 45 performs an exact-value equality against hashlib.new('md5', sample_bytes).hexdigest(). A correct MD5 digest guarantees both correct length and hex character set, so the format-only test is fully subsumed. Any stub that passes the format check (e.g. 'deadbeef'*4) fails the value-exact gate. Category weak-existence and severity low are correct."
  },
  "G0213": {
    "id": "G0213",
    "verdict": "accurate",
    "note": "test_sha1_output_is_40_hex_chars (def at line 216, assertions at lines 223-224) checks only len==40 and all hex chars. test_sha1_matches_hashlib at line 56 asserts exact-value equality against hashlib.new('sha1', sample_bytes).hexdigest(). A correct SHA-1 digest implies both correct length and hex charset; the format-only test adds no additional detection. Category weak-existence and severity low are correct."
  },
  "G0214": {
    "id": "G0214",
    "verdict": "accurate",
    "note": "test_sha512_output_is_128_hex_chars (def at line 226, assertions at lines 233-234) checks only len==128 and all hex chars. test_sha512_matches_hashlib at line 78 asserts exact-value equality against hashlib.sha512(sample_bytes).hexdigest(). A correct SHA-512 digest is always exactly 128 lowercase hex characters, so this format test is fully subsumed by the value-exact gate. Category weak-existence and severity low are correct."
  },
  "G0215": {
    "id": "G0215",
    "verdict": "accurate",
    "note": "test_crc32_output_format_is_hex_string (def at line 396, assertions at lines 411-413) checks isinstance(result, str), result is truthy, and all chars in hexdigits. test_crc32_standard_matches_binascii at line 316 asserts exact-value equality against binascii.crc32(sample_bytes) formatted as an 8-hex-char string. A correct CRC-32 result is always a non-empty hex string, so the format check is fully subsumed. A stub returning 'deadbeef' passes the format test but fails the exact-value test. Category weak-existence and severity low are correct."
  },
  "G0062": {
    "id": "G0062",
    "verdict": "refute",
    "note": "The claim asserts that frida_bridge.py failing to import causes a 'silent skip' via the autouse require_frida fixture. This mechanism cannot work as described. When _frida_available = False, FridaBridge is not in the runtime namespace (the TYPE_CHECKING import at line 34 is not executed at runtime). The class definition `class _TestableFridaBridge(FridaBridge):` at line 282 runs at module scope — if FridaBridge is not bound, Python raises NameError during collection, producing a loud collection error, not a silent skip. In the normal case (frida installed), _frida_available = True and test_crash_info_bridge_internal_accumulation IS a real gate: it calls bridge.trigger_crash_via_parsing_callback() which drives the real on_process_crashed closure, then asserts crash.pid == _PID, crash.process_name == 'target.exe', crash.summary, crash.parameters, etc. A refactoring that reads the wrong attribute (e.g. 'process_id' instead of 'pid') from the raw crash object would cause crash.pid to be wrong and the assertion would fail."
  },
  "G0063": {
    "id": "G0063",
    "verdict": "refute",
    "note": "Same structural refutation as G0062. If frida_bridge.py fails to import, `class _TestableFridaBridge(FridaBridge):` at line 282 raises NameError at collection time — the module fails to collect with a loud error, not a silent skip. In the normal case (frida installed), test_parse_stalker_batch_call_event IS a real gate: it calls bridge.parse_stalker_batch_public() which invokes the real _parse_stalker_batch method, then asserts evt.event_type == 'call', evt.from_address == _ADDR (0x00401000 parsed from '0x401000'), evt.to_address == _ADDR2, evt.depth == 1. A bug that parses hex addresses incorrectly (e.g. returns 0 for hex-prefixed strings) would fail the from_address equality assertion."
  },
  "G0064": {
    "id": "G0064",
    "verdict": "refute",
    "note": "Same structural refutation as G0062 and G0063. If frida_bridge.py fails to import, the module raises NameError at collection on `class _TestableFridaBridge(FridaBridge):` at line 282 — this is a collection error, not a silent skip. In the normal case, test_parse_stalker_batch_exec_event_no_destination IS a real gate: it calls bridge.parse_stalker_batch_public() with an exec-type event having no 'to' field, then asserts evt.to_address is None (not 0, not from_address), isinstance(evt.depth, int), and evt.depth == 0. A regression that substitutes 0 for a missing 'to' field would fail `assert evt.to_address is None`. A regression that fails float-to-int conversion for depth would fail isinstance(int) or the exact equality."
  },
  "G0065": {
    "id": "G0065",
    "verdict": "refute",
    "note": "The test at line 1300 has no explicit assert statement, but `closer(_Empty())` is an implicit no-exception assertion. The production _close_bridge_client at line 1382 uses `client = getattr(bridge, 'client', None)` and returns early when client is None. If this guard were changed to direct attribute access (`bridge.client`), calling closer(_Empty()) would raise AttributeError (since _Empty has no client attribute) and the test would fail. The test correctly gates the getattr safety guard. The finding's 'no-assertion' category is wrong: a test that passes only when no exception is raised is a real behavioral gate. The batch itself marks confirmed=false, agreeing this test IS a gate."
  },
  "G0066": {
    "id": "G0066",
    "verdict": "accurate",
    "note": "test_f0002_indented_script_would_have_failed_without_dedent (line 289) constructs a hard-coded indented string literal and asserts `compile(indented, '<test>', 'exec')` raises IndentationError. It calls only the Python built-in compile() — no GhidraBridge method, no prepare_remote_script(), no _execute_remote(). This is a stable property of the Python language itself (indented module-scope code always raises IndentationError) and will always pass regardless of any changes to ghidra.py, including removing textwrap.dedent from _execute_remote(). The test verifies a baseline Python behavior, not production code. Category 'tautological' and severity 'low' are both correct."
  },
  "G0067": {
    "id": "G0067",
    "verdict": "accurate",
    "note": "test_python_fallback_excludes_non_printable_high (line 780) calls _call_extract_strings_fallback with 16 bytes of 0xFFFE code units and then iterates `for r in results: assert '￾' not in r['content']`. If _extract_strings_fallback returns an empty list (e.g. because scanning is disabled, the early-return path is taken, or all filtering logic is removed), the loop body never executes and the test passes vacuously. The test has no pre-loop assertion that len(results) > 0 or that scanning was attempted. A regression that returns [] for all UTF-16 input would evade this test entirely. The category 'conditional-never-runs' and severity 'high' are accurate."
  },
  "G0068": {
    "id": "G0068",
    "verdict": "accurate",
    "note": "test_bps_export_via_mmap_pyfallback (line 864) asserts only `original_export[:4] == b'BPS1'`. The production _build_bps_patch (line 4134) always starts with `patch = bytearray(b'BPS1')` as the very first statement, so any call that reaches this function and returns normally will produce bytes starting with b'BPS1'. An encoder that writes garbage opcodes, wrong checksums, or a patch that cannot reconstruct the target would still pass this test. The test never verifies round-trip correctness (applying the patch to source yields target), never checks the CRC fields, and never verifies the mmap constraint is honored vs read_bytes(). Category 'weak-existence' and severity 'medium' are correct."
  },
  "G0151": {
    "id": "G0151",
    "verdict": "accurate",
    "note": "test_sha256_on_first_16_bytes_of_pe_is_nonempty_hex (line 106) only checks isinstance(result, str), result is truthy, and bytes.fromhex(result) succeeds. The bridge delegates to self.document.compute_hash_range(). If the backend returned a valid hex string of a wrong hash algorithm or wrong byte range, all three assertions would still pass. The companion at line 119-130 compares against hashlib.sha256, but this test does not. The category weak-existence and severity high are appropriate."
  },
  "G0152": {
    "id": "G0152",
    "verdict": "accurate",
    "note": "test_sha1_range_is_valid_hex_digest (line 157) asserts len(result)==40 and bytes.fromhex(result). SHA-1 output is 20 bytes = 40 hex chars, which is length-distinct from MD5 (32) and SHA-256 (64), so only an algorithm that happens to produce 40-char hex output could slip through. Any wrong byte range with a correct sha1 would pass, or a stub returning any 40-char hex string would pass. The companion at line 169-180 validates against hashlib.sha1 for correctness. The finding is accurate."
  },
  "G0153": {
    "id": "G0153",
    "verdict": "accurate",
    "note": "test_crc16_on_pe_bytes_returns_hex_string (line 299) asserts isinstance(result, str), result is truthy, and bytes.fromhex(result). A CRC with a wrong polynomial, wrong init value, or wrong byte range would still produce a valid hex string. The companion at line 285-297 validates the CRC-16/CCITT-FALSE value against _crc16_ccitt() reference, but this test is format-only."
  },
  "G0154": {
    "id": "G0154",
    "verdict": "accurate",
    "note": "test_crc8_returns_valid_hex_string (line 328) asserts isinstance(result, str), result is truthy, and bytes.fromhex(result). A CRC-8 with wrong polynomial/init/range would still produce a valid non-empty hex string. The companion at line 314-326 compares against _crc8_smbus() reference. This test is format-only and would pass even if the CRC computation were entirely wrong."
  },
  "G0155": {
    "id": "G0155",
    "verdict": "accurate",
    "note": "test_md5_range_differs_from_sha256_range (line 132) asserts only sha256_result != md5_result. If both algorithm paths were broken but returned different (wrong) values—e.g., sha256 path returns sha1 output and md5 path returns crc32 output—the test still passes. Neither result is compared against an independent oracle. This is a mutual-inequality proxy that fails to catch correct-looking-but-wrong individual results. The category 'other' and severity 'medium' are appropriate given this pattern."
  },
  "G0156": {
    "id": "G0156",
    "verdict": "accurate",
    "note": "test_crc32_result_is_8_hex_chars (line 342) asserts only len(result) <= 8. The actual CRC-32 value over bytes(range(64)) is never compared to binascii.crc32(). A stub always returning '00000000' or any 1-8 char hex string would pass. The companion tests at lines 252-266 and 268-283 validate actual CRC-32 values against binascii, but this test gates only length, not value correctness."
  },
  "G0157": {
    "id": "G0157",
    "verdict": "accurate",
    "note": "test_html_export_returns_valid_html (line 50) checks only '<!DOCTYPE html>' in html and '</html>' in html. In production, export_annotated_html() unconditionally calls self._html_header() (which starts with '<!DOCTYPE html>') and appends '</body></html>' regardless of whether _render_hex_rows() executed or is broken. If _render_hex_rows() were deleted or raised an exception that was suppressed, the header and footer would still be present. The companion test at line 64-78 validates actual hex data ('DE', 'AD', 'BE', 'EF') in the output. This test does not gate content rendering."
  },
  "G0182": {
    "id": "G0182",
    "verdict": "reclassify",
    "note": "The finding category (weak-existence) and severity (medium) are accurate — `assert results` at line 166 only checks truthiness and would pass even if matches contained garbage content like wrong rule names or corrupt offsets. However, the cited line 164 is wrong: line 164 is the `_run(loaded_bridge.yara_scan_files(...))` call expression, not the weak assertion. The weak assertion `assert results` is at line 166 (and `assert isinstance(results, list)` is at line 165). The corrected line should be 166.",
    "corrected_line": 166
  },
  "G0183": {
    "id": "G0183",
    "verdict": "accurate",
    "note": "The sole assertion at line 105 is `assert results` (truthiness). The sibling test `test_yara_scan_returns_list` (line 79-96) already gates everything this test does plus more: it verifies `len(results)==1`, `results[0]['rule']=='MZHeader'`, and `strings[0]['offset']==0`. Any realistic production breakage that would make G0183 fail (e.g., `yara_scan` returning an empty list) would also be caught by the sibling test. G0183 adds zero unique gating coverage. `weak-existence` and `low` severity are both correct.",
    "corrected_category": "weak-existence",
    "corrected_severity": "low"
  },
  "G0184": {
    "id": "G0184",
    "verdict": "refute",
    "note": "The test `test_yara_scan_match_has_required_keys` asserts `_EXPECTED_MATCH_KEYS.issubset(match.keys())` where `_EXPECTED_MATCH_KEYS = {'rule', 'tags', 'meta', 'strings'}`. This IS a real gate for the 'tags' and 'meta' keys. Looking at the sibling test `test_yara_scan_returns_list` (lines 90-96), it only accesses `results[0]['rule']` and `results[0]['strings']` — it never touches 'tags' or 'meta'. If production removed `'tags'` or `'meta'` from the returned dict (e.g., a refactor of the dict comprehension at hex_editor.py line 8911-8919 dropped those keys), `test_yara_scan_returns_list` would remain green while `test_yara_scan_match_has_required_keys` would correctly fail. This is a narrow but real gate for the presence of 'tags' and 'meta' keys. The finding's claim that 'test_bridge_yara_deep.py would catch it' doesn't refute the gate; even if another test file also catches a breakage, this test still independently fails on a real production defect."
  },
  "G0175": {
    "id": "G0175",
    "verdict": "accurate",
    "note": "The walrus operator at line 69 (`if result := _run(bridge.list_transforms()):`) makes the entire assertion block — which checks for 'name', 'category', and 'description' keys — conditional on a non-empty list. Production code at line 7772-7773 of hex_editor.py explicitly returns `[]` when `_pipeline_available` is False or `_get_all_transform_nodes` is None. When the transform module is unavailable, the test passes without evaluating any key-presence assertion. Category conditional-never-runs, severity high, and line 63 are all correct."
  },
  "G0176": {
    "id": "G0176",
    "verdict": "accurate",
    "note": "The for loop `for item in result:` at line 82 iterates over the return value of `bridge.list_transforms()`. When `_pipeline_available` is False, the production bridge at hex_editor.py line 7772-7773 returns `[]`. An empty list causes the for-body (which asserts `isinstance(item['name'], str)` and `len(item['name']) > 0`) to never execute. The test passes silently with zero assertions run. Category conditional-never-runs, severity high, and line 75 are correct."
  },
  "G0177": {
    "id": "G0177",
    "verdict": "reclassify",
    "corrected_line": 156,
    "note": "The defect is real: `test_apply_pipeline_with_single_xor_step` only asserts `isinstance(result, str)` and `len(result) == 8`. XOR of 0xFF with key 0xAA yields 0x55 bytes ('55555555'), but any wrong 8-char hex string (e.g., 'deadbeef') passes. Category weak-existence and severity medium are correct. However, the cited line 178 points at the middle of the test body (`result: str = _run(...)`); the test definition begins at line 156 in test_bridge_transforms.py. Corrected line: 156."
  },
  "G0178": {
    "id": "G0178",
    "verdict": "reclassify",
    "corrected_line": 54,
    "note": "The defect is real: `test_list_transforms_returns_list` asserts only `isinstance(result, list)`. Production code returns `[]` on unavailability, so a broken implementation that always returns `[]` passes this test silently. Category weak-existence and severity low are correct. However, the cited line 61 points at the assertion `assert isinstance(result, list)`; the test definition begins at line 54. Corrected line: 54."
  },
  "G0179": {
    "id": "G0179",
    "verdict": "reclassify",
    "corrected_line": 84,
    "note": "The defect is real: `test_single_xor_step_returns_hex_string` asserts only `len(result) == 8`. XOR of 0xFF with 0xAA produces 0x55 bytes ('55555555'), but any wrong 8-character hex string passes silently. Category weak-existence and severity medium are correct. Sibling test `test_single_xor_step_known_output` (line 103) provides the real gate via `assert result == '00000000'`, but that is a different test. The cited line 101 points at the assertion; the test definition begins at line 84 in test_bridge_transforms_deep.py. Corrected line: 84."
  },
  "G0180": {
    "id": "G0180",
    "verdict": "reclassify",
    "corrected_line": 199,
    "note": "The defect is real: `test_three_step_pipeline_produces_output` asserts only `len(result) == 8`. The XOR arithmetic note in the claim is slightly off (0xFF^0x0F^0xF0 = 0x00, not 0x00 cancelling to original; result for 0x00112233 is 0x001122 XOR chain applied byte-by-byte, producing 0x001122... nonetheless), but the core gap holds: any wrong 8-char hex string passes. Category weak-existence and severity medium are correct. Sibling test `test_apply_pipeline_three_steps_with_known_verification` (line 437) provides the real gate. The cited line 220 points at `assert len(result) == 8`; the test definition begins at line 199. Corrected line: 199."
  },
  "G0181": {
    "id": "G0181",
    "verdict": "reclassify",
    "corrected_line": 222,
    "note": "The defect is real: `test_pipeline_result_length_matches_input_length` asserts only `len(result) == byte_count * 2`. Input is `bytes(range(16))` XOR'd with 0x55; expected output is fully determined (each byte b gives b^0x55), but any 32-char hex string with wrong values passes silently. Category weak-existence and severity medium are correct. The cited line 240 points at `assert len(result) == byte_count * 2`; the test definition begins at line 222 in test_bridge_transforms_deep.py. Corrected line: 222."
  },
  "G0158": {
    "id": "G0158",
    "verdict": "accurate",
    "note": "The test at line 672 uses the real hexcore native binding (pytest.importorskip enforces it) and calls bridge.list_process_regions(os.getpid()) against the current process. It asserts: isinstance(regions, list), regions (non-empty), and then only checks key presence in regions[0]: 'base_address', 'size', 'protection', 'state'. The production code at hex_editor.py:8123 maps Rust tuple elements r[0]..r[3] to these keys. A broken implementation that returned [{\"base_address\": 0, \"size\": 0, \"protection\": 0, \"state\": 0}] would satisfy all assertions. No assertion checks that base_address is non-zero, size is a page multiple, or that protection/state match any valid Windows memory constant. Category weak-existence, severity medium, and line 672 are all correct."
  },
  "G0159": {
    "id": "G0159",
    "verdict": "accurate",
    "note": "The test at line 689 also uses the real hexcore binding against the current process. It asserts that at least one region in the returned list has r['state'] == 0x1000 (MEM_COMMIT). While this checks an actual Windows constant value rather than mere key presence, it remains a weak gate: a broken list_process_memory_regions that returned a single hardcoded region tuple (0, 4096, 4, 0x1000) would satisfy the assertion without correctly enumerating process memory. The test does not verify that multiple committed regions exist (any real Python process has dozens), nor that the enumeration is complete or ordered correctly. Low severity is appropriate because the companion G0158 test already verifies the return structure, and this test only adds a narrow semantic check on the state field."
  },
  "G0160": {
    "id": "G0160",
    "verdict": "accurate",
    "note": "The test at line 799 asserts only len(td.functions) > 42. The actual tool_definition contains well over 100 ToolFunction entries (the functions list in hex_editor.py spans lines 501-1600+, with at least 80+ distinct ToolFunction instances). Removing or renaming any of the 5 specifically intended new functions (encode_text, search_bytes, search_numeric_range, list_process_regions, open_process_memory) would not cause this test to fail, since the count would remain far above 42. The companion test test_new_tool_functions_registered at line 783 explicitly checks by name for each of those 5 functions and would fail on any removal. This count-only assertion therefore adds no semantic gate beyond what the companion test already provides. Category weak-existence, severity low, and line 799 are all correct."
  },
  "G0185": {
    "id": "G0185",
    "verdict": "accurate",
    "note": "Line 49-50: `result = sample_doc_from_bytes.inspect_at(0)` then `assert isinstance(result, dict)`. Confirmed pure type check. A stub returning `{'garbage': 'values'}` passes. Value correctness is gated only in TestInspectAtValues (e.g., test_uint8_at_zero_byte_in_known_data asserts result['uint8'] == '0'). Category weak-existence and severity low are correct."
  },
  "G0186": {
    "id": "G0186",
    "verdict": "accurate",
    "note": "Line 58-59: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint8' in result`. Pure key-presence check. If inspect_at returned `{'uint8': 'banana'}` this passes. Actual uint8 decoding correctness at offset 0 (expected '0') is gated only by TestInspectAtValues.test_uint8_at_zero_byte_in_known_data which asserts `result['uint8'] == '0'`. Category and severity are correct."
  },
  "G0187": {
    "id": "G0187",
    "verdict": "accurate",
    "note": "Line 67-68: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'int8' in result`. Pure key-presence check. A stub returning `{'int8': 'garbage'}` passes. No value correctness is gated here. The finding is accurately categorized as weak-existence/low."
  },
  "G0188": {
    "id": "G0188",
    "verdict": "accurate",
    "note": "Line 76-77: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint16_le' in result`. Only gates key presence. uint16_le at offset 0 of bytes(range(256)) should be 256 (0x0100), but that value is not checked here. The value correctness is gated only in TestInspectAtValues.test_uint16_le_known_value which checks a different offset. Category weak-existence/low is accurate."
  },
  "G0189": {
    "id": "G0189",
    "verdict": "accurate",
    "note": "Line 85-86: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint32_le' in result`. Only gates key presence; the uint32_le decoding capability is not value-checked. TestInspectAtValues.test_uint32_le_known_value gates the actual value. Category weak-existence/low is accurate."
  },
  "G0190": {
    "id": "G0190",
    "verdict": "accurate",
    "note": "Line 94-95: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint32_be' in result`. Only gates key presence. uint32_be decoding at offset 0 is value-checked in TestInspectAtValues.test_uint32_be_known_value, not here. Category weak-existence/low is accurate."
  },
  "G0191": {
    "id": "G0191",
    "verdict": "accurate",
    "note": "Line 103-104: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint64_le' in result`. Only gates key presence. No value check whatsoever for uint64_le decoding. Category weak-existence/low is accurate."
  },
  "G0192": {
    "id": "G0192",
    "verdict": "accurate",
    "note": "Line 112-113: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'float32_le' in result`. Only gates key presence. float32 decoding correctness is gated by TestInspectAtValues.test_float32_le_known_value (asserts parsed float is within 1e-5 of 1.0). Category weak-existence/low is accurate."
  },
  "G0193": {
    "id": "G0193",
    "verdict": "accurate",
    "note": "Line 121-122: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'float64_le' in result`. Only gates key presence. float64 decoding correctness is gated by TestInspectAtValues.test_float64_le_known_value. Category weak-existence/low is accurate."
  },
  "G0194": {
    "id": "G0194",
    "verdict": "accurate",
    "note": "Lines 130-132: `result = sample_doc_from_bytes.inspect_at(0)` then iterates `_EXPECTED_KEYS` asserting each is present in result. Checks all 14 expected keys exist but no values are checked. Any dict containing all 14 keys with arbitrary string values passes. Category weak-existence/low is accurate."
  },
  "G0195": {
    "id": "G0195",
    "verdict": "accurate",
    "note": "Lines 140-142: `result = sample_doc_from_bytes.inspect_at(0)` then `for key, value in result.items(): assert isinstance(value, str)`. Gates that all values are str type, not that values are numerically correct. A stub returning `{'uint8': 'banana', 'int8': 'orange', ...}` satisfies this assertion. The test does gate the string-return format but not value correctness. Category weak-existence/low is accurate."
  },
  "G0196": {
    "id": "G0196",
    "verdict": "accurate",
    "note": "Lines 274-277: `doc_length = sample_doc_from_bytes.length()`, `result = sample_doc_from_bytes.inspect_at(doc_length - 1)`, `assert isinstance(result, dict)`, `assert 'uint8' in result`. Only checks type and key presence at the last valid offset (255). The uint8 value at offset 255 ('255') is gated by TestInspectAtValues.test_uint8_at_offset_255 which asserts `result['uint8'] == '255'`. Category weak-existence/low is accurate."
  },
  "G0197": {
    "id": "G0197",
    "verdict": "accurate",
    "note": "Lines 285-287: `result = sample_doc_from_bytes.inspect_at(253)`, `assert isinstance(result, dict)`, `assert 'uint8' in result`. Only gates that a result dict with 'uint8' key is returned at near-boundary offset 253; no value is checked. A stub returning `{'uint8': ''}` passes. Category weak-existence/low is accurate."
  },
  "G0198": {
    "id": "G0198",
    "verdict": "accurate",
    "note": "Lines 295-297: `result: dict[str, str] = sample_doc_from_bytes.inspect_at(128)`, `assert isinstance(result, dict)`, `assert result`. Only checks that a non-empty dict is returned at mid-document offset 128. Neither the uint8 value (which should be '128' at offset 128 of bytes(range(256))) nor any field is verified. A stub returning any non-empty dict passes. Category weak-existence/low is accurate."
  },
  "G0230": {
    "id": "G0230",
    "verdict": "refute",
    "note": "The finding claims weak-existence but the test at line 46-48 of test_patch_export.py calls `sample_doc_from_bytes.write_bytes(10, b\"\\xaa\\xbb\\xcc\\xdd\")` then asserts `assert patches` (truthiness). If write_bytes is broken to not record patches in the undo manager, get_patches() returns [] and `assert patches` fails on a falsy empty list. This is a genuine gate on the patch-recording behavior: any realistic breakage where write_bytes does not persist a patch entry would be caught. The `confirmed: false` in the batch json reflects that the auditor themselves determined this is NOT a non-gate finding."
  },
  "G0231": {
    "id": "G0231",
    "verdict": "refute",
    "note": "The finding claims weak-existence but the test at line 108-110 of test_patch_export.py calls `sample_doc_from_bytes.export_patches_ips()` after a write_bytes and asserts `isinstance(result, bytes)`. If export_patches_ips() panics, raises, or returns None/str/int instead of bytes, the assertion fails. This gates that the IPS exporter is callable and returns the correct type on valid input. The `confirmed: false` in the batch json confirms the auditor already determined this is a real gate."
  },
  "G0232": {
    "id": "G0232",
    "verdict": "refute",
    "note": "The finding claims weak-existence but the test at line 154-156 of test_patch_export.py calls `sample_doc_from_bytes.export_patches_ips32()` after a write_bytes and asserts `isinstance(result, bytes)`. If export_patches_ips32() panics, raises, or returns the wrong type, the assertion fails. This gates that the IPS32 exporter succeeds and returns bytes on valid input. The `confirmed: false` in the batch json confirms the auditor already determined this is a real gate."
  },
  "G0233": {
    "id": "G0233",
    "verdict": "accurate",
    "note": "The test at lines 70-76 of test_patch_export.py calls write_bytes(5, b\"\\x01\\x02\") then iterates patches checking isinstance(entry, tuple), len(entry)==2, isinstance(entry[0], int), isinstance(entry[1], bytes). A stub returning [(0, b'')] after any write would pass all four assertions - offset 0 instead of 5, empty bytes instead of b\"\\x01\\x02\", yet no assertion fails. The actual offset and data correctness are gated by the sibling test_patch_entry_has_correct_offset_and_data at line 50-62, which explicitly checks offsets_and_data[offset] == payload."
  },
  "G0234": {
    "id": "G0234",
    "verdict": "accurate",
    "note": "The test at lines 67-75 of test_process_memory.py calls list_process_memory_regions(pid) and asserts only `isinstance(regions, list)`. An empty list [] passes this check since it is still an instance of list. This means a broken implementation returning [] (no regions enumerated) would pass this test. The actual enumeration and non-emptiness are gated by sibling tests: test_list_regions_nonempty_for_current_process at line 77 asserts `assert regions`, and test_list_regions_each_entry_has_four_elements at line 87 validates structure."
  },
  "G0235": {
    "id": "G0235",
    "verdict": "accurate",
    "note": "The test at lines 153-170 of test_process_memory.py calls from_process_memory(pid, addr, 8) and checks hasattr(doc, 'length'), hasattr(doc, 'read'), and doc.length() == 8. While doc.length() == 8 does gate the reported size matches the requested size argument, doc.read() is never called, so the actual bytes read from process memory are never verified. A stub HexDocument that records size=8 but never calls ReadProcessMemory would pass all assertions. The sibling test test_read_from_current_process_first_region at line 134 also stops at doc.length() == 16 without checking actual data content, meaning no test in this file verifies that from_process_memory reads correct byte values from the target process."
  },
  "G0236": {
    "id": "G0236",
    "verdict": "accurate",
    "note": "test_returns_nonempty_list at line 55 does only `assert templates` - a truthiness check. Any non-empty list, including [('dummy', '')], passes. The genuine gates on specific template names (IMAGE_DOS_HEADER, Elf64_Ehdr, ZIP_LOCAL_FILE_HEADER) are provided by sibling tests at lines 72-106. weak-existence/low is correct."
  },
  "G0237": {
    "id": "G0237",
    "verdict": "accurate",
    "note": "test_entries_are_name_description_pairs at line 66 checks structural shape only: len==2, entry[0] is non-empty str, entry[1] is str (no non-empty check on description). A registry returning [('x', '')] with placeholder data passes all assertions. Specific name registration is gated by sibling tests. weak-existence/low is correct."
  },
  "G0238": {
    "id": "G0238",
    "verdict": "accurate",
    "note": "test_returns_nonempty_list at line 121 does only `assert detailed`. Any single-element non-empty list passes. The stronger gate is test_dos_header_field_count_positive at line 140 which checks for IMAGE_DOS_HEADER specifically and verifies field_count > 0 and category == 'PE'. weak-existence/low is correct."
  },
  "G0239": {
    "id": "G0239",
    "verdict": "reclassify",
    "note": "The finding is substantively accurate (weak-existence/low): the test does only `assert fields` at line 198, while the real gate on correct template parsing is test_first_field_is_e_magic at line 200 (checks fields[0]['name'] == 'e_magic'). However, the cited line 197 points to the `apply_template` call (`fields: list[dict[str, Any]] = doc.apply_template('IMAGE_DOS_HEADER', 0)`), not the assertion. The assertion `assert fields` is on line 198. Correcting line to 198; category and severity are accurate.",
    "corrected_line": 198,
    "corrected_category": "weak-existence",
    "corrected_severity": "low"
  },
  "G0240": {
    "id": "G0240",
    "verdict": "accurate",
    "note": "test_apply_returns_fields at line 289 does only `assert fields`. Line 289 is the assertion (`assert fields`), line 288 is the apply_template call - line number is correct. Applying Elf64_Ehdr could return any non-empty list and pass. The real gate is test_first_field_is_e_ident at line 300 which checks fields[0]['name'] == 'e_ident'. weak-existence/low is correct."
  },
  "G0241": {
    "id": "G0241",
    "verdict": "accurate",
    "note": "test_apply_returns_fields at line 341 does only `assert fields` at line 341. Any non-empty list passes. The real gate is test_first_field_is_signature at line 352 which checks fields[0]['name'] == 'signature'. weak-existence/low is correct."
  },
  "G0242": {
    "id": "G0242",
    "verdict": "refute",
    "note": "The test at line 149 (`assert any('xor' in n.lower() for n in names)`) IS a real gate: if all XOR-related transforms (xor_single, xor_repeating, xor_rolling, mask_xor) were removed from the Rust TRANSFORM_LIST in transforms.rs, this test would fail because no name would contain 'xor'. The claim argues it is 'redundant' because the upstream count-and-name test also catches it, but redundancy does not negate gate-ness. The definition of a gate is that breaking the production capability makes this test fail, and removing all XOR transforms does exactly that. A narrower breakage (e.g., removing only xor_single while xor_repeating remains) would pass this test but fail the upstream test - that makes the upstream test stronger, not this test a non-gate. The substring match `'xor' in n.lower()` is satisfied by any of the four xor-named transforms remaining, so it would only pass with zero false negatives in the unlikely scenario where all xor transforms are renamed to 'multiply' while the upstream exact-name test fails first. This test is a genuine gate on XOR transform category presence."
  },
  "G0204": {
    "id": "G0204",
    "verdict": "accurate",
    "note": "test_entropy_sample_bytes_at_most_eight (line 118) asserts only `result <= 8.0`. The input is bytes(range(256)) whose real entropy is 8.0, so the production implementation passes. But any stub returning a constant like 0.0 also satisfies the bound. The constraint is a necessary mathematical property of Shannon entropy, not a discriminating check. A production regression that returned 0.0 for all inputs would pass this test while failing test_entropy_uniform_256_matches_independent_oracle (line 59). Weak-existence classification is correct."
  },
  "G0205": {
    "id": "G0205",
    "verdict": "accurate",
    "note": "test_entropy_is_float (line 127) asserts only `isinstance(result, float)`. Any stub returning a float constant (0.0, 1.0, 8.0) passes unconditionally. This is a pure type-presence check with no semantic content. A computation that returns the wrong entropy value but the correct Python type is completely invisible to this test. Weak-existence is the correct category."
  },
  "G0206": {
    "id": "G0206",
    "verdict": "accurate",
    "note": "test_entropy_lower_bound (line 136) asserts only `result >= 0.0`. Any non-negative constant satisfies this, including 0.0. A broken implementation returning a fixed non-negative float would pass regardless of whether it computes entropy correctly. The claim is correct: this is a trivial mathematical constraint check, not a behavioral gate. Weak-existence and low severity are appropriate."
  },
  "G0207": {
    "id": "G0207",
    "verdict": "accurate",
    "note": "test_entropy_map_returns_list_of_floats (line 164) checks `isinstance(result, list)` and `all(isinstance(v, (int, float)) for v in result)`. A stub returning the structurally correct `[0.0] * 8` (8 zeros for a 256-byte document with block_size=32) passes completely despite computing no entropy. Only wrong return types (e.g., returning None or a list of strings) would be caught. The original auditor flagged this as 'confirmed: false' due to sibling tests like test_entropy_map_block_count_matches_doc_length enforcing block counts, but that only means the suite together gates correctness — this individual test is still weak-existence because realistic value-level regressions slide through it silently."
  },
  "G0208": {
    "id": "G0208",
    "verdict": "accurate",
    "note": "test_byte_distribution_returns_ints (line 264) asserts `all(isinstance(count, int) for count in result)`. For the bytes(range(256)) input, returning `[0] * 256` (all integer zeros) passes despite being semantically wrong — the real counts are all 1. Only a type-level regression (e.g., returning floats or strings) would be caught. Sibling tests like test_byte_distribution_uniform_all_ones (line 244) gate the actual values; this test adds no value gate on top."
  },
  "G0209": {
    "id": "G0209",
    "verdict": "accurate",
    "note": "test_byte_type_distribution_returns_four_values (line 281) asserts `len(result) == 4`. A stub returning `(0, 0, 0, 0)` passes unconditionally because it has length 4 and the test checks nothing about the values. Swapping categories, zeroing counts, or returning wrong proportions all slide through. The original auditor's 'confirmed: false' reflects that sibling tests (lines 293, 304, 316) validate actual category values for controlled inputs, but those tests gate correctness independently — this structural-length check alone is a weak-existence gate."
  },
  "G0210": {
    "id": "G0210",
    "verdict": "accurate",
    "note": "test_content_classification_values_in_range (line 398) checks `isinstance(value, int) and 0 <= value <= 4` for every element. A stub returning `[0] * ceil(256/32)` (8 zeros) satisfies all three predicates regardless of whether the entropy-based classification logic is correct. The test exercises real production code (content_classification(32) on HexDocument) but the assertion is only a range/type constraint. Sibling test_content_classification_zeros_classified_low_entropy (line 419) pins class 0 to {0,1} for all-zero input; this test adds no discriminating check beyond bounds. Weak-existence and low severity are correct."
  },
  "G0199": {
    "id": "G0199",
    "verdict": "refute",
    "note": "The test at line 80-89 calls `doc.decode_text(0, 4, 'utf-8')` on real `b'test'` data via the native Rust/PyO3 `HexDocument.decode_text` binding (lib.rs line 571) which calls `encodings::decode_text` (encodings.rs line 139). If `decode_text` raised a `PyValueError` for valid UTF-8 (e.g., the encoding resolver mistakenly rejected `'utf-8'`) the test would fail with an exception rather than passing. `isinstance(result, str)` also catches any hypothetical return-type regression. The PyO3 binding returns `PyResult<String>` and any error path surfaces as a Python exception — exactly the failure mode `assert isinstance(result, str)` catches via the test failing with an unhandled exception. The companion equality tests (test_decode_utf8_hello_world, test_decode_ascii_text) are stronger, but this test is still a narrow real gate against decode_text raising on valid ASCII-range UTF-8 input. Category `weak-existence` mischaracterises it; the test cannot be passed by a no-op stub that raises, making it a genuine (if narrow) gate."
  },
  "G0200": {
    "id": "G0200",
    "verdict": "accurate",
    "note": "The test at line 190-198 asserts `isinstance(result, list)` and `assert result` (non-empty) on `list_encodings()`. The production code returns a fixed `ENCODING_LIST` slice of 35 entries (encodings.rs lines 323-363). A stub that returned `[('x', 'y')]` (one entry, non-empty) would satisfy both assertions and the test would pass, while `list_encodings` no longer returns the real encoding inventory. The companion tests `test_list_encodings_contains_utf8` (line 221) and `test_list_encodings_contains_ascii` (line 231) provide the semantic gate for actual encoding names. This specific test checks only non-emptiness and list type — `weak-existence` and `low` severity are correct."
  },
  "G0201": {
    "id": "G0201",
    "verdict": "accurate",
    "note": "The test at line 200-208 iterates the result of `list_encodings()` and asserts `len(entry) == 2` for each entry. The production code yields `(name.to_string(), desc.to_string())` 2-tuples (encodings.rs line 367-370). A stub returning `[('', '')]` satisfies `len(entry) == 2` exactly. This is a pure structural/format check; empty-string 2-tuples pass with no semantic gating of encoding names or labels. Category `weak-existence` and `low` severity are accurate."
  },
  "G0202": {
    "id": "G0202",
    "verdict": "accurate",
    "note": "The test at line 210-219 checks `isinstance(name, str)` and `isinstance(label, str)` for each tuple from `list_encodings()`. A stub returning `[('', '')]` satisfies both `isinstance` checks with empty strings. The PyO3 binding already guarantees `String` fields become Python `str`, so the type check provides no additional gate beyond the Rust type system. The real encoding-name content is gated by `test_list_encodings_contains_utf8` and `test_list_encodings_contains_ascii`. Category `weak-existence` and `low` severity are accurate."
  },
  "G0203": {
    "id": "G0203",
    "verdict": "accurate",
    "note": "The test at line 241-249 asserts `len(name) > 0` for each name in `list_encodings()`. A stub returning `[('x', 'x')]` satisfies `len('x') > 0` and passes. The check only prevents fully-empty string names; a single-character or meaningless name passes. The actual semantic gate (that real codec identifiers like 'utf-8' and 'ascii' are present) is provided by `test_list_encodings_contains_utf8` and `test_list_encodings_contains_ascii`. Category `weak-existence` and `low` severity are accurate."
  },
  "G0260": {
    "id": "G0260",
    "verdict": "accurate",
    "note": "The test at lines 228-232 checks only isinstance(model.context_window, int), isinstance(model.supports_tools, bool), isinstance(model.supports_vision, bool). These type checks pass for any stub or broken builder that returns context_window=0, supports_tools=False, supports_vision=False. The sibling test test_list_models_returns_model_info_instances (lines 131-170) already gates the hard-coded invariants: context_window == 4096 (_HUGGINGFACE_DEFAULT_CONTEXT_WINDOW), supports_streaming is True, provider is HUGGINGFACE, and name derivation. The test under review adds no new gate beyond trivial type correctness; the weak-existence/medium classification is correct."
  },
  "G0261": {
    "id": "G0261",
    "verdict": "accurate",
    "note": "The test (lines 147-149) calls provider.unload_model() then provider.disconnect() and asserts only provider.connected is False. The production unload_model() in local_transformers.py (lines 1599-1613) sets self._loaded_model = None at line 1611. If unload_model() became a no-op (the entire if-block never ran, or the assignment was removed), _loaded_model would retain the loaded model reference (memory leak, model still usable). The single assertion provider.connected is False would still pass because disconnect() independently sets self.connected = False through the base class, completely orthogonal to whether unload_model() cleared the reference. The production capability 'unload_model clears self._loaded_model' is entirely untested - no assertion on provider._loaded_model is made. The no-assertion/medium classification is correct."
  },
  "G0262": {
    "id": "G0262",
    "verdict": "accurate",
    "note": "The finding category 'conditional-never-runs' is correct despite confirmed:false in the batch file. The test (lines 191-195) evaluates b580 = is_arc_b580(), then asserts isinstance(b580, bool) (always trivially passes), and only inside 'if b580:' does it assert is_xpu_available() and get_xpu_device_count() > 0. On any host without an Arc B580 - which is the vast majority of CI/developer machines - is_arc_b580() returns False, the if-block never executes, and the meaningful assertions are dead code. Additionally, the invariant is tautological: the production is_arc_b580() at line 464 calls is_xpu_available() first and immediately returns False if XPU is unavailable, making 'is_arc_b580() -> is_xpu_available()' impossible to break by design. On non-B580 hardware the conditional assertions never run; on B580 hardware the invariant cannot break. The conditional-never-runs/medium classification is warranted."
  },
  "G0288": {
    "id": "G0288",
    "verdict": "accurate",
    "note": "The test at line 62-65 calls is_xpu_available() and asserts only isinstance(result, bool). Breaking is_xpu_available() to return the wrong boolean value (e.g., always False when XPU is present) leaves this test green. The sibling test_device_count_consistent_with_availability (line 68-76) provides a gate, but that is a different test. This specific test is correctly categorized as weak-existence, medium severity."
  },
  "G0289": {
    "id": "G0289",
    "verdict": "accurate",
    "note": "The test at line 79-84 calls is_arc_b580(), checks isinstance(result, bool), and only enters the if-branch if result is True (i.e., B580 is present). On a machine without an Arc B580, result is False and the if-branch never executes, leaving the test as a pure type check. The test does not skip on non-B580 machines, so it runs as a weak-existence check. The claim is accurate for the typical (non-B580) execution path."
  },
  "G0290": {
    "id": "G0290",
    "verdict": "accurate",
    "note": "The test at lines 186-193 runs check_windows_requirements() and asserts isinstance(met, bool), isinstance(warnings, list), and all(isinstance(w, str) for w in warnings). A stub returning (True, []) unconditionally passes all three assertions. The production code queries WMI via PowerShell, checks Windows version, driver presence, and ReBAR status, but none of those behavioral results are asserted. The finding is correctly confirmed as a weak-existence test."
  },
  "G0291": {
    "id": "G0291",
    "verdict": "accurate",
    "note": "The test at lines 682-688 resets the registry, creates a loader, calls get_provider_registry(credential_loader=loader) to get reg, then asserts only get_provider_registry() is reg (singleton identity). The production code at registry.py line 75 stores self._credential_loader = credential_loader, but the test never checks that reg._credential_loader is loader or that the loader is actually used. The production code could silently discard the credential_loader argument and this test would still pass because the only assertion is about object identity."
  },
  "G0292": {
    "id": "G0292",
    "verdict": "refute",
    "note": "The test contains TWO assertions: line 572 asserts providers_pkg.get_provider_registry is get_provider_registry (tautological since both resolve to the same imported object), and line 573 asserts 'get_provider_registry' in providers_pkg.__all__ (a genuine gate). Removing get_provider_registry from __all__ in the providers package would fail the assertion at line 573. The test is not purely tautological - it contains a real production gate on the public API surface. The finding incorrectly categorizes the whole test as tautological when line 573 provides real coverage."
  },
  "G0293": {
    "id": "G0293",
    "verdict": "accurate",
    "note": "The test is decorated with @pytest.mark.skipif(os.environ.get('INTELLICRACK_LOCAL_TESTS') != '1', reason='...') at lines 23-26. In any CI environment where INTELLICRACK_LOCAL_TESTS is not set to '1', the test is permanently skipped. A broken double-checked locking implementation in get_provider_registry() that produces multiple singleton instances under concurrent access (the scenario tested by reset_provider_registry() + 32 simultaneous threads at lines 41-73) would never be caught because the test never executes. The skip-masks-failure category and high severity are correct."
  },
  "G0279": {
    "id": "G0279",
    "verdict": "accurate",
    "note": "The test at lines 121-131 of test_realcov_10_cancel_request.py calls `cancel_request()` then immediately `break`s the async for loop, then asserts `getattr(anthropic_provider, '_cancel_requested') is True`. Since `anthropic.py:cancel_request()` unconditionally sets `self._cancel_requested = True` (line 774) and the base class does the same (base.py:507), the assertion is trivially guaranteed. The actual production guard — `if self._cancel_requested: break` at anthropic.py:732-733 inside `_iter_anthropic_stream` — is never reached during test execution because the `break` on the consumer side exits the generator before the next iteration begins. A broken implementation that sets the flag but removes the check at lines 732-733 would pass this test identically, so the test does not gate the streaming-loop cancellation path."
  },
  "G0280": {
    "id": "G0280",
    "verdict": "accurate",
    "note": "Same structural problem as G0279. The Grok test at lines 159-168 calls `cancel_request()` and immediately `break`s the async for loop, then asserts `getattr(grok_provider, '_cancel_requested') is True`. `grok.py:cancel_request()` (line 964-966) unconditionally sets `self._cancel_requested = True`, making the assertion tautological. The production guard at grok.py:943 (`if self._cancel_requested: break`) inside the streaming generator is never reached — the consumer-side `break` exits the generator first. Removing the guard at line 943 while keeping `cancel_request()` working would leave this test green."
  },
  "G0281": {
    "id": "G0281",
    "verdict": "accurate",
    "note": "The test at lines 195-203 of test_realcov_10_grok_reasoning_effort.py wraps the entire `grok_provider.chat()` call in `try/except ProviderError as exc: pytest.skip(...)`. A broken `_reasoning_effort_for` mapping that produced an invalid wire value (unrecognized tier string, malformed value, etc.) would cause the X.AI API to return HTTP 400 Bad Request; `openai.BadRequestError` (a subclass of `openai.APIError`) would be caught by the `_translate_openai_errors` context manager in grok.py and re-raised as `ProviderError`, which the test then catches and converts to `pytest.skip`. The test would report green (skipped) rather than red, making it unable to gate the correctness of the reasoning-effort mapping on a live call."
  },
  "G0282": {
    "id": "G0282",
    "verdict": "accurate",
    "note": "The test at lines 142-149 of test_realcov_11_gpu_pci.py asserts `cfg is not None` and then `callable(getattr(cfg, attr))` for four attribute names. In production, `_Cfgmgr32.__init__` (gpu_pci_resources.py:89-103) assigns ctypes function pointers from `cfgmgr32.dll` to these attributes. All ctypes function pointer objects are callable, so the `callable()` check passes regardless of which DLL symbol each attribute was bound to. If `locate_devnode` were accidentally bound to `CM_Get_Res_Des_Data` (a swapped pointer), the test would still pass because the misbinding remains callable. The test gates existence of the binding object and attribute callability, but not that each function pointer resolves to the correct Windows API symbol."
  },
  "G0304": {
    "id": "G0304",
    "verdict": "accurate",
    "note": "The test captures `before = inst.last_used` (an immutable datetime), calls `inst.touch()`, then asserts `inst.last_used >= before`. If `touch()` is mutated to a no-op (`pass`), `inst.last_used` remains the same datetime object as `before`, making `before >= before` reflexively true. The `>=` operator cannot detect this failure mode. The test only uses `_TestInstance` (not the real `SandboxInstance`), and the real `SandboxInstance.touch()` at line 107 in manager.py sets `self.last_used = datetime.now(UTC)` — but this test never exercises that real method. Both the weakness (reflexive comparison) and the category (weak-existence) are correct."
  },
  "G0305": {
    "id": "G0305",
    "verdict": "reclassify",
    "corrected_category": "other",
    "corrected_severity": "low",
    "corrected_line": 309,
    "note": "The finding is correct that the test exercises `_TestInstance` (the test-local stub class) rather than the real `SandboxInstance` imported from `intellicrack.sandbox.manager`. However, the category 'mock-shadows-target' is wrong: no `mock.patch` or `unittest.mock` is involved. The test uses a hand-rolled substitute class defined in the same test file — this is a fixture-substitution problem, not mock shadowing. The real `SandboxInstance.last_report` field (defined in manager.py line 88) is never exercised. Category should be 'other' since the mechanism is a test-local stub class replacing the real production class."
  },
  "G0306": {
    "id": "G0306",
    "verdict": "reclassify",
    "corrected_category": "other",
    "corrected_severity": "low",
    "corrected_line": 329,
    "note": "Same structural problem as G0305: the test exercises `_TestInstance(sb, binary_path=Path('test.exe'))` and asserts `inst.binary_path == Path('test.exe')`. This validates `_TestInstance.__init__`'s parameter storage (line 231 of the test file), not the real `SandboxInstance.binary_path` assignment (manager.py line 87). No `mock.patch` or `unittest.mock` is involved, so 'mock-shadows-target' is the wrong category. 'other' is correct since the mechanism is a test-local stub class that never imports or exercises the real production attribute."
  },
  "G0307": {
    "id": "G0307",
    "verdict": "accurate",
    "note": "The assertion `copy is not getattr(mgr, '_instances')` is tautologically true: `mgr.instances` returns `list(self._instances.values())` (a new list object), while `mgr._instances` is a dict. A list can never be `is` the same object as a dict, so this identity check always passes regardless of the implementation. The real contract being protected (mutations to the returned list should not affect internal state) is never validated. The finding, category (tautological), and severity (low) are all correct."
  },
  "G0308": {
    "id": "G0308",
    "verdict": "accurate",
    "note": "The test calls `await mgr.create()` and asserts `inst.id is not None`. `_TestableManager.create()` instantiates `_TestInstance`, whose `__init__` sets `self.id = f'test-{_TestInstance._counter:04d}'` — always a non-empty string, never None. The assertion cannot fail under any realistic breakage because the id is generated deterministically from a counter in the test fixture, not from the real `SandboxInstance` which uses `str(uuid4())`. Any id format regression in the real `SandboxManager.create()` would not be caught by this test. The weak-existence finding is correct."
  },
  "G0309": {
    "id": "G0309",
    "verdict": "accurate",
    "note": "The test only checks `'max_instances' in status`, `'active_count' in status`, `'total_count' in status`, and `'instances' in status` — pure key-existence checks with no value validation. A broken `_TestableManager.get_status()` returning `{'max_instances': None, 'active_count': -999, 'total_count': 'wrong', 'instances': None}` would pass all four assertions. The finding, category (weak-existence), and severity (low) are correct."
  },
  "G0294": {
    "id": "G0294",
    "verdict": "accurate",
    "note": "The test at line 635 asserts only `len(pattern_types) >= 2`. The fixture `sample_network_activity` produces 4 pattern types: beaconing (5 uniform-interval connections), dga_domain (xkqwzjrtmnpv.evil.com, entropy well above 3.5), known_c2_port (port 4444), and data_exfiltration (5MB sent / 100 received, ratio > 10). Removing `_detect_beaconing()` from `detect_c2_patterns` entirely still leaves dga_domain, known_c2_port, and data_exfiltration (3 types >= 2), so the test stays green. Similarly, removing `_detect_dga_domains` still leaves beaconing, known_c2_port, data_exfiltration. The >= 2 threshold is too weak to gate any individual sub-detector."
  },
  "G0295": {
    "id": "G0295",
    "verdict": "accurate",
    "note": "The test at line 848 asserts only `len(ioc_types) >= 2`. The full `sample_report` fixture populates: network_activity (public IPs → ipv4, DGA domain → domain), registry_changes (admin@evil.com → email, C:\\Temp\\payload.exe value_data), process_activity (https://evil.com/dl.exe command_line → url, domain). Production normally yields ipv4, domain, url, and email (4+ types). Removing three IOC extractors (e.g., _scan_text from registry, process, and domain extraction from network) still leaves ipv4 + domain from network_activity alone (2 types, >= 2). The >= 2 threshold does not individually gate any IOC extractor."
  },
  "G0296": {
    "id": "G0296",
    "verdict": "accurate",
    "note": "The test at line 1114 asserts `len(categories) >= 5`. The `sample_report` fixture provides data for all 10 category generators in `generate_timeline`: file, registry, network, process, api, service, kernel, dll, injection, clipboard. Removing 5 of the 10 generator calls (_timeline_add_dll_events, _timeline_add_injection_events, _timeline_add_service_events, _timeline_add_kernel_events, _timeline_add_clipboard_events) still leaves file + registry + network + process + api (5 categories, >= 5). The threshold is too permissive to gate any individual category generator."
  },
  "G0297": {
    "id": "G0297",
    "verdict": "accurate",
    "note": "The test at line 1467 asserts `len(categories) >= 3`. The `sample_report` fixture triggers all five MITRE category matchers: Persistence (service creation via MalSvc, Run key via HKLM Run, schtasks.exe), Defense Evasion (injection event, IsDebuggerPresent API, Sleep(120000ms)), Command and Control (beaconing from network, DoH to 1.1.1.1:443), Exfiltration (5MB outbound, clipboard read), Discovery (whoami.exe). That's 5 categories. Disabling two matchers (e.g., removing _match_command_and_control and _match_discovery) still leaves Persistence + Defense Evasion + Exfiltration (3 categories, >= 3). The >= 3 threshold does not gate each individual category matcher."
  },
  "G0298": {
    "id": "G0298",
    "verdict": "refute",
    "note": "The finding was already marked confirmed=false by the original auditor. The test contains hard membership assertions at lines 1819-1821: `assert 'beaconing' in pattern_types`, `assert 'data_exfiltration' in pattern_types`, `assert 'known_c2_port' in pattern_types`. These are specific gates on three distinct detectors. If `_detect_beaconing` is removed or broken, the test fails at line 1819. If `_detect_exfiltration_patterns` is removed, it fails at line 1820. If `_detect_c2_ports` is removed, it fails at line 1821. The `>= 3` check at line 1815 is redundant given the three explicit `in` assertions, but those assertions make this a real gate for each of the three named sub-detectors. A realistic breakage of any one detector causes a specific assertion failure."
  },
  "G0263": {
    "id": "G0263",
    "verdict": "accurate",
    "note": "Test lines 749-751 call check_windows_requirements() and assert only isinstance(all_met, bool) and isinstance(warnings, list). The production function (xpu_utils.py line 599) does real driver/ReBAR checking and returns (bool, list[str]). A broken implementation hardcoded to return (True, []) satisfies isinstance checks for both. No assertion verifies that actual validation logic ran, that warnings are non-empty when drivers are missing, or that all_met is False under bad conditions."
  },
  "G0264": {
    "id": "G0264",
    "verdict": "accurate",
    "note": "Test lines 1101-1133 asserts only len(response1.content) > 0 (line 1118) and len(response2.content) > 0 (line 1133). Although the second turn asks 'What is my name?' after establishing 'My name is Alice', the test never checks that response2.content contains 'Alice' or any related answer. A broken multi-turn implementation that ignores message history and returns a generic non-empty response (e.g., 'I do not know.') passes both assertions. The actual context preservation capability is ungated."
  },
  "G0265": {
    "id": "G0265",
    "verdict": "accurate",
    "note": "Test lines 1135-1171 assert only len(response1.content) > 0 (line 1163) and len(response2.content) > 0 (line 1171). No assertion verifies that the system prompt 'You are a helpful assistant.' influenced responses or that the third turn ('Tell me something interesting') relates to prior context. A stub that produces one non-empty byte for both turns would pass."
  },
  "G0266": {
    "id": "G0266",
    "verdict": "accurate",
    "note": "Test lines 1285-1302 produce a response at max_tokens=100 and assert only len(response.content) > 0. The docstring states 'should produce a longer response than max tokens of 10', but no comparison to a 10-token run is ever performed and no word-count or character-count lower bound is checked. Passing max_tokens=100 but having it ignored (function returning any non-empty string) would satisfy the assertion. The test is a pure existence check."
  },
  "G0267": {
    "id": "G0267",
    "verdict": "accurate",
    "note": "Test lines 1340-1358 assert response.role == 'assistant' and len(response.content) > 0. The production capability (CPU fallback generating real inference output for '2 + 2') is not gated — any non-empty assistant response passes. A stub returning Message(role='assistant', content='X') satisfies both assertions without real CPU inference occurring. The arithmetic correctness or any semantic property of the response is never verified."
  },
  "G0268": {
    "id": "G0268",
    "verdict": "accurate",
    "note": "Test lines 1450-1462 assert total > 0 (line 1461) and allocated >= 0 (line 1462). The docstring says 'VRAM allocation should be non-zero after model is loaded', but the assertion uses >= 0 not > 0. If get_xpu_memory_info() returns (0, total) when a model is loaded — which would happen if torch.xpu.memory_allocated is missing or broken — the test passes even though zero allocated bytes means VRAM tracking is broken. The claimed capability (non-zero VRAM after load) is not enforced by the assertion."
  },
  "G0269": {
    "id": "G0269",
    "verdict": "accurate",
    "note": "Test at line 1499 has no XPU skip guard inside the method body — the class carries @pytest.mark.xpu and @pytest.mark.b580 markers but test_total_vram_remains_stable uses no fixture like has_xpu_available to skip early. When XPU is unavailable, get_xpu_memory_info(0) returns (0, 0) (per xpu_utils.py line 574) on all three calls, so total1 == total2 == total3 resolves to 0 == 0 == 0 which is unconditionally True. The assertion does not validate that VRAM reporting actually occurred — it only validates consistency of a constant zero."
  },
  "G0270": {
    "id": "G0270",
    "verdict": "accurate",
    "note": "Test lines 1651-1666 assert only that four keys ('device_type', 'xpu_available', 'is_arc_b580', 'warnings') exist in the returned dict. The production get_device_info() (local_transformers.py line 1562) unconditionally populates exactly these keys from internal state variables. A stub returning {'device_type': None, 'xpu_available': None, 'is_arc_b580': None, 'warnings': None} satisfies all four 'in info' assertions. No value is checked — whether device_type is 'xpu', whether xpu_available is True, whether warnings is a list — none of this is verified."
  },
  "G0271": {
    "id": "G0271",
    "verdict": "accurate",
    "note": "Test lines 1776-1794 send a 10000-character prompt and assert only response.role == 'assistant' (line 1794). The production capability (handling long input without crashing and producing meaningful output) is only partially gated: a crash would cause a test failure, but a broken implementation that silently truncates to empty or returns any role='assistant' message with empty content would pass. There is no assertion on len(response.content) > 0 (unlike other tests) or any content property, only the role field."
  },
  "G0299": {
    "id": "G0299",
    "verdict": "accurate",
    "note": "test_file_change (line 74) constructs FileChange(path='C:\\\\test.txt', ...) and then asserts fc['path'] == 'C:\\\\test.txt'. Because TypedDicts have no runtime key validation — they are plain dicts at runtime — renaming the 'path' field to anything else in the FileChange TypedDict definition (base.py line 115) would not break this test at all; the keyword argument still populates a dict key 'path' and the assert reads it back. No production code that actually reads FileChange['path'] (e.g., analysis.py functions) is called. The test is a pure round-trip through Python's dict constructor. tautological/high is correct."
  },
  "G0300": {
    "id": "G0300",
    "verdict": "accurate",
    "note": "test_registry_change (line 89) constructs RegistryChange(key='HKLM\\\\SOFTWARE\\\\Test', operation='modified', ...) and asserts rc['key'] and rc['operation']. Identical pattern: TypedDict is a plain dict at runtime, so renaming 'key' or 'operation' in the production RegistryChange definition (base.py lines 125-130) would not break this test. Production code in analysis.py that reads rc['key'] to match behaviors is never called. Round-trip tautology; tautological/high is correct."
  },
  "G0301": {
    "id": "G0301",
    "verdict": "accurate",
    "note": "test_network_activity (line 105) constructs NetworkActivity(..., bytes_sent=100, bytes_received=200) and asserts na['protocol'] == 'tcp' and na['bytes_sent'] == 100. No production consumer of bytes_sent/bytes_received/protocol is invoked; analysis.py's detect_c2_patterns and generate_timeline are not called. If 'bytes_sent' were renamed in the TypedDict definition (base.py line 142), this test would still pass because the dict key 'bytes_sent' is set by the caller and read back immediately. tautological/high is correct."
  },
  "G0302": {
    "id": "G0302",
    "verdict": "accurate",
    "note": "test_api_call (line 134) constructs ApiCall(api_name='CreateFileW', arguments=['arg1','arg2'], ...) and asserts ac['api_name'] == 'CreateFileW' and len(ac['arguments']) == 2. The production function match_behaviors in analysis.py reads api_name to detect anti-debug patterns via _ANTI_DEBUG_APIS, but that function is never called here. Renaming 'api_name' in the TypedDict definition (base.py line 166) would not break this test. tautological/high is correct."
  },
  "G0303": {
    "id": "G0303",
    "verdict": "accurate",
    "note": "test_ioc_entry (line 226) constructs IOCEntry(ioc_type='ipv4', value='203.0.113.1', ...) and asserts ioc['ioc_type'] == 'ipv4' and ioc['value'] == '203.0.113.1'. IOCEntry is produced by extract_iocs in analysis.py; no production code that reads ioc['ioc_type'] or ioc['value'] is called in this test. TypedDicts have no runtime field validation, so renaming these fields in the production definition (base.py lines 251-256) would not break this test. tautological/high is correct."
  },
  "G0283": {
    "id": "G0283",
    "verdict": "accurate",
    "note": "The second branch (lines 418-423) sets cuda=False and xpu=_xpu_available_flag(provider) (the real flag from provider state), then asserts `selected in {'xpu', 'cpu'}`. Production `_select_device` at src/intellicrack/providers/local_transformers.py:343 requires `getattr(_torch, 'xpu', None) is not None` for XPU selection, which is absent in CI/sandbox. Even if torch.xpu were present and _xpu_available were True, `assert selected in {'xpu', 'cpu'}` would pass if the code returned 'cpu' erroneously (bug that always returns 'cpu' instead of 'xpu' when XPU should win stays green). The XPU-priority-over-CPU logic is not gated."
  },
  "G0284": {
    "id": "G0284",
    "verdict": "accurate",
    "note": "The only assertion is `isinstance(_probe_cuda(), bool)`. The production `_probe_cuda` (local_transformers.py:348) returns `bool(cuda_module.is_available())`. If a bug replaced this with `return False` unconditionally, or if the probe silently returned the wrong value on a CUDA-equipped machine, the test stays green because it never checks the returned bool against actual device state. A stub unconditionally returning `False` satisfies the isinstance check."
  },
  "G0285": {
    "id": "G0285",
    "verdict": "accurate",
    "note": "The test asserts `isinstance(count, int)`, `count >= 0`, and `count == 0` only when `_probe_cuda()` is False. On a CUDA machine, a stub returning 999 when `torch.cuda.device_count()` is 1 would pass because there is no assertion comparing the result to the real device count. The production code (local_transformers.py:369) calls `cuda_module.device_count()` but the test never validates the returned value against the actual hardware count."
  },
  "G0286": {
    "id": "G0286",
    "verdict": "accurate",
    "note": "All assertions are type/membership checks: `device_type in {'cuda','xpu','cpu'}`, `isinstance(cuda_available, bool)`, `isinstance(xpu_available, bool)`, `isinstance(warnings, list)`. The production `get_device_info` (local_transformers.py:1562) copies `self._cuda_available` and `self._xpu_available` directly. If `cuda_available` were always `False` on a CUDA machine (wrong value), the test stays green because it never verifies the flag matches the actual probe result. The XPU conditional at lines 441-443 is also only a type check (isinstance float) that never runs on non-XPU machines."
  },
  "G0287": {
    "id": "G0287",
    "verdict": "accurate",
    "note": "The only assertion is `isinstance(config, transformers.BitsAndBytesConfig)`. Production code (model_loader.py:821) returns `BitsAndBytesConfig()` for 'float16'. If a bug caused the int8 branch to be taken instead (returning `BitsAndBytesConfig(load_in_8bit=True)`), the isinstance check would still pass. The test does not verify that the returned config has no quantization flags set (i.e., it doesn't assert `load_in_8bit is False` or `load_in_4bit is False`), so any BitsAndBytesConfig instance regardless of its quantization settings satisfies the assertion."
  },
  "G0330": {
    "id": "G0330",
    "verdict": "accurate",
    "note": "The test at line 661 creates a fresh HxDPanel (process=None by construction at hxd_panel.py:150), then calls stop_tool() and asserts process is None. In _terminate_existing() (hxd_panel.py:375), the cleanup branch `if self.process is not None:` is skipped entirely because process was never set to a non-None value. The assertion `panel.process is None` trivially passes regardless of whether _terminate_existing() runs or not. To detect that stop_tool() correctly nullifies a real QProcess, the test would need to inject a non-None process first (as the sibling test at line 672 does). The finding is correctly categorized as tautological with medium severity."
  },
  "G0331": {
    "id": "G0331",
    "verdict": "accurate",
    "note": "The test at line 688 creates a fresh HxDPanel (embedded_container=None at hxd_panel.py:151) and calls stop_tool(). In _terminate_existing() (hxd_panel.py:389), the container cleanup branch `if self.embedded_container is not None:` is skipped entirely. The final assertion `panel.embedded_container is None` trivially passes because the container was never set to a non-None value. Deleting or corrupting the container cleanup code at line 398 (`self.embedded_container = None`) would not make this test fail. Correctly identified as tautological/medium."
  },
  "G0332": {
    "id": "G0332",
    "verdict": "accurate",
    "note": "The test at line 730 creates a fresh HxDPanel (process=None) and calls terminate_existing(), which delegates to _terminate_existing(). The `if self.process is not None:` branch at hxd_panel.py:375 is immediately skipped. `self.process = None` at line 387 is never reached. The assertion `panel.process is None` passes trivially. Removing line 387 entirely would not affect this test. Correctly identified as tautological/medium."
  },
  "G0333": {
    "id": "G0333",
    "verdict": "accurate",
    "note": "The test at line 741 creates a fresh HxDPanel (embedded_container=None) and calls terminate_existing(). The `if self.embedded_container is not None:` branch at hxd_panel.py:389 is skipped entirely. `self.embedded_container = None` at line 398 is never executed. The assertion `panel.embedded_container is None` passes trivially. Removing the container cleanup code would not make this test fail. Correctly identified as tautological/medium."
  },
  "G0334": {
    "id": "G0334",
    "verdict": "accurate",
    "note": "The test at line 748 creates a fresh HxDPanel (process=None) and calls cleanup(), which delegates to _terminate_existing(). Since process is None from construction, the entire cleanup block at hxd_panel.py:375-387 is skipped. The assertion `panel.process is None` trivially passes. The production line `self.process = None` (line 387) is never reached. This test cannot detect removal or corruption of the process cleanup path in _terminate_existing(). Correctly identified as tautological/medium."
  },
  "G0335": {
    "id": "G0335",
    "verdict": "accurate",
    "note": "The test at line 755 calls terminate_existing() twice on a fresh panel where process=None and embedded_container=None. Both calls skip all conditional branches in _terminate_existing() (lines 375 and 389 never enter their blocks). Both assertions trivially pass. The test validates that the no-op path does not raise or corrupt state, but cannot detect bugs in the actual teardown of non-None process or container objects. Severity low is appropriate since it at least covers crash-safety of the empty path."
  },
  "G0336": {
    "id": "G0336",
    "verdict": "accurate",
    "note": "The test at line 764 calls stop_tool() then cleanup() on a fresh panel where both process and embedded_container are None. stop_tool() calls _terminate_existing() (no-op on None state), then cleanup() calls it again (still no-op). Both assertions trivially pass because neither method can make process or embedded_container non-None when they started as None and no injection occurred. Removing the entire cleanup logic from _terminate_existing() would not affect this test. Severity low is appropriate."
  },
  "G0253": {
    "id": "G0253",
    "verdict": "accurate",
    "note": "The test calls list_configured_providers() and asserts isinstance(configured, list) and isinstance(provider, ProviderName) per element. In CI with no credentials, list_configured_providers() (env_loader.py lines 512-528) iterates ProviderName and appends only those that pass validate_credentials(). With no credentials, it returns []. The for-loop body (line 304-305 in test) runs zero times, making both type assertions vacuously satisfied. A broken implementation returning [] regardless of credential state, or returning all providers unconditionally, would pass in CI. The test provides no semantic gate on the configured vs. missing distinction when credentials are absent."
  },
  "G0254": {
    "id": "G0254",
    "verdict": "reclassify",
    "corrected_category": "weak-existence",
    "corrected_severity": "low",
    "note": "In CI with no credentials, list_missing_providers() (env_loader.py lines 530-546) returns ALL ProviderName values because no credentials are present. The for-loop on line 318-319 of the test actually executes over all providers, checking isinstance(provider, ProviderName). This means the loop body IS exercised and would catch if the function returned non-ProviderName items. However, the test has no semantic gate: if list_missing_providers() returned [] (all 'configured'), the for-loop would be vacuous and the test passes. The test cannot distinguish correct from incorrect behavior when all providers could legitimately be missing. The finding is accurate as weak-existence but the severity should be low rather than medium because the return type is still partially gated in the expected CI state (all-missing, all loop iterations run)."
  },
  "G0255": {
    "id": "G0255",
    "verdict": "accurate",
    "note": "In CI with no credentials, list_configured_providers() returns [] both before and after reload(). The assertion `before == after` becomes `set([]) == set([])` which is trivially True. The reload() method (env_loader.py lines 377-385) clears _env_vars and calls _load_env_file(). A no-op implementation, a buggy one that fails to restore state, or one that corrupts internal state but still returns [] would all produce the same empty set comparison. The test provides no gate on reload correctness when credentials are absent."
  },
  "G0256": {
    "id": "G0256",
    "verdict": "accurate",
    "note": "The batch claim says 'confirmed: false' (refuted), but the independent analysis shows the finding is correct. Looking at _find_env_file() (env_loader.py lines 223-248): it always returns a Path object, either a found existing .env or the default project_root/.env. The __init__ sets self.env_path to either the provided env_path argument or the result of _find_env_file(). Both paths are typed Path (non-optional). Therefore the assertion `assert loader.env_path is not None` (line 148 in test) is tautological - it can never fail under the current production contract. The 'realistic breakage' argument in the claim (if _find_env_file changed to return None) requires changing the type contract, not a natural code regression. This is weak-existence/tautological, and the finding is accurate. The claimed refutation is incorrect."
  },
  "G0257": {
    "id": "G0257",
    "verdict": "accurate",
    "note": "The test passes env_file_path (fixture = project_root / '.env', an existing file) to CredentialLoader(env_path=env_file_path). The constructor stores it as self.env_path (env_loader.py line 333). The assertion `loader.env_path.exists()` (line 160) checks that the stored path exists. Since the constructor stores the provided path unchanged, the assertion only passes because the fixture-supplied path already exists on disk - not because the CredentialLoader did anything with it. If production code changed to silently point to a different nonexistent path internally, the test would still pass (env_path is still stored as-is). There is no oracle on whether _load_env_file() actually parsed the file. The claim is accurate."
  },
  "G0258": {
    "id": "G0258",
    "verdict": "accurate",
    "note": "The test calls list_models() twice and asserts `ids1 == ids2` (line 260). If list_models() returns [] consistently (e.g., due to a complete breakage), then set() == set() is True and the test passes. The test has no guard asserting len(models1) > 0 or len(ids1) > 0. The sibling test test_list_models_returns_non_empty_list in a different test function checks non-emptiness, but that assertion does not apply here. A deterministically broken list_models() returning [] would pass the consistency check. The no-assertion/weak-existence category applies."
  },
  "G0259": {
    "id": "G0259",
    "verdict": "accurate",
    "note": "The test's usage assertion block (lines 145-148) is guarded by `if usage is not None:`. Looking at chat_stream() in huggingface.py: self._pending_usage is set to None at line 736 before streaming begins, then only set in _consume_stream_chunks() (lines 683-689) when `getattr(chunk, 'usage', None) is not None`. The HuggingFace streaming API may not provide usage in every chunk response. If the usage extraction logic is broken or removed, get_pending_usage() returns None, and the conditional guard silently skips all usage assertions. The test passes green with zero enforcement that the streaming path captures usage metadata. Contrast with the non-streaming test (lines 82-86) which uses unconditional assertions. The finding is accurate as conditional-never-runs/swallows-failure."
  },
  "G0243": {
    "id": "G0243",
    "verdict": "accurate",
    "note": "The test writes at line 156 (asserts is_modified=True), calls undo() at line 159 with NO follow-up assertion, then calls redo() at line 161 and asserts is_modified=True at line 162. In the Rust UndoManager (src/intellicrack-hexcore/src/undo.rs line 154-159), is_modified() returns undo_stack.len() != saved_index. After write, undo_stack.len()=1, saved_index=0 => modified=True. After a correct undo(), undo_stack.len()=0 => modified=False. But if undo() were broken (e.g., didn't pop from undo_stack), undo_stack.len() remains 1, redo_stack stays empty, and the redo() call at line 161 would return false but undo_stack still has len=1. The final assertion at line 162 would still pass (modified=True). The missing intermediate assertion on is_modified()==False after undo() means a broken undo() that fails to move the operation out of undo_stack is not caught."
  },
  "G0249": {
    "id": "G0249",
    "verdict": "accurate",
    "note": "The test (lines 634-648) calls anthropic_provider.chat() twice with enable_cache=True and asserts response1.role == 'assistant' and response2.role == 'assistant'. The AnthropicProvider._apply_cache_breakpoints() (src/intellicrack/providers/anthropic.py lines 295-375) inserts cache_control blocks into the request payload. If this method were deleted or never called, the API would still return a valid response with role='assistant' because caching is advisory - the request succeeds without cache_control headers. The test validates only that the call succeeds, not that cache breakpoints were emitted or that usage metadata shows cache hits."
  },
  "G0250": {
    "id": "G0250",
    "verdict": "accurate",
    "note": "The test (lines 523-538) checks trimmed[0].role == 'system' and len(trimmed) < len(messages). The production implementation (src/intellicrack/core/orchestrator.py lines 1605-1613) uses oldest_idx = next((i for i, m in enumerate(messages) if m.role != 'system'), -1) which correctly removes the oldest non-system message first. If this were changed to remove newest-first, the loop would still remove non-system messages (preserving system at index 0) and shrink the list. Both assertions would still pass: trimmed[0] would remain the system message, and len(trimmed) would still be < 4. The 'oldest-first' ordering invariant named in the test is not verified by the assertions."
  },
  "G0251": {
    "id": "G0251",
    "verdict": "reclassify",
    "corrected_category": "weak-existence",
    "corrected_severity": "low",
    "note": "The test does have an assertion: response.role == 'assistant' (line 615). The 'no-assertion' category is wrong. The test is better classified as 'weak-existence' - it verifies the call succeeds and returns an assistant role, which gates the basic 'does not error' behavior (per the docstring: 'Non-Anthropic providers should not error when thinking param passed'). However, it does not verify that thinking_content is None on the returned Message, nor that content is non-empty. The OpenAI provider's _reasoning_effort_for() (lines 449-454) logs and returns None for gpt-4o-mini, so thinking is silently dropped. A bug that accidentally set thinking_content on the response would pass this test. Severity should be low: the narrow 'no error' gate is real and meaningful, and the thinking_content=None gap is the only unverified aspect."
  },
  "G0252": {
    "id": "G0252",
    "verdict": "accurate",
    "note": "The test (lines 502-515) asserts len(models) > 0 and False in {m.supports_tools for m in models}. The production _build_model_info() (lines 287-337 in openrouter.py) derives supports_tools from supported_parameters or a family-name heuristic. The test verifies that SOME model exists with supports_tools=False, but does not validate accuracy - an implementation that hardcodes supports_tools=False for every model would trivially pass both assertions (False is in the set, len > 0). The test title claims 'accurate tool support' but only verifies existence of a False value. No independent oracle or ground-truth comparison is made."
  },
  "G0216": {
    "id": "G0216",
    "verdict": "accurate",
    "note": "The test at lines 1148-1167 runs 50 threads calling state.set_cursor(i). Each thread only appends to errors if a RuntimeError is caught. The single assertion is 'assert not errors'. A data race that silently corrupts _cursor_offset (e.g., a lost write from a torn update) would not raise RuntimeError and the test would pass green. No assertion checks the final cursor_offset value or that all 50 set_cursor calls produced consistent state. The no-assertion category and medium severity are correct."
  },
  "G0217": {
    "id": "G0217",
    "verdict": "accurate",
    "note": "The test at lines 1169-1199 spawns two threads: one spamming state.set_cursor(i) 100 times, one calling state.unregister_callback(cb). Both only append to errors on RuntimeError. The single assertion is 'assert not errors'. If the _dispatch_one snapshot mechanism were removed and a callback were called after unregistration, this would not raise RuntimeError (it would be a silent logic error or AttributeError at most). The test does not assert that callbacks are not invoked after unregistration or that the callback list is in a consistent state. The no-assertion category and medium severity are correct."
  },
  "G0218": {
    "id": "G0218",
    "verdict": "refute",
    "note": "This is a parametrized test covering all 12 HexDocumentEvent values. Lines 1619-1620 assert 'len(events) >= 1' and 'events[-1][0] == event_type'. If event delivery for any event type is broken (e.g., the trigger function fires no event at all), events would be empty and 'events[-1]' would raise IndexError, causing the test to FAIL. If the wrong event type is delivered last, 'events[-1][0] == event_type' fails. This is a real gate: breaking any of the 12 event delivery paths would cause the corresponding parametrized case to fail. The 'len >= 1' bound is reasonable because some operations (like DOCUMENT_OPENED) legitimately fire multiple events and the final one must be of the correct type. The finding was already marked confirmed=false by the prior reviewer, and independent analysis confirms the test IS a genuine gate."
  },
  "G0219": {
    "id": "G0219",
    "verdict": "accurate",
    "note": "conftest.py line 36-39 executes 'hexcore_mod = pytest.importorskip(\"intellicrack_hexcore\", ...)' at module level. When intellicrack_hexcore is absent, pytest raises a Skipped exception during conftest.py collection, which prevents conftest.py from loading. Since pytest loads conftest.py before collecting any tests in its directory, ALL tests in test_hexcore_e2e/ are silently skipped. test_hexpat_compiler_e2e.py's own importorskip at line 13-16 (for the pure Python 'intellicrack.core.hexpat_compiler') never runs because the conftest skip blocks directory collection first. Any regression in HexPatCompiler, HexPatLexer, or TokenType would produce skip rather than failure in environments without the Rust extension, masking production defects. The skip-masks-failure category and high severity are correct."
  },
  "G0220": {
    "id": "G0220",
    "verdict": "accurate",
    "note": "test_compile_simple_struct_has_name_key (lines 120-124) only asserts 'parsed[\"name\"] == \"Header\"'. The sibling test test_compile_simple_struct_returns_json_string (lines 111-118) asserts the SAME thing ('parsed[\"name\"] == \"Header\"') plus field names. Every breakage that G0220 would catch is also caught by the sibling test. G0220 is fully subsumed. The weak-existence category and low severity are correct. (Note: G0219's skip-masks-failure already covers all tests in this file, making this a secondary concern, but the weak-existence defect still stands on its own merits.)"
  },
  "G0221": {
    "id": "G0221",
    "verdict": "accurate",
    "note": "Lines 313-316: pytest.raises(HexPatError) followed by 'assert exc_info.value.message' (line 315, truthiness) then 'assert len(exc_info.value.message) > 0' (line 316, length check). For any string s: if s is truthy (line 315 passes), then len(s) > 0 is necessarily true (line 316 cannot independently fail). If message is empty, line 315 fails before line 316 is reached. Line 316 adds zero additional gate value beyond line 315. The tautological category (for line 316) and low severity are correct. The overall test still gates that HexPatError is raised and message is non-empty via line 315 alone."
  },
  "G0222": {
    "id": "G0222",
    "verdict": "accurate",
    "note": "test_hexpat_complex_patterns.py (line 1) imports only the pure Python HexPatInterpreter and defines its own 'interp' fixture locally. However, conftest.py in the same directory has module-level pytest.importorskip(\"intellicrack_hexcore\") (line 36-39). When the Rust extension is absent, pytest cannot load conftest.py and silently skips all test collection in the directory, including this file. The tests never run, so any regression in HexPatInterpreter's pattern execution, pointer dereferencing, or field extraction logic would produce skip rather than failure. The skip-masks-failure category and high severity are correct."
  },
  "G0316": {
    "id": "G0316",
    "verdict": "accurate",
    "note": "The test at line 308 only asserts `mgr is not None`. `ensure_manager()` creates a `SandboxManager()` instance and returns it. If the production code were changed to return an `object()` instance or a differently-typed wrong object, the assertion still passes since any object is not None. The test does not verify the returned object is actually a `SandboxManager` with functioning methods. The weak-existence classification and medium severity are appropriate."
  },
  "G0317": {
    "id": "G0317",
    "verdict": "accurate",
    "note": "The test at line 1049 only asserts `len(diff_section) > 0`. The production `diff_reports()` at analysis.py:1294 always initializes `result` with at least the `'scalars'` key before any loop iteration, so `len >= 1` is always true. If the entire `field_key_map` loop (lines 1319-1344) were deleted, `diff_reports` would still return `{'scalars': {...}}` with len=1, passing the test despite losing all 11 per-field comparison entries. The assertion cannot detect removal of per-field diff logic."
  },
  "G0318": {
    "id": "G0318",
    "verdict": "accurate",
    "note": "The test at line 79 asserts `bridge is not None` immediately after `bridge = SandboxBridge()`. In Python, a constructor call always returns an instance of the class (or raises an exception). It is impossible for a constructor to return `None`. Therefore, this assertion is logically tautological — it can never be False unless an exception is raised first, and if an exception is raised the test fails before reaching the assertion. No realistic production regression in `__init__` logic can cause the assertion itself to trigger a failure."
  },
  "G0319": {
    "id": "G0319",
    "verdict": "accurate",
    "note": "Lines 87-88 assert `str(alpha['module'])` and `str(alpha['function'])`, which only check for truthiness of the stringified value (non-empty string). The presence of the fields is already gated at lines 83-84 by the `_REQUIRED_FIELDS` loop. If the `_add_call_info()` processor were changed to return fixed placeholder values like `'unknown'` or `'<module>'`, `str('unknown')` is truthy, so lines 87-88 would still pass. The test does not verify that the module/function values reflect actual caller frame introspection, only that they are non-empty strings."
  },
  "G0320": {
    "id": "G0320",
    "verdict": "accurate",
    "note": "Line 215 `assert window.proxy.rowCount() >= 1` is redundant given the surrounding assertions. Line 213 asserts `'real_error_only' in vis_events` (which requires at least one visible row with that event), line 214 asserts `'real_info_only' not in vis_events`, and line 216 asserts `vis_levels == {'ERROR'}` (requires at least one visible ERROR-level row). If the filter were completely broken and returned zero rows, line 213 would fail before reaching line 215. Conversely, if lines 213-216 all pass, rowCount >= 1 is guaranteed. Line 215 adds no independent failure detection."
  },
  "G0321": {
    "id": "G0321",
    "verdict": "refute",
    "note": "The finding was already marked `confirmed: false` in the batch, and the test IS a real gate. The helper `_find_toolbar` at line 77-78 calls `window.findChildren(OverflowToolBar)` and asserts `toolbars` is truthy. If `MainWindow._setup_toolbar` were changed to use `QToolBar(...)` instead of `OverflowToolBar(...)`, `findChildren(OverflowToolBar)` would return an empty list, causing the `assert toolbars` at line 78 to fail before even reaching line 89. The test correctly catches the regression that matters: MainWindow no longer uses OverflowToolBar. The isinstance check at line 89 is redundant after line 78 succeeds, but the test as a whole is a real gate."
  },
  "G0322": {
    "id": "G0322",
    "verdict": "accurate",
    "note": "The test at lines 273-276 calls `shutdown_bridge_loop()` twice with zero assertions afterward. The production function at async_bridge.py:501-516 sets `_state.loop = None` and `_state.thread = None` at the end. If these assignments were removed, the test would still pass silently — it only verifies no exception is raised on double-call, not that loop state is actually cleaned up. A regression where the second call operates on a stale stopped loop (rather than returning early at the `if _state.loop is None` guard) would not be detected by this test."
  },
  "G0351": {
    "id": "G0351",
    "verdict": "accurate",
    "note": "base_panel.py line 301-309 shows start_tool() has a hardcoded `return True` with the comment 'True always since native panels are always ready'. SandboxPanel has no override of start_tool() (only _cleanup is overridden). The assertion `panel.start_tool() is True` cannot fail regardless of any SandboxPanel-specific regression in signal emission, state setup, or lifecycle behavior - the return value is a constant guaranteed by the base class contract."
  },
  "G0352": {
    "id": "G0352",
    "verdict": "accurate",
    "note": "base_panel.py line 311-320 shows stop_tool() calls self._cleanup() then returns True. SandboxPanel does override _cleanup() (line 329), but the test creates a fresh SandboxPanel() with no bridge (_bridge=None) and no sandbox_id (None). In that state, _cleanup() only calls _disconnect_vnc_display() and stops the poll timer - both are trivially safe operations that cannot raise. The `return True` is always reached in this no-bridge scenario. A real sandbox teardown regression (broken PCAP stop, failed destroy) would require a bridge to be configured, which this test deliberately omits. The test verifies only the trivial no-op case and is not a gate for actual sandbox lifecycle behavior."
  },
  "G0272": {
    "id": "G0272",
    "verdict": "accurate",
    "note": "Lines 250-254 confirm the claim exactly. configured_count is computed at line 250. At line 251-252, pytest.skip() is called whenever configured_count == 0. The assert at line 254 checks the same condition (configured_count > 0) that was just used as the skip guard, so execution reaches line 254 only when configured_count > 0 is already guaranteed. The assertion is logically unreachable in a failing state and contributes zero gate strength."
  },
  "G0273": {
    "id": "G0273",
    "verdict": "accurate",
    "note": "Line 71 asserts only isinstance(models, list). The production code in OllamaProvider.list_models() (ollama.py:436) initializes models as [] and can return an empty list when no local or cloud models are available. An empty list satisfies isinstance(..., list) per line 71 and the docstring explicitly states 'may be empty if no models installed'. A realistic regression (e.g., _fetch_local_models returning [] due to API/parsing change) would pass this assertion. The ollama_provider fixture at conftest.py:283 skips if Ollama is not running, so the test is integration-only, but even when running the weak-existence claim stands."
  },
  "G0274": {
    "id": "G0274",
    "verdict": "accurate",
    "note": "Lines 311-325 confirm the claim. The test creates a provider with an invalid key, calls connect() in a try block, catches AuthenticationError in a bare pass, and makes no assertions about state. The production connect() (openrouter.py:163) raises AuthenticationError for empty keys but for a non-empty invalid key ('sk-or-invalid-key-12345') it may succeed if the /models endpoint accepts it or raises HTTPStatusError which maps to AuthenticationError. Either way both branches of the try/except complete without any assert, so no falsifiable outcome exists. The test neither asserts connected==False, connected==True, nor any specific behavior."
  },
  "G0275": {
    "id": "G0275",
    "verdict": "accurate",
    "note": "Line 154 checks only isinstance(config, OAuthConfig). An OAuthConfig with blank authorization_url='', token_url='', scopes=() would still satisfy isinstance(config, OAuthConfig) and would pass this test. The test does not validate any fields on the config object. The companion test at line 157 (test_build_authorization_url_emits_spec_compliant_pkce_request) does verify actual URL content, so this test is redundant and does not gate meaningful OAuth config content."
  },
  "G0276": {
    "id": "G0276",
    "verdict": "accurate",
    "note": "Lines 432-439 show the test asserts only provider.name == ProviderName.LOCAL_TRANSFORMERS. The F-0004 fix is the logger binding self._logger = get_logger(__name__).bind(provider='local_transformers') in local_transformers.py. Removing that line would cause structured logs to lose the 'provider' context field, which is the actual regression F-0004 guards against. This test would still pass because LocalTransformersProvider.name is a simple property returning ProviderName.LOCAL_TRANSFORMERS (line 206-212 of local_transformers.py) and is unaffected by logger binding. The companion test at line 407 (test_f0004_init_logger_binds_provider_field) directly inspects the logger _context dict and is the real gate."
  },
  "G0277": {
    "id": "G0277",
    "verdict": "accurate",
    "note": "Lines 72-76 use hasattr(module, name) for all three assertions. hasattr returns True even when the attribute is set to None, 0, or any sentinel. The finding is correct that replacing DiscoveryEvent = None in discovery.py or DtypeOption = None in model_loader.py would pass the test but break production (DtypeOption is a Literal type alias at model_loader.py:57; DiscoveryEvent is a dataclass at discovery.py:32). The test gates only complete deletion from the module namespace, not correctness or usability of the exported symbols."
  },
  "G0278": {
    "id": "G0278",
    "verdict": "accurate",
    "note": "Lines 236-237 assert usage.prompt_tokens >= 0 and usage.total_tokens >= usage.prompt_tokens. These are trivially satisfied: _build_usage_from_message (anthropic.py:392-397) computes prompt_tokens = int(getattr(usage, 'input_tokens', 0) or 0) and total_tokens = input_tokens + output_tokens, so both are always >= 0. The test never checks cache_read_input_tokens (which the Anthropic API returns in the usage object for real cache hits). If _apply_cache_breakpoints were removed entirely, the second request would receive no cache_control markers, would return zero cache_read_input_tokens, but both assertions at lines 236-237 would still pass identically. The test does not gate whether caching actually occurred."
  },
  "G0310": {
    "id": "G0310",
    "verdict": "accurate",
    "note": "The sandbox_bridge fixture (conftest.py line 1473) attaches a StubManager whose get_status() (conftest.py lines 915-937) returns hardcoded data including `total_count: len(self._instances)` = 2. SandboxBridge.status() (sandbox_bridge.py line 1376-1384) simply calls `manager.get_status()` and the test asserts `result['total_count'] == 2`. If the real SandboxManager.get_status() changed its return schema or was removed, the test would still pass because StubManager provides all data. The real production SandboxManager.get_status() is never invoked. Category mock-shadows-target is correct."
  },
  "G0311": {
    "id": "G0311",
    "verdict": "reclassify",
    "note": "The defect is real (mock-shadows-target) but the claim's mechanism is wrong. The claim states 'SandboxBridge.list() delegates to manager.get_status()[\"instances\"]' but the actual production code at sandbox_bridge.py lines 1386-1405 iterates `manager.instances` directly (a property), NOT get_status(). StubManager.instances (conftest.py line 792) returns hardcoded stub instances from `_instances` dict. The assertions that returned ids equal {_WIN_INSTANCE, _QEMU_INSTANCE} only validate the stub's fixed data. A regression in the real SandboxManager's instances property would not be caught. Category mock-shadows-target is still correct; severity medium is correct; line 65 is correct.",
    "corrected_category": "mock-shadows-target",
    "corrected_severity": "medium",
    "corrected_line": 65
  },
  "G0312": {
    "id": "G0312",
    "verdict": "accurate",
    "note": "The sandbox_bridge fixture is backed by StubManager. When bridge.create(sandbox_type='qemu') is called, it invokes StubManager.create() (conftest.py lines 809-841) which creates an InMemoryQEMUSandbox and immediately starts it. StubInstance.id is a UUID and stub.state.status is 'running'. The assertions `result['type'] == 'qemu'` and `result['status'] == 'running'` check what StubManager returned. The real SandboxManager.create() path - which calls QEMUSandbox.__init__, checks actual QEMU availability, and performs real setup - is never exercised. A regression in the real create() would not be detected. Category mock-shadows-target is correct."
  },
  "G0313": {
    "id": "G0313",
    "verdict": "refute",
    "note": "This test exercises real bridge logic, not just a stub. SandboxBridge.destroy() (sandbox_bridge.py lines 1096-1129) calls manager.destroy(instance_id) and on success returns {\"success\": True}. SandboxBridge.list() (lines 1386-1405) iterates manager.instances. Both the StubManager.destroy() (conftest.py lines 854-867) and StubManager.instances (line 792) track the same _instances dict with real state mutations - destroy() calls _instances.pop() (line 866). The test validates the real BRIDGE CONTRACT: destroy() routes to manager, manager.destroy() removes from its dict, and the subsequent list() call correctly reflects the removal. A bug in the bridge's destroy-routing, error propagation, or list()-reading logic would be caught. The bridge's state mutation chain is genuine code under test, not a stub mock, even though the manager implementation is a stub. The finding should be refuted because the integration between bridge.destroy() -> StubManager.destroy() -> _instances.pop() -> bridge.list() -> manager.instances is a real gate on the bridge wiring."
  },
  "G0314": {
    "id": "G0314",
    "verdict": "accurate",
    "note": "At test line 414, `if report.process_activity:` guards the assertion `assert process_only, 'process category timeline must be non-empty when real process activity exists'`. This is the strongest assertion - that the process-category filter correctly returns events when process data exists. If the real PowerShell process monitor runs but the parser yields an empty list (e.g., due to a schema mismatch in parse_process_log), then report.process_activity would be [] (falsy), the entire if-block is skipped, and the test passes even though the capture-parse-filter pipeline is broken. The earlier assertion at line 410 only checks that combined timeline categories include 'process' or 'network', not that the process_activity was actually parsed. Category conditional-never-runs is correct; severity medium is correct."
  },
  "G0315": {
    "id": "G0315",
    "verdict": "refute",
    "note": "The test runs two real captures (lines 566-567: _capture_real_report for both tmp_path/a and tmp_path/b), calls the real diff_reports() function (line 569), and at line 579 asserts `assert proc_diff['common'], 'two captures of the same host must share long-lived processes in common'`. This assertion will fail if diff_reports() breaks partitioning and places all processes into unique_to_a or unique_to_b instead of common. Line 576 asserts `set(proc_diff.keys()) == {'unique_to_a', 'unique_to_b', 'common'}` which catches schema changes. Line 571 asserts the scalar diff structure. These assertions together make this test a genuine production gate - if diff_reports() misimplements the set-partitioning logic (e.g. always assigns to unique_to_a), the assertion at line 579 fails because long-lived system processes would not appear in common. The original auditor's confirmed=false assessment was correct."
  },
  "G0323": {
    "id": "G0323",
    "verdict": "accurate",
    "note": "test_get_font_info_returns_dict only asserts isinstance(info, dict). The production get_font_info() always returns a dict literal, so returning {} or a dict with wrong keys/values still passes this test. The real content gate is test_font_info_contains_required_keys which explicitly checks each required key after load_fonts()."
  },
  "G0324": {
    "id": "G0324",
    "verdict": "accurate",
    "note": "test_font_info_values_correct_types checks isinstance for bool/list/str types only. After load_fonts(), both fonts_loaded=False and code_font='' are valid bool and str instances respectively, so the test passes even with semantically broken values. Types alone do not verify meaningful population of the font info dict."
  },
  "G0325": {
    "id": "G0325",
    "verdict": "accurate",
    "note": "test_fallback_code_fonts_not_empty asserts only len(FALLBACK_CODE_FONTS) > 0. Since FALLBACK_CODE_FONTS is a module-level Final constant with 8 entries, this would pass even if the list were reduced to a single nonsense entry like ['garbage']. The real gate is test_fallback_code_fonts_contains_common_fonts which verifies specific fonts like 'Consolas', 'Courier New', 'monospace' are present."
  },
  "G0326": {
    "id": "G0326",
    "verdict": "accurate",
    "note": "test_fallback_ui_fonts_not_empty is identical in structure to G0325 but for FALLBACK_UI_FONTS. It asserts only len(FALLBACK_UI_FONTS) > 0, which passes as long as any single entry exists, regardless of whether it is a valid font name. The sibling test test_fallback_ui_fonts_contains_common_fonts provides specific-content gating."
  },
  "G0327": {
    "id": "G0327",
    "verdict": "accurate",
    "note": "test_path_is_set asserts not path.isEmpty(). In Qt, QPainterPath.isEmpty() returns False after any element is added, including just moveTo(). The EdgeItem constructor calls path.moveTo(start) before path.cubicTo(...), so even if the cubicTo call were removed the path would still be non-empty and the test would pass. The test does not verify that the Bezier curve actually connects start to end."
  },
  "G0328": {
    "id": "G0328",
    "verdict": "refute",
    "note": "The test asserts LAYOUT_BLOCK_ADDR_A in scene.block_items, which FAILS if scene.load_graph() raises an exception. If the graceful handling (block.get('ops', [])) at graph_view.py line 345 were replaced with block['ops'], a KeyError would propagate through load_graph(), leaving scene.block_items empty, and the assertion would fail. The test genuinely gates against crashes caused by a missing 'ops' key."
  },
  "G0329": {
    "id": "G0329",
    "verdict": "refute",
    "note": "The test asserts LAYOUT_BLOCK_ADDR_A in scene.block_items, which FAILS if scene.load_graph() raises. If the isinstance coercion (graph_view.py lines 346-347) were removed, ops='invalid' (a string) would be passed to BasicBlockItem. At line 148 of graph_view.py, 'if ops' is truthy for a non-empty string, so the generator max(len(op.get('disasm', '')) for op in ops) would iterate over characters of 'invalid' and call .get() on each char, raising AttributeError. This crash propagates up from load_graph(), leaving scene.block_items empty and failing the assertion."
  },
  "G0361": {
    "id": "G0361",
    "verdict": "accurate",
    "note": "The test at lines 82-93 creates a fresh ToolOutputPanel and checks that splitter_sizes is a list of exactly 2 ints. Production code at line 2586 returns `self.main_splitter.sizes()` which yields the QSplitter's real layout (initialized to [600, 200] at line 864). The assertions only verify structure (isinstance list, len 2, both ints) but not actual values. A production change returning hardcoded [0, 0] or [1, 1] would still pass. The finding correctly identifies this as weak-existence: it gates the structural shape of the saved state, but not the actual correspondence to the real splitter positions."
  },
  "G0362": {
    "id": "G0362",
    "verdict": "refute",
    "note": "The or-escape `or default_sizes == [400, 400]` in line 139 is inert with current production defaults. Production code initializes the splitter with `setSizes([_DEFAULT_SPLIT_LEFT, _DEFAULT_SPLIT_RIGHT])` at tools.py line 864, where `_DEFAULT_SPLIT_LEFT = 600` and `_DEFAULT_SPLIT_RIGHT = 200` (lines 376-377). So `default_sizes` is `[600, 200]`, never `[400, 400]`. The escape clause cannot fire under realistic conditions. If `setSizes([400, 400])` in `restore_tab_state` (line 2624) were broken (e.g., the method removed or a guard skipped), `sizes` would remain `[600, 200]` == `default_sizes`, making `sizes != default_sizes` False, and `default_sizes == [400, 400]` also False, so the assertion would FAIL. The test is a real gate against breakage of the splitter-restore path. The or-clause is a theoretical fragility if defaults were changed to [400,400], but that requires changing production code simultaneously with the test — not a realistic single-component breakage scenario."
  },
  "G0363": {
    "id": "G0363",
    "verdict": "accurate",
    "note": "Test at lines 147-154 only checks `isinstance(result, bool)`. Production `apply_theme` always returns `True` when a QApplication exists (line 1329) or `False` otherwise (line 1332). Any stub returning a bool would satisfy this. The sibling tests (lines 158, 180, 194) are the real gates. This narrow test adds no capability gate beyond 'the return type is boolean', which is a weak-existence defect as labelled."
  },
  "G0364": {
    "id": "G0364",
    "verdict": "accurate",
    "note": "Test at lines 284-287 calls `ThemeManager.get_available_themes()` and asserts `isinstance(themes, list)`. Production code at line 1557-1564 returns `[THEME_DARK, THEME_LIGHT, THEME_SYSTEM]`. A stub returning `[]` or `['x']` would satisfy the isinstance check. The sibling tests (lines 290-305) check for specific members and are the real gates. This test adds only a type-presence assertion with no capability gate, confirming the weak-existence finding."
  },
  "G0365": {
    "id": "G0365",
    "verdict": "accurate",
    "note": "Test at lines 562-569 calls `ThemeManager.reset_instance()` then `ThemeManager.get_instance()` inside a try/except, then asserts `manager is not None`. Production `get_instance()` at line 1187-1195 either returns the existing singleton or creates one via `cls()`. Since `ThemeManager.__init__` does lightweight work (sets attributes, calls `_check_styles_available()`), it practically never raises outside of catastrophic import failures. The assertion `manager is not None` is trivially satisfied once the try block succeeds because `get_instance()` always returns `self` (the newly created instance). Initialization state bugs (wrong `_current_theme`, `styles_available=False` due to missing directory, etc.) would not be caught. This is correctly categorised as a no-assertion / smoke-test only gate."
  },
  "G0223": {
    "id": "G0223",
    "verdict": "accurate",
    "note": "Confirmed. conftest.py lines 36-39 execute `pytest.importorskip('intellicrack_hexcore')` at module level. When the Rust extension is absent, this raises `Skipped` during conftest import, which causes pytest to skip every test in `tests/test_hexcore_e2e/`. The test file `test_hexpat_control_flow.py` imports only `HexPatInterpreter` (pure Python, no hexcore dependency), yet all its tests are masked by the conftest skip. Regressions in the pure-Python while/for/match/try/break/continue logic in `interpreter.py` would never surface as failures in environments without the Rust build."
  },
  "G0224": {
    "id": "G0224",
    "verdict": "accurate",
    "note": "Confirmed. Same conftest.py module-level `importorskip` issue as G0223. `test_hexpat_data_reader.py` imports only `DataReader` and `HexPatRuntimeError` from pure-Python modules (`data_reader.py`, `errors.py`), neither of which depends on `intellicrack_hexcore`. Despite this, the directory-level conftest skip suppresses every test when hexcore is absent. Endianness bugs, OOB handling, and type-coercion regressions in the pure-Python DataReader would be invisible."
  },
  "G0225": {
    "id": "G0225",
    "verdict": "accurate",
    "note": "Confirmed. Same conftest.py module-level `importorskip` as G0223/G0224. `test_hexpat_evaluator.py` imports only `HexPatRuntimeError` and `HexPatInterpreter` (both pure Python). The file explicitly states it drives the full interpreter pipeline (preprocessor → lexer → parser → evaluator) using `execute_bytes`, which has no hexcore dependency. All arithmetic, bitwise, comparison, logical, ternary, and scoping tests are masked by the directory skip when the Rust extension is absent."
  },
  "G0226": {
    "id": "G0226",
    "verdict": "accurate",
    "note": "Confirmed. The test (line 204-210) includes `once.hexpat` twice and asserts `result.count('once_field') == 1`. However, `preprocessor.py` line 390 adds `resolved_str` to `self._included_files` on the first successful resolution. The guard at line 387 checks `resolved_str in self._pragma_once_files or resolved_str in self._included_files` — so on the second include, the `_included_files` branch alone would return `None` even if lines 395-396 (the `#pragma once` detection and `_pragma_once_files.add`) were deleted entirely. The test passes via normal include-deduplication, not via the pragma_once mechanism. No breakage of the pragma_once code path causes the test to fail."
  },
  "G0227": {
    "id": "G0227",
    "verdict": "reclassify",
    "corrected_severity": "low",
    "note": "The weak-existence finding is correct: `assert not getattr(builtin, '_string_at')('hi', 10)` accepts any falsy value (None, 0, False) and does not enforce that the return type is `str`. If the OOB branch were changed to return `None` instead of `''`, the assertion still passes. However, severity should be 'low' not 'high'. A sibling test at line 319 (`assert getattr(builtin, '_string_at')('hello', 1) == 'e'`) strongly gates the primary in-bounds character-retrieval contract. The weakness is confined to the out-of-bounds edge case only; per the audit guidelines, a narrow gap whose capability is gated by sibling tests warrants low severity."
  },
  "G0228": {
    "id": "G0228",
    "verdict": "reclassify",
    "corrected_severity": "low",
    "corrected_line": 410,
    "note": "The weak-existence finding is correct: `assert not getattr(builtin, '_string_reverse')('')` at line 410 accepts any falsy value and does not enforce a `str` return type. If the empty-string branch returned `None` the assertion would still pass. Two corrections needed: (1) The `line` field is 409 but line 409 is `builtin = BuiltinFunctions(reader)` — the problematic assertion is at line 410; (2) severity should be 'low' not 'high', because a sibling test at line 403 (`assert getattr(builtin, '_string_reverse')('abcde') == 'edcba'`) strongly gates the primary reversal contract. The weakness only affects the empty-string edge case, making it a narrow gap per the audit guidelines."
  },
  "G0229": {
    "id": "G0229",
    "verdict": "accurate",
    "note": "Confirmed. The test at line 555-560 calls `getattr(builtin, '_io_print')('test message')` and makes zero assertions. Production code in `stdlib.py` lines 2700-2721 shows `_io_print` must: (a) log via `_logger.info`, and (b) invoke the registered sink callback via `sink(formatted)`. A no-op body, a stub that returns immediately, or an implementation that logs but never calls the sink would all leave the test green. The `no-assertion` category and high severity are correct: there is no other test that verifies the sink is invoked or that the formatted output reaches the registered callback."
  },
  "G0344": {
    "id": "G0344",
    "verdict": "refute",
    "note": "The test calls `panel.load_execution_report(real_report)` with a real `ExecutionReport` parsed from real log data (4 file changes), then asserts `panel._file_changes_tree.topLevelItemCount() == len(real_report.file_changes)`. Production `load_execution_report` (line 1978-1984 of sandbox_panel.py) iterates over `report.file_changes` and calls `self._file_changes_tree.addTopLevelItem(item)` once per change. If that `addTopLevelItem` call or the surrounding loop is removed, `topLevelItemCount()` returns 0 while `len(real_report.file_changes)` is 4, and the assertion fails. This is a real production gate for the item-creation contract. The `weak-existence` classification is incorrect — the test is comparing count against the actual data, not just checking `> 0`."
  },
  "G0345": {
    "id": "G0345",
    "verdict": "refute",
    "note": "The test calls `panel.load_execution_report(real_report)` with 3 real registry changes, then asserts `panel._registry_changes_tree.topLevelItemCount() == len(real_report.registry_changes)`. Production code (lines 1986-1992) iterates `report.registry_changes` and calls `addTopLevelItem` per record. Removing the loop or `addTopLevelItem` call makes `topLevelItemCount()` return 0 while `len(real_report.registry_changes)` is 3, causing the assertion to fail. This is a real gate against regressions that break registry item creation."
  },
  "G0346": {
    "id": "G0346",
    "verdict": "refute",
    "note": "The test calls `panel.load_execution_report(real_report)` with 2 real network activities and asserts `panel._network_tree.topLevelItemCount() == len(real_report.network_activity)`. Production code (lines 1994-2001) iterates `report.network_activity` and calls `addTopLevelItem` per activity. Removing the loop or `addTopLevelItem` call makes `topLevelItemCount()` return 0 while `len(real_report.network_activity)` is 2. The test is a real gate for network item-creation logic."
  },
  "G0347": {
    "id": "G0347",
    "verdict": "refute",
    "note": "The test performs two sequential loads. After the first load it asserts `topLevelItemCount() == 4` (gating initial population). After the second load with a single-file report, it asserts file tree count == 1, registry tree count == 0, and network tree count == 0 (gating the clear-on-reload behavior via `_clear_report_tabs()` at line 1976). If `_clear_report_tabs()` is removed, the second load would append to the first, giving counts of 5/3/2 instead of 1/0/0 — all three assertions fail. This is a real gate for the clear-and-replace lifecycle, not a weak check."
  },
  "G0348": {
    "id": "G0348",
    "verdict": "accurate",
    "note": "The test (line 233) asserts `panel.get_messages() == []` after calling `clear_messages()`, but never checks that `MessageBubble` widgets are removed from the layout. Production `clear_messages()` (chat.py line 450-461) both clears `self._messages` and calls `takeAt(0)` + `widget.deleteLater()` in a loop. A regression where only `self._messages.clear()` is performed (skipping the layout cleanup) would still pass `panel.get_messages() == []` while leaving orphaned `MessageBubble` widgets in the layout. The visual-removal contract is unverified. The `no-assertion` category is apt and severity `medium` is appropriate."
  },
  "G0349": {
    "id": "G0349",
    "verdict": "refute",
    "note": "Production `get_font_path(font_name)` returns `get_assets_path() / 'fonts' / font_name` (resource_helper.py line 156). The test asserts `'fonts' in str(path)`. If the production code were changed to omit the 'fonts' subdirectory (e.g., `get_assets_path() / font_name`), the assertion fails because get_assets_path() itself does not contain 'fonts' in its path. The test genuinely gates that the 'fonts' directory component is included in the path. `weak-existence` is a marginal classification — the assertion is narrow but it does fail on realistic breakage of the path-construction contract."
  },
  "G0350": {
    "id": "G0350",
    "verdict": "refute",
    "note": "Production `get_style_path(style_name)` returns `get_assets_path() / 'styles' / style_name` (resource_helper.py line 170). The test asserts `'styles' in str(path)`. If the production code omitted the 'styles' subdirectory component, the assertion fails because get_assets_path() does not contain 'styles'. This gates the path-construction contract for stylesheets. Like G0349, the assertion is narrow but fails on the realistic breakage of removing the 'styles' directory component."
  },
  "G0353": {
    "id": "G0353",
    "verdict": "accurate",
    "note": "test_rendering_at_every_progress_step (lines 1331-1363) calls draw_bg, draw_img, draw_title, draw_pipe, and draw_status_and_version inside a loop but makes zero assertions on the rendered output. The only action after all five calls is p.end() and splash.close(). If all five draw helpers became no-ops (pass), the test would still pass. No pixel sampling, no color checks, no exception-only guard — purely a crash gate, not a rendering gate."
  },
  "G0354": {
    "id": "G0354",
    "verdict": "refute",
    "note": "The test body at line 364-371 calls splash_screen.set_progress(_PROGRESS_30, 'Loading tools...') then asserts splash_screen.progress == _PROGRESS_30. If set_progress were broken so that it no longer updated progress_value (the backing field of the progress property, splash_screen.py line 546), this assertion would fail. The test name class TestShowLoadingStep and docstring are misleading (no show_loading_step method exists), but the test itself is a real gate for set_progress's numeric update path. A naming mismatch is cosmetic; the test does fail on realistic breakage of the capability it actually exercises."
  },
  "G0355": {
    "id": "G0355",
    "verdict": "refute",
    "note": "The test body at line 374-381 calls splash_screen.set_progress(_PROGRESS_30, 'Loading tools...') then asserts splash_screen.status == 'Loading tools...'. The status property (splash_screen.py line 953-960) returns self._status_message, which set_progress updates at line 548 when message is non-empty. If set_progress stopped updating _status_message, this assertion would fail. Same naming-mismatch cosmetic issue as G0354, but the test is a real gate for set_progress's status-update path."
  },
  "G0356": {
    "id": "G0356",
    "verdict": "accurate",
    "note": "test_pixmap_has_correct_dimensions (lines 502-506) asserts only pixmap.width() <= SPLASH_WIDTH and pixmap.height() <= SPLASH_HEIGHT. A 1x1 pixmap satisfies both upper-bound assertions. If _try_load_splash_image returned a grossly undersized pixmap due to a broken scaling call, or if _create_fallback_pixmap produced a tiny pixmap, both assertions would still pass. The test does not verify a lower bound, aspect ratio, or exact expected dimensions."
  },
  "G0357": {
    "id": "G0357",
    "verdict": "reclassify",
    "corrected_category": "other",
    "corrected_severity": "low",
    "note": "test_failed_stage_renders_through_full_sequence does contain real assertions at lines 1379-1382 (assert int(stages[2]) == _STATE_FAILED, assert int(stages[0]) == _STATE_COMPLETE, assert int(stages[7]) == _STATE_COMPLETE). These make it a genuine gate for mark_stage_failed and set_progress stage-state logic. Only the _draw_pipeline call at lines 1388-1389 is unasserted. The category 'no-assertion' is wrong — the test has assertions. The correct category is 'other' (partial render coverage gap). Severity should be low rather than medium because the state transitions are gated and the render gap is narrow (draw_pipeline is also exercised by G0353's sibling tests)."
  },
  "G0358": {
    "id": "G0358",
    "verdict": "refute",
    "note": "test_progress_signal_exists asserts hasattr(splash_screen, 'progress_updated'). In production, progress_updated = pyqtSignal(int, str) is declared as a class attribute at splash_screen.py line 161. If that class-level declaration were deleted, hasattr would return False and the assertion would fail. The finding itself has confirmed: false, acknowledging the test is a real gate. The hasattr check directly exercises the class-attribute contract; removing progress_updated from the class causes this test to fail."
  },
  "G0359": {
    "id": "G0359",
    "verdict": "refute",
    "note": "test_has_overlay_widget asserts hasattr(splash_screen, '_overlay'). Production code at splash_screen.py line 387 (inside _setup_overlay, called from __init__ at line 202) sets self._overlay = QWidget(self). If _setup_overlay were deleted or self._overlay assignment removed, hasattr returns False and the test fails. The finding has confirmed: false. The test directly gates the creation of the _overlay attribute in __init__."
  },
  "G0360": {
    "id": "G0360",
    "verdict": "refute",
    "note": "test_animation_timer_exists asserts hasattr(splash_screen, '_animation_timer'). Production code at splash_screen.py lines 196-198 sets self._animation_timer = QTimer(self) and wires its timeout signal. If that initialization were removed, hasattr returns False and the test fails. The finding has confirmed: false. The test directly gates the _animation_timer attribute creation in __init__, and its removal would also break downstream tests (test_show_animated_starts_timer calls timer.isActive()), reinforcing that this is a real contract gate."
  },
  "G0366": {
    "id": "G0366",
    "verdict": "accurate",
    "note": "test_disconnect_from_server_idempotent calls disconnect_from_server() on a widget that was never connected, so widget.client.connected is already False before the call. The assertion `assert not widget.client.connected` is trivially satisfied regardless of what disconnect_from_server() does. Production code at vnc_widget.py:1813-1822 performs update_timer.stop(), _cancel_pump_task(), client.disconnect(), and connection_status_changed.emit(False) — none of these are asserted by the test. A no-op implementation of disconnect_from_server() would pass this test."
  },
  "G0367": {
    "id": "G0367",
    "verdict": "refute",
    "note": "test_disconnect_idempotent calls asyncio.run(client.disconnect()) twice on a never-connected RFBClient. Production disconnect() at vnc_widget.py:1654-1666 sets self._writer = None at the end of the first call. If the `if self._writer is not None` guard (line 1658) were removed, the second call would execute self._writer.close() where _writer is None, raising AttributeError. That uncaught exception propagates through asyncio.run(), causing the test to fail with an error. The test is a real gate for idempotency: it would go red if the None-guard protecting the second disconnect were removed."
  },
  "G0368": {
    "id": "G0368",
    "verdict": "accurate",
    "note": "test_initial_client_disconnected at line 431-434 asserts only `not widget.client.connected`. This is a strict subset of the assertions in test_construction at line 421-428, which already asserts `not widget.client.connected` (line 427) alongside minimum-size and isinstance checks. Any production regression that breaks the initial-disconnected state would also fail test_construction. This test contributes zero independent gate coverage."
  },
  "G0369": {
    "id": "G0369",
    "verdict": "accurate",
    "note": "test_cache_usage_label_has_value at line 494-511 checks that the text ends with ' MB' and has a numeric prefix, or equals 'Model cache not available'/'Error reading cache'. Production _apply_cache_info() at xpu_status.py:553 sets `f'{usage_mb:.1f} MB'` derived from cache.get_memory_usage(). If _apply_cache_info were modified to hardcode 'cache_usage_label.setText(\"0.0 MB\")', that passes the is_mb_value regex and the test would remain green. The actual value from get_memory_usage() is never verified against the displayed label — only the format is checked."
  },
  "G0370": {
    "id": "G0370",
    "verdict": "accurate",
    "note": "test_cache_limit_label_has_value at line 514-528 accepts any text matching '<N> MB', 'N/A', or 'Error'. Production _apply_cache_info() at xpu_status.py:556 sets `f'{limit_mb:.0f} MB'` derived from cache.max_memory_bytes. Hardcoding the limit to '10240 MB' instead of the real value would satisfy the is_mb_value check. The actual max_memory_bytes value is never independently verified against the displayed label — only the output format is validated."
  },
  "G0371": {
    "id": "G0371",
    "verdict": "accurate",
    "note": "test_requirements_text_is_populated at line 536-554 accepts 'Checking system requirements...' as a known_stub (line 550). Production _refresh_requirements() at xpu_status.py:591 sets this placeholder synchronously before starting the background _RequirementsCheckWorker thread. If the worker thread is broken — its signals never fire, it raises inside run() before emitting, or the thread deadlocks — the text remains 'Checking system requirements...' indefinitely. The test would pass because the placeholder is explicitly listed in known_stubs, meaning a permanently stuck worker is indistinguishable from a healthy one that hasn't completed yet."
  },
  "G0372": {
    "id": "G0372",
    "verdict": "accurate",
    "note": "test_device_combo_has_xpu_entries_when_available at line 668-683: on the XPU-available path the assertion `assert info.device_name in combo.itemText(0)` (line 681) is guarded by `if info is not None` (line 680). When get_xpu_device_info(0) returns None — a valid production outcome even when is_xpu_available() is True — the critical device-name assertion is silently skipped and the test reduces to `assert combo.count() == count`, which only verifies item count, not text content. The sibling test test_device_combo_exists_and_populated (line 631-665) has the identical `if info is not None` guard around its exact-text assertion (lines 657-661), so it cannot rescue this gap. A production bug corrupting device text would go undetected when info is None during test execution."
  },
  "G0337": {
    "id": "G0337",
    "verdict": "accurate",
    "note": "The second loop (lines 201-204) gets the icon again (served from cache) and asserts `len(sizes) > 0 or not icon.isNull()`. Because the first loop already verified that all critical SVG icons are non-null (any null icon causes `assert not failed_load` to fail), `not icon.isNull()` is always True for every icon that reaches the second loop. The `or` short-circuits unconditionally, so `len(sizes) > 0` is never actually evaluated. An SVG that loads as a non-null QIcon but returns an empty availableSizes() list (e.g., Qt failed to parse geometry) would still pass this assertion. The `len(sizes) > 0` check is dead code."
  },
  "G0338": {
    "id": "G0338",
    "verdict": "accurate",
    "note": "Lines 323-327 of the test verify: (1) `icon_cache` is a dict, (2) `pixmap_cache` is a dict, and (3) that the string key `f'status_success_{_ICON_SIZE_24}'` exists in `icon_cache`. The `pixmap_cache` key format is never inspected — no key is ever read from or asserted against `pixmap_cache` contents. If the production code changed `pixmap_cache` from `dict[tuple[str, int], QPixmap]` to `dict[str, QPixmap]`, all three assertions would still pass, because the test never reads any key from `pixmap_cache`."
  },
  "G0339": {
    "id": "G0339",
    "verdict": "accurate",
    "note": "The test calls `icon_manager.clear_cache()`, then asserts `isinstance(icon_manager.icon_cache, dict)` and `'app_icon' not in icon_manager.icon_cache`. These two assertions only confirm that an explicitly cleared cache does not contain an entry that was never added to it. Clearing an empty dict leaves it empty; the assertion is trivially true by construction. If `get_app_icon()` were broken and never populated the cache, this precondition test would still pass; it gates no behavioral capability."
  },
  "G0340": {
    "id": "G0340",
    "verdict": "accurate",
    "note": "The sole assertion at line 118 is `assert panel._status_bar is not None`. In the production code (`base.py` line 172), `self._status_bar = self._build_status_bar()` is always assigned the return value of `_build_status_bar()`. That method always returns a `QFrame` instance (never None), so the assertion is vacuously satisfied. If `_build_status_bar()` were rewritten to return an empty `QFrame()` with no child labels (`_status_state`, `_status_pid`, `_status_arch`, `_status_priv`, `_status_bridge`), the test would still pass while the status-bar feature is entirely broken."
  },
  "G0341": {
    "id": "G0341",
    "verdict": "accurate",
    "note": "In `EntropyGraphWidget.paintEvent` (widgets.py lines 153-168), the background fill and all three gradient bands are painted unconditionally before the `if not values: return` early exit. The dark-theme background is `#1E1E1E`; the bands are `#1B3A1F`, `#3A3A1B`, `#3A1B1B` — all distinct from the background. `_count_foreground_colors` identifies the dominant color (background) and counts pixels that differ. Because the three bands are always drawn, at least some sampled pixels will have non-background colors even when `_entropy_values` is empty. If `set_data` were broken to never store the supplied values (e.g., `self._entropy_values = []`), paintEvent would paint the bands, hit `if not values: return`, and `_count_foreground_colors` would still return > 0. The assertion does not gate that the entropy polyline was actually rendered."
  },
  "G0342": {
    "id": "G0342",
    "verdict": "accurate",
    "note": "The `LargeFileSettingsDialog` constructor signature (widgets.py lines 698-703) accepts `current_chunk_kb`, `current_budget_mb`, `current_usage_mb`, and `parent` — there is no `current_prefetch_on_scroll` parameter. Line 735 unconditionally calls `self._prefetch_check.setChecked(True)`, so the checkbox is always initialized to True regardless of any input. The test constructs the dialog (line 227-231) and asserts `dialog.prefetch_on_scroll is True` (line 234). Because the checkbox is hardcoded to True, this assertion is always satisfied. There is no way to construct the dialog with prefetch_on_scroll=False via the API under test, so this assertion cannot detect any realistic defect in a round-trip prefetch property path."
  },
  "G0343": {
    "id": "G0343",
    "verdict": "refute",
    "note": "The finding focuses only on `assert longest > 0` (line 195), which is indeed computed from input `real_blocks[0]['ops']` rather than from the produced item. However, the test contains a directly gating assertion at line 190: `assert item.block_address == int(first['offset'])`. This asserts the produced `BasicBlockItem` stored its `block_address` correctly. In production, `BasicBlockItem.__init__` sets `self.block_address = block_address` at line 141. If this assignment were removed or corrupted (e.g., `self.block_address = 0`), the assertion at line 190 would fail because `first['offset']` would not equal 0. Line 194 (`assert single_line_height > 0`) also gates the non-zero height of the produced item's rect. The test is a real gate on `block_address` assignment and item geometry, even though the specific assertion at line 195 is indeed tautological."
  }
}
