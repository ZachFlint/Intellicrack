{
  "group": "test_hexpat",
  "files": {
    "tests/test_hexpat/test_interpreter.py": [
      {
        "id": "G0246",
        "test": "TestBitfields::test_bitfield_extraction",
        "line": 306,
        "category": "weak-existence",
        "severity": "medium"
      },
      {
        "id": "G0247",
        "test": "TestArrays::test_u8_array",
        "line": 337,
        "category": "weak-existence",
        "severity": "low"
      },
      {
        "id": "G0248",
        "test": "TestOutputFormat::test_parsed_field_keys",
        "line": 414,
        "category": "weak-existence",
        "severity": "low"
      }
    ]
  },
  "context": "Hexpat interpreter tests (intellicrack.core.hexpat.interpreter). Context:\n\n- Drive the REAL hexpat interpreter over a REAL .hexpat pattern against REAL binary bytes and assert the exact decoded variable values / struct field offsets against an INDEPENDENT oracle: hand-decode the same bytes with struct.unpack, or assert the known interpreter semantics (a computed expression's exact result). Never assert isinstance/not-None/len>=0.\n\n- The conftest in tests/test_hexcore_e2e already moved importorskip('intellicrack_hexcore') into the fixture; for pure-Python interpreter paths do NOT skip on the Rust backend - run and gate them. Only skip a test that genuinely requires the compiled Rust hexcore extension.\n\n- Choose pattern inputs whose decoded result is DISTINCT from the input bytes and from any trivial constant, so a no-op interpreter fails."
}
