The directory conftest tests/test_hexcore_e2e/conftest.py was ALREADY refactored: its module-level importorskip of the intellicrack_hexcore native module is REMOVED, and that module is now resolved ONLY through the 'hexcore' fixture (which performs the importorskip and skips appropriately). Do NOT edit conftest.py - it is already fixed.

For the skip-masks-failure findings on PURE-PYTHON test files (HexPat compiler / interpreter / preprocessor / evaluator / stdlib / control_flow / data_reader / complex_patterns), the fix is: REMOVE that file's OWN module-level importorskip of intellicrack_hexcore so the pure-Python tests run unconditionally and genuinely gate real behavior. These tests exercise pure-Python code (HexPatCompiler, HexPatInterpreter, etc.) that does not need the Rust native module. After removing the skip, ensure each formerly-masked test asserts a concrete oracle (not merely that it did not raise).

Do NOT add any blanket file-level skip. A test that genuinely constructs a Rust HexDocument should depend on the 'hexcore' fixture (or a HexDocument fixture), which skips legitimately when the native module is absent - that is an acceptable capability skip and must stay.
