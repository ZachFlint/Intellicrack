# Test-Gate Audit - `tests/test_ui`

**35 confirmed non-gate defects** (high: 4, medium: 16, low: 15) | 19 flags refuted (genuine gates).

## Confirmed non-gate tests

### `tests/test_ui/test_splash_screen.py`

- **TestEndToEndLifecycle::test_rendering_at_every_progress_step** (line 1331) - `no-assertion` / **high** - _[verified accurate]_
  - Why it is not a gate: Lines 1349-1359 call all five draw helpers (_draw_gradient_background, _draw_splash_image, _draw_title, _draw_pipeline, _draw_status_and_version) but make zero assertions about pixmap contents. All five methods could be replaced with no-ops (pass) and the test would still pass. A complete regression in any render layer would go undetected.
  - Fix: Apply sentinel-pixel verification after the draw loop: fill pixmap with known color before drawing, then assert pixmap pixels were overwritten (similar to _render_splash_layers helper on line 78). This gates that each layer actually produces visible output.
  - Independent check: test_rendering_at_every_progress_step (lines 1331-1363) calls draw_bg, draw_img, draw_title, draw_pipe, and draw_status_and_version inside a loop but makes zero assertions on the rendered output. The only action after all five calls is p.end() and splash.close(). If all five draw helpers became no-ops (pass), the test would still pass. No pixel sampling, no color checks, no exception-only guard — purely a crash gate…

### `tests/test_ui/test_xpu_status.py`

- **TestXPUStatusDialogCache::test_cache_usage_label_has_value** (line 494) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: The test accepts any string matching the pattern '<number> MB' regardless of the actual value. A realistic production breakage would be hardcoding cache_usage_label.setText('0.0 MB') in _refresh_cache (line 553), which would cause the cache display to show zero usage regardless of actual model memory consumption. The test would pass this breakage because '0.0 MB' matches the numeric-prefix-followed-by-MB format. The actual cache.get_memory_usage() value is never independently verified against the displayed text.
  - Fix: Call get_global_model_cache().get_memory_usage() / (1024*1024) directly in the test, format it to one decimal place (matching line 553's format), and assert that cache_usage_label.text() equals the computed value (allowing ±1 MB tolerance for timing skew).
  - Independent check: test_cache_usage_label_has_value at line 494-511 checks that the text ends with ' MB' and has a numeric prefix, or equals 'Model cache not available'/'Error reading cache'. Production _apply_cache_info() at xpu_status.py:553 sets `f'{usage_mb:.1f} MB'` derived from cache.get_memory_usage(). If _apply_cache_info were modified to hardcode 'cache_usage_label.setText("0.0 MB")', that passes the is_mb_value regex and the…

- **TestXPUStatusDialogCache::test_cache_limit_label_has_value** (line 514) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: The test accepts any string matching '<number> MB' without validating the actual value. A realistic breakage would be if _apply_cache_info (line 556) was changed to hardcode the limit as 10240 MB instead of deriving it from cache.max_memory_bytes. The test would pass because '10240 MB' matches the format. The real max_memory_bytes value is never independently verified against the label text.
  - Fix: Read get_global_model_cache().max_memory_bytes directly in the test, format it as '<N> MB' using integer formatting (matching line 556), and assert exact equality against cache_limit_label.text().
  - Independent check: test_cache_limit_label_has_value at line 514-528 accepts any text matching '<N> MB', 'N/A', or 'Error'. Production _apply_cache_info() at xpu_status.py:556 sets `f'{limit_mb:.0f} MB'` derived from cache.max_memory_bytes. Hardcoding the limit to '10240 MB' instead of the real value would satisfy the is_mb_value check. The actual max_memory_bytes value is never independently verified against the displayed label — only…

- **TestXPUStatusDialogRequirements::test_requirements_text_is_populated** (line 536) - `conditional-never-runs` / **high** - _[verified accurate]_
  - Why it is not a gate: The test accepts 'Checking system requirements...' as a valid known_stub (line 550), which is the synchronous placeholder set immediately in _refresh_requirements (line 591) before the background worker starts. If the _RequirementsCheckWorker thread is broken—for example, if the signal connections (lines 594-595) fail, the run() method raises an unhandled exception (caught at line 137 but worker never re-emits), or the thread deadlocks—the text stays at the placeholder indefinitely. The test would pass because the placeholder is in known_stubs, meaning a permanently-stuck worker would never be detected by this test.
  - Fix: Remove 'Checking system requirements...' from known_stubs and call _wait_for_requirements_render() before the assertion (as test_requirements_text_contains_met_or_warnings already does at line 574). This forces the test to wait for the actual worker completion, so a broken worker will cause the test to timeout or see stale placeholder text, turning it red.
  - Independent check: test_requirements_text_is_populated at line 536-554 accepts 'Checking system requirements...' as a known_stub (line 550). Production _refresh_requirements() at xpu_status.py:591 sets this placeholder synchronously before starting the background _RequirementsCheckWorker thread. If the worker thread is broken — its signals never fire, it raises inside run() before emitting, or the thread deadlocks — the text remains '…

### `tests/test_ui/log_viewer/test_realcov_15_tail_reader_real_logs.py`

- **test_reader_parses_real_structlog_records** (line 87) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Lines 87-88 assert `str(alpha['module'])` and `str(alpha['function'])` which only verify truthiness of the stringified field. The test would pass even if the structlog pipeline was modified to write hardcoded placeholder values (e.g., 'unknown', '<module>') instead of actual caller frame introspection. The production code's `_add_call_info()` processor performs real frame introspection (lines 234-236 in logging.py), but if that processor were modified to return fixed placeholders or if parsing were changed to substitute placeholders, the test provides no independent gate to catch this regression. The assertions bind only to non-emptiness, not to actual caller-location accuracy.
  - Fix: Strengthen lines 87-88 to assert specific content tied to the actual call site, such as `assert 'test_realcov_15_tail_reader_real_logs' in str(alpha['module'])` and `assert 'test_reader_parses_real_structlog_records' in str(alpha['function'])` to verify the values reflect the real caller location rather than merely being non-empty strings.
  - Independent check: Lines 87-88 assert `str(alpha['module'])` and `str(alpha['function'])`, which only check for truthiness of the stringified value (non-empty string). The presence of the fields is already gated at lines 83-84 by the `_REQUIRED_FIELDS` loop. If the `_add_call_info()` processor were changed to return fixed placeholder values like `'unknown'` or `'<module>'`, `str('unknown')` is truthy, so lines 87-88 would still pass. …

### `tests/test_ui/test_async_bridge.py`

- **TestShutdownBridgeLoop::test_shutdown_is_idempotent** (line 273) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test calls shutdown_bridge_loop() twice but makes no assertions. Production risk is real: if someone removed the _state.loop = None assignment in shutdown_bridge_loop, the test would still pass silently. The next call to ensure_loop() would then return the stopped loop instead of creating a new running loop, breaking downstream async functionality. The test verifies only that no exception is raised, not that the loop state is actually cleaned up.
  - Fix: Add assertions after the two shutdown calls: assert async_bridge_mod._state.loop is None and async_bridge_mod._state.thread is None. Then call ensure_loop() again and assert the returned loop is_running() to verify the loop can be cleanly recreated after double shutdown.
  - Independent check: The test at lines 273-276 calls `shutdown_bridge_loop()` twice with zero assertions afterward. The production function at async_bridge.py:501-516 sets `_state.loop = None` and `_state.thread = None` at the end. If these assignments were removed, the test would still pass silently — it only verifies no exception is raised on double-call, not that loop state is actually cleaned up. A regression where the second call o…

### `tests/test_ui/test_hxd_panel.py`

- **TestHxDPanelLifecycle::test_stop_tool_process_is_none_after_call** (line 661) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: Panel starts with process=None by construction (line 150 of hxd_panel.py). Calling stop_tool() on a None process does not exercise the cleanup code at line 387 (self.process = None) on a non-None value. The assertion trivially passes because process was already None.
  - Fix: Inject panel.process = QProcess() before calling stop_tool(), matching the pattern used in test_stop_tool_terminates_injected_not_running_process, so the assignment code path is actually exercised.
  - Independent check: The test at line 661 creates a fresh HxDPanel (process=None by construction at hxd_panel.py:150), then calls stop_tool() and asserts process is None. In _terminate_existing() (hxd_panel.py:375), the cleanup branch `if self.process is not None:` is skipped entirely because process was never set to a non-None value. The assertion `panel.process is None` trivially passes regardless of whether _terminate_existing() runs…

- **TestHxDPanelLifecycle::test_stop_tool_clears_embedded_container** (line 688) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: Panel starts with embedded_container=None (line 151 of hxd_panel.py). Calling stop_tool() does not exercise the cleanup at line 398 on a non-None value. The assertion passes trivially.
  - Fix: Set panel.embedded_container = QWidget() before calling stop_tool() so the non-None cleanup path is tested.
  - Independent check: The test at line 688 creates a fresh HxDPanel (embedded_container=None at hxd_panel.py:151) and calls stop_tool(). In _terminate_existing() (hxd_panel.py:389), the container cleanup branch `if self.embedded_container is not None:` is skipped entirely. The final assertion `panel.embedded_container is None` trivially passes because the container was never set to a non-None value. Deleting or corrupting the container c…

- **TestHxDPanelLifecycle::test_terminate_existing_sets_process_to_none** (line 730) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: Panel starts with process=None. Calling terminate_existing() (which calls _terminate_existing) does not exercise the cleanup code at line 387 on an actual process object.
  - Fix: Inject panel.process = QProcess() before calling terminate_existing() so the assignment is actually tested.
  - Independent check: The test at line 730 creates a fresh HxDPanel (process=None) and calls terminate_existing(), which delegates to _terminate_existing(). The `if self.process is not None:` branch at hxd_panel.py:375 is immediately skipped. `self.process = None` at line 387 is never reached. The assertion `panel.process is None` passes trivially. Removing line 387 entirely would not affect this test. Correctly identified as tautologica…

- **TestHxDPanelLifecycle::test_terminate_existing_sets_container_to_none** (line 741) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: Panel starts with embedded_container=None. Calling terminate_existing() does not exercise the cleanup code at line 398 on a non-None container.
  - Fix: Set panel.embedded_container = QWidget() before calling terminate_existing().
  - Independent check: The test at line 741 creates a fresh HxDPanel (embedded_container=None) and calls terminate_existing(). The `if self.embedded_container is not None:` branch at hxd_panel.py:389 is skipped entirely. `self.embedded_container = None` at line 398 is never executed. The assertion `panel.embedded_container is None` passes trivially. Removing the container cleanup code would not make this test fail. Correctly identified as…

- **TestHxDPanelLifecycle::test_cleanup_sets_process_to_none** (line 748) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: Panel starts with process=None. Calling cleanup() (which calls _terminate_existing) does not exercise the process cleanup code at line 387 on an actual process.
  - Fix: Inject a non-None QProcess before calling cleanup().
  - Independent check: The test at line 748 creates a fresh HxDPanel (process=None) and calls cleanup(), which delegates to _terminate_existing(). Since process is None from construction, the entire cleanup block at hxd_panel.py:375-387 is skipped. The assertion `panel.process is None` trivially passes. The production line `self.process = None` (line 387) is never reached. This test cannot detect removal or corruption of the process clean…

### `tests/test_ui/test_icon_manager.py`

- **TestIconLoading::test_critical_svg_icons_load_and_have_available_sizes** (line 201) - `swallows-failure` / **medium** - _[verified accurate]_
  - Why it is not a gate: The assertion at line 204 is `assert len(sizes) > 0 or not icon.isNull()`. The first loop (lines 191-196) already verified that icons are non-null, so `not icon.isNull()` is always True for all icons. This makes the `len(sizes) > 0` check dead code: an SVG that loads but has empty availableSizes() (Qt failed to parse geometry) would still pass because the `or` clause is always satisfied.
  - Fix: Remove the `or not icon.isNull()` disjunct and assert only `len(sizes) > 0`. The isNull check is already guaranteed by the first loop.
  - Independent check: The second loop (lines 201-204) gets the icon again (served from cache) and asserts `len(sizes) > 0 or not icon.isNull()`. Because the first loop already verified that all critical SVG icons are non-null (any null icon causes `assert not failed_load` to fail), `not icon.isNull()` is always True for every icon that reaches the second loop. The `or` short-circuits unconditionally, so `len(sizes) > 0` is never actually…

- **TestPixmapCacheStructure::test_pixmap_cache_key_format_is_tuple** (line 312) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test's stated purpose is to verify that pixmap_cache uses (name, size) tuple keys. However, the assertions at lines 323-327 only verify that icon_cache and pixmap_cache are dicts, and that icon_cache contains a str key. The actual pixmap_cache key format is never tested. If pixmap_cache were changed to use string keys, this test would still pass.
  - Fix: After calling get_icon() with a size parameter, directly inspect pixmap_cache to verify it contains at least one tuple key, or alternatively call get_pixmap() (if safe in this environment) and verify the cached key format.
  - Independent check: Lines 323-327 of the test verify: (1) `icon_cache` is a dict, (2) `pixmap_cache` is a dict, and (3) that the string key `f'status_success_{_ICON_SIZE_24}'` exists in `icon_cache`. The `pixmap_cache` key format is never inspected — no key is ever read from or asserted against `pixmap_cache` contents. If the production code changed `pixmap_cache` from `dict[tuple[str, int], QPixmap]` to `dict[str, QPixmap]`, all three…

### `tests/test_ui/test_process_panel.py`

- **TestPanelConstruction::test_panel_has_status_bar** (line 118) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts `panel._status_bar is not None`, a null-check with no functionality verification. The actual production capability—a status bar containing five QLabel widgets (_status_state, _status_pid, _status_arch, _status_priv, _status_bridge) with initial text values—is not gated by this test. If _build_status_bar() were modified to return an empty QFrame() with no labels, this test passes while the production status-bar feature is entirely broken. Other tests in the same file (test_attach_updates_status_pid, test_detach_clears_status_pid at lines 260-282) implicitly depend on the labels existing, but this specific test does not gate their presence or values.
  - Fix: Add assertions to verify the actual label widgets exist and contain expected initial text, e.g. `assert hasattr(panel, '_status_pid')`, `assert panel._status_pid.text() == 'PID: --'`, `assert panel._status_bridge.text() == 'Bridge: Disconnected'`, and similar checks for all five status labels. These assertions directly gate the production implementation against removal or gutting of the label initialization code.
  - Independent check: The sole assertion at line 118 is `assert panel._status_bar is not None`. In the production code (`base.py` line 172), `self._status_bar = self._build_status_bar()` is always assigned the return value of `_build_status_bar()`. That method always returns a `QFrame` instance (never None), so the assertion is vacuously satisfied. If `_build_status_bar()` were rewritten to return an empty `QFrame()` with no child labels…

### `tests/test_ui/test_realcov_13b_hex_widgets.py`

- **TestEntropyGraphWidget::test_renders_real_entropy_curve** (line 156) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The three gradient bands (gradient_low, gradient_mid, gradient_high at lines 155-163) are painted unconditionally before the isEmpty check at line 166. These bands are distinct from the background color #1E1E1E and will register as foreground pixels. If set_data were broken to never store the supplied entropy_values (e.g., line 117 changed to self._entropy_values = []), the paintEvent would still draw the three bands, return early at line 168, and _count_foreground_colors would return > 0. The actual entropy polyline rendering capability (lines 179-189) is not gated by this assertion.
  - Fix: Either (1) count distinct colors and assert count is strictly greater than the baseline produced by an empty entropy_values scenario, or (2) add a direct assertion that widget.entropy_values() equals the list passed to set_data() before checking painting output. Option 2 directly gates the storage path.
  - Independent check: In `EntropyGraphWidget.paintEvent` (widgets.py lines 153-168), the background fill and all three gradient bands are painted unconditionally before the `if not values: return` early exit. The dark-theme background is `#1E1E1E`; the bands are `#1B3A1F`, `#3A3A1B`, `#3A1B1B` — all distinct from the background. `_count_foreground_colors` identifies the dominant color (background) and counts pixels that differ. Because t…

- **TestLargeFileSettingsDialog::test_properties_reflect_inputs** (line 234) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: The constructor LargeFileSettingsDialog does not accept a current_prefetch_on_scroll parameter (lines 698-704). Line 735 unconditionally calls setChecked(True). The test can never construct the dialog with prefetch_on_scroll=False. The assertion at line 234 will always pass regardless of any production breakage, unless the hardcoded True in line 735 is changed—but such a change is breaking intentional behavior, not a realistic defect in the round-trip property path.
  - Fix: Add current_prefetch_on_scroll: bool parameter to __init__ (matching current_chunk_kb and current_budget_mb pattern), pass it to setChecked() at line 735, then construct the dialog in the test with current_prefetch_on_scroll=False and assert dialog.prefetch_on_scroll is False. This gates the property-forwarding path independently of constructor defaults.
  - Independent check: The `LargeFileSettingsDialog` constructor signature (widgets.py lines 698-703) accepts `current_chunk_kb`, `current_budget_mb`, `current_usage_mb`, and `parent` — there is no `current_prefetch_on_scroll` parameter. Line 735 unconditionally calls `self._prefetch_check.setChecked(True)`, so the checkbox is always initialized to True regardless of any input. The test constructs the dialog (line 227-231) and asserts `di…

### `tests/test_ui/test_realcov_15_chat_panel.py`

- **test_clear_messages_empties_history_and_view** (line 233) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test's docstring claims it verifies removal of both stored messages AND rendered bubbles (line 219). However, the only assertion after calling clear_messages() (line 233) is 'assert panel.get_messages() == []', which checks only the in-memory list. The visual contract—that MessageBubble widgets are actually removed from the layout—is never verified post-clear. Line 228 checks bubbles exist BEFORE the call, but there is no corresponding check AFTER. A regression where clear_messages() only clears self._messages but fails to call widget.deleteLater() or takeAt() would leave orphaned widgets in the layout while still passing this test.
  - Fix: Add 'QApplication.processEvents()' after 'qtbot.wait(50)' on line 231 to allow deferred deletion to complete, then add 'assert panel.findChildren(MessageBubble) == []' to gate the widget-tree cleanup alongside the in-memory assertion.
  - Independent check: The test (line 233) asserts `panel.get_messages() == []` after calling `clear_messages()`, but never checks that `MessageBubble` widgets are removed from the layout. Production `clear_messages()` (chat.py line 450-461) both clears `self._messages` and calls `takeAt(0)` + `widget.deleteLater()` in a loop. A regression where only `self._messages.clear()` is performed (skipping the layout cleanup) would still pass `pan…

### `tests/test_ui/test_splash_screen.py`

- **TestSplashPixmapLoading::test_pixmap_has_correct_dimensions** (line 502) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only upper bounds: pixmap.width() <= SPLASH_WIDTH and pixmap.height() <= SPLASH_HEIGHT. A 1x1 pixmap would pass both assertions. Looking at load_splash_pixmap (splash_screen.py:282-300), if _try_load_splash_image fails, it calls _create_fallback_pixmap which creates exactly SPLASH_WIDTH x SPLASH_HEIGHT (lines 314, 326-343). However, if _try_load_splash_image succeeds but scaling is broken, a small or wrongly-sized pixmap could be returned and the test would not catch it. The upper-bound-only assertion is a genuine gap.
  - Fix: Add lower-bound assertions: assert pixmap.width() >= SPLASH_WIDTH * 0.8 and assert pixmap.height() >= SPLASH_HEIGHT * 0.8 to detect regressions in image scaling or fallback generation.
  - Independent check: test_pixmap_has_correct_dimensions (lines 502-506) asserts only pixmap.width() <= SPLASH_WIDTH and pixmap.height() <= SPLASH_HEIGHT. A 1x1 pixmap satisfies both upper-bound assertions. If _try_load_splash_image returned a grossly undersized pixmap due to a broken scaling call, or if _create_fallback_pixmap produced a tiny pixmap, both assertions would still pass. The test does not verify a lower bound, aspect ratio,…

### `tests/test_ui/test_vnc_widget.py`

- **TestVNCWidget::test_disconnect_from_server_idempotent** (line 436) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: Calls disconnect_from_server() on a never-connected widget and asserts not widget.client.connected. Since client.connected was already False, the assertion is satisfied trivially. The real effects—stopping update_timer, cancelling the pump task, emitting connection_status_changed(False)—are not checked. A no-op implementation of disconnect_from_server() that does nothing would pass this test, even though it fails to stop the timer or emit the signal.
  - Fix: Manually start the timer (widget.update_timer.start(100)), connect a signal spy to connection_status_changed, call disconnect_from_server(), then assert not widget.update_timer.isActive() and verify the signal was emitted with False as the argument.
  - Independent check: test_disconnect_from_server_idempotent calls disconnect_from_server() on a widget that was never connected, so widget.client.connected is already False before the call. The assertion `assert not widget.client.connected` is trivially satisfied regardless of what disconnect_from_server() does. Production code at vnc_widget.py:1813-1822 performs update_timer.stop(), _cancel_pump_task(), client.disconnect(), and connect…

### `tests/test_ui/test_xpu_status.py`

- **TestProviderConfigXPUGroupBox::test_device_combo_has_xpu_entries_when_available** (line 668) - `conditional-never-runs` / **medium** - _[verified accurate]_
  - Why it is not a gate: On the XPU-available path (line 676), the device-name assertion (line 681) is nested inside 'if info is not None' (line 680). When get_xpu_device_info(0) returns None—a valid outcome even if is_xpu_available() is True (e.g., if PCI BAR enumeration fails transiently)—the critical assertion is silently skipped. A production bug in _populate_device_combo that corrupts the device text when info is available (e.g., setting 'XPU:0 - Unknown (0.0 GB)' instead of the real device name) would go undetected if info happens to be None during the test run. The test then reduces to verifying combo.count() == device_count, which does not validate the actual device names displayed.
  - Fix: Make get_xpu_device_info(0) None case explicit by asserting 'info is not None' when is_xpu_available() is True, then unconditionally assert the formatted string. Alternatively, remove this test and enhance test_device_combo_exists_and_populated to cover both the None and non-None cases with equivalent assertions, ensuring the test fails if _populate_device_combo sets incorrect text on either path.
  - Independent check: test_device_combo_has_xpu_entries_when_available at line 668-683: on the XPU-available path the assertion `assert info.device_name in combo.itemText(0)` (line 681) is guarded by `if info is not None` (line 680). When get_xpu_device_info(0) returns None — a valid production outcome even when is_xpu_available() is True — the critical device-name assertion is silently skipped and the test reduces to `assert combo.count…

### `tests/test_ui/log_viewer/test_realcov_15_window_real_logs.py`

- **test_window_level_filter_over_real_records** (line 215) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Line 215's assertion `assert window.proxy.rowCount() >= 1` is logically redundant. The surrounding assertions at lines 213-214 check specific events are visible or absent by examining `vis_events`, and line 216 asserts strict equality `vis_levels == {'ERROR'}`. If the filter is completely broken and returns zero rows, then `vis_events` would be empty (failing the event checks at 213-214) and `vis_levels` would be empty (failing the equality check at 216). Conversely, if the filter works correctly and returns the one ERROR row, then `rowCount() >= 1` passes but adds no independent failure detection beyond what lines 213-214 and 216 already cover. The assertion cannot catch any failure that the equality check does not already catch.
  - Fix: Remove line 215 as genuinely redundant, or replace it with `assert window.proxy.rowCount() == 1` to pin the exact expected count (exactly one ERROR record was emitted and remained visible after filtering), which would provide an independent gate stronger than the event-based checks.
  - Independent check: Line 215 `assert window.proxy.rowCount() >= 1` is redundant given the surrounding assertions. Line 213 asserts `'real_error_only' in vis_events` (which requires at least one visible row with that event), line 214 asserts `'real_info_only' not in vis_events`, and line 216 asserts `vis_levels == {'ERROR'}` (requires at least one visible ERROR-level row). If the filter were completely broken and returned zero rows, lin…

### `tests/test_ui/test_font_manager.py`

- **TestFontInfo::test_get_font_info_returns_dict** (line 400) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Test only asserts isinstance(info, dict) without verifying any dict content. The production code's get_font_info() does return a properly populated dict, but if the implementation returned {} or a dict with wrong keys/values, this specific test would still pass. The real gate comes from test_font_info_contains_required_keys which tests after load_fonts().
  - Fix: This test should be removed as redundant since test_font_info_contains_required_keys provides the real gate. Alternatively, call load_fonts() and verify the dict contains at least the 'fonts_loaded' key.
  - Independent check: test_get_font_info_returns_dict only asserts isinstance(info, dict). The production get_font_info() always returns a dict literal, so returning {} or a dict with wrong keys/values still passes this test. The real content gate is test_font_info_contains_required_keys which explicitly checks each required key after load_fonts().

- **TestFontInfo::test_font_info_values_correct_types** (line 431) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only that values are of correct Python types (bool, list, str). If fonts_loaded were hardcoded to False or code_font hardcoded to empty string, this test passes. The type check does not verify meaningful values.
  - Fix: Add assertions that bound the values to the real loaded state: assert info['fonts_loaded'] is True, assert len(info['code_font']) > 0, assert len(info['ui_font']) > 0.
  - Independent check: test_font_info_values_correct_types checks isinstance for bool/list/str types only. After load_fonts(), both fonts_loaded=False and code_font='' are valid bool and str instances respectively, so the test passes even with semantically broken values. Types alone do not verify meaningful population of the font info dict.

- **TestFallbackFonts::test_fallback_code_fonts_not_empty** (line 451) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only len(FALLBACK_CODE_FONTS) > 0, which passes even if the list contains a single nonsense entry. The specific-content test test_fallback_code_fonts_contains_common_fonts provides the real gate by verifying specific fonts are present.
  - Fix: Remove this test as it is redundant; test_fallback_code_fonts_contains_common_fonts already guarantees non-emptiness as a side effect of checking specific members.
  - Independent check: test_fallback_code_fonts_not_empty asserts only len(FALLBACK_CODE_FONTS) > 0. Since FALLBACK_CODE_FONTS is a module-level Final constant with 8 entries, this would pass even if the list were reduced to a single nonsense entry like ['garbage']. The real gate is test_fallback_code_fonts_contains_common_fonts which verifies specific fonts like 'Consolas', 'Courier New', 'monospace' are present.

- **TestFallbackFonts::test_fallback_ui_fonts_not_empty** (line 455) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Identical to test_fallback_code_fonts_not_empty: asserts only len(FALLBACK_UI_FONTS) > 0 without checking specific fonts. The real gate is test_fallback_ui_fonts_contains_common_fonts.
  - Fix: Remove this test as it is redundant; test_fallback_ui_fonts_contains_common_fonts already guarantees non-emptiness.
  - Independent check: test_fallback_ui_fonts_not_empty is identical in structure to G0325 but for FALLBACK_UI_FONTS. It asserts only len(FALLBACK_UI_FONTS) > 0, which passes as long as any single entry exists, regardless of whether it is a valid font name. The sibling test test_fallback_ui_fonts_contains_common_fonts provides specific-content gating.

### `tests/test_ui/test_graph_view.py`

- **TestEdgeItem::test_path_is_set** (line 220) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only that path.isEmpty() is False. If the cubicTo call at line 261 of graph_view.py were replaced with a degenerate path that still contains at least one element, this test would pass. It does not verify the path actually connects the two points with a proper Bezier curve.
  - Fix: Assert that the path bounding rect has non-zero width and height, or verify the path contains exactly the expected element count (moveTo + cubicTo = 2 elements).
  - Independent check: test_path_is_set asserts not path.isEmpty(). In Qt, QPainterPath.isEmpty() returns False after any element is added, including just moveTo(). The EdgeItem constructor calls path.moveTo(start) before path.cubicTo(...), so even if the cubicTo call were removed the path would still be non-empty and the test would pass. The test does not verify that the Bezier curve actually connects start to end.

### `tests/test_ui/test_hxd_panel.py`

- **TestHxDPanelLifecycle::test_double_terminate_is_safe_and_idempotent** (line 755) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: Panel starts with process=None and embedded_container=None. Two terminate_existing() calls operate on already-None state, never exercising the cleanup code on non-None objects.
  - Fix: Pre-set panel.process = QProcess() before the first call, assert it is None after the first call, then assert it remains None after the second call to demonstrate true idempotent behavior.
  - Independent check: The test at line 755 calls terminate_existing() twice on a fresh panel where process=None and embedded_container=None. Both calls skip all conditional branches in _terminate_existing() (lines 375 and 389 never enter their blocks). Both assertions trivially pass. The test validates that the no-op path does not raise or corrupt state, but cannot detect bugs in the actual teardown of non-None process or container objec…

- **TestHxDPanelLifecycle::test_stop_then_cleanup_is_safe** (line 764) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: Panel starts with process=None. Both stop_tool() and cleanup() operate on an already-None process, never exercising the cleanup code paths on actual objects.
  - Fix: Inject a non-None process before the first call so that stop_tool() actually exercises its cleanup path.
  - Independent check: The test at line 764 calls stop_tool() then cleanup() on a fresh panel where both process and embedded_container are None. stop_tool() calls _terminate_existing() (no-op on None state), then cleanup() calls it again (still no-op). Both assertions trivially pass because neither method can make process or embedded_container non-None when they started as None and no injection occurred. Removing the entire cleanup logic…

### `tests/test_ui/test_icon_manager.py`

- **TestApplicationIconStructure::test_app_icon_cache_starts_without_app_icon_key** (line 348) - `no-assertion` / **low** - _[verified accurate]_
  - Why it is not a gate: This test only verifies a pre-condition: that a freshly cleared cache does not contain 'app_icon'. It does not verify any behavioral capability. If get_app_icon() were broken and never populated the cache, this test would still trivially pass. The test verifies only that an empty cache is empty.
  - Fix: Reframe as a cache-isolation test: load some other icon, verify 'app_icon' still absent, to at least prove the key does not bleed in from unrelated operations. Or remove the test if it cannot call get_app_icon() safely.
  - Independent check: The test calls `icon_manager.clear_cache()`, then asserts `isinstance(icon_manager.icon_cache, dict)` and `'app_icon' not in icon_manager.icon_cache`. These two assertions only confirm that an explicitly cleared cache does not contain an entry that was never added to it. Clearing an empty dict leaves it empty; the assertion is trivially true by construction. If `get_app_icon()` were broken and never populated the ca…

### `tests/test_ui/test_sandbox_panel_fixes.py`

- **TestSandboxPanelLifecycle::test_start_tool_returns_true** (line 143) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: AnalysisPanelBase.start_tool() (base_panel.py:301-309) is documented to always return True with the comment 'True always since native panels are always ready'. SandboxPanel does not override this method. The assertion is guaranteed by the base-class contract, not by any SandboxPanel-specific behavior. A sandbox lifecycle regression (e.g. broken signal emission or state setup) would not prevent this test from passing.
  - Fix: Replace with a test that connects to tool_started signal and asserts it is emitted: 'spy = QSignalSpy(panel.tool_started); assert panel.start_tool() is True; assert spy.count() == 1'. Or assert post-conditions like 'assert panel._console_output is not None' to verify panel-specific state after tool start.
  - Independent check: base_panel.py line 301-309 shows start_tool() has a hardcoded `return True` with the comment 'True always since native panels are always ready'. SandboxPanel has no override of start_tool() (only _cleanup is overridden). The assertion `panel.start_tool() is True` cannot fail regardless of any SandboxPanel-specific regression in signal emission, state setup, or lifecycle behavior - the return value is a constant guar…

- **TestSandboxPanelLifecycle::test_stop_tool_returns_true** (line 148) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: AnalysisPanelBase.stop_tool() (base_panel.py:311-320) is documented to always return True. SandboxPanel does not override it. The assertion passes by base-class guarantee regardless of SandboxPanel internals. A broken _cleanup override or sandbox teardown would not prevent this test from passing.
  - Fix: Replace with: Connect to tool_closed signal, call stop_tool(), and assert signal was emitted. Or verify post-cleanup state: assert panel._sandbox is None and assert tool_closed signal was emitted.
  - Independent check: base_panel.py line 311-320 shows stop_tool() calls self._cleanup() then returns True. SandboxPanel does override _cleanup() (line 329), but the test creates a fresh SandboxPanel() with no bridge (_bridge=None) and no sandbox_id (None). In that state, _cleanup() only calls _disconnect_vnc_display() and stops the poll timer - both are trivially safe operations that cannot raise. The `return True` is always reached in …

### `tests/test_ui/test_splash_screen.py`

- **TestEndToEndLifecycle::test_failed_stage_renders_through_full_sequence** (line 1366) - `other` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: Lines 1380-1382 assert stage states correctly (real gates for mark_stage_failed and set_progress). However, lines 1388-1390 call _draw_pipeline without any assertion on the rendered output. If _draw_pipeline silently became a no-op or rendered nothing, the test would still pass. The state assertions are valid gates, but the rendering step is unguarded.
  - Fix: After draw_pipe(p, rect), sample the pixmap to confirm pixels were overwritten: fill pixmap with sentinel color, call draw_pipe, then assert painted pixel count >= threshold to verify the failed-stage glyph was actually rendered.
  - Independent check: test_failed_stage_renders_through_full_sequence does contain real assertions at lines 1379-1382 (assert int(stages[2]) == _STATE_FAILED, assert int(stages[0]) == _STATE_COMPLETE, assert int(stages[7]) == _STATE_COMPLETE). These make it a genuine gate for mark_stage_failed and set_progress stage-state logic. Only the _draw_pipeline call at lines 1388-1389 is unasserted. The category 'no-assertion' is wrong — the test…

### `tests/test_ui/test_theme_manager.py`

- **TestAvailableThemes::test_get_available_themes_returns_list** (line 284) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Test only checks isinstance(themes, list). A stub returning [] or ['x'] would pass. The sibling tests (lines 290, 296, 302) check for specific members (THEME_DARK, THEME_LIGHT, THEME_SYSTEM), which do gate the actual capability. This test adds only a type-check gate, not a capability gate. Removing this test would lose nothing; the sibling tests are the real gates.
  - Fix: Either delete this test (sibling tests provide all necessary coverage) or enhance it with assert len(themes) >= 3 to at least gate non-trivial population.
  - Independent check: Test at lines 284-287 calls `ThemeManager.get_available_themes()` and asserts `isinstance(themes, list)`. Production code at line 1557-1564 returns `[THEME_DARK, THEME_LIGHT, THEME_SYSTEM]`. A stub returning `[]` or `['x']` would satisfy the isinstance check. The sibling tests (lines 290-305) check for specific members and are the real gates. This test adds only a type-presence assertion with no capability gate, con…

- **TestThemeIntegrity::test_theme_manager_initialization_no_exceptions** (line 562) - `no-assertion` / **low** - _[verified accurate]_
  - Why it is not a gate: The try/except swallows exceptions, then asserts manager is not None. Since ThemeManager.get_instance() always returns a manager object (or raises), the assertion is tautological once the try block succeeds. Initialization bugs that leave the object in wrong state (e.g., styles_available=False, current_theme unset) would not be caught. The test is smoke-test only.
  - Fix: Replace the bare except with substantive assertions: assert manager.styles_available, assert manager.current_theme == DEFAULT_THEME, assert manager.theme_cache == {}. Remove the generic except wrapper and let initialization errors fail the test explicitly.
  - Independent check: Test at lines 562-569 calls `ThemeManager.reset_instance()` then `ThemeManager.get_instance()` inside a try/except, then asserts `manager is not None`. Production `get_instance()` at line 1187-1195 either returns the existing singleton or creates one via `cls()`. Since `ThemeManager.__init__` does lightweight work (sets attributes, calls `_check_styles_available()`), it practically never raises outside of catastroph…

### `tests/test_ui/test_vnc_widget.py`

- **TestVNCWidget::test_initial_client_disconnected** (line 431) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts not widget.client.connected on a freshly constructed VNCWidget. This is an exact duplicate of test_construction (line 421), which also asserts not widget.client.connected. Breaking the initial-state setup would fail both identically. This test contributes zero independent gate coverage beyond test_construction.
  - Fix: Delete this test. It is fully covered by test_construction. If retention is desired for clarity, broaden it to check additional widget state (e.g., timer.isActive() == False, focusPolicy == StrongFocus) that test_construction does not cover.
  - Independent check: test_initial_client_disconnected at line 431-434 asserts only `not widget.client.connected`. This is a strict subset of the assertions in test_construction at line 421-428, which already asserts `not widget.client.connected` (line 427) alongside minimum-size and isinstance checks. Any production regression that breaks the initial-disconnected state would also fail test_construction. This test contributes zero indepe…

## Refuted flags (verified to be real gates - no action needed)

- `tests/test_ui/test_app_toolbar_overflow.py` :: **test_main_window_uses_overflow_toolbar** (line 88) - The isinstance check on line 89 is redundant, but the test IS a real gate. The actual gate operates through line 78 (_find_toolbar's assert toolbars): if MainWindow._setup_toolbar changed from OverflowToolBar(...) to QToolBar(...), findChildren(OverflowToolBar) would return an empty list, failing the assertion before reaching line 89. The test catches the regression, just not via the isinstance check. _[Independent check: The finding was already marked `confirmed: false` in the batch, and the test IS a real gate. The helper `_find_toolbar` at line 77-78 calls `window.findChildren(OverflowToolBar)` and asserts `toolbars` is truthy. If `MainWindow._setup_toolbar` were changed to use `QToolBar(...)` instead of `OverflowToolBar(...)`, `findChildren(OverflowToolBar)` would return an empty list, causing the `assert toolbars` at line 78 to …]_
- `tests/test_ui/test_graph_view.py` :: **TestCFGGraphScene::test_blocks_with_missing_ops_key** (line 344) - Asserts only that a block was created and registered in scene.block_items. It does not verify that the missing 'ops' key was handled gracefully (no crash, empty ops list, correct dimensions). The production code at lines 345-348 of graph_view.py correctly coerces missing ops to [], but this test would pass even if the coercion were wrong or missing. _[Independent check: The test asserts LAYOUT_BLOCK_ADDR_A in scene.block_items, which FAILS if scene.load_graph() raises an exception. If the graceful handling (block.get('ops', [])) at graph_view.py line 345 were replaced with block['ops'], a KeyError would propagate through load_graph(), leaving scene.block_items empty, and the assertion would fail. The test genuinely gates against crashes caused by a missing 'ops' key.]_
- `tests/test_ui/test_graph_view.py` :: **TestCFGGraphScene::test_blocks_with_non_list_ops** (line 352) - Asserts only that a block was created and registered. The production code at lines 346-347 correctly coerces non-list ops to [], but this test would pass even if that coercion were broken or skipped. _[Independent check: The test asserts LAYOUT_BLOCK_ADDR_A in scene.block_items, which FAILS if scene.load_graph() raises. If the isinstance coercion (graph_view.py lines 346-347) were removed, ops='invalid' (a string) would be passed to BasicBlockItem. At line 148 of graph_view.py, 'if ops' is truthy for a non-empty string, so the generator max(len(op.get('disasm', '')) for op in ops) would iterate over characters of 'invalid' and call …]_
- `tests/test_ui/test_realcov_14b_graph_view.py` :: **TestGraphViewRealDisassembly::test_block_carries_real_mnemonics** (line 195) - Line 195 asserts `longest > 0` where `longest = max(len(op['disasm']) for op in first['ops'])` is derived from the input test data (real_blocks), not from the produced BasicBlockItem. If the production code in BasicBlockItem.__init__ completely ignored the `ops` parameter (e.g., set self._ops = []), the height would still be calculated as _HEADER_HEIGHT + 0 * _LINE_HEIGHT + _BLOCK_PADDING = 30px (still > 0), and longest would still be > 0 because it comes from real_blocks[0]['ops']. This test assertion gates nothing about whether ops are actually stored or used by the produced item. _[Independent check: The finding focuses only on `assert longest > 0` (line 195), which is indeed computed from input `real_blocks[0]['ops']` rather than from the produced item. However, the test contains a directly gating assertion at line 190: `assert item.block_address == int(first['offset'])`. This asserts the produced `BasicBlockItem` stored its `block_address` correctly. In production, `BasicBlockItem.__init__` sets `self.block_ad…]_
- `tests/test_ui/test_realcov_14b_sandbox_report.py` :: **TestSandboxPanelReportRendering::test_file_changes_tree_matches_real_records** (line 171) - This test IS a real gate for production code. If the `addTopLevelItem(item)` call in load_execution_report (line 1984) is removed or commented out, topLevelItemCount() will be 0, the assertion `topLevelItemCount() == len(real_report.file_changes)` (where len is 4) will fail, and the test will catch this regression. Any removal of the loop or item creation logic would be detected. The test gates the structural creation of items, which is a legitimate production contract. _[Independent check: The test calls `panel.load_execution_report(real_report)` with a real `ExecutionReport` parsed from real log data (4 file changes), then asserts `panel._file_changes_tree.topLevelItemCount() == len(real_report.file_changes)`. Production `load_execution_report` (line 1978-1984 of sandbox_panel.py) iterates over `report.file_changes` and calls `self._file_changes_tree.addTopLevelItem(item)` once per change. If that `a…]_
- `tests/test_ui/test_realcov_14b_sandbox_report.py` :: **TestSandboxPanelReportRendering::test_registry_changes_tree_matches_real_records** (line 184) - This test IS a real gate. If the `addTopLevelItem(item)` call in load_execution_report line 1992 is removed or the loop is skipped, topLevelItemCount() will return 0, the assertion `topLevelItemCount() == len(real_report.registry_changes)` (expected 3) will fail. The test catches any regression that prevents registry items from being added to the tree. This is a legitimate production gate despite being weak on content verification. _[Independent check: The test calls `panel.load_execution_report(real_report)` with 3 real registry changes, then asserts `panel._registry_changes_tree.topLevelItemCount() == len(real_report.registry_changes)`. Production code (lines 1986-1992) iterates `report.registry_changes` and calls `addTopLevelItem` per record. Removing the loop or `addTopLevelItem` call makes `topLevelItemCount()` return 0 while `len(real_report.registry_changes…]_
- `tests/test_ui/test_realcov_14b_sandbox_report.py` :: **TestSandboxPanelReportRendering::test_network_tree_matches_real_records** (line 197) - This test IS a real gate. If the `addTopLevelItem(item)` call in load_execution_report line 2001 is removed, topLevelItemCount() will be 0, and the assertion `topLevelItemCount() == len(real_report.network_activity)` (expected 2) will fail. Any production regression that prevents network items from being created will be caught. The test is a legitimate gate for item creation, despite not verifying content. _[Independent check: The test calls `panel.load_execution_report(real_report)` with 2 real network activities and asserts `panel._network_tree.topLevelItemCount() == len(real_report.network_activity)`. Production code (lines 1994-2001) iterates `report.network_activity` and calls `addTopLevelItem` per activity. Removing the loop or `addTopLevelItem` call makes `topLevelItemCount()` return 0 while `len(real_report.network_activity)` is 2…]_
- `tests/test_ui/test_realcov_14b_sandbox_report.py` :: **TestSandboxPanelReportRendering::test_reload_clears_previous_real_report** (line 212) - This test IS a real gate for the clear-on-reload behavior. Line 212 asserts that after the first load, file_changes_tree has 4 items. If the `clear()` call in load_execution_report (line 1976) is removed, or if the item-creation loop is skipped, the assertion will fail. The test gates both the initial load and the reload cycle. While weak on content (doesn't verify what's in the second load), it effectively catches removal of critical state-management logic. _[Independent check: The test performs two sequential loads. After the first load it asserts `topLevelItemCount() == 4` (gating initial population). After the second load with a single-file report, it asserts file tree count == 1, registry tree count == 0, and network tree count == 0 (gating the clear-on-reload behavior via `_clear_report_tabs()` at line 1976). If `_clear_report_tabs()` is removed, the second load would append to the fi…]_
- `tests/test_ui/test_resource_helper.py` :: **TestGetFontPath::test_resolves_font_path** (line 159) - get_font_path production code (resource_helper.py:156-158) guarantees the path is rooted under get_assets_path() / 'fonts' via construction, not circumstance. If get_font_path were changed to ignore get_assets_path() entirely (e.g. return Path('/wrong/root/fonts/test.ttf')), the assertion would still pass semantically but would represent a real production regression. The test gates that the string 'fonts' appears in the resolved path, which with the current production code is sufficient because get_assets_path() already guarantees the root is correct. A realistic breakage (removal of get_assets_path() call) WOULD be caught. _[Independent check: Production `get_font_path(font_name)` returns `get_assets_path() / 'fonts' / font_name` (resource_helper.py line 156). The test asserts `'fonts' in str(path)`. If the production code were changed to omit the 'fonts' subdirectory (e.g., `get_assets_path() / font_name`), the assertion fails because get_assets_path() itself does not contain 'fonts' in its path. The test genuinely gates that the 'fonts' directory compon…]_
- `tests/test_ui/test_resource_helper.py` :: **TestGetStylePath::test_resolves_style_path** (line 185) - Same pattern as test_resolves_font_path. Production code (resource_helper.py:170-172) constructs the path via get_assets_path() / 'styles' / style_name, guaranteeing correctness. If get_assets_path() call were removed, the test assertion 'styles' in str(path) would fail. The test gates a real contract. _[Independent check: Production `get_style_path(style_name)` returns `get_assets_path() / 'styles' / style_name` (resource_helper.py line 170). The test asserts `'styles' in str(path)`. If the production code omitted the 'styles' subdirectory component, the assertion fails because get_assets_path() does not contain 'styles'. This gates the path-construction contract for stylesheets. Like G0349, the assertion is narrow but fails on the r…]_
- `tests/test_ui/test_splash_screen.py` :: **TestShowLoadingStep::test_show_loading_step_updates_progress** (line 364) - Test class TestShowLoadingStep asserts it tests show_loading_step, but SplashScreen has no such method. The test body calls set_progress(), not show_loading_step(). If a show_loading_step method were added and broken, this test would not catch it because it exercises set_progress, not the stated capability. The gap is real: the test name/docstring mismatch creates a false impression of coverage. _[Independent check: The test body at line 364-371 calls splash_screen.set_progress(_PROGRESS_30, 'Loading tools...') then asserts splash_screen.progress == _PROGRESS_30. If set_progress were broken so that it no longer updated progress_value (the backing field of the progress property, splash_screen.py line 546), this assertion would fail. The test name class TestShowLoadingStep and docstring are misleading (no show_loading_step method…]_
- `tests/test_ui/test_splash_screen.py` :: **TestShowLoadingStep::test_show_loading_step_updates_status** (line 374) - Identical issue to test_show_loading_step_updates_progress. Claims to test show_loading_step (which does not exist in production) but calls set_progress. A broken show_loading_step method would not be detected. _[Independent check: The test body at line 374-381 calls splash_screen.set_progress(_PROGRESS_30, 'Loading tools...') then asserts splash_screen.status == 'Loading tools...'. The status property (splash_screen.py line 953-960) returns self._status_message, which set_progress updates at line 548 when message is non-empty. If set_progress stopped updating _status_message, this assertion would fail. Same naming-mismatch cosmetic issue as G…]_
- `tests/test_ui/test_splash_screen.py` :: **TestProgressSignal::test_progress_signal_exists** (line 388) - While hasattr check is weak in isolation, the immediate following test test_progress_signal_emits (line 397) connects to the signal and verifies it actually emits and updates state. The two tests together ensure both existence and behavior. For the hasattr test alone: if progress_updated were None or deleted, the next test would fail, and the production code (splash_screen.py:161) shows progress_updated is a real pyqtSignal. A missing signal would break the app. _[Independent check: test_progress_signal_exists asserts hasattr(splash_screen, 'progress_updated'). In production, progress_updated = pyqtSignal(int, str) is declared as a class attribute at splash_screen.py line 161. If that class-level declaration were deleted, hasattr would return False and the assertion would fail. The finding itself has confirmed: false, acknowledging the test is a real gate. The hasattr check directly exercises t…]_
- `tests/test_ui/test_splash_screen.py` :: **TestOverlayWidgets::test_has_overlay_widget** (line 458) - Asserts hasattr(splash_screen, '_overlay'). Production code (splash_screen.py:387) creates self._overlay = QWidget(self) in _setup_overlay(), called during __init__ (line 202). If _setup_overlay were deleted or _overlay initialization removed, hasattr would return False and the test would fail. Additionally, the following test test_overlay_hidden (line 1189) calls overlay.isVisible(), which would error if _overlay didn't exist or was None. _[Independent check: test_has_overlay_widget asserts hasattr(splash_screen, '_overlay'). Production code at splash_screen.py line 387 (inside _setup_overlay, called from __init__ at line 202) sets self._overlay = QWidget(self). If _setup_overlay were deleted or self._overlay assignment removed, hasattr returns False and the test fails. The finding has confirmed: false. The test directly gates the creation of the _overlay attribute in __…]_
- `tests/test_ui/test_splash_screen.py` :: **TestAnimatedGradient::test_animation_timer_exists** (line 877) - Asserts hasattr(splash_screen, '_animation_timer'). Production code (splash_screen.py:196-198) creates self._animation_timer = QTimer(self) and connects it to _on_animation_tick. The timer is started in show_animated() (line 455) and stopped in _on_fade_out_finished() (line 480). If the timer initialization were removed, hasattr would fail. Test gates a real contract. _[Independent check: test_animation_timer_exists asserts hasattr(splash_screen, '_animation_timer'). Production code at splash_screen.py lines 196-198 sets self._animation_timer = QTimer(self) and wires its timeout signal. If that initialization were removed, hasattr returns False and the test fails. The finding has confirmed: false. The test directly gates the _animation_timer attribute creation in __init__, and its removal would also …]_
- `tests/test_ui/test_state_persistence.py` :: **TestSaveTabState::test_save_tab_state_captures_splitter** (line 82) - Asserts splitter_sizes is a list of exactly two ints. Production code (tools.py:2586) calls self.main_splitter.sizes(), which returns the current QSplitter layout. If someone changed the implementation to always return [0, 0] or a hardcoded constant, the assertion would still pass. However, if the splitter initialization were removed or broken (line 864), the sizes would be empty or invalid, and the test would fail. The test gates that save_tab_state returns the splitter's current state. _[Independent check: The test at lines 82-93 creates a fresh ToolOutputPanel and checks that splitter_sizes is a list of exactly 2 ints. Production code at line 2586 returns `self.main_splitter.sizes()` which yields the QSplitter's real layout (initialized to [600, 200] at line 864). The assertions only verify structure (isinstance list, len 2, both ints) but not actual values. A production change returning hardcoded [0, 0] or [1, 1] wo…]_
- `tests/test_ui/test_state_persistence.py` :: **TestRestoreTabState::test_restore_tab_state_sets_splitter** (line 122) - Line 139 asserts: 'sizes != default_sizes or default_sizes == [400, 400]'. The or-clause is a silent escape hatch. The actual default splitter sizes are [600, 200] (tools.py:376-377, 864), NOT [400, 400]. If by chance default_sizes were changed to [400, 400] to match the restore target [400, 400], the 'or' clause would evaluate True regardless of whether setSizes actually worked. The conditional escape hatch masks failures. _[Independent check: The or-escape `or default_sizes == [400, 400]` in line 139 is inert with current production defaults. Production code initializes the splitter with `setSizes([_DEFAULT_SPLIT_LEFT, _DEFAULT_SPLIT_RIGHT])` at tools.py line 864, where `_DEFAULT_SPLIT_LEFT = 600` and `_DEFAULT_SPLIT_RIGHT = 200` (lines 376-377). So `default_sizes` is `[600, 200]`, never `[400, 400]`. The escape clause cannot fire under realistic conditi…]_
- `tests/test_ui/test_theme_manager.py` :: **TestApplyTheme::test_apply_theme_returns_bool** (line 147) - While the test only checks isinstance(result, bool), there are sibling tests that verify actual application behavior. Test at line 158 (test_apply_dark_theme_succeeds) asserts result is True, and test at line 194-223 (test_apply_invalid_theme_uses_default) verifies QApplication.styleSheet() actually changes. Together these gate that apply_theme truly applies stylesheets. However, the specific test at line 147 in isolation is weak-existence since a mock returning True would pass. The test suite as a whole is adequate. _[Independent check: Test at lines 147-154 only checks `isinstance(result, bool)`. Production `apply_theme` always returns `True` when a QApplication exists (line 1329) or `False` otherwise (line 1332). Any stub returning a bool would satisfy this. The sibling tests (lines 158, 180, 194) are the real gates. This narrow test adds no capability gate beyond 'the return type is boolean', which is a weak-existence defect as labelled.]_
- `tests/test_ui/test_vnc_widget.py` :: **TestRFBClientState::test_disconnect_idempotent** (line 169) - The test calls disconnect() twice and asserts not client.connected. While the assertion itself is tautological (connected was already False), the test does gate the critical idempotency behavior: calling disconnect() twice without raising an error. The production code checks if self._writer is not None before closing. If someone removed that None-check, the second call would raise AttributeError. The test WOULD fail in that case, making it a real gate for idempotency semantics, not just assertion truth. _[Independent check: test_disconnect_idempotent calls asyncio.run(client.disconnect()) twice on a never-connected RFBClient. Production disconnect() at vnc_widget.py:1654-1666 sets self._writer = None at the end of the first call. If the `if self._writer is not None` guard (line 1658) were removed, the second call would execute self._writer.close() where _writer is None, raising AttributeError. That uncaught exception propagates through…]_
