# Test-Gate Audit - `tests/test_bridges`

**38 confirmed non-gate defects** (high: 10, medium: 20, low: 8) | 7 flags refuted (genuine gates).

## Confirmed non-gate tests

### `tests/test_bridges/test_hex_editor_top_audit1.py`

- **TestF0005StateLockUsage::test_close_file_holds_state_lock** (line 125) - `no-assertion` / **high** - _[verified accurate]_
  - Why it is not a gate: The test verifies the observable end-state (bridge.document is None) but cannot detect lock removal. If _state_lock were removed entirely from close_file(), the document would still be set to None (line 4851 in hex_editor.py), and the assertion would pass. The lock's purpose is to serialize concurrent access to document/cursor/selection during mutation, but this test only observes the mutation's completion, not its serialization. A concurrent reader accessing bridge.document while close_file() clears it (without the lock) would see torn state, but this test never attempts concurrent access.
  - Fix: Replace with test_get_display_mode_blocks_while_set_document_holds_lock pattern: launch set_document() with slow operations in a thread while attempting get_display_mode() from another thread, asserting the getter blocks until the lock is released. Or add a concurrent reader that tries to read bridge.document repeatedly during close_file() execution and verify consistent atomic observation.
  - Independent check: The test at line 125-139 asserts only end-state: `bridge.document is not None` before and `bridge.document is None` after `close_file()`. As verified in hex_editor.py lines 4848-4855, the `_state_lock` wraps `self.document = None` plus cursor/selection/path clearing. If the lock were removed entirely but the `self.document = None` assignment remained, both assertions still pass because the test never attempts concur…

- **TestF0013PeImportsExportsDiskPath::test_get_pe_imports_succeeds_on_unmodified_pe** (line 446) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: The fixture PE (from conftest.py) is a minimal test PE with no actual import entries. The test only asserts isinstance(result, list), which passes regardless of whether get_pe_imports() uses the disk-path code path or memory bytes. If _document_disk_path_if_unmodified() were broken and always returned None (forcing memory fallback), or if _open_pe_for_inspection() always opened from memory instead of using the disk_path parameter, the result would still be [] and the test would pass. The test does not verify which code path was taken or that the import structure is correct.
  - Fix: Use a PE fixture with actual imports (kernel32.dll, user32.dll functions). Then assert that result is non-empty and contains dicts with keys 'dll', 'function', 'address', 'ordinal'. Verify content matches the known imports in the fixture. Optionally monkeypatch pefile.PE to log whether name= or data= was used and assert the disk-path variant was called for an unmodified document.
  - Independent check: The conftest `_build_pe_binary()` builds a minimal PE with only a `.text` section and no import directory entries (PE_NUM_SECTIONS=1, no import table set up, optional header size=240 but all zeros). So `get_pe_imports()` returns `[]` regardless of whether `_document_disk_path_if_unmodified()` returns the disk path or None. Both `_open_pe_for_inspection(disk_path)` and `_open_pe_for_inspection(None)` parse the same b…

- **TestF0013PeImportsExportsDiskPath::test_get_pe_imports_succeeds_after_modification** (line 465) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: After writing byte 0xEB over the MZ header (line 481), pefile will fail to parse the PE and _open_pe_for_inspection() will return None (caught at line 8707), causing get_pe_imports() to return []. The test only asserts isinstance(result, list), which passes for empty lists. If the memory fallback path is broken or a silent exception is swallowed and returned [], the test still passes. The test does not detect whether the memory fallback actually ran or whether the parser correctly handled the modified PE.
  - Fix: Modify non-critical bytes that preserve the MZ/PE signature and structure but alter import data (e.g., modify import table offsets, function names, or DLL names). Assert the returned list reflects the modified content. Alternatively, use unittest.mock to patch _pefile_mod.PE and verify that when document is modified, it is called with data= (not name=), proving the memory fallback executed.
  - Independent check: After `bridge.write_bytes(0, 'EB')` the MZ magic at byte 0 becomes 0xEB. In `get_pe_imports()` (hex_editor.py line 8701-8703), the bridge reads the first `_MIN_HEADER_SIZE` bytes and checks `head[:2] != b'MZ'`; since byte 0 is now 0xEB, this check fires and the method returns `[]` before even reaching `_open_pe_for_inspection`. `isinstance([], list)` is True. Whether the memory-fallback path is broken or silently sw…

- **TestF0014YaraScanDiskPath::test_yara_scan_unmodified_uses_filepath** (line 509) - `no-assertion` / **high** - _[verified accurate]_
  - Why it is not a gate: The test asserts any(m['rule'] == 'mz_marker' for m in matches), which checks that the MZ header pattern matched. This assertion is equally true whether yara scanned the file on disk (via scan_file_async()) or scanned the bytes loaded into memory (via scan_data_async()), because both paths scan the same MZ header at the start of the PE binary. If the production code were changed to always use the memory path (dropping the disk-path optimization), the MZ pattern would still match and the test would pass. The test does not verify which scan path was taken.
  - Fix: Monkeypatch YaraScanner.scan_file_async and YaraScanner.scan_data_async to record which method was called. Assert that for an unmodified document, scan_file_async was called (not scan_data_async), proving the disk-path optimization was used. Alternatively, instrument the bridge's _document_disk_path_if_unmodified() to count its calls and assert it was invoked.
  - Independent check: The test at line 509-526 asserts `any(m['rule'] == 'mz_marker' for m in matches)`. In hex_editor.py lines 8896-8909, the bridge uses `disk_path` from `_document_disk_path_if_unmodified()` to call `scan_file_async` or falls back to `scan_data_async` on the in-memory bytes. Both code paths scan the same underlying bytes (the minimal PE starting with `MZ`). If the disk-path optimization were removed and the production …

### `tests/test_bridges/test_hex_state_audit1.py`

- **TestF0038DisplayModeAsymmetricLocking::test_concurrent_set_get_display_mode_observes_consistent_values** (line 473) - `conditional-never-runs` / **high** - _[verified accurate]_
  - Why it is not a gate: On CPython, the Global Interpreter Lock (GIL) makes the read and write of a single string reference atomic at the machine level. The assertion all(value in allowed for value in samples) checks that every observed value is one of the three short allowed strings. Even if the lock were removed entirely from set_display_mode_state() and get_display_mode(), any torn read of the _display_mode string reference would still yield one of the three published values (no third state exists between them), so the assertion can never fail. The locking contract F-0038 is designed to prevent observing intermediate torn state, but CPython's GIL makes that intermediate state unobservable at the test level. This test does not actually exercise the lock's protecting behavior.
  - Fix: Replace with a blocking-semantics test like test_get_display_mode_blocks_while_set_document_holds_lock (line 513+): start a thread running set_document() with a known slow operation (e.g., via mock), then from another thread call get_display_mode() and measure that it blocks for the duration of the slow operation. Assert that get_display_mode() cannot proceed until set_document() releases the lock. This proves symmetric lock acquisition/release between getter and setter.
  - Independent check: The test at lines 473-511 runs writer threads cycling `_display_mode` through `{'hex8', 'hex16_le', 'hex32_le'}` and reader threads sampling `get_display_mode()`, then asserts `all(value in allowed for value in samples)`. As confirmed in hex_state.py lines 388-411, both getter and setter take `self._lock`. The claim is correct that on CPython, the GIL ensures a single object-reference store/load (`self._display_mode…

### `tests/test_bridges/test_process_bridge.py`

- **TestF0025ImageHlpModuleStruct::test_imagehlp_module64_struct_has_module_name** (line 3553) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: The test performs a set-then-assert-same-value pattern: assigns mod.SizeOfStruct = ctypes.sizeof(...) then asserts mod.SizeOfStruct == ctypes.sizeof(...). This is always true by construction. The third assertion (struct size > 584) is a compile-time constant check on the struct definition, not on production behavior. The test does not verify that production code correctly sets SizeOfStruct before calling SymGetModuleInfo64.
  - Fix: Remove this tautological test. The companion test test_resolve_module_returns_kernel32 (line 3564) properly exercises production code with real SymGetModuleInfo64 calls and will catch incorrect SizeOfStruct usage.
  - Independent check: The test at line 3553 performs a literal set-then-get: `mod.SizeOfStruct = ctypes.sizeof(IMAGEHLP_MODULE64)` then `assert mod.SizeOfStruct == ctypes.sizeof(IMAGEHLP_MODULE64)`. This is a tautology; ctypes field assignment and read-back of the same value is always equal. The second assertion `ctypes.sizeof(IMAGEHLP_MODULE64) > 584` is a compile-time struct-size check with no production behavior. Breaking the producti…

- **TestF0035HandleEnumNonBlocking::test_get_handles_yields_event_loop** (line 3949) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: The ticker task is awaited AFTER get_handles() completes. Step 1: create ticker task; Step 2: await get_handles(); Step 3: await ticker_task. The assertion ticks[0] == 100 will always pass because await ticker_task (step 3) finishes the ticker regardless of whether get_handles was blocking. If get_handles is synchronous/blocking, the ticker won't increment during step 2, but step 3 completes it, making ticks == 100 anyway. A fully blocking get_handles would pass this test.
  - Fix: Check ticks[0] BEFORE awaiting ticker_task to measure concurrency during get_handles execution. Assert ticks[0] > 0 after step 2 but before step 3, or sample ticks before and after known yield points to verify concurrent progress.
  - Independent check: The test at line 3949 creates `ticker_task`, awaits `get_handles()`, then awaits `ticker_task`. If `get_handles` blocks the event loop (no `asyncio.to_thread`), the ticker accumulates 0 ticks during step 2. But `await ticker_task` in step 3 then runs the ticker coroutine to completion (all 100 `asyncio.sleep(0)` iterations). The final assertion `ticks[0] == 100` holds in both the blocking and non-blocking cases, bec…

- **TestF0035HandleEnumNonBlocking::test_enum_handles_yields_event_loop** (line 3976) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: Same structural defect as test_get_handles_yields_event_loop: the ticker is awaited after enum_handles completes. The assertion ticks[0] == 50 will always be true because await ticker_task finishes the ticker in step 3 regardless of whether enum_handles blocked the event loop in step 2. A fully synchronous enum_handles implementation would pass this test.
  - Fix: Check ticks[0] BEFORE awaiting ticker_task (immediately after the enum_handles await) to measure whether the event loop was yielded to during the call itself.
  - Independent check: Identical structural defect to G0079, at line 3976. The ticker task is awaited AFTER `enum_handles` completes. Even if `enum_handles` blocked the event loop (no `asyncio.to_thread`), `await ticker_task` in step 3 lets the ticker run all 50 `asyncio.sleep(0)` iterations to completion. The assertion `ticks[0] == 50` is satisfied regardless of whether the event loop was yielded during `enum_handles`. A purely synchrono…

### `tests/test_bridges/test_x64dbg.py`

- **test_breakpoint_id_increments** (line 137) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: The test directly manipulates next_bp_id (x64dbg_bridge.next_bp_id += 1) and asserts the value equals 2. This verifies Python integer arithmetic, not any production breakpoint-allocation logic. Critically, next_bp_id is never used in production code - grep shows it only appears in property definitions, never in set_breakpoint or any other method. If the setter was deleted entirely, the test would fail, but if set_breakpoint was broken to not allocate IDs properly, this test would not catch it because set_breakpoint is never called.
  - Fix: Delete test_breakpoint_id_increments entirely (the property is dead code). If ID allocation is needed, implement a production-backed gate: call set_breakpoint twice with different addresses, verify the returned IDs differ, and verify breakpoints appear in get_breakpoints with correct IDs.
  - Independent check: The test manually performs `x64dbg_bridge.next_bp_id += 1` and asserts the result is 2, exercising only the property getter/setter round-trip. Examining `set_breakpoint` in x64dbg.py (lines 3200-3208): it stores breakpoints keyed by address (`self._breakpoints[address] = BreakpointInfo(id=address, ...)`) and never reads or modifies `_next_bp_id`. The `_next_bp_id` counter is entirely unused by `set_breakpoint`. If `…

- **test_watchpoint_id_increments** (line 202) - `tautological` / **high** - _[verified accurate]_
  - Why it is not a gate: The test directly increments next_wp_id (x64dbg_bridge.next_wp_id += 1) and asserts the result is 2. While next_wp_id IS used in set_watchpoint at lines 3442-3443 of x64dbg.py (wp_id = self._next_wp_id; self._next_wp_id += 1), this test does not call set_watchpoint. The test only verifies that a Python property can be manually incremented. If set_watchpoint was broken to not increment the counter, this test would still pass.
  - Fix: Call bridge.set_watchpoint(address, size, watch_type) twice with different addresses, verify the returned wp_ids are 1 and 2, and verify get_watchpoints returns both with correct IDs. This drives the actual production code path that increments the counter.
  - Independent check: The test at line 202 manually increments `x64dbg_bridge.next_wp_id += 1` and asserts the result is 2. While `_next_wp_id` IS used in `set_watchpoint` at lines 3442-3443, the test never calls `set_watchpoint`. The test only exercises the property getter/setter. If `set_watchpoint` were broken to not increment the counter or to use a wrong counter, this test would still pass because it bypasses the production allocati…

### `tests/test_bridges/test_cutter.py`

- **TestReadBytes::test_returns_bytes** (line 1482) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(result, bytes) but never verifies the actual byte content. The recorder returns '48 8b 05' which should parse to b'\x48\x8b\x05', but if read_bytes returned b'' (empty), b'\x00\x00\x00', or any other bytes object, the test would still pass. A production defect that breaks bytes.fromhex() parsing (e.g., returning the result of strip() without hex conversion) would not be caught.
  - Fix: Add `assert result == b'\x48\x8b\x05'` after the isinstance check to verify the actual parsed bytes match the expected hex-decoded value from the deterministic recorder response.
  - Independent check: The production read_bytes (lines 2896-2901) calls bytes.fromhex(result.strip()) and returns the result. The test at line 1482 only asserts isinstance(result, bytes). If read_bytes returned b'' (the fallback branch at line 2898-2899 when hex_str is empty), or b'\x00\x00\x00', or any other bytes object due to a parsing regression, isinstance(result, bytes) would still pass. The actual byte content b'\x48\x8b\x05' is n…

### `tests/test_bridges/test_hex_editor_bottom_audit1.py`

- **TestBpsExportStreaming::test_bps_export_via_mmap_pyfallback** (line 864) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only the 4-byte BPS1 magic header. It never validates (1) the patch is appliable to the source, (2) that applying it reconstructs the target, (3) that the streaming/mmap constraint stated in the method docstring is actually used (could use read_bytes instead). A broken implementation returning b'BPS1' + random bytes would pass. The production capability (correct BPS encoding with mmap streaming) is never verified.
  - Fix: Apply the patch and verify correctness: call _apply_bps_patch(bridge, original_export, source) and assert the result equals target. This is the pattern used in TestBpsEncoderRichOpcodes.test_round_trip_with_relocated_block (line 802). Optionally, also patch read_bytes to raise and confirm no exception occurs, gating the mmap behavior.
  - Independent check: test_bps_export_via_mmap_pyfallback (line 864) asserts only `original_export[:4] == b'BPS1'`. The production _build_bps_patch (line 4134) always starts with `patch = bytearray(b'BPS1')` as the very first statement, so any call that reaches this function and returns normally will produce bytes starting with b'BPS1'. An encoder that writes garbage opcodes, wrong checksums, or a patch that cannot reconstruct the target…

### `tests/test_bridges/test_hex_editor_top_audit1.py`

- **TestF0013PeImportsExportsDiskPath::test_get_pe_imports_does_not_raise_for_pe** (line 485) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: This test is byte-for-byte identical to test_get_pe_imports_succeeds_on_unmodified_pe (line 446): same _run(bridge.open_file(...)), same _run(bridge.get_pe_imports()), same isinstance(result, list) assertion. It contributes zero additional gate coverage beyond the first test. Any production defect that makes the first test pass will make this duplicate pass as well.
  - Fix: Delete this duplicate test. If retention is desired, convert it into a distinct gate: add assertions checking result element structure (assert all(isinstance(e, dict) for e in result), assert all('dll' in e and 'function' in e for e in result)) or add a test for error handling (assert get_pe_imports() returns [] for a non-PE file, not raises an exception).
  - Independent check: The test at lines 485-498 does have an assertion (`assert isinstance(result, list)`) so the category 'no-assertion' is wrong. It should be 'weak-existence' because `isinstance(result, list)` passes for both `[]` and any populated list. The test is structurally identical to `test_get_pe_imports_succeeds_on_unmodified_pe` (line 446-463): same fixture, same `open_file` call, same `get_pe_imports()` call, same `isinstan…

### `tests/test_bridges/test_plugin_deploy.py`

- **TestFindPluginSourceViaDeployment::test_finds_binary_in_build_release** (line 238) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: The test only asserts deploy_x64dbg_plugin(x64dbg, tmp_path) is True. It does not verify that the binary was actually copied to the correct destination, that the copied bytes match the source, or that the file landed in the correct architecture directory. If the production code found the source file in build/Release/ but deployed it to the wrong path (e.g., x64 instead of x32, or to a path outside release/{arch}/plugins/), or if it returned True from a code path that skipped copying entirely, the test would still pass. The post-deploy verification in deploy_x64dbg_plugin_detailed (line 2297) checks that the target file exists, but the test never verifies this.
  - Fix: After asserting deploy_x64dbg_plugin returns True, also assert that the deployed file exists at the correct target path: assert (x64dbg / 'release' / 'x64' / 'plugins' / 'intellicrack_bridge_x64.dp64').read_bytes() == DUMMY_PE, matching the pattern in test_finds_binary_in_bin_directory (line 191-202). This verifies both destination and content.
  - Independent check: The finding's core claim is correct: test_finds_binary_in_build_release (line 238) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` with no verification of the deployed artifact. A production bug that deployed `build/Release/intellicrack_bridge_x64.dp64` to the wrong arch directory (e.g., `release/x32/plugins/`) or corrupted bytes while still returning True would pass silently. However, the `is True` as…

- **TestFindPluginSourceViaDeployment::test_finds_binary_in_arch_specific_build** (line 255) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: The test only asserts deploy_x64dbg_plugin(x64dbg, tmp_path) is True. It does not verify the deployed file's location or content. If the plugin source in build_x64/plugins/ were deployed to the wrong architecture directory (e.g., x32 instead of x64), or not copied at all while returning True from a different code path, the test passes silently. The test name claims to verify architecture-specific discovery, but the actual assertion only checks the return value, not the deployed artifact.
  - Fix: After asserting True, also assert that x64dbg / 'release' / 'x64' / 'plugins' / 'intellicrack_bridge_x64.dp64' exists and its content equals DUMMY_PE. This pattern matches test_deploys_dp64_binary (line 204-221) and guarantees the file was deployed to the correct arch-specific directory.
  - Independent check: The finding's core claim is correct: test_finds_binary_in_arch_specific_build (line 255) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` with no verification of the deployed artifact's location or content. A bug that deployed `build_x64/plugins/intellicrack_bridge_x64.dp64` to the wrong path while still returning True would be undetected. The `is True` assertion is a real gate for whether `build_x64/pl…

- **TestFindPluginSourceViaDeployment::test_finds_binary_in_arch_specific_release** (line 272) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: The test only asserts deploy_x64dbg_plugin(x64dbg, tmp_path) is True. It does not verify the deployed file exists, where it was deployed, or that the bytes match. If the plugin source in build_x32/Release/ were deployed to the wrong arch tree (x64 instead of x32) or not copied at all, the test still passes. The test name suggests it verifies architecture-specific deployment for x32, but the only assertion is the return value.
  - Fix: After asserting True, also assert that x64dbg / 'release' / 'x32' / 'plugins' / 'intellicrack_bridge_x32.dp32' exists and its content equals DUMMY_PE_32. This mirrors the verification pattern in test_deploys_dp32_binary and confirms the x32-specific deployment path was exercised correctly.
  - Independent check: The finding's core claim is correct: test_finds_binary_in_arch_specific_release (line 272) asserts only `deploy_x64dbg_plugin(x64dbg, tmp_path) is True` for the `build_x32/Release/` discovery path, with no verification of the deployed artifact. A bug deploying to the wrong directory or corrupting content while returning True would pass undetected. The `is True` assertion is a real gate for `build_x32/Release/` being…

### `tests/test_bridges/test_process_bridge.py`

- **TestInitialization::test_initialize_debug_privilege_flag** (line 480) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts that _get_debug_privilege_enabled returns a bool. This passes whether the flag is True (privilege acquired) or False (privilege not acquired or code broken). If the entire _elevate_debug_privilege() method is removed or broken, the flag remains False (initialized value), and the test still passes. The test does not gate whether actual privilege elevation succeeded.
  - Fix: Assert that _get_debug_privilege_enabled() returns True when the test runs with sufficient permissions, or at minimum add a test that calls ToolError-raising code paths to confirm the privilege elevation was actually attempted.
  - Independent check: At line 486, the test asserts `isinstance(_get_debug_privilege_enabled(process_bridge), bool)`. The helper reads `bridge._debug_privilege_enabled`, which is declared as `bool = False` in `__init__` (line 1117 of process.py) and set to `True` only on successful elevation. It is always a `bool` regardless of whether elevation succeeded or failed. If `_elevate_debug_privilege` is removed or broken, the flag remains `Fa…

- **TestWindowEnumeration::test_get_windows_no_crash** (line 1013) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only checks isinstance(windows, list) and does not validate any actual Windows enumeration occurred. A stub implementation that always returns an empty list would pass. The test does not verify that get_windows actually invokes EnumWindows API or collects any window data; it only asserts the return type.
  - Fix: Assert that either windows is non-empty (for processes with visible windows) or assert that the EnumWindows API was actually called by checking the list structure or mocking the API call to verify invocation.
  - Independent check: At line 1013, `test_get_windows_no_crash` calls `attached_bridge.get_windows(os.getpid())` and asserts `isinstance(windows, list)`. While this would catch exceptions (e.g. if user32 became unavailable and ToolError was raised), it would not catch logic regressions where `get_windows` returns an empty list without actually calling `EnumWindows`. A stub `return []` implementation passes. The test class docstring itsel…

- **TestServiceListing::test_list_services_returns_list** (line 1026) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(services, list) without checking list content, length, or structure. A stub returning [] unconditionally would pass. The companion test TestF0023ServiceParseUnicode::test_service_name_fields_are_str (line 1591) properly validates service entries with len(services) > 0, but this standalone test gates nothing real.
  - Fix: Assert len(services) > 0 to ensure actual service enumeration occurred, or at minimum assert that at least one well-known Windows service (e.g., 'wuauserv', 'EventLog', or 'WinDefend') appears in the results.
  - Independent check: At line 1026, the test only asserts `isinstance(services, list)`. The production `list_services` calls `OpenSCManagerW` via advapi32. A stub returning `[]` would pass. The companion test `test_list_services_have_name_state` at line 1035 gates structure (name/state keys present) on real results, making this first test redundant as a gate. The finding correctly identifies it as `weak-existence`.

- **TestF0024SymbolInfoSizeOfStruct::test_sizeofsruct_equals_header_size** (line 3485) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test computes expected_header using only ctypes.sizeof() calls and then asserts expected_header > 0 and expected_header < ctypes.sizeof(SYMBOL_INFO). Both assertions are algebraically always true for any positive-sized struct. The test never exercises production code or verifies that _resolve_symbol actually uses the computed header size when calling SymFromAddr. Removing the SizeOfStruct assignment in production would not cause this test to fail.
  - Fix: Remove this pure formula check and rely on test_resolve_symbol_returns_nonempty_name (line 3498) which exercises the actual production code. Alternatively, patch the production _resolve_symbol method to capture and return the actual SizeOfStruct value passed to SymFromAddr for assertion.
  - Independent check: At line 3485, the test computes `expected_header = ctypes.sizeof(SYMBOL_INFO) - ctypes.sizeof(c_char*1024) + ctypes.sizeof(c_char)`. Since `SYMBOL_INFO.Name` is `c_char*1024`, this reduces to `sizeof(SYMBOL_INFO) - 1023`. Both assertions (`expected_header > 0` and `expected_header < ctypes.sizeof(SYMBOL_INFO)`) are algebraically guaranteed true for any positive-sized struct. No production code path is exercised; mod…

- **TestF0042SymbolBufferAllocation::test_sym_buf_size_accommodates_max_sym_name** (line 3727) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test recomputes sym_buf_size locally and asserts sym_buf_size >= sym_header_size + _MAX_SYM_NAME. This assertion is algebraically always true: sym_buf_size = sym_header_size + _MAX_SYM_NAME * sizeof(c_char) where sizeof(c_char) = 1, reducing to sym_header_size + _MAX_SYM_NAME >= sym_header_size + _MAX_SYM_NAME. No production code is exercised; a broken _resolve_symbol that allocated insufficient buffer would not be caught by this test.
  - Fix: Remove this pure formula check. The test_resolve_symbol_returns_nonempty_name (line 3498) properly exercises production code; if the buffer is too small, SymFromAddr fails and symbol resolution returns empty string, which that test catches.
  - Independent check: At line 3727, the test recomputes `sym_buf_size = sym_header_size + _MAX_SYM_NAME * ctypes.sizeof(c_char)` locally. The assertion `sym_buf_size >= sym_header_size + _MAX_SYM_NAME` is trivially true because `ctypes.sizeof(c_char) == 1`, so both sides are equal. The assertion `sym_buf_size > ctypes.sizeof(SYMBOL_INFO)` simplifies to `sym_header_size + 2000 > sizeof(SYMBOL_INFO)`, i.e. `(sizeof(SYMBOL_INFO) - 1023) + 2…

### `tests/test_bridges/test_process_win32.py`

- **test_enumerate_services_returns_list** (line 360) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(services, list) without checking content, count, or structure. A stub returning [] always would pass. No validation that enumerate_services actually invokes the Windows SCM API or retrieves any service data.
  - Fix: Assert len(services) > 0 to confirm actual enumeration occurred, and assert that at least one well-known Windows service (e.g., 'EventLog', 'WinDefend') appears by name, proving the SCM API was actually called and parsed.
  - Independent check: The test at line 360-367 only asserts `isinstance(services, list)`. The production `enumerate_services` in process.py calls OpenSCManagerW/EnumServicesStatusExW via advapi32, but a stub that returns `[]` — which is a valid `list` — would satisfy the sole assertion. The test provides no oracle that real SCM data was fetched (e.g. non-empty result, presence of well-known services like 'eventlog'). Weak-existence, medi…

- **test_enumerate_services_active_filter** (line 370) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(active_services, list) and explicitly notes results can be empty. The active=True filter parameter is never validated. An implementation that ignores the filter and returns all services, or returns [], both pass. There is no oracle or cross-check confirming the filter was actually applied.
  - Fix: Compare active_services with the full (unfiltered) list obtained from enumerate_services(active=False), assert that active_services is a subset of the full list, and assert that all entries in active_services have a running/active state.
  - Independent check: The test at line 370-377 asserts `isinstance(active_services, list)` with the docstring explicitly acknowledging results can be empty. The `active=True` filter path in production sets `state_filter = SERVICE_ACTIVE`, but the test cannot distinguish this from `state_filter = SERVICE_STATE_ALL` or from returning `[]`. No cross-check validates that the filter was applied. Weak-existence, medium severity is correct.

### `tests/test_bridges/test_realcov_03c_cutter.py`

- **TestRealDisassembly::test_disassemble_function_text** (line 263) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test accepts any non-empty string; if disassemble_function() were changed to return 'x' or an error message, both assertions pass. No x86 mnemonic, opcode, or structural disassembly content is verified. The method could be stubbed to return a placeholder and the test would silently pass.
  - Fix: Assert len(text.splitlines()) >= 2 and verify at least one line contains an x86 mnemonic like 'mov', 'push', 'ret', 'jmp', or 'call' to ensure real disassembly output is returned.
  - Independent check: The test at line 255-264 asserts `text.strip()` (truthy) and `len(text.splitlines()) >= 1`. The production method issues `pdf @ {address}` to radare2 on a real function address. However, a stub returning any non-empty string like `'x'` or an error message satisfies both assertions. No x86 mnemonic, address label, or disassembly structure is verified. Weak-existence, medium severity is correct.

### `tests/test_bridges/test_sandbox_bridge.py`

- **TestF0012AnalysisModuleCache::test_analysis_module_called_once_for_multiple_bridge_calls** (line 1998) - `tautological` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test name claims to verify caching across multiple calls, but only calls bridge.extract_iocs once (line 2024). It asserts call_count == 1 after this single call. A non-cached implementation that imports the module once per call would also produce call_count == 1. The lru_cache optimization is not exercised because there is no second call to verify the cache prevented a second import.
  - Fix: Make two or more distinct bridge calls (e.g., await bridge.extract_iocs(...) then await bridge.timeline(...)) within the same patch context, then assert call_count == 1 after both. This would actually demonstrate the cache avoids re-importing on the second call.
  - Independent check: The test patches `_get_analysis_module` at the module level and calls `bridge.extract_iocs` only once (line 2024), then asserts `call_count == 1`. A single call to any implementation — cached or not — always yields count == 1. The name claims 'multiple bridge calls' but none are made. The lru_cache behavior is not exercised because a second call never occurs. The test would pass with an uncached reimplementation tha…

- **TestF0016UTCTimestamps::test_tool_def_schemas_mention_utc** (line 2352) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The assertion checks any(kw in combined.lower() for kw in ['utc', 'iso', 'temp']) for sandbox.pcap_stop, sandbox.screenshot, and sandbox.memory_dump (lines 2357-2359). All three functions have 'auto-generated temp path' in their output_path parameter descriptions (lines 716, 736, 780 of sandbox_bridge.py). The keyword 'temp' matches regardless of whether UTC or ISO-8601 documentation exists. If UTC/ISO documentation was removed entirely from all three tool definitions, the test would still pass because 'temp' is always present.
  - Fix: Remove 'temp' from the keyword list and require either 'utc' or 'iso' to appear in the combined description. Alternatively, extract and check fn.description separately from parameter descriptions, specifically for temporal format documentation rather than auto-path caveats.
  - Independent check: The test at line 2359 checks `any(kw in combined.lower() for kw in ['utc', 'iso', 'temp'])`. Inspecting sandbox_bridge.py lines 716, 736, and 780: all three tool-definition parameter descriptions for `sandbox.pcap_stop`, `sandbox.screenshot`, and `sandbox.memory_dump` contain the string 'auto-generated temp path'. The keyword 'temp' always matches regardless of whether UTC or ISO-8601 documentation is present or has…

### `tests/test_bridges/test_x64dbg.py`

- **test_get_memory_map_current_process** (line 425) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(memory_map, list) and len(memory_map) > 0. A stub returning [object()] or [{}] would pass. The real logic of _walk_committed_memory_regions (VirtualQueryEx enumeration, state filtering, protection mapping at lines 4005-4056 of x64dbg.py) is completely untested. No region fields (base_address, size, protection, state, type) are examined.
  - Fix: Assert that at least one returned region has non-zero size, contains a known address (e.g., address of a local buffer), has a valid protection string from the set {'---', 'r--', 'rw-', '--x', 'r-x', 'rwx'}, and state == 'committed'. This verifies the VirtualQueryEx parsing and protection mapping actually work.
  - Independent check: The test at line 425 only asserts `isinstance(memory_map, list)` and `len(memory_map) > 0`. While it does call the real `get_memory_regions()` path including `_walk_committed_memory_regions` with an actual VirtualQueryEx call on the current process, no region field content is verified (base_address, size, protection flags, state, type). The internal logic of `_append_committed_region` (protection mapping, state filt…

### `tests/test_bridges/test_x64dbg_api_coverage.py`

- **test_watchpoint_management** (line 131) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test never injects an entry into bridge.watchpoints (unlike test_breakpoint_management which does inject into bridge.breakpoints at line 104). When get_watchpoints() is called, _watchpoints is empty and _pipe_client is None, so the method returns an empty list []. The assertion isinstance(wps, list) is satisfied by any list — [], [WatchpointInfo(address=0xBAD...)], or any other list. No realistic production breakage in get_watchpoints's merge-and-return logic (wrong address, wrong size, missing entries) would be caught. The capability under audit — that get_watchpoints correctly merges locally-tracked watchpoints by address — goes completely ungated.
  - Fix: After bridge.set_watchpoint raises ToolError, manually inject a WatchpointInfo into bridge.watchpoints (mirroring the breakpoint test's pattern at line 104), then call get_watchpoints() and assert len(wps) == 1 and wps[0].address == _ADDR_WATCHPOINT and wps[0].size == _WATCHPOINT_SIZE. This closes the gap between 'returned a list' and 'returned the correct watchpoints with correct fields'.
  - Independent check: test_watchpoint_management at line 130 calls get_watchpoints() on a freshly-constructed bridge with empty _watchpoints and _pipe_client=None. The only assertion is isinstance(wps, list). Because no watchpoint is injected before the call (contrast with test_breakpoint_management which injects into bridge.breakpoints at line 104), get_watchpoints() returns [] every time. Any broken merge-and-return logic that still re…

### `tests/test_bridges/test_x64dbg_new_methods.py`

- **TestScanMemory::test_scan_with_hex_string** (line 320) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The variable _buf_addr (underscore prefix signals it is intentionally unused) is computed but appears in no assertion. The test only asserts isinstance(results, list) and len(results) > 0. A broken hex-string parser — for example one that strips every other hex pair, interprets the string as big-endian, or silently returns the equivalent bytes for a different string — could still scan and find any matching region elsewhere in process memory, satisfying len(results) > 0 while the actual hex-parsing round-trip is not verified. By contrast, test_scan_with_bytes (line 275) anchors to buf_addr and validates matched_bytes, making it a genuine gate. This test provides no comparable anchor.
  - Fix: Add assert any(r.address == _buf_addr for r in results) (and rename _buf_addr to buf_addr to signal its use as an oracle). This confirms the hex-string parser converted the input to the exact same bytes as the marker buffer and found exactly that allocation, mirroring the address-verification pattern in test_scan_with_bytes.
  - Independent check: test_scan_with_hex_string at line 310-322 allocates a 16-byte marker buffer, computes ctypes.addressof into _buf_addr (underscore prefix, explicitly unused), then calls scan_memory('CAFEBABEDEADBEEFFEEDFACE12345678'). The only assertions are isinstance(results, list) and len(results) > 0. No assertion anchors any result back to _buf_addr. By contrast, test_scan_with_bytes (line 275-307) checks exact_matches[0].addre…

- **TestScanMemory::test_scan_with_spaced_hex_string** (line 335) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Identical structural weakness to test_scan_with_hex_string. _buf_addr is computed (line 333) but unused in any assertion. The production parser strips spaces via pattern.replace(' ', '') before calling bytes.fromhex(); if that replacement were broken (e.g. stripped only leading/trailing spaces, skipped alternate tokens, or produced off-by-one bytes), scan_memory could still find a spurious match elsewhere in process memory and pass both isinstance and len checks. The space-delimiter parsing path is the specific capability under audit here, but no result is tied back to the known buffer address where the correctly-parsed bytes must reside.
  - Fix: Rename _buf_addr to buf_addr and add assert any(r.address == buf_addr for r in results). This pins the assertion to the specific buffer whose bytes are the canonical reference encoding of the spaced hex pattern, confirming that space-delimited parsing round-trips correctly to the same byte sequence.
  - Independent check: test_scan_with_spaced_hex_string at line 325-339 is structurally identical to G0102. The 16-byte spaced-hex marker is written to buf, _buf_addr is computed but never appears in any assertion. The test only checks isinstance(results, list) and len(results) > 0. If the space-stripping via pattern.replace(' ', '') were broken—producing wrong bytes, off-by-one, or partial strips—scan_memory could still find a spurious m…

### `tests/test_bridges/test_cutter.py`

- **TestHexdump::test_sends_px_command** (line 1563) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(result, str) but the recorder supplies a real hexdump string with '0x00001000' in it. If hexdump returned '' (empty string) or failed to propagate the r2 output, the isinstance(str) assertion would still pass because '' is a valid str. The actual hex dump content is never verified.
  - Fix: Add `assert '0x00001000' in result` (or another substring from the recorder's configured response) to verify the hexdump content is actually returned, not merely that the type is str.
  - Independent check: The test at line 1563 asserts isinstance(result, str) for the return value of hexdump. The production hexdump (line 3898) returns result directly from _r2_cmd with no transformation. If hexdump returned '' (empty string) or any other str value, isinstance(result, str) would still pass. The test also verifies that a px command was dispatched with '128' in it (lines 1564-1566), which gates the command-dispatch path, b…

- **TestEsilOps::test_esil_eval** (line 1599) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only asserts isinstance(result, str) but never verifies the actual evaluation result. The recorder returns '0x42', but if esil_eval returned '', None (coerced to str), or any other string, the assertion would still pass. A broken implementation that silently strips or modifies the response would not be caught.
  - Fix: Add `assert result == '0x42'` (or similar) after the isinstance check to verify the actual ESIL evaluation result is propagated through from the Rizin response.
  - Independent check: The test at line 1599 asserts isinstance(result, str) for the return value of esil_eval. The production esil_eval (line 3229) returns result directly from _r2_cmd without transformation. If esil_eval returned '' or any other string instead of '0x42', isinstance(result, str) would still pass. The test also verifies ae_cmds contains one 'ae ' command (lines 1600-1601), gating command dispatch, but the evaluation resul…

### `tests/test_bridges/test_ghidra_audit6.py`

- **test_f0002_indented_script_would_have_failed_without_dedent** (line 289) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: The test is purely a Python language feature verification (that compile() rejects indented module-scope code). It calls only the built-in compile() function, never GhidraBridge._execute_remote() or prepare_remote_script(). Removing textwrap.dedent from the production code would NOT cause this test to fail because the test never exercises that code path. The test verifies a baseline property of Python, not production behavior.
  - Fix: Delete this test. It does not gate any production code. The real production gate is test_f0002_indented_call_site_does_not_leak_indentation (line 257), which drives the full bridge.execute_script() path end-to-end and confirms the dedented script reaches remote_exec.
  - Independent check: test_f0002_indented_script_would_have_failed_without_dedent (line 289) constructs a hard-coded indented string literal and asserts `compile(indented, '<test>', 'exec')` raises IndentationError. It calls only the Python built-in compile() — no GhidraBridge method, no prepare_remote_script(), no _execute_remote(). This is a stable property of the Python language itself (indented module-scope code always raises Indenta…

### `tests/test_bridges/test_process_bridge.py`

- **TestJobGuiCom::test_enumerate_com_servers_returns_list** (line 1239) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only checks isinstance(result, list). Because the Python interpreter process has no COM servers registered, an empty list is the expected real result. A stub returning [] always would pass. The test does not validate that the production code actually scans the registry; it only confirms the return type.
  - Fix: Seed the registry with a known test CLSID entry (as done in TestF0032AllInprocServerKeys at line 2516) and assert that entry appears in enumerate_com_servers results, proving real registry scanning is happening.
  - Independent check: At line 1239, `test_enumerate_com_servers_returns_list` asserts `isinstance(result, list)`. The Python interpreter process has no COM servers registered in HKCR\CLSID, so the expected real result is `[]`. A stub `return []` always passes. The test does not verify that the production code actually called `get_modules`, iterated DLL names, or queried the registry. Category `weak-existence` is correct; severity `low` i…

### `tests/test_bridges/test_realcov_03c_cutter.py`

- **TestRealByteIo::test_hexdump_real** (line 303) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Both 'assert dump.strip()' and 'assert words.strip()' accept any non-empty string. If hexdump() or hexdump_words() returned 'test' or an error message, both assertions pass. No hex-format structure (hex bytes, address labels, column widths) is verified.
  - Fix: Read the first byte at the .text section VA using read_bytes(), convert it to hex, and assert that the hex representation appears in the dump output. This anchors the test to actual binary content.
  - Independent check: The test at line 295-306 asserts only `dump.strip()` and `words.strip()` — any non-empty string from `hexdump` (using `px`) or `hexdump_words` (using `pxw`) passes. No hex-byte columns, address offset labels, or width structure is verified. A stub returning `'test'` would satisfy both assertions. Weak-existence, low severity is correct.

- **TestRealMetadata::test_headers_real** (line 488) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Both assertions only require at least one HeaderInfo with any truthy name; a stub returning [HeaderInfo(name='x', value='y', address=0)] would pass. No known PE header field name (Machine, NumberOfSections, MagicNumber, ImageBase, Subsystem) is verified.
  - Fix: Assert that at least one header reports a known PE field name: 'assert any(h.name.lower() in ('machine', 'numberofsections', 'magic', 'imagebase', 'subsystem') for h in headers)'.
  - Independent check: The test at line 481-489 asserts `headers` is non-empty and `any(header.name for header in headers)` — i.e. at least one HeaderInfo with any truthy name. A stub returning `[HeaderInfo(name='x', value='y', address=0)]` would pass. No known PE header field name (Machine, NumberOfSections, ImageBase, etc.) is verified. Weak-existence, low severity is correct.

- **TestRealCrossFormatElf::test_elf_symbols_real** (line 592) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Both assertions only require at least one SymbolInfo with any truthy name; a stub returning [SymbolInfo(name='_start', address=0, ...)] would pass. No ELF-standard symbol name (like _start, main, or libc symbols) is verified, and no address range check is performed.
  - Fix: Assert a minimum symbol count (len(symbols) >= 5) and verify that at least one symbol name is an ELF standard entry like '_start' or contains 'libc'. Alternatively, verify 'all(sym.address > 0 for sym in symbols)' to exclude zero-address stubs.
  - Independent check: The test at line 585-593 asserts `symbols` is non-empty and `any(symbol.name for symbol in symbols)` — at least one SymbolInfo with any truthy name. A stub returning `[SymbolInfo(name='_start', address=0, ...)]` would pass. No ELF-standard symbol names, address range validation, or real symbol-table structure is verified. Weak-existence, low severity is correct.

### `tests/test_bridges/test_sandbox_bridge.py`

- **TestF0012AnalysisModuleCache::test_get_analysis_module_returns_module** (line 2030) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Only asserts result is not None (line 2036). The function could return a completely wrong module object, a MagicMock, or a stub and the test would pass. The real correctness gates are in downstream tests that call actual analysis methods on the returned module.
  - Fix: Assert that the returned module has the required callables: hasattr(result, 'extract_iocs'), hasattr(result, 'generate_timeline'), hasattr(result, 'match_behaviors'), hasattr(result, 'detect_c2_patterns'), and hasattr(result, 'diff_reports'). Alternatively check result.__name__ == 'intellicrack.sandbox.analysis'.
  - Independent check: The test at line 2036 only asserts `result is not None`. `_get_analysis_module()` is an lru_cache-decorated function that returns `types.ModuleType`. Any non-None return — including a MagicMock or a wrong module — satisfies the assertion. The sibling test `test_get_analysis_module_returns_same_object` (lines 2039-2047) actually gates the cache behavior by asserting `first is second`. The finding's severity 'low' is …

## Refuted flags (verified to be real gates - no action needed)

- `tests/test_bridges/test_frida_bridge.py` :: **test_crash_info_bridge_internal_accumulation** (line 364) - This test is decorated with @pytest.fixture(autouse=True) require_frida() which calls pytest.skip() if _frida_available is False. The _frida_available flag is set to False if the try/except ImportError at module level (lines 56-61) catches ANY ImportError from importing FridaBridge. If frida_bridge.py fails to import due to a production code error (e.g., a refactored internal import, a typo in a module-level symbol, or a syntax error in a dependency), the skip is triggered silently. The test body uses only _TestableFridaBridge (a pure-unit double) and requires no live Frida, so a broken import would hide a real production defect rather than surfacing it as a collection error. _[Independent check: The claim asserts that frida_bridge.py failing to import causes a 'silent skip' via the autouse require_frida fixture. This mechanism cannot work as described. When _frida_available = False, FridaBridge is not in the runtime namespace (the TYPE_CHECKING import at line 34 is not executed at runtime). The class definition `class _TestableFridaBridge(FridaBridge):` at line 282 runs at module scope — if FridaBridge is n…]_
- `tests/test_bridges/test_frida_bridge.py` :: **test_parse_stalker_batch_call_event** (line 495) - Same root cause as test_crash_info_bridge_internal_accumulation. The autouse require_frida fixture will skip this test if frida_bridge.py fails to import, even though the test only exercises the internal _parse_stalker_batch parser via _TestableFridaBridge with synthetic dict input. No live Frida process is required. An ImportError in frida_bridge.py due to a production code defect (e.g., a missing symbol, a broken import chain, or a refactoring error) would silently skip the test instead of raising a collection error. _[Independent check: Same structural refutation as G0062. If frida_bridge.py fails to import, `class _TestableFridaBridge(FridaBridge):` at line 282 raises NameError at collection time — the module fails to collect with a loud error, not a silent skip. In the normal case (frida installed), test_parse_stalker_batch_call_event IS a real gate: it calls bridge.parse_stalker_batch_public() which invokes the real _parse_stalker_batch method, …]_
- `tests/test_bridges/test_frida_bridge.py` :: **test_parse_stalker_batch_exec_event_no_destination** (line 533) - Same root cause. This test exercises only the internal Stalker event parser with synthetic input using _TestableFridaBridge. The autouse require_frida fixture will silently skip it if frida_bridge.py fails to import, masking any production defect in the bridge code that caused the import failure. No live Frida dependency is required by the test itself. _[Independent check: Same structural refutation as G0062 and G0063. If frida_bridge.py fails to import, the module raises NameError at collection on `class _TestableFridaBridge(FridaBridge):` at line 282 — this is a collection error, not a silent skip. In the normal case, test_parse_stalker_batch_exec_event_no_destination IS a real gate: it calls bridge.parse_stalker_batch_public() with an exec-type event having no 'to' field, then asse…]_
- `tests/test_bridges/test_ghidra_audit6.py` :: **test_close_bridge_client_no_client_attr_safe** (line 1300) - The test IS a real gate. Removing the getattr(..., None) guard on line 1382 and directly accessing bridge.client would raise AttributeError when _Empty() lacks the attribute, failing the test. Similarly, removing the early return on line 1383-1384 would cause the code to try accessing client.sock when client is None, raising AttributeError. The guard is load-bearing and the test catches its removal. _[Independent check: The test at line 1300 has no explicit assert statement, but `closer(_Empty())` is an implicit no-exception assertion. The production _close_bridge_client at line 1382 uses `client = getattr(bridge, 'client', None)` and returns early when client is None. If this guard were changed to direct attribute access (`bridge.client`), calling closer(_Empty()) would raise AttributeError (since _Empty has no client attribute) a…]_
- `tests/test_bridges/test_hex_editor_bottom_audit1.py` :: **TestUtf16Scanner::test_python_fallback_excludes_non_printable_high** (line 780) - The assertion (assert '￾' not in r['content']) is inside a for loop over results. If _extract_strings_fallback returns an empty list (due to scanning disabled, filtering broken, or early return), the loop body never executes and the test passes vacuously. A regression that completely disables UTF-16 detection or drops all candidates would not be caught. The test cannot distinguish between 'correctly filtered non-printable sequences' and 'returned nothing at all'. _[Independent check: OVERRIDE of re-audit 'accurate': the test DOES gate the non-printable-rejection contract - if the filter regressed to emit U+FFFE, results would be non-empty AND contain the char, failing the 'U+FFFE not in r[content]' assertion. Detection-works is gated by the sibling test_python_fallback_finds_odd_aligned (L766). The finder's recommendation ('assert len(results)>0') is backwards: correct behavior for all-non-printable input is an empty result. Real (narrow) gate.]_
- `tests/test_bridges/test_realcov_03b_ghidra.py` :: **TestRealHeadlessLaunchHelpers::test_scrubbed_environment_strips_jvm_hijack_variables** (line 512) - The test DOES catch a noop scrubber. Changing _scrubbed_environment() to 'return dict(os.environ)' unmodified would leave all blocklist keys present in the environment, causing 'assert key not in env' to fail for at least PYTHONPATH (always set in pytest). The test IS a real gate because pytest execution guarantees at least one blocklist variable is present. _[Independent check: The batch JSON itself marks this `confirmed: false` and provides the refutation: the test iterates `_headless_env_blocklist` (which includes `PYTHONPATH`) and asserts each key is not in the returned environment. During pytest execution, pytest sets `PYTHONPATH` in `os.environ`. The production `_scrubbed_environment()` (ghidra.py:1569-1583) pops all blocklist keys. If the implementation were changed to `return dict(o…]_
- `tests/test_bridges/test_win32_types.py` :: **TestDllHelperCaching::test_get_dbghelp_resolves_symgetoptions** (line 515) - Only asserts isinstance(opts, int) (line 526). SymGetOptions always returns a DWORD, and ctypes.c_ulong is always an int in Python. Any DWORD-returning export from any valid DLL would satisfy this assertion. The test title claims to verify 'get_dbghelp returns the correct DLL', but only verifies that some DWORD-returning function was called. _[Independent check: The test calls `get_dbghelp()` and then does `dbg.SymGetOptions.restype = ctypes.c_ulong; opts = dbg.SymGetOptions()`. With ctypes.WinDLL, accessing an attribute by function name invokes `GetProcAddress` on the DLL handle. If `get_dbghelp` returned a wrong DLL (e.g. kernel32, ntdll, user32), `GetProcAddress` for 'SymGetOptions' would return NULL and ctypes would raise `AttributeError`, causing the test to fail. `Sym…]_
