# Test-Gate Audit - `tests/test_hexcore_e2e`

**104 confirmed non-gate defects** (high: 16, medium: 24, low: 64) | 14 flags refuted (genuine gates).

## Confirmed non-gate tests

### `tests/test_hexcore_e2e/test_bridge_display.py`

- **TestBridgeDisplayMode::test_get_display_mode_returns_hex8_by_default** (line 48) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: The conftest.py at test_hexcore_e2e/conftest.py line 36 contains a module-level `pytest.importorskip('intellicrack_hexcore')` that skips ALL tests in the directory when the Rust native module is absent. However, the display_mode methods (get_display_mode, set_display_mode) are pure-Python operations on self._display_mode (a str initialized to 'hex8' at line 444 of hex_editor.py) with no dependency on intellicrack_hexcore. If the Rust build fails in CI but the Python bridge remains, all 11 display/highlight tests would be skipped as a group, masking any regression in the pure-Python state management.
  - Fix: Move display_mode and highlight_rule tests to a separate conftest scope (or directory) that does not import intellicrack_hexcore, or refactor to remove the module-level importorskip guard so tests can run independently of the Rust extension's availability.
  - Independent check: The conftest.py at D:/Intellicrack/tests/test_hexcore_e2e/conftest.py line 36 has a module-level `pytest.importorskip('intellicrack_hexcore')` that causes pytest to skip the entire test directory when the Rust native extension is absent. The test at line 42-49 calls `bridge.get_display_mode()` which is implemented at hex_editor.py line 8505-8512 as pure Python: `return self._display_mode`. The `set_display_mode` met…

### `tests/test_hexcore_e2e/test_bridge_display_modes_complete.py`

- **TestDisplayModesExtended::test_roundtrip_hex32_le** (line 71) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: Same root cause as test_bridge_display.py: line 20 imports and skips on intellicrack_hexcore, despite all test methods (set_display_mode, get_display_mode) being pure-Python. The tests exercise only the state assignment `self._display_mode = mode` (line 8498 of hex_editor.py) and the getter `return self._display_mode` (line 8512), with no call to any Rust method. A broken implementation that ignores the set() argument (e.g., `self._display_mode = mode` deleted accidentally) would be undetected in any CI run where the Rust extension is unavailable.
  - Fix: Relocate these 19 display-mode tests to a test file outside test_hexcore_e2e/ (or create a separate conftest that excludes the importorskip guard) so they run independently of the Rust native module's build status.
  - Independent check: The file `test_bridge_display_modes_complete.py` has its own module-level `pytest.importorskip('intellicrack_hexcore')` at line 20, independent of the conftest. The test `test_roundtrip_hex32_le` at line 65-73 calls `bridge.set_display_mode('hex32_le')` then `bridge.get_display_mode()` - both are pure Python operations on `self._display_mode` (hex_editor.py lines 8498, 8512) with zero dependency on the Rust module. …

### `tests/test_hexcore_e2e/test_bridge_hash_advanced.py`

- **TestCalculateHashRange::test_sha256_on_first_16_bytes_of_pe_is_nonempty_hex** (line 106) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: Asserts only that result is non-empty and parseable as hex. If the bridge returned the wrong SHA-256 (e.g., hash of wrong byte range, or a different algorithm), this test would still pass as long as the output is valid hex. No oracle (hashlib) comparison is performed. The companion test at line 119-130 validates against hashlib, but this test in isolation is format-only.
  - Fix: Compute the expected hash independently: expected = hashlib.sha256(pe_bytes[:16]).hexdigest(), then assert result.lower() == expected.lower(), to actually gate SHA-256 computation correctness.
  - Independent check: test_sha256_on_first_16_bytes_of_pe_is_nonempty_hex (line 106) only checks isinstance(result, str), result is truthy, and bytes.fromhex(result) succeeds. The bridge delegates to self.document.compute_hash_range(). If the backend returned a valid hex string of a wrong hash algorithm or wrong byte range, all three assertions would still pass. The companion at line 119-130 compares against hashlib.sha256, but this test…

- **TestCalculateHashRange::test_sha1_range_is_valid_hex_digest** (line 157) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: Asserts only that result is 40 characters of valid hex. If the bridge computed SHA-1 of the wrong byte range, wrong algorithm, or returned another algorithm's digest, this format check would still pass. No hashlib comparison is performed. The companion test at line 169-180 validates against hashlib, but this test is format-only.
  - Fix: Compare against hashlib: expected = hashlib.sha1(_KNOWN_PAYLOAD[:64]).hexdigest(), then assert result.lower() == expected.lower(), to gate SHA-1 computation correctness.
  - Independent check: test_sha1_range_is_valid_hex_digest (line 157) asserts len(result)==40 and bytes.fromhex(result). SHA-1 output is 20 bytes = 40 hex chars, which is length-distinct from MD5 (32) and SHA-256 (64), so only an algorithm that happens to produce 40-char hex output could slip through. Any wrong byte range with a correct sha1 would pass, or a stub returning any 40-char hex string would pass. The companion at line 169-180 v…

- **TestCalculateHashCustomCrc::test_crc16_on_pe_bytes_returns_hex_string** (line 299) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: Asserts only that result is non-empty and valid hex. If the CRC-16 computation used the wrong polynomial, init value, or byte range, this test would still pass. No reference _crc16_ccitt() comparison is performed. The companion test at line 285-297 validates against the reference implementation, but this test is format-only.
  - Fix: Compare against the reference: crc_val = _crc16_ccitt(pe_bytes[:16]), expected = f'{crc_val:04x}', then assert result.lower() == expected.lower(), to gate CRC-16 computation correctness.
  - Independent check: test_crc16_on_pe_bytes_returns_hex_string (line 299) asserts isinstance(result, str), result is truthy, and bytes.fromhex(result). A CRC with a wrong polynomial, wrong init value, or wrong byte range would still produce a valid hex string. The companion at line 285-297 validates the CRC-16/CCITT-FALSE value against _crc16_ccitt() reference, but this test is format-only.

- **TestCalculateHashCustomCrc::test_crc8_returns_valid_hex_string** (line 328) - `weak-existence` / **high** - _[verified accurate]_
  - Why it is not a gate: Asserts only that result is non-empty and valid hex. If the CRC-8 computation returned garbage or the wrong polynomial, this test would still pass as long as the format is hex. No reference _crc8_smbus() comparison is performed. The companion test at line 314-326 validates against the reference implementation, but this test is format-only.
  - Fix: Compare against the reference: crc_val = _crc8_smbus(bytes(range(16))), expected = f'{crc_val:02x}', then assert result.lower() == expected.lower(), to gate CRC-8 computation correctness.
  - Independent check: test_crc8_returns_valid_hex_string (line 328) asserts isinstance(result, str), result is truthy, and bytes.fromhex(result). A CRC-8 with wrong polynomial/init/range would still produce a valid non-empty hex string. The companion at line 314-326 compares against _crc8_smbus() reference. This test is format-only and would pass even if the CRC computation were entirely wrong.

### `tests/test_hexcore_e2e/test_bridge_transforms.py`

- **TestBridgeListTransforms::test_list_transforms_items_have_required_keys** (line 63) - `conditional-never-runs` / **high** - _[verified accurate]_
  - Why it is not a gate: The walrus operator `if result := _run(bridge.list_transforms()):` at line 69 makes the entire assertion block conditional on non-empty list. When the pipeline module is unavailable or when code is broken and returns [], the if-block never executes, so assertions for 'name', 'category', 'description' keys never run. The test passes silently without validating any keys.
  - Fix: Add `if not _pipeline_available: pytest.skip(...)` at test start. Then assert `len(result) > 0` before the for loop to ensure the list is non-empty. This converts the silent pass into an explicit failure if list is empty.
  - Independent check: The walrus operator at line 69 (`if result := _run(bridge.list_transforms()):`) makes the entire assertion block — which checks for 'name', 'category', and 'description' keys — conditional on a non-empty list. Production code at line 7772-7773 of hex_editor.py explicitly returns `[]` when `_pipeline_available` is False or `_get_all_transform_nodes` is None. When the transform module is unavailable, the test passes w…

- **TestBridgeListTransforms::test_list_transforms_name_values_are_strings** (line 75) - `conditional-never-runs` / **high** - _[verified accurate]_
  - Why it is not a gate: The for loop `for item in result:` at line 82 never executes if result is []. When the pipeline module is unavailable or code returns [], the loop body never runs and no assertions fire. The test passes silently. This is identical gap to finding #3.
  - Fix: Add `if not _pipeline_available: pytest.skip(...)` at test start. Then assert `result` is non-empty before the loop (e.g., `assert len(result) > 0`). This ensures empty lists cause explicit failure, not silent pass.
  - Independent check: The for loop `for item in result:` at line 82 iterates over the return value of `bridge.list_transforms()`. When `_pipeline_available` is False, the production bridge at hex_editor.py line 7772-7773 returns `[]`. An empty list causes the for-body (which asserts `isinstance(item['name'], str)` and `len(item['name']) > 0`) to never execute. The test passes silently with zero assertions run. Category conditional-never-…

### `tests/test_hexcore_e2e/test_hexpat_compiler_e2e.py`

- **TestHexPatCompilerCompile (all tests)** (line 13) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: conftest.py line 36-39 contains module-level pytest.importorskip("intellicrack_hexcore"). When the Rust extension is not built, pytest skips conftest collection and silently skips all tests in test_hexcore_e2e/. This file only imports pure-Python modules (HexPatCompiler, HexPatLexer, TokenType) with zero dependency on intellicrack_hexcore. Deletions, refactorings, or API breakages in HexPatCompiler would produce skip (not red) whenever Rust is absent from the environment, masking production defects.
  - Fix: Move test_hexpat_compiler_e2e.py out of test_hexcore_e2e/ to a sibling directory (e.g., tests/test_hexpat/) or restructure conftest.py to only apply importorskip to fixtures that actually require the Rust module, not at module scope.
  - Independent check: conftest.py line 36-39 executes 'hexcore_mod = pytest.importorskip("intellicrack_hexcore", ...)' at module level. When intellicrack_hexcore is absent, pytest raises a Skipped exception during conftest.py collection, which prevents conftest.py from loading. Since pytest loads conftest.py before collecting any tests in its directory, ALL tests in test_hexcore_e2e/ are silently skipped. test_hexpat_compiler_e2e.py's ow…

### `tests/test_hexcore_e2e/test_hexpat_complex_patterns.py`

- **All tests in file** (line 1) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: Same root cause as test_hexpat_compiler_e2e.py: conftest.py module-level importorskip("intellicrack_hexcore") skips the entire directory when Rust is absent. This file only imports HexPatInterpreter (pure Python) and test fixtures. Any regression in pattern execution, pointer dereferencing, or complex field extraction would skip rather than fail in environments without the Rust extension.
  - Fix: Relocate to tests/test_hexpat/ or refactor conftest.py so importorskip guards only fixtures that genuinely require intellicrack_hexcore, not module-level collection.
  - Independent check: test_hexpat_complex_patterns.py (line 1) imports only the pure Python HexPatInterpreter and defines its own 'interp' fixture locally. However, conftest.py in the same directory has module-level pytest.importorskip("intellicrack_hexcore") (line 36-39). When the Rust extension is absent, pytest cannot load conftest.py and silently skips all test collection in the directory, including this file. The tests never run, so…

### `tests/test_hexcore_e2e/test_hexpat_control_flow.py`

- **All tests in file** (line 1) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: Same conftest.py module-level importorskip issue. This file tests while, for, match, try/catch, break, continue control-flow statements via HexPatInterpreter.execute_bytes (pure Python). Regressions in any control-flow logic would silently skip instead of fail when intellicrack_hexcore is absent.
  - Fix: Relocate to tests/test_hexpat/ or restructure conftest.py to scope importorskip only to Rust-dependent fixtures.
  - Independent check: Confirmed. conftest.py lines 36-39 execute `pytest.importorskip('intellicrack_hexcore')` at module level. When the Rust extension is absent, this raises `Skipped` during conftest import, which causes pytest to skip every test in `tests/test_hexcore_e2e/`. The test file `test_hexpat_control_flow.py` imports only `HexPatInterpreter` (pure Python, no hexcore dependency), yet all its tests are masked by the conftest ski…

### `tests/test_hexcore_e2e/test_hexpat_data_reader.py`

- **All tests in file** (line 1) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: conftest.py module-level importorskip prevents collection when intellicrack_hexcore is absent. This file only imports DataReader and HexPatRuntimeError (pure Python) and tests typed read methods (read_u8, read_u16, read_u32, read_u64, read_f32, read_f64, read_str, read_wstr). Endianness bugs, out-of-bounds handling, type-coercion regressions would skip rather than fail.
  - Fix: Relocate to tests/test_hexpat/ or refactor conftest.py to not apply module-level importorskip to pure-Python-only test files.
  - Independent check: Confirmed. Same conftest.py module-level `importorskip` issue as G0223. `test_hexpat_data_reader.py` imports only `DataReader` and `HexPatRuntimeError` from pure-Python modules (`data_reader.py`, `errors.py`), neither of which depends on `intellicrack_hexcore`. Despite this, the directory-level conftest skip suppresses every test when hexcore is absent. Endianness bugs, OOB handling, and type-coercion regressions in…

### `tests/test_hexcore_e2e/test_hexpat_evaluator.py`

- **All tests in file** (line 1) - `skip-masks-failure` / **high** - _[verified accurate]_
  - Why it is not a gate: conftest.py module-level importorskip("intellicrack_hexcore") skips the entire directory when Rust is absent. This file tests HexPatInterpreter.execute_bytes (pure Python) with comprehensive oracle strategies (value-oracle guards and offset-plus-byte oracles). Tests cover arithmetic, bitwise, comparison, logical, ternary, type-coercion, and scoping evaluator operations. Regressions in any operator would skip instead of fail when the Rust extension is unavailable.
  - Fix: Relocate to tests/test_hexpat/ or refactor conftest.py to only guard Rust-dependent fixtures, not module-level collection.
  - Independent check: Confirmed. Same conftest.py module-level `importorskip` as G0223/G0224. `test_hexpat_evaluator.py` imports only `HexPatRuntimeError` and `HexPatInterpreter` (both pure Python). The file explicitly states it drives the full interpreter pipeline (preprocessor → lexer → parser → evaluator) using `execute_bytes`, which has no hexcore dependency. All arithmetic, bitwise, comparison, logical, ternary, and scoping tests ar…

### `tests/test_hexcore_e2e/test_hexpat_preprocessor.py`

- **TestIncludeResolution::test_pragma_once_prevents_double_include** (line 198) - `other` / **high** - _[verified accurate]_
  - Why it is not a gate: The test (lines 204-210) verifies that including a file with `#pragma once` twice results in `once_field` appearing only once. However, the production code (preprocessor.py lines 387-396) adds every successfully resolved file to `_included_files` on first inclusion (line 390), then separately checks for and adds to `_pragma_once_files` (line 396). The guard on line 387 `if resolved_str in self._pragma_once_files or resolved_str in self._included_files:` blocks the second include via the `_included_files` branch regardless of whether the pragma_once detection code (line 395-396) is executed. Removing the `#pragma once` detection entirely would still leave the test passing because `_included_files` alone prevents the second inclusion. The `#pragma once` mechanism itself is not distinguishably tested.
  - Fix: Use two separate `HexPatPreprocessor` instances (which reset `_included_files` and `_pragma_once_files` on __init__ line 116), or manually reset `_included_files` to empty between the two includes. Then verify that: (1) without `#pragma once`, a second include produces duplicate content; (2) with `#pragma once`, the second include is suppressed. This isolates the pragma_once behavior from the general include-once tracking.
  - Independent check: Confirmed. The test (line 204-210) includes `once.hexpat` twice and asserts `result.count('once_field') == 1`. However, `preprocessor.py` line 390 adds `resolved_str` to `self._included_files` on the first successful resolution. The guard at line 387 checks `resolved_str in self._pragma_once_files or resolved_str in self._included_files` — so on the second include, the `_included_files` branch alone would return `No…

### `tests/test_hexcore_e2e/test_hexpat_stdlib.py`

- **TestIOFunctions::test_io_print_does_not_raise** (line 555) - `no-assertion` / **high** - _[verified accurate]_
  - Why it is not a gate: The test (line 560) calls `getattr(builtin, '_io_print')('test message')` and makes no assertion about the result or side effects. Reading the production code (stdlib.py line 2700-2721), `_io_print` is required to invoke the registered sink callback (if set) by calling `sink(formatted)` on line 2720 and must log the output on line 2718. A production defect where the sink callback is never invoked, or where logging is skipped, would not be caught by this test. The test would pass with a complete no-op body, a stub that returns early, or code that logs but never invokes the sink.
  - Fix: Register a capture sink (follow the pattern in test_format_via_interpreter_produces_string at line 616), call `_io_print('test message')`, and assert the captured list contains the expected string. For example: `captured = []; builtin._PrintSinkRegistry.sink = captured.append; getattr(builtin, '_io_print')('test message'); assert captured == ['test message']`.
  - Independent check: Confirmed. The test at line 555-560 calls `getattr(builtin, '_io_print')('test message')` and makes zero assertions. Production code in `stdlib.py` lines 2700-2721 shows `_io_print` must: (a) log via `_logger.info`, and (b) invoke the registered sink callback via `sink(formatted)`. A no-op body, a stub that returns immediately, or an implementation that logs but never calls the sink would all leave the test green. T…

### `tests/test_hexcore_e2e/test_undo_redo.py`

- **TestModificationTracking::test_is_modified_tracks_through_undo_redo** (line 159) - `no-assertion` / **high** - _[verified accurate]_
  - Why it is not a gate: The test writes data (line 156, asserts is_modified=True), calls undo() (line 159 with no assertion), then calls redo() (line 161, asserts is_modified=True). A broken undo() that fails to pop from the undo_stack or fails to reset modification state would not be caught because there is no assertion verifying is_modified() returned False after undo(). The UndoManager.is_modified() implementation relies on undo_stack.len() being decremented by undo() - if undo() failed to pop the operation, is_modified() would remain True and the test would pass uncaught.
  - Fix: Add `assert not sample_doc_from_bytes.is_modified()` immediately after the undo() call at line 159 to gate the undo path's ability to reset modification state.
  - Independent check: The test writes at line 156 (asserts is_modified=True), calls undo() at line 159 with NO follow-up assertion, then calls redo() at line 161 and asserts is_modified=True at line 162. In the Rust UndoManager (src/intellicrack-hexcore/src/undo.rs line 154-159), is_modified() returns undo_stack.len() != saved_index. After write, undo_stack.len()=1, saved_index=0 => modified=True. After a correct undo(), undo_stack.len()…

### `tests/test_hexcore_e2e/test_bridge_ai_context.py`

- **TestBridgeAIContext::test_get_context_for_ai_contains_expected_top_level_keys** (line 51) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only that five keys (file_path, size, modified, cursor, selection) exist in the returned dict, not their values. If get_document_info() were changed to always return stub values {file_path: None, size: 0, ...} even when a document is loaded, the key-presence assertions would still pass while the functionality is completely broken.
  - Fix: Assert concrete values: ctx['file_path'] == str(pe_binary), ctx['size'] == 1024, ctx['cursor'] == 0, ctx['modified'] == False. The PE fixture always produces a 1024-byte file, providing a deterministic oracle.
  - Independent check: test_get_context_for_ai_contains_expected_top_level_keys at line 51-59 only checks that the five keys (file_path, size, modified, cursor, selection) are present in the returned dict. The `loaded_bridge` fixture opens a real PE binary (1024 bytes). Even if `get_document_info()` were replaced with a stub that always returned `{file_path: None, size: 0, modified: False, cursor: 0, selection: None}`, all five key-presen…

### `tests/test_hexcore_e2e/test_bridge_alignment_color.py`

- **TestColorMode::test_all_color_modes_accepted** (line 328) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test iterates all five valid color modes and only asserts `result is True`. Production `set_color_mode` (hex_editor.py line 8593-8618) validates the mode string against `_VALID_COLOR_MODES` and always returns True for a valid mode. If storage were broken — for example `self._color_mode` was never updated, or was always set to a hardcoded wrong value — the method would still return True and this test would remain green. `test_color_mode_roundtrip` covers read-back for one mode ('byte_value'), and `test_set_color_mode_persists_and_notifies` covers storage and event dispatch for 'entropy', but no test verifies read-back for all five modes. This test therefore only gates that valid modes do not raise an exception and return True, not that they are persistently stored.
  - Fix: After each `set_color_mode(mode)` call, add `assert _run(bridge.get_color_mode()) == mode` to confirm the mode is actually persisted. This converts the loop into a round-trip gate for every supported mode value.
  - Independent check: The test calls set_color_mode for all 5 modes and only asserts `result is True`. Production set_color_mode at line 8614 of hex_editor.py does `self._color_mode = mode` then returns True. If storage were silently broken (e.g., the assignment were a no-op or wrote a wrong value), the return value would still be True and the test would stay green. Only test_color_mode_roundtrip (one mode, 'byte_value') and test_set_col…

### `tests/test_hexcore_e2e/test_bridge_arithmetic.py`

- **TestArithmeticEdgeCases::test_returns_operation_metadata** (line 223) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that the returned dict has the correct keys and that `result['operation'] == 'not'` and `result['length'] == 4`. Looking at production `apply_arithmetic_to_selection` (hex_editor.py lines 5759-5762), the return value `{'offset': start, 'length': length, 'operation': operation}` is constructed entirely from the input arguments, not from the transformed data. If the NOT transform were silently replaced with an identity no-op (bytes unchanged), the metadata dict would still be `{'offset': 0, 'length': 4, 'operation': 'not'}` and all assertions in this test would pass. The actual byte-inversion correctness is separately covered by `test_not_selection` (lines 136-144), which uses `_setup_and_apply` and asserts the result bytes equal `bytes([0xFF, 0x00, 0x55, 0xAA])`. This test therefore only gates metadata shape, not the NOT transform itself.
  - Fix: After `apply_arithmetic_to_selection('not')`, read back the bytes with `_run(bridge.read_bytes(0, 4))` and assert they equal `bytes([0x55, 0x44, 0x33, 0x22])` (bitwise NOT of 0xAA 0xBB 0xCC 0xDD). This makes the test a real gate for the NOT transform rather than a pure metadata shape check.
  - Independent check: The test asserts only metadata shape and values (`result['operation'] == 'not'`, `result['length'] == 4`). Production apply_arithmetic_to_selection returns `{'offset': start, 'length': length, 'operation': operation}` (hex_editor.py lines 5759-5762) constructed directly from input arguments, not from transformed byte output. Breaking the NOT transform (bytes unchanged, identity no-op) leaves the metadata dict intact…

### `tests/test_hexcore_e2e/test_bridge_disassembly_deep.py`

- **TestDisassemblePeTextSection::test_disassemble_pe_text_count_4_returns_up_to_4** (line 81) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The assertion `1 <= len(results) <= 4` only checks a range, not the exact count. The fixture is designed with exactly 4 INT3 bytes (0xCC each) at _PE_TEXT_OFFSET, and count=4 is requested. Each INT3 is a 1-byte instruction, so exactly 4 should be returned. A broken disassembler that stops after processing 1 byte (returning only 1 result) would pass this range check. The test provides no oracle to verify the count should be exactly 4, not just within a range.
  - Fix: Change assertion from `assert 1 <= len(results) <= 4` to `assert len(results) == 4` since the input fixture (4 INT3 bytes at a known offset) deterministically produces exactly 4 decodable instructions.
  - Independent check: The test at lines 74-81 asserts 1 <= len(results) <= 4 for disassembling 4 INT3 bytes (1 byte each) with count=4. The PE fixture places exactly 4 x 0xCC bytes at _PE_TEXT_OFFSET=0x200. A broken disassembler that decoded only 1 instruction (stopping after 1 byte) would return len=1, which satisfies 1 <= 1 <= 4 and the test would still pass. The range is too wide to gate the count-limiting behavior. No sibling test in…

### `tests/test_hexcore_e2e/test_bridge_encoding_decoding.py`

- **TestListEncodings::test_list_encodings_returns_nonempty_list** (line 218) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only non-emptiness without validating any content. A stub returning [{'name': '', 'label': ''}] or any malformed dict would pass. Companion tests (line 251-271) validate specific names like 'utf-8' and 'ascii', but this test in isolation is a format check that does not gate the actual encoding catalog correctness.
  - Fix: Assert that the result is non-empty AND includes at least two canonical encodings by name (e.g., assert any(e['name'].lower() in ['utf-8', 'ascii'] for e in result)), or remove this test as redundant if relying on companion tests for gating.
  - Independent check: test_list_encodings_returns_nonempty_list (line 218) only asserts isinstance(result, list) and truthiness. The bridge fixture has no document open so list_encodings() hits the Python fallback (hex_editor.py:6832-6849) which always returns a hardcoded 14-entry list. A stub returning [{'name': '', 'label': ''}] or any non-empty list would pass. Content correctness is gated elsewhere by test_list_encodings_contains_utf…

- **TestListEncodings::test_list_encodings_standalone_bridge** (line 330) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only isinstance(result, list) and truthiness. A stub returning [{'name': 'dummy', 'label': 'Dummy'}] on a fresh bridge would pass. The fallback at line 6832-6849 returns 14 real encodings, but this test does not validate that any specific or canonical encodings are present.
  - Fix: Assert that the returned list contains the Python fallback set of canonical codecs (e.g., assert len(result) > 10 and any('utf-8' in e['name'].lower() for e in result)) to gate the actual encoding availability.
  - Independent check: test_list_encodings_standalone_bridge (line 330) creates a fresh HexEditorBridge(), calls initialize(), then list_encodings() with no document open (self.document is None). This routes to the hardcoded Python fallback at hex_editor.py:6832-6849. The test asserts only isinstance(result, list) and result (truthiness). Any non-empty list passes. A stub returning [{'name': 'dummy', 'label': 'Dummy'}] would satisfy both …

### `tests/test_hexcore_e2e/test_bridge_hash_advanced.py`

- **TestCalculateHashRange::test_md5_range_differs_from_sha256_range** (line 132) - `other` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only that md5 and sha256 produce different results. If both paths were broken and returned wrong-but-distinct values (e.g., md5 path returns sha1, sha256 returns sha384), this test would still pass. Neither hash is compared against an independent oracle. Companion tests (line 144-155, 169-180) validate individual hashes, but this test uses mutual inequality as a proxy for correctness, which is insufficient.
  - Fix: Remove this test and rely on the companion tests (line 144-155, 169-180, 182-194) that each validate a hash against its hashlib reference value. If this test must remain, validate each hash independently rather than via mutual inequality.
  - Independent check: test_md5_range_differs_from_sha256_range (line 132) asserts only sha256_result != md5_result. If both algorithm paths were broken but returned different (wrong) values—e.g., sha256 path returns sha1 output and md5 path returns crc32 output—the test still passes. Neither result is compared against an independent oracle. This is a mutual-inequality proxy that fails to catch correct-looking-but-wrong individual results…

- **TestCalculateHashCustomCrc::test_crc32_result_is_8_hex_chars** (line 342) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only len(result) <= 8. A stub always returning '00000000' would pass. The actual CRC-32 value over bytes(range(64)) is never compared against binascii.crc32(). Companion tests (line 252-266, 268-283) validate content, but this test in isolation gates only length.
  - Fix: Compare against binascii: crc_val = binascii.crc32(bytes(range(64))) & 0xFFFFFFFF, expected = f'{crc_val:08x}', then assert result.lower() == expected.lower(), to gate CRC-32 computation correctness.
  - Independent check: test_crc32_result_is_8_hex_chars (line 342) asserts only len(result) <= 8. The actual CRC-32 value over bytes(range(64)) is never compared to binascii.crc32(). A stub always returning '00000000' or any 1-8 char hex string would pass. The companion tests at lines 252-266 and 268-283 validate actual CRC-32 values against binascii, but this test gates only length, not value correctness.

### `tests/test_hexcore_e2e/test_bridge_html_export_largefile.py`

- **TestHTMLExport::test_html_export_returns_valid_html** (line 50) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: Asserts only that the string contains '<!DOCTYPE html>' and '</html>'. These are emitted by the static _html_header() method (line 3442-3464) regardless of whether _render_hex_rows() executed or is broken. If _render_hex_rows() were removed or returned garbage, the header/footer would still be present and this test would pass. The companion test at line 64-78 validates actual hex data rendering, but this test does not gate content rendering.
  - Fix: Assert that the exported HTML contains at least one hex byte from the test payload: assert 'AA' in html and 'BB' in html (for the b'\xaa\xbb\xcc\xdd' data), to verify the rendering path actually executed.
  - Independent check: test_html_export_returns_valid_html (line 50) checks only '<!DOCTYPE html>' in html and '</html>' in html. In production, export_annotated_html() unconditionally calls self._html_header() (which starts with '<!DOCTYPE html>') and appends '</body></html>' regardless of whether _render_hex_rows() executed or is broken. If _render_hex_rows() were deleted or raised an exception that was suppressed, the header and footer…

### `tests/test_hexcore_e2e/test_bridge_new_capabilities.py`

- **TestProcessMemoryBridge::test_list_process_regions_returns_list** (line 672) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts the presence of keys (base_address, size, protection, state) but never validates their numeric values. The production list_process_regions (line 8121) calls _hexcore_mod.HexDocument.list_process_memory_regions(pid) and unpacks the result into these 4 fields. If the native binding returned tuples with all 4 zeros (e.g., from a stub), or if the Rust-to-Python conversion lost precision (e.g., truncated addresses), this test would still pass. No assertions check that base_address is non-zero, size is a reasonable page multiple, or that protection/state are valid Windows memory constants.
  - Fix: Add assertions: (1) base_address > 0 for at least some regions, (2) size is a multiple of 0x1000 (4KB page size), (3) protection is a known Windows constant (0x02, 0x04, 0x20, 0x40), (4) state is either 0x1000 (MEM_COMMIT) or 0x2000 (MEM_RESERVE).
  - Independent check: The test at line 672 uses the real hexcore native binding (pytest.importorskip enforces it) and calls bridge.list_process_regions(os.getpid()) against the current process. It asserts: isinstance(regions, list), regions (non-empty), and then only checks key presence in regions[0]: 'base_address', 'size', 'protection', 'state'. The production code at hex_editor.py:8123 maps Rust tuple elements r[0]..r[3] to these keys…

### `tests/test_hexcore_e2e/test_bridge_pattern_engine.py`

- **TestCompilePattern::test_compile_simple_struct_returns_nonempty_string** (line 67) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that the result is a non-empty string (isinstance and truthiness). The sibling test_compile_simple_struct_result_is_valid_json (line 78) checks JSON validity separately. This test adds no gate: a stub that returns 'E' or '{"error":"stub"}' would pass. If compile_pattern is broken and returns any non-empty string, the test stays green.
  - Fix: Remove this test and rely only on test_compile_simple_struct_result_is_valid_json, which is semantically stronger. Alternatively, assert that json.loads(result) succeeds in THIS test so the gate is in one place.
  - Independent check: test_compile_simple_struct_returns_nonempty_string (line 67) calls real bridge.compile_pattern() and asserts only isinstance(result, str) and bool(result). The sibling test_compile_simple_struct_result_is_valid_json (line 78) does the json.loads() check. A broken compile_pattern returning any non-empty garbage string (e.g., 'E') would pass this test, so it adds no structural gate over the sibling. Category weak-exis…

- **TestExecutePattern::test_execute_u32_field_returns_field_list** (line 135) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(fields, list) and truthiness. Companion tests (test_execute_u32_field_has_correct_size and test_execute_u32_field_has_correct_offset) verify the name, size, and offset separately. This test adds no gate: a stub returning [{'name': 'junk', 'offset': 999, 'size': 99}] would pass. The production execute_pattern (line 7253) calls interpreter.execute(), and if the interpreter is broken and returns wrong field data, this test would still pass.
  - Fix: Remove this test and consolidate into test_execute_u32_field_has_correct_size or test_execute_u32_field_has_correct_offset, which are semantically stronger. Or assert specific values in THIS test: `fields[0]['name'] == 'value'`, `fields[0]['offset'] == 0`, `fields[0]['size'] == 4`.
  - Independent check: test_execute_u32_field_returns_field_list (line 135) calls execute_pattern and checks only isinstance(fields, list) and assert fields. The sibling tests test_execute_u32_field_has_correct_size (line 148) and test_execute_u32_field_has_correct_offset (line 161) pin name='value', size=4, and offset=0 via next(f for f in fields if f['name'] == 'value'). A stub returning [{'name': 'junk', 'offset': 999, 'size': 99}] pas…

- **TestExecutePattern::test_execute_struct_with_multiple_fields_returns_multiple_results** (line 174) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test constructs all_names from the field list and asserts `'magic' in all_names or 'header' in all_names`. The OR operator is the critical flaw: since struct execution wraps the struct in a parent node named 'header', even a stub that returns only [{'name': 'header', 'children': []}] (with no actual 'magic' field parsed) would satisfy the OR branch. The multi-field parsing capability (correctly expanding both the magic and value fields inside the struct) is not verified.
  - Fix: Change the assertion to require BOTH fields: `assert 'magic' in all_names and 'header' in all_names`, or navigate to the 'header' entry, extract its children, and verify that 'magic' appears in the children's names.
  - Independent check: test_execute_struct_with_multiple_fields_returns_multiple_results (line 174) collects all_names from top-level name fields plus children, then asserts 'magic' in all_names or 'header' in all_names. Since the production interpreter wraps Header in a struct node named 'header', even a stub that returns [{'name': 'header', 'children': []}] (zero children, no magic field parsed) satisfies the OR branch 'header' in all_n…

- **TestListAndAutoDetect::test_auto_detect_with_pe_file_returns_list** (line 345) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(result, list). An empty list passes. The test uses loaded_bridge, which has a real PE file (from conftest) already open. If the auto_detect_pattern implementation is broken (e.g., returns empty or garbage), this test would still pass. The test never verifies that at least one PE-related pattern (e.g., 'pe' or 'mz') is detected, which would be expected for a valid PE file.
  - Fix: Assert that len(result) > 0 and that at least one entry's name or description contains 'pe' or 'mz', verifying that the PE magic was actually detected.
  - Independent check: test_auto_detect_with_pe_file_returns_list (line 345) uses loaded_bridge (PE binary opened in conftest) and asserts only isinstance(result, list). An empty list [] passes. The sibling test_auto_detect_with_no_document_raises_runtime_error (line 336) checks the no-document error path, but no sibling verifies that PE magic-byte auto-detection actually finds any patterns. A broken auto_detect_pattern returning [] would…

### `tests/test_hexcore_e2e/test_bridge_search.py`

- **TestBridgeSearchHex::test_search_hex_returns_list** (line 43) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test only validates that search_hex returns a list type via isinstance check. It does not verify the list is non-empty, contains correct results, has accurate offset/length values, or that the search actually found matches in the PE file. An implementation that returned an empty list [] or a list of dicts with arbitrary/incorrect offset and length values would pass this test. The test provides no gate against broken search functionality.
  - Fix: Add assertion `assert len(results) > 0` to verify non-empty results. Better yet, rely on the dedicated tests test_search_hex_finds_mz_signature_at_offset_zero and test_search_hex_result_length_matches_pattern_byte_count which already enforce correctness, and consider removing this redundant type-check-only test or consolidating it with a more comprehensive test.
  - Independent check: The test at line 43-50 calls `loaded_bridge.search_hex('4D 5A')` and only asserts `isinstance(results, list)`. An empty list `[]` satisfies this assertion. A broken implementation that returns an empty list or a list of garbage dicts would still pass. The real gates are in sibling tests: `test_search_hex_finds_mz_signature_at_offset_zero` (line 64) checks offset==0 is present and `test_search_hex_result_length_match…

- **TestBridgeSearchHex::test_search_hex_result_items_have_offset_and_length** (line 52) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that 'offset' and 'length' keys are present in result dicts (line 61-62) and that results is non-empty (line 59). It does not validate the actual values of those keys. An implementation returning [{"offset": -1, "length": 999}] or [{"offset": 0, "length": 0}] would pass this test. The real production capability—computing correct byte offsets and match lengths for a hex search—is not exercised by this key-presence assertion.
  - Fix: Add value-level assertions: `assert item["offset"] >= 0 and assert item["length"] > 0`. Better yet, consolidate with test_search_hex_finds_mz_signature_at_offset_zero (which checks offset==0) and test_search_hex_result_length_matches_pattern_byte_count (which checks length==2), and remove this key-presence-only test as redundant.
  - Independent check: The test at line 52-62 calls `search_hex('4D 5A')`, asserts `results` is non-empty (line 59), and then checks only that 'offset' and 'length' keys exist in each item (lines 61-62). Values are not validated. A stub returning `[{'offset': -1, 'length': 999}]` would pass every assertion. The actual correctness of offsets and lengths is gated by sibling tests: `test_search_hex_finds_mz_signature_at_offset_zero` checks `…

- **TestBridgeSearchRegex::test_search_regex_finds_pattern_match** (line 193) - `weak-existence` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that results is truthy (line 205), which requires a non-empty list. It does not validate that the regex pattern [A-Z]{3} was actually matched correctly against the payload b"ABC123DEF456". An implementation that returned hardcoded [{"offset": 0, "length": 1}] for any input, or returned only the first match at offset 0, would pass this test. The test does not verify correct pattern matching, match length, or the presence of the second match (DEF at offset 6).
  - Fix: Add concrete assertions: `assert results[0]["offset"] == 0 and assert results[0]["length"] == 3`. Better yet, add a second assertion that offset 6 is present in results to verify the second [A-Z]{3} match (DEF) is found. This ensures the regex engine is functioning, not just returning dummy non-empty results.
  - Independent check: The test at line 193-205 writes `b'ABC123DEF456'`, opens the file, calls `search_regex(r'[A-Z]{3}')`, and only asserts `results` is truthy (non-empty list). It does not verify the count of matches, their offsets, or their lengths. An implementation returning `[{'offset': 0, 'length': 1}]` for any input with any matches would pass. The sibling test `test_search_regex_no_match_returns_empty_list` (line 207) only gates…

### `tests/test_hexcore_e2e/test_bridge_transforms.py`

- **TestBridgeApplyPipeline::test_apply_pipeline_with_single_xor_step** (line 156) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: Assertions only check isinstance(result, str) and len(result)==8. XOR of 0xFF with key 0xAA must produce 0x55 bytes, so correct output is '55555555'. A broken pipeline returning any 8-char hex string (e.g. 'deadbeef', 'XXXXXXXX', or any wrong bytes) would still pass. The test does not verify the actual XOR computation is correct.
  - Fix: Assert the exact expected output: `assert result == '55555555'`. This matches the pattern of test_apply_transform_xor_single_known_output (line 127 in same file) and test_single_xor_step_known_output (line 120 in test_bridge_transforms_deep.py), which do verify exact output.
  - Independent check: The defect is real: `test_apply_pipeline_with_single_xor_step` only asserts `isinstance(result, str)` and `len(result) == 8`. XOR of 0xFF with key 0xAA yields 0x55 bytes ('55555555'), but any wrong 8-char hex string (e.g., 'deadbeef') passes. Category weak-existence and severity medium are correct. However, the cited line 178 points at the middle of the test body (`result: str = _run(...)`); the test definition begi…

### `tests/test_hexcore_e2e/test_bridge_transforms_deep.py`

- **TestApplyPipelineSingleStep::test_single_xor_step_returns_hex_string** (line 84) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: Only asserts len(result)==8. XOR of 0xFF with 0xAA must produce 0x55 bytes. Expected output: '55555555'. A broken XOR implementation returning any wrong 8-char hex string would pass silently. The actual byte computation is not verified.
  - Fix: Assert exact output: `assert result == '55555555'`. The complementary test test_single_xor_step_known_output (line 103+, which XORs 0xFF with 0xFF) does verify exact output ('00000000'), establishing the pattern for this test to follow.
  - Independent check: The defect is real: `test_single_xor_step_returns_hex_string` asserts only `len(result) == 8`. XOR of 0xFF with 0xAA produces 0x55 bytes ('55555555'), but any wrong 8-character hex string passes silently. Category weak-existence and severity medium are correct. Sibling test `test_single_xor_step_known_output` (line 103) provides the real gate via `assert result == '00000000'`, but that is a different test. The cited…

- **TestApplyPipelineMultiStep::test_three_step_pipeline_produces_output** (line 199) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: Only asserts len(result)==8. Three-step XOR with keys 0xFF, 0x0F, 0xF0 combined equals 0xFF^0x0F^0xF0=0x00, so result for input 0x00112233 should be '00112233' unchanged. A broken implementation returning any 8-char hex string would pass silently.
  - Fix: Precompute expected output from keys and assert exactly. For this test: keys combine to XOR(0x00), so output equals input '00112233'. Assert `assert result == '00112233'`. The test_apply_pipeline_three_steps_with_known_verification (line 437+) verifies exact output for a similar scenario, establishing this as the expected pattern.
  - Independent check: The defect is real: `test_three_step_pipeline_produces_output` asserts only `len(result) == 8`. The XOR arithmetic note in the claim is slightly off (0xFF^0x0F^0xF0 = 0x00, not 0x00 cancelling to original; result for 0x00112233 is 0x001122 XOR chain applied byte-by-byte, producing 0x001122... nonetheless), but the core gap holds: any wrong 8-char hex string passes. Category weak-existence and severity medium are cor…

- **TestApplyPipelineMultiStep::test_pipeline_result_length_matches_input_length** (line 222) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: Only asserts len(result)==byte_count*2. Input is bytes(range(16)) XOR'd with 0x55. Expected output is fully determined: each byte b becomes (b^0x55). A broken pipeline returning any 32-char hex string (wrong byte values, wrong order, etc.) would pass silently. The actual computation is not verified.
  - Fix: Precompute and assert exact output. For input bytes(range(16)) XOR with 0x55: expected is bytes(b^0x55 for b in range(16)). Assert `assert result == binascii.hexlify(bytes(b ^ 0x55 for b in range(16))).decode('ascii')` to verify actual byte values, matching the pattern in other tests like test_apply_pipeline_range_preserves_structure.
  - Independent check: The defect is real: `test_pipeline_result_length_matches_input_length` asserts only `len(result) == byte_count * 2`. Input is `bytes(range(16))` XOR'd with 0x55; expected output is fully determined (each byte b gives b^0x55), but any 32-char hex string with wrong values passes silently. Category weak-existence and severity medium are correct. The cited line 240 points at `assert len(result) == byte_count * 2`; the t…

### `tests/test_hexcore_e2e/test_bridge_yara.py`

- **TestBridgeYaraScanFiles::test_yara_scan_files_with_rule_file_matches_pe** (line 166) - `weak-existence` / **medium** - _[RECLASSIFIED]_
  - Why it is not a gate: After the isinstance check, the only substantive assertion is `assert results` (truthiness). yara_scan_files could return a list of malformed dicts with wrong rule names, wrong offsets, or other corrupt content and this test passes. A critical scenario - that yara_scan_files correctly loads the .yar file from disk instead of using cached/wrong results - is not gated. The test does not verify that the rule name is 'MZHeader' or that any string hit appears at offset 0.
  - Fix: Add assertions that match the rule name ('MZHeader') and verify at least one string hit at offset 0, mirroring test_yara_scan_returns_list. This closes the gate on file-loading behavior and ensures the correct rule was applied.
  - Independent check: The finding category (weak-existence) and severity (medium) are accurate — `assert results` at line 166 only checks truthiness and would pass even if matches contained garbage content like wrong rule names or corrupt offsets. However, the cited line 164 is wrong: line 164 is the `_run(loaded_bridge.yara_scan_files(...))` call expression, not the weak assertion. The weak assertion `assert results` is at line 166 (and…

### `tests/test_hexcore_e2e/test_hex_document_state.py`

- **TestThreadSafety::test_concurrent_set_cursor_does_not_raise** (line 1167) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only assert not errors where errors collects only RuntimeError instances. The test catches synchronization exceptions but makes no assertion about the final cursor_offset value. A data race that silently corrupts _cursor_offset (e.g., torn write, lost update) would NOT raise RuntimeError and the test would pass green. The auditor is correct: this test does not gate correctness of concurrent state, only that no exception is raised.
  - Fix: After threads finish, add an assertion: assert state.cursor_offset in range(50) to verify the final cursor value is one of the written values, not a torn-write garbage value. Also catch all Exception types, not just RuntimeError.
  - Independent check: The test at lines 1148-1167 runs 50 threads calling state.set_cursor(i). Each thread only appends to errors if a RuntimeError is caught. The single assertion is 'assert not errors'. A data race that silently corrupts _cursor_offset (e.g., a lost write from a torn update) would not raise RuntimeError and the test would pass green. No assertion checks the final cursor_offset value or that all 50 set_cursor calls produ…

- **TestThreadSafety::test_unregister_while_concurrent_notify_does_not_raise** (line 1199) - `no-assertion` / **medium** - _[verified accurate]_
  - Why it is not a gate: The test asserts only assert not errors where errors catches only RuntimeError. The test verifies no exception is raised during concurrent unregister and set_cursor calls, but makes no assertion about actual state correctness or whether callbacks were safely handled. If the thread-safe list snapshot mechanism in _dispatch_one were removed (a real regression), the test could still pass as long as no RuntimeError is raised. The auditor is correct that the test does not gate thread-safe correctness.
  - Fix: After threads join, add assertions: (1) verify state.cursor_offset is a valid written value (in range 0-99), (2) verify the callback was actually invoked the expected number of times. Also catch all Exception types in thread funcs.
  - Independent check: The test at lines 1169-1199 spawns two threads: one spamming state.set_cursor(i) 100 times, one calling state.unregister_callback(cb). Both only append to errors on RuntimeError. The single assertion is 'assert not errors'. If the _dispatch_one snapshot mechanism were removed and a callback were called after unregistration, this would not raise RuntimeError (it would be a silent logic error or AttributeError at most…

### `tests/test_hexcore_e2e/test_bridge_ai_context.py`

- **TestBridgeAIContext::test_get_context_for_ai_bookmarks_is_list** (line 77) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Only checks isinstance(ctx['bookmarks'], list), not the structure or content of bookmark entries. If _populate_ai_bookmarks() inserted an empty list or failed to serialize bookmark fields (offset, length, label), this test would still pass. The companion test test_get_context_for_ai_bookmarks_contain_expected_fields_when_present covers values, but this test alone is insufficient.
  - Fix: On a fresh document with no bookmarks, assert ctx['bookmarks'] == [] and ctx['bookmark_count_total'] == 0 to gate the metadata fields and verify the list is properly initialized.
  - Independent check: test_get_context_for_ai_bookmarks_is_list at line 77-85 asserts only `isinstance(ctx['bookmarks'], list)`. An empty list or a list of wrong-typed items would satisfy this check. The bookmark content correctness (offset, length, label fields) is only covered by the companion test `test_get_context_for_ai_bookmarks_contain_expected_fields_when_present` (line 87). This test alone would pass even if `_populate_ai_bookma…

- **TestBridgeAIContext::test_get_context_for_ai_size_is_positive** (line 106) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only isinstance(ctx['size'], int) and ctx['size'] > 0. Any positive integer satisfies this check. If get_document_info() returned a hardcoded sentinel size (e.g., 1) instead of calling self.document.length(), the test would still pass despite the actual document being 1024 bytes.
  - Fix: Assert the exact document size: assert ctx['size'] == 1024. The _build_pe_binary() fixture always produces a deterministic 1024-byte buffer, providing an oracle independent of the bridge's internal document state.
  - Independent check: test_get_context_for_ai_size_is_positive at line 106-114 asserts `isinstance(ctx['size'], int) and ctx['size'] > 0`. The `loaded_bridge` fixture opens a 1024-byte PE binary (from `_build_pe_binary()` which creates `bytearray(1024)`). If `get_document_info()` (lines 4952-4975) were changed to return a hardcoded sentinel like `size: 1` instead of calling `self.document.length()`, the assertion `ctx['size'] > 0` would …

### `tests/test_hexcore_e2e/test_bridge_alignment_color.py`

- **TestSnapToAlignment::test_snap_to_alignment_512** (line 129) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: The assertion at line 129 (`assert nearer == ceil_boundary`) only validates the test's own local conditional. `nearer` is assigned `ceil_boundary` when `(1000 - 512) > (1024 - 1000)` evaluates to True (488 > 24), which is an arithmetic constant — this can never fail regardless of what the production bridge does. The production `snap_to_alignment` at line 5977 of hex_editor.py uses `(self._cursor_offset - floor_offset) * 2 >= alignment_size`, which for this specific input yields the same result, but the test assertion at line 129 does not call the bridge at all. A production breakage (e.g., snap always returns `floor_offset`) would not affect line 129; lines 133-135 catch that instead.
  - Fix: Remove the inline oracle computation at lines 127-129. The docstring comment 'cursor at 1000, boundaries 512 and 1024, 1024 is nearer' already documents intent. The actual production gate is the three assertions at lines 133-135 which compare bridge output against `ceil_boundary`.
  - Independent check: Line 129 asserts `nearer == ceil_boundary` where `nearer` is assigned purely by the test's own arithmetic constant `ceil_boundary if (1000-512) > (1024-1000) else floor_boundary`. The condition 488 > 24 is always True regardless of what the bridge does, so this assertion can never fail due to a production breakage. The actual bridge gate is on lines 131-135 where `_run(bridge.snap_to_alignment(512))` is called and c…

### `tests/test_hexcore_e2e/test_bridge_disassembly.py`

- **TestBridgeDisassembly::test_disassemble_result_items_have_required_keys** (line 119) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only asserts that _EXPECTED_INSN_KEYS = {address, bytes, mnemonic, operands, size} is a subset of each instruction dict's keys. A regression where the disassembler returns dicts with the correct keys but all empty/zero values (e.g., mnemonic='', size=0, bytes='', address=0, operands='') would pass this test. The test provides no assertion of actual values. However, this is mitigated by test_disassemble_matches_independent_capstone_decode (line 55) and test_disassemble_int3_mnemonic (line 90) which DO verify field values against an oracle. But those tests could be disabled or skipped while this weak gate stays green.
  - Fix: Remove test_disassemble_result_items_have_required_keys entirely since test_disassemble_matches_independent_capstone_decode already gates correctness with full field-value comparison, or add concrete value assertions (mnemonic == 'nop', size == 1) to this test.
  - Independent check: The test at lines 105-119 uses a NOP-filled payload and asserts only that _EXPECTED_INSN_KEYS = {'address','bytes','mnemonic','operands','size'} is a subset of each instruction dict's keys. A stub returning dicts with all correct keys but empty/zero values (mnemonic='', address=0, bytes='', operands='', size=0) would satisfy this assertion. The test provides no oracle for field values. While sibling tests (test_disa…

### `tests/test_hexcore_e2e/test_bridge_disassembly_deep.py`

- **TestDisassemblePeTextSection::test_disassemble_pe_text_all_required_keys_present** (line 101) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Identical weakness as the analogous test in test_bridge_disassembly.py: only checks key presence (keys.issubset) without verifying field values. A stub returning dicts with empty/zero values for address, bytes, mnemonic, operands, size would pass. Mitigated only if test_disassemble_pe_text_int3_mnemonic (line 55) remains enabled, but that single test does not gate all field values (only mnemonic).
  - Fix: Remove this redundant test and rely on test_disassemble_pe_text_int3_mnemonic which asserts the actual mnemonic value, or add concrete assertions for all fields (address == _PE_TEXT_OFFSET, bytes == 'cc', mnemonic == 'int3', size == 1).
  - Independent check: The test at lines 93-102 asserts _EXPECTED_INSN_KEYS.issubset(insn.keys()) for each instruction decoded from the PE .text section (4 INT3 bytes). This only checks that the five required keys are present in each dict; it does not check any field values. A stub returning dicts with all five keys mapping to empty/zero values (address=0, bytes='', mnemonic='', operands='', size=0) would pass. Sibling test test_disassemb…

### `tests/test_hexcore_e2e/test_bridge_document_info.py`

- **TestDocumentInfoNoDocument::test_no_document_all_required_keys_present** (line 101) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that _REQUIRED_KEYS (file_path, size, modified, cursor, selection) is a subset of the returned dict's keys. It does not verify the values. A broken get_document_info() could return {file_path: 0, size: -1, modified: None, cursor: 'invalid', selection: 123} and still pass. This is weaker than the per-field tests (test_no_document_file_path_is_none, test_no_document_size_is_zero, etc.) which directly verify the expected values.
  - Fix: Remove this redundant key-presence gate since the per-field tests already enforce correct values (file_path==None, size==0, modified==False, cursor==0, selection==None).
  - Independent check: The test `test_no_document_all_required_keys_present` at line 94-101 only asserts `_REQUIRED_KEYS.issubset(info.keys())` - checking that keys {file_path, size, modified, cursor, selection} exist in the returned dict, without verifying any values. The production code at hex_editor.py lines 4959-4966 always returns a hard-coded dict with exactly those five keys, so this assertion can never fail under any realistic bre…

- **TestDocumentInfoWithFile::test_all_required_keys_present_with_file** (line 185) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only key presence (issubset check) after opening a file, without verifying field values. A broken implementation returning {file_path: None, size: 0, modified: True, cursor: None, selection: []} would pass despite failing to track the opened file's properties. The per-field tests (test_open_pe_file_path_matches, test_open_pe_size_is_1024, etc.) already gate the actual values.
  - Fix: Remove this redundant key-presence test or fold it into the per-field tests. The individual field assertions already gate correctness completely.
  - Independent check: The test `test_all_required_keys_present_with_file` at line 176-185 only asserts `_REQUIRED_KEYS.issubset(info.keys())` after opening a file. The production `get_document_info()` at hex_editor.py lines 4968-4974 always returns a dict with exactly those five keys, so this key-presence check cannot detect any value regression (e.g., returning None for file_path, 0 for size after a real file is opened, etc.). The sibli…

### `tests/test_hexcore_e2e/test_bridge_encoding_decoding.py`

- **TestDecodeText::test_decode_text_returns_string_type** (line 183) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only isinstance(result, str). If decode_text returned 'WRONG', 'X', or any incorrect string, this test would still pass. The companion tests at line 53-212 validate actual decoded content (e.g., 'Hello, World!' == expected text), but this test in isolation is type-only.
  - Fix: Add an assertion validating the specific decoded value: assert result == 'StrType' (matching the payload at line 190), or remove as redundant if relying on the stronger companion tests.
  - Independent check: test_decode_text_returns_string_type (line 183) opens a real file and calls decode_text(0, 7, 'ascii'), then checks only isinstance(result, str). This is a narrow type-contract gate: it would catch a regression where decode_text returns bytes or None, but any str value (including 'WRONG') passes. The Python fallback at hex_editor.py:6789 calls data.decode() which always returns str, making the type invariant very st…

- **TestListEncodings::test_list_encodings_entries_have_name_key** (line 229) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Tests only the presence of the 'name' key, not its value. A stub could return [{'name': '', 'label': ''}] and pass. The companion test at line 273 validates non-empty values, but this test in isolation is weak.
  - Fix: Combine with the non-emptiness check into a single assertion, or remove as redundant if relying on line 273 test for gating value quality.
  - Independent check: test_list_encodings_entries_have_name_key (line 229) iterates over list_encodings() results and asserts only 'name' in entry. A stub returning [{'name': '', 'label': ''}] passes because the key is present regardless of value. The companion test_list_encodings_names_are_nonempty_strings (line 273) validates non-emptiness, and test_list_encodings_contains_utf8_entry (line 251) validates real content. This test gates o…

- **TestListEncodings::test_list_encodings_entries_have_label_key** (line 240) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Tests only the presence of the 'label' key without validating content. A stub could return entries with empty or garbage label values and still pass. The companion test at line 285 validates non-empty values, but this test is weak in isolation.
  - Fix: Combine with the non-emptiness check from line 285 into a single assertion, or remove as redundant if relying on that test for gating.
  - Independent check: test_list_encodings_entries_have_label_key (line 240) asserts only 'label' in entry for every result entry. A stub returning [{'name': 'utf-8', 'label': ''}] or even [{'name': 'x', 'label': None}] (if None were present) could pass the key-presence check. The companion test_list_encodings_labels_are_nonempty_strings (line 285) validates non-empty string values. This test covers only schema shape. Weak-existence/low i…

- **TestListEncodings::test_list_encodings_names_are_nonempty_strings** (line 273) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Validates only that each name is a non-empty string. A stub returning [{'name': 'fake-codec-x', 'label': 'Label X'}] would pass even though the encoding catalog is entirely wrong or contains invalid codec names. Companion tests (line 251-271) validate specific real names, but this test in isolation gates format, not correctness.
  - Fix: Add a spot-check validating that at least one entry's name is in a set of known valid Python codec names, or rely on the companion tests at line 251-271 that check for 'utf-8' and 'ascii' specifically.
  - Independent check: test_list_encodings_names_are_nonempty_strings (line 273) checks isinstance(entry['name'], str) and len(entry['name']) > 0. A stub returning [{'name': 'fake-codec-x', 'label': 'Fake'}] passes because 'fake-codec-x' is a non-empty string, even though no such codec exists and decode_text would raise ValueError with it. Real codec name correctness is gated by test_list_encodings_contains_utf8_entry (line 251) and test_…

- **TestListEncodings::test_list_encodings_labels_are_nonempty_strings** (line 285) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Validates only that each label is a non-empty string. A stub returning [{'name': 'utf-8', 'label': 'x'}] would pass even with a completely wrong label table. The companion test at line 297-329 validates specific label values, but this test in isolation is format-only.
  - Fix: Remove as redundant if the test at line 297-329 is the actual gate, or add a spot-check assertion for at least one known label (e.g., entries['utf-8'] == 'UTF-8').
  - Independent check: test_list_encodings_labels_are_nonempty_strings (line 285) checks isinstance(entry['label'], str) and len(entry['label']) > 0 for every entry. A stub returning [{'name': 'utf-8', 'label': 'x'}] passes because 'x' is a non-empty string. The companion test_list_encodings_with_open_document_is_content_independent (line 297-328) validates specific label values like 'UTF-8', 'EBCDIC Code Page 037', and 'Big5 (Chinese Tra…

### `tests/test_hexcore_e2e/test_bridge_new_capabilities.py`

- **TestProcessMemoryBridge::test_list_process_regions_has_committed_regions** (line 689) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that at least one region has state == 0x1000 (MEM_COMMIT). It does not verify that the enumeration is complete or correct. If the native list_process_memory_regions were broken (e.g., returning only the first enumerated region or a single hardcoded stub region), as long as that one region has MEM_COMMIT, the test passes. The test does not verify that multiple committed regions exist (as any real Python process would have).
  - Fix: Assert that the count of committed regions is > 10 for any real Python process, or cross-validate that at least one region contains the current process's code or heap by checking address ranges against known memory (e.g., that the region containing the address of the current function is present).
  - Independent check: The test at line 689 also uses the real hexcore binding against the current process. It asserts that at least one region in the returned list has r['state'] == 0x1000 (MEM_COMMIT). While this checks an actual Windows constant value rather than mere key presence, it remains a weak gate: a broken list_process_memory_regions that returned a single hardcoded region tuple (0, 4096, 4, 0x1000) would satisfy the assertion …

- **TestToolDefinitionCompleteness::test_tool_function_count_increased** (line 799) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The assertion `len(td.functions) > 42` is purely quantitative and does not enumerate the specific 5 new functions (encode_text, search_bytes, search_numeric_range, list_process_regions, open_process_memory). If the implementation added 3 unrelated dummy functions instead of the 5 intended ones, the count would still pass 42. The companion test test_new_tool_functions_registered verifies the names, but this test adds no semantic gate -- a silent removal or rename of the intended functions could pass if the count threshold remained above 42.
  - Fix: Remove this test entirely, as test_new_tool_functions_registered already provides a stronger semantic gate by enumerating the exact function names required.
  - Independent check: The test at line 799 asserts only len(td.functions) > 42. The actual tool_definition contains well over 100 ToolFunction entries (the functions list in hex_editor.py spans lines 501-1600+, with at least 80+ distinct ToolFunction instances). Removing or renaming any of the 5 specifically intended new functions (encode_text, search_bytes, search_numeric_range, list_process_regions, open_process_memory) would not cause…

### `tests/test_hexcore_e2e/test_bridge_pattern_engine.py`

- **TestCompilePattern::test_compile_complex_struct_with_nested_types_is_valid_json** (line 98) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(parsed, dict) after json.loads(). An empty dict `{}` or a dict like `{"ok": True}` would pass. The test does not verify that the nested Inner and Outer struct definitions appear in the compiled result, nor that field names (tag, val, hdr, sub) are present. The compilation capability could be completely broken (returning a generic stub) and the test would still pass.
  - Fix: Assert that the result dict contains references to the declared type names ('Inner', 'Outer') or field names ('tag', 'val', 'hdr', 'sub') in string form, or check that the dict has expected keys like 'structs' or 'definitions' with non-empty values.
  - Independent check: test_compile_complex_struct_with_nested_types_is_valid_json (line 98) calls compile_pattern with nested Inner/Outer struct source and asserts isinstance(json.loads(result), dict). Any valid JSON object—including {} or {'ok': True}—passes. A stub compiler that returns '{}' regardless of input would pass this test. Neither the Inner/Outer struct names nor the field names (tag, val, hdr, sub) appear in any assertion. C…

- **TestCompilePattern::test_compile_enum_produces_valid_json** (line 109) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(parsed, dict). Any dict passes, even `{}` or `{"error":"stub"}`. The test does not verify that the enum Color or its member names (Red, Green, Blue) appear anywhere in the output. If enum compilation is broken, this test would still pass.
  - Fix: Assert that the result dict (when converted to a string or searched recursively) contains references to 'Color', 'Red', 'Green', or 'Blue'.
  - Independent check: test_compile_enum_produces_valid_json (line 109) compiles 'enum Color : u8 { Red = 0, Green = 1, Blue = 2 }; struct Wrapper { Color c; };' and asserts only isinstance(parsed, dict). Any JSON object passes. The enum name Color and members Red/Green/Blue are never asserted to appear in the output. A stub returning {} would pass. Category weak-existence, severity low, line 109 are correct.

- **TestCompilePattern::test_compile_union_produces_valid_json** (line 120) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(parsed, dict). Any dict passes. The test does not verify that the union Word or its members (as_u16, bytes) appear in the output, nor does it check that the compiled size reflects union semantics (overlapping memory, not sequential). A broken union compiler returning a struct-like layout would still pass.
  - Fix: Assert that the result dict contains 'Word' or 'as_u16' or 'bytes', or check that the compiled size is 2 bytes (max of the member sizes), not 3 or 4.
  - Independent check: test_compile_union_produces_valid_json (line 120) compiles 'union Word { u16 as_u16; u8 bytes[2]; }; struct Wrapper { Word w; };' and asserts only isinstance(parsed, dict). Any JSON object passes. Neither the union Word nor its members as_u16/bytes are asserted. A struct-like compiler would pass even with wrong union semantics. Category weak-existence, severity low, line 120 are correct.

- **TestListAndAutoDetect::test_list_hexpat_patterns_returns_list** (line 308) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(result, list). An empty list passes this test. The sibling test test_list_hexpat_patterns_items_have_required_keys (line 317) checks that the catalog is non-empty and verifies the structure. This test adds no gate: if the scanner is completely broken and returns [], the test stays green.
  - Fix: Remove this test and rely only on test_list_hexpat_patterns_items_have_required_keys, which is stronger (requires len(result) > 0). Alternatively, assert `len(result) > 0` in THIS test so any broken scanner turns it red.
  - Independent check: test_list_hexpat_patterns_returns_list (line 308) calls list_hexpat_patterns() and asserts only isinstance(result, list). An empty list passes. The sibling test_list_hexpat_patterns_items_have_required_keys (line 317) is the real gate: it asserts len(result) > 0, that 'pe' is in pattern_names, and that each entry has name/description/category. A broken scanner returning [] would pass this test but fail the sibling. …

### `tests/test_hexcore_e2e/test_bridge_pe_introspection.py`

- **TestToolDefinitionsRegistration::test_bridge_dispatches_via_getattr** (line 542) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test iterates over the three method names (get_pe_sections, get_pe_imports, get_pe_exports) and asserts callable(attr). Any callable object (including a stub function that always raises or returns []) satisfies the callable() check. The test does not actually invoke the methods or verify they produce correct results. The ToolRegistry dispatch capability (that getattr can retrieve the method) is tested, but the method's actual behavior is not gated.
  - Fix: For a genuine dispatch gate, call each method with appropriate arguments on the loaded bridge's PE document and assert the return type or a minimal structural shape (e.g., isinstance(sections, list)). Alternatively, drop this test since the E2E tests TestGetPeSections*, TestGetPeImports*, and TestGetPeExports* (line 588 onwards) already verify the methods work end-to-end and return correct results.
  - Independent check: The test at line 542-550 calls `getattr(bridge, fn, None)` for the three method names and then does `assert callable(attr)`. This only verifies the attribute exists on the bridge and is callable — it does not invoke the methods or check their return values. A stub that returns `[]` or always raises an unrelated exception would still be callable and would pass. The real behavioral gates for these methods are in sibli…

### `tests/test_hexcore_e2e/test_bridge_signatures.py`

- **TestSignatureResultStructure::test_scan_result_structure** (line 220) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test verifies only that result dictionaries contain the five keys 'name', 'type', 'version', 'offset', 'details' (lines 237-241) and that results is non-empty (line 235). It does not validate the actual values of those keys. An implementation returning [{"name": "", "type": "", "version": "", "offset": -1, "details": ""}] would pass this test. The test does not verify that the MZ Executable signature was correctly identified at the entry point, or that the signature name and type match the expected values from die_test.json.
  - Fix: Add concrete value assertions: assert any(r["name"] == "MZ Executable" for r in results), assert any(r["type"] == "PE" for r in results), and assert any(r["offset"] == 0 for r in results). This converts the test to the same correctness standard already provided by test_die_scan_detects_mz_header. Consider removing this key-presence-only test as it provides no independent gate beyond key-name validation.
  - Independent check: Hand-checked (no re-audit verdict): asserts only 5 key names present + non-empty; a shaped stub returning [{name:'',type:'',version:'',offset:-1,details:''}] passes. Signature-identification correctness ungated. Accurate weak-existence/low.

### `tests/test_hexcore_e2e/test_bridge_transforms.py`

- **TestBridgeListTransforms::test_list_transforms_returns_list** (line 54) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The assertion only checks isinstance(result, list). Production code at line 7773 legitimately returns [] when pipeline unavailable. A broken implementation that always returns [] (even when transforms are available) would pass this test silently. The test lacks verification that the list contains expected content (at minimum, that it's non-empty when the module is available). This is a weak existence check.
  - Fix: Add `if not _pipeline_available: pytest.skip(...)` at test start (matching other transform tests). Then assert `len(result) > 0` to verify the list is non-empty when pipeline is available. Optionally spot-check for a known transform name like 'xor_single'.
  - Independent check: The defect is real: `test_list_transforms_returns_list` asserts only `isinstance(result, list)`. Production code returns `[]` on unavailability, so a broken implementation that always returns `[]` passes this test silently. Category weak-existence and severity low are correct. However, the cited line 61 points at the assertion `assert isinstance(result, list)`; the test definition begins at line 54. Corrected line: …

### `tests/test_hexcore_e2e/test_bridge_yara.py`

- **TestBridgeYaraScan::test_yara_scan_mz_rule_matches_pe_file** (line 104) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The sole assertion is `assert results` (truthiness). A production bug that returned a list with wrong rule names, wrong string offsets, corrupted match dicts, or any other incorrect content would still pass this test. The stronger sibling test test_yara_scan_returns_list (line 79) verifies specific rule names and offsets, making this test redundant and non-gating for value correctness.
  - Fix: Remove this test as it adds no gate beyond test_yara_scan_returns_list, or add assertions for rule name ('MZHeader') and string offset (0) to make it distinct and complementary.
  - Independent check: The sole assertion at line 105 is `assert results` (truthiness). The sibling test `test_yara_scan_returns_list` (line 79-96) already gates everything this test does plus more: it verifies `len(results)==1`, `results[0]['rule']=='MZHeader'`, and `strings[0]['offset']==0`. Any realistic production breakage that would make G0183 fail (e.g., `yara_scan` returning an empty list) would also be caught by the sibling test. …

- **TestBridgeYaraScan::test_yara_scan_match_has_required_keys** (line 115) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The assertion `_EXPECTED_MATCH_KEYS.issubset(match.keys())` only verifies key presence, not values. Any dict with the four keys present but populated with garbage values passes. Additionally, the production code (lines 8916, 8971 of hex_editor.py) includes 'namespace' in the returned dict, but _EXPECTED_MATCH_KEYS omits it. If a bug removed 'namespace' from the output, this test would not catch it (test_bridge_yara_deep.py would catch it, but that's a separate test).
  - Fix: Add 'namespace' to _EXPECTED_MATCH_KEYS to be consistent with production output, and add spot-checks on actual field values (e.g., assert results[0]['rule'] == 'MZHeader') to gate value correctness.
  - Independent check: REVERSED re-audit refute under the strict standard: a shaped stub returning a match dict with rule/tags/meta/strings keys mapped to empty/garbage passes issubset(). The test gates only output-schema key presence (it does catch a dropped key), not actual YARA match correctness. Genuine weak gate; keep confirmed/low.

### `tests/test_hexcore_e2e/test_data_inspector.py`

- **TestInspectAtBasic::test_inspect_at_returns_dict** (line 49) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `isinstance(result, dict)`. Any dict returned by inspect_at() - even one with completely wrong keys or wrong values - passes. The actual data-type interpretation capability is not gated; a stub returning {'garbage': 'values'} would pass. Value correctness is gated elsewhere (TestInspectAtValues class), but this basic test does not verify the content.
  - Fix: Combine with a content check: assert result.get('uint8') == '0' to gate that at least the first byte interpretation is correct. Or clarify that this test is purely for type-schema validation.
  - Independent check: Line 49-50: `result = sample_doc_from_bytes.inspect_at(0)` then `assert isinstance(result, dict)`. Confirmed pure type check. A stub returning `{'garbage': 'values'}` passes. Value correctness is gated only in TestInspectAtValues (e.g., test_uint8_at_zero_byte_in_known_data asserts result['uint8'] == '0'). Category weak-existence and severity low are correct.

- **TestInspectAtBasic::test_inspect_at_has_uint8_key** (line 58) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'uint8' in result`. If inspect_at() returns {'uint8': 'completely_wrong_value'} this test passes. The actual uint8 decoding capability for offset 0 is not gated by this assertion - it only gates that the key exists.
  - Fix: Assert the value: `assert result['uint8'] == '0'` since the fixture's byte at offset 0 is 0x00 (from bytes(range(256))). This makes it a real gate for uint8 interpretation.
  - Independent check: Line 58-59: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint8' in result`. Pure key-presence check. If inspect_at returned `{'uint8': 'banana'}` this passes. Actual uint8 decoding correctness at offset 0 (expected '0') is gated only by TestInspectAtValues.test_uint8_at_zero_byte_in_known_data which asserts `result['uint8'] == '0'`. Category and severity are correct.

- **TestInspectAtBasic::test_inspect_at_has_int8_key** (line 67) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'int8' in result`. A stub returning {'int8': 'garbage'} passes. The signed-byte interpretation capability is not gated.
  - Fix: Assert the value: `assert result['int8'] == '0'` since byte 0x00 at offset 0 is 0 in both signed and unsigned interpretations.
  - Independent check: Line 67-68: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'int8' in result`. Pure key-presence check. A stub returning `{'int8': 'garbage'}` passes. No value correctness is gated here. The finding is accurately categorized as weak-existence/low.

- **TestInspectAtBasic::test_inspect_at_has_uint16_le_key** (line 76) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'uint16_le' in result`. The uint16_le decoding at offset 0 on bytes(range(256)) (expected value 0x0100 = 256) is not gated.
  - Fix: Assert the value: `assert result['uint16_le'] == '256'` (bytes 0x00, 0x01 at little-endian offset 0 yields 0x0100 = 256).
  - Independent check: Line 76-77: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint16_le' in result`. Only gates key presence. uint16_le at offset 0 of bytes(range(256)) should be 256 (0x0100), but that value is not checked here. The value correctness is gated only in TestInspectAtValues.test_uint16_le_known_value which checks a different offset. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_has_uint32_le_key** (line 85) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'uint32_le' in result`. The uint32_le decoding capability is not gated.
  - Fix: Assert the value: `assert result['uint32_le'] == '50462976'` (bytes 0x00,0x01,0x02,0x03 LE = 0x03020100 = 50462976).
  - Independent check: Line 85-86: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint32_le' in result`. Only gates key presence; the uint32_le decoding capability is not value-checked. TestInspectAtValues.test_uint32_le_known_value gates the actual value. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_has_uint32_be_key** (line 94) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'uint32_be' in result`. The big-endian uint32 decoding capability is not gated.
  - Fix: Assert the value: `assert result['uint32_be'] == '66051'` (bytes 0x00,0x01,0x02,0x03 BE = 0x00010203 = 66051).
  - Independent check: Line 94-95: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint32_be' in result`. Only gates key presence. uint32_be decoding at offset 0 is value-checked in TestInspectAtValues.test_uint32_be_known_value, not here. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_has_uint64_le_key** (line 103) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'uint64_le' in result`. The uint64_le decoding capability is not gated.
  - Fix: Assert the value: `assert result['uint64_le'] == str(0x0706050403020100)` (bytes 0-7 at LE).
  - Independent check: Line 103-104: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'uint64_le' in result`. Only gates key presence. No value check whatsoever for uint64_le decoding. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_has_float32_le_key** (line 112) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'float32_le' in result`. The float32 parsing capability is not gated; a stub returning {'float32_le': 'garbage'} passes.
  - Fix: Assert that `float(result['float32_le'])` does not raise and produces a value consistent with the IEEE 754 interpretation of bytes 0x00,0x01,0x02,0x03.
  - Independent check: Line 112-113: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'float32_le' in result`. Only gates key presence. float32 decoding correctness is gated by TestInspectAtValues.test_float32_le_known_value (asserts parsed float is within 1e-5 of 1.0). Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_has_float64_le_key** (line 121) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `'float64_le' in result`. The float64 parsing capability is not gated; same issue as float32.
  - Fix: Assert that `float(result['float64_le'])` succeeds and returns the expected IEEE 754 double interpretation of bytes 0-7.
  - Independent check: Line 121-122: `result = sample_doc_from_bytes.inspect_at(0)` then `assert 'float64_le' in result`. Only gates key presence. float64 decoding correctness is gated by TestInspectAtValues.test_float64_le_known_value. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_contains_all_expected_keys** (line 130) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Checks that each key in _EXPECTED_KEYS is present but does not check values. Any dict containing all 14 expected keys with arbitrary or wrong values passes. The capability being gated - correct multi-type data interpretation - is not verified.
  - Fix: This test is useful for schema validation but should be complemented by value checks. Add a spot-check: verify that result['uint8'] == '0' to ensure at least one interpretation is correct, not just that keys exist.
  - Independent check: Lines 130-132: `result = sample_doc_from_bytes.inspect_at(0)` then iterates `_EXPECTED_KEYS` asserting each is present in result. Checks all 14 expected keys exist but no values are checked. Any dict containing all 14 keys with arbitrary string values passes. Category weak-existence/low is accurate.

- **TestInspectAtBasic::test_inspect_at_all_values_are_strings** (line 141) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only that all values are str instances. A stub returning {'uint8': 'banana', 'int8': 'orange', ...} satisfies this assertion. The actual numeric correctness of the string representations is not gated.
  - Fix: Add spot-checks: e.g. `assert result['uint8'] == '0'` to ensure the string representation is actually a correct numeric string, not just any string.
  - Independent check: Lines 140-142: `result = sample_doc_from_bytes.inspect_at(0)` then `for key, value in result.items(): assert isinstance(value, str)`. Gates that all values are str type, not that values are numerically correct. A stub returning `{'uint8': 'banana', 'int8': 'orange', ...}` satisfies this assertion. The test does gate the string-return format but not value correctness. Category weak-existence/low is accurate.

- **TestInspectAtEdge::test_inspect_at_last_valid_offset** (line 275) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts `isinstance(result, dict)` and `'uint8' in result` at the last valid offset (255). Neither the uint8 value (which must be '255' at offset 255 of bytes(range(256))) nor any other field value is checked. A stub returning {'uint8': ''} passes.
  - Fix: Add `assert result['uint8'] == '255'` since the byte at offset 255 of bytes(range(256)) is 0xFF = 255. This gates the boundary handling for offset correctness.
  - Independent check: Lines 274-277: `doc_length = sample_doc_from_bytes.length()`, `result = sample_doc_from_bytes.inspect_at(doc_length - 1)`, `assert isinstance(result, dict)`, `assert 'uint8' in result`. Only checks type and key presence at the last valid offset (255). The uint8 value at offset 255 ('255') is gated by TestInspectAtValues.test_uint8_at_offset_255 which asserts `result['uint8'] == '255'`. Category weak-existence/low is…

- **TestInspectAtEdge::test_inspect_at_near_end_partial_types** (line 285) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `isinstance(result, dict)` and `'uint8' in result` at offset 253. No value is checked. The capability being gated - that inspect_at() handles near-boundary positions where multi-byte reads would exceed the document - is not substantiated.
  - Fix: Add `assert result['uint8'] == '253'` (byte at offset 253 of bytes(range(256)) is 0xFD = 253) to make this a real gate for the boundary handling path.
  - Independent check: Lines 285-287: `result = sample_doc_from_bytes.inspect_at(253)`, `assert isinstance(result, dict)`, `assert 'uint8' in result`. Only gates that a result dict with 'uint8' key is returned at near-boundary offset 253; no value is checked. A stub returning `{'uint8': ''}` passes. Category weak-existence/low is accurate.

- **TestInspectAtEdge::test_inspect_at_mid_document_offset** (line 295) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts `isinstance(result, dict)` and `result` (truthiness) at offset 128. Neither the uint8 value nor any field is verified. A stub returning any non-empty dict passes.
  - Fix: Add `assert result['uint8'] == '128'` (byte at offset 128 of bytes(range(256)) is 0x80 = 128) to gate the mid-document invocation.
  - Independent check: Lines 295-297: `result: dict[str, str] = sample_doc_from_bytes.inspect_at(128)`, `assert isinstance(result, dict)`, `assert result`. Only checks that a non-empty dict is returned at mid-document offset 128. Neither the uint8 value (which should be '128' at offset 128 of bytes(range(256))) nor any field is verified. A stub returning any non-empty dict passes. Category weak-existence/low is accurate.

### `tests/test_hexcore_e2e/test_encodings.py`

- **TestListEncodings::test_list_encodings_returns_nonempty_list** (line 198) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Without companion tests this would be weak, but alongside test_list_encodings_contains_utf8 (line 221) and test_list_encodings_contains_ascii (line 231), the overall test suite is strong. HOWEVER, this specific test in isolation is weak: a stub returning [('x', 'y')] would pass. The auditor is correct that THIS test by itself does not validate the real capability. The stronger tests elsewhere cover it.
  - Fix: This test alone is subsumable. Consider consolidating with test_list_encodings_contains_utf8 and test_list_encodings_contains_ascii into a single test that asserts both non-empty AND known encodings present.
  - Independent check: The test at line 190-198 asserts `isinstance(result, list)` and `assert result` (non-empty) on `list_encodings()`. The production code returns a fixed `ENCODING_LIST` slice of 35 entries (encodings.rs lines 323-363). A stub that returned `[('x', 'y')]` (one entry, non-empty) would satisfy both assertions and the test would pass, while `list_encodings` no longer returns the real encoding inventory. The companion test…

- **TestListEncodings::test_list_encodings_entries_are_two_tuples** (line 208) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks only tuple length. A list of ('', '') tuples would pass. This is a structural/format check without semantic validation. The real capability (returning meaningful encoding names) is tested by companion tests (test_list_encodings_contains_utf8, test_list_encodings_contains_ascii, test_list_encodings_entries_are_strings) but THIS test alone adds no gate against semantic correctness.
  - Fix: Consolidate this structural check into test_list_encodings_contains_utf8 or similar. Or strengthen it to assert len(entry) == 2 AND entry[0] in known_encoding_names.
  - Independent check: The test at line 200-208 iterates the result of `list_encodings()` and asserts `len(entry) == 2` for each entry. The production code yields `(name.to_string(), desc.to_string())` 2-tuples (encodings.rs line 367-370). A stub returning `[('', '')]` satisfies `len(entry) == 2` exactly. This is a pure structural/format check; empty-string 2-tuples pass with no semantic gating of encoding names or labels. Category `weak-…

- **TestListEncodings::test_list_encodings_entries_are_strings** (line 219) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks isinstance(name, str) and isinstance(label, str). Empty strings satisfy this. A stub returning [('', '')] would pass. The real capability (returning meaningful encoding identifiers with non-empty names) is gated by test_list_encodings_names_are_nonempty but THIS test alone is purely a type check.
  - Fix: Remove or consolidate with test_list_encodings_names_are_nonempty into a single test that asserts both isinstance AND len(name) > 0 AND some canonical encoding is present.
  - Independent check: The test at line 210-219 checks `isinstance(name, str)` and `isinstance(label, str)` for each tuple from `list_encodings()`. A stub returning `[('', '')]` satisfies both `isinstance` checks with empty strings. The PyO3 binding already guarantees `String` fields become Python `str`, so the type check provides no additional gate beyond the Rust type system. The real encoding-name content is gated by `test_list_encodin…

- **TestListEncodings::test_list_encodings_names_are_nonempty** (line 249) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only len(name) > 0, which a stub returning [('x', 'x')] satisfies. While companion tests (test_list_encodings_contains_utf8, test_list_encodings_contains_ascii) verify known encodings, THIS test alone does not validate that the returned encoding names correspond to real, usable codecs. It is a length check, not a semantic gate.
  - Fix: Strengthen to assert that at least one entry matches a known canonical encoding name (e.g., name in {'utf-8', 'utf8', 'ascii', 'latin-1'}) rather than only checking non-empty length.
  - Independent check: The test at line 241-249 asserts `len(name) > 0` for each name in `list_encodings()`. A stub returning `[('x', 'x')]` satisfies `len('x') > 0` and passes. The check only prevents fully-empty string names; a single-character or meaningless name passes. The actual semantic gate (that real codec identifiers like 'utf-8' and 'ascii' are present) is provided by `test_list_encodings_contains_utf8` and `test_list_encodings…

### `tests/test_hexcore_e2e/test_entropy.py`

- **TestEntropy::test_entropy_sample_bytes_at_most_eight** (line 118) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only result <= 8.0. A stub returning 0.0 always passes. This is a mathematical upper-bound constraint check, not a behavioral gate. The real gate is test_entropy_uniform_256_matches_independent_oracle (line 59) which compares against an independent oracle. This test adds no gate against wrong computation; it only asserts a trivial bound satisfied by any constant.
  - Fix: Remove this test as it is subsumed by test_entropy_uniform_256_matches_independent_oracle and test_entropy_repeating_byte_is_zero. If bound protection is desired, add it to the oracle test.
  - Independent check: test_entropy_sample_bytes_at_most_eight (line 118) asserts only `result <= 8.0`. The input is bytes(range(256)) whose real entropy is 8.0, so the production implementation passes. But any stub returning a constant like 0.0 also satisfies the bound. The constraint is a necessary mathematical property of Shannon entropy, not a discriminating check. A production regression that returned 0.0 for all inputs would pass th…

- **TestEntropy::test_entropy_is_float** (line 127) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(result, float). A stub returning 0.0 or any float constant passes. This is a type-check only. The actual entropy computation is verified by test_entropy_uniform_256_matches_independent_oracle (line 59), which is the real behavioral gate. This test provides no defense against wrong entropy calculation.
  - Fix: Remove as subsumed by test_entropy_uniform_256_matches_independent_oracle. If type checking is important, integrate into that test.
  - Independent check: test_entropy_is_float (line 127) asserts only `isinstance(result, float)`. Any stub returning a float constant (0.0, 1.0, 8.0) passes unconditionally. This is a pure type-presence check with no semantic content. A computation that returns the wrong entropy value but the correct Python type is completely invisible to this test. Weak-existence is the correct category.

- **TestEntropy::test_entropy_lower_bound** (line 136) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only result >= 0.0. Any non-negative constant (including 0.0) satisfies this. This is a mathematical constraint check, not a behavioral gate. The real gates are test_entropy_uniform_256_matches_independent_oracle and test_entropy_repeating_byte_is_zero. Breaking entropy computation would leave this test green.
  - Fix: Remove as subsumed by existing oracle-based and zero-entropy tests. If non-negativity is important, it is already implicitly tested by the oracle test.
  - Independent check: test_entropy_lower_bound (line 136) asserts only `result >= 0.0`. Any non-negative constant satisfies this, including 0.0. A broken implementation returning a fixed non-negative float would pass regardless of whether it computes entropy correctly. The claim is correct: this is a trivial mathematical constraint check, not a behavioral gate. Weak-existence and low severity are appropriate.

- **TestByteDistribution::test_byte_distribution_returns_ints** (line 264) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(count, int) for each element. A stub returning [0]*256 would pass despite being completely wrong in the case where the document contains non-zero bytes. However, line 237 (test_byte_distribution_uniform_all_ones) verifies that for the uniform bytes(range(256)) document, every count equals 1. And line 246 (test_byte_distribution_zeros_only) checks the all-zero case. THIS test in isolation is purely a type check without semantic validation.
  - Fix: This test is subsumed by test_byte_distribution_uniform_all_ones and test_byte_distribution_zeros_only. Consider removing it or integrating the type check into those stronger tests.
  - Independent check: test_byte_distribution_returns_ints (line 264) asserts `all(isinstance(count, int) for count in result)`. For the bytes(range(256)) input, returning `[0] * 256` (all integer zeros) passes despite being semantically wrong — the real counts are all 1. Only a type-level regression (e.g., returning floats or strings) would be caught. Sibling tests like test_byte_distribution_uniform_all_ones (line 244) gate the actual v…

- **TestContentClassification::test_content_classification_values_in_range** (line 398) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only isinstance(value, int) and 0 <= value <= 4 for each element. A stub returning [0]*N would pass. The auditor is correct: this is purely a range check without semantic validation. The actual classification logic (entropy-based categorization) is not tested by this assertion alone.
  - Fix: Strengthen to assert expected classification codes for the known uniform-distribution input, or for all-zero data (which should receive low classification). Or consolidate with test_content_classification_zeros_classified_low_entropy (line 412) into a more comprehensive test.
  - Independent check: test_content_classification_values_in_range (line 398) checks `isinstance(value, int) and 0 <= value <= 4` for every element. A stub returning `[0] * ceil(256/32)` (8 zeros) satisfies all three predicates regardless of whether the entropy-based classification logic is correct. The test exercises real production code (content_classification(32) on HexDocument) but the assertion is only a range/type constraint. Siblin…

### `tests/test_hexcore_e2e/test_hashing.py`

- **TestHashAlgorithms::test_sha256_output_is_64_hex_chars** (line 204) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks only len(result) == 64 and that all characters are hex digits. A stub returning 'a' * 64 or 'deadbeef' padded to 64 chars would pass. However, line 67 (test_sha256_matches_hashlib) already verifies the EXACT hash value against hashlib.sha256. That test is the real gate. This test adds no additional production defect detection; it only checks format, which is implicitly correct if the value is correct.
  - Fix: Remove this test as fully subsumed by test_sha256_matches_hashlib, which asserts the correct value (and implicitly the correct length).
  - Independent check: test_sha256_output_is_64_hex_chars (def at line 196, assertions at lines 203-204) calls compute_hash('sha256') and checks only len==64 and all hex chars. test_sha256_matches_hashlib at line 67 asserts result.lower() == hashlib.sha256(sample_bytes).hexdigest().lower(), which is an exact-value equality check. Any implementation producing the correct 64-hex-char SHA-256 digest satisfies both tests. A stub returning 'a'…

- **TestHashAlgorithms::test_md5_output_is_32_hex_chars** (line 214) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks only len(result) == 32 and hex-digit membership. A stub returning 'deadbeef' * 4 would pass. Line 45 (test_md5_matches_hashlib) verifies the exact value, making THIS test redundant. The format is implicitly validated by the value check.
  - Fix: Remove as subsumed by test_md5_matches_hashlib.
  - Independent check: test_md5_output_is_32_hex_chars (def at line 206, assertions at lines 213-214) checks only len==32 and all hex chars. test_md5_matches_hashlib at line 45 performs an exact-value equality against hashlib.new('md5', sample_bytes).hexdigest(). A correct MD5 digest guarantees both correct length and hex character set, so the format-only test is fully subsumed. Any stub that passes the format check (e.g. 'deadbeef'*4) fa…

- **TestHashAlgorithms::test_sha1_output_is_40_hex_chars** (line 224) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks only len(result) == 40 and hex-digit membership. Line 56 (test_sha1_matches_hashlib) verifies the exact value, making THIS test subsumed. Correct value implies correct length and format.
  - Fix: Remove as subsumed by test_sha1_matches_hashlib.
  - Independent check: test_sha1_output_is_40_hex_chars (def at line 216, assertions at lines 223-224) checks only len==40 and all hex chars. test_sha1_matches_hashlib at line 56 asserts exact-value equality against hashlib.new('sha1', sample_bytes).hexdigest(). A correct SHA-1 digest implies both correct length and hex charset; the format-only test adds no additional detection. Category weak-existence and severity low are correct.

- **TestHashAlgorithms::test_sha512_output_is_128_hex_chars** (line 234) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks only len(result) == 128 and hex-digit membership. Line 78 (test_sha512_matches_hashlib) verifies the exact value, fully subsuming this format check. Correct value guarantees correct length.
  - Fix: Remove as subsumed by test_sha512_matches_hashlib.
  - Independent check: test_sha512_output_is_128_hex_chars (def at line 226, assertions at lines 233-234) checks only len==128 and all hex chars. test_sha512_matches_hashlib at line 78 asserts exact-value equality against hashlib.sha512(sample_bytes).hexdigest(). A correct SHA-512 digest is always exactly 128 lowercase hex characters, so this format test is fully subsumed by the value-exact gate. Category weak-existence and severity low a…

- **TestCustomCRC::test_crc32_output_format_is_hex_string** (line 413) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test asserts only that result is a non-empty string with all hex digits. A stub returning 'deadbeef' would pass. Line 316 (test_crc32_standard_matches_binascii) verifies the EXACT CRC-32 value, making THIS test redundant. Correct value ensures correct format.
  - Fix: Remove as subsumed by test_crc32_standard_matches_binascii, which is the real behavioral gate.
  - Independent check: test_crc32_output_format_is_hex_string (def at line 396, assertions at lines 411-413) checks isinstance(result, str), result is truthy, and all chars in hexdigits. test_crc32_standard_matches_binascii at line 316 asserts exact-value equality against binascii.crc32(sample_bytes) formatted as an 8-hex-char string. A correct CRC-32 result is always a non-empty hex string, so the format check is fully subsumed. A stub r…

### `tests/test_hexcore_e2e/test_hexpat_compiler_e2e.py`

- **TestHexPatCompilerCompile::test_compile_syntax_error_missing_semicolon_raises** (line 316) - `tautological` / **low** - _[verified accurate]_
  - Why it is not a gate: Line 315 asserts truthiness of exc_info.value.message (non-empty string). Line 316 asserts len(message) > 0. Since HexPatError.message is typed as str (not None), any non-empty string is truthy (line 315), and any truthy string has len > 0 (line 316). If message is empty, line 315 fails; if line 315 passes, line 316 cannot independently fail. This is logically tautological given the str type guarantee.
  - Fix: Remove line 316. The truthiness check on line 315 is sufficient and idiomatic for asserting a non-empty string. If more specific error message content validation is desired (e.g., substring presence), add that instead of redundant length checks.
  - Independent check: Lines 313-316: pytest.raises(HexPatError) followed by 'assert exc_info.value.message' (line 315, truthiness) then 'assert len(exc_info.value.message) > 0' (line 316, length check). For any string s: if s is truthy (line 315 passes), then len(s) > 0 is necessarily true (line 316 cannot independently fail). If message is empty, line 315 fails before line 316 is reached. Line 316 adds zero additional gate value beyond …

### `tests/test_hexcore_e2e/test_hexpat_stdlib.py`

- **TestStringFunctions::test_string_at_out_of_bounds** (line 326) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The test (line 326) uses `assert not getattr(builtin, '_string_at')('hi', 10)` which only checks falsiness. The production contract (stdlib.py line 1010-1020 docstring) explicitly states the return value is 'an empty string when the index is out of bounds' and the implementation returns `''` (line 1020). However, the assertion accepts any falsy value: None, 0, False, [], etc. If production code were changed to return None instead of '' on out-of-bounds, the test would still pass despite violating the return-type contract.
  - Fix: Replace with exact equality check: `assert getattr(builtin, '_string_at')('hi', 10) == ''` to enforce the exact empty-string return value and type.
  - Independent check: The weak-existence finding is correct: `assert not getattr(builtin, '_string_at')('hi', 10)` accepts any falsy value (None, 0, False) and does not enforce that the return type is `str`. If the OOB branch were changed to return `None` instead of `''`, the assertion still passes. However, severity should be 'low' not 'high'. A sibling test at line 319 (`assert getattr(builtin, '_string_at')('hello', 1) == 'e'`) strong…

- **TestStringFunctions::test_string_reverse_empty** (line 410) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: The test (line 410) uses `assert not getattr(builtin, '_string_reverse')('')` which only checks falsiness. The production contract (stdlib.py line 1156-1163 docstring) specifies returning the 'reversed string' and the implementation returns `str(...)[::-1]` (line 1164), which for an empty string is `''`. The assertion accepts any falsy value (None, 0, False, [], etc.). If production were changed to return None for empty input, the test would incorrectly pass despite violating the string return-type contract.
  - Fix: Replace with exact equality check: `assert getattr(builtin, '_string_reverse')('') == ''` to enforce the exact empty-string return value and type.
  - Independent check: The weak-existence finding is correct: `assert not getattr(builtin, '_string_reverse')('')` at line 410 accepts any falsy value and does not enforce a `str` return type. If the empty-string branch returned `None` the assertion would still pass. Two corrections needed: (1) The `line` field is 409 but line 409 is `builtin = BuiltinFunctions(reader)` — the problematic assertion is at line 410; (2) severity should be 'l…

### `tests/test_hexcore_e2e/test_patch_export.py`

- **TestGetPatches::test_patch_list_entries_are_two_tuples** (line 72) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only validates types and tuple structure (isinstance(entry, tuple), len(entry)==2, isinstance(entry[0], int), isinstance(entry[1], bytes)). A stub implementation returning [(0, b'')] after any write would pass all four isinstance/len assertions. The test cannot distinguish between correct offset/data and placeholder values. No realistic production breakage scenario would fail this test while the sibling test at line 50-62 catches actual data corruption.
  - Fix: This test adds minimal value beyond shape-checking. Merge it with the stronger sibling test (test_patch_entry_has_correct_offset_and_data) by adding the data assertions to the loop: assert entry[0] == 5 and entry[1] == b'\x01\x02'.
  - Independent check: The test at lines 70-76 of test_patch_export.py calls write_bytes(5, b"\x01\x02") then iterates patches checking isinstance(entry, tuple), len(entry)==2, isinstance(entry[0], int), isinstance(entry[1], bytes). A stub returning [(0, b'')] after any write would pass all four assertions - offset 0 instead of 5, empty bytes instead of b"\x01\x02", yet no assertion fails. The actual offset and data correctness are gated …

### `tests/test_hexcore_e2e/test_process_memory.py`

- **TestListProcessMemoryRegions::test_list_regions_returns_list** (line 75) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test only validates isinstance(regions, list). A stub implementation returning [] (empty list) or a list with garbage/malformed tuples would pass. The sibling test at line 77-85 checks non-emptiness, and line 87-106 validates tuple structure. This test as written cannot distinguish between success and partial/empty results. The production capability—that list_process_memory_regions actually enumerates real memory regions—is not gated.
  - Fix: This test is redundant. Either merge it with the non-empty check (line 77-85) to create a combined gate, or remove it and rely on the structure validation at line 87-106 and the sibling test at line 134-151 which actually read from a region.
  - Independent check: The test at lines 67-75 of test_process_memory.py calls list_process_memory_regions(pid) and asserts only `isinstance(regions, list)`. An empty list [] passes this check since it is still an instance of list. This means a broken implementation returning [] (no regions enumerated) would pass this test. The actual enumeration and non-emptiness are gated by sibling tests: test_list_regions_nonempty_for_current_process …

- **TestFromProcessMemory::test_from_process_memory_returns_hex_document** (line 168) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: The test checks hasattr(doc, 'length') and hasattr(doc, 'read'), then asserts doc.length() == 8. The actual bytes read from process memory are never verified (no call to doc.read() occurs). A stub HexDocument with those methods but no real process memory backing would pass. The test gates the shape (has these methods) and claims the size is 8, but not that those methods actually access process memory.
  - Fix: Add a call to verify actual data: after doc.length() == 8, call data = doc.read(0, 8) and assert isinstance(data, bytes) and len(data) == 8. This would gate that read() actually returns process memory content.
  - Independent check: The test at lines 153-170 of test_process_memory.py calls from_process_memory(pid, addr, 8) and checks hasattr(doc, 'length'), hasattr(doc, 'read'), and doc.length() == 8. While doc.length() == 8 does gate the reported size matches the requested size argument, doc.read() is never called, so the actual bytes read from process memory are never verified. A stub HexDocument that records size=8 but never calls ReadProces…

### `tests/test_hexcore_e2e/test_templates.py`

- **TestListTemplates::test_returns_nonempty_list** (line 55) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only truthiness of the templates list via `assert templates`. A production implementation returning [('dummy', '')] would pass. The actual gate on correct template registration is provided by downstream tests (test_image_dos_header_present, test_elf_template_present, test_zip_template_present) which verify specific template names are present.
  - Fix: Replace with assertion on specific known templates: `assert any(name == 'IMAGE_DOS_HEADER' for name, _ in templates)` or remove if downstream specific-name tests are considered sufficient gating.
  - Independent check: test_returns_nonempty_list at line 55 does only `assert templates` - a truthiness check. Any non-empty list, including [('dummy', '')], passes. The genuine gates on specific template names (IMAGE_DOS_HEADER, Elf64_Ehdr, ZIP_LOCAL_FILE_HEADER) are provided by sibling tests at lines 72-106. weak-existence/low is correct.

- **TestListTemplates::test_entries_are_name_description_pairs** (line 66) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Checks only structural shape (2-tuple of non-empty str and str). A registry returning [('x', ''), ('y', '')] with placeholder data would pass. The real production capability - that specific built-in templates like IMAGE_DOS_HEADER are registered - is gated by downstream tests that check for these specific names.
  - Fix: Add content assertions: verify that known template names like 'IMAGE_DOS_HEADER' appear in the results, not just that structure is correct.
  - Independent check: test_entries_are_name_description_pairs at line 66 checks structural shape only: len==2, entry[0] is non-empty str, entry[1] is str (no non-empty check on description). A registry returning [('x', '')] with placeholder data passes all assertions. Specific name registration is gated by sibling tests. weak-existence/low is correct.

- **TestListTemplatesDetailed::test_returns_nonempty_list** (line 121) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only truthiness via `assert detailed`. The downstream test test_dos_header_field_count_positive (line 140) provides stronger gating by checking for the specific 'IMAGE_DOS_HEADER' template and its field count. This truthiness test would pass if list_templates_detailed() returned a single placeholder 4-tuple.
  - Fix: Remove this test or strengthen it to assert that known templates like 'IMAGE_DOS_HEADER' are present with field_count > 0, making it a real independent gate.
  - Independent check: test_returns_nonempty_list at line 121 does only `assert detailed`. Any single-element non-empty list passes. The stronger gate is test_dos_header_field_count_positive at line 140 which checks for IMAGE_DOS_HEADER specifically and verifies field_count > 0 and category == 'PE'. weak-existence/low is correct.

- **TestApplyPETemplate::test_apply_returns_nonempty_fields** (line 198) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: Asserts only `assert fields` (truthiness). Applying IMAGE_DOS_HEADER could return a single dummy field dict and this test passes. The downstream test test_first_field_is_e_magic (line 200) checks that the first field is specifically named 'e_magic', providing the actual gate on correct template parsing. The truthiness check adds no production gate beyond verifying the function doesn't crash.
  - Fix: Replace with assertion on expected field count (DOS header has 18 fields) or verify specific field names like 'e_magic' and 'e_lfanew' are present, making the test independently falsifiable.
  - Independent check: The finding is substantively accurate (weak-existence/low): the test does only `assert fields` at line 198, while the real gate on correct template parsing is test_first_field_is_e_magic at line 200 (checks fields[0]['name'] == 'e_magic'). However, the cited line 197 points to the `apply_template` call (`fields: list[dict[str, Any]] = doc.apply_template('IMAGE_DOS_HEADER', 0)`), not the assertion. The assertion `ass…

- **TestApplyELFTemplate::test_apply_returns_fields** (line 289) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `assert fields` (truthiness). Applying Elf64_Ehdr could return a single dummy field dict and this test passes. The downstream test test_first_field_is_e_ident (line 291) provides the real gate by checking that the first field is specifically named 'e_ident'. This truthiness-only test is redundant and would not catch a broken template that returns dummy fields.
  - Fix: Remove this test (already gated by test_first_field_is_e_ident) or replace with an assertion on expected field count or specific field names.
  - Independent check: test_apply_returns_fields at line 289 does only `assert fields`. Line 289 is the assertion (`assert fields`), line 288 is the apply_template call - line number is correct. Applying Elf64_Ehdr could return any non-empty list and pass. The real gate is test_first_field_is_e_ident at line 300 which checks fields[0]['name'] == 'e_ident'. weak-existence/low is correct.

- **TestApplyZIPTemplate::test_apply_returns_fields** (line 341) - `weak-existence` / **low** - _[verified accurate]_
  - Why it is not a gate: Asserts only `assert fields` (truthiness). Applying ZIP_LOCAL_FILE_HEADER could return any non-empty list and this test passes. The downstream test test_first_field_is_signature (line 352) provides the actual gate by checking that the first field is named 'signature'. This truthiness check adds no independent production gate.
  - Fix: Remove this test (already gated by test_first_field_is_signature) or replace with an assertion on expected field count or specific field names.
  - Independent check: test_apply_returns_fields at line 341 does only `assert fields` at line 341. Any non-empty list passes. The real gate is test_first_field_is_signature at line 352 which checks fields[0]['name'] == 'signature'. weak-existence/low is correct.

### `tests/test_hexcore_e2e/test_transforms.py`

- **TestListTransforms::test_list_transforms_contains_xor** (line 149) - `weak-existence` / **low** - _[RECLASSIFIED]_
  - Why it is not a gate: Uses substring matching `any('xor' in n.lower() for n in names)` which passes if ANY name contains 'xor'. The upstream test test_list_transforms_returns_nonempty_list (lines 53-73) already provides a much stronger gate by checking: (1) exact count of 23 transforms, (2) presence of all specific expected names including xor_single, xor_repeating, and xor_rolling. If any XOR variant was removed, the upstream count check would fail. This test is redundant.
  - Fix: Remove this test as redundant, or replace with exact name assertions: `assert 'xor_single' in names`, `assert 'xor_repeating' in names`, `assert 'xor_rolling' in names` to make it independently falsifiable and document that all three XOR variants are expected.
  - Independent check: REVERSED re-audit refute under the strict standard: asserts only that the substring 'xor' appears among transform NAMES. A hardcoded name list, or an xor transform that is registered but broken, passes - xor execution correctness is not gated here (it is exercised by the apply tests in test_transforms). Catches only registry removal. Keep confirmed/low.

## Refuted flags (verified to be real gates - no action needed)

- `tests/test_hexcore_e2e/test_bridge_bps_ups.py` :: **TestUPSRoundtrip::test_ups_identical_files_empty_patch** (line 285) - Test IS a real gate. A realistic breakage exists: remove the identical-file optimization in export_patches_ups so the algorithm generates a full diff patch instead of just headers. This would make the patch > 32 bytes, causing the test to fail. The test correctly gates against implementation bugs that fail to detect and optimize for identical source/target files. _[Independent check: The JSON already self-refutes this finding (`confirmed: false`). Independently verified: _build_ups_patch (hex_editor.py lines 4508-4558) walks source and target XOR-byte by byte; when both files are identical, xor_val is always 0 and no hunk data is written to the patch. The output is magic (4 bytes) + encoded source_size (~2 bytes for 64) + encoded target_size (~2 bytes) + 3 CRC32s (12 bytes) ≈ 20 bytes, well unde…]_
- `tests/test_hexcore_e2e/test_bridge_copy_as_complete.py` :: **TestCopyAsCsharpArray::test_csharp_array_ends_with_closing_brace** (line 101) - Test IS a real gate. Realistic breakage: remove the closing brace from the format string in copy_as (line 5492), changing from f"new byte[] {{{inner}}}" to f"new byte[] {{{inner}}". This would cause the test to fail. The test gates against implementation bugs that corrupt the structural format of the csharp_array output. _[Independent check: The test at line 92 (assertion at line 101) calls the real copy_as('csharp_array') on a real document and asserts result.endswith('}'). The production code at line 5492 of hex_editor.py returns f"new byte[] {{{inner}}}". If that closing brace were removed (e.g., changed to f"new byte[] {{{inner}}"), the production string would no longer end with '}' and this test would fail. The assertion is narrow but catches a rea…]_
- `tests/test_hexcore_e2e/test_bridge_copy_as_complete.py` :: **TestCopyAsJavaArray::test_java_array_starts_with_new_byte_array** (line 129) - Test IS a real gate. Realistic breakage: remove or change the prefix from "new byte[] {" in the java_array format (line 5500). For example, change it to just "byte[] {" or "ByteArray {" would cause this test to fail. The test gates against implementation bugs that corrupt the structural format of java_array output. _[Independent check: The test at line 120 (assertion at line 129) calls the real copy_as('java_array') on a real document and asserts result.startswith('new byte[] {'). The production code at line 5500 of hex_editor.py returns "new byte[] {" + ", ".join(parts) + "}". If the prefix were changed (e.g., to "byte[] {" or "ByteArray {"), this test would fail. It is a narrow but genuine gate against prefix corruption in the java_array format.…]_
- `tests/test_hexcore_e2e/test_bridge_copy_as_complete.py` :: **TestCopyAsJavascriptArray::test_javascript_array_ends_with_closing_bracket_paren** (line 198) - Test IS a real gate. Realistic breakage: remove or change the closing '])' in the format string (line 5503), changing from f"new Uint8Array([{inner}])" to f"new Uint8Array([{inner}]" would cause this test to fail. The test gates against implementation bugs that corrupt the structural format of javascript_array output. _[Independent check: The test at line 189 (assertion at line 198) calls the real copy_as('javascript_array') and asserts result.endswith('])'). The production code at line 5503 returns f"new Uint8Array([{inner}])". Removing or changing the closing '])' suffix would break this assertion. This is a narrow but real structural gate. A sibling test (test_javascript_array_full_string_matches_payload, line 176) also does an exact equality chec…]_
- `tests/test_hexcore_e2e/test_bridge_copy_as_complete.py` :: **TestCopyAsNasmDb::test_nasm_db_starts_with_db** (line 225) - Test IS a real gate. Realistic breakage: remove or change the 'db ' prefix in the nasm_db format (line 5511), changing from f"db {inner}" to f"nasm {inner}" would cause this test to fail. The test gates against implementation bugs that corrupt the required 'db' assembler directive prefix. _[Independent check: The test at line 217 (assertion at line 226) calls the real copy_as('nasm_db') and asserts result.startswith('db '). The production code at line 5511 returns f"db {inner}". If the assembler directive prefix were changed (e.g., to 'nasm ' or 'DB '), this test would fail. It is a narrow but genuine gate against prefix corruption in nasm_db output. The finding's own claim field states the test IS a real gate; the weak-…]_
- `tests/test_hexcore_e2e/test_bridge_state_integration.py` :: **TestPatternExecutedEvent::test_execute_pattern_fires_pattern_executed** (line 362) - The try/except at lines 362-367 is defensive but not problematic. The test's core assertion at line 369 checks that PATTERN_EXECUTED event is fired. Any realistic production breakage (e.g., removing line 1286 notify_pattern_executed call, or breaking the pattern execution) would cause the assertion to fail. The except clause only triggers if RuntimeError('hexpat not available') is raised, which should not happen at runtime if the import-time check at line 354 passed, since both checks verify the same import condition. The test IS a real gate for execute_pattern functionality. _[Independent check: The original auditor marked `confirmed: false` and the claim itself concludes 'The test IS a real gate for execute_pattern functionality.' The test at lines 345-369 first skips if `_hexpat_available` is False, calls `loaded_bridge.execute_pattern('u8 sig_byte @ 0x00;')`, and asserts `any(e[0] == HexDocumentEvent.PATTERN_EXECUTED for e in events)`. If a realistic production breakage removed or disabled the `notify_pa…]_
- `tests/test_hexcore_e2e/test_encodings.py` :: **TestDecodeText::test_decode_returns_string** (line 89) - This test is a valid type-contract gate. The function MUST return str, not bytes or int. The test file contains companion tests (test_decode_utf8_hello_world, test_decode_ascii_text) that verify content equality. If decode_text were changed to return int(4) or b'test' instead of 'test', this test WOULD fail. Breaking the type signature is a realistic production defect that this test catches. _[Independent check: The test at line 80-89 calls `doc.decode_text(0, 4, 'utf-8')` on real `b'test'` data via the native Rust/PyO3 `HexDocument.decode_text` binding (lib.rs line 571) which calls `encodings::decode_text` (encodings.rs line 139). If `decode_text` raised a `PyValueError` for valid UTF-8 (e.g., the encoding resolver mistakenly rejected `'utf-8'`) the test would fail with an exception rather than passing. `isinstance(result,…]_
- `tests/test_hexcore_e2e/test_entropy.py` :: **TestEntropyMap::test_entropy_map_returns_list_of_floats** (line 164) - While the test checks only structural properties (return type and element types), the auditor's claim that 'a stub returning [0.0] passes' is incomplete. In practice, breaking entropy_map to return wrong block counts would be caught because the number of blocks must satisfy the invariant: ceil(doc_length / block_size) == len(result). Test line 176 (test_entropy_map_block_count_matches_doc_length) enforces this. Additionally, if someone removed the blocking loop entirely and returned a single dummy float, test_entropy_map_block_count_matches_doc_length would fail. So the test suite together enforces correctness, though this test alone is weak. _[Independent check: test_entropy_map_returns_list_of_floats (line 164) checks `isinstance(result, list)` and `all(isinstance(v, (int, float)) for v in result)`. A stub returning the structurally correct `[0.0] * 8` (8 zeros for a 256-byte document with block_size=32) passes completely despite computing no entropy. Only wrong return types (e.g., returning None or a list of strings) would be caught. The original auditor flagged this as '…]_
- `tests/test_hexcore_e2e/test_entropy.py` :: **TestByteTypeDistribution::test_byte_type_distribution_returns_four_values** (line 281) - While the test checks only that len(result) == 4, breaking the categorization logic would cause failures in downstream tests that validate specific counts. For instance, test_byte_type_distribution_null_count (line 293), test_byte_type_distribution_printable_count (line 304), and test_byte_type_distribution_high_bytes_count (line 316) verify the CORRECTNESS of each category by checking their exact values for known inputs. If the tuple structure broke (e.g., returned a 3-tuple or 5-tuple), THOSE tests would fail. So while this test alone is weak, the suite structure is sound. _[Independent check: test_byte_type_distribution_returns_four_values (line 281) asserts `len(result) == 4`. A stub returning `(0, 0, 0, 0)` passes unconditionally because it has length 4 and the test checks nothing about the values. Swapping categories, zeroing counts, or returning wrong proportions all slide through. The original auditor's 'confirmed: false' reflects that sibling tests (lines 293, 304, 316) validate actual category val…]_
- `tests/test_hexcore_e2e/test_hex_document_state.py` :: **test_all_12_event_types_delivered** (line 1619) - The test asserts len(events) >= 1 and events[-1][0] == event_type. While >= 1 allows spurious events, this is a PARAMETRIZED test covering all 12 HexDocumentEvent values. The real gate is that for each event_type, the LAST event must be of that type (events[-1][0]). If an event type was never delivered, the test WOULD fail because events would be empty or the last event would be different. Some state changes (like DOCUMENT_OPENED) legitimately fire multiple events. The >= 1 bound is reasonable. Breaking the event delivery for any of the 12 types would cause this parametrized test to fail. _[Independent check: This is a parametrized test covering all 12 HexDocumentEvent values. Lines 1619-1620 assert 'len(events) >= 1' and 'events[-1][0] == event_type'. If event delivery for any event type is broken (e.g., the trigger function fires no event at all), events would be empty and 'events[-1]' would raise IndexError, causing the test to FAIL. If the wrong event type is delivered last, 'events[-1][0] == event_type' fails. This …]_
- `tests/test_hexcore_e2e/test_hexpat_compiler_e2e.py` :: **TestHexPatCompilerCompile::test_compile_simple_struct_has_name_key** (line 120) - This test WOULD catch real production breakage that test_compile_simple_struct_returns_json_string would miss. If the codegen regresses to return {"name": "Header", "fields": null} instead of a valid fields list, line 115 in the other test (field_names = [f["name"] for f in parsed["fields"]]) would crash with TypeError and fail. However, this test only checks the name assertion and would pass on green. Conversely, if fields structure is broken but name is correct, this test passes while a malformed-fields scenario would cause the comprehensive test to crash before checking field names. The tests are complementary, not subsumed. _[Independent check: test_compile_simple_struct_has_name_key (lines 120-124) only asserts 'parsed["name"] == "Header"'. The sibling test test_compile_simple_struct_returns_json_string (lines 111-118) asserts the SAME thing ('parsed["name"] == "Header"') plus field names. Every breakage that G0220 would catch is also caught by the sibling test. G0220 is fully subsumed. The weak-existence category and low severity are correct. (Note: G021…]_
- `tests/test_hexcore_e2e/test_patch_export.py` :: **TestGetPatches::test_write_bytes_produces_patch_entry** (line 48) - The test gates a real production capability: write_bytes() must cause get_patches() to return non-empty. If write_bytes is broken to not record patches in undo_mgr (line 158-162 of lib.rs), get_patches() would return [], and 'assert patches' would fail. This is a genuine gate on the patch-recording behavior. The sibling test at line 50-62 provides stronger content validation, but this test correctly gates that patches are recorded at all. _[Independent check: The finding claims weak-existence but the test at line 46-48 of test_patch_export.py calls `sample_doc_from_bytes.write_bytes(10, b"\xaa\xbb\xcc\xdd")` then asserts `assert patches` (truthiness). If write_bytes is broken to not record patches in the undo manager, get_patches() returns [] and `assert patches` fails on a falsy empty list. This is a genuine gate on the patch-recording behavior: any realistic breakage w…]_
- `tests/test_hexcore_e2e/test_patch_export.py` :: **TestIPSExport::test_ips_export_returns_bytes** (line 110) - The test gates a real production behavior: export_patches_ips() must not error or return None when given valid patches. If the export_ips() implementation (patch_export.rs lines 32-91) is broken to panic, raise, or return an error for valid input, the test would fail at line 109 (either exception or wrong type). This gates that the exporter is callable and successfully marshals bytes. The sibling tests at lines 82-90 and 92-100 validate format correctness separately. This test correctly gates the 'returns something' layer. _[Independent check: The finding claims weak-existence but the test at line 108-110 of test_patch_export.py calls `sample_doc_from_bytes.export_patches_ips()` after a write_bytes and asserts `isinstance(result, bytes)`. If export_patches_ips() panics, raises, or returns None/str/int instead of bytes, the assertion fails. This gates that the IPS exporter is callable and returns the correct type on valid input. The `confirmed: false` in t…]_
- `tests/test_hexcore_e2e/test_patch_export.py` :: **TestIPS32Export::test_ips32_export_returns_bytes** (line 156) - Same logic as test_ips_export_returns_bytes: the test gates that export_patches_ips32() (lib.rs line 528-532) succeeds and returns bytes when given valid patches. If the export_ips32() implementation (patch_export.rs lines 98-151) is broken to panic or error on valid input, this test fails at line 155. The sibling tests at lines 128-136 and 138-146 validate format. This test correctly gates the 'callable and returns' layer. _[Independent check: The finding claims weak-existence but the test at line 154-156 of test_patch_export.py calls `sample_doc_from_bytes.export_patches_ips32()` after a write_bytes and asserts `isinstance(result, bytes)`. If export_patches_ips32() panics, raises, or returns the wrong type, the assertion fails. This gates that the IPS32 exporter succeeds and returns bytes on valid input. The `confirmed: false` in the batch json confirms …]_
