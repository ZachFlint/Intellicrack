# Intellicrack GUI Audit — 2026-07-02

**Scope:** Exhaustive read-only audit of the PyQt6 GUI (~37,250 lines across 90 files under `src/intellicrack/ui/`).
**Method:** 26 parallel per-cluster finder agents (covering every UI file in full), each hunting four defect classes — layout/text-cutoff, theming/contrast, threading/crashes, logic/wiring — followed by one adversarial verifier per candidate finding (default-refute, Qt6-reality-aware). 163 agents total; 8.2M tokens.
**Result:** 131 raw findings verified → **131 CONFIRMED** (0 duplicate(s) merged), 6 rejected, 0 uncertain. **No code was modified.**

This audit is a follow-on to `audit/gui-audit-2026-07-01.md` (58 findings, since remediated). Verifiers were told not to re-report already-fixed items; findings here are new or previously uncovered. 4 finding(s) are flagged as regressions/incomplete prior fixes (see the REGRESSIONS list).

## Summary

| Severity | Count |
|----------|-------|
| CRITICAL | 6 |
| HIGH | 37 |
| MEDIUM | 69 |
| LOW | 19 |
| **Total** | **131** |

| Category | Count |
|----------|-------|
| Threading / crashes / freezes | 41 |
| Logic / wiring / leaks | 31 |
| Theming / contrast / readability | 6 |
| Layout / sizing / text-cutoff | 53 |

Severity legend: **CRITICAL/HIGH** = crash, dead feature, or data-integrity; **MEDIUM** = wrong behaviour / freeze / clipped-but-recoverable; **LOW** = cosmetic, edge-case, minor leak, i18n.

## Regressions / incomplete prior fixes

- **H1** — `src/intellicrack/ui/app.py:2528` — Applying sandbox settings blocks the GUI thread while tearing down live sandbox instances
- **H11** — `src/intellicrack/ui/panels/sandbox_panel.py:528` — _cleanup() blocks the GUI thread with untimed synchronous bridge calls during panel teardown
- **H12** — `src/intellicrack/ui/panels/vnc_widget.py:1878` — VNC disconnect blocks the GUI thread on unbounded network I/O
- **M46** — `src/intellicrack/ui/panels/ghidra_panel.py:375` — Labels/Bookmarks splitter is collapsible, unlike every other splitter in the panel

---

## CRITICAL — 6 finding(s)

### Threading / crashes / freezes

#### C1 — Process-memory open path schedules a coroutine via asyncio.ensure_future() with no running loop on the GUI thread — the task never executes during normal operation
- **File:** `src/intellicrack/ui/app.py:3026`
- **Severity:** CRITICAL  ·  **Category:** threading  ·  **Cluster:** app-shell
- **Defect:** After the user picks a memory region in the Process Memory dialog, `_on_process_attached` calls `open_fn(pid, base_addr, region_size)` (the async `HexBridge.open_process_memory`) and schedules it with `asyncio.ensure_future(coro_result)` instead of going through the established `run_bridge_coroutine`/`run_bridge_coroutine_async`/`AsyncWorker` patterns used everywhere else in this file. `ensure_future()` with no explicit `loop=` resolves via `asyncio.get_event_loop()`. On this GUI thread that resolves to the loop `main.py` created at startup (`loop = asyncio.new_event_loop(); asyncio.set_event_loop(loop)`) which is being driven only via `loop.run_until_complete(_run_application(...))`; `_run_application` itself is blocked synchronously inside the plain (non-awaited) call `exit_code = app.exec()` for the entire lifetime the GUI is usable. Because nothing yields back into that asyncio loop while `app.exec()` is running, `loop.create_task(coro_result)` merely enqueues a task that cannot actually run any of its steps until `app.exec()` returns (i.e., until the whole application is shutting down). The dialog's OK button therefore appears to silently do nothing while the app is open; the queued `_on_done` callback (which shows a `QMessageBox` and touches `self`) only fires during app teardown, by which point the MainWindow may already be destroyed, risking a crash/RuntimeError on shutdown. This is unlike every other bridge call in the file, which correctly routes through `AsyncWorker` or the persistent background loop in `async_bridge.py`.
- **Failure:** User attaches to a process via the Process panel, gets prompted with the memory-region picker, selects a region, and clicks OK. `open_process_memory` is scheduled but never actually runs while the app is open — no hex data loads, no error appears, and the feature is effectively dead. If it does eventually resolve during app shutdown, `_on_done`'s `QMessageBox.warning(self, ...)` can execute against a torn-down `MainWindow`.
- **Evidence:**
  ```python
  coro_result: object = open_fn(pid, base_addr, region_size)
          if not asyncio.iscoroutine(coro_result):
              return
  
          task: asyncio.Task[dict[str, object]] = asyncio.ensure_future(coro_result)
  
          def _on_done(fut: asyncio.Future[dict[str, object]]) -> None:
              try:
                  result = fut.result()
  ```
- **Verified:** Read src/intellicrack/ui/app.py:2948-3036. `_on_process_attached` is a normal Qt slot connected via `signal.connect(self._on_process_attached)` (line 2945) with no thread-affinity wrapper, so it executes on the GUI thread. At line 3018-3026 it does `open_fn = getattr(hex_bridge, "open_process_memory", None)`, calls it to get `coro_result`, checks `asyncio.iscoroutine(coro_result)`, and if true calls `asyncio.ensure_future(coro_result)` with no explicit loop. `HexBridge.open_process_memory` (src/intellicrack/bridges/hex_editor.py:8149) is declared `async def`, so the coroutine check passes and the buggy path is live, not dead code.

Verified main.py: `loop = asyncio.new_event_loop(); asyncio. …

#### C2 — State-holder callback mutates Qt widgets directly when invoked from the bridge's background event-loop thread
- **File:** `src/intellicrack/ui/panels/hex_editor/panel.py:1002`
- **Severity:** CRITICAL  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** HexEditorPanel.set_state_holder registers `on_state_event` as a plain Python callback via `state_holder.register_callback(on_state_event, source_id="panel")`. HexDocumentState._dispatch_one (bridges/hex_state.py) invokes every registered callback synchronously, with no thread marshalling of any kind (no pyqtSignal, no QMetaObject.invokeMethod, no queue). HexEditorBridge's async RPC methods (e.g. add_highlight_rule, remove_highlight_rule, replace_bytes/write_bytes paths, open_file) call `state_holder.notify_*(..., source="bridge")` from inside their coroutine bodies. Those coroutines are dispatched via async_bridge.BridgeCallWorker.run(), which does `asyncio.run_coroutine_threadsafe(self._coro, loop)` against the persistent background 'bridge-event-loop' thread -- i.e. the coroutine (and therefore the notify_* call and every registered callback) executes on that background thread, not the Qt GUI thread. `on_state_event` then performs direct Qt mutations for every event branch: `self.load_file(...)` (DOCUMENT_OPENED), `self._hex_widget.goto_offset(...)`/`_update_data_inspector` (CURSOR_MOVED), `_update_viewport()`/`_on_data_changed()` (DATA_MODIFIED), `set_selection_range`+`_update_viewport` (SELECTION_CHANGED), `_populate_template_combo()` (TEMPLATE_REGISTERED), and `_apply_bridge_highlight_rule_added/_removed` which call `QTreeWidget`/`QListWidget.addItem/takeItem` and `self._hex_widget.update()` (HIGHLIGHT_RULE_ADDED/REMOVED). None of this is wrapped in a pyqtSignal-based marshal back to the GUI thread, unlike the pattern the prior audit established elsewhere in this codebase (BridgeCallWorker/GenericCallableWorker deliver results via pyqtSignal so slots run on the receiver's thread). Any AI/bridge-driven hex-editor action taken while the panel is open (e.g. an MCP tool call adding a highlight rule, or saving the document) will paint/mutate Qt widgets from a non-GUI thread.
- **Failure:** An AI tool call invokes HexEditorBridge.add_highlight_rule(...) while the hex editor panel is open. The coroutine runs on the 'bridge-event-loop' thread and calls state_holder.notify_highlight_rule_added(rule, source="bridge"), which synchronously invokes the panel's on_state_event callback on that same background thread. That callback calls self._apply_bridge_highlight_rule_added(rule), which does `self._highlight_rules_list.addItem(item)` and `self._hex_widget.update()` off the GUI thread -- a Qt cross-thread widget-mutation violation that can crash the process or corrupt the widget's paint state.
- **Evidence:**
  ```python
  def on_state_event(event_type: object, data: dict[str, Any]) -> None:
      ...
      if event_type == evt.DOCUMENT_OPENED:
          file_path_str = data.get("file_path")
          if file_path_str:
              if self.document is not None:
                  self.document = None
              self.load_file(file_path_str)
      ...
  self._state_callback = on_state_event
  state_holder.register_callback(on_state_event, source_id="panel")
  ```
- **Verified:** Verified every link in the chain against actual source: (1) panel.py:1002-1048 defines on_state_event exactly as quoted and registers it unwrapped via state_holder.register_callback(on_state_event, source_id="panel") with no pyqtSignal/QMetaObject marshalling anywhere. (2) hex_state.py:_dispatch_one (lines 676-704) invokes every registered callback synchronously via `entry.fn(event_type, data)` inside the calling thread's stack -- there is no thread-marshalling code anywhere in HexDocumentState. (3) HexEditorBridge.add_highlight_rule (hex_editor.py:7556-7585) is `async def` and calls `self.state_holder.notify_highlight_rule_added(rule, source=\"bridge\")` synchronously inline in the coroutin …
- **Note:** Report is accurate as written. Minor nuance not stated in the report: MCP-driven bridge calls (the primary failure scenario given, "AI tool call invokes add_highlight_rule") reach the coroutine via core/tools.py ToolRegistry.execute_tool_call's `await method(...)` on the MCP server's own asyncio loop thread, not necessarily via async_bridge.BridgeCallWorker's 'bridge-event-loop' thread (that path is used for GUI-button-triggered bridge calls). Both paths are equally non-GUI threads, so the core defect and CRITICAL severity are unaffected -- this is only a refinement of which specific background thread executes the callback, not a correction to the verdict.

#### C3 — OAuth login flow runs synchronously on the Qt GUI thread, freezing the app for the duration of user browser login
- **File:** `src/intellicrack/ui/provider_config.py:1859`
- **Severity:** CRITICAL  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `_run_oauth_flow` (invoked directly from the `OAuth Login` button click handler `_on_start_oauth` -> `start_oauth_flow`, all on the GUI thread) calls `run_bridge_coroutine(_run_oauth())`. `run_bridge_coroutine` is documented in async_bridge.py as 'the **blocking** variant' that calls `future.result(timeout=timeout_s)` with no timeout given here, so it blocks the calling (GUI) thread until the coroutine finishes. The coroutine awaits `manager.run_authorization_flow(oauth_config)`, which opens a browser via `webbrowser.open(auth_url)` and then calls `server.wait_for_callback()` to wait for the user to complete login in their browser (oauth.py lines 1360-1366). This is a human-timescale wait (seconds to minutes) executed while the GUI thread is blocked.
- **Failure:** User clicks 'OAuth Login' for any provider (e.g. Anthropic/Google). The entire Intellicrack window becomes unresponsive ('Not Responding' on Windows) — cannot be moved, resized, or interacted with, and no other UI updates render — until the user finishes authenticating in the browser and the callback is received or the flow times out.
- **Evidence:**
  ```python
  async def _run_oauth() -> ProviderCredentials | None:
      await manager.run_authorization_flow(oauth_config)
      return await manager.to_provider_credentials(oauth_provider)
  
  creds = run_bridge_coroutine(_run_oauth())
  ```
- **Verified:** Read src/intellicrack/ui/provider_config.py 1725-1867: the OAuth button is wired via `self._oauth_btn.clicked.connect(self._on_start_oauth)` (line 1438), and `_on_start_oauth` → `start_oauth_flow` → `_run_oauth_flow` all execute synchronously on the calling (GUI) thread — no QThread, no `run_bridge_coroutine_async` (contrast with the async pattern used elsewhere in the same file at line 3161 for a different operation). At line 1859, `creds = run_bridge_coroutine(_run_oauth())` calls the blocking helper with no `timeout_s`. Read async_bridge.py 382-408: when there is no already-running asyncio loop on the calling thread (true for the Qt GUI thread, confirmed by the `except RuntimeError: runni …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

### Logic / wiring / leaks

#### C4 — SectionsMixin's empty _select_template stub shadows the real TemplatesMixin implementation via MRO
- **File:** `src/intellicrack/ui/panels/hex_editor/sections.py:121`
- **Severity:** CRITICAL  ·  **Category:** logic  ·  **Cluster:** hex-transforms
- **Defect:** SectionsMixin declares `_select_template(self, template_name)` as an empty no-op stub (docstring only, implicit `return None`). HexEditorPanel's base-class list in panel.py is `(DataInspectorMixin, ..., SectionsMixin, TemplatesMixin, ...)` (SectionsMixin precedes TemplatesMixin). Because none of these mixins share a common non-`object` ancestor, Python's MRO resolves `self._select_template` to the first class in `__mro__` that defines it, which is SectionsMixin's no-op stub -- TemplatesMixin's real implementation (`templates.py:357`, which does `idx = self._template_combo.findText(template_name); self._template_combo.setCurrentIndex(idx)`) is completely unreachable through `self._select_template(...)` and is dead code.
- **Failure:** User opens any PE/ELF/Mach-O/ZIP file. `load_file()` calls `_auto_detect_file_type()` (sections.py:413), which correctly detects the format and calls `self._select_template(template_name)` (sections.py:437) intending to auto-select the matching structure template in the Templates tab combo box. Because MRO resolves to SectionsMixin's empty stub instead of TemplatesMixin's real method, the Templates combo box is never updated -- the auto-detect-and-select-template feature silently does nothing on every file open, even though the file-info label correctly shows the detected format tag.
- **Evidence:**
  ```python
  def _select_template(self, template_name: str) -> None:
          """Select a structure template by name for the current file type.
  
          Args:
              template_name: Identifier of the template to activate.
          """
  ```
- **Verified:** Verified all three pieces of the claim directly in the source:
1. sections.py:121-126 — SectionsMixin._select_template is an empty stub: signature + docstring only, no body statements, implicitly returns None.
2. panel.py:116-139 — HexEditorPanel's base list is (DataInspectorMixin, SearchMixin, StatisticsMixin, HashingMixin, DisassemblyMixin, YaraMixin, TransformsMixin, PatternEditorMixin, BookmarksMixin, PatchesMixin, SectionsMixin, TemplatesMixin, CalculatorMixin, ...). SectionsMixin (index 10) precedes TemplatesMixin (index 11).
3. Both `class SectionsMixin:` (sections.py:94) and `class TemplatesMixin:` (templates.py:60) inherit only from object (no shared non-object ancestor), so C3 line …
- **Note:** Claim is accurate as written, no corrections needed. Severity CRITICAL is reasonable given this silently breaks a documented auto-detect-and-select-template feature on every single file open across all four supported formats (PE/ELF/Mach-O/ZIP), with no exception or visible error to alert the user.

#### C5 — Color-mode combo box (Entropy/Byte Value/Content Type) has no effect on hex view rendering
- **File:** `src/intellicrack/ui/panels/hex_editor_widget.py:514`
- **Severity:** CRITICAL  ·  **Category:** logic  ·  **Cluster:** hex-widget-core
- **Defect:** set_color_mode() stores self._color_mode and invalidates the entropy/content-class caches, and its docstring explicitly claims 'the actual recomputation is lazy and happens inside paint handlers when the mode requires it.' But self._color_mode is never read anywhere in the paint pipeline (_paint_content, _paint_data_rows, _paint_row_hex_groups, _paint_hex_group, _paint_hex_group_background, _set_hex_group_pen, _paint_ascii_byte) - only rgba8 display-mode coloring, highlight-rule coloring, selection, and modified/zero-byte coloring are applied. _ensure_content_class_cache() (the function that would compute the 'Content Type' data) is dead code: defined at line 589 but never called from anywhere in the codebase. This is wired to a real toolbar control in hex_editor/panel.py (QComboBox with items 'No Coloring'/'Entropy Heatmap'/'Byte Value'/'Content Type' calling widget.set_color_mode(mode) via _on_color_mode_changed), so three of the four user-selectable coloring modes are completely non-functional.
- **Failure:** User opens a binary in the hex editor, selects 'Entropy Heatmap' (or 'Byte Value' or 'Content Type') from the color-mode toolbar combo box expecting per-byte background coloring in the hex/ASCII grid. Nothing changes visually - the view renders identically to 'No Coloring' because no paint routine ever branches on self._color_mode.
- **Evidence:**
  ```python
  def set_color_mode(self, mode: str) -> None:
      """Change the byte color-mapping mode.
  
      Invalidates any cached per-byte colour data so that the next paint
      recomputes from the current document. The actual recomputation is
      lazy and happens inside paint handlers when the mode requires it.
      ...
      """
      self._color_mode = mode
      self._invalidate_color_caches()
      self.update()
  ```
- **Verified:** Read hex_editor_widget.py lines 400-627 and 725-1164, plus grepped the whole file for `_color_mode`. It appears exactly 3 times: declared at line 400 (`self._color_mode: str = "none"`), set at line 525 inside `set_color_mode()` (lines 514-527), and nowhere else. `set_color_mode()` only assigns `self._color_mode`, calls `_invalidate_color_caches()` (clears `_entropy_cache`/`_content_class_cache`, lines 529-532), and calls `self.update()`. The paint pipeline was traced in full: `_paint_content` (725) -> `_paint_data_rows` (760) -> `_paint_row_hex_groups` (815) -> `_paint_hex_group` (963) -> `_paint_hex_group_background` (1102) and `_set_hex_group_pen` (1137). None of these reference `self._col …

#### C6 — load_execution_report uses getattr() on TypedDict/dict report entries, always yielding placeholder data
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:2509`
- **Severity:** CRITICAL  ·  **Category:** logic  ·  **Cluster:** sandbox-panel
- **Defect:** ExecutionReport.file_changes / registry_changes / network_activity are typed as list[FileChange] / list[RegistryChange] / list[NetworkActivity], and FileChange/RegistryChange/NetworkActivity are all TypedDicts (plain dict instances at runtime, defined in src/intellicrack/sandbox/base.py). load_execution_report iterates these lists and reads fields via getattr(change, "operation", "unknown") instead of dict access (change["operation"] or change.get("operation")). Because a plain dict object has no attribute named "operation"/"path"/"details"/etc., every getattr() call falls through to its default value regardless of the actual report content. In addition, the network_activity block uses field names that do not even exist on NetworkActivity ("destination", "port", "data_size" instead of the real keys "remote_address", "remote_port", "bytes_sent"/"bytes_received"), so even a dict-access fix would still need the correct keys. The method is a live, wired code path (called from ui/tools.py's report loader whenever a saved execution report is opened), so this is not dead code.
- **Failure:** User opens a previously saved sandbox execution report (via the report-loading action in ui/tools.py, which calls sandbox_panel.load_execution_report(report)). Despite the report containing real file/registry/network activity, the File Changes tab shows 'unknown' / '' / '' for every row, the Registry Changes tab shows the same, and the Network Activity tab shows 'unknown' / '' / '0 / 0 bytes' for every row -- the loaded report is effectively blank/meaningless no matter what data it actually contains.
- **Evidence:**
  ```python
  for change in report.file_changes:
      op = getattr(change, "operation", "unknown")
      path = getattr(change, "path", "")
      details = getattr(change, "details", "")
      item = QTreeWidgetItem([str(op), str(path), str(details)])
  ```
- **Verified:** Read src/intellicrack/ui/panels/sandbox_panel.py lines 2498-2543. `load_execution_report` iterates `report.file_changes` / `report.registry_changes` / `report.network_activity` and reads each element's fields via `getattr(change, "operation", "unknown")` etc. (lines 2509-2511, 2517-2519, 2525-2528). Confirmed against src/intellicrack/sandbox/base.py: `FileChange` (line 112), `RegistryChange` (line 122), and `NetworkActivity` (line 133) are all `TypedDict`s, which are plain `dict` instances at runtime — they have no `.operation`/`.path`/`.key`/`.value`/`.protocol` attributes, so every `getattr()` call silently falls through to its default ("unknown", "", 0), regardless of actual content. `Exe …
- **Note:** Claim is accurate as stated; no correction needed to title, description, or failure scenario. Additionally verified via tests/test_ui/test_realcov_14b_sandbox_report.py that the existing test gate for this method only checks row counts, not cell values, which is why this bug was never caught.

---

## HIGH — 37 finding(s)

### Threading / crashes / freezes

#### H1 — Applying sandbox settings blocks the GUI thread while tearing down live sandbox instances  **[REGRESSION]**
- **File:** `src/intellicrack/ui/app.py:2528`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** app-shell
- **Defect:** `_apply_sandbox_settings` is invoked synchronously on the GUI thread from `_on_configure_sandbox` (right after the modal `SandboxConfigDialog.exec()` returns) and from `_on_sandbox_settings_updated` (the dialog's live Apply signal, also GUI-thread). It calls the **blocking** variant `run_bridge_coroutine(self.sandbox_manager.destroy_all())`, which internally awaits `instance.sandbox.stop()` for every live instance (Windows Sandbox/QEMU teardown can take real wall-clock seconds) and only returns once every instance has actually stopped. This directly contradicts the already-remediated root cause #3 from the prior audit ("blocking bridge calls on the UI thread ... moved to run_bridge_coroutine_logged async path") — this call site still uses the blocking form instead of `run_bridge_coroutine_async`/`run_bridge_coroutine_logged`.
- **Failure:** User has an active sandbox instance open, opens Settings -> Configure Sandbox, changes a value, and clicks OK/Apply. The entire UI (including the still-open dialog's owning window) freezes for as long as the live sandbox instance(s) take to stop.
- **Evidence:**
  ```python
  try:
              run_bridge_coroutine(self.sandbox_manager.destroy_all())
          except (RuntimeError, OSError) as e:
              _logger.warning("sandbox_manager_teardown_failed", error=str(e))
  ```
- **Verified:** Read src/intellicrack/ui/app.py:2461-2540. `_on_configure_sandbox` (wired to the "Configure Sandbox..." QAction at line 704, a plain GUI-thread menu trigger) calls `_apply_sandbox_settings` synchronously at line 2476 after `dialog.exec()` returns. `_on_sandbox_settings_updated` (connected at line 2473 to the dialog's `settings_updated` signal, a normal same-thread Qt signal fired while the modal dialog is still open) also calls `_apply_sandbox_settings` synchronously at line 2497. Inside `_apply_sandbox_settings`, line 2528 calls `run_bridge_coroutine(self.sandbox_manager.destroy_all())` — confirmed via src/intellicrack/ui/panels/async_bridge.py:346-408 that this is explicitly documented as  …
- **Note:** This is effectively a regression/incomplete application of the prior audit's root-cause #3 fix (blocking bridge calls moved to async path) — this specific call site (and its twin at line 3319, also `run_bridge_coroutine(self.sandbox_manager.destroy_all())`) was missed. The report's description and failure scenario are accurate as written; no correction needed beyond confirming is_regression.

#### H2 — LogFileTailReader.stop() blocks the GUI thread and risks destroying a still-running QThread
- **File:** `src/intellicrack/ui/log_viewer/_tail_reader.py:219`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** log-viewer
- **Defect:** `stop()` calls `self._initial_worker.wait(2000)` synchronously on the GUI thread (it is invoked from `LogViewerWindow.closeEvent` and `_on_reload_from_disk`, both GUI-thread call paths), which can stall the entire UI for up to two seconds. Worse, the return value of `wait()` is never checked: if the `InitialLoadWorker` QThread has not finished within the 2-second window (e.g. a slow network share, or a very large/rotated log file), `stop()` returns anyway, leaving the thread running. Immediately afterward, `_on_reload_from_disk` in window.py calls `self._tail_reader.deleteLater()` on the parent `LogFileTailReader`, and `InitialLoadWorker` was created as its child (`InitialLoadWorker(self._log_path, self._max_initial_bytes, parent=self)` in `start()`). When the parent's deferred deletion runs, Qt cascades destruction into the still-running child QThread, which is documented Qt behavior to abort the process with "QThread: Destroyed while thread is still running".
- **Failure:** User clicks "Reload from Disk" (or closes the log viewer window) while the historical tail read is still in progress on a large/slow log file (>2s to read). `stop()` times out without the worker finishing, `_on_reload_from_disk` still calls `deleteLater()` on the reader, and once the deferred deletion is processed while `InitialLoadWorker.run()` is still executing, the application crashes.
- **Evidence:**
  ```python
  if self._initial_worker is not None and self._initial_worker.isRunning():
              self._initial_worker.wait(2000)
  ```
- **Verified:** Read src/intellicrack/ui/log_viewer/_tail_reader.py and window.py in full to verify.

1. stop() runs on the GUI thread in both call paths. window.py:656 `_on_reload_from_disk` (a slot fired by `reload_action.triggered`, a GUI signal) and window.py:858 `closeEvent` (invoked synchronously by Qt's window-close machinery on the main thread) both call `self._tail_reader.stop()` directly — no thread hop.

2. `stop()` (_tail_reader.py:209-220):
```
if self._initial_worker is not None and self._initial_worker.isRunning():
    self._initial_worker.wait(2000)
```
This is a blocking wait on the GUI thread for up to 2000 ms while `InitialLoadWorker` is still running — a genuine event-loop freeze, and th …
- **Note:** Report is accurate as written; no correction needed to title, description, or failure scenario. The relevant blocking clause spans lines 219-220 (`if ... isRunning(): self._initial_worker.wait(2000)`), consistent with the reported anchor line 219.

#### H3 — Panel teardown blocks the GUI thread indefinitely waiting on Ghidra shutdown
- **File:** `src/intellicrack/ui/panels/ghidra_panel.py:214`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** ghidra-panel
- **Defect:** `_cleanup()` calls `run_bridge_coroutine(self._bridge.shutdown())` with no `timeout_s`. `run_bridge_coroutine`'s blocking path does `future.result(timeout=timeout_s)`; with `timeout_s=None` this waits forever on the calling (GUI) thread. Every other bridge call in this file goes through the non-blocking `run_bridge_coroutine_logged`/`BridgeCallWorker` path specifically to avoid freezing the UI, but the teardown path was left on the old fully-blocking helper with no wall-clock ceiling. If `GhidraBridge.shutdown()` stalls (e.g. `_close_bridge_client`'s synchronous socket close hangs, or a stdout/stderr drain thread never exits), the panel's `_cleanup()` -- invoked when the tab/window/application closes -- never returns and the whole Qt event loop is frozen with no way for the user to force-quit through the UI.
- **Failure:** User closes the Ghidra panel (or the app) while the headless Ghidra RPC socket is in a half-open/unresponsive state (e.g. the headless process was killed externally but the socket file descriptor is still held); `bridge.shutdown()`'s `asyncio.to_thread(self._close_bridge_client, ...)` blocks on the stalled socket, `run_bridge_coroutine` waits forever, and the entire GUI hangs on close with no timeout to fall back on.
- **Evidence:**
  ```python
  if self._bridge is not None and self._bridge.state.is_ready():
      try:
          run_bridge_coroutine(self._bridge.shutdown())
      except (RuntimeError, ConnectionError, OSError):
          _logger.exception("ghidra_shutdown_failed", bridge_type="ghidra")
  ```
- **Verified:** ghidra_panel.py:210-216 `_cleanup()` calls `run_bridge_coroutine(self._bridge.shutdown())` with no `timeout_s` keyword, unlike every other bridge call in the file which uses the non-blocking `run_bridge_coroutine_logged`/`BridgeCallWorker` path. In async_bridge.py, `run_bridge_coroutine`'s blocking path (lines 400-408) does `future.result(timeout=timeout_s)`; with `timeout_s` defaulting to `None` this is `future.result(timeout=None)`, which blocks the calling thread with no wall-clock ceiling. `_cleanup()` is invoked synchronously on the GUI thread: `base_panel.py:317` `stop_tool()` calls `self._cleanup()` directly (no worker thread), and `stop_tool()` is reached from `tools.py:844` `self.ta …
- **Note:** The auditor's specific illustrative failure mechanism ("_close_bridge_client's synchronous socket close hangs") is not the most precise trigger — socket.close() itself does not block indefinitely and comms_thread.join has an explicit 2.0s timeout. The genuine unbounded path is ghidra.py:5641, `await asyncio.to_thread(self._process.wait)` called after `self._process.kill()` with no timeout, inside the same shutdown() coroutine that run_bridge_coroutine() awaits without timeout_s. The core claim (missing timeout_s on the blocking run_bridge_coroutine call in _cleanup, causing a GUI-thread freeze inconsistent with the rest of the file) is fully validated regardless.

#### H4 — Annotated HTML/PDF export renders the full document while blocking the GUI thread
- **File:** `src/intellicrack/ui/panels/hex_editor/export_report.py:172`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-misc
- **Defect:** _on_export_annotated_html (line 172) and _on_export_annotated_pdf (line 230) call the blocking run_bridge_coroutine() to render an entire annotated byte range (defaulted to the full document length in AnnotatedExportRangeDialog) as HTML or PDF. Unlike the disassembly/YARA/sandbox/process-memory mixins in this same package, which all use run_bridge_coroutine_logged for exactly this kind of potentially slow bridge work, this mixin blocks the Qt main thread for the entire render+write duration.
- **Failure:** User exports an annotated HTML or PDF report for a large binary (the default range dialog pre-fills the end offset with the full document length); rendering thousands of hex rows into markup happens synchronously on the GUI thread via future.result(), freezing the entire application (including window repaint) for as long as the render takes, with no progress feedback.
- **Evidence:**
  ```python
  html_result = run_bridge_coroutine(bridge.export_annotated_html(start, end, bytes_per_row))
  ```
- **Verified:** export_report.py line 172 (`html_result = run_bridge_coroutine(bridge.export_annotated_html(start, end, bytes_per_row))`) and line 230 (`run_bridge_coroutine(bridge.export_annotated_pdf(...))`) are called synchronously from `_on_export_annotated_html`/`_on_export_annotated_pdf`, which are wired directly to `QAction.triggered` in panel.py (lines 1179, 1182) — i.e. they execute on the Qt GUI thread. `run_bridge_coroutine` in async_bridge.py is explicitly documented as 'the **blocking** variant' (line 361) and its implementation (lines 400-403) does `future = asyncio.run_coroutine_threadsafe(resolved_coro, loop); return future.result(timeout=timeout_s)`. Neither call site passes `timeout_s`, so …
- **Note:** No correction needed; report's line, severity, and mechanism are accurate. Same defect independently exists at line 230 for the PDF path.

#### H5 — Hash/PE-checksum operations call native document methods synchronously on the GUI thread
- **File:** `src/intellicrack/ui/panels/hex_editor/hashing.py:131`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-misc
- **Defect:** _on_calculate_hash, _on_hash_selection, _on_verify_pe_checksum and _on_repair_pe_checksum all call hexcore document methods (compute_hash, compute_hash_range, verify_pe_checksum, repair_pe_checksum) directly on the Qt main thread with no worker thread. The module docstring explicitly says 'the UI thread never has to materialise the full file in Python', but that only addresses memory, not time: hashing a large PE/ELF binary (SHA-256/SHA-512 over hundreds of MB to multiple GB) or repairing/verifying its checksum blocks the calling Python frame, and Qt cannot pump its event loop until the native call returns, so the whole application appears frozen for the duration. This is inconsistent with the file's own _on_custom_crc, which explicitly avoids this by spawning a streaming background worker (CustomCrcDialog) specifically to keep long-running CRC work off the UI thread.
- **Failure:** User loads a multi-hundred-MB or multi-GB binary and clicks Calculate Hash (SHA-256) or Verify/Repair PE Checksum; the entire UI (window dragging, other tabs, cursor movement) freezes for the multi-second-to-minute duration of the native hash pass, with no busy indicator and no way to cancel.
- **Evidence:**
  ```python
  result = self.document.compute_hash(algo)
  ```
- **Verified:** Read hashing.py lines 125-347: `_on_calculate_hash` (line 131: `self.document.compute_hash(algo)`), `_on_hash_selection` (line 200: `compute_hash_range`), `_on_verify_pe_checksum` (line 226: `verify_pe_checksum`), and `_on_repair_pe_checksum` (line 305: `repair_pe_checksum`, plus a second synchronous `verify_pe_checksum` call at line 323) are all invoked directly inside Qt slot methods on whatever thread the slot executes on, i.e. the GUI thread when wired to `clicked` signals (confirmed by `_create_pe_checksum_group`, lines 179-184, connecting `verify_btn.clicked` and `repair_btn.clicked` straight to these slots). None of the four spawns a QThread/QRunnable/worker — no `moveToThread`, no `Q …

#### H6 — Pattern-highlight offsets are re-resolved with a synchronous full-document search on every single data edit
- **File:** `src/intellicrack/ui/panels/hex_editor/highlighting.py:490`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** `refresh_pattern_highlights()` is invoked from `HexEditorPanel._on_data_changed()` (panel.py line 905, `self.refresh_pattern_highlights()`), which fires on every `HexEditorWidget.data_changed` signal -- i.e. on every single byte edit made in the hex view. For each active `pattern`-type highlight rule, it calls `search_fn(pattern_raw, _HIGHLIGHT_PATTERN_MAX_MATCHES)` (the document's native `search_hex`, a whole-document scan) synchronously on the GUI thread, with no worker/async dispatch, unlike the properly-async search paths in search.py.
- **Failure:** User has one or more pattern-type highlight rules active (e.g. highlighting all occurrences of a magic byte sequence) and edits a single byte in the hex view. Every keystroke/edit triggers a synchronous whole-document `search_hex` scan per active pattern rule on the GUI thread before the UI can repaint, making typing feel like it stutters or freezes proportional to document size and rule count.
- **Evidence:**
  ```python
  for rule in cast("list[Any]", rules_attr):
      condition_type = getattr(rule, "condition_type", None)
      if condition_type != "pattern":
          continue
      ...
      try:
          matches = search_fn(pattern_raw, _HIGHLIGHT_PATTERN_MAX_MATCHES)
  ```
- **Verified:** Confirmed by tracing the full call chain. In highlighting.py, `refresh_pattern_highlights()` (lines 461-509) iterates `self._hex_widget._highlight_rules`, and for each rule with `condition_type == "pattern"` calls `search_fn(pattern_raw, _HIGHLIGHT_PATTERN_MAX_MATCHES)` at line 490 where `search_fn = document.search_hex` (line 474) — a direct, synchronous call to the native `hexcore.HexDocument.search_hex` binding (src/intellicrack-hexcore/src/lib.rs:233, backed by `search_bytes` in search.rs), with no QThread/worker dispatch and no debounce/QTimer anywhere in the file (grep for QTimer/debounce/throttle/singleShot returned nothing).

This is wired directly into the per-edit path: panel.py:37 …
- **Note:** Report is accurate as written; no correction needed to line, severity, or failure scenario. Confirmed the codebase already has an established async pattern (GenericCallableWorker in search.py) for this exact same search_hex call, which strengthens the finding — this is an inconsistency/regression-prone gap rather than an inherent tool limitation.

#### H7 — Replace All / Replace issue blocking synchronous bridge RPCs directly from GUI button-click slots
- **File:** `src/intellicrack/ui/panels/hex_editor/search.py:983`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** `_on_replace_all` (and `_replace_all_regex_matches` / `_resolve_hex_replace_pair` / `_resolve_single_replacement_bytes`) call the **blocking** `run_bridge_coroutine()` variant (imported at top of file) directly from a synchronous Qt slot triggered by a button click, instead of the async `run_bridge_coroutine_async`/`run_bridge_coroutine_logged` pattern used throughout the rest of the hex-editor mixins (and explicitly documented as the non-blocking variant with signal-based delivery). `run_bridge_coroutine` schedules the coroutine on the background loop via `asyncio.run_coroutine_threadsafe` and then calls `future.result(timeout_s)` -- with `timeout_s` defaulted to `None`, this blocks the calling (GUI) thread until the whole-document byte-pattern replace RPC completes, freezing the Qt event loop for the duration.
- **Failure:** User clicks 'Replace All' in Hex/Text/Numeric mode on a large loaded binary. `bridge.replace_bytes(pattern_hex, replacement_hex)` performs a full-document pattern scan-and-replace natively; while that RPC runs, `future.result()` blocks the GUI thread, so the entire application (menus, repaint, other panels) is unresponsive until the replace finishes, with no way to cancel.
- **Evidence:**
  ```python
  count = run_bridge_coroutine(bridge.replace_bytes(pattern_hex, replacement_hex))
  ```
- **Verified:** Code confirms the claim exactly. `_on_replace_all` (search.py:941) is wired directly as a synchronous Qt slot via `btn.clicked.connect(handler)` in `base_panel.py:167` (`_add_secondary_button`), invoked from `panel.py:301` (`self._add_secondary_button(toolbar, "Replace All", self._on_replace_all)`). At search.py:983, `count = run_bridge_coroutine(bridge.replace_bytes(pattern_hex, replacement_hex))` calls the blocking variant imported at line 30 (`from intellicrack.ui.panels.async_bridge import GenericCallableWorker, run_bridge_coroutine`). `run_bridge_coroutine` (async_bridge.py:346-408) is explicitly documented as "the **blocking** variant" and, when called from the GUI thread (no running a …
- **Note:** Report is accurate as written; no correction needed to line, severity, or failure scenario. Confirmed via reading async_bridge.py's run_bridge_coroutine (blocking, future.result with default timeout_s=None) versus run_bridge_coroutine_async/GenericCallableWorker (non-blocking, signal-based) used elsewhere in the same file for search operations.

#### H8 — Quick-arithmetic apply blocks the GUI thread with the synchronous run_bridge_coroutine()
- **File:** `src/intellicrack/ui/panels/hex_editor/transforms.py:1027`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-transforms
- **Defect:** `_on_apply_arithmetic` is connected directly to `arith_apply_btn.clicked` (transforms.py:411) and, on the Qt main thread, calls the *blocking* `run_bridge_coroutine()` (not `run_bridge_coroutine_async`/`run_bridge_coroutine_logged`, which the same file and sibling mixins otherwise use for every other bridge dispatch). `run_bridge_coroutine()` submits the coroutine to the persistent background event-loop thread and then calls `future.result(timeout=None)`, which blocks the calling thread -- here, the Qt event loop -- until the native hexcore XOR/AND/shift/rotate transform completes on the selection.
- **Failure:** User selects a large region (e.g. several MB) in the hex view and clicks "Apply to Selection" for an XOR/rotate operation. The entire Qt event loop freezes (no repaint, no input processing, spinning cursor) for the duration of the native transform, unlike every other bridge-backed action in this panel (section/import/export population, signature scans, base-convert) which route through the non-blocking `run_bridge_coroutine_logged` and keep the UI responsive.
- **Evidence:**
  ```python
  run_bridge_coroutine(bridge.select_range(sel_start, bridge_end))
              run_bridge_coroutine(
                  bridge.apply_arithmetic_to_selection(op_short, key_hex=key_hex, count=count),
              )
  ```
- **Verified:** Read transforms.py:973-1041. `_on_apply_arithmetic` is a plain method connected via `arith_apply_btn.clicked.connect(self._on_apply_arithmetic)` (line 411), so it executes as a slot on the Qt GUI thread. At lines 1027-1030 it calls `run_bridge_coroutine(bridge.select_range(...))` and `run_bridge_coroutine(bridge.apply_arithmetic_to_selection(op_short, key_hex=key_hex, count=count))` — the blocking variant imported at line 32 (`from intellicrack.ui.panels.async_bridge import run_bridge_coroutine`), not `run_bridge_coroutine_logged`/`_async`. Reading async_bridge.py:346-408 confirms `run_bridge_coroutine()` submits the coroutine to a persistent background event-loop thread via `asyncio.run_cor …
- **Note:** Core defect (blocking GUI-thread call via unbounded future.result) is confirmed exactly as described. Minor overstatement in the narrative: it is not the *only* place in the panel using the synchronous run_bridge_coroutine (data_inspector.py, va_mapping.py, comparison.py, patches.py, and search.py also call it), so "unlike every other bridge-backed action in this panel" is slightly inaccurate, but this does not undermine the finding itself.</notes>

#### H9 — Full-document entropy scan runs synchronously on the GUI thread on every single-byte edit when the minimap is visible
- **File:** `src/intellicrack/ui/panels/hex_editor_widget.py:427`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** hex-widget-core
- **Defect:** data_changed is connected to both _invalidate_color_caches (clears self._entropy_cache) and _refresh_minimap_if_visible (which, when the minimap is visible, calls _refresh_minimap_entropy -> _ensure_entropy_cache -> document.entropy_map(block_size)). Since these are default (direct) connections fired synchronously on the emitting thread, and data_changed is emitted from _handle_hex_input/_handle_ascii_input/_delete_byte_impl/_delete_selection_impl/_do_paste/_do_undo/_do_redo - all invoked directly from keyPressEvent on the GUI thread - a single keystroke edit while the minimap is shown triggers a full O(document length) entropy recomputation (entropy_map covers the whole document, block size scaled only to bound bucket count, not total bytes scanned) inline on the UI thread before the next repaint. This is architecturally inconsistent with the app's own precedent: CustomCrcDialog (hex_editor/widgets.py) explicitly moved an equivalent whole-document scan (CRC) onto a GenericCallableWorker background thread specifically because synchronous scans of multi-hundred-megabyte files would freeze the UI, but the same class of full-file scan for entropy was left on the GUI thread.
- **Failure:** User opens a several-hundred-MB-to-multi-GB binary, enables the entropy minimap, and types a single hex digit to patch one byte. The keypress handler synchronously invokes document.entropy_map() over the entire file before returning control to the Qt event loop, causing the whole application to visibly freeze/hang for the duration of the scan on every subsequent edit.
- **Evidence:**
  ```python
  self.data_changed.connect(self._invalidate_color_caches)
  self.data_changed.connect(self._refresh_minimap_if_visible)
  ```
- **Verified:** Read lines 380-430, 529-587, 642-676, 1594-1777, 1959-1975 of hex_editor_widget.py. Confirmed exactly as claimed: line 427-428 connect data_changed to _invalidate_color_caches (clears self._entropy_cache, line 529-532) then to _refresh_minimap_if_visible (line 584-587), both default AutoConnection which resolves to DirectConnection since signal and slots live on the same (GUI) thread/object — so both run synchronously in emission order within the emitting call stack. _refresh_minimap_if_visible only fires _refresh_minimap_entropy -> _ensure_entropy_cache (line 550-566) when self._minimap.isVisible(); because _invalidate_color_caches already ran first (connected first, and Qt fires slots in c …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description. Confirmed entropy_map is a Rust/PyO3 binding that reads the entire document (read_all()) before computing per-block entropy, and py.detach() only releases the GIL for other threads — it does not yield to the Qt event loop, so the GUI thread is genuinely blocked for the duration of the scan.

#### H10 — Bridge privileges-changed callback invokes GUI-thread worker dispatch from the background event-loop thread
- **File:** `src/intellicrack/ui/panels/process_panel/base.py:334`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** process-system
- **Defect:** ProcessPanel registers `self._on_privileges_changed` via `bridge.add_privileges_changed_callback(...)`. The only call site of `ProcessBridge._notify_privileges_changed()` is at the end of the `adjust_token_privilege()` coroutine (bridges/process.py:4228), which is only ever invoked through `SystemTab._on_enable_debug()` -> `run_bridge_coroutine_logged(self._bridge.adjust_token_privilege(...), ...)`. That path submits the coroutine via `asyncio.run_coroutine_threadsafe(coro, loop)` onto the persistent background 'bridge-event-loop' thread (see async_bridge.py `_ensure_loop`/`BridgeCallWorker.run`), so the coroutine body -- including the synchronous `self._notify_privileges_changed()` call and every registered callback -- executes on that background thread, never the Qt GUI thread. `_on_privileges_changed` therefore runs off the GUI thread and calls `_refresh_privilege_label()`, which calls `run_bridge_coroutine_logged(_fetch_privs(), on_success=_on_privs, on_error=None, parent=self, ...)`. That helper is documented as being for dispatch 'from a synchronous Qt context' / 'without blocking the Qt main thread', and internally constructs `BridgeCallWorker(coro, parent)` with `parent=self` where `self` is the GUI-thread-owned `ProcessPanel`. Constructing a QObject with a parent that lives in a different thread than the calling thread is an invalid Qt operation (Qt refuses the reparenting and logs 'Cannot set parent, new parent is in a different thread'), so this specific callback-driven path is not safe the way every button-click-driven call site is.
- **Failure:** User clicks 'Enable Debug Privilege' on the System tab; once the bridge finishes adjusting the token it calls `_notify_privileges_changed()` on the background bridge-event-loop thread, which synchronously invokes `ProcessPanel._on_privileges_changed` -> `_refresh_privilege_label` -> `run_bridge_coroutine_logged(..., parent=self, ...)` from that non-GUI thread, constructing a `BridgeCallWorker` parented to a GUI-thread QWidget from the wrong thread and emitting Qt cross-thread-parenting warnings/undefined lifetime behavior instead of the safe click-triggered path used everywhere else in this file.
- **Evidence:**
  ```python
  def _on_privileges_changed(self) -> None:
          """Handle bridge notification that token privileges have changed."""
          self._refresh_privilege_label()
  ```
- **Verified:** Verified the full call chain in the actual source. bridges/process.py:4095 `adjust_token_privilege` is an `async def` invoked via `system_tab.py:1478` -> `run_bridge_coroutine_logged` -> `run_bridge_coroutine_async` (async_bridge.py:448) -> `BridgeCallWorker(coro, parent).start()` (async_bridge.py:466), whose `run()` (async_bridge.py:263-272) does `loop = _ensure_loop()` then `asyncio.run_coroutine_threadsafe(self._coro, loop)` and blocks on `future.result()`. `_ensure_loop` (async_bridge.py:179-219) lazily spins up a persistent daemon `threading.Thread(name="bridge-event-loop")` running `loop.run_forever()` -- a plain Python thread, not a QThread. The coroutine body therefore executes on th …
- **Note:** No corrections needed; the report's line, severity, and technical description are accurate as verified against base.py, process.py, and async_bridge.py.

#### H11 — _cleanup() blocks the GUI thread with untimed synchronous bridge calls during panel teardown  **[REGRESSION]**
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:528`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** sandbox-panel
- **Defect:** Every other bridge call site in this file dispatches via run_bridge_coroutine_logged/run_bridge_coroutine_async (non-blocking, signal-marshalled back to the GUI thread), consistent with the prior audit remediation. _cleanup(), however, calls the *blocking* run_bridge_coroutine() directly, twice, with no timeout_s argument -- once for stop_pcap and once for destroy. run_bridge_coroutine's blocking path calls future.result(timeout=timeout_s); with timeout_s=None it waits indefinitely for the background event loop to complete the coroutine. _cleanup runs synchronously on the calling (GUI) thread as part of stop_tool()/panel teardown.
- **Failure:** User closes the sandbox tab/panel (or the app shuts down) while a QEMU or Windows Sandbox instance is slow to destroy (e.g. VM shutdown, unresponsive guest, or an unreachable bridge). The entire Qt event loop freezes for the full duration of the stop_pcap and destroy RPCs -- with no timeout ceiling, an unresponsive backend hangs the whole application UI on close.
- **Evidence:**
  ```python
  try:
      run_bridge_coroutine(self._bridge.stop_pcap(self.sandbox_id))
  except (RuntimeError, ConnectionError, OSError):
      ...
  try:
      run_bridge_coroutine(self._bridge.destroy(self.sandbox_id))
  except (RuntimeError, ConnectionError, OSError):
      ...
  ```
- **Verified:** Read src/intellicrack/ui/panels/sandbox_panel.py:521-543. `_cleanup()` calls `run_bridge_coroutine(self._bridge.stop_pcap(self.sandbox_id))` (line 528) and `run_bridge_coroutine(self._bridge.destroy(self.sandbox_id))` (line 537) with no `timeout_s` kwarg, whereas every other bridge call site in this same file (30+ occurrences, e.g. lines 741, 803, 850, 873, 946, 1243...) uses the non-blocking `run_bridge_coroutine_logged`/`run_bridge_coroutine_async`. Confirmed `run_bridge_coroutine` in async_bridge.py:346-408: when not called from inside a running asyncio loop, it takes the blocking path `future.result(timeout=timeout_s)` (line 403); `timeout_s` defaults to `None`, which per the docstring ( …
- **Note:** The report's line/severity are accurate as given. Marking is_regression=true because the file's own established pattern (confirmed by ~30 other call sites) is to use run_bridge_coroutine_logged/async per 'the prior audit remediation' referenced in the finding, making these two blocking, untimed calls in _cleanup() an inconsistent regression rather than a fresh unaddressed area.

#### H12 — VNC disconnect blocks the GUI thread on unbounded network I/O  **[REGRESSION]**
- **File:** `src/intellicrack/ui/panels/vnc_widget.py:1878`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** vnc-sandbox-config
- **Defect:** disconnect_from_server() is a public method invoked directly from a GUI-thread slot (sandbox_panel.py's _disconnect_vnc_display). It calls the *blocking* run_bridge_coroutine(self.client.disconnect()) with no timeout_s. RFBClient.disconnect() awaits self._writer.wait_closed(), which can block indefinitely if the peer never acknowledges the TCP close (dead network, stalled QEMU sandbox). Unlike every other bridge call in this same file (connect_to_server, mouse/key events all use the non-blocking run_bridge_coroutine_logged), this one call reintroduces the exact 'blocking bridge call on the UI thread' pattern the prior audit's remediation notes say was fixed by moving to the async path.
- **Failure:** User clicks 'Disconnect' (or the panel calls _disconnect_vnc_display) while the QEMU sandbox's VNC socket is unresponsive (VM paused/crashed/network partition); wait_closed() never resolves, run_bridge_coroutine.future.result(timeout=None) blocks forever, and the entire Qt application UI freezes/hangs with no way to recover except killing the process.
- **Evidence:**
  ```python
  try:
              run_bridge_coroutine(self.client.disconnect())
          except (OSError, RuntimeError):
              _logger.debug("vnc_widget_disconnect_error", exc_info=True)
  ```
- **Verified:** vnc_widget.py:1873-1882 `disconnect_from_server()` calls `run_bridge_coroutine(self.client.disconnect())` with no `timeout_s`. `run_bridge_coroutine` (async_bridge.py:346-408) is the blocking variant: when invoked from a thread with no running asyncio loop (the Qt GUI thread), it does `future = asyncio.run_coroutine_threadsafe(...)` then `future.result(timeout=timeout_s)`; with `timeout_s=None` this blocks forever. `RFBClient.disconnect()` (vnc_widget.py:1684-1696) does `self._writer.close(); await self._writer.wait_closed()`, which per asyncio semantics can hang indefinitely if the peer never completes the TCP close handshake. The except clause only catches `(OSError, RuntimeError)`, not `T …
- **Note:** Regression relative to the file's own stated remediation pattern (VNCWidget class docstring at line 1769 explicitly claims "the Qt main thread is never blocked" and connect_to_server's docstring makes the same claim for the connect path) — disconnect was left on the old blocking call while every sibling operation (connect, mouse events, key events) was migrated to run_bridge_coroutine_logged/run_bridge_coroutine_async. Minimal fix: use run_bridge_coroutine_logged/run_bridge_coroutine_async for the disconnect call, or at minimum pass a bounded timeout_s to run_bridge_coroutine.

#### H13 — Panel teardown blocks the GUI thread on an untimed synchronous bridge RPC
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:289`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** x64dbg-panel
- **Defect:** `_cleanup()` (invoked synchronously from `AnalysisPanelBase.stop_tool()`, which runs on the calling/GUI thread) calls `run_bridge_coroutine(self._bridge.stop())` — the blocking variant from async_bridge.py that calls `future.result(timeout_s=None)`, i.e. waits forever with no timeout. Every other bridge call site in this file uses `run_bridge_coroutine_logged`/`run_bridge_coroutine_async`, which dispatch on a background QThread and deliver results via Qt signals without blocking the caller. This one teardown path was missed, so it freezes the Qt event loop until the (possibly hung or slow) debugger-stop RPC completes.
- **Failure:** User closes/switches away from the x64dbg panel while the target process or the debugger plugin is unresponsive (e.g. hung IPC pipe, process wedged in a syscall); `stop_tool()` -> `_cleanup()` -> `run_bridge_coroutine(self._bridge.stop())` blocks the entire UI thread indefinitely, freezing the whole application with no way to cancel.
- **Evidence:**
  ```python
  run_bridge_coroutine(self._bridge.stop())
  ```
- **Verified:** Read src/intellicrack/ui/panels/x64dbg_panel.py:276-291 — `_cleanup()` calls `run_bridge_coroutine(self._bridge.stop())` at line 289 with no `timeout_s` argument, unlike every other call site in the file which uses `run_bridge_coroutine_logged`/`run_bridge_coroutine_async` (background-thread dispatch with signal delivery). Read src/intellicrack/ui/panels/async_bridge.py:339-408: `run_bridge_coroutine` defaults `timeout_s=None`, schedules the coroutine on a background asyncio loop via `run_coroutine_threadsafe`, then does `future.result(timeout=timeout_s)` on the *calling* thread — with `timeout_s=None` this is an unbounded blocking wait (`concurrent.futures.Future.result(timeout=None)` block …
- **Note:** Claim is accurate as written. No correction needed to line/severity/description.

#### H14 — Credential-store enumeration blocks the GUI thread both at dialog construction and on 'Reload Keys' click
- **File:** `src/intellicrack/ui/provider_config.py:1369`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `_refresh_credential_overview` calls the blocking `run_bridge_coroutine(_load_store_credentials())`, which awaits `store.list_providers()` and, per provider, `store.get_source(cred.provider)` against `CredentialStore` (backed by OS keyring / file I/O). This method is reached from `_load_credential_overview()`, which is called synchronously in `ProviderConfigDialog.__init__` (line 1326, before the dialog is even shown) and again from the `Reload Keys` button handler `refresh_credentials()` — both on the GUI thread.
- **Failure:** Opening Provider Settings, or clicking 'Reload Keys', freezes the whole window while every configured provider's credential source is queried from the OS keyring/store; on a slow keyring backend or many configured providers this is a visible stall before the dialog appears/responds.
- **Evidence:**
  ```python
  async def _load_store_credentials() -> int:
      store_providers = await store.list_providers()
      for cred in store_providers:
          source = await store.get_source(cred.provider)
      ...
  result = run_bridge_coroutine(_load_store_credentials())
  ```
- **Verified:** Code reads exactly as claimed. In src/intellicrack/ui/provider_config.py, ProviderConfigDialog.__init__ (line 1311) calls self._load_credential_overview() at line 1326, synchronously before setWindowTitle/resize/exec, i.e. before the dialog is shown, and _refresh_credentials() (bound to the "Reload Keys" button at line 1414 -> refresh_credentials at 1754 -> _load_credential_overview at 1761) reaches the same code path. _load_credential_overview (1331) calls _refresh_credential_overview (1341), which builds an inner async def _load_store_credentials() that awaits store.list_providers() (iterates every ProviderName and calls _get_unlocked/_get_metadata per provider, store.py:536-551) and, per  …

#### H15 — 'Migrate' credentials button blocks the GUI thread during env-to-store migration
- **File:** `src/intellicrack/ui/provider_config.py:1778`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `migrate_credentials`, wired directly to the `Migrate` button's `clicked` signal, calls the blocking `run_bridge_coroutine(store.migrate_from_env())` on the GUI thread. `CredentialStore.migrate_from_env()` performs credential-store writes (keyring/file I/O) for every provider found in the environment/.env file, all while the Qt event loop is blocked.
- **Failure:** User clicks 'Migrate' to move .env credentials into the secure store; the dialog and entire application stop responding until every provider's credential has been migrated.
- **Evidence:**
  ```python
  store = CredentialStore()
  run_bridge_coroutine(store.migrate_from_env())
  ```
- **Verified:** Verified the full call chain. Line 1419: `self._migrate_creds_btn.clicked.connect(self.migrate_credentials)` connects the button directly to a slot that runs on the GUI thread (no QThread/worker wrapping). `migrate_credentials` (lines 1773-1782) calls `run_bridge_coroutine(store.migrate_from_env())` synchronously. `run_bridge_coroutine` (src/intellicrack/ui/panels/async_bridge.py:346-408) is explicitly documented as "the **blocking** variant": when there is no already-running asyncio loop on the calling thread (true for the Qt GUI thread), it does `future = asyncio.run_coroutine_threadsafe(resolved_coro, loop)` followed by `return future.result(timeout=timeout_s)` with `timeout_s` defaulting …
- **Note:** Report is accurate as written. The codebase already has the correct non-blocking primitive available (`run_bridge_coroutine_async` in the same async_bridge.py module, used elsewhere) which this call site should use instead, delivering the result dict via a Qt signal callback that then triggers `_load_credential_overview()`.

#### H16 — Model discovery for the selected provider blocks the GUI thread on a network round-trip
- **File:** `src/intellicrack/ui/provider_config.py:1802`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `discover_single_provider`, invoked directly from the `Discover` button's click handler `_on_discover_selected_provider` on the GUI thread, calls the blocking `run_bridge_coroutine(_discover())`, where `_discover()` awaits `discovery.discover_provider(pname)` — a network call to the provider's model-listing API. Unlike `pull_ollama_model` in the same file (which correctly uses `run_bridge_coroutine_async` with signal-based callbacks), this path uses the synchronous/blocking wrapper directly from a GUI event handler.
- **Failure:** User selects a provider and clicks 'Discover'. The window freezes for the duration of the provider's model-list HTTP request (can be several seconds, longer on a slow/unreachable network) before `_refresh_provider_status()` runs and the UI becomes responsive again.
- **Evidence:**
  ```python
  async def _discover() -> None:
      await discovery.discover_provider(pname)
  
  try:
      run_bridge_coroutine(_discover())
  ```
- **Verified:** Confirmed by direct code reading. `_discover_models_btn.clicked.connect(self._on_discover_selected_provider)` at line 1431 wires the Discover button to `_on_discover_selected_provider` (line 1719), which is a normal Qt slot executing on the GUI thread. That method calls `self.discover_single_provider(self._current_provider)` (line 1722), and `discover_single_provider` (line 1784) defines `async def _discover(): await discovery.discover_provider(pname)` and then calls `run_bridge_coroutine(_discover())` at line 1802 — with no `run_bridge_coroutine_async`/signal-callback usage anywhere in this method. `run_bridge_coroutine` in src/intellicrack/ui/panels/async_bridge.py (lines 346-408) is expli …
- **Note:** No corrections needed; the report's file/line/description/failure scenario and code_evidence match the actual source exactly.

#### H17 — 'Revoke Token' button blocks the GUI thread on a synchronous OAuth revoke network call
- **File:** `src/intellicrack/ui/provider_config.py:1881`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `_do_revoke_oauth_token`, reached from the `Revoke Token` button's click handler `_on_revoke_oauth` on the GUI thread, calls the blocking `run_bridge_coroutine(manager.revoke_token(oauth_provider))`, which performs a network call to the provider's token-revocation endpoint. As with the other call sites in this file, `run_bridge_coroutine_async` (already used correctly for `pull_ollama_model`) was not used here.
- **Failure:** User clicks 'Revoke Token' for a provider with an active OAuth session; the UI freezes for the duration of the revoke HTTP request/timeout before control returns.
- **Evidence:**
  ```python
  manager = get_oauth_manager()
  try:
      oauth_provider = OAuthProvider(provider_id)
  except ValueError:
      _logger.warning("unknown_oauth_provider", provider=provider_id)
      return
  run_bridge_coroutine(manager.revoke_token(oauth_provider))
  ```
- **Verified:** Traced the full call chain: `_revoke_btn.clicked.connect(self._on_revoke_oauth)` (line 1443) -> `_on_revoke_oauth` (line 1730, runs on GUI thread) -> `revoke_oauth_token` (line 1884) -> `_do_revoke_oauth_token` (line 1869) -> `run_bridge_coroutine(manager.revoke_token(oauth_provider))` (line 1881), all synchronous, no QThread/worker offload. `run_bridge_coroutine`'s own docstring (src/intellicrack/ui/panels/async_bridge.py:353-362) explicitly labels it 'the **blocking** variant' contrasted with `run_bridge_coroutine_async`. Its implementation confirms this: since the Qt GUI thread has no running asyncio loop, `asyncio.get_running_loop()` raises RuntimeError, so it falls through to `future.re …
- **Note:** Report is accurate as stated; no correction needed to line, severity, or description.

#### H18 — Sandbox stop uses synchronous asyncio.run() on the GUI thread with no timeout
- **File:** `src/intellicrack/ui/sandbox_config.py:1291`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** vnc-sandbox-config
- **Defect:** SandboxMonitorWidget._stop_via_manager() is reached from _stop_btn.clicked -> _stop_sandbox() -> _stop_via_manager(), all executing on the Qt GUI thread. It calls asyncio.run(self._manager.destroy_all()) synchronously, which spins up a brand-new event loop and blocks the calling thread until every managed sandbox instance is torn down. VM/container teardown is inherently variable-latency (seconds to tens of seconds), and there is no timeout or async dispatch (the codebase already has run_bridge_coroutine/run_bridge_coroutine_async/GenericCallableWorker specifically to avoid this pattern).
- **Failure:** User clicks 'Stop Sandbox' while a Windows Sandbox VM instance is running; destroy_all() takes several seconds to gracefully tear down the VM, during which the entire application (including any other panel, the VNC widget's repaint timer, and all other UI) is frozen and unresponsive to input.
- **Evidence:**
  ```python
  try:
              asyncio.run(self._manager.destroy_all())
          except (RuntimeError, OSError) as e:
  ```
- **Verified:** Confirmed by direct code reading. `sandbox_config.py:1224` wires `self._stop_btn.clicked.connect(self._stop_sandbox)` — this fires on the Qt GUI thread. `_stop_sandbox` (line 1269-1279) calls `self._stop_via_manager()` synchronously (no thread/worker dispatch). `_stop_via_manager` (line 1281-1297) calls `asyncio.run(self._manager.destroy_all())` at line 1291, inside a plain try/except with no timeout of any kind — `asyncio.run()` has no timeout parameter and blocks the calling thread until the coroutine finishes. `SandboxManager.destroy_all()` (src/intellicrack/sandbox/manager.py:393-400) iterates instances calling `self.destroy(instance_id)` → `_destroy_locked` → `await instance.sandbox.sto …
- **Note:** Report is accurate as written; no correction needed to line, severity, or failure scenario. The file's own docstring at line 1284 explicitly confirms the synchronous-blocking design, and the same file already contains the correct async-dispatch primitives (run_bridge_coroutine, run_bridge_coroutine_async) used elsewhere for other manager calls, underscoring that this is an inconsistent/unmitigated instance rather than an intentional design choice.

#### H19 — Session delete routed through the blocking run_bridge_coroutine on the GUI thread, freezing the dialog during SQLite I/O
- **File:** `src/intellicrack/ui/session_manager.py:1018`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** splash-xpu-session
- **Defect:** `_delete_session_sync` is called directly from the Delete button's click handler on the Qt GUI thread. It calls `run_bridge_coroutine(self._manager.delete(session_id))`, which per its own docstring in async_bridge.py is 'the **blocking** variant': it submits the coroutine to the persistent background asyncio loop via `asyncio.run_coroutine_threadsafe` and then calls `future.result(timeout_s)` with `timeout_s=None` (waits forever) on the calling (GUI) thread. `SessionManager.delete` ultimately performs synchronous `sqlite3` operations (BEGIN IMMEDIATE / COMMIT transactions) in `SessionStore`. This is exactly the blocking-bridge-call-on-GUI-thread pattern the prior audit's systemic fix (`run_bridge_coroutine_logged`) was meant to eliminate, but this file only imports the blocking `run_bridge_coroutine` (no `_async`/`_logged` variant is used anywhere in it), so the fix was not applied here.
- **Failure:** User selects a session and clicks Delete, confirms the QMessageBox; while the background loop thread executes the SQLite DELETE/COMMIT (which can stall under disk contention, antivirus scanning, or a busy shared sessions.db), the entire Qt event loop is blocked inside `future.result()`, freezing the whole dialog (and, since this runs on the main GUI thread, potentially the whole application) until the write completes.
- **Evidence:**
  ```python
  result = run_bridge_coroutine(self._manager.delete(session_id))
  ```
- **Verified:** Read session_manager.py:966-1052 and async_bridge.py:339-408. Confirmed: (1) `_delete_session` is connected to the Delete button's `clicked` signal (line 536: `self._delete_btn.clicked.connect(self._delete_session)`), so it runs on the Qt GUI thread. (2) It calls `self._delete_session_sync(session_id)` synchronously (line 997), which at line 1018 calls `run_bridge_coroutine(self._manager.delete(session_id))` with no `timeout_s` argument. (3) `run_bridge_coroutine`'s own docstring (async_bridge.py:353-372) states it is 'the **blocking** variant', submits via `asyncio.run_coroutine_threadsafe` to a background loop, and calls `future.result(timeout=timeout_s)` where `timeout_s=None` means it 'w …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### H20 — Session import routed through the blocking run_bridge_coroutine on the GUI thread
- **File:** `src/intellicrack/ui/session_manager.py:1232`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** splash-xpu-session
- **Defect:** `_import_via_manager`, invoked synchronously from the Import... button's click handler, calls `run_bridge_coroutine(manager.import_json(path, replace=replace))` -- the documented blocking variant of the bridge helper (`future.result(timeout_s=None)`). `SessionManager.import_json` reads the session JSON and writes it into the SQLite-backed SessionStore synchronously inside the coroutine. As with the delete path, this freezes the Qt event loop for the duration of the file read plus the DB write instead of using the non-blocking `run_bridge_coroutine_async`/`run_bridge_coroutine_logged` path that the rest of the codebase was migrated to per the prior audit remediation.
- **Failure:** User imports a larger or many-message session JSON file; while `manager.import_json` runs on the background loop, the GUI thread is blocked in `future.result()` waiting on the future, so the dialog becomes unresponsive (no repaint, no cancel) until the import completes.
- **Evidence:**
  ```python
  run_bridge_coroutine(manager.import_json(path, replace=replace))
  ```
- **Verified:** session_manager.py:663 wires `import_btn.clicked.connect(self._import_session)`, so `_import_session` (line 1185) runs on the Qt GUI thread. It calls `self._import_via_manager(path)` (line 1204), which at line 1232 does `run_bridge_coroutine(manager.import_json(path, replace=replace))` with no `timeout_s`. Reading async_bridge.py's `run_bridge_coroutine` (lines 346-408): it first checks `asyncio.get_running_loop()`; since this is the Qt GUI thread (not an asyncio loop), that raises RuntimeError, so `running=None`, and execution falls through to the blocking branch: `future = asyncio.run_coroutine_threadsafe(resolved_coro, loop); return future.result(timeout=timeout_s)` with `timeout_s=None`, …
- **Note:** Claim accurate as written; no correction needed to title, line, or severity.

#### H21 — _cleanup_bridge blocks the GUI thread with an unbounded synchronous RPC wait on tab close
- **File:** `src/intellicrack/ui/tools.py:1145`
- **Severity:** HIGH  ·  **Category:** threading  ·  **Cluster:** tools
- **Defect:** `_on_tab_close_requested` is connected directly to `tabCloseRequested` (a GUI-thread signal fired when the user clicks a tab's close 'x', line 844) and calls `self._cleanup_bridge(bridge, bridge_attr)` synchronously for the matched panel's bridge (Ghidra/Cutter/x64dbg/Frida/Process). `_cleanup_bridge` resolves `run_bridge_coroutine` (the blocking variant, not `run_bridge_coroutine_async`) and, for every coroutine `detach`/`shutdown`/`stop` method the bridge exposes, calls `run_coro(method())` with no `timeout_s`. `run_bridge_coroutine` with `timeout_s=None` calls `future.result(timeout=None)`, which blocks indefinitely until the background event-loop thread finishes the bridge RPC. If the external tool process (Ghidra headless RPC server, x64dbg pipe, Cutter/rizin socket) is slow, hung, or unresponsive when the user closes the tab, this freezes the entire Qt event loop with no ceiling.
- **Failure:** User has the Ghidra (or x64dbg/Cutter/Frida) tab open against a slow-to-respond or hung external process and clicks the tab's close button. `_on_tab_close_requested` -> `_cleanup_bridge` -> `run_bridge_coroutine(bridge.shutdown())` blocks the GUI thread forever waiting on `future.result()`, freezing the entire application (no spinner, no way to cancel) until the external process eventually responds or the process is killed externally.
- **Evidence:**
  ```python
  async_mod = importlib.import_module(".panels.async_bridge", "intellicrack.ui")
  run_coro = async_mod.run_bridge_coroutine
  
  for method_name in ("detach", "shutdown", "stop"):
      method = getattr(bridge, method_name, None)
      if method is not None and callable(method):
          try:
              if inspect.iscoroutinefunction(method):
                  run_coro(method())
              else:
                  method()
  ```
- **Verified:** Read src/intellicrack/ui/tools.py:836-844 — QTabWidget has setTabsClosable(True) and tabCloseRequested is connected directly (default auto/direct connection, same-thread GUI signal) to self._on_tab_close_requested, with no QThread/worker wrapping. tools.py:1156-1216 (_on_tab_close_requested) synchronously calls self._cleanup_bridge(bridge, bridge_attr) at line 1200 for any matched ghidra/cutter/x64dbg/frida/process bridge. tools.py:1130-1154 (_cleanup_bridge) resolves run_coro = async_bridge.run_bridge_coroutine (the blocking variant) and for each coroutine detach/shutdown/stop method calls run_coro(method()) at line 1145 with no timeout_s argument. async_bridge.py:346-408 confirms run_bridg …
- **Note:** The report is accurate as written. Worth noting for the fix: not all five bridges are equally unbounded — Ghidra/x64dbg/Cutter internally bound their RPC waits (10s / COMMAND_TIMEOUT / R2_COMMAND_TIMEOUT respectively via asyncio.wait_for), so their _cleanup_bridge calls will eventually return (with a raised exception caught by the (RuntimeError, OSError, AttributeError, ToolError) handler) rather than hang forever. The genuinely unbounded path is FridaBridge.detach/shutdown, whose asyncio.to_thread(session.detach)/asyncio.to_thread(device.kill/disable_spawn_gating) calls have no wait_for/timeout anywhere in the chain, so a hung frida-server/device transport will block the to_thread thread and therefore run_bridge_coroutine's future.result(timeout=None) indefinitely, freezing the GUI thread as described. The fix should pass an explicit timeout_s to run_coro(method(), timeout_s=...) in _cleanup_bridge (or route through run_bridge_coroutine_async) regardless, since relying on each bridge's internal bounding is fragile and Frida already demonstrates it isn't universally true.

### Logic / wiring / leaks

#### H22 — Block-comment scanner does not exclude string literals, misformatting string content and risking cascading corruption
- **File:** `src/intellicrack/ui/highlighter.py:249`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** highlighter-toolbar-dock
- **Defect:** `_scan_block_comments` (shared by `CSyntaxHighlighter`, `JavaScriptSyntaxHighlighter`, and `HexPatSyntaxHighlighter`) searches for `/*` with `self._comment_start.match(text)` over the raw block text with no awareness of string literals. Contrast this with `_line_comment_start_index` (used for `//` detection), which explicitly walks the text tracking `"`, `'`, and `` ` `` delimiters (honoring backslash escapes) so a `//` inside a string is never treated as a comment start. No equivalent string-tracking exists for `/*`. Because `_apply_rules(text)` runs before `_scan_block_comments(text)` in `highlightBlock`, the comment-format `setFormat` call executed afterward overwrites whatever string format was applied to the same character range, so any `/*` that appears inside a double/single-quoted string literal is treated as a genuine block-comment opener.
- **Failure:** Decompiled/disassembled C code, a Frida JS snippet, or a HexPat pattern definition containing a string literal such as `"format /* debug */"` or a Windows/glob-style path fragment with `/*` in it gets the substring from `/*` onward recolored with the comment format instead of the string format. Worse, if the string does not also contain a `*/` on the same physical line (e.g. a truncated/garbled string extracted from a stripped binary, or a URL like `"http://host/*"`), `_scan_block_comments` sets `self.setCurrentBlockState(_BLOCK_STATE_BLOCK_COMMENT)` for the rest of that block, and every subsequent line in the document is then treated as 'inside a block comment' (via the `previousBlockState() == _BLOCK_STATE_BLOCK_COMMENT` branch at line 246) until an unrelated `*/` is eventually encountered — corrupting the syntax highlighting of the remainder of the displayed file.
- **Evidence:**
  ```python
  match = self._comment_start.match(text)
              candidate = match.capturedStart() if match.hasMatch() else -1
              start_index = self._guard_block_start(candidate, line_comment_index)
  ```
- **Verified:** Read highlighter.py lines 88-267 and 394-403. Confirmed all three claimed facts: (1) `_comment_start = QRegularExpression(r"/\*")` (line 88) is a raw regex with no string-awareness, used at line 249 `match = self._comment_start.match(text)` and again at line 265 for subsequent matches within the same block — unlike `_line_comment_start_index` (lines 180-214), which explicitly tracks `"`, `'`, `` ` `` delimiters with backslash-escape handling to avoid treating `//` inside a string as a comment. `_guard_block_start` (lines 217-231) only suppresses a `/*` match when it falls after a `//` on the same line (`line_comment_index`); it has no knowledge of quote state, so a `/*` inside a double/singl …
- **Note:** Claim is accurate as written; no correction needed to line, severity, or failure scenario. Confirmed the exact code-path (highlightBlock order, string regex rules, _scan_block_comments/_guard_block_start logic, and block-state carry-over via previousBlockState) all behave as described, across all three affected highlighter subclasses (C, JavaScript, HexPat).

#### H23 — _on_remove_hook desyncs _hook_ids from the hooks table when no bridge is attached
- **File:** `src/intellicrack/ui/panels/frida_panel.py:982`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** frida-panel
- **Defect:** `_on_remove_hook` only pops `self._hook_ids[selected]` on the bridge-backed path (`selected < len(self._hook_ids) and self._bridge is not None`). When that condition is false because `self._bridge is None` — a state the code itself explicitly checks for, meaning it is a recognised, reachable branch — execution falls through to `self._hooks_table.removeRow(selected)` alone, which removes the table row but leaves the corresponding entry in `self._hook_ids` in place. `_hook_ids` is a parallel list indexed by table row (see `_find_hook_row`, `_on_hook_installed`, `add_hook_entry`), so after this call every row below `selected` is now associated with the wrong id in `_hook_ids`.
- **Failure:** Hook rows can exist before a bridge is attached to this panel instance (`add_hook_entry` is a public method invoked externally via `ToolsMainWindow.add_frida_hook_entry` in `ui/tools.py`, independent of this panel's own `self._bridge` state). If the user removes such a row while `self._bridge is None`, `_hook_ids` keeps a stale entry; the next `_on_hook_installed`/`_find_hook_row` lookup or a later remove-hook click (once a bridge is attached) will resolve the wrong row/hook_id for every row that shifted up, causing the wrong hook's status/address/function cells to be updated or the wrong hook_id to be sent to `bridge.remove_hook()`.
- **Evidence:**
  ```python
  if selected < len(self._hook_ids) and self._bridge is not None:
      hook_id = self._hook_ids[selected]
      ...
      return
  
  self._hooks_table.removeRow(selected)
  ```
- **Verified:** Read frida_panel.py lines 961-982: `_on_remove_hook` only pops `self._hook_ids[selected]` inside the `if selected < len(self._hook_ids) and self._bridge is not None:` branch (line 967-980), which returns early. When `self._bridge is None`, execution falls through to line 982 `self._hooks_table.removeRow(selected)` with no corresponding `self._hook_ids.pop(selected)`. Confirmed `_hook_ids` is a genuine parallel-to-rows list: `add_hook_entry` (line 1032) always appends a `hook_id` (default "" if none supplied) whenever a row is inserted, regardless of `self._bridge` state, and `add_hook_entry` is reachable externally via `ToolsMainWindow.add_frida_hook_entry` (ui/tools.py lines 2374-2394) whic …

#### H24 — "Signed" checkbox in the calculator tab is created but never read (dead control)
- **File:** `src/intellicrack/ui/panels/hex_editor/calculator.py:71`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** hex-transforms
- **Defect:** `self._calc_signed_check = QCheckBox("Signed")` is instantiated and added to the options row, but `_calc_signed_check` is never referenced again anywhere in the file (or anywhere else in the hex_editor package) -- no `.isChecked()` call exists. Both the bridge-backed path (`_on_convert_success`) and the local fallback path (`_convert_local` -> `_add_sized_int_results`) unconditionally compute and display both the signed and unsigned representations for every width regardless of the checkbox state.
- **Failure:** User unchecks/checks the "Signed" checkbox in the Calculator tab expecting it to filter the results tree to only signed or only unsigned representations (as its label and presence imply), then clicks Convert. The results tree and IEEE-754 labels are byte-for-byte identical no matter what the checkbox state is -- the control has zero effect on any displayed output.
- **Evidence:**
  ```python
  self._calc_signed_check = QCheckBox("Signed")
          opts_row.addWidget(self._calc_signed_check)
  ```
- **Verified:** Read the full file (368 lines). Line 46 declares `_calc_signed_check: QCheckBox | None` as an instance attribute; lines 71-72 instantiate it (`QCheckBox("Signed")`) and add it to `opts_row`. Grep across the entire hex_editor package confirms only those 3 references exist -- no `.isChecked()` call anywhere. Both output paths compute all sized-int representations unconditionally: `_add_sized_int_results` (lines 258-285) builds a fixed list of both `intN`/`uintN` labels for every width and adds all of them via `_add_result`; `_on_convert_success`'s bridge path (lines 231-240) likewise unconditionally adds both `uint8`/`int8`, `uint16_LE`/`int16_LE`, etc. from the bridge dict. There is no condit …
- **Note:** Report is accurate as written; no correction needed to title, line, or severity.

#### H25 — Reconnecting to a pipe with the same name leaves a stale table row that resolves to the wrong handle
- **File:** `src/intellicrack/ui/panels/process_panel/system_tab.py:1702`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** process-system
- **Defect:** `_on_pipe_connect`'s success handler always inserts a new row into `_pipe_table` and unconditionally overwrites `self._pipe_handles[name] = result`, without checking whether a row for that pipe name already exists. `_selected_pipe()` resolves the acting handle by looking up `self._pipe_handles.get(pipe_name)` from the SELECTED ROW'S NAME TEXT, not from the handle text displayed in that row. If a user connects to the same pipe name twice (e.g., reconnect after the pipe server restarted), two rows display the same name but different handle values, while the dict only tracks the newest handle for that name. Selecting the OLDER row and clicking Read/Write resolves to the NEWER handle (mismatched with what's displayed), and `_on_pipe_close`'s row-removal loop removes the FIRST row matching the name (which may be the stale one), leaving a row displayed for a handle that was actually just closed.
- **Failure:** User connects to `\\.\pipe\MyPipe` (handle 0x10), the pipe server cycles, user reconnects to the same name (handle 0x14) -- the table now shows two rows both labelled `\\.\pipe\MyPipe` with handles 0x10 and 0x14. Selecting the 0x10 row and clicking Read actually issues `pipe_read(0x14, ...)` because `_selected_pipe()` looks the handle up by name, not by the row's own handle cell; selecting the 0x10 row and clicking Close removes the 0x10 row via `pipe_close(0x14)`, closing the wrong (still-displayed) handle 0x14 while the table now shows a live-looking row for handle 0x10 that was never actually connected under that mapping.
- **Evidence:**
  ```python
  self._pipe_handles[name] = result
              row = self._pipe_table.rowCount()
              self._pipe_table.insertRow(row)
              self._pipe_table.setItem(row, 0, QTableWidgetItem(name))
              self._pipe_table.setItem(row, 1, QTableWidgetItem(f"0x{result:X}"))
  ```
- **Verified:** Read src/intellicrack/ui/panels/process_panel/system_tab.py lines 1685-1788. Confirmed exactly as claimed: `_on_pipe_connect`'s `_on_success` (lines 1694-1706) unconditionally does `self._pipe_handles[name] = result` then always appends a brand-new row (`insertRow`/`setItem`) with no check for a pre-existing row/entry for `name`. `_selected_pipe()` (lines 1722-1742) reads the name text from column 0 of the selected row and looks up the handle via `self._pipe_handles.get(pipe_name)` — i.e. by name, not by the handle text actually displayed in column 1 of that row. `_on_pipe_close`'s success handler (lines 1753-1767) pops the dict entry by name and then removes the FIRST table row (linear scan …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### H26 — _on_delete_snapshot_success removes whatever row is currently selected, not the row that was actually deleted
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:1951`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** sandbox-panel
- **Defect:** _on_delete_snapshot captures the selected snapshot name into self._pending_snapshot_id and issues an async bridge.snapshot_delete call. When the async result returns, _on_delete_snapshot_success does NOT look up the row by self._pending_snapshot_id; instead it re-queries get_current_tree_item(self._snapshots_tree) for whatever is selected *at completion time* and removes that row. Because the delete is asynchronous (a background-thread bridge round trip), the user's tree selection can change before the callback fires.
- **Failure:** User selects snapshot 'A' and clicks Delete Snap. Before the async delete completes, the user clicks snapshot 'B' in the Snapshots tree (or clears the selection). When the delete-A response arrives, _on_delete_snapshot_success removes row 'B' (or does nothing if no selection) from the UI, even though only 'A' was actually deleted on the backend. The tree now shows 'A' as still present (stale -- it's gone server-side) while 'B' is missing from the UI despite still existing server-side.
- **Evidence:**
  ```python
  snapshot_name = self._pending_snapshot_id
  self._log(f"[+] Snapshot deleted: {snapshot_name}")
  selected = get_current_tree_item(self._snapshots_tree)
  if selected is not None:
      idx = self._snapshots_tree.indexOfTopLevelItem(selected)
      if idx >= 0:
          self._snapshots_tree.takeTopLevelItem(idx)
  ```
- **Verified:** Read sandbox_panel.py lines 1918-1965. _on_delete_snapshot (1918) captures the selected item's name into self._pending_snapshot_id (1929) and disables only delete_snap_btn (1930), then issues an async bridge.snapshot_delete via run_bridge_coroutine_logged (1931-1941) -- a genuine background-thread round trip, not synchronous. The tree widget itself (_snapshots_tree) is never disabled or locked during the pending call (grepped for setEnabled/setDisabled on it -- none found), so the user's selection can change before the callback fires. In _on_delete_snapshot_success (1943-1956), snapshot_name = self._pending_snapshot_id (1949) is used only for the log line (1950) and is never used to find the …
- **Note:** Report is accurate as written; no correction needed beyond confirming line 1951 as the defect site.

#### H27 — Handles table always shows handle 0x0 because bridge returns hex strings, not ints
- **File:** `src/intellicrack/ui/panels/x64dbg_advanced_tab.py:1066`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** X64DbgBridge.get_handles() (src/intellicrack/bridges/x64dbg.py:8454) returns `"handle": hex(int(entry.HandleValue or 0))`, i.e. a string such as "0x1a4". _apply_handles reads that value and only accepts it if `isinstance(handle_val, int)`; since it is always a str, `handle_int` is silently coerced to 0 for every row, every render, regardless of the real handle value.
- **Failure:** User clicks "Enumerate Handles" on any real process: every row in the Handles table displays "0x0" in the Handle column (instead of the actual handle values), and clicking a row to auto-populate the Close Handle field (`_on_handle_row_selected`, which reads back the same coerced-to-0 UserRole data) always inserts "0x0", so "Close Handle" can never target the handle the user actually selected — the handle enumeration/close workflow is non-functional.
- **Evidence:**
  ```python
  handle_val = entry.get("handle", 0)
              handle_int = handle_val if isinstance(handle_val, int) else 0
              handle_item = QTableWidgetItem(f"0x{handle_int:X}")
              handle_item.setData(Qt.ItemDataRole.UserRole, handle_int)
  ```
- **Verified:** Read src/intellicrack/bridges/x64dbg.py:8453-8459: _parse_handle_buffer builds each handle dict with `"handle": hex(int(entry.HandleValue or 0))`, which is a str (e.g. \"0x1a4\"), not an int. Read src/intellicrack/ui/panels/x64dbg_advanced_tab.py:1066-1069: `handle_val = entry.get(\"handle\", 0)` then `handle_int = handle_val if isinstance(handle_val, int) else 0` — since handle_val is always a str, isinstance(handle_val, int) is always False, so handle_int is unconditionally 0 for every row, every refresh. The rendered QTableWidgetItem text is f\"0x{handle_int:X}\" = \"0x0\" for all rows, and the same 0 is stored via setData(Qt.ItemDataRole.UserRole, handle_int). _on_handle_row_selected (li …
- **Note:** Report is accurate as written; no corrections needed to line, severity, or description.

#### H28 — Status-label updates are silently dropped: panel assigns to `_status_label` instead of the base class's `status_label`
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:215`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** x64dbg-panel
- **Defect:** AnalysisPanelBase._set_status() (base_panel.py) checks `self.status_label` and only calls setText() when it is not None; `AnalysisPanelBase.__init__` initializes `self.status_label = None` and never assigns it except when a subclass's `_populate_toolbar` does so. X64DbgPanel._populate_toolbar creates the toolbar status QLabel but stores it in `self._status_label` (leading underscore), a completely different attribute than the one `_set_status` reads. `self.status_label` therefore remains None for the panel's entire lifetime, so every one of the panel's many `self._set_status(...)` calls (load/attach/run/pause/stop/restart/detach/spawn success and the plugin-not-ready diagnostic) is a silent no-op — the toolbar label is created, added, and visible, but its text is never updated after the initial "Not loaded". Sibling panels (frida_panel.py, cutter_panel.py, ghidra_panel.py) all correctly assign to `self.status_label`, confirming this is a wrong-attribute bug specific to this file.
- **Failure:** User loads an executable via Load..., attach succeeds, and Run/Pause/Stop are pressed; `_on_load_success`, `_on_attach_success`, `_on_run_success`, etc. all call `self._set_status(...)` but the toolbar label permanently reads "Not loaded" regardless of actual debugger state, giving the user no visual status feedback at all.
- **Evidence:**
  ```python
  self._status_label = self._add_toolbar_label(toolbar, "Not loaded")
  ```
- **Verified:** Read x64dbg_panel.py:215 — toolbar label is stored as `self._status_label = self._add_toolbar_label(toolbar, "Not loaded")` (leading underscore). Read base_panel.py:72 — `AnalysisPanelBase.__init__` sets `self.status_label: QLabel | None = None`, and base_panel.py:245-246 `_set_status` only calls `self.status_label.setText(text)` when `self.status_label is not None`. Grepped the whole file for `self.status_label` (no leading underscore) — zero matches, so it is never assigned and remains None for the panel's lifetime. Grepped all `_set_status(` call sites (10 total: lines 1086, 1130, 1221, 1300, 1336, 1368, 1401, 1444, 2407, 2443 covering load/attach/run/pause/stop/restart/detach/spawn succe …
- **Note:** Report is accurate as written; no corrections needed to line, severity, or description.

#### H29 — SessionManagerDialog's only real call site never wires session_manager/current_session, so the dialog shows the wrong session data source and the active-session-protection logic is dead
- **File:** `src/intellicrack/ui/session_manager.py:690`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** splash-xpu-session
- **Defect:** SessionManagerDialog accepts session_manager, current_session_id, and current_session as optional constructor params. When session_manager is None, _load_sessions() falls back to _load_sessions_from_disk(), which reads JSON sidecar files from SESSIONS_DIR (get_config_dir()/sessions). The only production call site, MainWindow._on_load_session in src/intellicrack/ui/app.py:1645 (`dialog = SessionManagerDialog(parent=self)`), passes none of these optional arguments. The application's real sessions are created via `self._orchestrator.start_session(...)` and persisted through the Orchestrator's SessionManager/SessionStore, which is backed by a SQLite file (`config.data_directory / "sessions.db"`, see main.py) -- a completely different store than SESSIONS_DIR. SESSIONS_DIR is only ever written to by this same dialog's disk-fallback import path (_save_session_to_disk), so in the shipped app it is normally empty. Additionally, because current_session_id is never supplied, `self._current_session_id` stays None: `_delete_btn.setEnabled(session['id'] != self._current_session_id)` is always True (any real ID != None), so the 'Cannot delete the currently active session' guard never fires for the true active session, and because current_session is never supplied the TagChipsWidget is permanently disabled (session is None), making the Tags group non-functional.
- **Failure:** User opens Help/File > 'Load Session' (MainWindow._on_load_session) on a real install with orchestrator-managed sessions in sessions.db. The dialog shows an empty or stale list from the unrelated SESSIONS_DIR sidecar folder instead of the user's actual sessions; the Tags panel is greyed out with no session to edit; and if any session (from the disk fallback) happens to share the id of the truly active in-memory session, the delete-protection check compares against None and does not disable the Delete button, so the user could delete data thinking they are protected.
- **Evidence:**
  ```python
  if self._manager is not None:
      ...
  else:
      self._load_sessions_from_disk()
  
  # app.py:1645
  dialog = SessionManagerDialog(parent=self)
  ```
- **Verified:** Verified every link in the chain. (1) SessionManagerDialog.__init__ (session_manager.py:442-463) accepts session_manager/current_session_id/current_session as optional, defaulting to None, and stores them as self._manager/self._current_session_id/self._current_session. (2) grep confirms app.py:1645 (`dialog = SessionManagerDialog(parent=self)`) is the only production instantiation of this dialog in the entire src tree - none of the optional args are passed. (3) _load_sessions (line 675-690): `if self._manager is not None: ... else: self._load_sessions_from_disk()` - with self._manager None, line 690's disk fallback always runs. (4) _load_sessions_from_disk (line 771-794) globs `self.SESSIONS …

#### H30 — Mid-download httpx transport errors are not caught, permanently freezing the install progress UI
- **File:** `src/intellicrack/ui/tool_config.py:256`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** tool-config-prefs
- **Defect:** _install_tool() wraps the call to _stream_download() with except clauses for only httpx.TimeoutException and httpx.ConnectError. httpx's exception hierarchy also includes httpx.ReadError, httpx.WriteError, httpx.RemoteProtocolError, httpx.LocalProtocolError, etc. (all under httpx.HTTPError but NOT under OSError). Any of those raised while streaming response.iter_bytes() (e.g. connection reset mid-download) propagates past this narrow except block, then past the outer ToolInstallWorker.run() handler which only catches (RuntimeError, OSError, ValueError) -- so it is never caught. The sibling method _fetch_github_release() correctly catches the broad httpx.HTTPError as a fallback (line 382-384), showing the download path is missing the same safety net. Because install_finished is never emitted in this path, _on_install_finished() never runs.
- **Failure:** A user starts installing x64dbg/Cutter/Ghidra and the network connection drops mid-download (common on flaky Wi-Fi/VPN). httpx raises httpx.ReadError/httpx.RemoteProtocolError from inside iter_bytes(); this is not RuntimeError/OSError/ValueError so it escapes both except blocks and terminates run() without emitting install_finished. The Install button stays disabled and the progress bar stays visible/stuck forever (no error dialog, no way to retry) until the whole Tool Settings dialog is closed and reopened.
- **Evidence:**
  ```python
  try:
      result = self._stream_download(url, zip_path)
  except httpx.TimeoutException:
      ...
      return
  except httpx.ConnectError:
      ...
      return
  # no except httpx.HTTPError here, unlike _fetch_github_release()'s
  # except httpx.HTTPError as exc: (line 382)
  ```
- **Verified:** Confirmed by reading tool_config.py lines 249-260 and 164-171, and verifying httpx's actual exception hierarchy at runtime. The try/except around `self._stream_download(url, zip_path)` (line 250) only catches `httpx.TimeoutException` (251) and `httpx.ConnectError` (256). Runtime introspection of the installed httpx package shows `httpx.ReadError`, `httpx.WriteError`, `httpx.RemoteProtocolError`, and `httpx.LocalProtocolError` all derive from `httpx.NetworkError`/`httpx.ProtocolError` -> `httpx.TransportError` -> `httpx.RequestError` -> `httpx.HTTPError` -> `Exception`, and are NOT subclasses of `OSError`, `RuntimeError`, or `ValueError`. Since `_stream_download` (lines 173-213) calls `respon …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### H31 — close_embedded_tools() drops ProcessBridge without releasing its Win32 handles
- **File:** `src/intellicrack/ui/tools.py:2216`
- **Severity:** HIGH  ·  **Category:** logic  ·  **Cluster:** tools
- **Defect:** close_embedded_tools() (invoked from MainWindow.closeEvent via app.py:3316 on every app shutdown) calls `self._process_panel.stop_tool()` and then simply nulls `self.process_bridge` via `setattr(self, attr_name, None)` in the bridge-clearing loop. Unlike the per-tab-close path (`_on_tab_close_requested`), it never calls `self._cleanup_bridge(bridge, bridge_attr)`. For the x64dbg/cutter/ghidra/frida panels this happens to be harmless because each panel's own `_cleanup()` (invoked by `stop_tool()`) shuts down the exact same bridge object that is also referenced by `self.x64dbg_bridge`/etc. (the panel's `set_bridge()` stores it in `self._bridge`, the identical object). However `ProcessPanel._cleanup()` (src/intellicrack/ui/panels/process_panel/base.py:410-414) only calls `self._process_tab.cleanup()` and `self._threads_tab.cleanup()` -- it never touches `self._bridge`. `ProcessBridge.shutdown()` (src/intellicrack/bridges/process.py:9338-9384) is the only code path that unmaps section views and calls `CloseHandle` on every tracked section/pipe/device handle plus the attached process handle. Because that coroutine is never awaited on this teardown path, every Win32 handle the ProcessBridge accumulated during the session (attached process handle, mapped sections, opened pipes/devices) leaks on application exit whenever the user opened the Process panel and attached to a target.
- **Failure:** User opens the Process tab, attaches to a target process (or maps a memory section / opens a device handle through the bridge), then closes Intellicrack normally. MainWindow.closeEvent calls tool_panel.close_embedded_tools(), which stops the panel's tabs/threads but never awaits ProcessBridge.shutdown(); the process handle and any mapped-section/pipe/device handles opened by the bridge are never closed, leaking OS handles for the lifetime of the Intellicrack process (or until process exit forcibly reclaims them).
- **Evidence:**
  ```python
  if self._process_panel is not None:
      self._process_panel.stop_tool()
      self._process_panel = None
  ...
  for attr_name in ("x64dbg_bridge", "ghidra_bridge", "cutter_bridge", "frida_bridge", "process_bridge"):
      if getattr(self, attr_name, None) is not None:
          setattr(self, attr_name, None)
          _logger.debug("bridge_reference_released", bridge=attr_name)
  ```
- **Verified:** Read tools.py:2177-2225 (close_embedded_tools), 1130-1155 (_cleanup_bridge), 1156-1216 (_on_tab_close_requested), and 1840-1898 (_create_process_panel/_resolve_process_bridge). Confirmed: close_embedded_tools() calls self._process_panel.stop_tool() then, in the loop at 2216-2219, only does setattr(self, 'process_bridge', None) — it never calls self._cleanup_bridge(bridge, bridge_attr) as _on_tab_close_requested does at line 1200. self.process_bridge and ProcessPanel._bridge are literally the same object (tools.py:1866-1868: set_bridge(bridge) then self.process_bridge = bridge). Read process_panel/base.py:409-441: ProcessPanel._cleanup() only calls self._process_tab.cleanup() and self._thread …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description. Verified the asymmetry claim (other bridge panels' _cleanup shuts down the same shared bridge object internally, whereas ProcessPanel's does not) by reading process_panel/base.py directly.

### Theming / contrast / readability

#### H32 — Python scripting editor syntax highlighter uses hardcoded dark-theme colors invisible in light theme
- **File:** `src/intellicrack/ui/panels/hex_editor/scripting.py:376`
- **Severity:** HIGH  ·  **Category:** theming  ·  **Cluster:** hex-scripting
- **Defect:** _PythonSyntaxHighlighter._build_rules() hardcodes a VSCode 'Dark+' color palette (#569CD6, #4EC9B0, #B5CEA8, #C586C0, #CE9178, #6A9955) for keywords/builtins/numbers/decorators/strings/comments, and never queries ThemeManager or subscribes to ThemeManager.theme_changed. Contrast this with the codebase's own HexPatSyntaxHighlighter (intellicrack/ui/highlighter.py), which explicitly resolves colors via ThemeManager.get_analysis_colors() and re-applies them on theme_changed -- the exact pattern that was already used to fix the prior audit's hardcoded-color findings. This highlighter attaches to the same _script_editor QPlainTextEdit whose background comes from the global stylesheet (#ffffff in light_theme.qss, QTextEdit/QPlainTextEdit rule) and #1e1e1e-class dark background in dark_theme.qss. The palette was designed for a dark background; on the light theme's white background several tokens become nearly unreadable, most severely the number literal color #B5CEA8 (pale green), which computes to roughly a 1.7:1 luminance contrast ratio against white -- far below the ~4.5:1 WCAG AA minimum and visually close to invisible.
- **Failure:** User switches to the light theme and opens the hex editor's Python Scripting tab. Any numeric literal in their script (e.g. `offset = 0x1000`) renders in pale green (#B5CEA8) on the white QPlainTextEdit background and is essentially unreadable; keyword/builtin/decorator tokens are also low-contrast. The highlighter never reacts to the theme change because it has no theme_changed subscription, so the problem persists for the whole session regardless of later theme toggles.
- **Evidence:**
  ```python
  kw_fmt.setForeground(QColor("#569CD6"))
  ...
  builtin_fmt.setForeground(QColor("#4EC9B0"))
  ...
  number_fmt = QTextCharFormat()
  number_fmt.setForeground(QColor("#B5CEA8"))
  ...
  decorator_fmt.setForeground(QColor("#C586C0"))
  ...
  string_fmt.setForeground(QColor("#CE9178"))
  ...
  comment_fmt.setForeground(QColor("#6A9955"))
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/scripting.py lines 346-411: `_PythonSyntaxHighlighter._build_rules()` hardcodes `QColor("#569CD6")` (keywords, line 365), `"#4EC9B0"` (builtins, line 371), `"#B5CEA8"` (numbers, line 376), `"#C586C0"` (decorators, line 383), `"#CE9178"` (strings, line 387), `"#6A9955"` (comments, line 394) — an exact VSCode Dark+ palette. A grep of the whole file for `ThemeManager|theme_changed` returns zero matches, confirming no theme awareness anywhere in this class or file. The highlighter is instantiated at line 1265 (`_PythonSyntaxHighlighter(editor_doc)`) attached to `self._script_editor = QPlainTextEdit()` (line 1257) with no `setObjectName` override, so it  …
- **Note:** The report is accurate as written; no correction needed to line/severity. This could arguably be framed as a regression relative to the fix pattern established in highlighter.py, but since scripting.py's highlighter was likely never touched by that prior fix (different file/class), it is more accurately a missed/incomplete rollout of the established pattern rather than a regression of a previously-fixed instance in this same file, so is_regression=false.

### Layout / sizing / text-cutoff

#### H33 — Only the last table column stretches, so the actual data column (Value/Function/Name) is clipped with no tooltip
- **File:** `src/intellicrack/ui/panels/analysis_panel.py:159`
- **Severity:** HIGH  ·  **Category:** layout  ·  **Cluster:** chat-misc-panels
- **Defect:** _create_table configures h_header.setStretchLastSection(True) with QHeaderView.ResizeMode.Interactive and no other sizing call. The extra space is granted only to the LAST column (Section/Address/Return Type), while the column holding the actual variable-length payload -- Value in the Strings table (col 1 of 4), Function/DLL in Imports (col 0-1 of 4), Name in Functions (col 1 of 5) -- stays at Qt's default ~100px Interactive width. No resizeColumnsToContents()/ResizeToContents mode is ever applied after population (_populate_strings/_populate_imports/_populate_functions), and no QTableWidgetItem in this file ever gets setToolTip(), so long extracted strings, DLL/function names, or C++ symbol names are truncated with no way to read the full value short of manually dragging the column wider.
- **Failure:** A binary analysis yields a string like "C:\\Program Files\\Vendor\\App\\license_validation_error_message.dll" or a mangled C++ export name; it lands in the Value/Name column at the default ~100px width, is visually truncated by Qt's cell renderer, and there is no tooltip or auto-resize to reveal the rest -- the user must manually drag the header divider for every long entry.
- **Evidence:**
  ```python
  h_header.setStretchLastSection(True)
              h_header.setSectionResizeMode(QHeaderView.ResizeMode.Interactive)
  ```
- **Verified:** Read src/intellicrack/ui/panels/analysis_panel.py lines 137-162 (_create_table) and 263-320 (_populate_strings/_populate_imports/_populate_functions). _create_table only calls h_header.setStretchLastSection(True) and h_header.setSectionResizeMode(QHeaderView.ResizeMode.Interactive) (lines 159-160) — no other sizing call. A repo-wide grep of the file for setToolTip, WordWrap, resizeColumnsToContents, and ResizeToContents returned zero matches, confirming no mitigation exists anywhere in the file. Column layout matches the claim exactly: Strings table headers = ["Address","Value","Encoding","Section"] (line 111) so Value is col 1 while stretch goes to Section (col 3, last); Imports = ["DLL","F …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### H34 — Modules table divides width equally, clipping the full module Path column
- **File:** `src/intellicrack/ui/panels/cutter_debugger_tab.py:368`
- **Severity:** HIGH  ·  **Category:** layout  ·  **Cluster:** cutter-tabs
- **Defect:** The modules table's 5 columns (Name, Base, Size, Entry Point, Path) all use uniform QHeaderView.ResizeMode.Stretch, which divides the available width equally among all sections. `ModuleInfo.path` is documented as the 'Full path to module' (src/intellicrack/core/types.py:874) e.g. `C:\Windows\System32\ntdll.dll` or much longer paths under Program Files, while the other four columns (Name, Base, Size, Entry Point) only need a fraction of that width for short hex/identifier values. Because QTableWidgetItem text does not wrap and Stretch mode forces the Path column into the same narrow share as the numeric columns, long paths are elided/clipped with no way to see the full value (no tooltip is set either), making it hard to identify the actual loaded module during debugging.
- **Failure:** User attaches the debugger to a process with modules under long paths (e.g. `C:\Program Files (x86)\SomeVendor\SomeLongApplicationName\plugins\protect.dll`); the Modules tab shows the Path column squeezed to roughly 1/5 of the table width, so the path is truncated and the user cannot tell which physical file a module maps to without manually widening the column (if even possible under Stretch mode, which ignores manual resize).
- **Evidence:**
  ```python
  modules_h = self._modules_table.horizontalHeader()
          if modules_h is not None:
              modules_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
  ```
- **Verified:** Read lines 353-370 and 904-919 of cutter_debugger_tab.py. The Modules table is built with 5 columns (_MODULE_COLUMNS = ["Name","Base","Size","Entry Point","Path"], line 56) and at lines 366-368 the horizontal header is set to uniform QHeaderView.ResizeMode.Stretch for all sections with no per-column resize mode override, no resizeColumnsToContents call, and no setStretchLastSection-only pattern anywhere else in the file. Stretch mode locks all sections to share available width proportionally to their default (equal) size and disallows manual interactive resize. The population code at line 919 writes str(getattr(mod, "path", "")) — a full filesystem path — into a QTableWidgetItem with no setT …
- **Note:** Report is accurate as written; no mitigation exists elsewhere in the file (regions/threads tables at lines 328-330 and 347-349 have the same pattern but were not part of this claim).

#### H35 — Search results table has no column resize mode at all, clipping the Detail column
- **File:** `src/intellicrack/ui/panels/cutter_search_tab.py:174`
- **Severity:** HIGH  ·  **Category:** layout  ·  **Cluster:** cutter-tabs
- **Defect:** `self._results_table` is constructed directly (not via a shared helper) and, unlike every other table in this file/cluster, never has `horizontalHeader().setSectionResizeMode(...)` called on it anywhere in the file (confirmed by grep - zero matches for header/ResizeMode/resizeColumn in this file). It keeps Qt's default `Interactive` resize mode with default section widths for both `Address` and `Detail`. The `Detail` column carries variable, often long text: for crypto-constant and magic-signature results it is populated from `entry.get(key)` over `("name", "type", "info", "comment")` or falls back to `str(entry)` (the whole dict) when none of those keys are present (see `_apply_dict_results`), so it can be a lengthy dict repr. Without any stretch/resize-to-contents and without word wrap (QTableWidgetItem doesn't wrap), this text is clipped at the small default column width.
- **Failure:** Running a crypto-constant or magic-signature search returns matches whose dict entries lack a `name`/`type`/`info`/`comment` key; `_apply_dict_results` falls back to `str(entry)` (a full dict repr, e.g. `{'offset': 4198400, 'type': 'aes-sbox-fwd', 'size': 256, 'extra': '...'}`), which is placed in the Detail column of a table whose header was never given a resize mode, so it renders truncated to Qt's default ~100px section width with no way to read the match description.
- **Evidence:**
  ```python
  self._results_table = QTableWidget(0, len(_RESULT_COLUMNS))
          self._results_table.setHorizontalHeaderLabels(_RESULT_COLUMNS)
          self._results_table.setSelectionBehavior(QTableWidget.SelectionBehavior.SelectRows)
          self._results_table.setSelectionMode(QTableWidget.SelectionMode.SingleSelection)
          vlayout.addWidget(self._results_table)
  ```
- **Verified:** Read the full file (508 lines). Lines 174-178 construct self._results_table and only call setHorizontalHeaderLabels/setSelectionBehavior/setSelectionMode — no setSectionResizeMode, resizeColumnsToContents, or setStretchLastSection anywhere in the file (grep confirmed zero matches for header/ResizeMode/resizeColumn). Detail column data comes from _apply_addresses (line 388, empty string, benign) and _apply_dict_results (lines 405-412), where line 407 shows `detail = next((str(entry[key]) for key in detail_keys if entry.get(key)), str(entry))` — falling back to a full dict repr via str(entry) when none of name/type/info/comment keys are present. That text goes into a plain QTableWidgetItem (no …
- **Note:** Report is accurate as written; no correction needed to line/severity/description.

#### H36 — ClassesTab tree has no header resize configuration for long class/method names
- **File:** `src/intellicrack/ui/panels/cutter_static_extra_tab.py:219`
- **Severity:** HIGH  ·  **Category:** layout  ·  **Cluster:** cutter-tabs
- **Defect:** `ClassesTab` builds a `QTreeWidget` with `setHeaderLabels(_CLASS_COLUMNS)` (Class, Address, Methods, Fields) but never calls `setSectionResizeMode`, `resizeColumnToContents`, or sets any column width. This is the only table/tree view in the four audited files with zero resize configuration (contrast with every `QTableWidget` in the same file, which is built through the `_make_table` helper that applies `QHeaderView.ResizeMode.Stretch` via `_stretch_headers`). The default `Interactive` resize mode gives the first column (Class) only the default header width, and RTTI/C++ class names surfaced by rizin's `icj` (especially demangled, templated, or namespaced names) are frequently far longer than that default width, so entries are clipped with an ellipsis and no tooltip.
- **Failure:** Analyzing a C++ binary with RTTI populates the Classes tab with entries such as `std::__1::basic_string<char, std::__1::char_traits<char>, std::__1::allocator<char>>` or a vendor namespace like `Acme::Licensing::Protection::ValidationEngine`; because no resize mode/width is set for the tree header, the Class column keeps its narrow default width and the name is clipped, leaving the analyst unable to identify the class without manually dragging the column wider for every row.
- **Evidence:**
  ```python
  self._tree = QTreeWidget()
          self._tree.setHeaderLabels(_CLASS_COLUMNS)
          layout.addWidget(self._tree)
  ```
- **Verified:** Read lines 206-278 and searched the whole file. ClassesTab.__init__ (lines 215-220) creates `self._tree = QTreeWidget()`, calls `setHeaderLabels(_CLASS_COLUMNS)`, and adds it to the layout — no `setSectionResizeMode`, `resizeColumnToContents`, `setColumnWidth`, or `header()` call follows. A file-wide grep for `setToolTip|resizeColumnToContents|setSectionResizeMode|header\(\)` finds only one hit: line 86 inside `_stretch_headers(table: QTableWidget)`, which is invoked exclusively by `_make_table` (used by CallGraphTab, VTable, Syscall, Zignature, Block tabs — all QTableWidget). ClassesTab never calls `_make_table`/`_stretch_headers` and uses a QTreeWidget instead, so it is genuinely excluded  …
- **Note:** Line 219 (setHeaderLabels call) is the more precise anchor than 218 (bare constructor), but both are within the same 2-line init block so the original line number is acceptable. Severity HIGH is reasonable given this is a static-analysis tab where class-name identification is the primary purpose and the failure is silent/data-dependent (only manifests with long RTTI names, but analysts cannot recover the text without manual resizing).

#### H37 — Module list tree has no column resize configuration for the Path column
- **File:** `src/intellicrack/ui/panels/process_panel/modules_tab.py:130`
- **Severity:** HIGH  ·  **Category:** layout  ·  **Cluster:** process-tabs
- **Defect:** _mod_tree is a 5-column QTreeWidget (Module, Base Address, Size, Path, Entry Point) built with only set_header_labels(...) -- no header().setSectionResizeMode(...) call anywhere. QTreeWidget's stretchLastSection defaults to True (verified), so only the last column ('Entry Point') auto-expands; 'Path' (index 3), which holds a full on-disk module path such as C:\Program Files\Common Files\Microsoft Shared\OFFICE16\something.dll, stays at the default interactive width and is not stretched, has no tooltip, and is not resized to content -- this is the primary, first-shown view of the Modules tab.
- **Failure:** User attaches to any real-world process and opens the default 'Module List' sub-tab; every module row's Path column is clipped to the default ~100px width for typically 40-100+ character DLL paths, with no way to widen automatically or hover to read the full path.
- **Evidence:**
  ```python
  self._mod_tree = QTreeWidget()
  set_header_labels(self._mod_tree, ["Module", "Base Address", "Size", "Path", "Entry Point"])
  tab_layout.addWidget(self._mod_tree)
  ```
- **Verified:** Read src/intellicrack/ui/panels/process_panel/modules_tab.py lines 130-132: `self._mod_tree = QTreeWidget()` followed only by `set_header_labels(self._mod_tree, ["Module", "Base Address", "Size", "Path", "Entry Point"])` and `tab_layout.addWidget(self._mod_tree)` — there is no `self._mod_tree.header()` call anywhere in the file (confirmed via full-file read/grep of the class), so no `setSectionResizeMode`, no `resizeColumnToContents`, no explicit `setColumnWidth`. QTreeWidget's default header resize mode is Interactive with only `stretchLastSection=True` applied automatically, so only the final column ('Entry Point', index 4) auto-expands. Column 3 ('Path') retains the small default interact …
- **Note:** Claim is accurate as reported. Confirmed no header().setSectionResizeMode call exists anywhere for _mod_tree, and no per-item tooltip is set in _refresh_modules, unlike the sibling _handle_table in the same file which has both mitigations. Severity HIGH is reasonable given this is the default-shown first sub-tab and Path is a primary identifying data field for RE workflows.

---

## MEDIUM — 69 finding(s)

### Threading / crashes / freezes

#### M1 — Synchronous native FFI enumeration of process memory regions runs directly on the GUI thread
- **File:** `src/intellicrack/ui/app.py:2963`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** app-shell
- **Defect:** `_on_process_attached` is a slot invoked synchronously on the GUI thread (connected directly to the process panel's `process_attached` signal). It calls `_hexcore.HexDocument.list_process_memory_regions(pid)` — a synchronous native call that walks the target process's entire virtual address space — directly inline, blocking the Qt event loop until enumeration completes. The codebase already provides `GenericCallableWorker` in `async_bridge.py`, whose docstring explicitly states it is "intended for FFI calls into native modules (PyO3, ctypes) ... that should not block the Qt event loop", but it is not used here.
- **Failure:** User attaches to a process with a large address space (e.g. a browser or another heavily-instrumented process with thousands of mapped regions); the GUI freezes for the duration of the native enumeration call before the region-picker dialog can even appear.
- **Evidence:**
  ```python
  regions: list[tuple[int, int, int, int]] = _hexcore.HexDocument.list_process_memory_regions(pid)
  ```
- **Verified:** Read src/intellicrack/ui/app.py:2934-2963. `_on_open_process` connects the panel's `process_attached` signal directly to `self._on_process_attached` (line 2945: `signal.connect(self._on_process_attached)`) with no `Qt.ConnectionType.QueuedConnection` or worker wrapper, so the slot runs synchronously on the GUI thread when the signal fires. Inside `_on_process_attached` (line 2948), line 2963 calls `_hexcore.HexDocument.list_process_memory_regions(pid)` — a native FFI call — directly inline inside a plain try/except, with no `GenericCallableWorker`, `QThread`, or async dispatch. Confirmed `GenericCallableWorker` exists at src/intellicrack/ui/panels/async_bridge.py:275 and is unused here. Imme …
- **Note:** Original claim's line, severity, and description are accurate as-is; no correction needed.

#### M2 — "Save All As..." serializes and writes potentially hundreds of thousands of rows synchronously on the GUI thread
- **File:** `src/intellicrack/ui/log_viewer/window.py:636`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** log-viewer
- **Defect:** `_on_save_all` collects every currently-visible (post-filter) row by iterating `self._proxy.rowCount()` — which can be as large as the configured Max Rows setting, up to 500,000 (`_MAX_ROWS`) — and `_save_records` then runs a synchronous Python loop calling `json.dumps` and `handle.write` for every record directly inside the toolbar action's slot, entirely on the GUI thread. There is no `QThread`/worker offload and no progress feedback, so a large capture (which the UI itself lets the user configure via the Max Rows spin box) will visibly freeze the whole application for the duration of the serialize+write.
- **Failure:** User raises Max Rows to e.g. 200,000+ and lets the viewer run for a while, then clicks "Save All As...". The event loop is blocked for several seconds while the entire filtered set is JSON-serialized and written line-by-line, during which the window is unresponsive and Windows may report it as "Not Responding".
- **Evidence:**
  ```python
  with Path(target).open("w", encoding="utf-8", newline="\n") as handle:
                  for record in records:
                      handle.write(json.dumps(dict(record), ensure_ascii=False, default=repr))
                      handle.write("\n")
  ```
- **Verified:** Read src/intellicrack/ui/log_viewer/window.py lines 76 (_MAX_ROWS: Final[int] = 500_000), 429 (save_all_action.triggered.connect(self._on_save_all)), 607-615 (_on_save_all iterates self._proxy.rowCount() building a records list), and 617-641 (_save_records opens the target file and runs a plain Python for-loop calling json.dumps + handle.write per record). _on_save_all and _save_records are plain instance methods with no QThread/QRunnable/QtConcurrent/moveToThread/QTimer.singleShot deferral and no decorator wrapping — the triggered signal from the toolbar action invokes this code directly on the GUI thread. The spin box controlling row retention is range-bound up to _MAX_ROWS=500,000 (line 5 …
- **Note:** No mitigation found anywhere in the file (no worker thread, no chunking/progress dialog, no async offload). Severity MEDIUM is appropriate: it's a real but bounded/opt-in freeze (requires user to raise Max Rows and accumulate a large capture), not a crash or data-loss bug.

#### M3 — Panel teardown blocks the GUI thread on an un-timed-out bridge shutdown RPC
- **File:** `src/intellicrack/ui/panels/cutter_panel.py:192`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** cutter-core
- **Defect:** `_cleanup()` is invoked synchronously from `AnalysisPanelBase.stop_tool()` (base_panel.py) on whatever thread calls it — i.e. the GUI thread for a normal panel-close action. It calls the blocking variant `run_bridge_coroutine(self._bridge.shutdown())` with no `timeout_s`, which resolves to `future.result(timeout=None)` in async_bridge.py — an unbounded wait. If the underlying r2pipe subprocess is unresponsive or its shutdown RPC hangs, the entire Qt event loop is frozen with no way for the user to cancel, unlike every other bridge call in this panel which uses the non-blocking `run_bridge_coroutine_logged` path.
- **Failure:** User closes/stops the Cutter panel while the r2pipe backend process is wedged (e.g. still executing a long `aaaa` deep analysis or its stdio pipe is stalled); `shutdown()` never completes, `future.result()` blocks forever, and the whole Intellicrack UI becomes unresponsive until the process is killed externally.
- **Evidence:**
  ```python
  @override
  def _cleanup(self) -> None:
      """Shut down the Cutter bridge if active."""
      if self._bridge is not None and self._bridge.state.is_ready():
          try:
              run_bridge_coroutine(self._bridge.shutdown())
          except (RuntimeError, ConnectionError, OSError):
              _logger.exception("cutter_shutdown_failed", bridge_type="cutter")
  ```
- **Verified:** cutter_panel.py:187-194 `_cleanup()` calls `run_bridge_coroutine(self._bridge.shutdown())` with no `timeout_s` argument. In async_bridge.py:346-408, `run_bridge_coroutine` defaults `timeout_s=None`, and when not on a running event loop (the normal case for a synchronous GUI call) it does `future.result(timeout=timeout_s)` at line 403 — with `timeout_s=None` this is an unbounded blocking wait (`concurrent.futures.Future.result(timeout=None)` blocks forever). base_panel.py:310-319 `stop_tool()` calls `self._cleanup()` synchronously (no thread offload) then emits `tool_closed`. `stop_tool()` is invoked from `tools.py` `close_embedded_tools()`/`_on_tab_close_requested()` (line 1194, 2188), and ` …
- **Note:** Verified end-to-end: GUI signal (tabCloseRequested) -> _on_tab_close_requested -> stop_tool (synchronous) -> _cleanup -> run_bridge_coroutine(no timeout_s) -> future.result(timeout=None) in async_bridge.py. No mitigation found anywhere in the call chain.

#### M4 — Bit-toggle click blocks the GUI thread with the synchronous run_bridge_coroutine()
- **File:** `src/intellicrack/ui/panels/hex_editor/data_inspector.py:265`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-transforms
- **Defect:** `_toggle_bit_via_bridge`, invoked synchronously from `_on_bit_toggled` which is connected to each of the 8 bit-editor `QPushButton.clicked` signals, calls the blocking `run_bridge_coroutine(bridge.toggle_bit(offset, bit_index))` directly on the Qt main thread rather than an async/logged variant.
- **Failure:** Every click of a bit-editor toggle button blocks the Qt event loop for the duration of the bridge round-trip (dispatch to the background event-loop thread plus `future.result()` wait) instead of remaining responsive; repeated bit toggling (a common workflow for manually patching flag bytes) produces a stutter on every click, and any transient delay in the shared bridge event loop (e.g. a concurrent slow RPC queued on the same loop) stalls the entire UI, not just this one control.
- **Evidence:**
  ```python
  run_bridge_coroutine(bridge.toggle_bit(offset, bit_index))
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/data_inspector.py lines 130-269. `btn.clicked.connect(_make_bit_handler(bit_index))` (line 156) wires each of the 8 bit-editor QPushButtons to `_on_bit_toggled` (line 190), a plain slot that runs synchronously on the Qt GUI thread (default direct connection, same thread as the widget). `_on_bit_toggled` calls `self._toggle_bit_via_bridge(bridge, offset, bit_index, checked=checked)` (line 216) directly (not via a worker thread), and inside that helper, line 265 is exactly `run_bridge_coroutine(bridge.toggle_bit(offset, bit_index))`.

Verified `run_bridge_coroutine` in src/intellicrack/ui/panels/async_bridge.py (lines 346-408): when called from a thre …
- **Note:** Claim is accurate as stated. Note for context (not a reason to reject): several other call sites in the same hex_editor panel directory (va_mapping.py, search.py, patches.py, comparison.py, export_report.py, transforms.py) use the same blocking run_bridge_coroutine() pattern for similarly lightweight bridge calls, suggesting this may be a systemic pattern rather than an isolated oversight — but that does not make this specific instance any less real.</notes>
</invoke>

#### M5 — Encode-text action blocks the GUI thread with the synchronous run_bridge_coroutine()
- **File:** `src/intellicrack/ui/panels/hex_editor/data_inspector.py:486`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-transforms
- **Defect:** `_on_encode_text` is wired directly to `encode_btn.clicked` (line 385 in the same mixin) and calls the blocking `run_bridge_coroutine(bridge.encode_text(text, encoding))` on the Qt main thread instead of the non-blocking `run_bridge_coroutine_logged`/`run_bridge_coroutine_async` variants used for every other bridge call in the surrounding code (e.g. `_populate_sections`, `_scan_signatures_via_bridge`, `_on_convert`). This freezes the event loop for the full round trip of dispatching to the persistent background asyncio loop and waiting on `future.result()`.
- **Failure:** User types text into the encode field and clicks "Encode" while a codec that requires non-trivial work (e.g. a large EBCDIC/Shift-JIS string, or the bridge event loop is momentarily busy with another queued coroutine) is selected; the whole hex editor UI becomes unresponsive until the RPC round-trip completes.
- **Evidence:**
  ```python
  hex_str = run_bridge_coroutine(bridge.encode_text(text, encoding))
  ```
- **Verified:** Read data_inspector.py:455-502. `_on_encode_text` (wired to encode_btn.clicked at line 385) calls `hex_str = run_bridge_coroutine(bridge.encode_text(text, encoding))` at line 486 with no timeout, on the Qt GUI thread (it's a plain slot method, not run on a worker thread). `run_bridge_coroutine`'s own docstring in async_bridge.py (lines 353-362) explicitly states: 'This is the **blocking** variant. Use `run_bridge_coroutine_async` for non-blocking execution with signal-based result delivery.' It dispatches to a persistent background asyncio loop and blocks on `future.result()` (implementation continues past line 386, waiting synchronously). The claim that sibling code in the same composed pan …
- **Note:** No correction needed; claim is accurate as stated. Note for context: line 265 (_on_toggle_bit path) has the same blocking-call pattern and could be a companion finding, but that is outside the scope of this specific claim.

#### M6 — Pattern highlight rule resolution runs a blocking native search on the GUI thread when adding a rule
- **File:** `src/intellicrack/ui/panels/hex_editor/highlighting.py:188`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** `_resolve_pattern_rule`, called synchronously from `_on_add_highlight_rule` (the 'Add Rule' button's click slot) before any bridge dispatch, calls `document.search_hex(pattern, _HIGHLIGHT_PATTERN_MAX_MATCHES)` directly on the GUI thread to pre-resolve match offsets. This is the same class of issue as the per-edit refresh above: a whole-document native scan (bounded only by a 10000-match cap, not by scan cost) executes with no worker/async dispatch, blocking the event loop while it runs.
- **Failure:** User adds a 'Pattern' condition highlight rule on a large loaded binary; clicking 'Add Rule' freezes the UI for the duration of the native `search_hex` call before the bridge RPC that actually persists the rule is even dispatched.
- **Evidence:**
  ```python
  if document is not None and hasattr(document, "search_hex"):
      try:
          matches = document.search_hex(pattern, _HIGHLIGHT_PATTERN_MAX_MATCHES)
  ```
- **Verified:** Read highlighting.py lines 168-250. `_on_add_highlight_rule` (bound to the 'Add Rule' button's `clicked` signal at line 134) calls `_resolve_pattern_rule` synchronously (line 233) for the Pattern condition path, *before* `run_bridge_coroutine_logged` is invoked at line 240. Inside `_resolve_pattern_rule`, line 188 calls `document.search_hex(pattern, _HIGHLIGHT_PATTERN_MAX_MATCHES)` directly and synchronously — `_HIGHLIGHT_PATTERN_MAX_MATCHES = 10000` (line 40), a match-count cap, not a scan-cost bound. `self.document` is set in panel.py:758 as `hexcore.HexDocument.open(...)`, a direct PyO3-bound native object (not an async bridge proxy), and `search_hex` is implemented in intellicrack-hexcor …

#### M7 — Patch export/import block the GUI thread via the blocking run_bridge_coroutine variant
- **File:** `src/intellicrack/ui/panels/hex_editor/patches.py:164`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-misc
- **Defect:** _on_export_patches (line 164) and _on_import_patches (line 252) call run_bridge_coroutine(bridge.export_patches(...)) / run_bridge_coroutine(bridge.import_patches(...)) synchronously from the Qt click handler. For BPS/UPS formats the bridge must diff/rebuild against the full original file on disk, which for large binaries is a non-trivial synchronous operation performed while the Qt main thread is blocked on future.result().
- **Failure:** User exports patches to BPS/UPS format against a large original binary; the bridge computes a full byte-diff of the original vs. patched document on its background loop while the GUI thread sits blocked in future.result(), so the whole window becomes unresponsive until the diff/rebuild finishes.
- **Evidence:**
  ```python
  patch_b64 = run_bridge_coroutine(bridge.export_patches(patch_format, original_path))
  ```
- **Verified:** patches.py:112-269 wires _on_export_patches/_on_import_patches directly to QPushButton.clicked (panel.py:655,658), so they execute on the Qt GUI thread. Both call the blocking variant run_bridge_coroutine (patches.py:164, 252), which (async_bridge.py:400-408) does asyncio.run_coroutine_threadsafe(...).result(timeout=timeout_s) — a synchronous block on the calling (GUI) thread with no processEvents/event-loop pumping. For BPS/UPS formats, bridges/hex_editor.py export_patches_bps/import_patches_bps and export_patches_ups/import_patches_ups perform a full source-vs-target diff/rebuild (via asyncio.to_thread offloading the actual bytes work to a worker thread, but the GUI thread still sits block …
- **Note:** Both line 164 (export) and line 252 (import) exhibit the same defect; report's primary line 164 is accurate. Severity MEDIUM is appropriate — it's a real UI-freeze risk but scoped to a manual, infrequent user action (patch export/import) rather than a routine hot path, and there is a bounded async alternative (run_bridge_coroutine_async) already present in the same module that could be used instead.

#### M8 — HexPat pattern applied via direct interpreter execution runs synchronously on the GUI thread with no timeout or worker offload
- **File:** `src/intellicrack/ui/panels/hex_editor/pattern_editor.py:417`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-scripting
- **Defect:** _apply_via_interpreter() is invoked directly from the 'Apply at Cursor' button click handler (_on_pattern_apply) and calls `interpreter.execute(source, self.document, offset)` inline on the GUI thread. HexPatInterpreter.execute (core/hexpat/interpreter.py) is a pure-Python tree-walking interpreter for a user-authored DSL that supports loops/arrays over arbitrary struct definitions, with no iteration cap or timeout. This is inconsistent with the same file's other document/bridge operations (_populate_template_combo, _on_auto_bookmark_structure) which route through `run_bridge_coroutine_logged` specifically to keep long-running work off the GUI thread -- the exact remediation pattern the prior audit applied for blocking bridge calls. Direct interpreter execution bypasses that pattern entirely.
- **Failure:** User writes or loads a HexPat pattern containing a large/nested array (e.g. `MyStruct entries[10000] @ 0x0;`) and clicks 'Apply at Cursor'. interpreter.execute() runs synchronously inside the Qt click-event handler, blocking the event loop (frozen window, no repaint, no cancel) for the full duration of the interpretation instead of running on a worker thread the way equivalent bridge calls in the same file already do.
- **Evidence:**
  ```python
  try:
              fields: list[dict[str, Any]] = interpreter.execute(source, self.document, offset)
          except (ValueError, TypeError, AttributeError) as exc:
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/pattern_editor.py lines 299-317 (_on_pattern_apply, wired via `apply_btn.clicked.connect(self._on_pattern_apply)` at line 187) and 393-458 (_apply_via_interpreter). Line 417 confirms `fields = interpreter.execute(source, self.document, offset)` is called directly and synchronously inside the click-handler call chain, with no QThread/QRunnable/worker/moveToThread anywhere in the file (grep for those terms returned zero matches). HexPatInterpreter.execute (core/hexpat/interpreter.py lines 111-160) runs preprocess -> lex -> parse -> `evaluator.evaluate(program)` all in-process with no timeout parameter and no iteration/array-size cap; evaluator.py show …
- **Note:** Minor imprecision in the report: _populate_template_combo and _on_auto_bookmark_structure are not literally defined in pattern_editor.py but in the sibling mixin file hex_editor/templates.py, composed into the same panel class via multiple inheritance. This does not change the substance of the finding — the async pattern exists in the same class/component and is not applied to the interpreter execution path.

#### M9 — Superseded strings-extraction worker is not actually cancelled and can overwrite fresh results with stale data
- **File:** `src/intellicrack/ui/panels/hex_editor/sections.py:325`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-transforms
- **Defect:** When `_populate_strings` is invoked while a previous extraction is still running, it calls `previous.requestInterruption()` intending to cancel the stale worker. `GenericCallableWorker.run()` (async_bridge.py) never checks `isInterruptionRequested()`/`isInterruptionRequested` at all -- it unconditionally runs `self._func(*self._args, **self._kwargs)` (here, `execute_strings_extraction`, a plain synchronous call with no interruption hooks) to completion and always emits `call_finished`/`call_error`. The old worker's `call_finished` signal remains connected to `_on_strings_ready`, which unconditionally calls `self._strings_tree.clear()` and repopulates it.
- **Failure:** User re-triggers a strings scan (e.g. switches away and back to the Strings tab, or reloads) before the first extraction on a large binary finishes. The new worker starts and, depending on relative completion timing of the two background threads, the OLD worker's `call_finished` can fire after the NEW worker's, silently clearing the freshly-populated, correct strings tree and replacing it with the stale results from the superseded scan -- with no indication to the user that displayed data is out of date.
- **Evidence:**
  ```python
  previous = getattr(self, "_strings_worker", None)
          if isinstance(previous, GenericCallableWorker) and previous.isRunning():
              previous.requestInterruption()
  ```
- **Verified:** Read sections.py:309-390 and async_bridge.py:128-336. `_populate_strings` (sections.py:323-325) calls `previous.requestInterruption()` on the stale `GenericCallableWorker`, but `GenericCallableWorker.run()` (async_bridge.py:322-335) never calls `isInterruptionRequested()` anywhere; it unconditionally executes `self._func(*self._args, **self._kwargs)` (here `execute_strings_extraction`, sections.py:69-89, a plain synchronous `document.extract_strings(...)` call with zero interruption support) and always emits `call_finished` or `call_error`. The new worker is connected (sections.py:340-341) without disconnecting the old worker's identical connections to `_on_strings_ready`/`_on_strings_failed …
- **Note:** Report is accurate as written; no correction needed to line/severity/description.

#### M10 — Statistics worker's Qt-parent check tests the wrong type, so the worker is always parented to None
- **File:** `src/intellicrack/ui/panels/hex_editor/statistics.py:240`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** Every other worker-launch site in the hex_editor package (60+ occurrences across hashing.py, comparison.py, disassembly.py, bookmarks.py, sandbox.py, sections.py, templates.py, transforms.py, yara.py, va_mapping.py, highlighting.py, search.py, pattern_editor.py, scripting.py, process_memory.py, export_report.py, calculator.py) uses `self if isinstance(self, QWidget) else None` to give the background worker a Qt parent when `self` is the panel/widget. `_update_statistics` instead checks `isinstance(self, QThread)`, which is never true because `self` here is the HexEditorPanel (a QWidget-derived AnalysisPanelBase subclass), never a QThread. `parent_obj` is therefore always `None`, so `GenericCallableWorker(compute_statistics, ..., parent=parent_obj)` never receives the intended Qt parent -- the statistics worker is the one worker in the whole mixin family with no Qt-owned lifecycle, relying solely on the module's best-effort self-retention registry and the manual `_stop_pending_workers()` bookkeeping in panel.py rather than normal Qt parent-child teardown.
- **Failure:** Any code path that expects `self._statistics_worker`'s Qt parent chain to participate in normal Qt object-tree cleanup (e.g. if the panel were ever destroyed via Qt's parent-delete cascade rather than through the explicit `_cleanup()`/`_stop_pending_workers()` path) will not have the statistics worker cleaned up alongside the panel, unlike every other hex-editor worker.
- **Evidence:**
  ```python
  parent_obj: QThread | None = self if isinstance(self, QThread) else None
  worker = GenericCallableWorker(
      compute_statistics,
      self.document,
      ENTROPY_BLOCK_SIZE,
      parent=parent_obj,
  )
  ```
- **Verified:** Read statistics.py lines 200-250: at line 240, `parent_obj: QThread | None = self if isinstance(self, QThread) else None`. `self` here is `StatisticsMixin`, a mixin used by `HexEditorPanel` which extends `AnalysisPanelBase`/`QWidget` (confirmed via imports at line 14: `from PyQt6.QtWidgets import QLabel, QTreeWidget, QTreeWidgetItem, QWidget`, and the correct sibling usage `parent = self if isinstance(self, QWidget) else None` at line 438 in the very same file). `self` is never a `QThread` instance, so `isinstance(self, QThread)` is always False and `parent_obj` is always `None`. Verified `GenericCallableWorker.__init__` in src/intellicrack/ui/panels/async_bridge.py (line 301) takes `parent: …
- **Note:** Report is accurate as written; no correction needed. Confirmed by direct comparison with the correct pattern at line 438 of the same file.

#### M11 — Digram matrix computed synchronously on the GUI thread, unlike the equivalent statistics computation
- **File:** `src/intellicrack/ui/panels/hex_editor/statistics.py:432`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-panel-search
- **Defect:** `_on_show_digram_matrix` calls `document.digram_matrix()` (a native FFI call that scans the whole document to build a 256x256 byte-pair frequency table) directly inside the button-click slot, then does `[int(v) for v in digram_result]` over up to 65536 elements in pure Python, all on the GUI thread. This is inconsistent with `_update_statistics()` in the very same class, which correctly offloads the comparable-cost `compute_statistics` call to a `GenericCallableWorker` specifically to avoid blocking the Qt event loop.
- **Failure:** User clicks 'Digram Matrix' on a large loaded binary; the native scan plus the 65536-element Python list comprehension run on the GUI thread before the dialog opens, freezing the whole application (no repaint, no input processing) for the duration of the computation.
- **Evidence:**
  ```python
  digram_result: Any = digram_fn()
  raw_matrix: list[int] = [int(v) for v in digram_result]
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/statistics.py lines 421-440: `_on_show_digram_matrix` is a plain slot (not wired through any worker) that calls `digram_fn()` (i.e. `self.document.digram_matrix()`) directly at line 432, then does `raw_matrix: list[int] = [int(v) for v in digram_result]` at line 433, all synchronously on the calling (GUI) thread, before `dlg.exec()` opens the dialog. Checked the native implementation in src/intellicrack-hexcore/src/entropy.rs (`pub fn digram_matrix`, lines 94+): it scans the entire byte buffer building a 65536-entry (256x256) frequency table (`data.windows(2)` for buffers <1MB, or a rayon-parallel chunked path above 1MB) — a full-document scan compa …
- **Note:** Report is accurate as written; line 432 (and 433) correctly identified as the blocking synchronous FFI call plus the 65536-element Python list comprehension inside the button-click slot. Severity MEDIUM is appropriate: it's a real but bounded (single-click, self-limiting duration) UI freeze, not a crash or data-corruption issue.

#### M12 — VA<->file-offset lookups block the GUI thread via the blocking run_bridge_coroutine variant
- **File:** `src/intellicrack/ui/panels/hex_editor/va_mapping.py:303`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-misc
- **Defect:** _on_goto_va (line 303) and _on_cursor_offset_to_va (line 334) call run_bridge_coroutine(...) directly instead of run_bridge_coroutine_logged used everywhere else in this same file (_on_add_va_mapping, _on_remove_va_mapping, _on_auto_detect_va_mappings, _on_refresh_va_mappings all use the async/signal-based logged variant). run_bridge_coroutine is documented in async_bridge.py as 'the blocking variant': it calls future.result(timeout_s) on the calling thread, so when invoked from a Qt slot it blocks the Qt main thread until the bridge event-loop thread finishes the RPC round trip.
- **Failure:** User enters a VA in 'Go to VA' or clicks 'Cursor Offset -> VA' while the bridge event loop is busy servicing another slow operation (e.g. a concurrent disassembly or YARA scan on the shared bridge loop); the GUI thread stalls waiting on future.result() with no timeout configured, freezing the whole window until the bridge call completes.
- **Evidence:**
  ```python
  offset = run_bridge_coroutine(bridge.va_to_file_offset(va))
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/va_mapping.py: line 106 `goto_btn.clicked.connect(self._on_goto_va)` and line 112 `offset_lookup_btn.clicked.connect(self._on_cursor_offset_to_va)` wire these handlers directly as Qt slots on the GUI thread. Inside, line 303 is exactly `offset = run_bridge_coroutine(bridge.va_to_file_offset(va))` and line 334 is `va = run_bridge_coroutine(bridge.file_offset_to_va(cursor_offset))`, both called with no `timeout_s` kwarg. Confirmed in src/intellicrack/ui/panels/async_bridge.py (lines 346-408) that `run_bridge_coroutine` is documented as "the **blocking** variant" and its implementation does `future = asyncio.run_coroutine_threadsafe(resolved_coro, loop …
- **Note:** Also worth noting (not part of the original claim, doesn't change verdict): _on_open_performance_settings at line 364 has the same blocking run_bridge_coroutine(bridge.get_memory_usage()) pattern with no timeout, reinforcing that this is a real, repeated deviation from the file's established async-logged convention rather than an isolated one-off.

#### M13 — Large-file Performance Settings dialog itself uses blocking bridge calls on the GUI thread
- **File:** `src/intellicrack/ui/panels/hex_editor/va_mapping.py:364`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** hex-misc
- **Defect:** _on_open_performance_settings uses the blocking run_bridge_coroutine for get_memory_usage() (line 364) and again for set_chunk_size()/set_memory_budget() (lines 383-384), each of which calls future.result() and stalls the Qt main thread until the background bridge loop responds. This is the dialog specifically intended for tuning large-file streaming behavior, so it is exercised precisely in the large-file scenarios where the shared bridge event loop is most likely to be busy servicing chunked reads for the same oversized document, making the block longer and more noticeable.
- **Failure:** User opens 'Performance Settings...' while working with a very large file whose chunked reads are still in flight on the bridge loop; the dialog's own attempt to read current usage blocks the GUI thread waiting on the busy bridge loop, freezing the window before the dialog even appears.
- **Evidence:**
  ```python
  usage_result = run_bridge_coroutine(bridge.get_memory_usage())
  ```
- **Verified:** va_mapping.py:349-390 defines VaMappingMixin._on_open_performance_settings, wired directly to perf_btn.clicked (line 117), so it executes on the Qt GUI thread. It calls the blocking `run_bridge_coroutine` (imported from async_bridge.py, distinct from the non-blocking `run_bridge_coroutine_async`/`run_bridge_coroutine_logged` used elsewhere in the same file at lines 162/208/227/258) three times: line 364 for get_memory_usage(), and lines 383-384 for set_chunk_size()/set_memory_budget(). Reading async_bridge.py confirms `run_bridge_coroutine` (lines 346-408) submits the coroutine to a persistent background event-loop thread via `asyncio.run_coroutine_threadsafe` and then calls `future.result(t …
- **Note:** Report is accurate; no correction needed to line/severity. Confirmed both the get_memory_usage() call and the subsequent set_chunk_size()/set_memory_budget() calls use the blocking, unbounded-timeout run_bridge_coroutine while sibling handlers in the same file correctly use the non-blocking run_bridge_coroutine_logged/async variant, showing this is an inconsistency rather than an inherent constraint.</notes>
</invoke>

#### M14 — OpenRouter generation cost lookup blocks the GUI thread on a synchronous network call
- **File:** `src/intellicrack/ui/provider_config.py:3211`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** provider-config
- **Defect:** `_fetch_openrouter_generation`, called from `get_openrouter_generation` which is invoked directly by the 'Lookup Cost' button's click handler `_on_lookup_generation` on the GUI thread, uses the blocking `run_bridge_coroutine(_fetch())`, where `_fetch()` connects to OpenRouter and awaits `provider.get_generation(generation_id)` over the network.
- **Failure:** User enters a generation ID and clicks 'Lookup Cost'; the whole application freezes until the OpenRouter API responds (or times out).
- **Evidence:**
  ```python
  async def _fetch() -> dict[str, Any] | None:
      try:
          await provider.connect(creds)
          return await provider.get_generation(generation_id)
      finally:
          await provider.disconnect()
  
  return run_bridge_coroutine(_fetch())
  ```
- **Verified:** Confirmed by direct code trace. `_on_lookup_generation` (provider_config.py:2509-2531) is connected to the button's `clicked` signal (line 2135: `gen_btn.clicked.connect(self._on_lookup_generation)`), so it runs synchronously on the Qt GUI thread. At line 2517 it calls `self.get_openrouter_generation(gen_id)` directly (no QThread, no `run_bridge_coroutine_async`, no worker dispatch). That method (3163-3183) calls `self._fetch_openrouter_generation(...)` (3180), a `@staticmethod` (3185-3211) that builds a coroutine `_fetch()` performing `await provider.connect(creds)`, `await provider.get_generation(generation_id)` (network I/O), `await provider.disconnect()` (3204-3209), and returns it via ` …
- **Note:** No corrections needed; report is accurate as written. MEDIUM severity is appropriate: it freezes the GUI thread for the duration of a synchronous network call (connect + get_generation + disconnect) with no timeout, but does not crash the app and only affects the OpenRouter cost-lookup feature.

#### M15 — SandboxTestWorker QThread has no Qt parent and is never stopped on dialog teardown
- **File:** `src/intellicrack/ui/sandbox_config.py:863`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** vnc-sandbox-config
- **Defect:** SandboxTestWorker(...) is constructed without passing parent=self even though the constructor accepts a QThread parent, so the only reference keeping it alive is the Python attribute self._test_worker. SandboxConfigDialog has no closeEvent or reject() override that calls self._cancel_test()/self._test_worker.stop() when the dialog is dismissed while a test is in flight (e.g. during the 10-second 'waiting for sandbox to initialize' window in _launch_sandbox_test). Clicking Cancel/closing the dialog via the titlebar X or Escape routes only to QDialog.reject via button_box.rejected.connect(self.reject), which never touches _test_worker.
- **Failure:** User opens Sandbox Settings, clicks 'Test Sandbox', then immediately closes the dialog (X button or Escape) before the test completes. The QThread and its already-launched WindowsSandbox.exe subprocess keep running detached from the now-closed dialog with no UI affordance to cancel them, and the worker's finished/output signals continue firing into a dialog the user believes is gone.
- **Evidence:**
  ```python
  self._test_worker = SandboxTestWorker(
              network_enabled=self._network_enabled_checkbox.isChecked(),
              memory_limit_mb=self._memory_spin.value(),
              shared_folder=self._shared_folder_input.text(),
              read_only=self._read_only_checkbox.isChecked(),
          )
  ```
- **Verified:** Read src/intellicrack/ui/sandbox_config.py fully around the relevant sections. Confirmed all elements of the claim: (1) Line 863: `self._test_worker = SandboxTestWorker(...)` does not pass `parent=self` even though `SandboxTestWorker.__init__` (line 251-269) accepts `parent: QThread | None = None` and forwards it to `super().__init__(parent)` — so no Qt parent is set. (2) `SandboxConfigDialog` (class starts line 502) has no `closeEvent`, `reject`, or `done` override anywhere in the file (grep for `def reject|def closeEvent|def done` returns zero matches) — `button_box.rejected.connect(self.reject)` at line 642 wires straight to the unmodified `QDialog.reject`, which is also what fires on Esc …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M16 — Sandbox stop-by-PID/name paths block the GUI thread on synchronous subprocess calls
- **File:** `src/intellicrack/ui/sandbox_config.py:1329`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** vnc-sandbox-config
- **Defect:** _dispatch_pid_kill() and _invoke_taskkill_by_name() (line 1371) are reached from the same GUI-thread _stop_sandbox() click handler and call ProcessManager.run_tracked(...) - the synchronous/blocking subprocess wrapper - with timeout=10, directly on the GUI thread. The codebase already provides an async counterpart (run_tracked_async) used elsewhere for exactly this reason, but these two call sites use the blocking variant, freezing the UI for up to 10 seconds while taskkill runs.
- **Failure:** User clicks 'Stop Sandbox' when no SandboxManager is attached and no PID is tracked; _terminate_sandbox_by_name() runs 'taskkill /F /IM WindowsSandbox.exe' via the blocking run_tracked with a 10s timeout, during which the whole application UI is unresponsive.
- **Evidence:**
  ```python
  process_manager.run_tracked(
                  ["taskkill", "/F", "/PID", str(pid)],
                  name="taskkill-sandbox-pid",
                  check=False,
                  timeout=10,
                  creationflags=CREATE_NO_WINDOW,
              )
  ```
- **Verified:** sandbox_config.py:1224 wires `_stop_btn.clicked.connect(self._stop_sandbox)` directly on the GUI thread. `_stop_sandbox` (line 1269) calls `_stop_via_pid` -> `_dispatch_pid_kill` (line 1317-1339, the reported call at 1329) or `_terminate_sandbox_by_name` -> `_invoke_taskkill_by_name` (line 1356-1378, call at 1371) with no thread offload. Both call `ProcessManager.run_tracked(...)` (process_manager.py:1006), which is confirmed synchronous: it does `Popen(...)` then `ProcessManager._communicate_tracked(process=process, ..., timeout=timeout, ...)` (line 1066-1073), which blocks the calling thread on `communicate(timeout=...)` for up to the 10s timeout before returning. The codebase does provide …
- **Note:** Report is accurate as written; both call sites (1329 for PID-kill, 1371 for name-kill) exhibit the same blocking pattern. No mitigation found in the file.

#### M17 — Tag add/remove persistence uses the blocking run_bridge_coroutine on the GUI thread
- **File:** `src/intellicrack/ui/session_manager.py:582`
- **Severity:** MEDIUM  ·  **Category:** threading  ·  **Cluster:** splash-xpu-session
- **Defect:** `_on_tags_changed`, connected to `TagChipsWidget.tags_changed` which is emitted synchronously from the chip-click / Add-Tag button handlers, calls `run_bridge_coroutine(manager.update(session))` -- again the blocking variant documented in async_bridge.py (`future.result(timeout_s=None)` on the calling thread). Every tag add or remove therefore blocks the Qt GUI thread on a synchronous SQLite write via `SessionManager.update`/`SessionStore`, rather than using the non-blocking `run_bridge_coroutine_async`/`run_bridge_coroutine_logged` path used elsewhere after the prior remediation.
- **Failure:** User adds or removes a tag chip; the click handler blocks the GUI thread until the background loop's SQLite UPDATE completes, causing a perceptible stall/beachball on every single tag edit, worse under disk contention.
- **Evidence:**
  ```python
  run_bridge_coroutine(manager.update(session))
  ```
- **Verified:** session_manager.py:564 connects TagChipsWidget.tags_changed to _on_tags_changed with a normal (synchronous/direct) Qt connection. tags_changed is emitted at lines 396 and 417 directly inside _on_add_clicked (Add-Tag button/Enter key handler) and _on_chip_clicked (chip click handler) — both invoked on the GUI thread. _on_tags_changed (lines 569-588) calls run_bridge_coroutine(manager.update(session)) at line 582 with no timeout_s argument. async_bridge.py's run_bridge_coroutine (lines 346-446) is documented as 'the blocking variant' and uses future.result(timeout_s=None) which waits forever on the calling (GUI) thread for the coroutine to finish on the background event-loop thread. SessionMan …

### Logic / wiring / leaks

#### M18 — Multi-line JavaScript template literals are not tracked across blocks
- **File:** `src/intellicrack/ui/highlighter.py:1125`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** highlighter-toolbar-dock
- **Defect:** `JavaScriptSyntaxHighlighter`'s backtick-string rule `` `[^`\\]*(\\.[^`\\]*)*` `` is a single-line regex applied only through `_apply_rules`, which iterates matches within one text block. Unlike Python's triple-quoted strings (which get dedicated cross-block state tracking via `_highlight_triple_quotes`/`previousBlockState()`), there is no block-state mechanism for backtick template literals here, even though JS/Frida template literals routinely span multiple lines.
- **Failure:** A Frida script (this highlighter is explicitly documented for 'JavaScript/Frida script keywords') containing a multi-line template literal, e.g. `` const payload = `line one\nline two`; ``, has its second and later lines rendered with default/keyword/operator formatting instead of the string color, and any keyword-like tokens inside the un-terminated literal (e.g. `return`, `if`) get incorrectly highlighted as JS keywords rather than as string content.
- **Evidence:**
  ```python
  (r"`[^`\\]*(\\.[^`\\]*)*`", "string", False, False),
  ```
- **Verified:** Read src/intellicrack/ui/highlighter.py in full context. Line 1125 confirms the backtick-string rule `r\"`[^`\\\\]*(\\\\.[^`\\\\]*)*`\"` is registered as a plain rule tuple in JavaScriptSyntaxHighlighter.__init__ (lines 1119-1129). highlightBlock for this class (lines 1133-1142) calls only self._apply_rules(text) and self._scan_block_comments(text). _apply_rules (lines 167-177) does a single QRegularExpression globalMatch pass over the current block's text only — it has no access to or dependency on previousBlockState()/setCurrentBlockState(), so it cannot recognize a string that started in a prior block. _scan_block_comments (lines 233-260+) does implement genuine cross-block state via setC …
- **Note:** Claim is accurate as written; no correction needed to line, severity, or description.

#### M19 — Add-Flag validation/error messages are written into the Resolve-Flag result label
- **File:** `src/intellicrack/ui/panels/cutter_tabs.py:592`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** cutter-core
- **Defect:** FlagsTab has two distinct rows: an "Add Flag" row (Name/Size/Address inputs + Add Flag button) and a "Resolve Address" row with its own result label `self._resolve_result_label` (created at line 481, directly to the right of the Resolve Flag button). `_on_add_flag` (validation failures) and `_on_add_flag_error` (RPC failure) both write their status text into that same `_resolve_result_label` instead of a label associated with the Add Flag row, so failures from adding a flag are displayed anchored to the unrelated Resolve Address control below it. `_on_add_flag_success` (lines 577-582) also never clears `_resolve_result_label`, so a stale "Add failed: ..." message can persist next to the Resolve row indefinitely, and conversely a later resolve failure/success silently overwrites what was actually an add-flag status.
- **Failure:** User types a flag name and an invalid/empty address in the Add Flag row and clicks "Add Flag": no feedback appears near the Add row; instead "Enter a name and a valid address" (or later "Add failed: <exc>") appears next to the Resolve Address input/button, which the user was not interacting with, making the error look like it came from a Resolve operation.
- **Evidence:**
  ```python
  def _on_add_flag(self) -> None:
      ...
      if not name or address is None:
          self._resolve_result_label.setText("Enter a name and a valid address")
          return
      try:
          size = int(self._add_size_input.text().strip() or "1")
      except ValueError:
          self._resolve_result_label.setText("Invalid size")
          return
  ...
  def _on_add_flag_error(self, exc: object) -> None:
      self._add_btn.setEnabled(True)
      _logger.warning("cutter_add_flag_failed", error=str(exc))
      self._resolve_result_label.setText(f"Add failed: {exc}")
  ```
- **Verified:** Read src/intellicrack/ui/panels/cutter_tabs.py lines 440-620. The Add Flag row (lines 456-471) has no dedicated status/result label — only static prompt labels (Name/Size/Address) and the _add_btn button. The Resolve Address row (473-484) owns self._resolve_result_label (line 481), packed directly into resolve_row. Yet _on_add_flag's validation-failure paths write into that label ("Enter a name and a valid address" at line 556, "Invalid size" at line 561), and _on_add_flag_error (line 592) writes "Add failed: {exc}" into the same self._resolve_result_label. _on_add_flag_success (577-582) never clears it, so a stale add-flag error can linger next to the Resolve row indefinitely, and a subsequ …
- **Note:** Report is accurate as written; no correction needed. Confirmed also that lines 556 and 561 (the two validation branches inside _on_add_flag) exhibit the same misdirected-label bug as the reported line 592, reinforcing the finding.

#### M20 — Symbols table 'Is Global' and 'Type' columns are permanently blank for Enumerate Symbols results
- **File:** `src/intellicrack/ui/panels/frida_instrumentation_tab.py:671`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** frida-instrumentation
- **Defect:** _populate_symbols_from_module unconditionally inserts empty-string QTableWidgetItems into columns 2 ("Is Global") and 3 ("Type") for every row it adds, regardless of the actual symbol data, so those two declared, headered columns are non-functional dead placeholders for the entire Enumerate Symbols code path.
- **Failure:** User enters a module name and clicks 'Enumerate Symbols'; every resulting row shows a populated Name and Address but permanently blank 'Is Global' and 'Type' cells no matter what module is queried, even though the columns are presented as real data fields.
- **Evidence:**
  ```python
  self._symbols_table.setItem(row, 2, QTableWidgetItem(""))
                  self._symbols_table.setItem(row, 3, QTableWidgetItem(""))
  ```
- **Verified:** Read frida_instrumentation_tab.py lines 655-674: `_populate_symbols_from_module` iterates `result` (a `list[SymbolInfo]`), extracts only `name` and `address` via getattr, and unconditionally does `self._symbols_table.setItem(row, 2, QTableWidgetItem(""))` / `setItem(row, 3, QTableWidgetItem(""))` (lines 671-672) for every row — matching the reported code exactly. Table headers at line 55 are `_SYMBOL_COLUMNS = ["Name", "Address", "Is Global", "Type"]`, so columns 2/3 are declared as "Is Global"/"Type" but never populated from real data, for every call. Traced the root cause: FridaBridge.enumerate_symbols (frida_bridge.py lines 4826-4885) actually queries `s.isGlobal` and `s.type` from the Fr …
- **Note:** Root cause traced one layer deeper than the report: the empty strings in the UI are a direct symptom of FridaBridge.enumerate_symbols() (frida_bridge.py lines 4874-4882) discarding the isGlobal/type fields it already fetched from the Frida script, because the shared SymbolInfo dataclass (core/types.py) has no fields to carry them. A full fix requires either extending SymbolInfo or having the bridge return the raw dicts so the UI can read isGlobal/type as well as fixing the two hardcoded empty QTableWidgetItem calls.

#### M21 — Symbols table 'Is Global' column populated with module name instead of a global/local flag
- **File:** `src/intellicrack/ui/panels/frida_instrumentation_tab.py:750`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** frida-instrumentation
- **Defect:** _SYMBOL_COLUMNS defines column index 2 as "Is Global" (list is ["Name", "Address", "Is Global", "Type"]). _populate_symbols_from_matching (the handler for the "Find Functions Matching" glob search) writes the symbol's module name into that exact column instead of a boolean/global-flag value, while _populate_symbols_from_module (the handler for "Enumerate Symbols") leaves the same column blank for the same header. The same table therefore shows two mutually inconsistent, and both wrong, meanings for the "Is Global" header depending on which button was clicked.
- **Failure:** User enters a glob pattern (e.g. '*CreateFile*') and clicks 'Find Functions Matching'; the resulting table's 'Is Global' column shows values like 'kernel32.dll' (a module name) rather than true/false, misleading the user about which symbols are actually global versus local.
- **Evidence:**
  ```python
  self._symbols_table.setItem(row, 2, QTableWidgetItem(module))
  ```
- **Verified:** Read frida_instrumentation_tab.py lines 600-753. _SYMBOL_COLUMNS (line 55) = ["Name", "Address", "Is Global", "Type"], so column index 2 is header "Is Global". In _populate_symbols_from_matching (handler for the glob "Find Functions Matching" search, wired at line 725), line 745 computes `module = str(getattr(sym, "module_name", "") or "")` and line 750 does `self._symbols_table.setItem(row, 2, QTableWidgetItem(module))` — placing the module name string (e.g. "kernel32.dll") into the "Is Global" column. Meanwhile _populate_symbols_from_module (handler for "Enumerate Symbols", wired at line 647) sets the same column to an empty string at line 671 (`QTableWidgetItem("")`), never populating any …
- **Note:** Report is accurate as written; no correction needed. Additional detail: the companion blank-column bug is at line 671 in _populate_symbols_from_module, reinforcing the inconsistency described.

#### M22 — Shared symbol-lookup error handler force re-enables buttons belonging to unrelated in-flight requests
- **File:** `src/intellicrack/ui/panels/frida_instrumentation_tab.py:762`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** frida-instrumentation
- **Defect:** _on_symbol_lookup_error is used as the on_error callback for three independent asynchronous bridge calls (Enumerate Symbols, Find Module by Address, Find Functions Matching), each of which disables only its own button before dispatching. Instead of re-enabling just the button for the operation that actually failed, this handler unconditionally re-enables all three buttons. This is inconsistent with the correct pattern used elsewhere in the same file: StalkerConfigControls._on_stalker_config_error and ScriptMessagingControls._on_script_messaging_error both take an explicit `button` parameter and only re-enable that one control.
- **Failure:** User clicks 'Find Module by Address' (disables _find_module_btn, dispatches request A), then quickly clicks 'Enumerate Symbols' (disables _enum_symbols_btn, dispatches request B) before A resolves. If request B fails first, _on_symbol_lookup_error re-enables _find_module_btn even though request A is still pending, letting the user click it again and fire a second overlapping 'Find Module by Address' bridge call while the first has not completed.
- **Evidence:**
  ```python
  self._enum_symbols_btn.setEnabled(True)
          self._find_module_btn.setEnabled(True)
          self._find_matching_btn.setEnabled(True)
  ```
- **Verified:** Read lines 634-766 of frida_instrumentation_tab.py. Each of the three operations disables only its own button before dispatch: `_on_enumerate_symbols` disables `_enum_symbols_btn` (line 644), `_on_find_module_by_address` disables `_find_module_btn` (line 686), `_on_find_functions_matching` disables `_find_matching_btn` (line 722). All three success handlers correctly re-enable only their own button (lines 661, 703, 739). But the shared `_on_symbol_lookup_error` (lines 755-766), wired as `on_error` for all three via `run_bridge_coroutine_logged` (lines 648, 690, 726), unconditionally re-enables all three buttons (lines 762-764) regardless of which operation actually failed. Checked `run_bridg …
- **Note:** Confirmed exactly as described. No mitigation found elsewhere in the file; run_bridge_coroutine_logged does not serialize or queue concurrent dispatches, so the overlapping-click race is real.

#### M23 — Pattern library tree treats category/folder nodes as clickable templates, silently failing on every category click
- **File:** `src/intellicrack/ui/panels/hex_editor/pattern_editor.py:572`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** hex-scripting
- **Defect:** _on_pattern_library_clicked only special-cases the true top-level roots ('Built-in', 'User', 'HexPat Patterns') by checking `item.parent() is None`. But _populate_pattern_library() and _populate_hexpat_library_entries() build a THREE-level tree: root -> category (e.g. 'PE', 'ELF', 'Mach-O', 'ZIP', 'Common', 'Other', or a HexPat community-pattern category name) -> actual template/pattern leaf. Category nodes have a non-None parent (they pass the guard) but carry no UserRole data and are not real template names. Clicking one falls through to `template_name = item.text(0)` followed by `self.document.export_template_json(template_name)`, which is called with the category label (e.g. "PE") instead of a real template name and raises AttributeError/ValueError, silently swallowed by the except block with only a logger.exception call.
- **Failure:** User expands the 'Built-in' node in the pattern library tree and single-clicks the 'PE' category folder expecting either a no-op or an expand/collapse. Instead `document.export_template_json("PE")` is invoked and raises, the exception is logged and discarded, and nothing visible happens -- the same silent-failure path fires for every category node under every root, including all HexPat community-pattern categories.
- **Evidence:**
  ```python
  parent_item = item.parent()
          if parent_item is None:
              return
  
          hexpat_path = item.data(0, Qt.ItemDataRole.UserRole)
          if isinstance(hexpat_path, str) and hexpat_path:
              self._load_hexpat_from_library(hexpat_path, item.text(0))
              return
  
          template_name = item.text(0)
          try:
              json_str_val: str = self.document.export_template_json(template_name)
          except (AttributeError, ValueError):
              _logger.exception("pattern_library_load_failed")
  ```
- **Verified:** Confirmed by reading pattern_editor.py lines 558-711. The tree built by _populate_pattern_library (lines 622-671) and _populate_hexpat_library_entries (673-711) is three levels deep: top-level root ('Built-in'/'User' at 637-641, 'HexPat Patterns' at 698) -> category node (e.g. cat_item = QTreeWidgetItem([category]); builtin_root.addChild(cat_item) at 660-663, or cat_item = QTreeWidgetItem([category]); hexpat_root.addChild(cat_item) at 701-703) -> leaf template/pattern item. Category items are never given Qt.ItemDataRole.UserRole data and are not made non-selectable/non-clickable (no ItemFlag changes).

_on_pattern_library_clicked (558-594) only guards `parent_item = item.parent(); if parent_ …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M24 — 'User' root node in the pattern library tree is created but never populated by any code path
- **File:** `src/intellicrack/ui/panels/hex_editor/pattern_editor.py:640`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** hex-scripting
- **Defect:** _populate_pattern_library() creates a top-level QTreeWidgetItem labeled 'User' and adds it to the tree, but `user_root` is never referenced again -- no `.addChild()` call targets it anywhere in the file or the rest of the hex_editor package (verified via project-wide search). Every template returned by document.list_templates() is instead bucketed into `builtin_root` by the name-prefix heuristic a few lines below, and user-imported templates (via TemplatesMixin._on_import_template / _import_template_from_path in templates.py) only ever populate the separate template combo box, never this tree. The 'User' folder is therefore a permanently empty dead control that is always shown to the user.
- **Failure:** User imports a custom JSON template via 'Import Template', then opens the pattern editor panel and expands the 'User' node in the library tree expecting to find and re-select their imported template there. The node has zero children and always will, regardless of how many templates are imported, because nothing ever calls user_root.addChild(...).
- **Evidence:**
  ```python
  user_root = QTreeWidgetItem(["User"])
          self._pattern_library_tree.addTopLevelItem(user_root)
  ```
- **Verified:** Read pattern_editor.py lines 622-671. `_populate_pattern_library` creates `user_root = QTreeWidgetItem(["User"])` and adds it via `self._pattern_library_tree.addTopLevelItem(user_root)` (lines 640-641), but the local variable `user_root` is never referenced again in the function or file. The template loop at lines 643-667 buckets every entry from `document.list_templates()` into `categories[category]`, which are children of `builtin_root` (line 662: `builtin_root.addChild(cat_item)`) via the name-prefix heuristic — never `user_root`. The subsequent `_populate_hexpat_library_entries` (lines 673-711) adds a third top-level item `hexpat_root` ("HexPat Patterns") populated from the community-pat …
- **Note:** Report is accurate as written; no correction needed to title, description, or failure scenario.

#### M25 — Alignment grid is never drawn in the hex view despite toolbar tooltip promising 'Alignment grid'
- **File:** `src/intellicrack/ui/panels/hex_editor_widget.py:511`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** hex-widget-core
- **Defect:** set_alignment_grid_size() stores self._alignment_grid_size and calls self.update() to trigger a repaint, but self._alignment_grid_size is never referenced in any paint routine, so no grid lines/markers are ever drawn at the alignment boundaries. The only consumer of self._alignment_grid_size in the codebase is panel.py's _on_snap_alignment(), which uses it purely for cursor-snap math (goto_offset), not visualization. The panel's toolbar combo box that drives this setter is explicitly labeled with tooltip 'Alignment grid' (hex_editor/panel.py line 345), so the visible feature that the UI promises (a grid overlay) does not exist; only an invisible snap-to-boundary side effect works.
- **Failure:** User selects '4096 (Page)' from the Alignment combo box (tooltip: 'Alignment grid') expecting visual grid markers every 4096 bytes in the hex view. No grid lines appear anywhere in the rendered hex/ASCII columns; only pressing the separate 'snap to alignment' action silently jumps the cursor, with no visual confirmation of the alignment boundaries.
- **Evidence:**
  ```python
  def set_alignment_grid_size(self, size: int) -> None:
      """Set the alignment grid column width and refresh the viewport.
  
      Args:
          size: Grid alignment size in bytes. Use 0 to disable alignment grid.
      """
      self._alignment_grid_size = size
      self.update()
  ```
- **Verified:** Read hex_editor_widget.py lines 401, 505-512: `_alignment_grid_size` is initialized to 0 and set only by `set_alignment_grid_size()`, which stores the value and calls `self.update()` — no other write/read exists in this file besides those two sites (confirmed via grep across the whole file, 3 total hits). The paint pipeline is `paintEvent` -> `_paint_content` (lines 709-745), which calls only `_paint_separators`, `_paint_data_rows`, and `_paint_highlight_overlays`; none of these, nor any other paint helper in the file (`_paint_row_hex_groups`, `_paint_hex_group`, `_paint_hex_group_background`, `_paint_hex_byte`, `_paint_ascii_byte`), reference `_alignment_grid_size`. The only other consumer  …
- **Note:** Report is accurate as written; line 505 (def) vs 511 (the actual assignment inside the setter) are both reasonable anchors, no correction needed to severity or scenario.

#### M26 — System-tab device/pipe/section handles never closed on panel teardown
- **File:** `src/intellicrack/ui/panels/process_panel/base.py:412`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** process-system
- **Defect:** `SystemTab` tracks OS-level resources opened by the user in its 'Kernel Objects' and 'Pipes' sub-tabs: `_device_handles` (device_open), `_pipe_handles` (pipe_connect), `_section_handles`/`_section_views` (create_section/map_section). None of these are closed automatically. `ProcessPanel._cleanup()` (called from `stop_tool()` and on panel teardown) only calls `self._process_tab.cleanup()` and `self._threads_tab.cleanup()`; it never calls anything on `self._system_tab`, and `SystemTab` itself defines no `cleanup()`/`closeEvent` method at all. `set_bridge()` also silently drops the previous bridge's tracked handles when a new bridge is assigned.
- **Failure:** User opens a device handle (Device I/O group), connects a named pipe, and creates/maps a section object via the System tab, then closes the panel or calls `stop_tool()` (or the panel replaces its bridge via `set_bridge()`) without manually clicking Close/Unmap for each resource; the underlying HANDLEs, pipe connections, and mapped section views are leaked for the lifetime of the process bridge/target process since nothing in the teardown path releases them.
- **Evidence:**
  ```python
  @override
      def _cleanup(self) -> None:
          """Stop timers and cancel pending workers."""
          self._process_tab.cleanup()
          self._threads_tab.cleanup()
  ```
- **Verified:** Verified against the actual code. `ProcessPanel._cleanup()` (src/intellicrack/ui/panels/process_panel/base.py:410-413) only calls `self._process_tab.cleanup()` and `self._threads_tab.cleanup()` — it never touches `self._system_tab` or calls `bridge.shutdown()`. `SystemTab` (system_tab.py) defines `_pipe_handles`, `_device_handles`, `_section_handles`, `_section_views` dicts (lines 68-71) populated by `_on_device_open`, `_on_pipe_connect`, `_on_create_section`, `_on_map_section` (lines 1209-1413, 1685-1759), but a grep of the entire file for `cleanup`/`closeEvent` returns zero matches — SystemTab has no teardown method at all. `ProcessPanel.set_bridge()` (base.py:93-109) reassigns `self._brid …
- **Note:** Report is accurate as written; no correction needed to title, description, or failure scenario. Confirmed additionally that ProcessBridge.shutdown() (process.py:9338) is the real handle-closing mechanism that should be invoked but never is from this panel's teardown/bridge-swap paths, unlike sibling panels (cutter_panel.py, ghidra_panel.py) which correctly call bridge.shutdown() in their _cleanup().

#### M27 — Register name column is editable and its raw text is trusted as the write-back key
- **File:** `src/intellicrack/ui/panels/process_panel/threads_tab.py:552`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** process-tabs
- **Defect:** _reg_table's column 0 (register name) items are created with QTableWidgetItem(str(reg_name)) and no ItemFlag restriction is ever applied (Qt.ItemFlag.ItemIsEditable is set by default). The default cell edit triggers (double-click / edit-key / any-key) let the user directly modify the register name text. _on_write_registers later reads name_item.text() verbatim as the dict key sent to bridge.set_thread_context, with no validation against the known register set. The devs were clearly aware column 0 needed special handling (cellChanged explicitly ignores col==0 via `if col not in {1, 2}: return`), but never restricted its edit flags, so it remains user-editable while being trusted as an authoritative key.
- **Failure:** User double-clicks a register-name cell (e.g. accidentally while trying to select the row) and types a stray character before clicking away; on the next 'Write Registers' click, regs[<corrupted-name>] = <value> is sent to bridge.set_thread_context, silently writing to the wrong/nonexistent register or causing an unexplained bridge error with no indication the register name itself was altered.
- **Evidence:**
  ```python
  self._reg_table.setItem(row, 0, QTableWidgetItem(str(reg_name)))
  ...
  name_item = self._reg_table.item(row, 0)
  val_item = self._reg_table.item(row, 1)
  if name_item is not None and val_item is not None:
      register_name = name_item.text()
      raw_value = val_item.text()
      try:
          regs[register_name] = int(raw_value, 16)
  ```
- **Verified:** Read threads_tab.py lines 225-682. Line 552 creates the register-name cell with `QTableWidgetItem(str(reg_name))` and no `setFlags()` call is ever made on it (confirmed via grep for `ItemIsEditable|setFlags|NoEditTriggers` across the whole file — zero matches), so the item keeps Qt's default flags including `ItemIsEditable`, and the table has no `setEditTriggers(NoEditTriggers)` call either, so default triggers (DoubleClicked, EditKeyPressed, AnyKeyPressed) apply to every cell including column 0. The devs clearly reasoned about column-specific behavior: `_on_reg_cell_changed` (line 569) explicitly does `if col not in {1, 2}: return` (line 582), showing awareness that column 0 is different/sh …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M28 — _on_destroy_error forces the panel into 'destroyed' state even though the destroy call failed, orphaning the sandbox
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:836`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** sandbox-panel
- **Defect:** When the bridge's destroy() call itself fails (network error, backend exception), _on_destroy_error unconditionally clears self.sandbox_id, forces the status indicator to 'Inactive', disables every sandbox control via _set_sandbox_controls_active(active=False), and stops the status poll timer -- identical to the success path. This differs from the equivalent pattern used elsewhere in the codebase (e.g. ghidra_panel._on_disconnect_error, which reports the failure and resyncs from actual state instead of assuming the operation succeeded).
- **Failure:** User clicks Destroy on a sandbox that has an active PCAP capture (self._pcap_capture_id set) or is otherwise still running server-side, but the destroy RPC fails (e.g. transient bridge/network error). The panel immediately loses self.sandbox_id and disables all controls (including PCAP stop, screenshot, memory dump), even though the backend sandbox instance is still alive and its PCAP capture is still running. The user has no way to retry destroy, stop the capture, or otherwise interact with that now-orphaned instance short of restarting the whole bridge/app.
- **Evidence:**
  ```python
  self._log(f"[-] Failed to destroy sandbox: {exc}")
  self.sandbox_id = None
  self._status_indicator.setText("Inactive")
  self._set_sandbox_controls_active(active=False)
  self._status_poll_timer.stop()
  self.tool_closed.emit()
  ```
- **Verified:** Read src/intellicrack/ui/panels/sandbox_panel.py lines 796-841. `_on_destroy` (796-812) disables destroy_btn and calls `self._bridge.destroy(self.sandbox_id)` async via `run_bridge_coroutine_logged`, wiring both `_on_destroy_success` (814-827) and `_on_destroy_error` (829-841) as callbacks. The error handler at line 829 is byte-for-byte identical in its state mutations to the success handler: it sets `self.sandbox_id = None` (836), forces `self._status_indicator.setText(\"Inactive\")` (837), calls `self._set_sandbox_controls_active(active=False)` (838, which per line 674-681 disables destroy_btn and other controls), stops `self._status_poll_timer` (839), and emits `self.tool_closed` (840) -- …
- **Note:** Report is accurate as written; no correction needed to line/severity/description.

#### M29 — Restart-create failure leaves sandbox_id pointing at an already-destroyed instance with controls still enabled
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:899`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** sandbox-panel
- **Defect:** _on_restart destroys the current sandbox first (_on_restart_destroy_success), then creates a replacement. If the create phase fails, _on_restart_error only re-enables restart_btn -- it never clears self.sandbox_id, never calls _set_sandbox_controls_active(active=False), never stops _status_poll_timer, and never resets the status indicator. Every other failure handler that represents a real state change (_on_destroy_error, _on_create_error) resyncs at least some UI state; this one leaves stale state entirely intact.
- **Failure:** User clicks Restart. The destroy phase succeeds (old sandbox instance is gone) but the subsequent create phase fails (e.g. resource exhaustion, backend error). self.sandbox_id still holds the old, now-destroyed instance id, yet Screenshot/PCAP/Memory Dump/Execute/Copy In-Out/etc. remain enabled and the status indicator still reads 'Active'. Any of those actions the user clicks next dispatches a bridge call against a nonexistent sandbox id and fails with a confusing backend error instead of the UI having already reflected the destroyed state.
- **Evidence:**
  ```python
  def _on_restart_error(self, exc: object) -> None:
      self._log(f"[-] Failed to restart sandbox: {exc}")
      self.restart_btn.setEnabled(True)
      _logger.warning("sandbox_restart_failed", error=str(exc))
  ```
- **Verified:** Read lines 843-907 and 2331-2411. `_on_restart` (843-859) issues `bridge.destroy(self.sandbox_id)` with `on_error=self._on_restart_error`; on success it proceeds to `bridge.create(...)` (861-883), again with `on_error=self._on_restart_error`. The single shared error handler at 899-907 only does `self._log(...)`, `self.restart_btn.setEnabled(True)`, and a warning log — it never touches `self.sandbox_id`, never calls `_set_sandbox_controls_active(active=False)`, never stops `_status_poll_timer`, and never resets `_status_indicator`. This is fine for a destroy-phase failure (nothing changed), but for a create-phase failure (reached via `_on_restart_destroy_success` after the destroy already suc …
- **Note:** Report is accurate as written; no correction needed. Confirmed via reading _on_restart/_on_restart_destroy_success/_on_restart_create_success/_on_restart_error (843-907) and verifying no mitigation exists in the status-poll error path (2331-2411).

#### M30 — Connecting a bridge does not refresh the stale "Not connected" status label
- **File:** `src/intellicrack/ui/panels/stack_viewer.py:738`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** `set_x64dbg_bridge`/`set_frida_bridge` attach the bridge to the underlying data source but never call `self._update_status()` or `self.refresh()`. The panel is constructed with `status_label` defaulted to "Not connected" (line 548) and only re-evaluates status inside `_on_source_changed()` or `refresh()`. `tools.py._wire_stack_viewer_bridges()` calls exactly these two setters when the x64dbg/Frida tool tabs are created, with no follow-up refresh call.
- **Failure:** User opens the Stack Viewer panel before attaching to a debugger tab; once the x64dbg tab attaches its bridge via `_wire_stack_viewer_bridges()`, the already-visible Stack Viewer keeps showing "Not connected" (its construction-time default) even though the bridge is now live, until the user manually clicks Refresh, toggles Auto, or reselects the source combo — the connection state displayed is stale/never applied.
- **Evidence:**
  ```python
  source = self._sources.get("x64dbg")
          if isinstance(source, X64DbgStackSource):
              source.set_bridge(bridge)
              _logger.info("bridge_attached", source="x64dbg", component="stack_viewer")
  ```
- **Verified:** Read stack_viewer.py lines 732-752: set_x64dbg_bridge/set_frida_bridge only call source.set_bridge(bridge) and log; neither calls self._update_status() nor self.refresh(). _update_status() (lines 633-654) is the only method that rewrites status_label's text/property based on source.is_connected(), and it is invoked solely from _on_source_changed() (line 607) and refresh() (line 676) — both of which require either the user changing the combo box or an explicit/auto refresh. The auto-refresh QTimer is not started by default; it only starts when the user toggles it on via _on_auto_refresh_toggled (line 610), which requires user action. status_label defaults to 'Not connected' at construction (l …
- **Note:** Report is accurate as written; no mitigation elsewhere in the file (no timer auto-starts by default, no signal connects bridge-set to status refresh). Severity MEDIUM is appropriate: cosmetic/status staleness, not a crash, but genuinely misleading UI state that could confuse users about whether the debugger/Frida connection is live.

#### M31 — Module detail table leaves a stale, always-empty 4th column when switching from Sections to Exports view
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:1933`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** x64dbg-panel
- **Defect:** `self._mod_detail_table` is constructed once with a fixed column count of `len(_SECTION_DETAIL_COLUMNS)` == 4 (line 366). `_on_show_module_exports` calls `setHorizontalHeaderLabels(_EXPORT_DETAIL_COLUMNS)`, which only has 3 entries, and never calls `setColumnCount(3)`. `_apply_module_exports` (line 1945) only populates columns 0–2 for each row. The table therefore always retains a 4th column whose header text is left over from whatever the Sections view last set ("Characteristics") and whose cells are never populated for any Exports row.
- **Failure:** User selects a module and clicks "Exports": the resulting table shows 4 columns (Name, Ordinal, Address, Characteristics) but every row has a permanently blank 4th cell under the stale "Characteristics" header, which does not belong to the exports data model at all.
- **Evidence:**
  ```python
  self._mod_detail_table.setHorizontalHeaderLabels(_EXPORT_DETAIL_COLUMNS)
  ```
- **Verified:** Read lines 358-373 and 1875-1960. `_mod_detail_table` is constructed at line 366 as `QTableWidget(0, len(_SECTION_DETAIL_COLUMNS))` i.e. 4 columns, with `_SECTION_DETAIL_COLUMNS = ["Name", "Address", "Size", "Characteristics"]` (line 72) and `_EXPORT_DETAIL_COLUMNS = ["Name", "Ordinal", "Address"]` (line 73, only 3 entries). `_on_show_module_exports` (line 1919) calls `self._mod_detail_table.setHorizontalHeaderLabels(_EXPORT_DETAIL_COLUMNS)` at line 1933 — with only 3 strings supplied to a 4-column table, PyQt6 sets columns 0-2 and leaves column 3's header text unchanged (retaining whatever it was previously, e.g. "Characteristics" from Sections view, or the default numeric label "4" if Expo …
- **Note:** Claim is accurate as written; no correction needed to title, description, or failure scenario.

#### M32 — set_tab_content/set_tab_info/clear_tab are dead for every OutputType except "log"
- **File:** `src/intellicrack/ui/tools.py:905`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** tools
- **Defect:** `set_tab_content`, `set_tab_info`, `append_tab_content`, and `clear_tab` all gate on `self.tabs.get(tab_name.lower())`, but the only place that ever inserts into `self.tabs` is `append_log_message` (`self.tabs["log"] = log_tab`, line 1072). None of the native panel factories (`add_analysis_panel`, `add_ghidra_tab`, `add_cutter_tab`, etc.) register into `self.tabs` -- they register into `self.panels`/`self.embedded_tools` instead. Consequently `display_analysis_result(tab_name, content, info)` (which calls `set_tab_content` then `set_tab_info`) is a silent no-op for content/info display whenever `tab_name != "log"`. This is exercised concretely by `app.py:_on_bridge_analysis_displayed`, which calls `self.tool_panel.clear_analysis_tab("analysis")` and `self.tool_panel.display_analysis_result("analysis", str(analysis))` on every bridge-analysis completion -- both calls do nothing because `"analysis"` is never a key in `self.tabs`; only `activate_tab`'s fallback to `self.panels.get("analysis")` (inside `display_analysis_result`) actually brings the (unmodified) Analysis tab into focus. The formatted `str(analysis)` text is discarded.
- **Failure:** Bridge analysis completes and app.py's `_on_bridge_analysis_displayed` slot runs `display_analysis_result("analysis", str(analysis))` expecting the formatted text to appear; because "analysis" never exists in `self.tabs`, `set_tab_content`/`set_tab_info` silently do nothing -- the call has no visible effect beyond switching focus to the (separately-populated) Analysis tab. Any future caller relying on `set_tab_content`/`append_tab_content`/`clear_tab` for a native-panel-backed OutputType ("ghidra", "cutter", "process", "binary", "sandbox", etc., all present in the OutputType Literal) will likewise see no content update with no error raised.
- **Evidence:**
  ```python
  def set_tab_content(self, tab_name: OutputType, content: str) -> None:
      if tab := self.tabs.get(tab_name.lower()):
          tab.set_content(content)
  ```
- **Verified:** Verified directly in src/intellicrack/ui/tools.py: self.tabs is declared at line 796 (dict[str, ToolTab]) and the ONLY place anything is ever inserted into it is append_log_message (lines 1070-1072, key \"log\"). set_tab_content (905-906), set_tab_info (916-917), append_tab_content (926-927), and clear_tab (1082-1083) all gate exclusively on self.tabs.get(tab_name.lower()). Meanwhile every native panel factory registers elsewhere: add_analysis_panel -> self.panels[\"analysis\"] (1475), add_script_panel -> self.panels[\"scripts\"] (1493), add_ghidra_tab -> self.embedded_tools[\"ghidra\"] (1728), add_cutter_tab -> self.embedded_tools[\"cutter\"] (1664), plus frida/process/sandbox all going int …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description. Both concrete call sites in app.py (1202-1203 and 1585-1588) confirmed to pass \"analysis\" as tab_name, which is never inserted into self.tabs.

#### M33 — restore_tab_state() cannot restore x64dbg/x32dbg tabs even though save_tab_state() records them
- **File:** `src/intellicrack/ui/tools.py:2598`
- **Severity:** MEDIUM  ·  **Category:** logic  ·  **Cluster:** tools
- **Defect:** `save_tab_state()` captures every open tab's title via `self.tab_widget.tabText(i)`. `add_x64dbg_tab()` names its tab "x64dbg" or "x32dbg" depending on `is_64bit` (line 1597). `restore_tab_state()`'s `tab_openers` dict maps 9 tab titles ("Hex Editor", "Frida", "Ghidra", "Cutter", "Process", "Sandbox", "Analysis", "Scripts", "Stack") to their opener callables but has no entry for "x64dbg" or "x32dbg" at all. `opener = tab_openers.get(tab_name)` silently returns `None` for those names and the `if opener is not None: opener()` guard skips them with no warning logged, so a previously open debugger tab is simply dropped on restore even though it was faithfully recorded in the saved state.
- **Failure:** User opens the x64dbg tab, triggers `save_tab_state()` (e.g. on workspace/project save), then later calls `restore_tab_state()` with that saved state (e.g. reopening the project). Every other previously open tab (Ghidra, Cutter, Hex Editor, etc.) reopens, but the x64dbg/x32dbg debugger tab never reappears and no error or log entry indicates why.
- **Evidence:**
  ```python
  tab_openers: dict[str, Callable[[], object]] = {
      "Hex Editor": self.add_hex_editor_tab,
      "Frida": self.add_frida_tab,
      "Ghidra": self.add_ghidra_tab,
      "Cutter": self.add_cutter_tab,
      "Process": self.add_process_tab,
      "Sandbox": self.add_sandbox_tab,
      "Analysis": self.add_analysis_panel,
      "Scripts": self.add_script_panel,
      "Stack": self.add_stack_panel,
  }
  ```
- **Verified:** Read src/intellicrack/ui/tools.py lines 2571-2624 and 1564-1610. save_tab_state() (2578-2581) records tab_names via self.tab_widget.tabText(i) verbatim. add_x64dbg_tab -> _create_x64dbg_panel sets the tab label to \"x64dbg\" or \"x32dbg\" depending on is_64bit at line 1597 (self.tab_widget.addTab(qwidget, tab_name)). restore_tab_state()'s tab_openers dict (2598-2608) contains exactly 9 keys: Hex Editor, Frida, Ghidra, Cutter, Process, Sandbox, Analysis, Scripts, Stack — no \"x64dbg\" or \"x32dbg\" entry. At line 2613 opener = tab_openers.get(tab_name) returns None for those names, and the guard `if opener is not None: opener()` at 2614-2615 silently skips them with no logging and no other co …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

### Theming / contrast / readability

#### M34 — Address-column color cached once at init, never updated on theme change
- **File:** `src/intellicrack/ui/panels/analysis_panel.py:67`
- **Severity:** MEDIUM  ·  **Category:** theming  ·  **Cluster:** chat-misc-panels
- **Defect:** self._addr_color is resolved from ThemeManager.get_instance().get_analysis_colors()["accent"] exactly once in __init__ and then applied via item.setForeground(self._addr_color) on every address cell in every table (_set_addr_item, used by _populate_strings/_populate_imports/_populate_exports/_populate_functions/_populate_sections). The panel never connects to ThemeManager.theme_changed, unlike the established fix pattern used elsewhere in the codebase for the exact same get_analysis_colors() API (e.g. hex_editor_widget.py connects theme_changed to a re-render handler at line 429). Address values are the most numerous colored cells in the panel and are the visual cue the prior audit's root-cause #2 fix (theme_changed subscription) specifically targeted; this file was missed.
- **Failure:** User loads a binary (address cells rendered with dark-theme accent QColor(0,122,204)), then switches the app theme to light in Preferences. ThemeManager emits theme_changed and the rest of the UI restyles via QSS, but every already-rendered and every subsequently-populated address cell in this panel keeps using the color captured at construction time -- re-running set_analysis() after the switch still calls self._addr_color (stale) instead of re-querying ThemeManager, so address text never reflects the active theme.
- **Evidence:**
  ```python
  self._addr_color = ThemeManager.get_instance().get_analysis_colors()["accent"]
  ```
- **Verified:** Read src/intellicrack/ui/panels/analysis_panel.py in full for relevant symbols. Line 67 in __init__: `self._addr_color = ThemeManager.get_instance().get_analysis_colors()[\"accent\"]` — resolved exactly once at construction. This value is applied at line 341 inside `_set_addr_item` via `item.setForeground(self._addr_color)`, called from `_populate_strings` (271), `_populate_imports` (288), `_populate_exports` (300), `_populate_functions` (310), and `_populate_sections` (325) — i.e. every address-column cell in the panel. A grep of the whole file for `theme_changed` returns zero matches, so the panel never subscribes to ThemeManager's theme-change notification (confirmed the signal genuinely  …
- **Note:** Claim is accurate as written; no correction needed to line, severity, or description.

#### M35 — CFG block/edge/background colors cached at construction, never refreshed on theme_changed
- **File:** `src/intellicrack/ui/panels/graph_view.py:519`
- **Severity:** MEDIUM  ·  **Category:** theming  ·  **Cluster:** ghidra-extras-graph
- **Defect:** _get_graph_colors() reads ThemeManager.get_instance().is_dark_theme() once per call, but callers only invoke it at construction time and cache the result: CFGGraphView.__init__ bakes the background brush into the viewport once (line 519), BasicBlockItem.__init__ stores self._colors = _get_graph_colors() once (line 148) and paint() re-uses that cached dict every repaint (lines 185-223), and EdgeItem.__init__ likewise resolves 'colors' once and bakes them into self.setPen/self._arrow_brush (lines 253-284). None of these classes, nor CFGGraphView itself, connect to ThemeManager.theme_changed. The file also duplicates ThemeManager.get_analysis_colors() (the canonical helper used by other already-remediated analysis views, per the prior audit's fix pattern) as a bespoke local dict instead of consuming the shared, theme-change-aware source.
- **Failure:** User opens a Cutter/Ghidra CFG tab (which builds one long-lived CFGGraphView, e.g. cutter_panel.py instantiates it once and calls load_graph() repeatedly on the same view), then switches the application theme (light->dark or dark->light) via the app's theme selector while the graph is on screen. The QGraphicsView background brush and every already-created BasicBlockItem/EdgeItem keep painting with the old theme's palette (e.g. dark block backgrounds QColor(40,44,52) on a now-light-themed background QColor(248,248,248), or vice versa) until the user navigates to a new function and load_graph() rebuilds the scene from scratch, producing a visibly inconsistent/mismatched graph view relative to the rest of the now-retheme GUI chrome.
- **Evidence:**
  ```python
  self.setBackgroundBrush(QBrush(_get_graph_colors()["background"]))
  ```
- **Verified:** Read graph_view.py in full around the relevant sites. Confirmed: (1) `_get_graph_colors()` (lines 57-93) re-derives a theme-dependent dict from `ThemeManager.get_instance().is_dark_theme()` every call, but nothing re-invokes it after construction. (2) `BasicBlockItem.__init__` (line 148) does `self._colors = _get_graph_colors()` once and `paint()` (lines 185-223) reuses `self._colors` on every repaint without re-querying the theme. (3) `EdgeItem.__init__` (lines 253-284) resolves `colors = _get_graph_colors()` once and bakes it into `self.setPen(...)` and `self._arrow_brush = QBrush(color)`; its `paint()` (from line 286) just reuses `self._pen()`/`self._arrow_brush`, never re-resolving. (4)  …

#### M36 — Stack frame colors never refresh on live theme switch
- **File:** `src/intellicrack/ui/panels/stack_viewer.py:458`
- **Severity:** MEDIUM  ·  **Category:** theming  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** `_get_stack_colors()` correctly derives colors from `ThemeManager.get_instance().is_dark_theme()`, but it is only invoked inside `StackFrameTable.set_frames()`, which only runs when new stack data arrives (manual Refresh click or the auto-refresh timer). Neither `StackFrameTable` nor `StackViewerPanel` connects to `ThemeManager.theme_changed`, unlike the established pattern used elsewhere in the codebase (e.g. hex_editor_widget.py's `_on_theme_changed` re-render hook). The row colors are painted directly via `QTableWidgetItem.setForeground(QColor(...))`, which is not governed by the app QSS and therefore is not re-applied when the global theme changes.
- **Failure:** User views the Stack Viewer in dark theme with auto-refresh off (its default state), then switches the app to light theme without touching the panel again: the already-rendered rows keep dark-theme colors such as "function_unknown"/#888888 (muted grey), which has ~2.9:1 contrast against the new white background — under WCAG AA's 4.5:1 threshold — until the user manually clicks Refresh or changes the source combo.
- **Evidence:**
  ```python
  colors = _get_stack_colors()
  ```
- **Verified:** stack_viewer.py never connects to ThemeManager.theme_changed anywhere in the file (grep for the signal only matches the import at line 34 and the is_dark_theme() call at line 61 inside _get_stack_colors, lines 55-83). _get_stack_colors() is invoked exactly once, at line 458, inside StackFrameTable.set_frames() (lines 447-500), which paints per-cell colors via QTableWidgetItem.setForeground(QColor(...)) — direct item-level styling that Qt does not re-apply on a stylesheet/theme change (unlike QSS-driven `property`-based widgets that get polish/unpolish, e.g. status_label's _restyle_status_label at line 656). set_frames() is only reached through StackViewerPanel._render_frames() (line 724), wh …

#### M37 — LIGHT_THEME_FALLBACK omits QLabel[heading]/[subheading] and several other widget rules present in DARK_THEME_FALLBACK
- **File:** `src/intellicrack/ui/resources/theme_manager.py:1059`
- **Severity:** MEDIUM  ·  **Category:** theming  ·  **Cluster:** theme-resources
- **Defect:** DARK_THEME_FALLBACK (used by ThemeManager._load_stylesheet when the bundled .qss file for the active theme is missing/unreadable, gated only by 'styles directory exists' not 'this specific file exists') defines `QLabel[heading="true"]` (12pt bold) and `QLabel[subheading="true"]` (10pt, muted color) at lines 586-594, plus complete QScrollArea (280-287), QTreeWidget/QTreeView base colors (379-397), QRadioButton base+indicator (466-486), QSlider (502-518), QSpinBox/QDoubleSpinBox base (489-499), and QFrame[frameShape] separator coloring (576-578). LIGHT_THEME_FALLBACK's equivalent `/* Label */` block (1058-1103) and rest of the sheet contain none of these selectors at all -- only the `:disabled`/`:focus` pseudo-states for some of them survive (e.g. QRadioButton:disabled at 1131, QTableWidget:focus at 1140), with no base style. Because `styles_available` is a single directory-existence check computed once at ThemeManager construction (`_check_styles_available`), a corrupted/missing `light_theme.qss` file alone (dark_theme.qss still present) silently routes light theme through this incomplete fallback while dark theme continues to render from the complete DARK_THEME_FALLBACK or its own real file, producing a visibly asymmetric, degraded light theme.
- **Failure:** If light_theme.qss is missing or unreadable on disk while the styles directory itself still exists (e.g. partial asset corruption in a frozen build), ThemeManager._load_stylesheet falls back to LIGHT_THEME_FALLBACK. Widgets that call `.setProperty("heading", "true")` -- e.g. `src/intellicrack/ui/tool_config.py:1365` (`self._name_label.setProperty("heading", "true")`) and `src/intellicrack/ui/panels/analysis_panel.py:92` (`self._binary_label.setProperty("heading", "true")`) -- lose their bold/12pt heading styling entirely under light theme (falling back to plain 9pt QWidget text), while the same property under dark theme still renders bold/12pt via DARK_THEME_FALLBACK. Tree widgets, radio buttons, sliders and scroll areas likewise lose their explicit theme coloring/border treatment only on the light side.
- **Evidence:**
  ```python
  QLabel[heading="true"] {
      font-size: 12pt;
      font-weight: bold;
  }
  
  QLabel[subheading="true"] {
      font-size: 10pt;
      color: #888888;
  }
  ... (LIGHT_THEME_FALLBACK's /* Label */ block at line 1059 has no corresponding QLabel[heading="true"] or QLabel[subheading="true"] rule anywhere in the string)
  ```
- **Verified:** Verified directly in src/intellicrack/ui/resources/theme_manager.py. DARK_THEME_FALLBACK (lines 79-700) contains QLabel[heading="true"] (586-589) and QLabel[subheading="true"] (591-594), plus base rules for QScrollArea (202), QTreeWidget/QTreeView (301), QRadioButton + indicator (388-406), QSpinBox/QDoubleSpinBox (411-419), QSlider groove/handle (424-438), and QFrame[frameShape="4"/"5"] (498). LIGHT_THEME_FALLBACK's Label block (1058-1103, exactly at the reported line 1059) has zero heading/subheading rules -- only success/error/warning/info/muted/status variants -- and a full-file grep of the light fallback (701-1183) shows only leftover pseudo-state rules (QRadioButton:disabled at 431, QSp …

### Layout / sizing / text-cutoff

#### M38 — Window's minimum floor size (800x600) is smaller than the combined minimum widths of the two splitter panes it must contain (900px)
- **File:** `src/intellicrack/ui/app.py:326`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** app-shell
- **Defect:** `_apply_smart_window_size` floors the window at `min_w, min_h = 800, 600` and will resize the window down to 800px width on any screen whose available width minus the 6px margin is below 900px. But `_setup_ui` sets `self._chat_panel.setMinimumWidth(400)` and `self.tool_panel.setMinimumWidth(500)` inside a `QSplitter` with `setChildrenCollapsible(False)`, giving the central layout an effective minimum width of 900px (plus the splitter handle). These two constants are inconsistent: on any display where the computed `target_w` lands between 800 and ~900 (e.g. a 1024x600 netbook/tablet panel, a narrow secondary monitor, or a scaled-down VM display), the app either forces the top-level window wider than the screen-fit size it just computed (defeating `_apply_smart_window_size`'s purpose and potentially pushing part of the window off-screen, since `move_widget` centers using the smaller `target_w` value while Qt's layout-enforced minimum silently produces a wider window) or squeezes the chat/tool panels below their declared minimums.
- **Failure:** On a screen with ~850px of available width, `_apply_smart_window_size` computes `target_w = max(800, min(1400, 850-6)) = 844` and centers the window assuming an 844px-wide window, but the splitter's enforced minimum of ~900px (400+500) makes the actual rendered window wider than 844px, so the window is positioned off-center relative to its real size and can extend past the visible screen edge.
- **Evidence:**
  ```python
  max_w, max_h = 1400, 900
          min_w, min_h = 800, 600
          margin_w, margin_h = 6, 8
          ...
          self._chat_panel.setMinimumWidth(400)
          ...
          self.tool_panel.setMinimumWidth(500)
  ```
- **Verified:** Read src/intellicrack/ui/app.py directly. Line 326 sets `min_w, min_h = 800, 600` as the floor used at line 336 (`target_w = max(min_w, min(max_w, avail_w - margin_w))`) and line 339 (`self.resize(target_w, target_h)`), called from `_apply_smart_window_size` at line 315 inside `__init__`. `_setup_ui` (called earlier at line 297, i.e. before the resize) builds the central widget's layout with `self._chat_panel.setMinimumWidth(400)` (line 529) and `self.tool_panel.setMinimumWidth(500)` (line 533) added to `self._splitter` with `self._splitter.setChildrenCollapsible(False)` (line 537). No code elsewhere in the file relaxes these minimums or the 800 floor, and `_restore_window_state` (lines 385- …
- **Note:** Original report is accurate as written; no correction needed to line, severity, or description. Confirmed both constants (800 floor at line 326, and 400+500=900 splitter minimums at lines 529/533) and confirmed no mitigation exists elsewhere in app.py (checked _restore_window_state, which is a no-op on first run / fresh QSettings).

#### M39 — Truncated tool-call arguments and tool-result text have no tooltip to view the full value
- **File:** `src/intellicrack/ui/chat.py:167`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** chat-misc-panels
- **Defect:** When rendering a tool call, args_text is hard-truncated to _MAX_ARGS_DISPLAY_LEN (100 chars) with an ellipsis and placed in a QLabel with only setWordWrap(True) -- no setToolTip(args_text) with the untruncated value. The same pattern repeats for tool results at _MAX_RESULT_DISPLAY_LEN (200 chars) in _create_tool_result_widget (lines 206-214). Because the label's own text is already the truncated string, wordWrap on an already-truncated string does nothing to recover the missing content, and there is no other UI affordance (tooltip, expand button, click-to-copy) to see the original argument/result value.
- **Failure:** A tool call carries a long argument such as a full file path or a large JSON payload (>100 chars); the chat bubble permanently shows "..." with no way to inspect the actual value that was sent to the bridge, and likewise a tool result exceeding 200 chars (e.g. a decompiled snippet or string list) is permanently truncated with no recovery path in the UI.
- **Evidence:**
  ```python
  args_text = ", ".join(f"{k}={v!r}" for k, v in call.arguments.items())
              if len(args_text) > _MAX_ARGS_DISPLAY_LEN:
                  args_text = f"{args_text[: _MAX_ARGS_DISPLAY_LEN - 3]}..."
  ```
- **Verified:** Read src/intellicrack/ui/chat.py lines 143-216. In _create_tool_call_widget (line 165-173), args_text is truncated to _MAX_ARGS_DISPLAY_LEN=100 chars at lines 167-168, then QLabel(args_text) is constructed at line 169 from the already-truncated string and only setWordWrap(True) is applied at line 172 -- no setToolTip. Same pattern in _create_tool_result_widget: result_text truncated to _MAX_RESULT_DISPLAY_LEN=200 at lines 208-209, QLabel(result_text) at line 210, only setWordWrap(True) at line 213, no tooltip. A grep for setToolTip across the whole file returns zero matches, confirming no tooltip/expand/copy affordance exists anywhere to recover the truncated original value. setWordWrap only …
- **Note:** Report is accurate as written; no correction needed. Also affects _create_tool_result_widget at lines 206-214 as noted in the original report.

#### M40 — Logger/Location/Event table columns default to narrow Interactive width, clipping the primary log content
- **File:** `src/intellicrack/ui/log_viewer/window.py:244`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** log-viewer
- **Defect:** The table header is configured with `Interactive` resize mode for every section, then only the Time and Level columns are overridden to `ResizeToContents`. The Logger (column 2), Function:Line (column 3), and Event (column 4) columns — which hold the most information-dense, variable-length data in a log viewer (dotted logger names like `intellicrack.ui.log_viewer._tail_reader`, and event identifiers like `log_viewer_settings_int_coerce_failed`) — are left at the default Interactive section width (Qt's default ~100 logical px). No `resizeColumnsToContents()` call is made after the model is populated, and no Stretch/ResizeToContents mode is applied to these columns; only the last column (Extras) benefits from `setStretchLastSection(True)`. As a result the Logger and Event columns start out visually truncated on every window open, showing only a few characters of the record's most important fields until the user manually drags the column border.
- **Failure:** User opens the Log Viewer for the first time (or after a fresh reload); the table shows rows where the Logger and Event columns display only a handful of characters (e.g. "intelli…", "log_vie…") because their column width defaults to ~100px, while the far-right Extras column consumes the remaining window width via stretch. The user cannot read the event name or logger without either hovering for a tooltip or manually widening each column by hand.
- **Evidence:**
  ```python
  horizontal_header.setSectionResizeMode(QHeaderView.ResizeMode.Interactive)
              horizontal_header.setStretchLastSection(True)
              horizontal_header.setSectionResizeMode(_TIME_COLUMN, QHeaderView.ResizeMode.ResizeToContents)
              horizontal_header.setSectionResizeMode(_LEVEL_COLUMN, QHeaderView.ResizeMode.ResizeToContents)
  ```
- **Verified:** Read src/intellicrack/ui/log_viewer/window.py lines 238-249: the header is set to Interactive for all sections (line 244), then only `_TIME_COLUMN` (0) and `_LEVEL_COLUMN` (1) are overridden to ResizeToContents (lines 246-247), and `setStretchLastSection(True)` (line 245) only benefits the last column, Extras (index 5, per `_model.py` `_COLUMN_EXTRAS = 5`). Cross-referenced `_model.py` lines 34-43: columns are Time(0)/Level(1)/Logger(2)/Function:Line(3)/Event(4)/Extras(5), i.e. exactly the Logger, Function:Line and Event columns cited in the claim are left at Interactive default section size with no ResizeToContents/Stretch and no `resizeColumnsToContents()` call anywhere in window.py (grep  …
- **Note:** Line pointer corrected from 242 (the `horizontal_header = self.horizontalHeader()` assignment) to 244, the line that actually sets Interactive mode; the substantive claim (columns 2/3/4 left at narrow Interactive default width while only 0/1 get ResizeToContents and only the last column stretches) is accurate. Note the model does supply ToolTipRole text identical to the display value, so hovering does reveal full content — worth mentioning as partial mitigation, but it does not prevent the default visual clipping described.</notes>
</invoke>

#### M41 — Header summary labels (format/sources) show unbounded, variable-length text without word wrap
- **File:** `src/intellicrack/ui/panels/analysis_panel.py:243`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** chat-misc-panels
- **Defect:** self._bridges_label is set to f"Sources: {', '.join(analysis.source_bridges)}" -- a comma-joined list whose length grows with the number of connected bridges (ghidra, cutter, frida, x64dbg, sandbox, ...) -- and self._format_label is set to f"Format: {analysis.format_info}" where format_info is a free-form detected-format description (e.g. a PE/ELF/Mach-O descriptor string) that can be long. Neither QLabel (nor _binary_label) has setWordWrap(True) called anywhere in the file, and the grid layout gives them no stretch/elide handling, so on a narrow panel width the text either overflows the panel bounds or forces the panel wider than intended.
- **Failure:** Analysis aggregates data from all five bridges (ghidra, cutter, frida, x64dbg, sandbox) producing "Sources: ghidra, cutter, frida, x64dbg, sandbox" in a single unwrapped QLabel inside a docked/narrow panel; the text is clipped by the panel's actual width instead of wrapping to a second line, and there is no tooltip to recover the full list.
- **Evidence:**
  ```python
  self._bridges_label.setText(f"Sources: {', '.join(analysis.source_bridges)}")
  ```
- **Verified:** Verified in src/intellicrack/ui/panels/analysis_panel.py: _bridges_label (line 103) and _format_label (line 95) are plain QLabel widgets added to a QGridLayout (lines 93-104) with no setWordWrap, setToolTip, setMinimumWidth/setMaximumWidth, or elide handling anywhere in the file (grep for these confirms zero matches). At line 241 `self._format_label.setText(f"Format: {analysis.format_info}")` and at line 243 `self._bridges_label.setText(f"Sources: {', '.join(analysis.source_bridges)}")` set genuinely variable-length text: format_info is a free-form detected-format description and source_bridges can list up to 5 items (ghidra, cutter, frida, x64dbg, sandbox) joined by commas. Since neither la …
- **Note:** Report is accurate as written. Both _format_label (line 241) and _bridges_label (line 243) share the same defect; line 243 (the explicitly cited code_evidence line) is an appropriate anchor. No mitigation found anywhere in the file.

#### M42 — Function tree has no header resize mode; last (least useful) column stretches instead of the variable-length Name column
- **File:** `src/intellicrack/ui/panels/cutter_panel.py:245`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** cutter-core
- **Defect:** `self._func_tree` is a QTreeWidget with columns ["Name", "Address", "Size"] (`_FUNC_COLUMNS`, line 95). Unlike every QTableWidget in this panel (strings/imports/exports/sections, all given `setSectionResizeMode(QHeaderView.ResizeMode.Stretch)`), no resize mode or `resizeColumnToContents` call is ever applied to `_func_tree`. QTreeView/QTreeWidget headers default `stretchLastSection` to True, so the last column ("Size", which only ever needs 2-4 digits) consumes all leftover width while "Name" — which holds real function/symbol names, frequently long (demangled C++/Rust symbols, compiler-generated thunks) — is stuck at the small Interactive default section width and gets elided.
- **Failure:** Loading a binary with C++ or Rust exports/functions (e.g. `std::__1::basic_string<char, std::char_traits<char>, std::allocator<char>>::operator[]`) populates the functions sidebar; the Name column stays at its narrow default width while the near-empty Size column expands to fill the pane, so most function names are ellipsized and the user must manually drag the header to read them, with no tooltip provided as a fallback.
- **Evidence:**
  ```python
  self._func_tree = QTreeWidget()
  set_header_labels(self._func_tree, _FUNC_COLUMNS)
  set_sorting_enabled(self._func_tree, enable=True)
  set_selection_mode(self._func_tree, QAbstractItemView.SelectionMode.SingleSelection)
  ```
- **Verified:** Confirmed by direct code reading. `self._func_tree = QTreeWidget()` is created at cutter_panel.py:245 with `_FUNC_COLUMNS = ["Name", "Address", "Size"]` (line 95) applied via `set_header_labels(self._func_tree, _FUNC_COLUMNS)` (line 246). Unlike the QTableWidgets in the same file (strings/imports/exports/sections at lines 317, 327, 336, 345), which each get `setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` explicitly, grepping the whole file shows zero calls to `setSectionResizeMode`, `resizeColumnToContents`, `setColumnWidth`, or `header().setStretchLastSection(False)` anywhere involving `_func_tree`. The population code at `_apply_functions` (lines 643-666) builds `NumericSortTreeItem …
- **Note:** Report is accurate as written; no corrections needed to line, severity, or description.

#### M43 — CallGraphTab stretches Caller/Callee/Address equally, squeezing long function names
- **File:** `src/intellicrack/ui/panels/cutter_static_extra_tab.py:293`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** cutter-tabs
- **Defect:** `CallGraphTab` uses `_make_table(["Caller", "Callee", "Address"])`, which applies uniform `Stretch` resize mode to all 3 columns via `_stretch_headers`. `Caller` and `Callee` are populated from rizin call-graph symbol names (`edge_dict.get("name", "")` and each entry of the `imports` list in `_apply_data`), which for stripped-but-symboled or C++ binaries are frequently long mangled/demangled names, while `Address` only ever needs to display a short fixed-width hex string (`0x{...:X}`). Giving all three columns equal share wastes width on the short Address column while clipping the two name columns that most need it.
- **Failure:** The call graph for a C++ binary includes edges like caller `_ZN4Acme9Licensing17ValidationEngine18verifyLicenseKeyEPKcS3_` calling callee `_ZN4Acme6Crypto11AesDecryptorC1Ev`; both names are squeezed into roughly the same width as the 10-character Address column and get clipped, making the call graph unreadable without manually widening columns (which Stretch mode does not allow the user to do persistently, since it recomputes on every resize).
- **Evidence:**
  ```python
  self._table = _make_table(["Caller", "Callee", "Address"])
  ```
- **Verified:** Read cutter_static_extra_tab.py lines 78-103 and 281-334. `_make_table` (line 89-103) always calls `_stretch_headers` (line 78-86), which does `header.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` — applied uniformly to every column with no per-column override anywhere in this file (grep for ResizeToContents/resizeColumnsToContents/setToolTip/per-section setSectionResizeMode found no matches). `CallGraphTab.__init__` (line 293) builds its table via `_make_table(["Caller", "Callee", "Address"])`, so all 3 columns get equal Stretch treatment. `_apply_data` (lines 311-334) populates Caller from `edge_dict.get("name", "")` (rizin function/symbol names, which can be long/mangled for C++ o …
- **Note:** Report's code_evidence and description are accurate as written; no changes needed to title, description, or failure scenario.

#### M44 — Frida panel splitters are collapsible to zero, unlike sibling panels
- **File:** `src/intellicrack/ui/panels/frida_panel.py:176`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** frida-panel
- **Defect:** Neither `main_splitter` (Vertical: top area / console) nor `top_splitter` (Horizontal: process browser / script editor / hooks-tabs) calls `setChildrenCollapsible(False)`, and none of the panes they host receive a `setMinimumWidth`/`setMinimumHeight` floor. QSplitter defaults to `childrenCollapsible=True`, so any handle can be dragged fully to one side, collapsing that pane to 0px. This is the exact defect class the prior GUI audit fixed in sibling panels (`ghidra_panel.py:195/198` calls `main_splitter.setChildrenCollapsible(False)` / `left_splitter.setChildrenCollapsible(False)`; `cutter_panel.py:178/203` does the same), but the fix was never applied to `frida_panel.py`.
- **Failure:** A user dragging the top_splitter handle between the process browser and the script editor (or between the editor and the Hooks/Threads/Stalker/Modules/Memory/Symbols/Advanced tab widget) past the edge collapses that pane to zero width with no visible handle remnant to grab, effectively hiding the process list, the script editor, or the entire right-hand tool tab set until the user resizes the window or otherwise finds the invisible boundary to drag back.
- **Evidence:**
  ```python
  main_splitter = QSplitter(Qt.Orientation.Vertical)
  
  top_splitter = QSplitter(Qt.Orientation.Horizontal)
  top_splitter.addWidget(self._create_process_browser())
  top_splitter.addWidget(self._create_editor_section())
  top_splitter.addWidget(self._create_right_tabs())
  top_splitter.setSizes(_TOP_SPLIT)
  main_splitter.addWidget(top_splitter)
  ```
- **Verified:** Read frida_panel.py lines 160-204 and grepped the whole file for "splitter|Collapsible|setMinimum": `main_splitter` (line 176, Vertical) and `top_splitter` (line 178, Horizontal) are both plain `QSplitter(...)` constructions with no `setChildrenCollapsible(False)` call anywhere in the file, and none of the three widgets added to `top_splitter` (process browser, editor section, right tabs at lines 179-181) or the two added to `main_splitter` (top_splitter, console_container at lines 183/199) receive `setMinimumWidth`/`setMinimumHeight`. PyQt6 QSplitter defaults `childrenCollapsible` to True, so dragging a handle to the edge collapses that pane to 0px with the handle flush against the containe …
- **Note:** No correction needed; claim is accurate as reported. Not a regression of a prior fix to this specific file — it's a fix pattern applied to sibling panels (ghidra_panel.py, cutter_panel.py) that was never applied to frida_panel.py.

#### M45 — Load-module result QLabel shows unbounded exception text with no word wrap or tooltip
- **File:** `src/intellicrack/ui/panels/frida_panel.py:1760`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** frida-panel
- **Defect:** `self._load_module_result` is a bare `QLabel("")` placed in a `QHBoxLayout` (`load_row`) after the path `QLineEdit` and the `Load Module` button, with no `addStretch()` after it. QLabel does not word-wrap by default, and none of the setText call sites for this label bound the text length except the two static fixed strings; `_on_load_module_error` sets it to `f"Load failed: {exc}"`, where `exc` is an arbitrary bridge/Frida exception whose `str()` can be long (stack traces, native module load errors, path-embedded messages). No `setWordWrap(True)` or `setToolTip(...)` is ever applied to this label.
- **Failure:** Attempting to load a module from an invalid or permission-denied path yields a bridge exception whose string representation is tens to hundreds of characters (e.g. a Windows `OSError`/`WinError` message embedding the full attempted path); the label either forces the Modules tab section of `top_splitter` to grow far past its allotted ~300px share (squeezing the process browser and script editor) or gets silently clipped with no way to read the full error, since there is neither wrapping nor a tooltip fallback.
- **Evidence:**
  ```python
  self._load_module_result = QLabel("")
  ...
  self._load_module_result.setText(f"Load failed: {exc}")
  ```
- **Verified:** Verified in src/intellicrack/ui/panels/frida_panel.py: `_create_modules_section` (def at line 1594) builds `load_row = QHBoxLayout()` (1657) containing a path QLineEdit (1659-1661), a Load Module button (1662-1665), and `self._load_module_result = QLabel("")` (1666) added last with no `addStretch()` anywhere in `load_row`. A grep for `setWordWrap` across the entire file returns zero matches, and a grep for `_load_module_result` shows only six references, none of which call `setWordWrap` or `setToolTip`. The error path is real: `_on_load_module_error` (1753-1761) is wired as the `on_error` callback for `self._bridge.load_module(path_str)` via `run_bridge_coroutine_logged` (1729-1738), and it  …
- **Note:** Claim is accurate as written; no correction needed to line, severity, or description.

#### M46 — Labels/Bookmarks splitter is collapsible, unlike every other splitter in the panel  **[REGRESSION]**
- **File:** `src/intellicrack/ui/panels/ghidra_panel.py:375`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** ghidra-panel
- **Defect:** The two splitters built in `_create_content` (`main_splitter`, `left_splitter`) both call `setChildrenCollapsible(False)` -- added specifically in the prior GUI-audit remediation to stop panes collapsing to zero. The vertical splitter created here for the Labels/Bookmarks tab does not get the same call, so it keeps Qt's default `childrenCollapsible = True`. Dragging its handle to either extreme fully collapses the Labels section (address/name/primary form + labels table + refresh/remove buttons) or the Bookmarks section (category/comment/type form + bookmarks table + refresh/remove buttons) to zero height, hiding all of its controls with no visual affordance to restore them beyond blindly dragging the now-invisible handle.
- **Failure:** User drags the splitter handle in the Labels/Bookmarks tab toward the top; the entire Labels section (address input, name input, Primary checkbox, Set Label button, labels table, Refresh/Remove buttons) collapses to a 0px strip and disappears, leaving the user unable to find or restore it without knowing to hunt for a near-invisible splitter handle.
- **Evidence:**
  ```python
  splitter = QSplitter(Qt.Orientation.Vertical)
  
          labels_widget = QWidget()
  ```
- **Verified:** Confirmed by reading src/intellicrack/ui/panels/ghidra_panel.py. In `_create_content` (lines 188-198), `main_splitter` (line 194-195) and `left_splitter` (line 197-198) both explicitly call `.setChildrenCollapsible(False)` immediately after construction. In `_create_labels_bookmarks_tab` (lines 369-454), the vertical `splitter` is constructed at line 375 and has `labels_widget` added at line 411 and `bm_widget` added at line 452, but no `setChildrenCollapsible(False)` call exists anywhere in this method (verified via grep across the whole file: only 2 occurrences, at lines 195 and 198). There is also no `setMinimumHeight`/`setMinimumSize` call on `labels_widget` or `bm_widget` (grep returned …
- **Note:** Report is accurate as written; no correction needed to line/severity. Marking is_regression=true because the codebase already established the pattern (setChildrenCollapsible(False) on all splitters) in a prior GUI-audit remediation per the finding's own description, and this splitter was simply missed / not brought into line with that pattern.

#### M47 — Structure-fields summary label has no word wrap for an unbounded field list
- **File:** `src/intellicrack/ui/panels/ghidra_panel.py:497`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** ghidra-panel
- **Defect:** `_struct_fields_label` is a plain `QLabel("")` inside a `QVBoxLayout` with no `setWordWrap(True)` and no tooltip. `_on_add_struct_field` appends to `self._struct_fields_list` on every 'Add Field' click and rewrites the label with `f"Fields: {fields_str}"`, where `fields_str` is a comma-joined `name:type` list with no length cap -- e.g. defining a realistic struct like `IMAGE_NT_HEADERS` with 15-20 fields produces a long single-line string. Without word wrap, QLabel does not wrap; the text is clipped to whatever width the vertical layout allots (bounded by the left splitter / tab width), and there is no tooltip fallback to read the full list.
- **Failure:** User builds a structure with many fields via repeated 'Add Field' clicks (e.g. 12 fields such as `magic:uint32`, `machine:uint16`, ...); the resulting `Fields: ...` label text is far wider than the Structures tab column and gets truncated by the container with no way to read the remaining fields short of resizing the whole panel.
- **Evidence:**
  ```python
  self._struct_fields_list: list[tuple[str, str]] = []
          self._struct_fields_label = QLabel("")
          layout.addWidget(self._struct_fields_label)
  ```
- **Verified:** Read src/intellicrack/ui/panels/ghidra_panel.py lines 460-504 and 2979-3009, plus a whole-file grep for setWordWrap/setToolTip/QScrollArea (zero matches anywhere in the file). Confirmed: line 497 creates `self._struct_fields_label = QLabel("")` added directly to a QVBoxLayout with no setWordWrap(True) call and no tooltip anywhere in the file. `_on_add_struct_field` (line 2979-2989) appends each user-supplied (name, type) pair to `self._struct_fields_list` with no cap and rebuilds the label text via `fields_str = ", ".join(f"{n}:{t}" for n, t in self._struct_fields_list)` then `self._struct_fields_label.setText(f"Fields: {fields_str}")` — a single unbounded line that grows without limit as fi …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M48 — External references table has no column resize policy, clips variable-length symbol/library names
- **File:** `src/intellicrack/ui/panels/ghidra_panel_extras.py:446`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** ghidra-extras-graph
- **Defect:** _ext_refs_table is built as a raw QTableWidget with setSelectionBehavior/setEditTriggers but no header.setSectionResizeMode(...) or resizeColumnsToContents() call anywhere in the file. This is a direct deviation from the sibling convention in ghidra_panel.py's _make_table() helper (same QTableWidget/selection/edit-trigger setup), which explicitly does `header.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)`. Rows are populated in _apply_external_refs with QTableWidgetItem(str(ref.get('external_name',''))) and QTableWidgetItem(str(ref.get('library',''))), both of which are arbitrary, potentially long (mangled C++ symbol names, long DLL/library paths) user- or binary-derived strings. Without any resize mode, QHeaderView defaults to Interactive with a small default section width, so long External Name/Library values are visually truncated in the fixed 100px-tall table.
- **Failure:** User refreshes external references at an address that resolves to a long imported symbol (e.g. a C++-mangled export like '?_Xlength_error@std@@YAXPEBD@Z' or a long library path); the 'External Name' and 'Library' columns render at their narrow default width and the text is clipped/ellipsized with no way to see the full value short of manually dragging the column divider, since no tooltip or resize-to-content call is ever made.
- **Evidence:**
  ```python
  self._ext_refs_table = QTableWidget(0, len(_EXT_REF_COLUMNS))
          self._ext_refs_table.setHorizontalHeaderLabels(_EXT_REF_COLUMNS)
          self._ext_refs_table.setSelectionBehavior(QTableWidget.SelectionBehavior.SelectRows)
          self._ext_refs_table.setEditTriggers(QTableWidget.EditTrigger.NoEditTriggers)
          self._ext_refs_table.setFixedHeight(100)
  ```
- **Verified:** Read ghidra_panel_extras.py lines 435-490 and grepped the whole file for "header"/"Header" and "resize". The only header-related calls in the file are setHorizontalHeaderLabels (lines 447, 577) and a qt_compat set_header_labels call for a tree widget (line 657) — none of these set a QHeaderView.SectionResizeMode, call resizeColumnsToContents(), or setStretchLastSection(). The _ext_refs_table (line 446) is constructed with only setSelectionBehavior/setEditTriggers/setFixedHeight(100) and no resize policy. _apply_external_refs (lines 473-489) populates column 1 with str(ref.get("external_name","")) and column 2 with str(ref.get("library","")) — both arbitrary/variable-length strings sourced fr …

#### M49 — Properties table has no column resize policy, clips variable-length property values
- **File:** `src/intellicrack/ui/panels/ghidra_panel_extras.py:576`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** ghidra-extras-graph
- **Defect:** _properties_table is built the same way as _ext_refs_table (QTableWidget + setSelectionBehavior + setEditTriggers) with no setSectionResizeMode/resizeColumnsToContents call, again diverging from ghidra_panel.py's _make_table() helper which applies QHeaderView.ResizeMode.Stretch to every table it builds. _apply_properties populates the 'Value' column with str(value) for arbitrary Ghidra user-defined properties, which can be long strings (free-form metadata). With the default narrow Interactive column width, long property values are clipped in the fixed 90px-tall table with no tooltip or resize fallback.
- **Failure:** User queries properties at an address carrying a long user-defined property value (e.g. an analyst-entered note or a long serialized string); the 'Value' column truncates the text at the default section width and there is no way to read the full value without manually widening the column.
- **Evidence:**
  ```python
  self._properties_table = QTableWidget(0, len(_PROPERTY_COLUMNS))
          self._properties_table.setHorizontalHeaderLabels(_PROPERTY_COLUMNS)
          self._properties_table.setSelectionBehavior(QTableWidget.SelectionBehavior.SelectRows)
          self._properties_table.setEditTriggers(QTableWidget.EditTrigger.NoEditTriggers)
          self._properties_table.setFixedHeight(90)
  ```
- **Verified:** Read ghidra_panel_extras.py lines 576-620. The `_properties_table` is constructed as a raw QTableWidget with only setSelectionBehavior/setEditTriggers/setFixedHeight(90) — no setSectionResizeMode, no resizeColumnsToContents, no setToolTip, no setWordWrap anywhere in the file (grep for those confirms zero hits for this table). `_apply_properties` (lines 616-620) populates column 1 ("Value") with `str(value)` for arbitrary user-defined Ghidra properties, which are free-form and can be long strings. Comparing to the sibling module ghidra_panel.py, its `_make_table()` helper (line 116) explicitly calls `header.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` (line 132) for every one of its  …
- **Note:** Report is accurate as written; no correction needed.

#### M50 — Disassembly table 'Hex Bytes' column clips long instruction byte sequences
- **File:** `src/intellicrack/ui/panels/hex_editor/disassembly.py:129`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-misc
- **Defect:** h_header.setStretchLastSection(True) only stretches the final 'Operands' column; the 'Hex Bytes' column (index 1, populated at line 267 with a space-joined uppercase hex dump built at line 263) keeps the default ~100px Interactive width. x86 instructions can legally be up to 15 bytes, rendering as up to 44 characters ('AA BB CC ... 99') in a 9pt Consolas font -- far wider than the default column, so longer instructions' byte dumps are clipped even though instruction length is inherently variable.
- **Failure:** User disassembles a region containing long-encoded x86 instructions (e.g. AVX/EVEX-prefixed instructions or instructions with large immediates/SIB+disp32), and the Hex Bytes column shows only the first few byte pairs before being cut off, hiding the true instruction length from the analyst.
- **Evidence:**
  ```python
  h_header = self._disasm_table.horizontalHeader()
  if h_header is not None:
      h_header.setStretchLastSection(True)
  ```
- **Verified:** Confirmed against the actual code. disassembly.py:118-119 creates the 4-column table with header labels ["Address", "Hex Bytes", "Mnemonic", "Operands"], making "Hex Bytes" column index 1. Line 127-129 calls h_header.setStretchLastSection(True), which per Qt semantics only stretches the final section (index 3, "Operands"); a whole-file grep for setSectionResizeMode/setColumnWidth/resizeColumnsToContents/ResizeToContents returned no matches, so column 1 remains at QHeaderView's default Interactive width (~100px). Line 263 builds hex_str via space-joined uppercase hex pairs, and line 267 places it into column 1's QTableWidgetItem — exactly as described. base.py:149 defines MAX_INSN_BYTES = 15, …
- **Note:** Line and severity as reported are accurate; no correction needed. Root cause is the omission of setSectionResizeMode(1, ResizeToContents) or a tooltip fallback for column 1, not merely the setStretchLastSection call itself, but line 129 is a reasonable anchor since it's where header resize behavior is configured (or rather, under-configured) for the table.

#### M51 — Toolbar encoding combo box is fixed at 120px while codec descriptions run up to ~49 characters
- **File:** `src/intellicrack/ui/panels/hex_editor/panel.py:313`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-panel-search
- **Defect:** `_encoding_combo.setFixedWidth(_ENCODING_COMBO_WIDTH)` fixes the combo at 120 logical px. `_populate_toolbar_encoding_combo` fills it from `hexcore.HexDocument.list_encodings()`, whose descriptions (verified in `src/intellicrack-hexcore/src/encodings.rs`) include entries such as "ISO-8859-16 (Latin-10, South-Eastern European)" (48 chars), "GB18030 (Chinese National Standard)" (36 chars), and "Windows-1252 (Western European)" (32 chars). A 120px-wide combo box at typical UI font sizes fits roughly 15-18 characters, so most of these descriptions are truncated in the closed combo box with no `setSizeAdjustPolicy(AdjustToContents)`, no wider popup-view width, and no tooltip set on the combo to reveal the full description.
- **Failure:** User opens the toolbar encoding dropdown to pick a codec for the ASCII column and selects "ISO-8859-16 (Latin-10, South-Eastern European)"; the closed combo box then displays only a truncated fragment (e.g. "ISO-8859-16 (Lati") with no way to confirm which codec is actually selected without reopening the dropdown.
- **Evidence:**
  ```python
  self._encoding_combo = QComboBox()
  self._encoding_combo.setFixedWidth(_ENCODING_COMBO_WIDTH)
  self._populate_toolbar_encoding_combo(self._encoding_combo)
  ```
- **Verified:** Confirmed via direct code reading. panel.py:312-315 creates `self._encoding_combo = QComboBox()`, calls `setFixedWidth(_ENCODING_COMBO_WIDTH)` where `_ENCODING_COMBO_WIDTH: Final[int] = 120` (line 97), then populates it via `_populate_toolbar_encoding_combo` (lines 1088-1110), which does `combo.addItem(description, userData=name)` for every `(name, description)` pair returned by `hexcore.HexDocument.list_encodings()`. Verified in src/intellicrack-hexcore/src/encodings.rs that the description strings genuinely include long human-readable labels such as "ISO-8859-16 (Latin-10, South-Eastern European)" (line 343), "GB18030 (Chinese National Standard)" (line 359), and "Windows-1252 (Western Euro …
- **Note:** Report is accurate as written. No mitigation (tooltip, AdjustToContents, wider popup view) exists anywhere in panel.py for this specific combo, in contrast to sibling combos in the same method which do set tooltips. Severity MEDIUM is appropriate: it's a usability/discoverability issue (can't confirm selected codec at a glance) rather than a crash or data-loss bug.</notes>
</invoke>

#### M52 — Shared tree factory never configures column resize modes, clipping variable-length non-last columns
- **File:** `src/intellicrack/ui/panels/hex_editor/panel.py:709`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-panel-search
- **Defect:** `_make_tree` is the single shared factory used to build every side-panel `QTreeWidget` in the hex editor (Sections, Imports, Exports, Strings, Statistics, Templates, Patches, Bookmarks, Inspector). It sets `setRootIsDecorated(False)` and `setAlternatingRowColors(True)` but never calls `setSectionResizeMode`, `resizeColumnToContents`, or sets any tooltip on the header/items. A repo-wide search of `src/intellicrack/ui/panels/hex_editor/` for `resizeColumnToContents`/`setSectionResizeMode` returns zero matches, confirming no downstream populate method configures resize behavior either. Because only the *last* header section auto-stretches by Qt's default `QHeaderView` behavior, non-last columns holding genuinely variable-length content are left at the default Interactive section width: the Imports tree's middle "Function" column (line 452, import function names, which can be long WinAPI or C++ names) and the Exports tree's first "Name" column (line 455, export names, which can include long/mangled C++ symbols) will be clipped at the default column width with no tooltip to reveal the full text.
- **Failure:** A loaded PE/DLL exports or imports functions with long names (e.g. mangled C++ symbols or verbose WinAPI names); the Imports/Exports tab's "Function"/"Name" columns truncate the text at the default ~100px section width and there is no tooltip or resize-to-contents to let the user read the full name without manually dragging the column divider.
- **Evidence:**
  ```python
  tree = QTreeWidget()
  tree.setHeaderLabels(headers)
  tree.setRootIsDecorated(False)
  tree.setAlternatingRowColors(True)
  return tree
  ```
- **Verified:** panel.py lines 699-713 confirm `_make_tree` only calls setHeaderLabels/setRootIsDecorated(False)/setAlternatingRowColors(True) — no setSectionResizeMode, resizeColumnToContents, or per-item tooltip. Grep across the whole hex_editor package (panel.py + sections.py) shows zero hits for resizeColumnToContents/setSectionResizeMode/setToolTip on these trees. sections.py:219-240 (`_on_pe_imports_ready`) builds QTreeWidgetItem([dll_name, function_name, address_text]) and adds it directly to `_imports_tree` (headers ["Library","Function","Address"], line 452) with no post-processing; sections.py:277-298 (`_on_pe_exports_ready`) does the same for `_exports_tree` (headers ["Name","Address","Ordinal"], …
- **Note:** Confirmed as reported. Manual column-divider dragging is still possible (Interactive mode is not fixed-width), which keeps this at MEDIUM rather than a hard usability blocker, but there is no auto-resize, stretch, or tooltip fallback so long import/export names are clipped by default with no way to discover the full text short of manual resize.

#### M53 — Process-memory regions table has no column resize mode and clips long Base Address / Size text
- **File:** `src/intellicrack/ui/panels/hex_editor/process_memory.py:117`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-misc
- **Defect:** self._regions_table is a QTableWidget (Base Address, Size, Protection, State) created without any setSectionResizeMode/resizeColumnsToContents/setStretchLastSection call anywhere in the file. QTableWidget/QTableView does not default stretchLastSection to true the way QTreeView does, so all four columns keep Qt's default ~100px Interactive section width. _populate_regions (line 396-399) fills Base Address with 'f"0x{base:016X}"' (18 characters) and Size with 'f"0x{size:X} ({size})"' (e.g. '0x7FFFFFFFFFFF (140737488355327)', 30+ characters) -- both well beyond what a default 100px column can show, so cell text is clipped/elided even though process memory addresses and sizes are exactly the kind of long, variable content this dialog exists to display.
- **Failure:** User opens Process Memory, enters a PID, and clicks List Regions; every row's Base Address (16 hex digits) and Size ('0xHEX (decimal)') columns show truncated/elided text at the dialog's default width, forcing the user to manually widen every column before they can read a full address or size.
- **Evidence:**
  ```python
  self._regions_table = QTableWidget(0, _TABLE_COLUMNS)
  self._regions_table.setHorizontalHeaderLabels([
      "Base Address",
      "Size",
      "Protection",
      "State",
  ])
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/process_memory.py lines 93-134 and grepped the whole file for resize-mode calls. The table is constructed at line 117 (`self._regions_table = QTableWidget(0, _TABLE_COLUMNS)`) and configured only with `setSelectionBehavior` and `setEditTriggers` (lines 124-125) — there is no `setSectionResizeMode`, `resizeColumnsToContents`, or `setStretchLastSection` call anywhere in the file. `_populate_regions` (lines 388-399) fills Base Address with `f"0x{base:016X}"` (always 18 chars) and Size with `f"0x{size:X} ({size})"` (can exceed 30 chars for large addresses/sizes), Protection/State with shorter `0xHEX` strings. QTableWidget's QHeaderView defaults to Inter …
- **Note:** Report is accurate as written; no mitigation found elsewhere in the file.

#### M54 — Signature database filename label has no word-wrap or tooltip and can overflow
- **File:** `src/intellicrack/ui/panels/hex_editor/signatures.py:544`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-transforms
- **Defect:** `_sig_db_path_label` is created as `QLabel("(none)")` without `setWordWrap(True)` and no tooltip is ever attached. After the user picks a database via the file dialog, `_on_select_sig_db` sets its text to `Path(db_path).name` (line 572-573) -- an arbitrary, user-controlled filename (signature databases are frequently named descriptively, e.g. malware-family or vendor DIE/ClamAV/YARA rule set names). QLabel does not wrap or elide by default, so a long filename either forces the enclosing QHBoxLayout/side-panel tab to grow or gets visually clipped at the panel edge, and there is no tooltip to recover the full name.
- **Failure:** User selects a signature database file with a long descriptive filename (e.g. "win32_trojan_generic_backdoor_signature_database_2026.ndb"); the label either overflows past the narrow hex-editor side-panel tab width (clipped, unreadable end of the name) or forces the side panel wider, and there is no way to hover for the full path/name since no tooltip is set.
- **Evidence:**
  ```python
  self._sig_db_path_label = QLabel("(none)")
  ```
- **Verified:** Read signatures.py lines 528-573. self._sig_db_path_label = QLabel("(none)") at line 544 is added to db_row (a QHBoxLayout) alongside a "Select Database..." button, inside the side-panel tab built by _create_signatures_tab. In _on_select_sig_db (lines 563-573), after the user picks a file via QFileDialog.getOpenFileName, the label's text is set to Path(db_path).name — an arbitrary, user-controlled filename with no length constraint. A file-scoped search for setWordWrap, setToolTip, elide, setMaximumWidth/setMinimumWidth found zero matches anywhere in the file, confirming no mitigation exists. Since QLabel does not wrap or elide by default in Qt6/PyQt6, and this sits in a narrow hex-editor si …
- **Note:** Report is accurate as written; no correction needed to line/severity/description.

#### M55 — CustomCrcDialog result label lacks word-wrap for variable-length exception text
- **File:** `src/intellicrack/ui/panels/hex_editor/widgets.py:451`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-widget-core
- **Defect:** self._result_label is a plain QLabel with no setWordWrap(True) and no tooltip, but it is repeatedly set to arbitrary-length, variable text via f"Error: {exc}" (raw Python exception str(), which can include OS error messages, file paths, or int()-parse failure text of unbounded length) at lines 491 and 539, plus a fixed 'Error: worker returned non-integer result' string at line 524. The dialog only has setMinimumWidth(360) with no maximum width, so a long exception message forces the dialog to grow far wider than its designed 360px minimum instead of wrapping, and if the user or window manager constrains the dialog width the text is clipped since wrapping is never enabled.
- **Failure:** User enters CRC parameters that raise a ValueError with a long message (e.g. pasting non-hex text into the Polynomial field), or the streaming CRC read raises an OSError with a full path in its message; _result_label.setText(f"Error: {exc}") renders the unwrapped string, forcing the 360px-wide dialog to expand horizontally to fit the full message on one line, or clipping it if the dialog cannot grow further.
- **Evidence:**
  ```python
  self._result_label = QLabel("Result: —")
  layout.addWidget(self._result_label)
  ...
  self._result_label.setText(f"Error: {exc}")
  ```
- **Verified:** Read widgets.py lines 420-544. Line 451 creates `self._result_label = QLabel("Result: —")` with no `setWordWrap(True)` call anywhere in the file (grep for setWordWrap/setMaximumWidth/setToolTip across the whole file returns no matches). The dialog only calls `self.setMinimumWidth(360)` at line 421, with no maximum width constraint. The label's text is subsequently set to variable-length, unbounded strings: `f"Error: {exc}"` at line 491 (ValueError from `int(self._poly_edit.text().strip(), 16)` etc. in `_read_crc_inputs`, which embeds the raw user-entered text into the message and the QLineEdits at lines 432/435/446 have no setMaxLength), a fixed string at line 524, `f"Result: 0x{result:0{hex …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M56 — YARA results tree 'Rule' column has no resize mode and clips long rule names
- **File:** `src/intellicrack/ui/panels/hex_editor/yara.py:127`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** hex-misc
- **Defect:** self._yara_results_tree.setHeaderLabels(["Rule", "Offset", "Identifier", "Match Data"]) creates a QTreeWidget with no per-column resize mode. QTreeView defaults to stretching only the last section ('Match Data' here), leaving 'Rule' (populated at line 284-285 with the raw, arbitrary-length rule name from the compiled YARA source) at the default Interactive width. Real-world YARA rule identifiers are frequently long, descriptive strings, so this column truncates exactly the field an analyst needs to read first when triaging matches.
- **Failure:** User scans with a YARA ruleset containing descriptively-named rules (e.g. 'Suspicious_PE_Packer_UPX_Modified_Header_Anomaly'); the top-level 'Rule' row shows only the first handful of characters before clipping, and the tree gives no tooltip to recover the full name without widening the column.
- **Evidence:**
  ```python
  self._yara_results_tree.setHeaderLabels(["Rule", "Offset", "Identifier", "Match Data"])
  ```
- **Verified:** Read src/intellicrack/ui/panels/hex_editor/yara.py lines 94-132 and 263-297. Line 126-127 creates `self._yara_results_tree = QTreeWidget()` then `setHeaderLabels(["Rule", "Offset", "Identifier", "Match Data"])` with no follow-up call to `setSectionResizeMode`, `resizeColumnToContents`, or any header configuration — a grep across the whole file for `setSectionResizeMode|resizeColumnToContents|setStretchLastSection|header()` found zero matches. Rule names are populated at line 284-285 directly from the compiled YARA rule's identifier (`rule_name = str(match.get("rule", "")); rule_item = QTreeWidgetItem([rule_name, "", "", ""])`), which is arbitrary-length, user/rule-author controlled text — no …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M57 — COM server table's Loaded Path column is not stretched and does not auto-expand
- **File:** `src/intellicrack/ui/panels/process_panel/modules_tab.py:288`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** process-tabs
- **Defect:** _com_table sets Stretch only on column 1 ('DLL Path'); column 2 ('Loaded Path', the last column) is left at the default Interactive resize mode. QTableWidget's stretchLastSection defaults to False (verified empirically in this environment), so the last column does NOT auto-fill remaining space the way it would in a QTreeWidget. 'Loaded Path' holds the actual on-disk load path of a COM server DLL, a genuinely long/variable string, and has no tooltip set.
- **Failure:** User runs 'Inspect COM' on a process with COM servers loaded from deep system paths (e.g. under WinSxS or Program Files); the Loaded Path column is clipped at the default interactive width with no way to read the full path via hover or resize-to-fit.
- **Evidence:**
  ```python
  self._com_table = QTableWidget(0, 3)
  self._com_table.setHorizontalHeaderLabels(["CLSID", "DLL Path", "Loaded Path"])
  ch = self._com_table.horizontalHeader()
  if ch is not None:
      ch.setSectionResizeMode(1, QHeaderView.ResizeMode.Stretch)
  tab_layout.addWidget(self._com_table)
  ```
- **Verified:** Read lines 288-293: self._com_table = QTableWidget(0, 3) with headers ["CLSID", "DLL Path", "Loaded Path"]; only ch.setSectionResizeMode(1, QHeaderView.ResizeMode.Stretch) is called (column index 1 = 'DLL Path'). Column 2 ('Loaded Path') is left at the QHeaderView default Interactive mode. Grep of the whole file confirms no setStretchLastSection call anywhere, and no resizeColumnsToContents call for _com_table. The only setToolTip call in the file (line 418, inside _set_handle_cell) is scoped to a different widget, self._handle_table, not self._com_table — so items inserted into _com_table at lines 515-517 (via QTableWidgetItem(str(...))) get no tooltip either. loaded_path (line 517) is popu …
- **Note:** Report is accurate as written; no changes needed to line/severity. Verified the setToolTip at line 418 belongs to a separate _set_handle_cell helper for _handle_table, not _com_table, ruling out that potential mitigation.

#### M58 — DLL-injection log Details column clips raw exception text with no resize mode or tooltip
- **File:** `src/intellicrack/ui/panels/process_panel/modules_tab.py:392`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** process-tabs
- **Defect:** _inject_log only stretches column 0 (DLL Path); column 2 ('Details') stays at the default interactive width and receives str(exc) on injection failure -- an arbitrarily long bridge/OS error message -- with no tooltip. This is a direct regression-style inconsistency within the same file: _set_handle_cell (used for the Handles sub-tab a few methods below) explicitly documents and implements 'a full-value tooltip ... so that wide values ... remain readable when the column is too narrow', but that pattern was never applied to the injection log's error-detail column.
- **Failure:** User attempts DLL injection that fails with a descriptive Windows error (e.g. an access-denied message with process/DLL context); the Details cell in the injection log is clipped by the default column width and there is no tooltip or resize to recover the full failure reason, unlike the adjacent Handles table which solves exactly this problem.
- **Evidence:**
  ```python
  self._inject_log = QTableWidget(0, 3)
  self._inject_log.setHorizontalHeaderLabels(["DLL Path", "Status", "Details"])
  ilh = self._inject_log.horizontalHeader()
  if ilh is not None:
      ilh.setSectionResizeMode(0, QHeaderView.ResizeMode.Stretch)
  ...
  self._inject_log.setItem(row, 2, QTableWidgetItem(str(exc)))
  ```
- **Verified:** Verified against the actual file. Lines 184-189 create `self._inject_log = QTableWidget(0, 3)` with headers ["DLL Path", "Status", "Details"] and only call `ilh.setSectionResizeMode(0, QHeaderView.ResizeMode.Stretch)` — columns 1 and 2 are left at the QHeaderView default (Interactive) with their initial header-label-based width. Line 392 (`self._inject_log.setItem(row, 2, QTableWidgetItem(str(exc)))`) writes the raw exception string into column 2 with no `setToolTip` call and no prior/later call anywhere in the file to `resizeColumnsToContents`/`resizeColumnToContents`/`setWordWrap`/`setTextElideMode`. A `grep` for those APIs across the file returns nothing. Meanwhile `_set_handle_cell` (lin …
- **Note:** Title's "no resize mode" is slightly imprecise — the column does have a resize mode (Interactive, the Qt default), which permits manual drag-resize or double-click-to-fit. The real defect is the missing tooltip (setToolTip) and missing auto-size-to-content for a column that holds variable-length raw exception text, exactly the pattern _set_handle_cell already solves a few methods below for the Handles tab. Severity corrected from HIGH to MEDIUM since the text is recoverable via manual column resize and no data is actually lost, just initially clipped/elided.

#### M59 — Process-info splitter is collapsible with no minimum size, can hide a pane entirely
- **File:** `src/intellicrack/ui/panels/process_panel/process_tab.py:295`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** process-tabs
- **Defect:** The QSplitter built for the Process Info sub-tab is never given setChildrenCollapsible(False), and neither the info tree nor the environment-variable table/widget is given a minimum size. QSplitter's children are collapsible by default, so dragging the splitter handle to either edge (or double-clicking it, a standard Qt splitter gesture) fully collapses one pane to zero height with no visible affordance to restore it other than manually dragging the (now hairline) handle back.
- **Failure:** User drags the horizontal splitter handle between the info tree and the environment-variables table all the way up or down; the info tree (or env table) collapses to 0px height and disappears from view, leaving the user unable to see process fields or environment variables until they precisely locate and drag the now-invisible splitter handle back.
- **Evidence:**
  ```python
  splitter = QSplitter(Qt.Orientation.Vertical)
  
  self._info_tree = QTreeWidget()
  ...
  splitter.addWidget(self._info_tree)
  ...
  splitter.addWidget(env_widget)
  
  splitter.setSizes([_SPLIT_LEFT, _SPLIT_RIGHT])
  tab_layout.addWidget(splitter)
  ```
- **Verified:** Read process_tab.py lines 284-321. `splitter = QSplitter(Qt.Orientation.Vertical)` (line 295) is populated with `self._info_tree` (line 303) and `env_widget` containing `self._env_table` (line 317), then `splitter.setSizes([_SPLIT_LEFT, _SPLIT_RIGHT])` (line 319) is called with no `setChildrenCollapsible(False)` call and no minimum-size constraint on either child widget anywhere in the file (grep for setChildrenCollapsible/setMinimumHeight/setMinimumSize/setMinimumWidth in this file returns zero matches). QSplitter's default `childrenCollapsible` property is True in PyQt6/Qt6, and double-clicking or dragging a splitter handle to the edge with this default will collapse a pane to 0px with no  …
- **Note:** Report is accurate as written; no correction needed beyond confirming line 295 (the QSplitter construction line) as the anchor.

#### M60 — Wait-status label shows raw unwrapped exception text with no tooltip or dialog fallback
- **File:** `src/intellicrack/ui/panels/process_panel/threads_tab.py:523`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** process-tabs
- **Defect:** self._wait_status is a plain QLabel (created at line 202 with no setWordWrap(True) and no tooltip) placed directly under the thread table. On time-wait failure its ONLY error surface is this label's text, which is set to f"Wait failed: {exc}" -- an arbitrarily long, variable string (Windows API/bridge error messages). Every other sibling error path in this file/cluster (e.g. memory_tab.py's _on_write, _on_search) keeps the inline status label short and routes the full exception text to a QMessageBox instead. Here there is no QMessageBox call at all, so a long exception message is silently clipped by the label's fixed-in-place layout with no way for the user to read the rest.
- **Failure:** User clicks 'Time Wait' on a thread that the bridge fails to wait on with a verbose error (e.g. a Win32 error string or nested bridge exception); the _wait_status QLabel renders the truncated/overflowing single-line text and the user has no way to see the full failure reason since there is no tooltip, no word wrap, and no QMessageBox with the complete message.
- **Evidence:**
  ```python
  self._wait_status = QLabel("")
  self._wait_status.setObjectName("toolbar_label")
  tab_layout.addWidget(self._wait_status)
  ...
  def _on_error(exc: object) -> None:
      _logger.warning("time_thread_wait_failed", tid=tid, error=str(exc))
      self._wait_status.setText(f"Wait failed: {exc}")
  ```
- **Verified:** Confirmed against the actual source. `self._wait_status = QLabel("")` (line 202) is created with no `setWordWrap(True)` and no `setToolTip` call anywhere in the file (verified via grep across the whole file). On failure, `_on_time_thread_wait`'s `_on_error` callback (lines 521-523) does exactly `self._wait_status.setText(f"Wait failed: {exc}")` with no QMessageBox anywhere in that error path. Tracing the failure surface: `run_bridge_coroutine_logged` (src/intellicrack/ui/panels/async_bridge.py) only logs a structured warning and invokes the caller's `on_error` callback — it never itself pops a QMessageBox — so the QLabel truly is the only user-visible surface for the exception text. By contr …
- **Note:** Core claim verified as written: QLabel at line 202 has no setWordWrap/tooltip, error handler at line 523 sets raw f"Wait failed: {exc}" with no QMessageBox fallback, and this is inconsistent with memory_tab.py's QMessageBox-based error surfacing for the same cluster. Severity corrected from HIGH to MEDIUM since the underlying bridge call's actual error strings are currently short/bounded and the full text remains available via structured logging, making this a usability/clipping gap rather than a functional failure.

#### M61 — Vertical main splitter is fully collapsible, unlike sibling panels that lock it open
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:349`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** sandbox-panel
- **Defect:** main_splitter is created without setChildrenCollapsible(False). Other panels in the same directory (ghidra_panel.py, cutter_panel.py, cutter_search_tab.py, cutter_debugger_tab.py) explicitly call setChildrenCollapsible(False) on their splitters to prevent a pane from being dragged to zero width/height. sandbox_panel's splitter separates the execution/config controls (top) from a QTabWidget holding 15 output tabs including Console, File Changes, Registry Changes, Network Activity, VM Display, API Calls, and more (bottom) -- both are default-collapsible.
- **Failure:** User drags the splitter handle between the exec controls and the output tabs; QSplitter's default collapsible behavior lets either pane be dragged to zero size. Dragging it all the way down hides the entire 15-tab output area (including the live Console log and VM Display) with only a thin sliver of the widget visible and no obvious indication of what happened or how to restore it (the handle itself becomes very hard to grab at zero height).
- **Evidence:**
  ```python
  main_splitter = QSplitter(Qt.Orientation.Vertical)
  ```
- **Verified:** Read src/intellicrack/ui/panels/sandbox_panel.py lines 340-519. Line 349 creates `main_splitter = QSplitter(Qt.Orientation.Vertical)` and it is the only QSplitter in the file (grep confirms). No `setChildrenCollapsible` call exists anywhere in this file, and neither `exec_container` (added at line 412) nor `output_tabs` (a QTabWidget with 15 tabs, added at line 516) has a minimum height set that would prevent zero-size collapse. PyQt6 QSplitter panes are collapsible by default (this is the documented Qt behavior the other panels explicitly override), so dragging the handle fully in either direction will collapse one pane to zero height. Grep across the same directory confirms `setChildrenCol …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M62 — Script list/editor QSplitter is collapsible to zero width, missing the codebase's established fix
- **File:** `src/intellicrack/ui/panels/script_manager.py:556`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** chat-misc-panels
- **Defect:** splitter = QSplitter(Qt.Orientation.Horizontal) is used with no setChildrenCollapsible(False) call and no setMinimumWidth() on either child panel. QSplitter defaults to childrenCollapsible=True, so dragging the handle (or a quick drag past the left panel's small size hint) collapses the left_panel -- which holds the entire script list and type filter -- to zero width, hiding it. Every other splitter-using panel in this codebase (app.py:537, cutter_panel.py:178/203, cutter_debugger_tab.py:163, cutter_search_tab.py:117, ghidra_panel.py:195/198) explicitly calls setChildrenCollapsible(False), confirming this is the established, intentionally-applied fix that this file is missing.
- **Failure:** User drags the splitter handle toward the left edge (or double-clicks it, standard Qt splitter-collapse gesture) while working in the Script Manager panel; the script list and type filter combo vanish entirely, leaving only the editor/result pane, with no visible affordance indicating the list is still present and collapsed rather than removed.
- **Evidence:**
  ```python
  splitter = QSplitter(Qt.Orientation.Horizontal)
  ```
- **Verified:** Read src/intellicrack/ui/panels/script_manager.py lines 549-661. Line 556 creates `splitter = QSplitter(Qt.Orientation.Horizontal)`. left_panel (holding the type filter combo and the entire script list, lines 558-580) is added via `splitter.addWidget(left_panel)` at line 580, and right_panel (editor/results) via `splitter.addWidget(right_panel)` at line 648. The only width-related calls anywhere in this setup are `left_panel.setMaximumWidth(_LEFT_PANEL_MAX_WIDTH)` (line 579) and `splitter.setSizes([_SPLITTER_LEFT_SIZE, _SPLITTER_RIGHT_SIZE])` (line 649) — a maximum width and an initial size, neither of which prevents the handle from being dragged to zero. There is no `setChildrenCollapsible( …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### M63 — Handles table splits width equally across 5 columns, squeezing the variable-length Object column
- **File:** `src/intellicrack/ui/panels/x64dbg_advanced_tab.py:1035`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** `setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` is called with no column index, so all 5 columns ("Handle", "Object", "Granted Access", "Type Index", "Attributes") stretch to equal width. "Object" holds `hex(int(entry.Object or 0))` from the bridge but, more importantly, is the field meant to represent arbitrary kernel object identifiers/paths and is the longest-content column, yet it gets exactly the same share as the short numeric "Type Index"/"Attributes" columns, and no tooltip is set on the cell items to recover elided text. This contrasts with `StackFrameTable._setup_ui()` in the companion file, which correctly gives `Stretch` only to its variable-length "Function" column and `ResizeToContents` to the short fixed-format columns.
- **Failure:** Enumerating handles on a process with many open handles (typical: dozens to hundreds), the "Object" values are compressed into 1/5 of the table width alongside four narrow numeric/hex columns, eliding the very information a user needs to identify which handle to close, with no tooltip to reveal the full text.
- **Evidence:**
  ```python
  handles_h = self._handles_table.horizontalHeader()
          if handles_h is not None:
              handles_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
  ```
- **Verified:** Read src/intellicrack/ui/panels/x64dbg_advanced_tab.py lines 1028-1074. Line 1035 calls `handles_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` with no column-index argument — this is the header-wide overload, which sets Stretch mode on all 5 columns ("Handle", "Object", "Granted Access", "Type Index", "Attributes" from `_HANDLE_COLUMNS` at line 51). Qt's Stretch algorithm for a header where every section is Stretch distributes the available width essentially evenly across all stretch sections, so all 5 columns end up the same width regardless of content length. Checked the data populated into row cells (lines 1071-1074): column 1 ("Object") is `str(entry.get("object", ""))`, and th …
- **Note:** Minor imprecision in the original description: the "Object" column holds a raw hex kernel-object pointer (`hex(int(entry.Object or 0))` per bridges/x64dbg.py:8455), not a path string, but it is still genuinely the longest/most variable of the 5 columns, so the substance of the claim is accurate.

#### M64 — Main content splitters are collapsible to zero with no minimum size protection
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:252`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** x64dbg-panel
- **Defect:** `main_splitter` (vertical) and `top_splitter` (horizontal) are created and populated with the disassembly view, the register/stack/module/thread inspect tabs, and the breakpoints/memory/console/etc. bottom tabs, but neither splitter calls `setChildrenCollapsible(False)` nor sets a `setMinimumWidth`/`setMinimumHeight` on its child panes. QSplitter children are collapsible to 0px by default in Qt6, so a small mouse drag on either splitter handle can fully hide the disassembly pane, the entire inspect-tabs column, or the entire bottom tab area.
- **Failure:** User double-clicks or drags the horizontal splitter handle between the disassembly view and the register/stack/module tabs past the edge; the inspect-tabs pane collapses to 0 width and disappears entirely, with no obvious affordance indicating how to restore it (the handle itself becomes a hairline at the panel edge).
- **Evidence:**
  ```python
  main_splitter = QSplitter(Qt.Orientation.Vertical)
  
          top_splitter = QSplitter(Qt.Orientation.Horizontal)
          top_splitter.addWidget(self._create_disasm_section())
          top_splitter.addWidget(self._create_inspect_tabs())
  ```
- **Verified:** Read src/intellicrack/ui/panels/x64dbg_panel.py lines 246-274. At line 252, main_splitter = QSplitter(Qt.Orientation.Vertical) is created; top_splitter (line 254, Horizontal) has _create_disasm_section() and _create_inspect_tabs() added (lines 255-256), and main_splitter has top_splitter and _create_bottom_tabs() added (lines 258, 260). Neither splitter has setChildrenCollapsible(False) called on it anywhere in the file (grep for 'setChildrenCollapsible' across the file returns zero matches). The only setMinimumWidth calls in the file (lines 646, 782, 825, 831, 916, 964) are on unrelated small input widgets inside dialogs/toolbars (eval_input, search_pattern_input, trace_cond_input, etc.), n …
- **Note:** Claim is accurate as reported; no changes needed to line, severity, or description.

#### M65 — Process-info Path and Command Line labels have no word-wrap for arbitrarily long variable text
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:418`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** x64dbg-panel
- **Defect:** `_procinfo_path` and `_procinfo_cmdline` are plain `QLabel("--")` instances added as QFormLayout field widgets, with no `setWordWrap(True)` and no tooltip. `_apply_procinfo` (line 3274) sets their text from the attached process's real filesystem path and full command line via `setText(str(path))`/`setText(str(command_line))`, both of which are unbounded, variable-length strings (Windows paths routinely exceed 80 characters; command lines with multiple quoted arguments can be far longer). Because the labels live inside a QSplitter pane with no scroll area, long text either gets hard-clipped at the pane boundary or forces the pane wider at the expense of the disassembly/inspect-tab split, with no way to read the full value.
- **Failure:** User attaches to a process launched with a long command line (e.g. an installer invoked with several `--flag="C:\long\path\..."` arguments) and opens the Process Info tab; the Command Line row is clipped mid-argument with no wrap and no tooltip, making the actual command line unreadable and unrecoverable from the UI.
- **Evidence:**
  ```python
  self._procinfo_path = QLabel("--")
          self._procinfo_cmdline = QLabel("--")
  ```
- **Verified:** Read x64dbg_panel.py lines 412-434: _procinfo_path and _procinfo_cmdline are created as plain QLabel(\"--\") (418-419) and added as QFormLayout field widgets (423-424) inside procinfo_container, which lives in the horizontal top_splitter (QSplitter at line 254). _apply_procinfo (3274-3287) sets their text via setText(str(path)) and setText(str(command_line)) from bridge-supplied ProcessInfo, both unbounded/variable-length strings (real filesystem paths and full command lines). A file-wide grep for setWordWrap, setToolTip on these labels, and QScrollArea returns zero matches anywhere in the file, confirming no wrap, no tooltip, and no scrollable container mitigate the clipping. QLabel does no …
- **Note:** Report is accurate as written; line 418 and 3274/3282-3287 evidence both check out verbatim. No mitigation found anywhere in the file.

#### M66 — Sandbox availability status label lacks word-wrap/tooltip for variable-length reason text
- **File:** `src/intellicrack/ui/sandbox_config.py:737`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** vnc-sandbox-config
- **Defect:** self._status_label is a plain QLabel (constructed at line 555) placed in a QHBoxLayout with addStretch() after it, and never has setWordWrap(True) or a tooltip configured anywhere in the file. _set_unavailable() sets its text to f"Windows Sandbox unavailable: {reason}" where reason is a caller-supplied, genuinely variable string - including raw OS exception text via f"Could not determine Windows Sandbox status: {exc}" in _probe_windows_sandbox (sandbox_config.py:173), which can be an arbitrarily long Windows error message. QLabel has no default minimum size floor, so within the QHBoxLayout the label can be compressed below its unwrapped text's sizeHint, visually clipping the reason text inside the fixed 550px-wide dialog.
- **Failure:** Windows Sandbox availability probe fails with a verbose OSError (e.g. a WinError message with a long description), and _set_unavailable(reason) sets the label text to a 60-100+ character string; inside the fixed-width (550px) dialog the QHBoxLayout compresses the unwrapped label, clipping the explanation so the user cannot read why Sandbox is unavailable and has no tooltip fallback.
- **Evidence:**
  ```python
  self._status_label.setText(f"Windows Sandbox unavailable: {reason}")
  ```
- **Verified:** Confirmed against the actual source. `self._status_label = QLabel(...)` is constructed at sandbox_config.py:555 and added to `status_layout` (a QHBoxLayout) followed by `status_layout.addStretch()` at line 557 — exactly as claimed. A repo-wide grep of the file shows zero calls to `setWordWrap` and the only `setToolTip` call in the file (line 599) is on an unrelated checkbox, not on `_status_label`. `_set_unavailable()` (line 728-737) does `self._status_label.setText(f"Windows Sandbox unavailable: {reason}")`, and `reason` is genuinely caller-supplied variable text, confirmed by `_probe_windows_sandbox()` at line 173: `return False, f"Could not determine Windows Sandbox status: {exc}"`, which …
- **Note:** Failure mechanism should be corrected: the dialog is a normal resizable QDialog with no setFixedSize/setMaximumWidth, so the more accurate visible symptom is the window forcibly growing wider than the intended 550px (potentially off-screen) to fit the unwrapped OS-error text, rather than the label being silently clipped inside a fixed-width frame. The core defect — QLabel carrying unbounded/variable exception text with no setWordWrap(True) and no tooltip fallback — is confirmed by the code at lines 555, 557, 173, and 737.

#### M67 — Per-tool status_label lacks word-wrap and can overflow the fixed-width settings panel for long check-failure messages
- **File:** `src/intellicrack/ui/tool_config.py:1025`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** tool-config-prefs
- **Defect:** ToolSettingsWidget.status_label is a bare QLabel("Unknown") with no setWordWrap(True), placed in an HBoxLayout (status_row) with addStretch() after it, itself inside a QFormLayout row. Its text is later set via self.status_label.setText(message) in _on_status_checked() (line 1201) with values that include the unbounded "Check failed: {e}" string from ToolStatusCheckWorker.run() -- e can be any RuntimeError/OSError/ImportError message, including full paths or nested error text. Because the widget lives inside ToolConfigDialog's QStackedWidget page, whose width is bounded by the splitter's fixed initial right-hand size (_SPLIT_RIGHT = 570px) and the dialog is not auto-resized to content, a long single-line, non-wrapping label forces the row wider than the available space and gets visually clipped at the panel/dialog edge instead of wrapping onto multiple lines.
- **Failure:** Clicking "Check Status" for a tool whose configured path triggers an OSError with a long message (e.g. a deeply nested or permission-denied network path), the status_label receives "Check failed: <long OSError text>" and, lacking word-wrap, is clipped at the right edge of the fixed-width settings panel instead of wrapping so the user can read the full diagnostic.
- **Evidence:**
  ```python
  self.status_label = QLabel("Unknown")
  self.status_label.setObjectName("status_label")
  status_row.addWidget(self.status_label)
  status_row.addStretch()
  ```
- **Verified:** Read src/intellicrack/ui/tool_config.py lines 1018-1030: `self.status_label = QLabel("Unknown")` (line 1025) is added to `status_row` (QHBoxLayout with addStretch()) inside a QFormLayout row, with no `setWordWrap(True)` call anywhere in the file (grep for "status_label" shows only construction at 1025-1027 and two setText calls at 1171/1201 — no wrap/tooltip/elide mitigation). By contrast, the sibling `desc_label` immediately above (line 1006-1008) does call `setWordWrap(True)`, confirming the omission on status_label is an inconsistency/oversight, not an intentional design choice. The text source is genuinely variable and unbounded: ToolStatusCheckWorker.run() (line 652-662) catches Runtime …
- **Note:** No correction needed to the original report; claim is accurate as stated.

#### M68 — Tool status list rows show variable-length status/error messages with no tooltip to read the full text
- **File:** `src/intellicrack/ui/tool_config.py:1708`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** tool-config-prefs
- **Defect:** ToolStatusDialog's left-hand QListWidget (_status_list) renders items as f"{status_icon}  {display_name} - {message}" where message is an arbitrary status/error string coming from ToolStatusCheckWorker (e.g. "GitHub API rate limit exceeded; try again later", "analyzeHeadless not found in installation", or a raw "Check failed: {exception}" string). QListWidget's default item delegate elides overflowing text (Qt.TextElideMode.ElideRight) rather than wrapping it, and no setToolTip() is ever called on these items (contrast with ToolConfigDialog's tool list at line 897, which does set a tooltip). The left panel starts at a fixed 300px (_COMPAT_SPLIT_LEFT), so any message longer than that is silently truncated with no way for the user to read the rest.
- **Failure:** A tool status check fails with a longer diagnostic message (e.g. "GitHub API request forbidden (HTTP 403)" combined with a fallback URL hint, or an OSError string containing a path), and the left panel is left at its default 300px width. The row is rendered as "...(truncated)" with no tooltip, so the user cannot see why the tool is unavailable without resizing the splitter by trial and error.
- **Evidence:**
  ```python
  status_icon = "✓" if available else "✗"
  item = QListWidgetItem(f"{status_icon}  {display_name} - {message}")
  self._status_list.addItem(item)
  ```
- **Verified:** Read tool_config.py 1560-1750 and 855-899. Confirmed: (1) `_status_list = QListWidget()` (line 1573) is created with no `setWordWrap(True)` call anywhere in the file for this widget (grep shows wordWrap only used on unrelated QLabels at 1008/1394/1402); default QListView behavior without word wrap is single-line elided text (Qt::ElideRight). (2) Items are built as `f"{status_icon}  {display_name} - {message}"` at lines 1699, 1708, and 1733, where `message` is an arbitrary status/diagnostic string from ToolStatusEntry/ToolStatusCheckWorker. (3) No `.setToolTip()` call exists anywhere on any `_status_list` item — grep for `setToolTip` across the whole file finds only 2 hits: line 897 (a *diffe …
- **Note:** Report is accurate as written; no correction needed. Additional confirmed instances of the same unmitigated pattern also exist at lines 1699 and 1733 in the same file (the "Status unknown" and "Checking..." fallback items), though those are lower-severity since their text is short and fixed rather than variable/long.

#### M69 — main_splitter can be dragged fully collapsed, hiding the tab area or the function/xref navigator
- **File:** `src/intellicrack/ui/tools.py:828`
- **Severity:** MEDIUM  ·  **Category:** layout  ·  **Cluster:** tools
- **Defect:** `self.main_splitter` separates the left tab area (Ghidra/x64dbg/Cutter/Frida/Hex Editor/etc.) from the right Functions/XRefs navigator. `left_panel`/`right_panel` get `setMinimumWidth(_LEFT_MIN_WIDTH)`/`setMinimumWidth(_RIGHT_MIN_WIDTH)`, but QSplitter's `childrenCollapsible` property defaults to `True`, which lets a user drag a handle past a child's minimum size and snap it to 0 width regardless of the minimum -- `setMinimumWidth` alone does not prevent this. The sibling panels in this same codebase (src/intellicrack/ui/app.py:537, ui/panels/cutter_panel.py:178/203, ui/panels/cutter_debugger_tab.py:163, ui/panels/cutter_search_tab.py:117, ui/panels/ghidra_panel.py:195/198) all explicitly call `setChildrenCollapsible(False)` on their splitters; `main_splitter` here has no such call.
- **Failure:** User drags the main splitter handle toward the right edge; the right panel (Functions list + Cross References, the only navigator for jumping between functions/xrefs) collapses to 0 width and disappears, with no visible handle remnant to grab back out except hunting for the very edge of the widget -- effectively losing the navigator until the user knows to hunt for the hidden handle.
- **Evidence:**
  ```python
  self.main_splitter = QSplitter(Qt.Orientation.Horizontal)
  
  left_panel = QFrame()
  left_panel.setMinimumWidth(_LEFT_MIN_WIDTH)
  ...
  right_panel = QFrame()
  right_panel.setMinimumWidth(_RIGHT_MIN_WIDTH)
  ...
  self.main_splitter.setSizes([_DEFAULT_SPLIT_LEFT, _DEFAULT_SPLIT_RIGHT])
  ```
- **Verified:** Read src/intellicrack/ui/tools.py:828-866. self.main_splitter = QSplitter(Qt.Orientation.Horizontal) is created at line 828; left_panel gets setMinimumWidth(_LEFT_MIN_WIDTH) (line 831) and right_panel gets setMinimumWidth(_RIGHT_MIN_WIDTH) (line 850), then both are added to main_splitter (lines 847, 863) and setSizes([_DEFAULT_SPLIT_LEFT, _DEFAULT_SPLIT_RIGHT]) is called (line 864). A full-file grep for `setChildrenCollapsible` in tools.py returns zero matches, confirming childrenCollapsible is left at its Qt default of True. Per Qt's documented QSplitter behavior, childrenCollapsible=True lets the user drag a handle and collapse a child to 0 width even when that child has an explicit minimu …
- **Note:** Report is accurate as written; line 828 (QSplitter construction) is a reasonable anchor even though the missing call would logically be inserted right after, e.g. around line 829 or right before line 866. Severity MEDIUM is appropriate: it's a usability/discoverability regression (panel can vanish with no obvious way back) but not a crash or data-loss issue, consistent with how the fix is trivially available (one-line setChildrenCollapsible(False) call) elsewhere in the same codebase.

---

## LOW — 19 finding(s)

### Logic / wiring / leaks

#### L1 — File Changes tree column labeled 'Details' always displays the file size, never any detail text
- **File:** `src/intellicrack/ui/panels/sandbox_panel.py:423`
- **Severity:** LOW  ·  **Category:** logic  ·  **Cluster:** sandbox-panel
- **Defect:** The File Changes tree header is set to ["Operation", "Path", "Details"], but _populate_file_changes (the path used when a bridge run_binary report dict is displayed) fills the third column with change.get("size", "") -- the FileChange schema (src/intellicrack/sandbox/base.py) has no 'details' field at all, only path/operation/old_path/timestamp/size. Every row's third column is therefore always a byte-size value (or blank), never descriptive detail text, despite the column header implying otherwise.
- **Failure:** User runs a binary in the sandbox and inspects the File Changes tab. The third column, headed 'Details', shows numbers like '1024' or is blank -- there is no way to see what the 'Details' column is supposed to convey (e.g. old path for a rename) because the underlying data source never carries that information under this header.
- **Evidence:**
  ```python
  set_header_labels(self._file_changes_tree, ["Operation", "Path", "Details"])
  ```
- **Verified:** Verified against src/intellicrack/ui/panels/sandbox_panel.py. Line 423: `set_header_labels(self._file_changes_tree, ["Operation", "Path", "Details"])` — header literally says "Details". The dict-based report path `_populate_file_changes` (lines 1004-1021), invoked from `_display_execution_report`/similar via `self._populate_file_changes(report.get("file_changes"))` at line 993, builds each row as `[operation, path, str(change.get("size", ""))]` (line 1019) — it reads the `size` key, not any detail text. Cross-checked `src/intellicrack/sandbox/base.py` FileChange TypedDict (lines 112-119): fields are `path, operation, old_path, timestamp, size` — there is no `details` field anywhere in the sc …
- **Note:** Confirmed as described. Worth noting for the fix: the alternate object-based path (load_execution_report, lines 2507-2513) has the same symptom via a different, arguably worse bug (`getattr(dict, "details", "")` always returns the default), which a fix to this column should also address since both paths feed the same tree/header.

#### L2 — font_config.json is loaded into FontManager._font_config but never consulted for actual font selection
- **File:** `src/intellicrack/ui/resources/font_manager.py:140`
- **Severity:** LOW  ·  **Category:** logic  ·  **Cluster:** theme-resources
- **Defect:** `_load_font_config` reads `assets/fonts/font_config.json` (which specifies `monospace_fonts.primary/fallback`, `ui_fonts.primary/fallback`, and `font_sizes.ui_default/ui_small/ui_large/code_default/code_small/code_large/hex_view`) into `self._font_config`, but that dict is never read anywhere else in the class -- `_setup_fonts_from_loaded`, `_setup_fallback_fonts`, and `_find_available_font` operate exclusively on the hardcoded Python constants `FALLBACK_CODE_FONTS`/`FALLBACK_UI_FONTS`, and `get_code_font`/`get_ui_font` use hardcoded default size parameters (10/9) rather than the JSON's `font_sizes` values. `self._font_config` is only ever surfaced back out verbatim through `get_font_info()`'s `"config"` key, i.e. it is read from disk purely for display and has zero effect on which fonts or sizes are actually applied. Editing font_config.json's primary/fallback font lists or font_sizes therefore silently does nothing, contrary to what the file's presence and structure imply.
- **Failure:** A user or packager edits `assets/fonts/font_config.json` to reorder the monospace fallback list or change `font_sizes.ui_default` from 10 to a different value, expecting `FontManager.get_ui_font()`/`get_code_font()` to honor it; the change has no effect because the selection logic never reads `self._font_config`, only the hardcoded `FALLBACK_CODE_FONTS`/`FALLBACK_UI_FONTS` lists and the hardcoded default-size parameters in `get_code_font`/`get_ui_font`.
- **Evidence:**
  ```python
  self._font_config = cast("dict[str, object]", json.load(f))
  _logger.debug("font_config_loaded", config=self._font_config)
  ... (self._font_config is only read again at line 343: "config": self._font_config, inside get_font_info())
  ```
- **Verified:** Read the full file. `_load_font_config` (lines 134-144) parses font_config.json into `self._font_config`. Tracing every other use of that attribute: `_setup_fonts_from_loaded` (168-182) only iterates `self._loaded_families` and falls back to the hardcoded `FALLBACK_CODE_FONTS`/`FALLBACK_UI_FONTS` module constants via `_find_available_font`; `_setup_fallback_fonts` (184-194) does the same; `_find_available_font` (196-216) only takes the `candidates` list argument, never `self._font_config`. `get_code_font`/`get_ui_font` (218-234, 249-264) take hardcoded default `size: int = 10`/`9` parameters and construct `QFont(self._code_font_family, size)` — no reference to `self._font_config["font_sizes" …

### Theming / contrast / readability

#### L3 — VNCWidget never subscribes to theme_changed, leaving idle placeholder on stale theme colors
- **File:** `src/intellicrack/ui/panels/vnc_widget.py:1984`
- **Severity:** LOW  ·  **Category:** theming  ·  **Cluster:** vnc-sandbox-config
- **Defect:** paintEvent() correctly fetches ThemeManager.get_instance().get_analysis_colors() fresh on every paint rather than hardcoding colors, but VNCWidget never connects to ThemeManager's theme_changed signal to force a repaint proactively. When the widget is not connected (client.snapshot_frame() is None, showing the 'VNC Display' placeholder), the only repaint driver is the update_timer, which is only started in _on_connect_finished() and stopped again on disconnect/idle. In the idle/disconnected state there is no periodic timer or signal connection causing update(), so if the application theme is toggled while the VNC panel is idle, the placeholder background/muted text keeps rendering with colors computed at whatever moment the next unrelated repaint happens to occur (e.g. window resize), rather than immediately.
- **Failure:** User opens the Sandbox panel's VNC tab before connecting to any VNC server (placeholder 'VNC Display' visible), then switches the app theme from light to dark; the placeholder's background/text keep the old theme's colors until some other event (resize, tab switch back) forces a native repaint, producing a visibly mismatched panel against the rest of the now-dark UI.
- **Evidence:**
  ```python
  colors = ThemeManager.get_instance().get_analysis_colors()
              painter.fillRect(self.rect(), colors["background"])
              painter.setPen(colors["muted"])
              painter.drawText(self.rect(), Qt.AlignmentFlag.AlignCenter, "VNC Display")
  ```
- **Verified:** vnc_widget.py:1781-1798 (VNCWidget.__init__) connects only update_timer.timeout->_on_update_tick, framebuffer_updated->update, and _connect_finished->_on_connect_finished; there is no connection to ThemeManager.theme_changed anywhere in the file (grep returns zero matches). theme_manager.py:76 confirms theme_changed is a real pyqtSignal(str) that other widgets (e.g. hex_editor_widget.py) do subscribe to, establishing this as an established pattern the VNC widget omits. paintEvent (1966-1988) computes colors freshly via ThemeManager.get_instance().get_analysis_colors() at line 1984 only when snapshot is None (idle placeholder), matching the reported code evidence exactly. update_timer is star …

### Layout / sizing / text-cutoff

#### L4 — Filter-panel splitter is collapsible to zero width, hiding all filter controls with no restore affordance
- **File:** `src/intellicrack/ui/log_viewer/window.py:387`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** log-viewer
- **Defect:** The horizontal `QSplitter` housing the filter panel and the table view is built with default `QSplitter` behavior; `setChildrenCollapsible(False)` is never called and neither child widget has a `setMinimumWidth()`. QSplitter children are collapsible by default, so a small drag of the splitter handle toward the left edge snaps the filter panel to zero width, hiding the Minimum Level combo, logger regex, text search, case-sensitivity, Max Rows, and Auto-scroll controls entirely.
- **Failure:** User drags the splitter handle left past the panel's natural minimum size; the filter panel collapses to 0px and disappears. The only way back is to locate and drag the now razor-thin handle at the window's left edge, which is easy to miss, effectively locking the user out of all filter controls.
- **Evidence:**
  ```python
  splitter = QSplitter(Qt.Orientation.Horizontal, central)
          layout.addWidget(splitter)
  
          filter_panel = self._build_filter_panel()
          splitter.addWidget(filter_panel)
  ```
- **Verified:** Read src/intellicrack/ui/log_viewer/window.py lines 378-469. The QSplitter is created at line 387 (`splitter = QSplitter(Qt.Orientation.Horizontal, central)`), filter_panel is added at line 391, table_view at line 395, and only setStretchFactor/setSizes are called (lines 396-398) — no `setChildrenCollapsible(False)` call anywhere on the splitter, and `_build_filter_panel` (lines 452-469) never calls `setMinimumWidth` on the returned panel or any child widget. A whole-file grep for "Collapsible" and "MinimumWidth" returns zero matches. PyQt6 QSplitter defaults childrenCollapsible to True, so this is the stock unmitigated default-collapse behavior described in the claim: dragging the splitter  …
- **Note:** Report is accurate as written; line 387 correctly anchors the QSplitter construction that lacks setChildrenCollapsible(False)/setMinimumWidth mitigation. Severity LOW is reasonable given it's recoverable (drag handle back) though the affordance is thin.

#### L5 — Debugger status label has no word-wrap for potentially long exception text
- **File:** `src/intellicrack/ui/panels/cutter_debugger_tab.py:157`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** cutter-tabs
- **Defect:** `self._status_label` is placed at the end of `attach_row` after `attach_row.addStretch()`, and is subsequently set via `f"Attach failed: {exc}"`, `f"Failed to set {reg_name}: {exc}"`, `f"Detach failed: {exc}"`, `f"Step failed: {exc}"`, `f"Breakpoint operation failed: {exc}"`, etc. (see `_on_attach_error`, `_on_reg_set_error`, `_on_bp_error`, and others). `str(exc)` for OS/socket/RPC failures (e.g. `ConnectionRefusedError`, JSON-RPC error payloads from the rizin bridge) commonly exceeds the width available in the toolbar row. `QLabel` does not word-wrap by default and `setWordWrap(True)` is never called on this label, nor is a tooltip set, so a long error message will either be clipped by the panel edge or force the toolbar row wider than the visible tab area.
- **Failure:** The rizin debug backend is unreachable and `self._bridge.attach(pid)` raises an exception whose `str()` is a long OS-level message (e.g. a Windows socket error string), which is written verbatim into `self._status_label` at the far right of a horizontal toolbar with no wrap; the message is clipped by the container edge and the user cannot read the actual failure reason.
- **Evidence:**
  ```python
  self._status_label = QLabel(self.tr("Not attached"))
          self._status_label.setFont(fm.get_ui_font(9))
          attach_row.addWidget(self._status_label)
  ```
- **Verified:** Read lines 100-160 and grepped all 15 `_status_label.setText(...)` sites (lines 383-745) in cutter_debugger_tab.py. Confirms: `self._status_label = QLabel(self.tr(\"Not attached\"))` (line 157) is appended to `attach_row` (a QHBoxLayout already populated with PID input, 5 buttons, addSpacing(16), and addStretch() at line 155) via `attach_row.addWidget(self._status_label)` (line 159). No `setWordWrap`, tooltip, or elide-mode call exists anywhere in the file for this widget. Multiple setText calls embed `str(exc)` of unbounded length: line 424 `f\"Attach failed: {exc}\"`, line 461 `f\"Detach failed: {exc}\"`, line 511 `f\"Step failed: {exc}\"`, line 543 `f\"Continue failed: {exc}\"`, line 598  …

#### L6 — Type Browser tree has no header resize mode; Name column (types/structs/enums) is squeezed by the stretched Details column
- **File:** `src/intellicrack/ui/panels/cutter_tabs.py:1180`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** cutter-core
- **Defect:** `TypeBrowserTab._tree` uses `setHeaderLabels(["Name", "Details"])` with no `setSectionResizeMode`/`resizeColumnToContents` call anywhere in the class. Because QTreeWidget headers default `stretchLastSection` to True, the trailing "Details" column (short strings like `size=N` or a primitive type name) expands to fill the pane while "Name" — which holds type/struct/enum/member names that can be long, especially templated or qualified C/C++ type names — stays at the narrow Interactive default width.
- **Failure:** Analyzing a binary with long struct/type names (e.g. auto-generated or templated type names recovered by Rizin's type analysis) causes the Name column in the Type Browser tab to ellipsize entries, with no tooltip fallback, while the Details column next to it is mostly empty whitespace.
- **Evidence:**
  ```python
  self._tree = QTreeWidget()
  self._tree.setHeaderLabels(["Name", "Details"])
  layout.addWidget(self._tree)
  ```
- **Verified:** Read src/intellicrack/ui/panels/cutter_tabs.py lines 1167-1297 (full TypeBrowserTab class body). __init__ (1170-1181) only does `self._tree = QTreeWidget(); self._tree.setHeaderLabels(["Name", "Details"]); layout.addWidget(self._tree)` — no header(), setSectionResizeMode, resizeColumnToContents, setTextElideMode, or setToolTip call anywhere in the class (verified via grep for those tokens scoped to the file: the only setSectionResizeMode hit in the whole file is line 90, inside `_stretch_headers()`, a helper applied only to QTableWidget instances built via `_make_table()` for other tabs like AllStringsTab/SymbolsTab — TypeBrowserTab does not use `_make_table` or call `_stretch_headers`, so t …

#### L7 — Symbols table Name column squeezed to a fixed 1/4 width by uniform Stretch across 4 columns, no tooltip fallback
- **File:** `src/intellicrack/ui/panels/frida_instrumentation_tab.py:620`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** frida-instrumentation
- **Defect:** setSectionResizeMode(QHeaderView.ResizeMode.Stretch) is applied with no column index, which stretches all 4 columns (Name/Address/Is Global/Type) to equal widths. The 'Name' column holds variable-length, potentially long symbol/function names (including mangled C++ symbols from Windows DLLs), while 'Is Global'/'Type' are short (and, per related findings, frequently blank). Because Stretch mode removes the horizontal scrollbar, long names are elided within their fixed 25% share of the table width, and the QTableWidgetItem for name is created without setToolTip, so there is no way to recover the full text.
- **Failure:** Enumerating symbols of a C++ DLL containing long mangled export names (e.g. 60+ character symbols) produces a table where the Name column truncates mid-name with an ellipsis while the equally-wide, often-empty 'Is Global'/'Type' columns waste space, and hovering the cell shows no tooltip with the full name.
- **Evidence:**
  ```python
  sym_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
  ```
- **Verified:** Read lines 615-621 and 662-753 of frida_instrumentation_tab.py. Confirmed: `_SYMBOL_COLUMNS = ["Name", "Address", "Is Global", "Type"]` (line 55), the table is built with `self._symbols_table = QTableWidget(0, len(_SYMBOL_COLUMNS))` and `sym_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` at line 620 is called with no column index argument, so per Qt docs this sets the resize mode for ALL sections in the header to Stretch, forcing all 4 columns to equal (25%) width and disabling the horizontal scrollbar. The 'Name' column receives variable-length data (`QTableWidgetItem(name)` at lines 669 and 748) while 'Is Global' and 'Type' are populated with empty strings `QTableWidgetItem("")` i …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### L8 — RPC result label has no word-wrap or tooltip for arbitrary variable-length script return values
- **File:** `src/intellicrack/ui/panels/frida_instrumentation_tab.py:1296`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** frida-instrumentation
- **Defect:** _on_rpc_call_done sets _rpc_result_label's text to f"Result: {result}" where result is whatever value an arbitrary rpc.exports-declared JavaScript function returns (a string, number, array, or nested object stringified by Python's default repr/str). The label was created with QLabel("") and never has setWordWrap(True) or a tooltip applied, so a long or structured return value renders as one unwrapped line that is clipped once it exceeds the panel's available width.
- **Failure:** An RPC export that returns a list of addresses or a JSON-like object (e.g. `{'status': 'ok', 'addresses': [...]}`) produces a single long line in _rpc_result_label; since the label cannot wrap and has no tooltip, the tail of the result is invisible once the panel width is exceeded, with no way for the user to read the rest of the value.
- **Evidence:**
  ```python
  self._rpc_result_label.setText(f"Result: {result}")
  ```
- **Verified:** Read lines 1207-1208 (QLabel("") construction, added to layout) and line 1296 (self._rpc_result_label.setText(f"Result: {result}") in _on_rpc_call_done). A whole-file grep for setWordWrap/setToolTip confirms neither is ever called on _rpc_result_label. result is `object` (line 1289 signature: def _on_rpc_call_done(self, result: object)), populated from an arbitrary rpc.exports JS function's return value via bridge.rpc_call, so it can be a long string, list, or dict whose str() is unbounded in length. The label sits directly in a QVBoxLayout with no QScrollArea, no setMaximumWidth/setFixedWidth on it, and no stretch/eliding configured, and the containing tab panel has a bounded width (sibling …
- **Note:** Report is accurate as written; no mitigation found anywhere in the file (grep for setWordWrap/setToolTip/_rpc_result_label returns only the construction and setText lines). Severity LOW is appropriate since it's a display/readability issue, not a crash or data-loss defect.

#### L9 — Module combo box truncates long module names (no AdjustToContents policy)
- **File:** `src/intellicrack/ui/panels/frida_panel.py:1628`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** frida-panel
- **Defect:** `self._module_combo` is created empty and given only a `setMinimumWidth(_DEVICE_COMBO_MIN_WIDTH)` (120px) floor; it never receives `setSizeAdjustPolicy(QComboBox.SizeAdjustPolicy.AdjustToContents)`. QComboBox's default `sizeAdjustPolicy` is `AdjustToContentsOnFirstShow`, which fixes the widget's width based on its contents (empty) at first paint and does not grow for items added afterward via `_populate_modules_table`'s `self._module_combo.addItem(name)` loop.
- **Failure:** After `_on_refresh_modules` enumerates process modules and populates the combo with real module filenames (e.g. `api-ms-win-core-synch-l1-2-0.dll`, `libcrypto-3-x64.dll`), the closed combo box remains fixed at ~120px, so the selected item's text is elided; the user cannot tell which of several similarly-prefixed modules is selected before clicking Exports/Imports without opening the dropdown.
- **Evidence:**
  ```python
  self._module_combo = QComboBox()
  self._module_combo.setMinimumWidth(_DEVICE_COMBO_MIN_WIDTH)
  ...
  self._module_combo.addItem(name)
  ```
- **Verified:** Read frida_panel.py:1625-1706. Line 1627 creates `self._module_combo = QComboBox()` empty, line 1628 sets only `setMinimumWidth(_DEVICE_COMBO_MIN_WIDTH)` (120, from the `_DEVICE_COMBO_MIN_WIDTH: Final[int] = 120` constant at line 64 shared with the device combo). No `setSizeAdjustPolicy` call exists anywhere in the file (grep for "SizeAdjustPolicy" returns nothing), and no tooltip is wired to the combo's current text (grep for "setToolTip"/"currentIndexChanged" near this widget shows nothing relevant — the only setToolTip in the file is on an unrelated thread-id input at line 419). The combo is added to `detail_row` (line 1629) which ends with `detail_row.addStretch()` (line 1638); that trai …
- **Note:** No correction needed; report is accurate as written.

#### L10 — Functions sidebar tree has no column-resize policy for variable-length function names
- **File:** `src/intellicrack/ui/panels/ghidra_panel.py:1295`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** ghidra-panel
- **Defect:** `_func_tree` is a `QTreeWidget` with columns `["Name", "Address", "Size"]`, populated in `_apply_functions` with real function names from the binary (which can be long, e.g. mangled C++ symbols or decorated Windows API names). No `resizeColumnToContents`, `setSectionResizeMode(ResizeToContents)`, or comparable call is made anywhere in the file for this tree (nor for `_xrefs_tree` or `_call_graph_tree`), unlike the `QTableWidget`s built via `_make_table`, which explicitly set `QHeaderView.ResizeMode.Stretch`. `QHeaderView`'s default Interactive section size for the first column is sized from the header label ("Name"), not the data, and this tree lives in the functions sidebar which the panel deliberately keeps narrow (`_MAIN_SPLIT_RATIO_RIGHT = 250` vs `_MAIN_SPLIT_RATIO_LEFT = 600`), so long function names are clipped until the user manually drags the column border.
- **Failure:** After 'Analyze' or 'Refresh' populates the functions list on a binary with long/decorated symbol names (e.g. `?CreateInstanceHelper@ClassFactoryImpl@@UEAAJPEAUIUnknown@@0PEAPEAX@Z`), the Name column in the 250px-wide sidebar shows only the first few characters with no way to see the rest without manually widening the column each time the tree is repopulated.
- **Evidence:**
  ```python
  self._func_tree = QTreeWidget()
          set_header_labels(self._func_tree, _FUNC_COLUMNS)
          set_sorting_enabled(self._func_tree, enable=True)
  ```
- **Verified:** Read ghidra_panel.py lines 1295-1302 (functions tree construction) and 1842-1864 (_apply_functions population). Confirmed: `self._func_tree = QTreeWidget()` is created with `set_header_labels(self._func_tree, _FUNC_COLUMNS)` (columns ["Name","Address","Size"], line 79) and populated in `_apply_functions` with real `func.name` values from the bridge (line 1855), but no `resizeColumnToContents`, `setSectionResizeMode(ResizeToContents/Stretch)`, `setStretchLastSection`, or `setToolTip` call exists anywhere for `_func_tree` in the file (grep for those symbols only matches `_make_table`'s QTableWidget helper at line 132, used by unrelated QTableWidget instances, not the QTreeWidget trees). Grep a …
- **Note:** Report is accurate as written; no correction needed. Also applies identically (unreported) to _xrefs_tree (line 333) and _call_graph_tree (line 742), which the auditor already noted in passing within the description.

#### L11 — Pattern library tree width capped at 200px with no full-name fallback when description tooltip is empty
- **File:** `src/intellicrack/ui/panels/hex_editor/pattern_editor.py:155`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** hex-scripting
- **Defect:** self._pattern_library_tree.setMaximumWidth(200) hard-caps the library tree's width; combined with two levels of tree indentation (root > category > leaf) the usable text column is narrower still. QTreeWidgetItem text elides by default when it overflows the column, and the only mitigation is the per-item tooltip, which is set to the pattern/template *description*, not the name: `tooltip = pattern.description or ""` (line 706) and `template_item.setToolTip(0, desc_val)` (line 666) where desc_val can also be an empty string. Community pattern file stems observed in vendor/community-patterns/patterns run up to ~29 characters (e.g. 'dotnet_binaryformatter'), which combined with tree indentation can exceed the available column width. When the description is empty there is no tooltip at all, so an elided long name has no way to be read in full.
- **Failure:** A HexPat community pattern with a long file-stem name and no pragma description (e.g. 'dotnet_binaryformatter') is loaded into the library tree under 'HexPat Patterns'. At the fixed 200px max width minus two levels of indentation, the name is elided to something like 'dotnet_binaryfor...' and hovering shows no tooltip (empty description), leaving the user unable to determine the full pattern name from the UI.
- **Evidence:**
  ```python
  self._pattern_library_tree.setMaximumWidth(200)
  ...
  tooltip = pattern.description or ""
  p_item.setToolTip(0, tooltip)
  ```
- **Verified:** Code confirms every element of the claim. Line 155: `self._pattern_library_tree.setMaximumWidth(200)` hard-caps the tree widget's width inside a QSplitter (line 151, stretch factors 1:4 at line 223-224), so the library pane cannot grow beyond 200 logical px even if the user wants more room. The tree has two/three levels of nesting (root e.g. "HexPat Patterns" > category > leaf pattern, lines 698-709, or "Built-in" > category > template, lines 637-667), and QTreeWidget applies per-level indentation, further shrinking the usable text column for leaf items. QTreeWidgetItem does not word-wrap and Qt elides item text (ElideRight/Middle depending on style) when it exceeds the column width — there  …
- **Note:** Confirmed as reported. Root cause spans line 155 (setMaximumWidth cap) plus lines 665-666 and 705-707 (tooltip = description, not name, and can be empty). No mitigation (word-wrap, elide-mode override, resize-to-contents, or name-fallback tooltip) exists elsewhere in the file.

#### L12 — Signature results tree has no column resize mode and no tooltips for long Name/Details text
- **File:** `src/intellicrack/ui/panels/hex_editor/signatures.py:553`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** hex-transforms
- **Defect:** `_sig_results_tree` is configured with `setHeaderLabels(["Name", "Type", "Version", "Offset", "Details"])`, `setRootIsDecorated(False)`, `setAlternatingRowColors(True)` but no `setSectionResizeMode`/`resizeColumnsToContents` call, and populated rows (`_on_sig_scan_finished`, sections tab) never set per-item tooltips. The "Details" text is genuinely long and variable: for YARA matches it is built as `f"Namespace: {match.namespace}, Meta: {match.meta}, Tags: {match.tags}"` (signatures.py:173), which can be an arbitrarily long dict/list repr, and "Name" holds signature/rule names of unbounded length from user-supplied database files.
- **Failure:** A YARA scan match with a non-trivial `meta` dict or multiple `tags` produces a Details string long enough to exceed the visible side-panel width; with no tooltip and only the QTreeView default interactive column sizing, the user cannot see or recover the full match details without manually dragging the column wider (and the panel itself is narrow), and long signature Name values in a non-last column are similarly clipped at the default section width.
- **Evidence:**
  ```python
  self._sig_results_tree.setHeaderLabels(["Name", "Type", "Version", "Offset", "Details"])
          self._sig_results_tree.setRootIsDecorated(False)
          self._sig_results_tree.setAlternatingRowColors(True)
  ```
- **Verified:** Read signatures.py lines 520-561 and 684-733. The tree is built at line 552-557: `QTreeWidget()` with `setHeaderLabels([...])`, `setRootIsDecorated(False)`, `setAlternatingRowColors(True)`, and only an `itemDoubleClicked` connection — no `setSectionResizeMode`, no `resizeColumnsToContents`, and `header()` is never touched, so the header stays in Qt's default Interactive resize mode with a fixed default section width. Population happens in `_on_sig_scan_finished` (lines 684-704), which builds `QTreeWidgetItem([name, type, version, offset_hex, details])` from `match.get(...)` and never calls `item.setToolTip(...)` on any column — grepping the whole file for `setToolTip`/`resizeColumnToContents …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### L13 — Windows tab "Class Name" column has no resize mode and clips long/variable Win32 class names
- **File:** `src/intellicrack/ui/panels/process_panel/system_tab.py:249`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** process-system
- **Defect:** `_win_table` has columns `["HWND", "Title", "Class Name", "Visible"]`; only column 1 (Title) is set to `QHeaderView.ResizeMode.Stretch`. Column 2 (Class Name) is left at the default Interactive resize mode with the default ~100px section width, and `_refresh_windows()` never calls `resizeColumnsToContents()`. Win32 window class names are genuinely variable-length and frequently exceed that width (e.g. "ApplicationFrameWindow", "Chrome_WidgetWin_1", generated WinForms class names like "WindowsForms10.Window.8.app.0.141b42a_r9_ad1"), so they render clipped until the user manually drags the column border wider.
- **Failure:** User clicks 'Enumerate Windows' against a process hosting WinForms/Chrome/UWP-style windows; the Class Name column shows truncated text (e.g. "WindowsForms1...") with no tooltip or wrap, making the value unreadable without a manual column resize.
- **Evidence:**
  ```python
  self._win_table.setHorizontalHeaderLabels(["HWND", "Title", "Class Name", "Visible"])
          self._win_table.setSelectionBehavior(QTableWidget.SelectionBehavior.SelectRows)
          wh = self._win_table.horizontalHeader()
          if wh is not None:
              wh.setSectionResizeMode(1, QHeaderView.ResizeMode.Stretch)
  ```
- **Verified:** Confirmed by reading system_tab.py lines 244-251 and 1562-1595. The `_win_table` header labels are `["HWND", "Title", "Class Name", "Visible"]` (line 245) and only column 1 (Title) is set to `QHeaderView.ResizeMode.Stretch` (line 249: `wh.setSectionResizeMode(1, QHeaderView.ResizeMode.Stretch)`). Column 2 (Class Name) has no resize-mode call anywhere in the file, so it stays at Qt6's default Interactive mode with the default ~100px section width. In `_refresh_windows` (lines 1562-1595), the class-name cell is populated at line 1584 via `self._win_table.setItem(row, 2, QTableWidgetItem(str(typed_win.get("class_name", ""))))` with no `setToolTip()` call and no subsequent `resizeColumnsToConten …
- **Note:** Report is accurate as written; no correction needed to line, severity, or description.

#### L14 — Script list items elide user-entered names with no tooltip in a narrow (140-250px) panel
- **File:** `src/intellicrack/ui/panels/script_manager.py:419`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** chat-misc-panels
- **Defect:** ScriptListWidget._refresh_list builds each row's text as f"[{type_prefix}] {info['name']}" where info['name'] is an arbitrary user-entered script name (set via the free-text _name_edit field), and never calls item.setToolTip(...). The containing left_panel is capped at _LEFT_PANEL_MAX_WIDTH (250px) and the splitter's initial left size is only _SPLITTER_LEFT_SIZE (140px), so with a type-prefix tag like "[Ghidra] " prepended, moderately long names are elided by Qt's default list rendering with no way to read the full name without widening the splitter.
- **Failure:** User names a script "License Validation Bypass For XYZ Application v2"; the list shows "[Frida] License Validation B..." at the default 140px splitter width, and hovering the item shows no tooltip revealing the rest of the name, so the user must manually drag the splitter to disambiguate similarly-prefixed scripts.
- **Evidence:**
  ```python
  item = QListWidgetItem(f"[{type_prefix}] {info['name']}")
              item.setData(Qt.ItemDataRole.UserRole, script_id)
  ```
- **Verified:** Read script_manager.py lines 345-431, 560-649. ScriptListWidget._refresh_list (line 410-421) builds each item as `QListWidgetItem(f"[{type_prefix}] {info['name']}")` (line 419) and only sets `Qt.ItemDataRole.UserRole` data (line 420) — no `setToolTip` call anywhere on the list or its items. `info['name']` originates from `self._name_edit`, a free-text `QLineEdit` (line 590-592) with no length restriction. The containing `left_panel` is capped with `left_panel.setMaximumWidth(_LEFT_PANEL_MAX_WIDTH)` where `_LEFT_PANEL_MAX_WIDTH = 250` (line 579, constant at line 57), and the splitter is initialized with `splitter.setSizes([_SPLITTER_LEFT_SIZE, _SPLITTER_RIGHT_SIZE])` where `_SPLITTER_LEFT_SIZ …
- **Note:** Report is accurate as written; no correction needed to line/severity.

#### L15 — Module Info imports table splits width equally, clipping variable-length import names
- **File:** `src/intellicrack/ui/panels/x64dbg_advanced_tab.py:138`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** Same uniform `setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` call (no per-column mode) is applied to the 4-column imports/PE-directories table shared by `_apply_module_imports`/`_apply_pe_directories`. The "Name" column carries import names (including long undecorated C++ symbol names) but is stretched to the same width as the short "Ordinal"/"IAT RVA"/"IAT VA" hex columns, and no tooltip is set on the name cell to recover elided text.
- **Failure:** Fetching imports from a module with long/decorated import names (common with C++ DLLs) forces the "Name" column into only ~1/4 of the table width, truncating names that are the primary identifying data in that view, with no tooltip fallback.
- **Evidence:**
  ```python
  modinfo_h = self._modinfo_table.horizontalHeader()
          if modinfo_h is not None:
              modinfo_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)
  ```
- **Verified:** Read lines 47-48 (_MODINFO_IMPORT_COLUMNS = [\"Name\",\"Ordinal\",\"IAT RVA\",\"IAT VA\"], _MODINFO_PEDIR_COLUMNS = [\"Index\",\"Name\",\"RVA\",\"Size\"]) and lines 133-256. Line 138's `modinfo_h.setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` uses the no-index overload, which sets Stretch mode uniformly on all 4 header sections — Qt then divides the header width across all stretch sections (roughly equal, proportional to their size at the time stretch was applied), so the variable-length \"Name\" column (import names / undecorated C++ symbols, populated at line 184 via entry.get(\"undecoratedName\")) gets the same share as the short fixed-format hex columns (Ordinal/IAT RVA/IAT VA or  …
- **Note:** Report is accurate as written; no correction needed. Confirmed the table is reused for both imports and PE-directories views (both share the same 4-column Stretch header at line 136-138), and neither _apply_module_imports nor _apply_pe_directories sets per-item tooltips.

#### L16 — Status labels holding variable-length error text lack word-wrap
- **File:** `src/intellicrack/ui/panels/x64dbg_advanced_tab.py:669`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** x64dbg-advanced-stack
- **Defect:** `_bpcfg_status_label` (and identically `_script_status_label` at line 1189) is a plain `QLabel("")` added to a `QVBoxLayout` with no `setWordWrap(True)`. Its text is later set to `f"[-] {operation} failed: {exc}"` where `exc` is an arbitrary bridge/RPC exception message of unbounded length. Other panels in the same codebase (e.g. `ghidra_panel_extras.py:122`, `_status_label.setWordWrap(True)`) establish word-wrap as the convention for exactly this kind of status label.
- **Failure:** A bridge call fails with a verbose error (e.g. a long RPC/connection error string), and `_on_bpcfg_error`/`_on_script_error` sets that full string as the label text; without word-wrap the single-line label either forces the sub-tab wider than its container or gets visually cut off at the container edge, unlike the codebase's established status-label convention.
- **Evidence:**
  ```python
  self._bpcfg_status_label = QLabel("")
          layout.addWidget(self._bpcfg_status_label)
  ```
- **Verified:** Read src/intellicrack/ui/panels/x64dbg_advanced_tab.py lines 669-670 and 1189-1190: `self._bpcfg_status_label = QLabel("")` / `self._script_status_label = QLabel("")` are each added directly to a QVBoxLayout via `layout.addWidget(...)` with no `setWordWrap(True)` call anywhere in the file (grep for `bpcfg_status_label\.|script_status_label\.` shows only `.setText(...)` calls, never `.setWordWrap`). At line 783, `_on_bpcfg_error` sets `self._bpcfg_status_label.setText(f\"[-] {operation} failed: {exc}\")` where `exc` is an arbitrary exception object (bridge/RPC error) coerced to string — unbounded, unpredictable length. The identical pattern exists at line 1291 for `_script_status_label`. The  …
- **Note:** Report is accurate as written; both instances (line 669 _bpcfg_status_label and line 1189 _script_status_label) verified to lack setWordWrap(True) and to receive unbounded exception-message text via setText at lines 783 and 1291 respectively.

#### L17 — Module table's variable-length Path column is squeezed equally with short Name/Base/Size columns and has no tooltip
- **File:** `src/intellicrack/ui/panels/x64dbg_panel.py:2345`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** x64dbg-panel
- **Defect:** The module table's horizontal header has `setSectionResizeMode(QHeaderView.ResizeMode.Stretch)` applied uniformly to all 4 columns (Name, Base, Size, Path) with no per-column customization, `resizeColumnsToContents()`, or `ResizeToContents` mode. Base and Size are fixed-width hex strings (~10-18 chars) while Path holds a full on-disk module path that can be well over 60 characters; Stretch mode divides the available width roughly evenly across all Stretch columns rather than by content, so Path is typically squeezed into the smallest practical share and elided with no `setToolTip` to recover the full value on hover.
- **Failure:** User opens the Modules tab for a process with modules loaded from deep paths (e.g. `C:\Program Files\SomeVendor\LongApplicationName\bin\x64\dependency.dll`); the Path cell shows only a truncated prefix and hovering over it shows no tooltip, so the full path cannot be read without external tooling.
- **Evidence:**
  ```python
  self._module_table.setItem(row, 3, QTableWidgetItem(str(getattr(mod, "path", ""))))
  ```
- **Verified:** Read x64dbg_panel.py lines 344-354 and 65: _MODULE_COLUMNS = [\"Name\", \"Base\", \"Size\", \"Path\"] (line 65); the module table header gets a single uniform setSectionResizeMode(QHeaderView.ResizeMode.Stretch) applied to the whole header at line 353 with no per-column QHeaderView.setSectionResizeMode(col, mode) overrides, no resizeColumnsToContents(), and no ResizeToContents mode anywhere near this table. Line 2345 populates column 3 (Path) with the raw, potentially long path string via QTableWidgetItem(str(getattr(mod, \"path\", \"\"))), and a grep for setToolTip across the entire file returns zero matches, confirming no tooltip is ever attached to any module-table cell. Stretch mode divi …
- **Note:** Line number 2345 (where the Path cell is populated) is a reasonable anchor; the actual resize-mode defect is set up earlier at lines 351-353. Severity LOW is appropriate given this is a usability/readability issue, not a crash or data-loss defect.

#### L18 — Provider-list splitter panel is collapsible to zero width, hiding all provider-management controls
- **File:** `src/intellicrack/ui/provider_config.py:1451`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** provider-config
- **Defect:** `QSplitter` defaults to `childrenCollapsible == True`, and `setChildrenCollapsible(False)` is never called on `splitter`, nor is a `setMinimumWidth` applied to `left_panel` itself (only the inner `QListWidget` has `setMinimumWidth`/`setMaximumWidth`, which does not constrain splitter collapse behavior for the containing panel). Dragging the splitter handle fully left collapses `left_panel` — containing the provider list plus every action button (Set Active, Refresh, Reload Keys, Migrate, Create .env, Discover, OAuth Login, Revoke Token) — to zero width.
- **Failure:** User drags the splitter handle toward the left edge (or double-clicks it, which Qt uses as a collapse shortcut); the provider list and all left-side action buttons disappear entirely, and the only way to select a different provider is to widen the panel back out, which is not obvious without prior knowledge of Qt splitter collapse behavior.
- **Evidence:**
  ```python
  splitter.addWidget(left_panel)
  splitter.addWidget(self._settings_stack)
  splitter.setSizes([220, 580])
  ```
- **Verified:** Read lines 1376-1453 of provider_config.py. `left_panel` (QWidget containing the QListWidget provider list plus all left-side action buttons: Set Active, Refresh, Reload Keys, Migrate, Create .env, Discover, OAuth Login, Revoke Token) is added to `splitter` (a QSplitter, default `childrenCollapsible=True`) with no `setChildrenCollapsible(False)` call anywhere in the file (verified via grep for 'Collapsible' — zero matches) and no `setMinimumWidth`/size policy applied to `left_panel` itself. Only the inner `QListWidget` has `setMinimumWidth(_LIST_MIN_WIDTH)`/`setMaximumWidth(_LIST_MAX_WIDTH)` (lines 1387-1388), which constrains the list widget but not the splitter's collapse behavior for the  …
- **Note:** The claim's mention of "double-click, which Qt uses as a collapse shortcut" is not accurate — QSplitter does not bind double-click-to-collapse by default in Qt6/PyQt6; only edge-dragging collapses a collapsible child. This inaccuracy is minor and does not undermine the core, verified defect (drag-to-zero-width collapse due to missing setChildrenCollapsible(False) / no minimum width on left_panel).

#### L19 — ToolTab's internal splitter can be dragged fully collapsed, hiding the info panel
- **File:** `src/intellicrack/ui/tools.py:514`
- **Severity:** LOW  ·  **Category:** layout  ·  **Cluster:** tools
- **Defect:** `ToolTab._splitter` (vertical, code display over the Details/info panel) is created without `setChildrenCollapsible(False)`, so -- same as `main_splitter` -- the info panel can be dragged to 0 height and disappear, unlike sibling splitters elsewhere in the codebase that explicitly disable collapsibility.
- **Failure:** User drags the vertical splitter handle in a ToolTab (e.g. the Log tab) down past the info panel's `setMaximumHeight(150)` bound; the Details/info panel collapses to 0 height and is no longer reachable by normal resizing, only by hunting for the collapsed handle at the very bottom edge.
- **Evidence:**
  ```python
  self._splitter = QSplitter(Qt.Orientation.Vertical)
  ...
  self._splitter.addWidget(self._info_panel)
  self._splitter.setSizes([_CODE_SPLIT_LEFT, _CODE_SPLIT_RIGHT])
  ```
- **Verified:** Read src/intellicrack/ui/tools.py lines 507-543: `self._splitter = QSplitter(Qt.Orientation.Vertical)` (line 514) is constructed and never has `.setChildrenCollapsible(False)` called on it anywhere in the file, nor does `_info_panel` (a QFrame) get a `setMinimumHeight`. QSplitter's `childrenCollapsible` property defaults to True in PyQt6/Qt6, meaning a user can drag the handle until a child's size hits 0, regardless of `setMaximumHeight(150)` on `_info_panel` (line 520) — max-height only bounds the upper size, not the lower one. Grepping the codebase (`setChildrenCollapsible`) confirms the claim's comparison is accurate: sibling splitters in cutter_panel.py, app.py, ghidra_panel.py, cutter_s …
- **Note:** Report is accurate as written; no correction needed to line/severity/description.

---

## Appendix — findings per file

| File | Findings |
|------|----------|
| `src/intellicrack/ui/panels/sandbox_panel.py` | 7 |
| `src/intellicrack/ui/provider_config.py` | 7 |
| `src/intellicrack/ui/panels/x64dbg_panel.py` | 6 |
| `src/intellicrack/ui/tools.py` | 6 |
| `src/intellicrack/ui/panels/frida_instrumentation_tab.py` | 5 |
| `src/intellicrack/ui/app.py` | 4 |
| `src/intellicrack/ui/panels/frida_panel.py` | 4 |
| `src/intellicrack/ui/panels/ghidra_panel.py` | 4 |
| `src/intellicrack/ui/panels/hex_editor/pattern_editor.py` | 4 |
| `src/intellicrack/ui/panels/x64dbg_advanced_tab.py` | 4 |
| `src/intellicrack/ui/sandbox_config.py` | 4 |
| `src/intellicrack/ui/session_manager.py` | 4 |
| `src/intellicrack/ui/log_viewer/window.py` | 3 |
| `src/intellicrack/ui/panels/analysis_panel.py` | 3 |
| `src/intellicrack/ui/panels/hex_editor/panel.py` | 3 |
| `src/intellicrack/ui/panels/hex_editor_widget.py` | 3 |
| `src/intellicrack/ui/panels/process_panel/modules_tab.py` | 3 |
| `src/intellicrack/ui/tool_config.py` | 3 |
| `src/intellicrack/ui/highlighter.py` | 2 |
| `src/intellicrack/ui/panels/cutter_debugger_tab.py` | 2 |
| `src/intellicrack/ui/panels/cutter_panel.py` | 2 |
| `src/intellicrack/ui/panels/cutter_static_extra_tab.py` | 2 |
| `src/intellicrack/ui/panels/cutter_tabs.py` | 2 |
| `src/intellicrack/ui/panels/ghidra_panel_extras.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/data_inspector.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/highlighting.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/sections.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/signatures.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/statistics.py` | 2 |
| `src/intellicrack/ui/panels/hex_editor/va_mapping.py` | 2 |
| `src/intellicrack/ui/panels/process_panel/base.py` | 2 |
| `src/intellicrack/ui/panels/process_panel/system_tab.py` | 2 |
| `src/intellicrack/ui/panels/process_panel/threads_tab.py` | 2 |
| `src/intellicrack/ui/panels/script_manager.py` | 2 |
| `src/intellicrack/ui/panels/stack_viewer.py` | 2 |
| `src/intellicrack/ui/panels/vnc_widget.py` | 2 |
| `src/intellicrack/ui/chat.py` | 1 |
| `src/intellicrack/ui/log_viewer/_tail_reader.py` | 1 |
| `src/intellicrack/ui/panels/cutter_search_tab.py` | 1 |
| `src/intellicrack/ui/panels/graph_view.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/calculator.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/disassembly.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/export_report.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/hashing.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/patches.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/process_memory.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/scripting.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/search.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/transforms.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/widgets.py` | 1 |
| `src/intellicrack/ui/panels/hex_editor/yara.py` | 1 |
| `src/intellicrack/ui/panels/process_panel/process_tab.py` | 1 |
| `src/intellicrack/ui/resources/font_manager.py` | 1 |
| `src/intellicrack/ui/resources/theme_manager.py` | 1 |

